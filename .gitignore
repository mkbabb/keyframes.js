backend/config.php
.prettierrc
.vscode/
.DS_Store
node_modules/
my_venv/
.env
auth/
_*
dist/
.playwright-mcp/
.wrangler/
# proof:boundary writes a throwaway spring-only entry it removes on exit;
# ignore it in case a crashed run leaves one behind.
.proof-spring-entry.*
.agents/
skills-lock.json
.claude/
# J.W5.S6 (LS-12) — root-level PNGs are stray diagnostics (the I.B2
# b2-gen-crash shot accreted here by mistake; its home is
# docs/tranches/I/audit/investigate/shots/). Tracked screenshots live under
# docs/**/shots/ or assets/; a future root-level diagnostic cannot re-accrete.
/*.png

# workflow-orchestration artifacts (not source)
.wf-fix.js
**/.wf-fix.js
