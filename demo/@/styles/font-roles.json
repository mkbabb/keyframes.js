{
    "_doc": "T.D1 — the role → tuple manifest (the committed artifact proof:font-census's clause (e) reads). Each role binds a SELECTOR CONTRACT to the tuple it must resolve live: voice ∈ {display, body, mono} (family axis), computed weight, size bounds, style. The VALUES consume the OD-6 owner token (APPROVED 2026-07-05 \"Good.\", the P-THEME blessed reference worktree-wf_1e744f4d-2bb-3): Instrument Serif display at its TRUE 400 ≥28px, Plus Jakarta Sans body/chrome, Fira Code confined to code/data. `monoAllowedSelectors` is the T.D4 data-register contract — a Fira-Code leaf must match it or the census reds (mono is DATA, never the UI voice); `monoCeiling` is the declared non-editor mono leaf ceiling (desktop sweep, regression-proof).",
    "roles": [
        {
            "role": "dock-label",
            "selector": ".dock-label",
            "expect": { "voice": "body" },
            "note": "the dock band rides glass-ui's own Jakarta dock-label register (T.D3; the K.W2 serif force is deleted — VERDICT #24 + T.C 'kill the serif chrome flip')"
        },
        {
            "role": "transport-button",
            "selector": ".btn-playback",
            "expect": { "voice": "body", "weight": 500 },
            "note": "Play/Pause + the transport band: CONTROLS ride the body register at medium (T.D3; the K.W2 display bind died)"
        },
        {
            "role": "filing-tab",
            "selector": ".tab-trigger-base[data-state='inactive']",
            "expect": { "voice": "body", "weight": 500 },
            "note": "inactive filing tabs: body medium; activation steps to 600 (a REAL Jakarta weight) via the active rule"
        },
        {
            "role": "filing-tab-active",
            "selector": ".tab-trigger-base[data-state='active']",
            "expect": { "voice": "body", "weight": 600 },
            "note": "active filing tab: Jakarta semibold — an honest shipped weight, never synthesis"
        },
        {
            "role": "pill-tab",
            "selector": ".kf-pill-tab",
            "expect": { "voice": "body" },
            "note": "KfPillTabs pills are controls → body register (the component itself is a T.H gated-on-publish excision; the register holds until then)"
        },
        {
            "role": "hero-display",
            "selector": "h1.hero-display",
            "expect": { "voice": "display", "weight": 400 },
            "note": "the home hero: Instrument Serif at its one TRUE weight (T.D2 honest ink; the seat/composition is OD-4's, batch ⑤)"
        }
    ],
    "monoAllowedSelectors": [],
    "monoCeiling": null
}
