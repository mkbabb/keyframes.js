# Tranche S — prompt-recap · THE CONSOLIDATED OWNER-ASK SUBSTRATE for S.Z1 (the "ensure they've been addressed" audit)

**Lane:** `prompt-recap` (S audit, pass1 · DOCS-ONLY — this lane PROPOSES/consolidates; it
never edits source, never runs a gate, and makes no git change). **Branch:** `tranche-s-dev`.
**Role:** this file is the **development-phase recap substrate** — the consolidated,
fully-dispositioned corpus that wave **S.Z1** materializes into the gate's fixed parse target
`docs/tranches/S/PROMPT-RECAP.md` at the impl drive (SPEC §5.1, §3 S.Z1). It is audit evidence;
S.Z1 is what executes.

**Sources (transcribed with zero load-bearing loss):**
- `docs/tranches/S/audit/pass1/research/r1-session-history.md` — the mined 120 MB transcript
  corpus (287 owner messages, ~114 distinct authored prompts) + the A→Q recap chain + the
  preliminary ADDRESSED map (r1 §1–§6).
- `SPEC-v3.md §5` (the prompt-recap plan + its five principles) and `§3 S.Z1` (the rewritten
  gate shape) — the CONVERGED source of truth (Pass-2, 100% / 11-of-11 critics).

---

## The top note — S.Z1's close gate re-verifies THIS table's cited oracles from a clean run

**⚠ Binding (SPEC §S.Z1, §5.2, §5.3).** This substrate is not a chain-trusted document. At S
close, wave **S.Z1's** born-RED `proof:prompt-recap-s` re-verifies every oracle this table cites:

- **(ii)** every `ADDRESSED` row must name an oracle that **resolves to a real gate or
  live-observation artifact AND matches a green exit in the S.Z3 from-clean closeable-roster
  run** — a cited oracle that is absent or red **REDs the row** (row-shape alone can no longer
  green the gate) (SPEC §3 S.Z1 clause ii).
- **(i)** any row without a terminal disposition (C-20's structural definition) **REDs** (SPEC
  §3 S.Z1 clause i).
- **(iii)** any enumerated precept lacking a band mapping **REDs** (SPEC §3 S.Z1 clause iii).
- **Non-vacuity:** a planted un-dispositioned row AND a planted row citing a red oracle must
  each RED (SPEC §3 S.Z1).
- **Symmetric re-measure:** the ledger re-points the apparatus/device rows from r1's
  p12-refuted device-dependence framing to S.A0/S.A2's fix-by-cause dispositions, and
  **re-measures r1's own ADDRESSED-map** rather than chain-trusting it (SPEC §3 S.Z1, §5.2).
- **Cross-repo rows** (S.H, value.js dispatches) are dispositioned via the **kf-side CONSUME
  gate** — a sibling's internal gate is never cited as the oracle, and the ledger says so (SPEC
  §3 S.Z1).

Consequently every `ADDRESSED` disposition below carries its **oracle** (gate name or
live-observation artifact) so S.Z1 can resolve it from a clean run; every deferral names a
terminal home (S-wave / ratified KILL / HANDOFF / Recorded-future §8 / owner-ruling §6.3).
**Zero rows are un-dispositioned** (the gate REDs otherwise).

---

## 1. The standing mandate — the immutable spine (the recurring precept block)

First captured at `J/J.md:111-119`; carried VERBATIM into K, L, M, O, P, Q, R; re-issued
word-for-word in the July-2 S kickoff (r1 §1, r1:60-71). This is the owner's canonical
instruction and the anchor the whole recap hangs from:

> DEEPLY audit (32 agents in parallel) our original plan and waves thereof … recapitulate our
> original prompts, plans, and precepts: **NO quick solutions, NO workarounds: idiomatic,
> gestalt approaches.** … This is a development product, **architectural transpositions in the
> sake of elegance, simplicity, and performance above all are both necessary and desirable. NO
> legacy code.** Delineate any chronically deferred items and fold them … **Recap ALL of our
> prompts and requests hitherto and ensure they've been addressed. This is NOT an implementation
> phase.**

Plus the recurring **design directive** (`J/J.md:123-130`) and its S extension: *"all design
must be routed using Fable and the frontend design plugin … Each frontend page … analyzed using
the frontend design plugin with a Fable instance."* Plus the recurring **orchestration
directive** (the 5-step convergence loop; core-model orchestrates, Opus/Sonnet fan out; batches
of three) (r1 §1).

---

## 2. Precept recap — the load-bearing precepts, each mapped to its S enforcement

Every enumerated precept must carry a band mapping or S.Z1 clause (iii) REDs. Frequencies are
r1's per-message counts over the 287-message corpus (r1 §2); the S enforcement column is what
roots the *spirit*, not just the letter (SPEC §5.5 — the spirit column is the point).

| Precept / demand | Msgs | Disposition — S enforcement (oracle / band) |
|---|---:|---|
| **maximal parallelism / worktrees** | 124 | ADDRESSED (method) — S dev-drive runs 2–3 worktrees + delegated agents (R lesson carried, r1 §Finding 10); orchestration precept in SPEC §7 |
| **in totality / do not relinquish** | 93 | ADDRESSED (process) — S.Z close discipline; S.Z2 re-executes every CLOSED wave's oracle; S.Z3 re-gates master-green on the FINAL SHA (SPEC §3 S.Z2/Z3) |
| **no exception(s)** | 94 | ADDRESSED — the standing recovery cron (J/J.md); worked J/Q/R autonomously (r1 §Finding 10) |
| **orchestrate as team lead / Opus-Sonnet fanout** | 88 | ADDRESSED (method) — S kickoff item 11/12; SPEC §7 orchestration mandate |
| **recap ALL prompts** | 82 | **THIS LANE → S.Z1** — `docs/tranches/S/PROMPT-RECAP.md` + `proof:prompt-recap-s` (SPEC §3 S.Z1) |
| **idiomatic** | 76 | → **BAND S.B** — cohesion-first sub-zoning (SPEC §3 S.B); charter §1 "No quick solutions" (transposition over patch) |
| **NO workarounds / NO quick solutions** | 73 / 65 | → **S.A2** (harness net-deletion makes the deploy route-around unnecessary) + charter §1; live-hit sweep → **S.C** (r1 §Finding 8) |
| **gestalt** | 71 | → **BAND S.D** (demo gestalt) + **S.B** (library gestalt) |
| **chronic / deferral folding** | 67 / 52 | → **§4 fold table** (SPEC §4 — no un-dispositioned rows; "terminal" = C-20 structural) |
| **design (routed via Fable/plugin)** | 64 | → **BAND S.G** (+ E3/E5) — mandatory-Fable routing (SPEC §3 S.G) |
| **wave-shaped planning** | 77 | ADDRESSED — the S band/wave set (SPEC §3); "develop out that exact tranche plan" at 100% convergence |
| **harden** | 45 | ADDRESSED — every S wave carries a falsifiable born-RED (or born-SPECIFIED) gate (SPEC §1, §3) |
| **NO legacy / deprecated** | 43 | → **BAND S.C** (legacy purge with teeth; born-RED gates, not rubrics — SPEC §3 S.C) |
| **deploy authorization (Cloudflare/AWS)** | 166 | ADDRESSED — keyframes.babb.dev live on CF Pages; auto-deploy-of-record revival → **S.A3** (DM-20) |
| **SOTA (research the frontier)** | 21 | → **BAND S.F** (animation) + **BAND S.H** (parse-that) — SPEC §3 S.F/S.H; value.js SOTA lane recorded as owner-ruling §6.3 (not added speculatively — §8-17) |
| **32-agent deep audit** | 29 | ADDRESSED — the pass1 32-lane audit (`audit32/`) executed this precept |
| **first-principles / from-scratch** | 6 | → **BAND S.E** — scene-stage resurrected first-principles, one stage at a time (the owner's own prescription, SPEC §3 S.E) |

---

## 3. THE FULL RECAP TABLE — every distinct owner ask/precept, era-tagged, classified, dispositioned

The A→Q lineage is exhaustively dispositioned in `R/audit/retro-prompt-recap.md` §2 (rows
A1–Q4) and is the authoritative chain; what follows is the **consolidated** corpus (r1 §3 +
r1 §6 ADDRESSED map) with a terminal disposition on **every** row. Class ∈ {directive, precept,
correction, feedback, question}. Disposition legend: **ADDRESSED-in-<tranche>** (with the oracle
S.Z1 resolves) · **→ S.<wave>** (S-scope open) · **§8-#** (Recorded-future) · **§6.3** (owner
ruling) · **KILL/HANDOFF** (ratified terminal).

| Era | Distinct ask (verbatim / tight paraphrase) | Class | Disposition — oracle / home (SPEC §ref) |
|---|---|---|---|
| A | "Execute tranche A in full … Publish 3.0.0 first. Gate on green CI." | directive | ADDRESSED-in-A — npm 3.0.0 published; the green-CI clause is re-armed at close by **S.Z3** master-green precondition (SPEC §3 S.Z3) |
| D | "Read CONSTELLATION.md + tranche docs. inv-16: write only your own repo. Orchestrate waves, parallel agents. No workarounds, idiomatic, no legacy." | directive+precept | ADDRESSED (recurring precept, HELD) — inv-16 honored (S is DEV-ONLY, no source writes; SPEC §1 "What S is NOT") |
| D | "audit the dock implementations (value.js, kf, fourier); refine glass-ui dock-forward waves. 6 agents parallel." | directive | ADDRESSED (outward) — dock lives in glass-ui (USER-DOMAIN); glass-ui 5.0.0 forthcoming → **S.E6** consume-edge (SPEC §1, §3 S.E6) |
| D | "re-name our constellation dock usage; fold all items into base glass-ui and leverage THAT." | directive | ADDRESSED — dock folded into glass-ui base; oracle = glass-ui repo (USER-DOMAIN; consume-only) |
| D | "FULL multi-repo specification spanning keyframes, dock, etc. Drive all to completion." | directive | ADDRESSED — the constellation (parse-that/value.js/kf) shipped + deployed (MEMORY "Constellation IMPL drive SHIPPED") |
| E | "Audit tranches; keyframes & CSS parsing FULLY modern, SOTA, up-to-spec; 32 agents; animation SOTA." | directive | ADDRESSED (perf) — SoA compositor 2.5×, 16.6×@K=8 (benches present); **re-research re-opened → BAND S.F** (SPEC §3 S.F) |
| E | "NO quick solutions, NO workarounds: idiomatic, gestalt … architectural transpositions … NO legacy code." | precept | ADDRESSED (recurring) — see §2 precept recap; spirit-gap → **BAND S.B** + **S.C** |
| F | "What of parse-that, value.js's parsing — how best SOTA? Deploy 6 agents." | question+directive | ADDRESSED (parse-that CSS parser deleted, Constellation A) + **"uplift parse-that" re-opened → BAND S.H** (SPEC §3 S.H) |
| G | "No file: syntax — glass-ui imported properly. Perfect CI. Merge all feature branches into master — make a backup." | correction+directive | ADDRESSED (no `file:` per manifest/CI, MEMORY registry-consumption); the "perfect CI" clause is the S keystone → **S.A0** (master red since K; fix-by-cause) + **S.Z3** (SPEC §3 S.A0/Z3) |
| G | "glass-ui begins AW; complete G in totality with cognizance; widen value.js dep to admit ^0.11.0." | directive | ADDRESSED-in-G — dep widened; pin discipline preserved → **S.E6** tilde-pin (never caret) (SPEC §1) |
| H | "I don't want icons re-created. I want them re-instantiated. Only new icons for those lacking them; if SVG, 1-1." | correction | ADDRESSED-in-H — icon re-instantiation landed (demo; oracle = demo asset set) |
| H | "within easing, just the dropdown — no text input; duration slider full card width; remove the 'value' label." | feedback | ADDRESSED-in-H (demo controls); easing-sidebar refinement continues → **S.G2** (easing-sidebar-minimal backlog row, SPEC §4 row 5) |
| H | "The broken demo is fine. We'll fix with the next tranche. This is a development product." | feedback | ADDRESSED (posture accepted) — S is the tranche that makes the instruments honest (SPEC §1 charter) |
| H | "the demo name should just be 'keyframes.js'." | correction | ADDRESSED-in-H — demo title = "keyframes.js" |
| I | "DEEPLY audit with 32 agents … recapitulate our original prompts, plans, and precepts." | directive+precept | ADDRESSED (recurring) — the 32-lane pass1 audit + **this recap → S.Z1** |
| I | "Performance/animation quality — are we truly SOTA? Lighthouse audit?" | question | ADDRESSED (benches) + Lighthouse floors DM-8 → **S.A1** (C-20 terminal: relative budgets or ratified KILL — SPEC §4 row 6); frontier → **BAND S.F** |
| I | "Divine improvements to push the frontier further, folded in from informed research." | directive | ADDRESSED (recurring) — **BAND S.F** SOTA uplift (VT/SplitText/@starting-style/animation-trigger, SPEC §1, §3 S.F) |
| J | The 7-clause mandate (first canonical capture) + "deploy anything and everything via Cloudflare/AWS CLI; defer to Opus/Sonnet for fanout." | directive+precept | ADDRESSED (process, recurring) — every tranche A→R ran the audit/recap/fold/dev-only cycle; deploy auth exercised (CF Pages) |
| J | "if we hit a usage limit … return with: Continue. Re-deploy all workflows and agents — no exceptions." (standing cron) | directive | ADDRESSED — recovery cron worked J/Q/R autonomously (r1 §Finding 10; carried as an S orchestration precept) |
| K | "Explicate what deferred L is to be — fold into K." | directive | ADDRESSED-in-K — L scope explicated + partially folded (MEMORY Tranche K) |
| K | "Plan full value.js execution and orchestration. Not just asks." | directive | ADDRESSED — value.js tranches O/P executed + published (1.0.0→1.2.0) |
| K | "Ratify all three and properly harden … batches of 3 in parallel … Fully develop, NO workarounds." | directive+precept | ADDRESSED-in-K — three ratified + hardened; batches-of-3 is a standing S method (§2) |
| K | "Dock totally broken; controls UI far too thin — ~25% wider; glass card = subtle border around the two controls." | correction+feedback | ADDRESSED-in-K (demo controls widened) + dock → glass-ui (USER-DOMAIN); dock-doubleclick is a glass-ui-root chronic (MEMORY project_dock_doubleclick) |
| L | "recap research, then deploy a frontend design fleet (opus + frontend design plugin) on every page of demo and value.js." | directive (design-routing) | ADDRESSED-in-L + **re-issued/sharpened → BAND S.G** (now mandatory-Fable routing, SPEC §3 S.G; S kickoff item 10) |
| L | "crayon primaries, folded with proportion (not yet); design language **refined, not abrogated** — glass, paper, audacious type/math; breathing sRGB gamut egg." | feedback | ADDRESSED (TASTE, USER-DOMAIN) — L.W11 refined; **BAND S.G** refinement (conservative, isomorphic-except-named-deltas per r1 §Finding 5); ten easter eggs → **§8-1** (Recorded-future, de-scoped x2) |
| M | "proof:hygiene running 3 hours — **preposterous** — why so slow? why not proper `tests/`? what are these proof: scripts?" | correction+question | **PARTIAL** (M.W1 run-all shipped the speed half) → **S.A2** (harness net-deletion) + **S.A4** (one-page tier legend; ~185 proof:* legibility) (SPEC §3 S.A2/A4; r1 §Finding 7) |
| M | "Did all the UI changes land — new lighting, hierarchy, glass-ui primitives? Harden and validate." | question | ADDRESSED-in-M — UI changes validated; design continuity re-audited → **BAND S.G** |
| N | "a downlighting DK64-barrel stage animation switcher — 15° tilt, circular carousel, LIVE per-scene previews, liquid-glass transition, glassy arrows, spin-to-front, per-scene idle, hover-brightens-stage. Design and prototype now, with a fully-specified wave set." | directive (scene-switcher) | **REVIVED → BAND S.E** (DM-24; owner-reopened, salvage probe p05; SPEC §4 row 17, §3 S.E) — the oldest unfulfilled first-class ask |
| N | "The prototype is completely **awful**. Not 3d. LIVE previews. angled 15-30°. grid not pure black. carousel janky/blurry." | correction | → **BAND S.E** (execution-quality reqs folded into the E charter: LIVE previews, correct tilt; SPEC §3 S.E) |
| N | "**still awful** … cube view: zoom out, docks fade, bottom dock houses arrows … LOD rendering … carousel pointed downward 15° (right now inverted). Begin again with a fleet. Proper glass-ui components." | correction | → **BAND S.E** (the `rotateX(-15°)` back-higher tilt — the `+deg` inversion was the bug; LOD/content-visibility; SPEC §3 S.E) |
| N | "not only slow but **totally and unmeasurably wrong. Approach from first principles** … one stage at a time … atomic parts … chrome dev tools mcp … dogfood our own libraries." | correction | → **BAND S.E** — the owner's own prescription IS the E method (first-principles, one choreographed stage at a time, chrome-devtools-mcp verified, dogfood kf's engine; SPEC §3 S.E) |
| N | "**shelf this idea and revert the stage selector (keep the spec). Going nowhere.** Focus on demo perf/design, library perf/design, locksteps with value.js/fourier, glass-ui BC." | correction (SHELVE) | The SHELVE (spec preserved, `docs/tranches/N/STAGE-SPEC.md`) is precisely why DM-24 is **OPEN → REVIVED BAND S.E**; the focus items landed across O→R |
| M/N | "**Stop stopping.** Do we not already have a wave set? What of the latest tranche." | correction | ADDRESSED (orchestration discipline) — carried as the S "in totality / do not relinquish" precept (§2) |
| O | "We own value.js — this tranche resolves it … optimize kf, value.js, parse-that, fourier." | directive | ADDRESSED (constellation O/P/Q) + **BAND S.F/S.H** re-open the kf+parse-that uplift |
| O | "parse-that = parsing **primitives**; value.js implements the combinator/scanner hybrid over them. **Remove the full CSS parser from parse-that.** Support 2026+ CSS." | directive | ADDRESSED (parse-that CSS parser deleted — Constellation A) + **BAND S.H** uplift (SPEC §3 S.H); 2026+ grammar landed (`resolve/`) + **BAND S.F** |
| O | "Moderate. Not a massive re-write. Small changes. Experimental CSS spec items; full function/timeline/scroll support." | feedback (scope-limit) | ADDRESSED (`resolve/` emerging-CSS resolver present; `if()`/`@function`) — element-dependent arm verified in **BAND S.F** (SPEC §6.1 Q9/S.F1) |
| P | "harden value.js/parse-that/kf in a triumvirate; aggressively optimize; improve demo usability/clarity." | directive | ADDRESSED (Tranche P/Q/R) — SoA compositor, emerging-CSS resolve; demo usability continues → **BAND S.D/S.G** |
| P | "prototype and validate their worth **now** … test the SoA re-write rather than abrogating it … `if()`/`@function`/`spring()` SHOULD be in our grammar." | directive+precept | ADDRESSED — SoA validated (not abrogated); emerging-CSS grammar in engine (`resolve/`); frontier uplift → **BAND S.F** |
| P | "You do these **NOW** — prototype NOW, research NOW. This informs tranche development." | directive | ADDRESSED (method) — the pass1 prototype fleet (`prototypes/`) + 12 probes executed this precept |
| P | "bbnf-lang is in prototype dev; **not to be used for any features.** What is our parse-that plan?" | question+constraint | ADDRESSED + **re-issued S kickoff item 4** — bbnf-lang HARD-EXCLUDED (SPEC §1 "No bbnf-lang"); Pratt combinator → **§8-2** (design seed, no wave, no gate) |
| P | "Analyze the last 10 tranches for **contrived** items and reformulate. Be **conservative and fastidious.**" | directive | ADDRESSED (Q no-deferral terminal tranche); conservative-vs-bold tension resolved per-wave (r1 §Finding 5; SPEC applies transposition for structure, refinement for design) |
| Q | "Harden every item in each tranche set — loop; full tranche hardening/authoring/pruning/analysis." | directive | ADDRESSED — Tranche Q shipped in totality (5.0.0, MEMORY Q IMPL DRIVE SHIPPED) |
| Q | "5.0 is fine. **Remove `animate()` in favor of our more idiomatic solutions.**" | directive (API cut) | ADDRESSED — `animate()` excised R.W4 (`./engine` 39-key mirror carries no `animate`); the zombie `animate.ts` cluster residue → **S.C1** DELETE (SPEC §4 row 23) |
| Q/R | "**Overfit nonsense. Remove completely and revoke from NPM.**" (keyframes-vue) | correction (KILL) | ADDRESSED (KILL, R.W0 `23a6867`) — **S.Z1 re-verify oracle: `npm view @mkbabb/keyframes-vue` → 404 AND repo-grep zero** (SPEC §3 S.Z1; r1:375) |
| R | "Begin/continue the tranche; adhere to the plan (agent orchestration, deep parallelization); indefatigably; IN TOTALITY; NO workarounds; deploy via Cloudflare/AWS; cron on session-limit." | directive+precept | ADDRESSED — Tranche R shipped (5.1.0) + deployed (MEMORY R IMPL DRIVE SHIPPED); orchestration precepts carried into S (§2) |
| R | "What remains? What's red?" | question | ADDRESSED → this S audit **answers it**: master CI red since K → **S.A0** fix-by-cause (2 source + 5 born-RED + 1 shared harness bug + 1 staleness + LoAF decouple; SPEC §1 keystone, §2.1-2) |
| — | Terse recurring corrections: "Another workflow, no." · "Nonsense. Fully re-deploy and harden." · "Continue indefatigably." | correction | ADDRESSED (orchestration discipline) — folded into the S method precepts (§2) |
| — | "Deploy. **Pushing to master should always deploy, no?**" | correction | → **S.A2/S.A3** — auto-deploy-of-record revived (DM-20 dead; fires when demo-correctness green, SPEC §4 row 16, DAG S.A2→S.A3) |
| S | **The full kickoff** — 12 elements | directive+precept | **See §4 below** — each element mapped to its band (SPEC §5, r1:333-356) |

### 3.1 The ADDRESSED-map rows re-measured (r1 §6, symmetric re-verification per SPEC §5.2)

r1's own ADDRESSED-map is **re-measured, not inherited** (its device-dependence framing was
p12-refuted). The rows that changed disposition under re-measure:

| r1 ADDRESSED-map row | r1 status | **Re-measured S disposition (oracle)** |
|---|---|---|
| Architectural transposition / NO god modules | PARTIAL | → **BAND S.B** (cohesion-first sub-zoning: `compile/backward/`, `engine/css/`) + gate reform **S.B5** (empty override, ≤460L, no re-export bridge) + **S.A4** tier reform — the twelve-tranche DF-11 chronic rooted in *spirit* (SPEC §3 S.B, §5.1) |
| Apparatus: "why so slow / not in tests/ / what are proof scripts" | PARTIAL | → **S.A2** (harness net-deletion) + **S.A4** (tier legend) — the speed half shipped (M.W1); legibility half is S (SPEC §3 S.A2/A4) |
| Device-dependent demo gate greened | PARTIAL/chronic | **RE-POINTED (p12): not device-dependence.** → **S.A0** fix-by-cause + **S.A2** four-bucket disposition; render-races = 0 (SPEC §1 keystone, §2.1-2; the "device-dependence plane" narrative pruned → §8-18) |
| Scene-switcher (N-Stage DK64 carousel) | OPEN (shelved w/ spec) | → **BAND S.E** (DM-24 REVIVED; SPEC §4 row 17, §3 S.E) |
| Broken Q.WC3 phone scroll-snap carousel | OPEN (removal planned) | → **S.E** (DELETE the broken duplicate; one switching authority; SPEC §3 S.E, r1 §Finding 6) |
| demo/app colocation; demo/playground identity | OPEN | → **S.D1** (app partition, ~38 files) + **S.D3** (playground fold; SPEC §3 S.D, §6.1 Q4/Q6) |
| SOTA parsing / parse-that as primitives | PARTIAL | → **BAND S.H** (parse-that 1.0.0 uplift, born-SPECIFIED H4; SPEC §3 S.H) |

**Every other r1 §6 row remains ADDRESSED** with its oracle unchanged (7-clause process,
NO-workaround, NO-legacy residue-is-comments, `animate()` cut, keyframes-vue KILL,
reduced-motion, deploy-auth, orchestration-method, dev-only). Zero rows are DROPPED (r1 §6 close;
re-affirmed here).

---

## 4. THE CURRENT S DIRECTIVE — each element mapped to its band (SPEC §5, r1 §5)

The S kickoff (2026-07-02) re-issues the 7-clause standing mandate and adds the enumerated
asks below. The gate's cited precept set is **r1 §5's twelve kickoff asks (r1:333-356) + the
7-clause standing mandate (r1:60-71)** — v1's unsourced "the nine in the kickoff" is pruned
(SPEC §3 S.Z1, §9 SZ-2). Elements **1–10 are the substantive asks**; **11–12 are the
orchestration method** (re-issued, governing the DEV process itself). Every element carries a
band mapping or S.Z1 clause (iii) REDs.

| # | S kickoff element (r1 §5) | Band / disposition (SPEC §ref) |
|---|---|---|
| 1 | **Deeper library sub-zoning** — "within compile/, why not a backward/ module, and an easing module; within engine, why not a css/ module." | **BAND S.B** — S.B1 (constants split, 10 light repoints), **S.B2 (`engine/css/`; owns the animation.ts/playback.ts ceiling carves)**, S.B3, and the gate reform **S.B5** (empty override + max-file ≤460L + no re-export bridge) + **S.A4** tier taxonomy (SPEC §3 S.B, §6.1 Q1) |
| 2 | **demo/app is "a mess"; "what even is demo/playground"** — deeper feature colocation. | **BAND S.D** — S.D1 (app partition: scene-machine/, raf/, cube/ homes; ~38 files/~60 lines, FROZEN-set immune) + **S.D3** (playground identity/fold, six-item touch set) (SPEC §3 S.D, §6.1 Q4/Q6) |
| 3 | **SOTA re-research in parsing AND animation**, applied to **uplift parse-that too**. | **BAND S.F** (animation: VT/SplitText/@starting-style/animation-trigger, all round-trip to zero-runtime CSS) + **BAND S.H** (parse-that primitives, 2026+ CSS, 1.0.0 uplift). "SOTA both repos" reading → **owner-ruling §6.3** (two-repo default; no speculative value.js band, §8-17) (SPEC §3 S.F/S.H, §6.3) |
| 4 | **bbnf-lang is in active development — NOT to be considered yet.** | **Hard constraint** (charter SPEC §1 "No bbnf-lang" — combinator-tier only; grammar-DSL is a separate session). Pratt binding-power combinator → **§8-2** Recorded-future (design seed; no wave, no gate) (SPEC §1, §8-2) |
| 5 | **glass-ui 5.0.0 forthcoming (BG/BH tranches)** — consume-published-only. | **S.E6** — the external consume-edge (third-party); FIRES only on joint 5.0.0 publish; tilde-pin (never caret). Else structured HANDOFF + **fold rows 51/52/53 RESIDUAL CARRY** (explicit non-terminal) (SPEC §1, §3 S.E6, §4) |
| 6 | **Resurrect the shelved scene-switcher** — "develop an entire tranche set, based on the shelved scene switcher from many tranches/sessions ago." | **BAND S.E** (multi-wave: S.E1 8-scene core → E2 → E3 → E4 → E5 → E6; DM-24 REVIVED; salvage probe p05; DELETE the broken Q.WC3 duplicate; built to not repeat N's failure loop) (SPEC §3 S.E, §4 row 17) |
| 7 | **NO legacy code** (re-issued). | **BAND S.C** — S.C1 (zombie `animate.ts` cluster DELETE + MIGRATION-5.1.0 backfill), C2/C3/C4 (shadcn islands, dead devDeps, era-narration) — born-RED gates, not rubrics; the 5 `workaround`/3 `HACK` live hits swept (SPEC §3 S.C, §4 rows 23/24; r1 §Finding 8) |
| 8 | **Fold ALL chronic + open deferrals.** | **§4 fold table** (SPEC §4 — every open-at-R-close row → a wave / ratified KILL / HANDOFF / §8; "terminal" = C-20 structural: a deterministic re-shaped gate or a ratified KILL with a re-run witness — never "observe-in-CI"). DQ-4/5/6/7 assumed FOLD-LANDED in Q → **S.Z1 re-verifies each** (SPEC §4 row 72) |
| 9 | **Full prompt recap — "ensure they've been addressed."** | **BAND S.Z → S.Z1** — this substrate materializes as `docs/tranches/S/PROMPT-RECAP.md`; born-RED `proof:prompt-recap-s`; mid-S asks → `docs/tranches/S/OWNER-ASKS.md` (fully-dispositioned) (SPEC §3 S.Z1, §5) |
| 10 | **Design routing is now mandatory-Fable** — "all design must be routed using Fable and the frontend design plugin … each frontend page analyzed using the frontend design plugin with a Fable instance." | **BAND S.G** (design refinement fleet; + E3/E5 for the scene-stage motion design) — Fable-routed per page (SPEC §3 S.G) |
| 11 | **The explicit 5-step convergence loop** (research → synthesize → prototype → critique[return %convergence] → synthesize; loop to 100%, then develop the exact wave set). | **Orchestration method** — governed THIS pass1 (converged 100% / 11-of-11 critics, empty blocking); SPEC §7 orchestration mandate |
| 12 | **Batches of three agents in parallel** (rate-limit discipline, re-issued). | **Orchestration method** — the standing fanout discipline; core-model orchestrates, Opus/Sonnet fan out (SPEC §7; §2 precept recap) |

---

## 5. The three mandated re-verifications — bound to concrete oracles (SPEC §3 S.Z1)

S.Z1 does not chain-trust the two R reversals or the decomposition-spirit gap; each is bound to
a concrete oracle S.Z1 resolves from a clean run:

| Re-verification | Bound oracle (S.Z1 resolves at close) |
|---|---|
| **keyframes-vue KILL** (Q/R "overfit — revoke from NPM") | `npm view @mkbabb/keyframes-vue` returns **404** AND **repo-grep zero** (r1:375; SPEC §3 S.Z1) |
| **lint-tier retraction** (R.W0 staged `.dependency-cruiser.cjs` for deletion — r1 §Finding 7) | dep-cruiser **baseline `[]`** + **the enforcer file exists** (SPEC §3 S.Z1) |
| **decomposition-spirit gap** (DF-11, twelve tranches; carve-to-ceiling not carve-to-cohesion) | **S.B5's exit codes:** empty-override map + max-file **≤460L** + no-re-export-bridge (SPEC §3 S.Z1, §3 S.B5) |

---

## 6. Recap-integrity clauses (SPEC §3 S.Z1, §5.2/§5.4)

- **Symmetric re-measure.** The ledger re-points the apparatus/device rows from r1's
  p12-refuted device-dependence framing to **S.A0/S.A2's fix-by-cause dispositions**, and
  re-measures r1's own ADDRESSED-map (§3.1 above) — the recap must not chain-trust its own
  input's superseded narrative.
- **Cross-repo rows** (S.H parse-that dispatch; value.js dispatches) are dispositioned via the
  **kf-side CONSUME gate**, and the ledger says so — a sibling's internal gate is never cited as
  the oracle (SPEC §3 S.Z1).
- **Mid-S ask capture (concrete mechanism, sz-B7).** Owner asks arriving mid-S are appended to
  **`docs/tranches/S/OWNER-ASKS.md`** with a wave assignment or an owner-ratified deferral;
  `proof:prompt-recap-s` asserts that file is **fully-dispositioned** (an appended ask without a
  disposition REDs) (SPEC §3 S.Z1, §5.4).
- **MEMORY updates** are listed as **observe-tier** (user-domain, non-gated) — stated, not
  dressed as gated work (SPEC §3 S.Z1).
- **The spirit column stays** — per ask, what would falsify "addressed." A recap that lists
  letter-compliance while the same mechanism leaks (r1's headline) fails S's own bar (SPEC §5.5).

---

## 7. Coverage assertion

**Zero un-dispositioned rows.** Every distinct owner ask (§3), every enumerated precept (§2),
every S-kickoff element (§4), and every re-measured ADDRESSED-map row (§3.1) carries a terminal
disposition — an ADDRESSED-in-<tranche> with a re-verifiable oracle, an S-wave home, a
Recorded-future §8 entry, or a §6.3 owner ruling. The genuine S-scope open set is exactly:
**decomposition-spirit sub-zoning (S.B) · demo/app + playground restructure (S.D) ·
scene-switcher resurrection (S.E) · master-CI / device-gate fix-by-cause + apparatus legibility
(S.A) · parse-that + animation SOTA uplift (S.F/S.H) · legacy purge (S.C) · design-refinement
fleet (S.G)** — plus the standing recap (S.Z1) and fold-all-deferrals (§4).

This substrate is **development-phase only** (SPEC §1 "What S is NOT" — no source writes, no
gate runs, no git change). S.Z1 executes it at the impl drive and re-verifies every oracle above
from a clean run (see the top note).
