# Re-critique (Pass-2) — S.Z Close

**Band:** S.Z (S.Z1–S.Z3) · **Track:** close
**Pass-1 score:** 33% (the most-repaired band — 6 deductions, 8 blocking edits + 2 missing items)
**Pass-2 verdict:** every Pass-1 edit is REAL in the v3 band text (not merely table-claimed); both
close gates are now re-derivation-shaped; the template partition, the closeable-roster T10⊥T12
reconciliation, and the Z3 master-green pre-gate all land and are internally consistent.
**Convergence: 100%.** No admissible blocking items.

---

## 1. Blocking-edit absorption — verified line-by-line in the v3 band

The §9 disposition table lists 9 sz-close edits (SZ-1…SZ-9), which map onto the Pass-1 §8 blocking
list B1–B8 plus B-miss-1 (B-miss-2 folded into SZ-7). Each verified against band text, not the table.

| Edit | Pass-1 ask | v3 evidence (quoted) | Real? |
|---|---|---|---|
| **SZ-1 (B1)** | ADDRESSED rows cite an oracle that resolves AND matches a green exit; bind the three re-verifications | S.Z1:1214–1216 "every `ADDRESSED` row must name an oracle that **resolves to a real gate or live-observation artifact AND matches a green exit in the S.Z3 from-clean closeable-roster run** — a cited oracle that is absent or red REDs the row (row-shape alone can no longer green)"; bound oracles 1219–1222 (keyframes-vue → npm view 404 + repo-grep zero; lint-tier → dep-cruiser `[]` + enforcer file; decomposition-spirit → S.B5 empty-override + ≤460L + no-re-export-bridge exit) | ✓ |
| **SZ-2 (B2)** | Replace "the nine" with r1 §5's twelve kickoff asks + 7-clause standing mandate, cited | S.Z1:1210–1212 "the **cited enumeration**: r1 §5's **twelve** kickoff asks (r1:333-356) + the 7-clause standing mandate (r1:60-71) — v1's unsourced 'the nine in the kickoff' is pruned"; clause (iii) 1217 "REDs if any enumerated precept lacks a band mapping" | ✓ |
| **SZ-3 (B3)** | Template gate: direct-check {T2,T3,T4,T5,T10}; sibling-presence {T1,T6,T7,T8,T9}; per-mandate plant table | S.Z2:1236–1243 "**Directly checked (FINAL-parseable):** {T2…T3…T4/T5…T10…}. **Sibling-asserted:** for {T1, T6, T7, T8, T9} the template gate asserts only the PRESENCE and GREEN state of the named sibling gate… T1→proof:gate-is-runtime, T6→proof:no-orphan-module… 'REDs on any violation' of the un-parseable mandates is an overclaim and is deleted." Plant table 1248–1250 "one deliberately-malformed FINAL per checked clause, each with its expected RED message" | ✓ |
| **SZ-4 (B4)** | Closeable roster; reconcile T10⊥T12; external HANDOFF as explicit third state | S.Z3:1252–1255 "from-clean **closeable-roster** run (C-21: full roster MINUS owner-ratified external HANDOFF gates named by ledger row, each rendered in the FINAL table as `HANDOFF — external — row N`… never omitted; the table lists every gate)"; C-21:398–403; T10:1550–1553 "**Reconciled with T12 in one sentence (C-21)**" | ✓ |
| **SZ-5 (B5)** | Post-close fallout guard distinct from the FINAL parser | S.Z3:1258–1261 "**Post-close fallout guard (distinct from the FINAL parser, which cannot see the future):** any commit between the FINAL SHA and the version tag that touches `src/`/`bench/`/`scripts/` REDs"; T10:1548–1550 | ✓ |
| **SZ-6 (B-miss-1)** | keyframes-vue KILL oracle | S.Z1:1219–1220 "keyframes-vue KILL → `npm view @mkbabb/keyframes-vue` returns 404 AND repo-grep zero (r1:375)" | ✓ |
| **SZ-7 (B7 + B-miss-2)** | Concrete mid-S ask-capture mechanism + name ask-ledger path | S.Z1:1209 "materializes at **`docs/tranches/S/PROMPT-RECAP.md`**"; 1228–1231 "owner asks arriving mid-S are appended to **`docs/tranches/S/OWNER-ASKS.md`**… proof:prompt-recap-s asserts that file is fully-dispositioned (an appended ask without a disposition REDs)"; §5.4:1411–1413 | ✓ |
| **SZ-8 (B6)** | Re-point device rows to A0/A2 fix-by-cause; re-measure r1's ADDRESSED-map | S.Z1:1223–1226 "the ledger re-points the apparatus/device rows from r1's p12-refuted device-dependence framing to S.A0/S.A2's fix-by-cause dispositions, and **re-measures r1's own ADDRESSED-map** rather than chain-trusting it"; §5.2:1403–1407 | ✓ |
| **SZ-9 (B8)** | Surface the two version questions to the owner, not glossed | S.Z3:1262–1267 "**an OWNER RULING with its inputs surfaced, not glossed (sz-B8):**… (i) S.B6's `=any→=Vars` flip is a source-breaking d.ts narrowing… potentially major; (ii) the 126-leaked-private API-Extractor strip… whether leaked privates are 'surface'… is the owner's call. Both are pre-booked as §6 owner questions"; §6.3:1481–1484 | ✓ |

All 9 absorbed for real. The lower-severity Pass-1 items are also delivered in band text:
- **Ask-ledger path** — `docs/tranches/S/PROMPT-RECAP.md` (S.Z1:1209).
- **Cross-repo rows** — S.Z1:1226–1228 "**Cross-repo rows** (S.H, value.js dispatches) are dispositioned via the kf-side CONSUME gate… a sibling's internal gate is never cited as the oracle."
- **MEMORY observe-tier** — S.Z1:1231–1232 "MEMORY updates are listed as **observe-tier** (user-domain, non-gated — stated, not dressed as gated work)."
- **Spirit column kept** — S.Z1:1232 "The spirit column stays"; §5.5:1414.

---

## 2. The four re-critique focus areas — each verified

1. **Both close gates re-derivation-shaped (the Pass-1 headline disease).**
   - `proof:prompt-recap-s` no longer checks row-*shape*: the ADDRESSED oracle must *resolve and
     match a green exit in the from-clean run* (S.Z1:1214–1216); the non-vacuity plants include "a
     planted row citing a red oracle must … RED" (1218). The r2-F1-on-itself defect is cured.
   - `proof:tranche-template` no longer re-parses a claimed exit code: the **RE-EXECUTION clause**
     (S.Z2:1244–1247) "**re-executes the closure oracle of every wave marked CLOSED** (a re-run, not
     a re-parse of the asserted exit code) and REDs on any exit-code mismatch — a FINAL that cites a
     green gate that is actually red at the close commit REDs (the Q-mode cure)." This is the x2-#4
     absorption and it directly answers the Pass-1 "scope is prose" indictment.

2. **The template gate partition.** S.Z2:1236–1243 splits the mandates into the FINAL-parseable
   direct-check set {T2,T3,T4,T5,T10} and the sibling-asserted set {T1,T6,T7,T8,T9} (presence+green
   of the named sibling gate), and explicitly *deletes* the "REDs on any violation" overclaim for
   the un-parseable mandates. The §2.3 acid-test cure — the template gate must *require the presence
   of* `proof:no-orphan-module` to catch a zombie-excision — is delivered: T6→proof:no-orphan-module
   (S.Z2:1241).

3. **The closeable-roster T10⊥T12 reconciliation.** C-21:398–403 defines it once; S.Z3:1252–1255
   consumes it; T10:1550–1553 states the one-sentence reconciliation with the third state
   `HANDOFF — external — row N` (never omitted, never counted green); T12:1563–1569 names *exactly
   two* external edges (E6, H4). Fold row 52:1363 renders proof:peer-satisfied as
   `HANDOFF — external — row 52`. The Pass-1 "close is unrunnable as written" structural blocker is
   fully resolved.

4. **The Z3 master-green pre-gate (x2-#4).** S.Z3:1256–1258 "**Born-RED precondition:**
   `gh run list --workflow ci.yml --branch master` shows a **green push run on the FINAL SHA** — the
   A0 keystone re-gated at close; the tranche is not closable over a red master (the Q/R mode is
   structurally forbidden)." DAG:1285 "(Z3 pre-gated on master-green at the FINAL SHA)."

---

## 3. New-contradiction check (v3-introduced) — cleared

The one candidate I probed: does the **x2-#4 master-green precondition** (new to v3) collide with
the **born-RED external HANDOFF gate** (S.E6/proof:peer-satisfied inside proof:all) if glass-ui
5.0.0 hasn't published — i.e. does the same T10⊥T12 collision recur at the CI-push level, where
C-21 (which speaks to "the FINAL table" and "the from-clean run") might not reach?

**Cleared — v3 reconciles it explicitly.** S.A0:491–492 ties A0's full-green to the closeable
roster by citation: "Master push CI turns fully green when the **last backlog row's owning wave
closes** — that full-green is **re-asserted at S.Z3 (C-21/T10)**." The external HANDOFF gates are
*not* members of A0's enumerated internal backlog (drag-gesture / easing-sidebar-minimal /
scene-perf-budget-A2 / icon-paint-live, S.A0:479–482); they are the T12 external edges. The
parenthetical "(C-21/T10)" on line 492 is the bridge that makes the master-push green a
*green-modulo-closeable-roster* green, so a still-born-RED E6 does not falsify the Z3 precondition —
consistent with T12's permission to close as a HANDOFF. No unreconciled cross-band collision.

DAG edges touched by absorbed edits are intact: `ALL ──► S.Z1 ──► S.Z2 ──► S.Z3` (1285); S.Z2 Deps
Z1 (1251); S.Z3 Deps Z1, Z2 (1270). No edge broken by an absorbed edit.

---

## 4. Dropped-evidence check — none

- §2.3 acid-test cures both delivered (T6→no-orphan-module presence; the B5 post-close guard).
- The two "keep, do not prune" load-bearing wins survive: T10's full-roster clause (T10:1547–1553)
  and the spirit-column doctrine (S.Z1:1232, §5.5:1414).
- Both record-future prunes landed as owner items: §8-6 (docs/precepts/audits ownership; fold row
  65:1376) and §8-7 (TEMPLATE.md amendment/version discipline so it "does not become the next
  stale-doc-authority" — 1595–1597).

---

## 5. Version-ruling deduction (Pass-1 #4) — correctly disposed, not re-deducted

Per this round's scoring clarification (a), the S.B6 `=any→=Vars` narrowing and the 126-private
strip are now a **pre-booked OWNER RULING with a recorded default** ("Default remains
additive-minor; the ruling lands at S.Z3", §6.3:1481–1484) and a **named decision point** (S.Z3).
That is the correct development-phase disposition — **not** an open design uncertainty. No deduction.

---

## 6. Polish (non-blocking — recorded, not scored)

- **P1.** The Z3 master-green ↔ external-HANDOFF reconciliation leans entirely on the "(C-21/T10)"
  parenthetical at S.A0:492; a reader of S.Z3 alone ("the A0 keystone re-gated at close") must chase
  A0→L492→C-21 to see that the external-gate exclusion applies to the CI push run too. One clause in
  S.Z3 restating "green modulo the closeable roster" would make it self-contained. Correct as-is.
- **P2.** S.Z2's T7 sibling mapping is `T7→proof:ci-coverage (+ the co-edit evidence)` (1241–1242) —
  the parenthetical "co-edit evidence" is looser than the crisp single-gate mappings for T1/T6/T8.
  Non-blocking; the direct gate is named.

---

## 7. Deduction ledger (Pass-2)

| Pass-1 deduction | Pass-2 status |
|---|---|
| #1 prompt-recap checks row-shape + "the nine" undefined (−15) | RESOLVED — oracle-resolving clause (SZ-1) + cited twelve+7 enumeration (SZ-2) |
| #2 template scope is prose; non-vacuity under-specified (−15) | RESOLVED — partition (SZ-3) + RE-EXECUTION clause + per-mandate plant table |
| #3 T10⊥T12 close unrunnable (−10) | RESOLVED — C-21 closeable roster + T10 one-sentence reconciliation |
| #4 version ruling unpriced (−10) | RESOLVED — pre-booked §6.3 owner ruling w/ recorded default (not deductible per clarification a) |
| #5 keyframes-vue KILL no oracle (−10) | RESOLVED — npm view 404 + repo-grep zero (SZ-6) |
| #6 §5.4 mid-S ask-capture unfalsifiable (−10) | RESOLVED — OWNER-ASKS.md fully-dispositioned mechanism (SZ-7) |

**Convergence: 100%.** Blocking: none. Every Pass-1 edit verified absorbed in band text with quoted
v3 evidence; no mis-absorption, no v3-introduced contradiction, no dropped evidence.
