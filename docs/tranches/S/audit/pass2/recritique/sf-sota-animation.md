# Re-critique (Pass-2 convergence) — S.F SOTA Animation Uplift

**Agent:** re-critique · **Band:** S.F (S.F1–S.F6) · **Track:** lib
**Predecessor Pass-1 score:** 52% · **This round:** **100%** — converged, empty blocking.
**Verdict:** every Pass-1 blocking edit (11) is verified REAL in the v3 band text (not merely
table-claimed); all four P2-2 adjustment clusters are folded; EN-a/EN-b are hoisted to S.B3 with
the DAG edge stated and cycle-free; the D2 Typed-OM fork is now a pre-booked OWNER-DEFAULT (KILL)
with a named decision point → not an open uncertainty under this round's scoring clarification (a).
No mis-absorption, no v3-introduced contradiction, no dropped evidence.

---

## 1. Pass-1 blocking edits (§8, 11 items) — absorption verified against v3 band text

| # | Pass-1 edit | v3 evidence (quoted) | Verdict |
|---|-------------|----------------------|---------|
| 1 | F1 input = name-keyed `VTRoleSpec`, bare group = sugar | VT-c `:963-966`: "the primary shape is the name-keyed role spec — `compileToViewTransition(spec: Record<name, VTRoleSpec>, opts)` with `VTRoleSpec = { old?, new?, group?, class? }` — a bare group/Sequence has no old/new axis and survives only as sugar behind a role callback." | REAL |
| 2 | THREE emission surfaces; group timing-only, mandatory-by-default | VT-c `:966-971`: "THREE emission surfaces, group mandatory-by-default … `::view-transition-group(name)` gets a **timing-only** override (duration + timing-function, NEVER `animation-name` …); omitting the group emission ships the observed 250ms/ease temporal incoherence)." | REAL |
| 3 | Four VT refusals enumerated | VT-c `:974-979`: "The four VT refusals, enumerated (p09 F5): `vt-scroll-grammar` … `vt-element-scoped-computed` … `vt-snapshot-inapplicable` … `vt-name-collision` … atop the inherited four." | REAL |
| 4 | Name files + surfaces + VT-b carve; decompose VT-a..VT-d | VT-a `:953` (`orchestration/view-transition/`), VT-b `:958-961` (selector-factory at `backward.ts:339`, replay/deterministic stay green), VT-c `:962` (`compile/view-transition.ts`), wiring `:979-982` (`compileToViewTransition` + `./engine` mirror + `viewTransition` LIGHT barrel + `published-surface` co-edits), VT-d `:983-984` | REAL |
| 5 | Gate → `proof:vt-roundtrip` structural + one settled-rect; no per-frame threshold | `:985-989`: "born-RED **proof:vt-roundtrip** — browser-actuating, device-INDEPENDENT: **structural `getAnimations()` assertions** … + **ONE settled-state rect-tolerance clause** … NO per-frame pixel/ms thresholds." | REAL |
| 6 | Split F5 → F5a/F5b/F5c | `:1056` S.F5a, `:1063` S.F5b, `:1067` S.F5c — each its own wave with its own born-RED gate | REAL |
| 7 | Pre-book Typed-OM: default KILL-with-bench; ADOPT only above threshold, separate wave | S.F5c `:1068-1072`: "the **default verdict is KILL-with-recorded-bench-measurement** … ADOPT fires ONLY if a pre-stated write-throughput threshold is beaten, and then as a SEPARATE authored wave (never folded here …)." | REAL |
| 8 | WAAPI densify gate home named | S.F5c `:1072-1075`: "ships as an **extension of the existing `proof:waapi-adaptive-densify`** with a new born-RED clause … no un-gated feature." (RULED: extends) | REAL |
| 9 | F3 home ruled; demote-or-prototype; dangling refusal pointer fixed | Demote clause DISCHARGED — P2-2 executed → C-22 revised `:405-416` ("`compile/entry.ts` is CONFIRMED as the home … F3 is PROMOTED"); S.F3 `:1001-1006`; refusals home = `CompiledEntryCSS.refusals` channel `:1023-1024` (dangling pointer closed) | REAL |
| 10 | F2 line-split posture + accessible-name-equality gate | S.F2 `:993-999`: "`by:\"line\"` is **measure-or-refuse** … a browser-actuated **computed-accessible-name equality** assertion … T8" | REAL |
| 11 | F6 byte-count → `proof:consume-bundle` | S.F6 `:1079-1082`: "The measured light-entry byte-count claim rides `proof:consume-bundle` … NOT readme-runs …" | REAL |

All 11 verified in band text. The §9 `sf-sota-animation` disposition table (`:1718-1731`) matches;
no row over-claims relative to what the band delivers.

## 2. The four P2-2 adjustment clusters (this round's specific mandate) — folded

**Cluster 1 — declared-endpoint projection.** S.F3 `:1002-1005`: "The emitter is a **declared-endpoint
projection** over the `format.ts` substrate (first/last stop bodies via `declaredKeyframeBodyFor`;
easing via the EN-a twin-fixed `serializeEasing`) — NOT a post-transform over the `compileToCSS`
artifact (refuted live by attempt A)." Reinforced by C-22 `:408-412` and §2.1-15 `:258-262`. FOLDED.

**Cluster 2 — refusal taxonomy incl. the perceptual-oklab inversion.** S.F3 EN-c `:1023-1032`
enumerates `EntryRefusalReason` = **3 inherited** (`custom-renderer`, `weighted-blend`,
`computed-unit-drift`) **+ 6 entry-specific** (`entry-multi-keyframe`, `entry-iteration`,
`entry-composition`, `entry-scroll-grammar`, `entry-color-space`, `entry-easing-twin`). The
inversion is explicit at `:1016-1019` ("the `perceptual-oklab` refusal INVERTS to a feature on this
surface … `oklab()` endpoint canonicalization … NO densify and ZERO intermediate stops") and C-22
`:415-416`. FOLDED.

**Cluster 3 — gate/doc semantics.** S.F3 EN-c `:1040-1044`: "born-open elements RUN the entry at
first render (a platform semantic — documented + demo-stated; escape hatch = … `.kf-no-initial`
guard); entry `display` (`none → block`) produces NO CSSTransition — **gate assertions target the
EXIT hold only**. Degrade-honest where `transition-behavior` is unsupported (snap-entry, correct
end state)." `overlay` unconditional at `:1013-1015`. FOLDED.

**Cluster 4 — tier reconciliation for browser-actuating library-value gates.** Band preamble
`:945-949`: "the band's browser-actuating library-value gates (`proof:vt-roundtrip`,
`proof:entry-roundtrip`, the EN-a browser-parse clause) enroll in the browser-harness
(demo-correctness) chain — their library-value severity is recorded in their taxonomy rows; placing
them in `proof:library-correctness` would correctly RED under S.A4's symmetric mis-tier clause (the
clause working as designed, not an exception to it)." This coheres with p08's symmetric mis-tier
clause and is cross-referenced in the P2-2 addendum note `:1826-1827`. FOLDED.

## 3. EN-a/EN-b hoist to S.B3 + the DAG edge — verified real and cycle-free

- **Ruling C-25 `:418-432`:** "hoist both into S.B3 … same-file cohesion … honesty-first sequencing
  … T7 fixture co-edits already in B3's blast radius. **DAG edge, stated: S.B3 (carrying EN-a +
  EN-b) ──► S.F3/EN-c ──► EN-d.**" The `declaredKeyframeBodyFor` deletion is REVERSED (fold row 58).
- **S.B3 wave text `:618-638`:** EN-a authored ("`serializeEasing` CSS-twin fix … Born-RED gate
  clause: a browser-actuated parse of an emitted `easeOutCubic` artifact (computed `animation-name
  !== none`)") and EN-b authored ("thread the densify through `keyframesBlock`'s `bodyByStop` …
  Born-RED gate clause: a mixed `opacity+color` compile artifact contains BOTH properties"); T7
  fixture co-edit `:634-635`; "**BLOCKS S.F3/EN-c (C-25 DAG edge).**"
- **The DAG `:1278,:1280`:** `S.B1 ──► S.B3 ──► S.B4` and `S.B3 (carrying EN-a + EN-b) ──►
  S.F3/EN-c ──► EN-d`. Chain B1 → B3(+EN-a+EN-b) → F3/EN-c → EN-d is acyclic.
- **S.F3 deps `:1047`:** "B3 (which carries EN-a + EN-b — the C-25 DAG edge; EN-c is unshippable on
  today's `serializeEasing`)" — matches the DAG.

Cross-band consistency check on the hoist: EN-b threads `bodyByStop` in the same `compileChild`/
`densify` region that S.B3 already rewrites (a18 F4 color-ramp hoist; `format.ts` → `compile/
backward/`). C-25 cites this same-file cohesion as the *reason* for co-locating — deliberate, not a
collision. The EN-a browser-parse clause is homed in B3 yet enrolled in the S.F band preamble's tier
note; the two references are coherent (the note names "the EN-a browser-parse clause" explicitly).
No contradiction introduced.

## 4. Pass-1 deduction ledger — every deduction discharged

| Pass-1 | Type | Disposition in v3 |
|--------|------|-------------------|
| D1 (−10) F1 gate device-dependent | gate not-honest-as-written | RESOLVED — `proof:vt-roundtrip` structural `getAnimations()` + one settled-rect, no per-frame race (`:985-989`) |
| D2 (−10) Typed-OM open Q, no prototype/seam | open design Q | RESOLVED — pre-booked OWNER DEFAULT (KILL-with-bench) + named decision point (write-throughput threshold), ADOPT as separate wave (`:1068-1072`). Per this round's scoring clarification (a): a pre-booked ruling with a recorded default is NOT an open uncertainty → **no deduction** |
| D3 (−10) F5 kitchen-sink, missing gate coverage | missing gate coverage | RESOLVED — F5a/F5b/F5c each born-RED-gated; resolve.bench + cold-import via `proof:bench-taxonomy` rows (`:1063-1066`); WAAPI densify via `proof:waapi-adaptive-densify` clause (`:1072-1075`) |
| D4 (−10) F3 substrate/home unruled, no prototype | open design Q | RESOLVED — P2-2 executed live; home CONFIRMED `compile/entry.ts` sibling; substrate = declared-endpoint over `format.ts`; F3 PROMOTED (`:1001-1050`, C-22) |
| D5 (−8) F2 line-split + AT-tree gate unnamed | missing evidence-detail | RESOLVED — measure-or-refuse + computed-accessible-name equality (T8) (`:993-999`) |

## 5. Admissible-blocking scan (mis-absorption / new v3 contradiction / dropped evidence)

- **Mis-absorption:** none. Every §9 table row that claims "ABSORBED" is delivered in the band
  text (verified §1 above, row by row, with quotes).
- **New v3 contradiction (EN-a/EN-b hoist, DAG edges):** none. The B1→B3→F3/EN-c→EN-d chain is
  acyclic; the EN-b same-region co-location is deliberate (C-25) not a collision; the EN-a
  cross-reference between B3 and the S.F tier note is coherent.
- **Dropped evidence:** none. All five Pass-1 deductions and all four P2-2 clusters are folded;
  the two pre-existing compile bugs (EN-a/EN-b, P2-2 F5/F6) are captured as fold rows 73/74 and
  authored waves.

**Blocking: empty.**

## 6. Polish (non-blocking — NOT deducted)

- P1. S.F6's gate line (`:1081-1082`) splits the claim across two gates ("readme-runs covers the
  executable claims; consume-bundle carries the byte floor"); a single sentence naming which
  specific README byte-number rides consume-bundle would remove any residual ambiguity. Cosmetic.
- P2. EN-a's born-RED clause appears both in S.B3 (`:624-627`) and is referenced in the S.F band
  tier note (`:946`). Correct and intentional, but a one-word "(homed in S.B3)" beside the tier-note
  mention would spare a reader the cross-band lookup. Cosmetic.
- P3. S.F5c leaves the Typed-OM ADOPT write-throughput threshold as "pre-stated" without a number;
  fixing a concrete ratio at author-time would be marginally tighter, but the pre-booked-default +
  named-decision-point structure already satisfies the rubric (clarification a). Non-blocking.

---

## Score

Base 100. All 11 Pass-1 blocking edits verified REAL in band text; all four P2-2 clusters folded;
EN-a/EN-b hoist + DAG edge stated and cycle-free; D1/D3/D4/D5 discharged; D2 is a pre-booked owner
default (no deduction under clarification a). No mis-absorption, no new contradiction, no dropped
evidence → **blocking empty**. **Convergence = 100% — CONVERGED.**
