# Re-critique (Pass-2) — x2-antipattern (cross-cutting)

**Agent:** x2-antipattern (re-critique) · **Target:** SPEC-v3 (standalone) · **Date:** 2026-07-02
**Predecessor Pass-1 score:** 55% · **This re-score:** **100%** · **Verdict:** CONVERGED — every
Pass-1 blocking edit absorbed with load-bearing band text; the three live anti-pattern instances are
cured; no new v3 contradiction, no dropped evidence. Blocking list EMPTY.

---

## 0. Method

I re-applied r2's failure catalogue (F1–F6, Part V risk signature) to v3, then verified each of the
10 Pass-1 x2 edits is REAL in the band text (not merely table-claimed), quoting the absorbing line(s).
I gave special attention to the three live instances my predecessor isolated (device-model / B2
un-authorable gate / Z2-Z3 meta-shape), and I probed the one place v3 could have introduced a NEW
cross-band contradiction — the C-25 EN-a/EN-b hoist into S.B3 vs S.A4's symmetric mis-tier clause.

---

## 1. The three live instances — all CURED

### Instance 1 — the self-certifying device-dependence causal model (§2.1-2 / S.A0 / S.A1 / S.A2)

**v1 defect:** §2.1 pt2 asserted the 14 blocking reds were "the device-dependence plane"; A0/A1/A2
pre-wrote reclassification dispositions on that (p12-falsified) assumption.

**v3 CURE — verified:**
- §2.1-2 (l.106-107): *"Master CI has been RED on every push since Tranche K … — and the red plane is
  **DETERMINISTIC, not device-dependent (p12)**."* The full p12 taxonomy is enumerated inline
  (2 source · 2-gates/1-harness-importmap · 5 genuine demo born-RED · 1 gate-staleness · 1
  exit-plumbing), closing with (l.130): *"True device-dependence render-races in the verified sample:
  **zero.**"* The "device-dependence plane" framing is deleted, not softened.
- S.A0 (l.459-460): recast to *"CI-GREEN **by cause** … Recast per p12 from reclassification to a
  **fix-by-cause sweep**"* — the 7 owned items are enumerated; the gate (l.486-490) is cause-shaped and
  falsifiable both ways: *"discharging via threshold-loosen, timeout-widen, residual continue-on-error,
  or observe-reclassification is FORBIDDEN."* This is X2-1 delivered.
- S.A2 (l.511-513): the FOUR buckets are present, including the new *"**stale-gate → re-point the
  gate's parser/selector**"* bucket demanded by X2-1.

### Instance 2 — the un-authorable single-writer gate (S.B2)

**v1 defect (X2-3):** S.B2's *"fields written only through PlaybackState"* clause is un-gateable while
`anim.paused =` is public → vacuous green.

**v3 CURE — verified.** S.B2 gate (l.606-607): *"the honest FSM clause — **'no FSM transition field is
DECLARED on the class body (accessor delegates only)'** (plant a class-body field declaration →
RED)."* The goal is restated as single-STORAGE (l.591-596), v1's "animation.ts shrinks" is STRUCK
(l.596-597: *"the delegate fold **grows** the class 442→455L"*), and the sequencing is fixed
(l.597-599 + DAG l.1278-1279: *"B2 owns the animation.ts/playback.ts ceiling carves — B2 precedes B5
by construction"*). Exactly the p02/C-15 reframe.

### Instance 3 — the document-shaped meta-gate + un-re-gated keystone (S.Z2 / S.Z3)

**v1 defect (X2-4):** Z2 certifies FINAL's *shape* not its *truth*; Z3 never re-asserts A0's
master-green.

**v3 CURE — verified.**
- S.Z2 RE-EXECUTION clause (l.1244-1247): *"the template gate **re-executes the closure oracle of
  every wave marked CLOSED** (a re-run, not a re-parse of the asserted exit code) and REDs on any
  exit-code mismatch — a FINAL that cites a green gate that is actually red at the close commit REDs
  (the Q-mode cure)."* Plus the honest partitioned scope (directly-checked vs sibling-asserted) that
  retires the v1 "REDs on any violation" overclaim.
- S.Z3 born-RED precondition (l.1256-1258): *"`gh run list --workflow ci.yml --branch master` shows a
  **green push run on the FINAL SHA** — the A0 keystone re-gated at close; the tranche is not closable
  over a red master (the Q/R mode is **structurally forbidden, not cited**)."*
- The DAG now GATES closure on it (l.1285): *"ALL ──► S.Z1 ──► S.Z2 ──► S.Z3 (Z3 pre-gated on
  master-green at the FINAL SHA)"* — the "advisory-prose-not-a-gate" hole from v1 danger #2 is closed.

---

## 2. The remaining 7 x2 edits — each verified absorbed

| # | Edit | v3 evidence (quoted/located) | Status |
|---|------|------------------------------|--------|
| X2-2 | correctness↔observe split may NOT green an off-runner-reproducing red | S.A2 l.514-517 "**Hard clause (x2-#2):** the correctness↔observe split may NOT green a red whose signature reproduces off-runner … never by reclassification"; mirrored in S.A0 gate l.489-490 | ✅ REAL |
| X2-5 | redefine "terminal": deterministic-re-shape OR ratified-KILL; observe-in-CI NOT terminal | C-20 (l.390); §4 header l.1303-1305 "'Terminal' uses C-20's structural definition … never 'observe-in-CI', WATCH, or a re-verify verb"; fold rows 6/7/8/9/14 each carry "observe-in-CI is NOT accepted" | ✅ REAL |
| X2-6 | rows 51/52/53 = explicit non-terminal RESIDUAL CARRY; kf-internal dock contingency | S.E6 l.930-936 "if 5.0.0 has not published at S close … **explicit non-terminal RESIDUAL CARRY** … a kf-internal press handler authored at E5-time so the ≥4-tranche chronic's terminal does not depend on an external publish"; fold rows 51-53; C-12 amended (l.340); T12 l.1563-1569 | ✅ REAL |
| X2-7 | FROZEN discharge machine-distinguishable + symmetric mis-tier clause | S.A4 l.547-551 "**FROZEN-set discharge is machine-distinguishable** … successor-mapping row in ci-coverage that REDs if a FROZEN key is deleted without a successor row — OR … owner-ratified S-ledger row with a re-run witness … Free-prose 'deletion-with-cause' is banned"; symmetric clause l.542-544; fold row 20 | ✅ REAL |
| X2-8 | extend T3's deferral-verb meta-gate to the S ledger's disposition column | T3 l.1519-1523 "**Extended (x2-#8): the same grep runs over the S ledger's disposition column**"; S.A1 l.503-504 wires it into `proof:chronic-closure` | ✅ REAL |
| X2-9 | engine-subpath-mirror diffs the AnimationEngine TYPE key list, not runtime Object.keys() | S.B6 l.669-673 "asserts **runtime keys ⊆ the `AnimationEngine` d.ts TYPE key list** … a runtime-vs-runtime `Object.keys()` diff is vacuous once the loader sources from the same import" | ✅ REAL |
| X2-10 | S.G1 gate-arming audit (.controls-pane--open arming waits) | S.G1 l.1103-1106 "**The arming audit sub-task (p10 F5):** grep the gate roster for `.controls-pane--open` born-open arming waits and re-arm them"; generalized into T7 l.1537-1538 | ✅ REAL |

---

## 3. Cross-cutting probe adjustments (my lane) — folded

- **p12 → §2.1-2 / S.A0 / S.A1 / S.A2 / §4:** the defect-not-device recast is total (Instance 1). The
  enumerated born-RED backlog carve (S.A0 l.479-482, DAG-owned by named downstream waves) is the
  correct disposition of the "green modulo enumerated backlog" prescription; fold row 5 carries the
  full taxonomy. DM-11b folded into DM-13's ONE importmap fix (row 10/12); DM-14 correctly recast as a
  GENUINE spring source defect (row 13), reversing v1's r8-F1 repeat.
- **p07 → S.B6:** TYPE-diff gate absorbed (X2-9); the loader-collapse decoupled to an owner-recordable
  option (§6.3 l.1485-1490) — the gate ships regardless. Correct.
- **p10 → S.G1:** arming audit absorbed (X2-10); the three-writer cure + one-open-axis + mode-token
  band all present.

---

## 4. New-contradiction sweep (the only admissible source of a fresh block)

I checked the one place v3's own edits could collide: the Pass-2 **C-25 hoist** of EN-a/EN-b into
**S.B3 (track: lib)**. EN-a's gate is a *browser-actuated parse* (S.B3 l.624-627: "browser-harness by
necessity"). S.A4's newly-added symmetric mis-tier clause REDs a `proof:library-correctness` member
that carries browser-harness anchors. A naive tiering of EN-a as library-correctness (B3 being
lib-track) would have been a genuine v3-introduced collision.

**It is pre-reconciled, explicitly.** S.F band preamble l.945-949: *"the band's browser-actuating
library-value gates (`proof:vt-roundtrip`, `proof:entry-roundtrip`, **the EN-a browser-parse
clause**) enroll in the browser-harness (demo-correctness) chain … placing them in
`proof:library-correctness` would correctly RED under S.A4's symmetric mis-tier clause (the clause
working as designed, not an exception to it)."* The tier note names the EN-a clause by its gate, not
by its home wave — so the reconciliation holds even though EN-a is homed in B3. **No contradiction.**

DAG edges checked: `S.B3 (carrying EN-a + EN-b) ──► S.F3/EN-c ──► EN-d` (l.1280) with fold rows 73/74
pointing to B3 — acyclic, and the EN-c dependency on B3 is stated three times consistently
(l.638, l.1006, l.1047). No absorbed edit broke a DAG edge.

---

## 5. Risk-signature (r2 Part V) — now STRUCTURAL, not cited

v1's finding was "mostly cited." v3's T11 (l.1554-1562) now lists concrete structural compensators —
each of which I verified lands in a gate, not prose: cause-shaped keystone with masking forbidden
(S.A0), the observe-split no-reclassification clause (S.A2), machine-distinguishable FROZEN discharge
(S.A4), closure re-execution + master-green-on-FINAL-SHA precondition (S.Z2/Z3), the residual-carry
honesty for rows 51-53 (C-20), and the §6.3 breadth owner decision point. The "breadth is un-justified"
recommendation my predecessor flagged as owner-ruling is pre-booked at §6.3 l.1494-1495 — per this
round's scoring clarification (a), a pre-booked owner ruling with a recorded default is NOT an open
design uncertainty and is not deductible.

---

## 6. Scoring

Every Pass-1 deduction maps to an edit now verified absorbed in band text:
- −15 B2 unfalsifiable → **restored** (§1 Instance 2)
- −15 Z2/Z3 meta-shape + un-re-gated keystone → **restored** (§1 Instance 3)
- −15 E6 deferral laundering → **restored** (X2-6 residual-carry + dock contingency)
- −10 p12 device-model refutation → **restored** (§2.1-2 rewritten)
- −10 no closure re-exec / master-green meta-gate → **restored** (Z2 re-exec + Z3 precondition)
- −10 A4 FROZEN cause-validation → **restored** (machine-distinguishable discharge)

Blocking list EMPTY (no mis-absorption, no new v3 contradiction, no dropped evidence). Every Pass-1
x2 edit verified absorbed with load-bearing text. **convergence_pct = 100.**

### Polish (non-blocking)
- S.B3's wave text lists the EN-a browser-parse clause among its gates but does not itself cross-ref
  the S.F preamble tier note (l.945-949); an implementer reading B3 in isolation could momentarily
  mis-tier it. The spec is internally consistent (the tier note governs by gate name), so this is a
  reader-convenience cross-reference, not a defect.
- The residual anti-pattern surface is now execution-discipline, not spec-honesty: S.Z2's
  re-execution clause and S.Z3's `gh run list` precondition are only as strong as the impl drive's
  willingness to actually re-run rather than re-read — which is exactly what T4/T5 mandate. Nothing to
  add to the spec; flagged for the impl orchestrator.
