# Re-critique x1 — Cross-cutting completeness (Pass-2 convergence check)

**Agent:** x1-completeness (re-critique) · **Scored:** SPEC-v3.md band section (§4 fold table) +
§2 rulings + §6 residue + §9 disposition tables (x1 rows + Pass-2 addendum) + the DAG · **Date:**
2026-07-02 · **Mode:** analysis only. · **Predecessor Pass-1 score: 82.**

**Verdict:** All six Pass-1 x1 blocking edits are absorbed FOR REAL in v3 band text (not merely
table-claimed). The two genuine Pass-1 drops (DQ-4/5/6/7, KfPillTabs) now carry fold rows. Rows
73/74 were added and are DAG-consistent; rows 31/58 were updated coherently with the Pass-2 probe
results; §6 residue reduces cleanly to the §6.3 owner rulings only. No mis-absorption, no new v3
contradiction, no dropped evidence item.

**convergence_pct: 100.**

---

## 1. The six Pass-1 x1 blocking edits — absorption verified in band text

| Pass-1 edit | Table claim | Band-text evidence (quoted) | Real? |
|---|---|---|---|
| X1-1 KfPillTabs a12 F1/F2 fold row | fold row 71 | **Row 71** (SPEC-v3:1382): *"KfPillTabs roving-tabindex keyboard-broken + TransportDock auto-repeat (a12 F1/F2, HIGH) … WAVES S.B7 … + S.D2 (promotion …) — NOT S.E5 (se-B6: it is a panel primitive, not scene-nav)"*; delivered in **S.B7** (:689–693 *"KfPillTabs.test.ts + the interaction-axis fixes … TransportDock auto-repeat; fold row 71 … KfPillTabs.test.ts green"*) and **S.D2** (:795 *"the KfPillTabs promote-to-standard-panel-primitive item lands"*), removed from **S.E5** (:919 *"KfPillTabs is REMOVED from this wave's scope (se-B6)"*) | ✅ |
| X1-2 DQ-4/5/6/7 disposition | fold row 72 | **Row 72** (:1383): *"DQ-4 (false-RED S1/S2) · DQ-5 (ci-coverage) · DQ-6 (emerging-CSS-P2) · DQ-7 (wave-charter) … Assumed FOLD-LANDED in Q; S.Z1's recap re-verifies each actually landed (the same treatment DQ-1/DQ-2 received) — closing r8-F2 totally"* — exactly the treatment Pass-1 §2.1 said would satisfy | ✅ |
| X1-3 §4-header "open item" definition | §4 header | **§4 header** (:1300–1302): *"'open item' = open at R-close … The five already-ratified R KILL/RECORD terminals (DM-7, DM-1, DM-5 S1, DQ-3, VJ-Q9 — r8 §1a) are excluded by definition and are NOT re-litigated as rows."* | ✅ |
| X1-4 shadcn-census gate clause | §3 S.C3a | **S.C3a** (:740–742): *"the shadcn census clause (x1-#4: repo-wide grep for `cn(`/`class-variance-authority`/`@radix-*` returns empty post-purge — NO-legacy proven, not menubar-scoped)"*; wired as a gate in C3a (:747) and C3b (:753 *"the shadcn census clause (C3a) REDs while ui/menubar exists"*) | ✅ |
| X1-5 S.A4 absorbs p08's three adjustments | §3 S.A4 | **S.A4** (:528–544): the **5-artifact atomic co-edit set** (a–e, incl. *"the clause-0b three-tier union at `:238-239` (the airtightness linchpin)"*), the **direct `&&` chain** (*"keep it a direct `&&` chain … with no `resolveTier` indirection"*), and the **symmetric mis-tier clause** (*"a `proof:library-correctness` member's script must NOT carry browser-harness anchors … falsifiable in BOTH directions"*) | ✅ |
| X1-6 SOTA-both-repos owner ruling | §6.3 | **§6.3** (:1491–1493): *"'SOTA both repos' reading (x1-#6): the spec serves kf (S.F) + parse-that (S.H), matching the r5/r6 lane structure — the supported default. If the owner meant the full constellation, a value.js SOTA lane is a missing band (not added speculatively — x1 prune)."* — pre-booked ruling with recorded default + named decision point (scoring clarification (a): not an open uncertainty) | ✅ |

All six verified in the absorbing band text, not just the §9 table.

## 2. Assigned re-verification — the exact charge

**Rows 73/74 added (correct + DAG-consistent).**
- Row 73 (:1384): serializeEasing emits browser-invalid easing names (P2-2 F6) → **S.B3/EN-a**,
  born-RED browser-parse gate (C-25).
- Row 74 (:1385): compileChild whole-block densify drops every non-color property on mixed tracks
  (`backward.ts:289-293`, P2-2 F5) → **S.B3/EN-b**, born-RED mixed `opacity+color` gate (C-25).
- DAG integrity: line 1280 *"S.B3 (carrying EN-a + EN-b) ──► S.F3/EN-c ──► EN-d"* and line 1278
  *"S.B1 ──► S.B3 ──► S.B4"* — both EN-a/EN-b live in S.B3 exactly as the rows point; no broken
  edge. Consistent with Pass-2 addendum P2-2.2 (*"HOMED IN S.B3 … DAG edge S.B3 → EN-c stated"*).

**Rows 31/58 updated (correct + probe-consistent).**
- Row 31 (:1342): animation-controls "do not touch" fiat → S.D2, now carrying *"probe-VALIDATED —
  P2-1 confirms-spec: gate-repoint-dominated, ~3–4 days, LOW risk"* — matches §6.2 P2-1 CLOSED and
  Pass-2 addendum P2-1.1.
- Row 58 (:1369): declaredKeyframeBodyFor "dead export" → *"WAVE S.B3 — REVERSED at Pass-2 (a18 F3
  overturned by P2-2 F1/F5: the export is the EN-b/EN-c load-bearing substrate; constructed, not
  deleted)"* — matches P2-2.2 (*"the a18-F3 declaredKeyframeBodyFor deletion REVERSED (fold row
  58)"*). No contradiction with rows 73/74: the reversal keeps the substrate the EN emitter set
  requires.

**Pass-1 dropped-items list all absorbed.** The Pass-1 critique named exactly two true drops:
DQ-4/5/6/7 (§2.1) and KfPillTabs (§2.2). Both now have fold rows (72, 71) delivered in band text
(above). The eight NEW debts of Pass-1 §3 all retain their rows (39/18/3/71/66/41/40/45); the
previously-missing KfPillTabs is now present. Nothing from the Pass-1 evidence base is dropped.

**Section 6 residue = owner rulings only.** §6 intro (:1423): *"The only remaining open items are
the §6.3 owner rulings — no probe work remains before impl authorization."* Structure confirms:
§6.1 = v1's Q1–Q12 all CLOSED by probes (per item); §6.2 = the two Pass-2 probes EXECUTED + CLOSED;
§6.3 = four pre-booked owner rulings, EACH with a recorded default + named decision point (version
ruling → default additive-minor, lands S.Z3; loader-collapse → ships only on owner approval, drift
gate ships regardless; SOTA reading → two-repo default; breadth ruling → default not-split, owner
may order); §6.4 = disputed/dropped = *"None, at either pass."* Per scoring clarification (a), the
§6.3 rulings are the correct development-phase disposition, not open design uncertainty — no
deduction.

## 3. Admissible-blocking scan (mis-absorption / new v3 contradiction / dropped evidence)

- **Mis-absorption:** none. Every table claim checked against band text delivered (rows 71–74,
  31, 58; S.C3a; S.A4; §4 header; §6.3). The KfPillTabs S.E5→S.D2 wave-pointer correction (X1-1
  note; SE-6) is coherent across all three sites (B7 test, D2 promote, E5 removed).
- **New v3 contradiction (EN-a/EN-b hoist / DAG):** none. The row-58 reversal, rows 73/74, the
  S.B3-carries-EN-a/EN-b DAG edge, and P2-2.2 are mutually consistent. The S.B3→S.F3/EN-c→EN-d
  chain is intact.
- **Dropped evidence:** none. The full Pass-1 x1 evidence base is folded.

Empty blocking list; every Pass-1 edit verified absorbed → **100**.

## 4. Polish (non-blocking)

- Row 58 labels declaredKeyframeBodyFor the *"EN-b/EN-c load-bearing substrate"* while P2-2.1 frames
  it primarily as the F3 emitter (EN-c/EN-d) input. The granularity is loose but not contradictory —
  the reversal's load-bearing point (do not delete; the EN set needs it) is correct. Wording only.
