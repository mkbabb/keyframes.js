[high] (boundary-invariants) inv alpha gate covers only SpringProgress; 5 of 7 light modules unguarded; static value.js import in numeric.ts passes GREEN; also source-not-dist, parse-that-blind, no dynamic-chunk assert
    @ scripts/proof-boundary.mjs:49-51,57; index.ts:30-48
    →B: SHIP B: enumerate all light exports off index.ts; empty node_modules allowlist; assert engine in a non-entry dynamic chunk; post-build dist-grep; per-module negative tests.
[high] (boundary-invariants) inv beta: npm ci on a clean runner makes a DANGLING glass-ui symlink, not a skip; lockfile node lacks optional:true
    @ package-lock.json:849-851; FINAL.md:113
    →B: BOOK B keyframes-CI: drop file:../glass-ui from the published manifest or assert path absent-or-resolves, failing on a dangling link.
[high] (boundary-invariants) Eager-resolve engine import has no catch; chunk-load failure is an unhandled rejection pinning easing to identity forever; unknown name also silently pins identity
    @ internal/easing-resolvable.ts:64,93-100; utils.ts:136
    →B: SHIP B: a catch rethrowing on awaited ready (fail-explicit per precept); unknown-name must throw or warn-persistently.
[high] (deferred-ledger) 16 in-source TODOs (1 CRITICAL / 4 HIGH / 9 MEDIUM / 2 LOW) are entirely absent from the deferred-ledger — the single largest unenumerated debt class
    @ src/animation/engine.ts:130,396,412,416,429,450,456,474,480,493,887,906,937; group.ts:540; constants.ts:148; animations.ts:17 (counts via grep TODO\((CRITICAL|HIGH|MEDIUM|LOW)\))
    →B: SHIP → a dedicated B wave 'fail-explicit option validation' that closes the CRITICAL + 4 HIGH TODOs as one gestalt (a strict-validation contract on AnimationOptions setters + reset/fill, throwing on malformed input per the inherited invariant). NOT a workaround-by-workaround sweep — the user's directive is gestalt: define the explicit reset/fill contract (group.ts:540) and option-validation contract (engine.ts cluster) together. The 9 MEDIUM (implicit defaulting on delay/direction/fill/colorSpace/blendMode/transform-strategy) fold into the same strict-mode option once the contract exists. Owner: keyframes engine. Trigger: B open (this is no-legacy debt, exactly the kind B must absorb).
[high] (deferred-ledger) Playwright >50ms-trace gate is a pure stub that predates A — the A.W4 hard gate it backs was never run, only unit-surrogated
    @ bench/playwright.bench.ts:1-42 (expect(true).toBe(true), dated Feb 26 — predates A); W4.md:150-155; FINAL.md:210-212; A.md:31,64 (A.W4 completion criterion: 'the group loop breaking >50ms tasks under a Playwright trace')
    →B: SHIP → B perf wave. The scheduler.yield() work shipped without its proving gate; B must close the loop. Wire bench/playwright.bench.ts to a real CDP trace (Tracing.start/end on the bench page, assert no >50ms task in the large-group composite) — this is the perf analogue of proof:boundary and gives scheduler.yield() the causal evidence it lacks. CHRONIC: deferred since grand-audit §2.4 → A.W4 → now. Owner: keyframes engine. Trigger: B perf wave open (do not let it slip a third cycle).
[high] (precept-adherence) Fail-explicit invariant is declared but not honored: unresolvable easing name silently degrades to identity
    @ src/animation/internal/easing-resolvable.ts:93-100; A.md:40 (inv); design/W1-boundary-and-ci.md:65
    →B: SHIP→wave (B boundary-ergonomics-II): make `EasingResolvable.ready()` fail-explicit on an unresolvable name — throw a named `KeyframesError`/`UnknownEasingError` when `getTimingFunction(name)` returns undefined (the registry-lookup is a library-internal contract, not a browser degradation), OR if a thrown error is too disruptive for a fire-and-forget eager-resolve, latch a permanent dev-warn that survives `_pendingName = null` and add a `.resolutionFailed` getter the consumer can assert. Add a test asserting the unknown-name path is observable (throw or latched-warn). Reconcile A.md inv line 40 with reality — the current text is a false invariant.
[high] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Dependency update (vite/vue/vue-router/shadcn/tsc/vitest) — PLANNED-FOR-B (decision: plan-as-wave, not executed)
    @ package.json:97-105 (typescript ^6.0.3, vite ^8.0.13, vitest ^4.1.7, vue ^3.5.34, vue-router ^5.0.7); locked decision 'dep update = plan-as-wave (not executed now)'
    →B: SHIP → B.W-deps (a dedicated dependency-matrix wave). Author the matrix (each dep: current→latest, breaking-change notes, the proof:boundary/check:lib re-gate), DO NOT execute the bump this engagement. Explicitly scope-correct 'shadcn' (it is glass-ui-owned, not a keyframes direct dep) so the ask is not mis-targeted.
[high] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Install audit tooling AND MCP browser (lighthouse + best-practices e2e of every demo page + library) — PLANNED-FOR-B (decision: install tooling AND MCP browser)
    @ Locked decision 'audits = install tooling AND MCP browser'; P2 'full e2e audit with lighthouse + google best-practices of every demo page + every facet of the library'
    →B: SHIP → B.W-e2e-audit. Plan the wave: install lighthouse + chrome MCP, enumerate every demo route + the playground, define the lighthouse/best-practices score floors as gates, and a per-facet library coverage map. The decision locks tooling-install as in-scope for B's impl — name the install step explicitly.
[high] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Screenshot-every-page BEFORE and AFTER tranches — ENSURE WE HAVE AN EDICT — HIGHEST DROP-RISK
    @ docs/precepts/instructions/tranche/SPEC.md:216-251 (π visual-runtime lane only); adoption fold constellation-adoption-2026-06-02.md:204-225 (before/after subsection is UNCOMMITTED in value.js precepts); precepts gitlink 63240e6
    →B: SHIP → B.W0 (precepts edict authoring). Pull latest precepts, AUTHOR the committed before/after-screenshot-every-page edict in SPEC.md (and LESSONS-LEARNED), advance the gitlink, user pushes. Per the locked decision this is keyframes-authored. Do NOT assume the π lane already satisfies it — the π lane is ship-visual-conditional; the directive wants an unconditional every-page before/after capture edict.
[high] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Remove the keyframes.js loading screen + dramatically improve loading times — NOT-YET (loading screen present on disk)
    @ demo/app/index.html:50-79 (Loading skeleton, .loading-skeleton/.loading-title, 'keyframes.js' title); demo/app/App.vue:123 ('Loading scene…' animate-pulse), :198 (hideLoader)
    →B: SHIP → B.W-loading (loading-screen removal + loading-time perf). Plan: remove the index.html skeleton + App.vue scene-loader, replace with instant-render / skeleton-free first paint, and a loading-time budget gate (tie to the lighthouse wave). This is a clear pixel-shipping wave → it MUST run the before/after π capture (ties to the edict above).
[high] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Audit every page desktop + mobile — NO occlusion/overlap, dock perfected, fully Playwright-validated — PLANNED-FOR-B
    @ User P2 'Audit every page in desktop + mobile — validate scrupulously NO occlusion/overlapping, the dock is perfected … Fully playwright validation'; mcp__plugin_playwright + claude-in-chrome tools available (deferred)
    →B: SHIP → B.W-validation (Playwright desktop+mobile occlusion/dock sweep). Per the decision, MCP browser is installed for B. Gate: zero occlusion/overlap at named breakpoints, dock-perfected assertion. Route any dock defect to glass-ui repo (memory precept), do not patch in demo. Pair with the before/after edict capture.
[medium] (plan-fidelity) Committed lockfile is glass-ui-PRESENT, contradicting the FINAL.md/W1 'regenerated glass-ui-absent' claim that anchors inv β
    @ package-lock.json:67-69,849-851; contradicts FINAL.md:64-66 and design/W1-boundary-and-ci.md:93
    →B: BOOK → trigger: tranche B's first CI/release touch. Owner: keyframes author. Either (a) regenerate the lockfile with `../glass-ui` genuinely absent and commit the glass-ui-free tree so the artefact matches inv β's prose, or (b) correct FINAL.md to state the honest reality (glass-ui recorded-but-optional; clean runner skips it). Do NOT ship B's release wave on the unstated assumption the lockfile is glass-ui-absent — verify with a /tmp clean-runner archive run as part of B's π floor.
[medium] (plan-fidelity) Two A.W4 gates were UNIT-substituted, not met as A.md specified — Playwright >50ms trace deferred and π/visual floor skipped — recorded honestly but they remain unverified at the browser level
    @ docs/tranches/A/waves/W4.md:149-154; FINAL.md:131 (π-skip), FINAL.md:210-212 (trace deferral)
    →B: BOOK → trigger: tranche B's perf/INP wave (B will have consumer-driven group composites via glass-ui/speedtest). Owner: keyframes author. Run bench/playwright.bench.ts as a real browser trace asserting the >50ms break, AND pair it with the named-forward LoAF observer (PerformanceObserver long-animation-frame) — together they give the yield a CONSUMER (the trace gate), clearing the overfitting precept that kept the observer unshipped in A. This is the single largest 'claimed-but-browser-unverified' surface A left open; B's INP lever should close it rather than re-substitute another unit test.
[medium] (boundary-invariants) dev-warn NOT stripped: console.warn plus full string survive in dist; esbuild.drop dead under Vite 8 rolldown minify
    @ vite.config.ts:187-189; easing-resolvable.ts:116-126; FINAL.md:50-51
    →B: SHIP B: a verified rolldown console-strip or guarded DEV block plus a dist-grep assertion; or revise the false claim.
[medium] (deferred-ledger) The 'A demo-polish' BOOK destination does not exist — three demo asides booked to a phantom home
    @ FINAL.md:229-231; constellation-grand-audit-2026-06-02.md:83-86,138,152 ('BOOK → A demo-polish')
    →B: Two paths, pick per B scope. If B touches the demo: SHIP → a real B demo-polish wave (φ-ladder rungs via glass-ui .text-display-*; unify --font-display/--font-serif to Instrument; drive scene-swap through glass-ui startViewTransition) with an actual wave id. If B is engine-only: BOOK with a REAL owner+trigger — owner: glass-ui adoption (rungs/VT are glass-ui-owned per the audit), trigger: next demo-touching wave in any consumer. Either way the phantom 'A demo-polish' string must be replaced with a concrete destination. Record the cube dead-center as KILL (already correctly classified; aesthetic, no defect).
[medium] (deferred-ledger) Zero-deferral declaration conflicts with P-invariant-28 — named-forwards ARE deferrals at close
    @ PROGRESS.md:54 ('A runs zero-deferral'); FINAL.md:200-235 §5 (LoAF NAMED-FORWARD, Playwright trace named-forward, Worker NAMED-FORWARD/KILL); SPEC.md:191 (P-invariant-28)
    →B: BOOK → B open ledger reconciliation. For each A named-forward, B must make the permanent-vs-temporary call P-invariant-28 requires: LoAF observer + Playwright >50ms trace are TEMPORARY (they have a clear landing condition — a wired bench consumer), so they are B-inheritable debt, not permanent archives. Worker/OffscreenCanvas is PERMANENT-ARCHIVE (substrate-without-consumer, genuinely no consumer). B's open docs MUST inherit the two temporary deferrals (SPEC.md:128 'receiving tranche's open docs MUST inherit the deferral'). Owner: keyframes B-author. Trigger: B open.
[medium] (deferred-ledger) LoAF observer is chronic (grand-audit → A.W4 → forward) and is the missing causal half of the unproven scheduler.yield() claim
    @ constellation-grand-audit-2026-06-02.md:55-61 §2.4; W4.md:144-148; FINAL.md:205-208; grep long-animation-frame|PerformanceObserver src/ demo/ → 0 hits (confirmed)
    →B: SHIP → B perf wave, co-landed with the Playwright trace (prior finding) so they break the circular deferral together: the LoAF observer becomes the bench gate's attribution source, giving each the consumer the other lacked. This is the gestalt move the user's directive wants — not two more isolated deferrals but one perf-evidence subsystem (observer + trace + assertion) landed as a unit. CHRONIC (2 cycles). Owner: keyframes engine. Trigger: B perf wave open.
[medium] (deferred-ledger) gh-pages CI deploy was REMOVED (node.js.yml retired) with no replacement — the demo now has zero automated deploy, and dev.sh/deploy.sh remain only-drafted
    @ FINAL.md:75-78,182-185; W1-boundary-and-ci.md:103; node.js.yml deleted in d84faf5 (-41 lines, confirmed git show); constellation-adoption-2026-06-02.md:47-102 §1 (dev.sh/deploy.sh BOOKED); scripts/ contains only proof-boundary.mjs (confirmed ls)
    →B: SHIP → B infra/CI wave. Land scripts/deploy.sh (the drafted library-SHAPE wrapper: gh-pages on master push, npm on tag — the adoption fold §1 already wrote the CONFIG) so the demo deploy is automated again, glass-ui-free, from a path that resolves. Co-land scripts/dev.sh (also drafted, also unshipped). The do_build() already points at build:lib (inv β). This closes the regression A's CI repair opened. Owner: keyframes infra. Trigger: B infra wave OR the cross-repo dev.sh rollout step, whichever lands first — do not leave the demo deploy manual-only across B.
[medium] (deferred-ledger) VAL-9 --spring-* token regen is correctly BOOK'd outward but is a cross-repo dependency B cannot close unilaterally — and A only landed the keyframes-side half
    @ FINAL.md:216-222 §5; constellation-grand-audit-2026-06-02.md:109-122 §4.2; W4.md (WAAPI linear() = LAND)
    →B: BOOK → keep outward (glass-ui owner) but FORMALIZE the handoff B is missing: if B touches the spring surface, author coordination/glass-ui.md per SPEC.md:19 naming the springLinearStops() export contract glass-ui's codegen consumes, the writer/reader boundary, and the drift-gate. Trigger: next glass-ui spring-token edit. Owner: glass-ui (token regen) consuming keyframes springLinearStops() (the mint). B's own move: ensure springLinearStops() is a stable public export with a versioned signature so glass-ui's codegen has a contract — that is the keyframes-side enabler still owed.
[medium] (precept-adherence) π-lane skipped via non-visual-delta argument, but the demo renders live engine scenes the reduced-motion path alters; the precept-named animation-timing probe was never run
    @ docs/tranches/A/waves/W5.md:14-66; FINAL.md:131; demo/app/scenes/{Cube,Amiga,Easing,Spring,Square}Scene.vue; precepts SPEC.md:234-251
    →B: BOOK→trigger: B inherits A's π re-probe as a NAMED close obligation (the precept makes a floor-run π verdict 'provisional', SPEC.md:243-247). Trigger: the first B wave that touches any engine play path with browser automation available. Owner: B close-ceremony π lane. The probe is concrete — toggle `prefers-reduced-motion` on the Spring/Cube scene, capture ≥5 animation-timing frames, assert the reduced-motion path renders the final frame (non-empty readPixels) and the normal path animates. This also discharges A's `AnimationGroup._playReducedMotion` reset()-repaint defect (caught by the pre-publish review, FINAL.md:139-145) with a runtime visual confirmation rather than a unit assertion.
[medium] (precept-adherence) Playwright >50ms-trace gate declared a HARD gate in A.md, downgraded to named-forward at close with unit-verification substituted
    @ A.md:31 (completion criterion) + A.md:64 (A.W4 wave-row closes-on); FINAL.md:211-212; W4.md:149-154; W5.md:135-137
    →B: BOOK→trigger+owner: fold the Playwright >50ms-trace gate into a B perf wave as a REAL hard gate (run `bench/playwright.bench.ts`, capture a trace, assert the per-frame composite long-task is broken under 50ms for a >YIELD_BATCH group). Trigger: B's first perf/INP wave. Owner: B perf-wave agent. Also pair it with the deferred dev-only LoAF observer (FINAL.md:206-208) — the observer gives the trace gate its causal measurement consumer, so the two ship together and clear the overfitting bar jointly (the LoAF observer's missing consumer was the sole reason it was named-forward).
[medium] (precept-adherence) Precept gap: the before/after-screenshot edict has no clause for headless-engine-with-rendering-demo, which let A self-certify a π-skip the demo contradicts
    @ precepts SPEC.md:216-251 (π visual-runtime lane); adoption fold constellation-adoption-2026-06-02.md:161-202; A.md:84 (spine REFUTES CWV/forms on 'engine renders no document')
    →B: Architectural transposition → BOOK to a precept amendment (owner: constellation precepts maintainer): add a third π disposition — 'library-with-rendering-demo' — that binds the demo's animation-timing probe when a library tranche changes any play/render path, distinct from both the floor and the docs-only skip. The trigger is 'the library's own demo renders the changed behavior.' For keyframes specifically, B should adopt this as the canonical π form (probe the five demo scenes' reduced-motion + WAAPI paths). This closes the gap A drove through and makes the before/after edict bind exactly the surface that proves a library tranche's runtime claim — the rendered demo, not the headless dist.
[medium] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Pull latest precepts + sync constellation — PLANNED-FOR-B (decision: pull latest + author edict, user pushes)
    @ git ls-tree HEAD docs/precepts → 63240e6; submodule HEAD → 63240e6 (in sync today); adoption fold §3 'nothing to bump today'
    →B: SHIP → B.W0 (precepts sync, paired with the edict). The act of authoring the edict IS the canonical-advance; after authoring, bump keyframes' gitlink to the new canonical and sync the constellation. User pushes (locked decision).
[medium] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Deploy 6 frontend-design agents to audit design + glass-ui — PLANNED-FOR-B
    @ User P2 'deploy 6 frontend-design agents in a workflow to audit design + glass-ui'; no design-audit artefact in docs/tranches/
    →B: SHIP → B.W-design (6 frontend-design-agent workflow). Fold in the grand-audit's already-booked demo-polish rows (constellation-grand-audit-2026-06-02.md:83-87,138-140) as the starting backlog so they are not re-discovered. Note glass-ui changes go in the glass-ui repo (memory: glass-ui root changes never patched in demo).
[medium] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: 'NOT an implementation phase — tranche development only' — constrains B authoring; CARRY
    @ User P2 'This is NOT an implementation phase — tranche development only'; mirrors A.md:5 dev/impl boundary
    →B: RECORD as a binding constraint on B's authoring. B's plan must mark its dev/impl boundary (W0-W1 dev: audit-fold + design + precepts edict; W2+ impl: deps, loading, e2e, design, validation — authored-now-run-on-authorization). Every impl wave above is PLANNED-FOR-B, executed only on the user's explicit go.
[medium] (prompt-recap (coverage of P1 + P2 + locked decisions)) A-internal deferral folding into B: Playwright >50ms-trace + LoAF observer
    @ FINAL.md:205-212; W4.md:142-154; PROGRESS.md:24 (LoAF + Playwright trace named-forward)
    →B: SHIP → B.W-validation (absorb the deferred Playwright >50ms-trace gate as a concrete probe) + BOOK the LoAF observer (trigger: the trace gate landing gives it a consumer; owner: B engine-perf wave). Fold both explicitly so A's only two internal deferrals are closed in B.
[low] (plan-fidelity) W4.md wave-spec describes the pre-fix group reduced-motion behavior that the multi-agent review explicitly reversed — stale spec contradicts shipped source
    @ docs/tranches/A/waves/W4.md:58-59 vs src/animation/group.ts:453-457,477-481
    →B: KILL → record. Do not author B against W4.md's stale group-snap prose. If B reuses the reduced-motion-snap pattern (likely for any new heavy play path), derive it from the SHIPPED group.ts:459-489 / engine.ts:762-772 invariant — child Animation.reset() clears flags only (engine.ts:841, no repaint) so it is safe, while group.reset() repaints to frame 0 (group.ts:537) so it is NOT. This child-vs-group reset asymmetry is the real, load-bearing contract and should be lifted into a precept/LESSONS-LEARNED note, not left buried in a code comment.
[low] (plan-fidelity) proof:boundary gate is real and bites, but ONLY on a reachable value.js binding — a dead `void _probe` import is silently tree-shaken away and passes
    @ scripts/proof-boundary.mjs:56-79; FINAL.md:103-105 / design/W1:120
    →B: SHIP → a B hardening wave (small). The gate is sound for its stated invariant (bytes-in-bundle) and should ship as-is. Optionally add a SECOND, complementary check that greps the light modules' SOURCE import statements for any `from "@mkbabb/value.js"` static specifier (not the bundled graph) — catching the dead-import-armed-regression class before it becomes reachable. Record the reachability caveat in the proof-boundary.mjs header so a future author doesn't read a passing gate as 'no light module imports value.js'.
[low] (boundary-invariants) Two ungated cross-seam contracts: css-easing global-Symbol tag and leaves.ts value.js byte-copy can silently drift
    @ internal/css-easing.ts:15; internal/leaves.ts:1-81; commit 470814e
    →B: BOOK B keyframes-engine: e2e WAAPI-spring-linear test across the chunk split; leaves-vs-value.js parity test or build-time generation.
[low] (boundary-invariants) Deferred: A.W4 50ms scheduler.yield Playwright trace gate not run; INP-relief unit-asserted only (asserted-not-gated)
    @ FINAL.md:210-212; A.md:64
    →B: SHIP B: land the 50ms trace gate in CI, or KILL-and-record with the unit test as documented authority.
[low] (deferred-ledger) ScrollTimeline native (animation-timeline:scroll()/view()) is chronic and the BOOK trigger may never fire — risks becoming a perpetual punt
    @ constellation-grand-audit-2026-06-02.md:87,140 §3; FINAL.md:231-233; src/animation/timeline.ts:179-194 (ScrollTimeline JS-polls window.scrollY via getScrollY, confirmed)
    →B: BOOK → but tighten the trigger so it is falsifiable. Either (a) KILL with rationale if B confirms no constellation consumer is scroll-linked (record it as permanent-archive per P-invariant-28, not an open named-forward), OR (b) SHIP a feature-detected native ScrollTimeline beside the JS fallback in B's perf wave as pure modern-web posture (it is Baseline-Newly, ≤20 LOC detect, and the JS-poll is the documented fallback) — this matches A.W4's own scheduler.yield() pattern and removes the unfalsifiable-trigger problem. Recommend (b): it is the gestalt, no-workaround move. Owner: keyframes engine. Trigger: B perf wave.
[low] (deferred-ledger) Doc-reality drift in the close ledger: W5.md says '13-test engine-modern-web' but the file has 15 (FINAL.md says 15) — small but it is the close record
    @ W5.md:28-30 ('the 13-test engine-modern-web.test.ts'); FINAL.md:130,133 ('15 tests'); test/engine-modern-web.test.ts (grep it(|test( → 15, confirmed)
    →B: BOOK → trivial reconciliation at B open (or a docs-fix commit): correct W5.md '13' → '15' so the close record is self-consistent. No wave needed. Owner: keyframes B-author. Trigger: B open doc-sync pass. Flagged because in a development product the close ledger is load-bearing and B inherits it as ground-truth.
[low] (precept-adherence) Internal doc drift: W5.md cites a stale '13-test engine-modern-web.test.ts' against the landed 15
    @ docs/tranches/A/waves/W5.md:27-28 ('W4 the 13-test engine-modern-web.test.ts'); FINAL.md:130,133 and W4.md:196 (both correctly say 15); test/engine-modern-web.test.ts (15 it-blocks verified)
    →B: KILL→record: cosmetic; fix W5.md:28 '13-test' → '15-test' if A's docs are touched in B's opening reconciliation, otherwise record and move on. No B wave needed. Worth noting only because B's opening wave will read A's wave specs as the load-bearing input audit, and a 13/15 mismatch is exactly the kind of drift the constellation's reconciliation lane re-derives.
[low] (precept-adherence) Spaced-em-dash style drift in tranche docs (~16 genuine mid-prose instances)
    @ docs/tranches/A/A.md (58 ` — ` hits, ~16 mid-prose); FINAL.md, W4.md, design/W1; precepts STYLE.md:83-87
    →B: KILL→record: the violation is minor and the prose is otherwise the strongest in the constellation's style register. Record the pattern; B's doc authors should run an unspaced-em-dash pass on their own tranche docs at authoring time (close the spaced ` — ` to `—` in mid-prose; leave heading dashes). Not worth a B wave; a STYLE-lint grep (`grep -nE '[a-z] — [a-z]'` excluding headings) at doc-close would catch it cheaply if B wants a tooling check.
[low] (prompt-recap (coverage of P1 + P2 + locked decisions)) A-booked adoption items folding into B: dev.sh/deploy.sh, screenshot archival, cruft sweep
    @ constellation-adoption-2026-06-02.md:251-262 (manifest, 5 BOOKED actions); W5.md:70-96; scripts/dev.sh ABSENT on disk; .gitignore already has .playwright-mcp/ + .DS_Store
    →B: SHIP → B (fold the un-landed bookings): dev.sh/deploy.sh into a B tooling wave; the dock-PNG archival + paired-π into B's first visual wave (which now ships pixels via the loading-screen removal, so the π-skip no longer applies). Record the two already-done (gitignore) as closed so the fold is honest.
[info] (plan-fidelity) A.md title claims '261→286' tests; real base is 260 (off-by-one) — FINAL.md is correct, the headline is not
    @ docs/tranches/A/A.md:1 (title) vs FINAL.md:133, PROGRESS.md:11
    →B: KILL → record. Trivial; no action beyond noting that A.md's headline numerics were not reconciled to the close. For B, treat FINAL.md (not the plan title) as the authority for landed test counts, and reconcile the headline number at B's close ceremony rather than carrying a known-wrong figure forward into B's baseline-bar prose.
[info] (plan-fidelity) Every resolved design decision and wave gate VERIFIED landed as specified — boundary hardening headline is real, not asserted
    @ src/animation/internal/easing-resolvable.ts; scripts/proof-boundary.mjs; .github/workflows/ci.yml+release.yml; src/animation/{engine,group,waapi}.ts; CHANGELOG.md; README.md:303-324
    →B: SHIP → carry forward as B's regression bar. The boundary (inv α via proof:boundary) and the glass-ui-free library build (inv β) are now hard gates that bind every B light-module edit — B must keep proof:boundary green and must run the clean-runner chain at its own π floor. The EasingResolvable + reduced-motion.ts + scheduler.ts + css-easing.ts internal/ modules are the proven net-deletion pattern (relocate, never alias) B should follow for any further de-duplication; no legacy shims were added, consistent with the no-legacy directive.
[info] (deferred-ledger) Worker/OffscreenCanvas/Atomics path is correctly classified KILL/permanent-archive — the one deferral with no B obligation
    @ A.md:104 §Folded-ledger; FINAL.md:213-215 §5; constellation-grand-audit-2026-06-02.md:142 §5
    →B: KILL → record as PERMANENT-ARCHIVE in B's open docs (not a named-forward). It carries no B obligation unless a real off-main-thread animation consumer appears in the constellation (speedtest canvas / fourier are candidates but neither has asked). If such a consumer surfaces, re-open as a fresh tranche item with that consumer as the trigger — do not pre-build. Owner: none (archived). Trigger: a wired OffscreenCanvas consumer, else never.
[info] (precept-adherence) Positive: changeset↔tranche reconciliation, no-legacy/no-alias, and overfitting precepts are honored cleanly
    @ FINAL.md:1-13,182-197; .changeset/ (consumed); CHANGELOG.md:1-30; src/animation/internal/ (6 modules); commits d84faf5→2105e7e
    →B: SHIP→wave: carry A's reconciliation pattern, net-deletion discipline, and the proof:boundary 'gate must bite' standard forward as B's baseline. B should keep the changeset-per-version-wave cadence and the FINAL-does-not-duplicate-CHANGELOG rule. The proof:boundary bite-verification (negative test turns it red, then revert) is the template every B gate should meet — no asserted gates.
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P1: Execute tranche A in full — ADDRESSED
    @ docs/tranches/A/PROGRESS.md:8-25 (all six waves 'landed'); FINAL.md:122-131 gate table; commit d84faf5
    →B: RECORD as DONE. No B action — this is the foundation B builds on. The full-A-execution ask is closed; do not re-litigate.
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P1: Publish 3.0.0 first — ADDRESSED
    @ package.json:3 (version 3.0.0); CHANGELOG.md:3 (## 3.0.0 Major); git tag v3.0.0; `npm view @mkbabb/keyframes.js version` → 3.0.0
    →B: RECORD as DONE. B's dep-update + loading-removal + design waves will require a NEXT release (3.1.0 minor or 4.0.0 if breaking) — name the version owner in B's close-ceremony wave so the publish leg is not orphaned.
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P1: Exported RAFPlayback PRM gate — ADDRESSED
    @ src/animation/index.ts:48 `export { RAFPlayback } from './playback'`; src/animation/playback.ts:36 class RAFPlayback; FINAL.md:158
    →B: RECORD as DONE. BOOK a cross-repo follow in B: glass-ui must actually adopt the exported gate (the constellation reason it was exported). Trigger: glass-ui's next /motion tranche; owner: glass-ui. Surface it so the export does not sit unconsumed.
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P1: Changesets + --provenance — ADDRESSED
    @ .github/workflows/release.yml:26 (id-token: write), :46-47 (`npm publish --provenance --access public`); .changeset/config.json; FINAL.md:73-74
    →B: RECORD as DONE. Carry forward unchanged into B's release wave — the provenance leg is now the standing publish path.
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P1: Gate on green CI — ADDRESSED
    @ .github/workflows/ci.yml:42-48 + release.yml:39-45 (check:lib → build:lib → test → proof:boundary); node.js.yml RETIRED (absent on disk)
    →B: RECORD as DONE. B's directive demands 'perfected CI' — treat A's chain as the baseline and EXTEND it in a B wave (lighthouse gate, playwright e2e gate, dep-audit gate), do not rebuild it. This is a SHIP→wave (CI-extension) not a fresh build.
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: 6-agent parallel deep audit of plan + waves + changes — PARTIAL (this engagement IS that audit)
    @ User P2 'DEEPLY audit with 6 agents in parallel'; this multi-dimension audit run (prompt-recap is one dimension)
    →B: RECORD this audit as the satisfying artefact for the 6-agent ask. The downstream obligation is to FOLD all six lanes' findings into B's plan before B.FINAL — name a B.W0 'audit-fold' wave that absorbs every lane's findings (mirroring how A.W0 folded the constellation grand-audit).
[info] (prompt-recap (coverage of P1 + P2 + locked decisions)) P2: Recap ALL prompts/requests + ensure addressed — THIS dimension satisfies it; surface drop-risks
    @ User P2 'Recap ALL of our prompts and requests and ensure they've been addressed'; this recap finding-set
    →B: RECORD this recap in B's W0 audit-fold. Carry the three drop-risk flags (edict, loading-screen, shadcn scoping) into B's plan as explicit named waves so the recap's coverage is provably closed at B.FINAL.