I have all the evidence I need. The occlusion and demo-smoke gates **only ever test the idle/controls-closed state** — neither clicks to open the controls pane, so the controls-open dock-over-content overlap state (acknowledged in the gate's own note at line 235: "the controls-open mobile state") is never actually gated. The changeset is cut (`tranche-b-3-1-0.md` → minor, matching FINAL). The hero file has post-B commits (W9-phase2 from a later tranche), so the B-era hero state I read is what B shipped.

I now have a complete, exhaustive picture. Let me compile the report.

---

# Tranche B — Gate-Fidelity Audit (lane: were B's hard gates met as specified, or diluted?)

**Summary:** B's engine wave (W2) and boundary/CI plumbing (W1/W6 boundary+lockfile-disclosure) are genuinely shipped and gated — proof:boundary is self-enforcing, 309 tests green, all 16 TODOs and 3 modules deleted. But the **demo-correctness invariants were materially weakened against their specs**: the occlusion gate's "zero dock-over-content overlap" was downgraded to an advisory console note; **W5's a11y/SEO hard gate (A11y=100, SEO≥90) is contradicted by B's own after-prod evidence (0 pages reach either)** yet marked MET; the **W5 φ-ladder/serif/cartoon-shadow gate was deferred while PROGRESS marks W5 done**; and **π ran at the screenshot floor, not full visual binding** (no animation-timing reduced-motion probe, no contrast measurement) despite W7 swearing the contingency "does not apply."

---

1. **[critical] W5 a11y/SEO hard gate asserted MET but B's own evidence shows it FAILED on every page** — W5.md §Hard gate: "lighthouse A11y = 100 (`landmark-one-main` + `image-alt` resolved) **and SEO ≥ 90**." FINAL.md gate table W5 row = "✅ MET"; PROGRESS.md:22 = "done … a11y … + SEO closed". But `audit/lighthouse/after-prod/_summary.json` — B's own authoritative artefact — records **A11y 75–98 (0 pages at 100), SEO 75–82 (0 pages ≥90, identical to the W0 75–82 baseline the wave existed to close)**. Every one of the 12 records still fails `landmark-one-main`; cube/amiga/square/easing fail `image-alt`; square fails `color-contrast`. Root cause on disk: `EditorShell.vue:36` ships `<main class="contents">` — `display:contents` removes the element from the box tree so axe/Lighthouse never registers the landmark. **Disposition: BOOK (C must re-open).** GESTALT fix: drop `class="contents"` from `<main>` (or use a real flex/grid wrapper that keeps it in the box tree); add the missing `alt`/`aria-label`/`button-name` on the scene-control widgets; add per-route `<title>`/meta for SEO; then re-assert the gate against fresh lighthouse — and correct FINAL.md/PROGRESS.md, which presently propagate a known-false "closed" into C's baseline.

2. **[high] Occlusion gate's "zero dock-over-content overlap" (inv δ) silently downgraded to ADVISORY** — B.md inv δ + W3 §S6 + W6 §δ all specify "zero dock-over-content overlap"; the W0 schema (`occlusion-report.json`) carried a real `dockOverlaps[]` array, and W6 §δ says the gate "fails when … any `dockOverlaps[]` is non-empty." Shipped `scripts/occlusion-gate.mjs:147–156` computes `dockOverlapsSubject` (and only for the subject's *focal-center point*, not any overlap) and then **never adds it to `failures`** — it is printed as a console note at lines 233–239 ("expected for a full-bleed scene or the controls-open mobile state"). The dock-over-content class the user named as their #1 demand is therefore unenforced. **Disposition: BOOK.** GESTALT fix: restore the dock-overlap as a hard assertion, but resolve the genuine tension honestly — define a per-scene "floating-dock-allowed" manifest flag so a full-bleed canvas legitimately permits an edge-floating dock while the cube/hero target still fails on a dock covering its content rect (not just its center point).

3. **[high] π lane ran at the SCREENSHOT FLOOR, not full visual binding — the exact contingency W7 swore did not apply** — W7 §S3 + Hard gate #2: "animation-timing samples (≥5 frames spanning the named duration) on every state-toggle transition the tranche modified … assert the reduced-motion path renders the FINAL frame (non-empty `readPixels`) … contrast-vs-background ≥ WCAG AA"; Design decision #3: "the wave-spec does NOT carry a tooling-contingency floor, because the contingency does not apply." Evidence on disk: `screenshots/after/` = 18 PNGs + `_capture-report.json` (only `{page, viewport, consoleErrors:[]}`); **no animation-timing probe artifact, no `readPixels` reduced-motion final-frame assertion, no contrast measurement exists anywhere**. FINAL.md W7 row = "✅ MET". The π lane was discharged as screenshots + console-error capture + lighthouse — precisely the floor A took and B was the receiving tranche obligated to lift. **Disposition: BOOK.** GESTALT fix: add the ≥5-frame `prefers-reduced-motion` animation-timing probe on the Spring/Cube scene (assert final-frame paint, the runtime confirmation of W2's rest-position contract) and a contrast sweep on the W5 surfaces; or, if deferred, FINAL must record π as PROVISIONAL (not MET) so C inherits the obligation explicitly rather than as a false-closed gate.

4. **[high] W5 φ-ladder + single-serif + cartoon-shadow hard gate DEFERRED while W5 marked "done" and inv-counted** — W5 §Hard gate: "`grep` confirms **zero raw display rungs on the hero**; one display serif resolved in the token system (**no `.instrument-serif` alias residue**); the cartoon-shadow ad-hoc CSS migrated." Shipped reality: `EditorStartScreen.vue:6,22,28` still carries `text-6xl lg:text-8xl`, `text-5xl`, `text-2xl` + the `instrument-serif` alias; **58 `instrument-serif` occurrences across 25+ source files**; `--font-display` was never set to Instrument Serif (only the pre-existing `--font-serif` override remains — S2 unfulfilled); `CSSCodeEditor.vue:6` still hand-rolls `border-gray-700 shadow-[4px_4px_0px_0px_rgba(0,0,0,0.6)]`. FINAL.md § Deferrals openly says these are "DEFERRED to a demo-polish follow-up" — but **PROGRESS.md:22 marks W5 "done" with "text-display-*/--type- adopted; one display serif"** and FINAL.md's W7 overfitting/ι table treats W5 as closed. The gate was diluted: marked-done ≠ gate-met. **Disposition: BOOK (honest re-scope).** GESTALT fix: either land the φ-ladder/serif/cartoon-shadow migration to actually meet the W5 hard gate, or downgrade PROGRESS/FINAL to record W5 as "a11y/SEO + correctness attempted; φ-ladder migration DEFERRED" so the gate table stops asserting a gate that was not met.

5. **[high] Neither inv-γ nor inv-δ gate ever exercises the controls-OPEN state — the one state where docks genuinely cover content** — `occlusion-gate.mjs` and `demo-smoke.mjs` `goto` each route, wait, and probe the **idle/controls-closed** layout only; neither clicks to open the controls pane or transport. The gate's own note (line 235) names "the controls-open mobile state" as a place a dock floats over the subject — and then exempts it. W3 §S5 ("dock exclusion zones … wherever a scene element reaches the top/bottom ~64px band it is occluded") is structurally addressed via `--work-area-top-offset` (present, `style.css:25`), but it is never *gated* in the state where it matters. **Disposition: BOOK.** GESTALT fix: parametrize the occlusion gate over a `{controls: closed|open}` axis so the dock-exclusion-zone invariant is proven in the state it was authored to protect, not only in the trivially-clear idle state.

6. **[medium] The before/after capture harness is NOT checked into the repo — the edict's "re-runs identically" contract is unfulfillable** — W7 §S1 + Design decision #1 + B.md §Resolved-decision-5 bind the edict to "a single Playwright/Chrome-MCP harness **checked into the tranche's audit dir** so it re-runs identically at open and close." The 18 BEFORE + 18 AFTER PNGs and both `_capture-report.json` files exist, but **no capture harness script exists anywhere in the tree** (`scripts/` holds only the three gate `.mjs`; nothing under `audit/` is executable). C cannot re-run the identical harness; the "true diff" guarantee is asserted, not deliverable. **Disposition: BOOK.** GESTALT fix: check the capture harness into `scripts/` (or `audit/`) so the before/after edict B authored into precepts is actually reproducible — the precept B minted is otherwise un-executable by its own first adopter.

7. **[medium] inv β shipped as the FALLBACK disposition (b) the spec reserved "only if (a) breaks the demo," with no recorded contingency trigger** — W6 §Hard gate #4 + Design decision #4 RESOLVED to disposition **(a) "regenerate genuinely glass-ui-absent … the no-legacy choice,"** with **(b) "reserved strictly for the case where regenerating absent destabilizes the `file:` demo dev install — a contingency, not the plan."** Shipped: `package-lock.json:995–996` records `node_modules/@mkbabb/glass-ui → resolved: ../glass-ui` (glass-ui-PRESENT, optional-skip) = **disposition (b)**. This is openly disclosed in FINAL.md inv β and the `ci.yml` header (so it is *honest*, unlike findings 1/3/4), and (b) is technically inside the gate's accepted OR-clause. But the no-legacy-preferred (a) was abandoned without recording the contingency that (a) actually broke the demo install. **Disposition: RECORD (disclosed weakening).** GESTALT fix: either complete (a) (regenerate absent, the no-legacy artefact the mandate prefers) or have FINAL explicitly state *why* (a) was infeasible (the `file:` demo dev-install breakage) so the downgrade from preferred to fallback is justified, not merely permitted.

8. **[medium] FINAL.md W4 perf headline cherry-picks desktop-home/cube and presents it as the gate result** — FINAL.md W4 row: "prod perf measured (desktop FCP ~1.0 s, LCP ~1.3 s, **perf 89-96**)". B's own `after-prod/_summary.json` shows the full distribution is **perf 49–96**: mobile is 49–64 (home 63, amiga 49, square 62, easing 61), and **spring-mobile FCP 15.3 s / LCP 28.5 s** (spring-desktop perf 63). W4 §Hard gate #6 required "FCP and LCP each bounded by an explicit ceiling on **both mobile + desktop**." Presenting "89-96" as the perf verdict hides that the mobile half and both spring viewports are far below. **Disposition: RECORD.** GESTALT fix: FINAL should state the honest range (perf 49–96, the mobile/spring floor named) and either set a real ceiling B met or BOOK the mobile/spring perf for C — the headline must not read as a uniform 89-96 when the artefact says otherwise.

9. **[low] Scene-swap `<Transition mode=out-in>` was DROPPED in W3 (acknowledged), eliminating the cross-scene VT the W5 fold named** — DELTA.md:30–33 records the four blank scenes were fixed by "dropping both wrappers" (`<KeepAlive>` + `<Transition mode=out-in>` around the keyed `<Suspense>`/`defineAsyncComponent`, which never triggered the async loader). W5 §Folds lists "scene-swap VT" as one of the demo-polish BOOKs W5 was "the home that BOOK pointed at." The drop is **acknowledged** (DELTA names it as the root-cause fix) and correct (the wrapper was *causing* the blank-scene blocker), but the scene-transition polish itself is now silently gone with no re-entry in FINAL § Deferrals. **Disposition: RECORD.** GESTALT fix: note in FINAL that the scene-swap transition was removed-as-defective (not migrated) so C does not expect a VT that no longer exists; if a polished scene transition is still wanted, BOOK it with the async-loader-safe pattern (transition the resolved component, not the `<Suspense>` boundary).

10. **[low] proof:boundary, the engine transposition, and the W1/W2/W6 structural gates are GENUINELY MET — ship as-is** — Contrary to the dilution pattern above, the engine/boundary lane holds under scrutiny: `proof:boundary.mjs` is self-enforcing (entry set PARSED from the barrel, per-entry value.js+engine negative coverage, dynamic-chunk presence assertion, source-grep complement — all four W2/W6 assertions present and real); `grep TODO( src/animation` = **0**; the 3 claimed-deleted modules (`easing-resolvable.ts`, `internal/css-easing.ts`, `renderer.ts`) are gone with no alias; `withReducedMotion`/`settle`/`reset`/`restPosition`/`paintRest`/`parseOption`/`AnimationOptionError`/`resolveEasing` all exist and are distributed across the play paths as specified; the strict-options/resolve-easing/reducedMotion/waapi-lifecycle/leaves-parity test suites are real; **309 tests pass** (the 260→309 claim verified by execution); `release.yml` + `ci.yml` both carry `actions/checkout@v5`/`setup-node@v5` (W6 hygiene #5 met). **Disposition: SHIP.** No fix — this is the part of B that did what it said; it is the contrast that makes the demo-lane dilutions (findings 1–5) legible as the locus of the gate-erosion.