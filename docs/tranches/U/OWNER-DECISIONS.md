# Tranche U — OWNER-DECISION REGISTER

> Decisions requiring (or pre-carrying) the owner's ruling. Defaults are the
> synthesis recommendation; a default marked PRE-RATIFIED is read directly off the
> 2026-07-09 edict's plain text and executes unless overruled. Everything else
> awaits the ruling before its wave closes.

| # | Decision | Default (synthesis RECO) | Status |
|---|---|---|---|
| OD-U1 | **How far does the CI trim go?** Lane 07's trimmed ~40-gate product roster vs lane 09's three-mechanism dissolution (`npm test` + `proof:publish` + owner-golden review; product invariants fold INTO vitest). | The THREE-MECHANISM model — a genre deletion, not a count shave. "Trimmed substantially / most tautological" reads as apparatus dissolution; a 227→120 shave would re-run T's mistake. | PRE-RATIFIED (edict) — scale confirmation requested |
| OD-U2 | **`demo/@/` → `demo/shared/` + dissolve `components/custom/`** (T.F1/F2, reversing S.D4's "keep @/"). | EXECUTE as U.B's keystone first move. S.D4's premise was false (zero source files import a literal `@/`); the owner's "#26 half-baked" verdict + this edict falsify the keep. | PRE-RATIFIED (edict) |
| OD-U3 | **The per-push Linux demo-correctness browser job dies; deploy-of-record re-gates.** What replaces the gating: library-green + the last nightly-green demo ancestor SHA (lane 08's design), with on-device pre-push as the fast lane. | Adopt lane 08's design. The runner is owner-ruled superfluous; the browser roster moves to nightly + on-device. | PRE-RATIFIED (runner) — gating redesign needs the ruling |
| OD-U4 | **glass-ui pin: tilde → caret within the major** (the tilde froze the tree at 4.0.1 while 4.1/4.2 shipped; every tripwire probes the installed dist = vacuously green). | Caret within the BG/BH major + the tripwire re-architected to probe dist-tags.latest. Supersedes the PIN-LEDGER "tilde never caret" note. | NEEDS RULING (reverses a recorded posture) |
| OD-U5 | **Monaco slimmed to editor.api + css contribution** — 8.0MB of ts/html/json workers (~48% of shipped JS) is dead weight; the demo edits CSS only. | SLIM, with a born-RED gate that fails if any non-css language worker reaches dist. | RECO-DEFAULT (low risk, huge win) |
| OD-U6 | **presets: 34 presets × 4 parallel hand-lists → ONE data table** (code generated or derived at module init). | DATA. The four lists cannot drift once they are one table. | RECO-DEFAULT |
| OD-U7 | **Tests: mirror or colocate?** The edict says colocation everywhere; lane 26's R5 rules tests MIRROR (test/<area>) and never colocate into src/ — colocated tests would ride into shipped-package globs and fight the lazy chunk graph. | MIRROR, ratified as an explicit edict CARVE-OUT with the rationale recorded — befitting-for-the-language per the edict's own abstraction clause. | NEEDS RULING (an edict carve-out must be owner-blessed) |
| OD-U8 | **Version at close**: the exports rationalization (CJS-era `main` dropped), the dead-export excisions, and the surface collapse are potentially breaking. | Decide AT CLOSE from the actual surface delta: 6.0.0 if any published symbol/entry changed, else 5.3.0. | DEFERRED-TO-CLOSE (by design, not a deferral row) |
| OD-U9 | **The two scene genres** (four instrument scenes with full specimen grammar vs cube/amiga with none): design the telemetry-whisper register for the 3D scenes, or bless designed silence as a codex rule. | Fable + frontend-design designs BOTH options in U.G; the owner picks in the review loop (the T lesson: critic consensus ≠ owner verdict). | NEEDS RULING (in-loop) |
| OD-U10 | **Approve the NEW standalone `proof:scripts-colocated` (U.A9)** — the backend-colocation twin of `proof:colocation`. Anti-sprawl rationale: the standing gate replacing the DELETED per-file colocation genre, so the flat `scripts/` pile cannot re-form. | RECO-APPROVE — the anti-sprawl covenant's owner sign-off for a new standalone gate; it replaces a whole deleted genre (net DOWN). | NEEDS RULING (anti-sprawl sign-off) |
| OD-U11 | **Approve the NEW standalone `proof:chunk-graph` (U.D6)** — the dist-artifact gate (a class no source gate can see). Anti-sprawl rationale: born-RED on the 906KB spring→vendor-highlight leak — the structurally-blind gate class the source-shape roster cannot see. | RECO-APPROVE — the anti-sprawl covenant's owner sign-off; born-RED on a measured dist defect, one of the two new standalone gates U charters. | NEEDS RULING (anti-sprawl sign-off) |
