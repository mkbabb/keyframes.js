# Tranche U — OWNER-DECISION REGISTER

> Decisions requiring (or carrying) the owner's ruling. **The 2026-07-10 ruling
> batch (OWNER-ASKS row 3, verbatim) resolved or re-scoped most rows** — each row
> below records the ruling where one landed. A default marked RECOMMENDED awaits
> a ruling; RULED rows are terminal.

| # | Decision | Ruling / default | Status |
|---|---|---|---|
| OD-U1 | **How far does the CI trim go?** | **RULED (2026-07-10): "The trim is substantial. Total reimagining. KISS. Ensure no loss of functionality. We must get this sped up by at least an order of magnitude."** The three-mechanism dissolution executes (`npm test` + `proof:publish` + owner-golden review), with TWO hard exit criteria added to U.A: (a) zero functionality loss — every real invariant the deleted gates asserted is folded into vitest/proof:publish, witnessed by a fold-map; (b) **≥10× wall-clock**: the merge-gating CI path drops from ~35-40 min to ≤4 min. | **RULED** |
| OD-U2 | **The demo root + component structure.** | **RULED (2026-07-10, two parts).** (1) The component CORE is redesigned to the glass-ui post-BH idiom (the audit: `audit/glassui-idioms-post-bh.md`) — every component a kebab-case dir (PascalCase SFCs + `index.ts` barrel + `constants.ts` + per-component `composables/`), types through the barrel, >500L renderers carved into composables; `components/custom/` dissolves. (2) On the literal `@` dir the owner then asked "Why even have this @ — is this a shadcn idiom? … We'd like to have some sort of consistency": ANSWERED — it IS a shadcn-scaffold vestige (shadcn's `@/` is an ALIAS to the src root; the scaffold materialized it as a real directory). glass-ui has NO on-disk `@` (3-plane `@glass` alias only); value.js's demo carries the same vestigial `demo/@`; both library `src/` trees are clean. REVISED RECO for consistency: DISSOLVE `demo/@/` — hoist its five children to `demo/{components,composables,state,styles,utils}/` (the glass-ui root shape, no `shared/` wrapper either), keeping the existing `@components/@state/…` alias SPELLINGS pointing at the new homes, declared in all three planes (tsconfig + vite + vitest — the glass-ui discipline); a letter note to value.js's active tranche for its twin. Toolchain integrity is an acceptance clause. | **RULED — @ dissolution CONFIRMED 2026-07-11 ("Ratify all"); keystone step ① ungated** |
| OD-U3 | **The per-push Linux demo-correctness browser job dies; deploy-of-record re-gates** on library-green + last nightly-green demo ancestor (lane 08's design), on-device pre-push as the fast lane. | Adopt lane 08's design — subsumed under OD-U1's total reimagining + 10× target. | PRE-RATIFIED (runner) — folded into OD-U1 |
| OD-U4 | **The glass-ui consume edge.** | **RULED (2026-07-10): "Glass-ui has a forthcoming 5.0.0 release. We use this."** Supersedes the tilde→caret question entirely: U.F re-targets the consume edge to glass-ui **5.0.0** — the pin moves on its publish, the five workaround caps re-probe against the 5.0.0 dist, the BG/BH letter items reconcile against what 5.0.0 actually ships, and the tripwires re-arm on the 5.0.0 edge. | **RULED** |
| OD-U5 | **Monaco.** | **RULED (2026-07-10): keep Monaco unless an alternative is TRULY proven superior AND slimmer — no compromises. The demo wants the fully-featured editor: prettier on demand, language support, theming.** U.D5 re-charters from "slim to css-only" to **ON-DEMAND DELIVERY of full Monaco**: nothing on first paint, everything available when the editor opens — language workers loaded per-language on demand, prettier as an on-demand chunk, themes lazy. The 8MB problem is a *delivery* defect (eagerly emitted, never fetched), not a feature-set defect. An alternatives bake-off (CodeMirror 6 etc.) may be RECORDED but replaces Monaco only on proof of strict superiority + slimness. | **RULED** |
| OD-U6 | **presets: 34 presets × 4 parallel hand-lists → ONE data table.** | DATA — the four lists cannot drift once they are one table. | RECOMMENDED (unopposed) |
| OD-U7 | **Tests: mirror or colocate?** | **RULED (2026-07-10): "Tests stay in their own dir."** The `test/<area>` mirror is terminal; recorded as the edict's befitting-for-the-language carve-out. | **RULED** |
| OD-U8 | **Version at close.** | **RULED (2026-07-10): "5.3"** — 5.3.0, which BINDS U to a compatible published surface: U.C's exports rationalization, dead-export excisions, and transport verbs must land additively/internally (no published symbol or entry removed or re-shaped). The U.C waves flagged "potentially breaking" carry this as a hard constraint; anything truly breaking is out of U's scope. | **RULED** |
| OD-U9 | **Scene readouts** — four scenes carry on-stage instrument readouts (live labels/numbers: curve name, spring values, position); the 3D scenes (cube, amiga) show only the object. | **RULED (2026-07-10): "ratify those 3d scenes getting idiomatic instrumentation."** Option (a) executes: U.G designs the idiomatic instrument register for cube + amiga (Fable + frontend-design, owner-reviewed mocks — one direction, no both-ways fork). | **RULED** |
| OD-U10 | **`proof:scripts-colocated`** (a new standalone gate policing the `scripts/` restructure). | **RULED (2026-07-10): "sounds overfit junk — unless this is truly well designed."** DROPPED as a standalone gate. The `scripts/` family restructure still executes (U.A9); its shape is held by the EXISTING colocation gate gaining one clause + lint — no new bespoke gate. If the drive later shows a one-clause extension cannot hold it, the redesign returns HERE for sign-off rather than shipping. | **RULED — DROPPED** |
| OD-U11 | **`proof:chunk-graph`** (the after-build check that a scene chunk downloads only its own code — the spring scene currently drags in a 906KB syntax-highlighting chunk it never uses). | **RULED (2026-07-10): "Both new proof: items sound like junk"** — DROPPED as a standalone gate. The DEFECT still cures (it is the owner's own complaint: "Why does spring bring in so much"): the fix is structural (OD-U12) and ONE post-build clause inside `proof:publish` (per-chunk weight budget + no-heavy-vendor-reach) holds the line — zero new gates. | **RULED — DROPPED (defect cures structurally)** |
| OD-U12 | **The scene-facet loading model (owner-stated, 2026-07-10 verbatim):** "They should only bring in monaco when the keyframes editor is shown. We should not have an inline monaco editor for spring — each scene, if befitting, should leverage our multi-control view of controls (whatever those are for that scene), keyframes (if befitting), etc (cube has matrix controls, for example)." | The ruling IS the design: every scene composes the shared multi-facet instrument (controls / keyframes-if-befitting / scene-specific facets like cube's matrix); heavy editors (Monaco, highlight) load ONLY when their facet is SHOWN — never inline in a scene, never eagerly in a facet module graph. The spring inline-editor import is the named violation; U.B executes the facet seams, U.D the delivery. | **RULED (verbatim design authority)** |
| OD-U13 | **The broken amiga animation + scene suspend/resume facilities** (owner, 2026-07-10: "broken for many tranches … investigated from first principles and fixed within this tranche"). | The first-principles defect investigation is RUNNING in U-dev (dossier → `audit/defect-amiga-suspend-resume.md`); the fix waves fold into the corpus with the dossier as evidence; the impl drive lands them IN U. A vacuous-green exhibit for RC-1 (the gates said green throughout). | **RULED (mandated in-U)** |
| OD-U14 | **The compositing/stacking/layering assay** (owner, 2026-07-10 verbatim: "The primary issue with our animations was that the compositing and stacking and layering. Ensure that's assayed."). The C14 leaf-staleness bug lives in exactly this seam (the group projection ↔ interpolator hand-off) — the owner names the WHOLE compositor/stacking/layering system (the replace/add/weighted blend fold, z-order, the SoA compositor, multi-animation stacking on one target, layer config) as the primary animation defect class. | A 3-lane first-principles assay runs in U-dev (semantics vs documented behavior · a live probe harness over the stacking matrix · the architecture critique of the layering model itself); its dossier becomes U.C charter substance beside C14 — the fix waves land IN U. **ASSAY COMPLETE (3 dossiers, 2026-07-10 — `audit/assay-compositor-{semantics,behavior,architecture}.md`):** the C14 freeze is one of a 3-instance stale-leaf class; `add`/`weighted` collapse to the base layer at the first segment boundary (demo-reachable via `LayerConfigPanel`); blending is unit-blind + colour-blind; `weighted` non-normalized; no group WAAPI path. **Chartered:** U.C14 widened (owned `CompositeState`), U.C3 re-chartered (`Renderer` seam + declarative stack), U.C15 (one op axis `replace\|add\|accumulate` + orthogonal normalized weight), U.C16 (group WAAPI lowering). | **RULED (mandated in-U) — ASSAY COMPLETE** |
| OD-U15 | **ALL CLAUDE.md files deprecated and removed TOTALLY** (owner, 2026-07-10) — the documentation lives inline (docstrings at the code it describes) or briefly/deftly in the README. | The removal executes in U: the three CLAUDE.md files' load-bearing content is inventoried (the convergence loop's research step), each piece re-homed inline-or-README, then the files DELETE (with every gate that reads them — claude-paths-live, claude-structure-sync class — dying in the same motion; glass-ui's own 5.0.0 plans the identical B4f "delete CLAUDE.md" act, so the constellation converges). | **RULED** |
| OD-U16 | **Module granularity, BOTH directions** (owner, 2026-07-10): long files break into module dirs (the named example: `compile/easing-option.ts` → `compile/easing/`); "absurdly small modules should be abrogated for superfluity and instead made inline". | The convergence loop assays EVERY library + demo module against both bounds — a granularity RULING per module (carve / keep / inline-and-delete), converged to 100%. The small-module inlining is as binding as the long-file carving. | **RULED** |
| OD-U17 | **Suppression files removed** (owner, 2026-07-10, named: `.dependency-cruiser-known-violations.json`). | The violations FIX (or the rule honestly re-scopes) — suppression-by-ledger dies; the loop's research step inventories every suppression surface (depcruise known-violations, lint disables, cspell words, api-extractor overrides) for the same treatment. | **RULED** |
| OD-U18 | **The 5-step convergence loop IS U's development methodology** (owner, 2026-07-10, verbatim in OWNER-ASKS row 6): research(≤8∥) → synthesis(1) → prototype fleet (worktree-isolated, sometimes concrete impl) → critique fleet (each item scored % convergence + critical analysis) → agglomerator → loop; at 100% convergence the exact wave sets develop. | Ratified; Track A (spec coherence) + Track B (module restructure) run it from 2026-07-10. Prototypes are EVIDENCE (kept worktree branches), never merged — the S scene-stage discipline; the owner review sits inside the loop (the S.E lesson: critic consensus ≠ owner verdict). | **RULED** |
| OD-U19 | **ONE component home** (owner, 2026-07-10, screenshots attached): demo components are scattered across `@/components/custom/`, `@/skeletons/`, `app/`, and `app/dock/` — "wildly confusing"; `app/dock/` as a component home "needs to be straight up extirpated"; the wrapper stack (`@/components/custom/instrument/...` = 5 levels before content) collapses. | The perfected module structure gives demo components EXACTLY ONE home with the sub-component pattern (keyframes/-style, owner-blessed) as the recursive unit; app/ keeps only the shell (router/machine bindings), never components. The loop's pass 3 specs the unified home; the target tree lands in SPEC-B3. | **RULED** |
| OD-U20 | **The meta/legacy file class DELETES** (owner, 2026-07-10, named: `demo/glass-ui-gaps.ts`): in-tree meta-ledgers, workaround registries, and legacy artifacts die. | glass-ui-gaps.ts deletes (its live workaround caps either FIX on the glass-ui 5.0.0 re-pin (OD-U4) or become plain code comments at the workaround site; the tripwire machinery was already U.A-condemned); the loop's pass 3 sweeps for the full class (meta-ledgers, decision JSONs, registries) with per-file delete/re-home verdicts. | **RULED** |
| OD-U21 | **Demo-library duplication EXCISES from the demo** (owner, 2026-07-10): "likely a great deal of duplication in functionality between the demo and the library". | Pass 3 runs the duplication census: every demo utility/composable/math/parse path that re-implements library (or value.js) functionality is named with its library equivalent and excised — the demo DOGFOODS the library (the lane-19 num()/toRGB findings were the first instances; the census generalizes). | **RULED** |
| OD-U22 | **The ride-queue ratification + impl authorization** (owner, 2026-07-11: "Ratify all. Prepare for compaction and tranche execution."). | All four rides discharged: DESIGN.md KEEP (U.G codex home) · @-dissolution CONFIRMED · D1 value.js-canonical (23 rows adopt bezierPresets; 6 quart/quint = documented delta pending D-GAP-1; curve delta = a recorded correction) · D5 oklab blessed. **THE U IMPL DRIVE IS AUTHORIZED, beginning post-compaction**, executing the PASS-5 ratified wave-set order from the frozen evidence. | **RULED — IMPL AUTHORIZED** |
| OD-U23 | **Terminal anti-contrivance and sibling scheduling ruling** (owner, 2026-07-15). | The incomplete 204-key apparatus is not grandfathered. Delete the flat proof/meta population, red-classification devices, posture helpers, orphan baselines, and meta registries; retain only the package-boundary bundle and owner-golden oracle because each protects a real consumer/human boundary. Product behaviors live in Vitest or the six direct nightly observations. The vacuous recap script is abrogated in favor of a one-shot terminal record review. Current sibling asks are explicitly owner-re-deadlined—not silently deferred—to Glass 6.0, value 4.0/Keyframes 6/Glass 6, and Atlas's next published surface, with exact artifact packets required before later consumption. | **RULED — EXECUTED AT U CLOSE** |
