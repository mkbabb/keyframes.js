# Tranche U — PROMPT-RECAP-U (the born-at-entry, TREE-cleared total ask ledger J..U)

> **Status: DEVELOPMENT. Implementation NOT authorized.** This is the ENTRY artifact:
> every row is populated at U's authoring with its *current* status read off the LIVE
> tree (`tranche-u-dev`, 5.2.0), and each row is **updated at the close of the U wave
> that addresses it** — never reconstructed at close (the T.M9 freshness discipline;
> lane 03 F1's failure mode made structurally impossible). A drive pre-empted at any
> point still has a real, current recap.
>
> **The transposition this ledger IS (lane 01 §1).** Every recap A→S measured
> **letter** (did a wave/mechanism land) and bolted on a **spirit column that
> self-certified by citing a band** (→ S.G / → S.D / → S.E — the three bands the owner
> then rejected on sight). "The anti-leak column certified the leak." T's recap cured
> half of this — its spirit column cites an owner token, never a band. **U cures the
> other half: a status may clear only against an owner-observed token OR a TREE WITNESS
> (a `file:line` the owner can read at first `ls`), never against a wave/band mapping,
> and never against a green source-shape gate for a design/appearance/interaction ask.**
> The mandate has been "chartered" 9 consecutive times and rejected 9 times; U stops
> re-chartering and starts CLEARING each clause against the tree at the exact bar the
> owner uses.
>
> **Gate binding.** The falsifiable teeth are `proof:prompt-recap-u` (**U.R2** —
> supersedes `proof:prompt-recap-t` by rename+retarget, net gate count FLAT). This file
> is the **artifact** it binds; born ONCE, at entry, here (U.R1). The gate refuses any
> ADDRESSED row that cites a band/wave or a green design-gate as its oracle, keeps the
> re-issued mandate honestly auto-RED until an owner/tree clearance, and requires
> `OWNER-ASKS.md` fully-dispositioned.

---

## The spirit-column schema (the token vocabulary)

Every ask's **spirit-status** ∈ exactly one of:

| Token | Meaning | Falsification (`proof:prompt-recap-u`) |
|---|---|---|
| `OWNER-APPROVED shot:NN` | An owner (or owner-proxy live-review) observed the U-rebuilt surface and blessed it | requires a committed owner token; a design row flipping here without one REDs |
| `TREE-WITNESSED file:line` | A structural/hygiene ask cleared against a tree fact the owner can read directly (the "read the tree at first `ls`" oracle, lane 03 rec 7) | requires a live `file:line` or gate-of-CORRECTNESS citation; an ADDRESSED row with no witness REDs |
| `PENDING-OWNER` | The U cure is not yet built + owner-reviewed | a row stuck here at close of its owning wave REDs |
| `RE-ISSUED → auto-RED` | A verbatim-re-issued precept, auto-RED until a post-re-issuance owner/tree clearance | the entry state of every §1 mandate clause |
| ~~`→ Band` / `→ Wave`~~ | **FORBIDDEN as a spirit oracle** | an ADDRESSED row citing a band/wave, not an owner token or tree witness, REDs |
| ~~green gate~~ | **FORBIDDEN as a spirit oracle for a design/appearance/interaction ask** | REDs (lane 26 "green was the defect") |

At **entry** (this artifact) **nothing is `OWNER-APPROVED`**: no U surface has been built
or owner-reviewed. Every design/appearance ask carries the owner's *rejection* token or
`PENDING-OWNER`; every standing-mandate clause carries `RE-ISSUED → auto-RED`. That honest
zero-approval baseline is the anti-leak instrument.

---

## §0 — The shape of the total ask corpus (lane 01 §0)

The S recap already consolidated **287 owner messages / ~114 distinct authored prompts**
(`S/audit/prompt-recap.md:12`). Across J→U the corpus resolves into two strata:

- **The immutable standing mandate** — a 7-clause block first captured at `J/J.md:111-119`,
  re-issued VERBATIM through K, L, M, O, P, Q, R, S, T, and now **re-issued a ~10th time in
  U's own prompt** (`U/ORIGINAL-PROMPT.md:14-16`). Every recap marks it "ADDRESSED
  (chartered)"; **the re-issuance itself is the falsification** — a precept spirit-met to the
  owner's bar would not need restating. A verbatim re-issue **auto-REDs** its ADDRESSED claim
  (§1).
- **The per-tranche feature/correction asks** — mostly ADDRESSED-and-terminal at their
  tranche. The residue that OUTLIVES its tranche is what U owes (§2/§4).

**The single load-bearing fact:** U's prompt is not a tranche of new asks — it is the owner
re-issuing the same mandate for the ~10th time PLUS two escalations (CI-trim, grand-restructure).
**The recurrence is the finding.** U must stop chartering the mandate and clear it against an
owner-observable bar.

---

## §1 — The standing 7-clause mandate: letter vs current tree (the recurrence ledger)

The mandate is in its **~10th** verbatim re-issue. **None may be marked ADDRESSED until a
post-2026-07-09 owner observation OR a live tree witness clears it** — the recap gate enforces
this automatically. The **U clearing oracle** column is the transposition lane 01 §1 demanded:
stop re-chartering; bind each clause to the owner-observable gate/witness at the owner's bar.

| Clause | Re-issue count J→U | Tree witness (current) | Spirit-status | U clearing oracle |
|---|---|---|---|---|
| NO quick solutions / NO workarounds — idiomatic gestalt | 10× (`U/ORIGINAL-PROMPT.md:14`) | hand-roll census not re-run post-T; T named survivors (`useToolbarKeyboard`, `KfPillTabs`) | **RE-ISSUED → auto-RED** | a standing hand-roll census (U.C/U.E — every hand-rolled primitive enumerated with a named idiomatic replacement, RED until zero); the `Kf`-vanity de-vanitized (U.B) |
| architectural transposition for elegance/simplicity/**performance** | 10× (`:14`) | perf now the "grand edict"; VERDICT #19 "performance god awful"; render seam ~95% of tick cost untouched (lane 21) | **RE-ISSUED → auto-RED** | ONE owner-anchored headless-failable perf floor (U.D — the render-seam transposition + a vitest-runnable allocation/throughput ratio, NOT per-scene source-shape gates the owner rejected) |
| NO legacy code | 10× (`:16`) | 50 "legacy" markers are removal-narration, no live legacy tier; but the apparatus IS the legacy (RC-1) | **RE-ISSUED → auto-RED** | the apparatus dissolution (U.A) + tranche-tag archaeology purge (U.E — provenance lives in docs/); the back-compat barrel re-chartered or dissolved |
| delineate + fold chronic/deferred items | ×2 in U (`:18-20`) | 8 live `T_BORNRED_BACKLOG` rows (`scripts/gate-bands.mjs:609`); external tripwires deferred-by-design | **RE-ISSUED → auto-RED** | every backlog row terminally adjudicated in-U or converted to a deadlined external covenant (U.E + U.F) — NONE carries to V |
| Recap ALL prompts — ensure addressed | 10× (`:22`) | T built `PROMPT-RECAP.md` + ran `proof:prompt-recap-t` (first time); THIS artifact is U's maintained re-execution | **RE-ISSUED → auto-RED** | THIS ledger under `proof:prompt-recap-u` (U.R), born-at-entry, updated-per-wave, TREE-cleared — never a once-per-tranche re-derivation |
| This is NOT an implementation phase | every dev-tranche (`:24`) | U is dev-only; the recursion (dev tranches keep shipping rejected impls) is itself the finding | **ADDRESSED (posture)** — U honors it | the corpus IS the deliverable; impl awaits explicit owner authorization (charter §5) |
| batches of 3 · Fable+frontend-design for ALL design · Opus/Sonnet fanout | 10× (`:26,59`) | orchestration spec restated; shipped surfaces historically skipped per-page Fable+owner pass | **RE-ISSUED → auto-RED** | the owner-golden review loop (U.G — all design routed Fable+frontend-design; owner review is a wave-closure precondition, the T.M2 rule made standing) |

**Census verdict:** 6 of 7 clauses are verbatim re-issues auto-RED; the 7th (dev-only) is
addressed by posture. The mandate does not clear by mapping to a band — each clause clears only
when its named U oracle greens AT THE OWNER'S BAR and (for design/perf) an owner token lands.

---

## §2 — THE OPEN-RESIDUE (what OUTLIVES its tranche — U's actual debt)

### ROW 1 — the T #26 GRAND-COLOCATION overstatement (lane 03 F-1/F-2) — the keystone residue

> **This is row 1 by the band brief: the T-verdict residue is the ledger's lead entry.**

T verdict #26 ("demo/ re-structure from first-principles — the GRAND COLOCATION EDICT") is
marked **LANDED · `proof:colocation` KEYSTONE ⑩** in `T/FINAL.md:44`. Against the live tree it is
**OVERSTATED**: the band's two ROOT structure waves never executed, and the keystone gate was
shaped to pass without them.

| Fact | Tree witness (verified live) | Verdict |
|---|---|---|
| `demo/@/ → demo/shared/` (T.F1, "every move below rides its alias repoint") | `demo/@` EXISTS; `demo/shared` ABSENT | **NEVER RAN** |
| dissolve `components/custom/` (T.F2) | `demo/@/components/custom` EXISTS (holds ALL the good `instrument/` work) | **NEVER RAN** |
| the styles path the owner named ("demo/@/styles — what the fuck is this?") | `demo/@/styles/` persists verbatim | **NEVER RAN** |
| `proof:colocation` the KEYSTONE oracle | `proof-colocation.mjs:50` `SHARED = ["demo/@","demo/shared"].find(exists)` — tolerant; DEFERRED map (`:70-89`) holds 3 live uncured violations yet the gate exits 0 | **GREEN ON A REJECTED TREE** — the S root-cause reproduced inside T |

**Spirit-status: `OWNER-REJECTED shot:2026-07-04 → U.B`.** The good invisible work (SceneFacility,
the `instrument/` fold, the scene prunes, the file-size discipline) is genuinely in-tree — but
nested one level under the two un-dissolved vendored-legacy wrappers (`@`, `custom`), so the owner
opens `demo/`, sees them, and correctly reads "not restructured." **U clearing oracle:** U.B
executes F1/F2 as DELETIONS (not moves) + re-forges `proof:colocation` from tolerant to REQUIRING
(it must assert the hoisted `demo/{components,composables,state,styles,utils}/` homes EXIST and
`demo/@` + `components/custom` do NOT — the OD-U2 dissolution shape; the
DEFERRED map emptied by landing its moves; born-RED on any residual). Cleared only by a
post-restructure owner review of a tree readable at first `ls` (lane 03 rec 7).

### The remaining open residue (each row owned by a U wave, TREE-witnessed)

| # | Ask (origin) | Tree witness | Spirit-status | U home |
|---|---|---|---|---|
| 2 | perf "from the ground up" (#19) is BLOCKING but the oracle exits 0 headless | `proof-perf-counters.mjs:186-187` `process.exit(0)` when unmeasurable; the CI trim removes the browser tier it rides | `OWNER-REJECTED → U.D` | U.D headless-failable ratio floor + render-seam transposition |
| 3 | the `Kf`-vanity surface the owner derided (#18 "KF? Pills?") | `demo/@/components/custom/.../KfPillTabs.vue` + `useKfPillTabs.ts` still in tree behind an external-blocked defer | `OWNER-REJECTED → U.B` | de-vanitize now over glass-ui primitives; NO-legacy is binding |
| 4 | CI trim (U escalation 2a) — "runner superfluous, most tautological" | 227 `proof:*` keys (the U.A ground-truth census) vs `ROSTER_CEILING = 120` (`gate-bands.mjs:595`); `proof:roster-ceiling` a permanently-RED backlog row (`:665`) | `PENDING-OWNER → U.A` | the three-mechanism dissolution (npm test + proof:publish + owner-golden); the permanently-RED ceiling pattern DELETED |
| 5 | grand restructure widened to "**all backend files too**" (U escalation 2b) | `scripts/lib/` shared vs per-gate colocation unaudited; library ~80% compliant with two long-flat zones (lanes 15/16) | `PENDING-OWNER → U.A/U.B/U.C` | colocation as standing drift-RED enforcement across demo + library + `scripts/`; long-file god-modules disproven (the R-flagged-cosmetic `decomposition` overrides re-checked) |

---

## §3 — U's two escalations (the genuinely-new asks, lane 01 §2)

- **CI TRIM (`ORIGINAL-PROMPT.md:8`) — UNADDRESSED at entry.** 227 `proof:*` keys against
  `ROSTER_CEILING = 120`; `.github/workflows/ci.yml` a 60KB/152-step monolith; the owner calls
  most of it tautological, and the permanently-RED ceiling row proves the boards already agree.
  **Gestalt cure (not a shave to 120 — that is the forbidden workaround):** classify all keys into
  CORRECTNESS (CI) / HYGIENE (local) / DESIGN-OWNER (owner-golden, not CI-gateable); collapse the
  tautological families into their single surviving oracle; DELETE the "declared ceiling that never
  converges." → **U.A**.
- **THE GRAND RESTRUCTURING / recursive colocation (`:28-36`) — PARTIALLY.** The demo IS
  substantially colocated internally, the library zone-partitioned; but the owner re-issued it in U
  (auto-RED) AND widened it to "all backend files too." **Gestalt cure:** colocation as *standing
  enforcement* over demo + library + `scripts/`, drift REDs; the two root wrappers deleted (see §2
  row 1). → **U.B/U.C** (+ U.A for the backend half).

---

## §4 — The chronic / external residue the owner ordered FOLDED (NO MORE DEFERRALS, lane 01 §3)

`ORIGINAL-PROMPT.md:18-20` terminates the honest-defer device. "No more deferrals" cannot delete
the external reality that value.js/glass-ui ship on their own cadence — but it DELETES the
*tripwire-as-terminal-state* pattern. Each row exits U either cured in-U or as a **deadlined
external covenant with a named upstream producer** (never an open tripwire); NONE rides to V.

| Deferred item | Tree witness | Class | Spirit-status | U home |
|---|---|---|---|---|
| KF-7 `PropertyDescriptor_2` d.ts collision | present in BOTH `dist/keyframes.d.ts` + `dist/engine/index.d.ts` at value.js 3.1.0; `proof:no-collision-rename` born-RED | EXTERNAL (value.js) | `PENDING → U.F` | §E of `KF-TO-VALUEJS-U.md`; discharge on the value.js rename |
| glass-ui backdrop-compositor cost (BG-5) | `proof:blur-not-resampled` born-RED (`gate-bands.mjs:609`); glass-ui pinned `~4.0.0` freezes tree at 4.0.1 while 4.1/4.2 shipped | EXTERNAL (glass-ui) | `PENDING → U.F` | `KF-TO-GLASSUI-U.md` absorb-or-expire deadline; the glass-ui 5.0.0 re-target (OD-U4 — supersedes tilde-vs-caret); the pin moves on the 5.0.0 publish; tripwire → `dist-tags.latest` |
| dock de-blur / morph continuity (GU-1/GU-2) + dock z-inversion | `proof:{subject-legible,dock-rest-crisp,dock-morph-continuity,dock-zorder}` external tripwires | EXTERNAL (glass-ui) | `PENDING → U.F` | folded into the ONE glass-ui letter |
| `T_BORNRED_BACKLOG` (8 live rows incl. `roster-ceiling`, `stage-inventory`, `blur-not-resampled`, `subject-legible`) | `gate-bands.mjs:609` | mixed | `PENDING → U.E/U.A/U.F` | each row cured in-U or converted to a deadlined covenant; the ledger dissolved at U.Z |

---

## §5 — parse-that: "has parse-that been driven?" (`:38`) — **ADDRESSED (certified clean)**

**Zero** `@mkbabb/parse-that` specifier in `package.json` or `src/` (only removal-narration
survives — `src/animation/compile/parse-flatten.ts:128`). The direct dependency was removed at
Q.DM-5; kf reaches the CSS grammar through value.js's own `parseCSSSubValue`. parse-that stands at
1.0.0, transitive-only via value.js 3.1.0. **The dep-drive is genuinely done — one line, no band**
(lane 01 §4; lane 28 A1–A4). `TREE-WITNESSED parse-flatten.ts:128` (removal narration only).

---

## §6 — The per-tranche terminal asks (RECORDED so U does not re-open them, lane 01 §5)

ADDRESSED-and-terminal against the tree — U must NOT re-open: icon re-instantiation (H); demo
title "keyframes.js" (H); auto-deploy revival (`deploy-pages.yml workflow_run`, T.S6); the SoA
compositor perf transposition (P.W2/Q.WF2); the constellation publish spine (parse-that 0.11→1.0,
value.js O→3.x, kf 4.4→5.2); the 24/28 T live-review verdict items genuinely LANDED
(`T/FINAL.md:24-57`, cross-checked lane 03 — 24 REAL, #26 overstated, #19/#28-CI shadowed);
`MotionPath`/`MorphSVG`/`DrawSVG` LIBRARY factories survive the OD-1 demo-scene prune; the
scene-stage / N-Stage switcher SHELVED by owner taste verdict (**terminal — do not resurrect**).
These are the ADDRESSED mass; the ledger records them so U's scope stays the residue, not the
whole history.

---

## §7 — OWNER-ASKS disposition mirror (`U/OWNER-ASKS.md` — ALL rows, grows at the event)

| # | Ask (operative) | Disposition |
|---|---|---|
| 1 | THE DISSOLUTION EDICT (in full) | OPEN — this corpus is the response; the 32-lane fleet ran; parse-that answered (§5) |
| 2 | "Why is this taking so long?" — the close-out CI serialization | ANSWERED in-session: ~40-min runner round-trips × fail-fast masking; the same pathology is RC-1 / U.A's charter |
| 3 | THE RULING BATCH (2026-07-10) | EXECUTED into the register: OD-U1/U2/U4/U5/U7/U8/U10 RULED; the corpus amendment propagated (U.A/U.B/U.C/U.D/U.F) |
| 4 | THE MID-TURN RULING BATCH II (2026-07-10) | EXECUTED: OD-U2 revised (`@` dissolution reco), OD-U9 RULED, OD-U10/U11 DROPPED, OD-U12 (facet loading model) + OD-U13 (amiga/suspend-resume) registered |
| 5 | The compositing/stacking/layering assay | EXECUTED: OD-U14 registered; assay COMPLETE → dossiers at `audit/assay-compositor-{semantics,behavior,architecture}.md`; folds into U.C (C14 widened, C3 re-chartered, C15/C16 new) |
| 6 | THE CONVERGENCE-LOOP MANDATE (2026-07-10) | EXECUTED: OD-U15..U18 registered; Track A (spec coherence) + Track B (module restructure) deployed; the OD-U15/U16/U17 wave sets develop at 100% convergence (§10 tracks them) |

With every row carrying a real disposition, `OWNER-ASKS.md` is fully-dispositioned — the recap
gate's disposition clause can green. This mirror covers ALL current rows and grows at the event
(the U.R2 clause (v) census bites on every row, not a frozen two).

---

## §8 — The recurring-correction-shape register (promote a rejection pattern to a standing precept)

A **rejection pattern that recurs ≥2×** is promoted from N discrete per-feature rulings to ONE
standing precept with its own gate (the T.M10 / lane 28 F4 mechanism, carried forward).

| Correction shape | Recurrences | Promoted precept | Gate |
|---|---|---|---|
| **a green source-shape gate certifies a tree the owner rejects on sight** | S root-cause #1 (three bands green + rejected); then INSIDE T at #26 (`proof:colocation` tolerant, green on `demo/@`+`custom/`); the perf oracle exits-0 headless (#19) — **≥3× and recurring inside the tranche built to kill it** | *a structural/appearance gate must FAIL on the rejected structure; a keystone that cannot fail on the tree the owner rejected is retired — assert the canonical name EXISTS and the rejected name does NOT, never a tolerant `.find(exists)`* | `proof:colocation` re-forged REQUIRING (U.B) + this ledger's clause forbidding wave/band and green-design-gate oracles (U.R2) |
| **owner rejects on taste what the process passes on convergence** | N-Stage ×4 → S.E scene-stage SHELVED → the whole S impl demo (2026-07-04) — carried from T's register | *critic-convergence + a green born-RED oracle is necessary but NOT sufficient to close a design wave; a committed owner (or owner-proxy live-review) token is required* | the owner-golden review loop (`proof:owner-review-gate`, surviving the apparatus dissolution — ring-fence 3; U.G) |

---

## §9 — Per-wave update protocol (born-at-entry → updated-per-wave)

This artifact is the entry state. The discipline that keeps it honest (`proof:prompt-recap-u`'s
binding):

1. **A design/appearance/interaction row** flips `OWNER-REJECTED → OWNER-APPROVED shot:NN` **only**
   when its owning U wave closes **with a committed owner (or owner-proxy live-review) token** —
   critic-convergence + a green gate is necessary but **not sufficient**. No band, no green gate,
   stands in for the token.
2. **A structural/hygiene row** flips `PENDING-OWNER → TREE-WITNESSED file:line` when its owning
   wave lands AND the fact is readable directly in the tree (the "first `ls`" oracle) — a green
   source-shape gate alone does not clear a row the owner observed; the witness must be a tree fact
   or a CORRECTNESS gate under the U.A three-mechanism set.
3. **A §1 standing-mandate clause** flips off `RE-ISSUED → auto-RED` **only** on a post-2026-07-09
   owner observation OR a live tree clearance — never on a band/wave mapping.
4. **The ledger is updated at the event**, appended to U's board (`PROGRESS.md`) under the T.M9
   freshness discipline — never reconstructed at close. This makes the lane-03-F1 failure mode
   (recap scheduled last, never reached; keystone gate green on the rejected tree) structurally
   impossible for U.

**Entry-state summary:** 0 rows `OWNER-APPROVED`; the §2 residue rows `OWNER-REJECTED → U-wave` or
`PENDING-OWNER`; 6 standing-mandate clauses `RE-ISSUED → auto-RED` (1 posture-addressed); the
external residue folded into deadlined covenants. That honest zero-approval baseline is the
anti-leak instrument.

---

## §10 — Mid-U owner asks (arriving DURING U — the S.Z1/T.§8 mechanism carried forward)

Owner asks that land mid-development are recorded here at the event with their spirit-status token,
mirroring `OWNER-ASKS.md`. Per §9 clause 4 the row is appended when the ask arrives, never
reconstructed at close.

| # | Date | Ask (verbatim headline) | Spirit-status (token) | U home |
|---|---|---|---|---|
| 1 | 2026-07-10 | THE RULING BATCH ("The trim is substantial. Total reimagining. KISS. … 5.3 …") | RULED — OD-U1/U2/U4/U5/U7/U8/U10 registered terminal | OWNER-DECISIONS.md; propagated U.A/U.B/U.C/U.D/U.F |
| 2 | 2026-07-10 | RULING BATCH II ("Why even have this @ … ratify those 3d scenes … amiga … suspend/resume … fixed within this tranche") | RULED — OD-U9/U12/U13 registered; OD-U10/U11 DROPPED; OD-U2 revised | U.B13/U.C14 (amiga), U.G3 (3D instrumentation), U.B11/U.D (facets) |
| 3 | 2026-07-10 | "The primary issue with our animations was that the compositing and stacking and layering. Ensure that's assayed." | EXECUTED — OD-U14; assay COMPLETE (3 dossiers) | U.C14/U.C3/U.C15/U.C16 |
| 4 | 2026-07-10 | THE CONVERGENCE-LOOP MANDATE (delete ALL CLAUDE.md; granularity both directions; suppression files removed; the 5-step loop to 100% convergence) | `PENDING-OWNER` — OD-U15..U18 registered; Track A + Track B deployed; the exact wave sets develop at 100% convergence (each ruling either lands as a wave set in-U or carries an explicit owner deferral token at close — U.Z2) | U.E7/U.E8 (CLAUDE.md + suppression), U.C7/U.C8 + the loop (granularity), U.md §2 U.L |
