# Tranche U — PROMPT RECAP (terminal record)

> **Status: CLOSED.** This file began as U's born-at-entry J→U ask ledger.
> The entry-state language below is preserved as history; it is not current
> authorization or an active backlog. The former `proof:prompt-recap-u` script
> was deleted at close because it could pass while rows still said
> `PENDING-OWNER`. The terminal review is direct, owner-readable, and bound to
> tree facts instead of a markdown-policing meta gate (OD-U23).

## Terminal amendment — 2026-07-15

The terminal owner ruling changed the recap mechanism, not the obligation to
recap. U therefore closes each operative ask here against a tree witness, an
owner decision, or an explicitly named sibling release boundary.

| Standing mandate | Terminal disposition | Witness |
|---|---|---|
| No quick solutions or workaround-shaped closure | **CLEARED.** The demo/library restructuring and direct defect cures landed; the remaining component-level Glass work is not disguised as local completion and is owner-re-deadlined to Glass 6. | canonical `demo/{components,composables,state,styles,utils}` homes; `src/animation/`; `KF-TO-GLASSUI-U.md` |
| Architectural transposition for elegance, simplicity, and performance | **CLEARED.** Transport/compositor/spring/preset and delivery work landed; the Vite backend was split into cohesive plugins instead of adding wrappers. | `src/animation/`; `scripts/build/vite/`; `vite.config.ts` |
| No legacy code | **CLEARED.** All tracked `CLAUDE.md` files, suppression ledger, flat proof population, red-classification machinery, capability registries, and orphan proof assets were deleted or re-homed. | `git ls-files '*CLAUDE.md'` = 0; `find scripts -name 'proof-*.mjs'` = 0 |
| Delineate and fold chronic/deferred items | **CLEARED FOR U.** Internal rows were cured or deleted with their subjects. External rows have named producers and immutable artifact boundaries: Glass 6.0, value 4.0→Keyframes 6→Glass 6, and Atlas's next published surface. Nothing is silently inherited by a Keyframes U/V ledger. | `KF-TO-GLASSUI-U.md`; `KF-TO-VALUEJS-U.md`; OWNER-ASKS row 9 |
| Recap all prompts and verify they were addressed | **CLEARED BY THIS TERMINAL REVIEW.** The nine current owner rows are mirrored below. | `OWNER-ASKS.md`; `OWNER-DECISIONS.md` OD-U1..U23 |
| Development-only posture | **CLEARED HISTORICALLY.** The corpus remained docs-only until the owner explicitly authorized implementation on 2026-07-11. | OWNER-ASKS row 8; OD-U22 |
| Batches of three, specialized design review, and deep orchestration | **CLEARED.** The five-pass convergence loop produced the ratified wave order; implementation used staged fan-out and direct sibling-root handoffs. The 14-frame owner-golden authority, including sequence light/dark, is blessed. | `loop/PASS-5.md`; `docs/tranches/T/goldens/BLESSED.json` |

## U escalations and open-residue reconciliation

| Ask / residue | Entry state (historical) | Terminal state |
|---|---|---|
| T #26 grand-colocation overstatement | `demo/@` and `components/custom` existed behind a tolerant gate. | **TREE-CLEARED.** Both wrapper concepts are absent; canonical demo homes are visible at first `ls`. |
| CI trim | 227 census keys; 204 still present when the terminal owner audit reopened U.A. | **TREE-CLEARED.** `package.json` has 18 scripts and exactly two `proof:*` commands: `proof:publish` and `proof:owner-golden`. A total of 182 flat proof files were removed; no root `scripts/proof-*.mjs` remains. |
| Backend restructuring | `scripts/` and the 747-line Vite config still carried flat/inlined backend concerns. | **TREE-CLEARED.** The useful publish and visual mechanisms live under `scripts/gates/`; six direct browser observations live under `scripts/observe/demo/`; Vite plugins live under `scripts/build/vite/`; `vite.config.ts` is 392 lines. |
| Behavior lost when source-shape scripts died | Shape checks and Vitest were frequently doubled. | **TREE-CLEARED.** Product behavior lives in Vitest. The sequence transport event contracts were folded into `test/orchestration/sequence-transport.test.ts`; browser truth is the six direct observations. |
| Performance oracle could green vacuously | Headless/source-shape machinery could certify without exercising the product. | **CLEARED BY MEASUREMENT AND PRODUCT TESTS.** Apply-path, composition, delivery, and scene behavior are exercised in the normal test/build/observation paths; no expected-red or exit-zero backlog class remains. |
| Sequence visual authority | Sequence light/dark were absent from the inherited 12-frame matrix. | **OWNER-APPROVED.** The 14-entry blessing includes sequence-light `4f590c97…` and sequence-dark `e9c3e414…`; both committed golden images match those hashes. |
| parse-that drive | Questioned at entry. | **TREE-CLEARED.** Keyframes has no direct package/source consume edge; parse-that remains transitive through value.js. |

## External covenant disposition

External ownership is recorded honestly; it is not converted into a local
tripwire or a claim that unpublished sibling bytes are consumable.

| Producer | Current immutable consumer state | Owner-redlined next boundary |
|---|---|---|
| glass-ui | Keyframes consumes exact Glass 5.0.0 with one physical core. | Glass 6.0.0 artifact packet after BG-5 and Dock-z; then a registry-only Keyframes successor with optional Glass exactly 6.0.0 and a fresh one-core witness. |
| value.js | Keyframes consumes the published 3.1.0 subpath taxonomy. | value 4.0 artifact, then the coordinated Keyframes 6 → Glass 6 rail for timing/plain-vars/diagnostics/unit/property-descriptor work. |
| Atlas / sci-report | The current coherent published tuple remains intact. | Atlas 2.0 waits for the post-Glass-6 Keyframes successor; no worktree link or unpublished import is treated as a release. |

These are owner-re-deadlined producer commitments, not `PENDING-OWNER` rows and
not permission for Tranche V to invent a new Keyframes deferral apparatus.

## OWNER-ASKS disposition mirror

| # | Operative ask | Terminal disposition |
|---|---|---|
| 1 | The dissolution edict in full | **EXECUTED** — direct implementation, apparatus collapse, restructuring, performance and constellation records landed. |
| 2 | “Why is this taking so long?” | **ANSWERED AND CURED** — serialized fail-fast meta CI was the cause; the merge/release workflows now invoke the direct product mechanisms. |
| 3 | First ruling batch: total trim, component idiom, Glass 5, Monaco, tests, 5.3, reject new proof junk | **EXECUTED** through OD-U1..U11 and the implementation tree. |
| 4 | Second ruling batch: dissolve `@`, instrument 3D scenes, facet loading, amiga/suspend-resume | **EXECUTED** through OD-U2/9/10/11/12/13 and the U.B/U.C/U.D/U.G implementation. |
| 5 | Compositing/stacking/layering assay | **EXECUTED** — three dossiers informed the owned composite state, vocabulary, and WAAPI work. |
| 6 | Five-step convergence loop; remove CLAUDE/suppressions; assay granularity both ways | **EXECUTED** — five passes, ratified PASS-5 order, deletion/fold work replayed into U. |
| 7 | Structure escalation, one component home, delete meta files, excise duplication | **EXECUTED** — canonical homes, direct dogfood, and terminal meta deletion landed. |
| 8 | Ratify all; begin implementation | **EXECUTED** — OD-U22 authorized the post-compaction drive. |
| 9 | Coordinate active siblings; favor KISS/direct code; abrogate contrived gates; visually verify | **EXECUTED** — root handoffs queued, 204→2 command collapse completed, and sequence goldens reviewed/blessed. |

## Historical entry-state record

At U authoring, six of the seven standing clauses were deliberately marked
`RE-ISSUED → auto-RED`; the development-only clause alone was posture-cleared.
The entry ledger also carried `PENDING-OWNER` rows for CI dissolution, backend
restructure, the T #26 residue, visual blessing, and external dependency work.
Those tokens describe the 2026-07-09 entry tree only. They are superseded by the
terminal tables above and must not be read as live status.

The methodological finding survives the deleted recap script: a sweep is a
measurement; a measured refutation amends the charter; every number is verified
on the current tree; and real residue graduates honestly to a named product
clause or producer boundary instead of to a policing ledger.
