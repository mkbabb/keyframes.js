# PASS-2 — Track B convergence loop, agglomeration record (step 5, pass 2)

**Date:** 2026-07-10 · **Authority:** OWNER-ASKS row 6 (verbatim mandate) + OD-U15..U18
· **Inputs:** SPEC-B2, the six iterated worktree prototypes (branches
`worktree-wf_ca7d0632-287-{10,11,12,16,17,18}`), the pass-2 critique reports (step 4),
and — where a critique was absent or void (§2) — the agglomerator's own direct
verification against the trees. Prototype branches remain EVIDENCE, never merged
(OD-U18). Plain language throughout; each term of art is glossed at first use
(agglomerator ruling 6, binding).

---

## 1. The convergence table

| Item | Pass-1 | **Pass-2** | Δ | Basis | One-line verdict |
|---|---:|---:|---:|---|---|
| P1 — compile-easing-carve | 55% | **95%** | +40 | critique (independently ran the full gate roster) | All three chartered moves live and green — the `backward/`→`emit/` rename, the entry/view-transition move-in, the re-key correction stated, the template in-tree; docked ONLY for three fresh stale prose references to the pre-move path and the template's missing doc/prose site class. |
| P2 — small-module-inline-sweep | 35% | **100%** | +65 | critique (all gates/tests/build re-run) | **EXITS.** The full charter surface: constants delete + both G12 gate retirements in ONE atomic commit, all four demo executables green, all six KEEP flips measured and recorded, fold-map in-tree. Nothing remains on the charter. |
| P3 — claudemd-fold | 72% | **100%** | +28 | critique ABSENT → agglomerator ran step 4 directly (§2) + cross-checks from the P6 and P1 critiques | **EXITS.** The 42-claim fold-map committed (32 HOMED / 10 DROPPED-for-cause), the barrel-inventory fork DECIDED (option b, argument recorded), the three home-less docstrings written and accurate, the demo re-home real, the README single-owner split honored to the byte (diff = exactly the two gate-required edits). |
| P4 — known-violations-fix | 95% | **96%** | +1* | critique (gates/build/tests re-run) | All four deliverables discharged against the FULL §8.4 surface (*pass-1's 95% covered deliverable 1 only — the surfaces differ, so the small Δ understates the gain): cogency distilled, `_diff/` premise refuted against the tree, vjL2 probe re-grounded on the real contract, the any-ceiling slice 99→86 with the census behind §9 Q5's answer. Two honest-form edges keep it under 100. |
| P5 — demo-component-recut | 84% | **100%** | +16 | critique VOID (placeholder — §2) → agglomerator verification, end-to-end | **EXITS.** Rename landed (zero `ControlSuite` survivors), G13 applied (10 reflexive types file-local; the no-dead-export ratchet 26→16, gate PASS), both chartered verdicts in-tree (P5-BOUNDARY.md + ChromeDock.vue prose), the 473L host scored KEEP on a real Hidden-Components argument, the `progress` edge measured DEAD and deleted whole (ruling 9 ratifies the supersession). tsc + 1050 vitest + gh-pages + demo gates all green, verified. |
| P6 — readme-redesign | 88% | **95%** | +7 | critique (both README gates run live) | The three chartered reconciliations done — the §Structure sentence tracks P3's ACTUAL landed fork decision byte-exact, the boundary reduced to a one-line pointer, the drag2D sentence absorbed verbatim; docked for ONE fresh factual error (`internal/` "shared across the light zones" — the import graph says 6 HEAVY + 2 LIGHT zones consume it). |
| SPEC-B2 (step-2 synthesis) | 85% | **92%** | +7 | critique (all 8 rulings traced to the tree) | All eight pass-1 rulings folded, several IMPROVED (the re-key correction, G11/G12/G13 as general law, the §0 glossary); docked for a thin residue of the imprecision it exists to kill: one stale line-ref, two loose counts, and LIGHT/HEAVY missing from its own glossary. |

**Overall pass-2 convergence: 97%** (mean of the seven scored items; pass 1 was 73%).
Three items are at 100 and EXIT; four carry small, mechanical residues. The loop
iterates ONE more pass — a **close pass**, not a rework pass.

---

## 2. The two critique anomalies, adjudicated (binding)

Two of the seven step-4 inputs failed the loop's own record standard and could not be
consumed as-is:

- **The P5 critique was a PLACEHOLDER** (analysis: "test"; gaps: "a", "b"; a bare 95%).
  A number with no verifiable analysis behind it is not a score. **Ruled VOID — the
  95% is discarded, never inherited.** The agglomerator re-ran step 4 directly against
  worktree `…287-17`: rename grep (zero survivors), the G13 type-locality sweep (zero
  `export interface/type` in either trio `composables/` dir; no laundering barrel exists
  — §9 Q7 answered NO), `proof:no-dead-export` PASS at 16 rows (26→16 confirmed),
  `proof:scene-control-dfa` PASS, `proof:demo-no-oversize` PASS, `npm run check` clean,
  vitest 1050 passed / 2 expected-fail / 15 skipped, `npm run gh-pages` built, the
  in-tree artifacts read in full, the dead-`progress` claim verified at its terminal
  reader (`TimingFunctionPanel.vue` contains zero `progress` reads; `ChannelOptions`
  receives exactly `animation` + `layer-config` and emits `layerConfigUpdate` — the
  zero-shared-edge claim holds precisely), and the claimed line counts exact
  (473/334/383). The 100% above rests on that verification, not on the void critique.
- **P3 received NO critique at all.** The agglomerator ran step 4 directly against
  worktree `…287-12`: all three CLAUDE.md files deleted; the README diff against the
  base commit is EXACTLY the two gate-required edits (3 insertions / 5 deletions — the
  dead-pointer strips + the drag2D LIGHT sentence); the three chartered docstrings
  exist and are accurate (`format.ts:1`, `morph.ts` composes-NumericAnimation contract,
  `playback.ts` export rationale); the demo re-home is real (`demo/DESIGN.md`
  §Structure orientation tree; `app/scene/scenes.ts:1` six-row scene table); four
  spot-checked HOMED-(prior) fold-map rows are TRUE at their cited anchors
  (`numeric-plan.ts:1`, `useAnimationSync.ts:5`, `gestureSelectSuppression.ts:1`,
  `group/group.ts` managed-child block); the drag2d born-RED comment overstatement is
  distilled (commit 319a8933); every surviving `CLAUDE.md` string in the tree is
  deliberate past-tense narration ("U's claudemd-fold DELETED…"), not a live pointer;
  `proof:readme-paths-live` PASS; `npm run check` clean. Two sibling critiques
  independently cross-checked P3's fork decision and README diff (P6's) and its
  doc-tree state (P1's). The 100% rests on this composite verification.

**Standing rule from this pass (ruling 10 below):** a step-4 critique that is not
self-contained — placeholder analysis, unverifiable gaps, or absent entirely — is VOID;
its number is never inherited. Step 5 re-verifies from the tree before scoring.

---

## 3. What converged to 100 — EXITS the loop

These three prototypes are **DONE as evidence**. No pass-3 lane touches their
worktrees except to read them (ruling 15). Their patterns are ratified inputs to the
wave-set development:

- **P2 — the small-module sweep, whole.** The library fragment (constants
  `index.ts` DELETE + 57-importer retarget + both G12 dead-guard retirements in the
  same commit, with the pass-1 "strictly stronger" error explicitly corrected), all
  four demo executables (matrix-editor INLINE, orbital-drag/types FOLD byte-identical
  to P5's landed `constants.ts` — the triplication cure — timelineTypes FOLD,
  instrument DELETE), all six KEEP flips with live-measured counts, zero behavior
  change (vitest exact-match, gh-pages green), P2-FOLD-MAP.md in-tree with four
  genuine rule refinements. The adjudication was the deliverable, and it is delivered.
- **P3 — the CLAUDE.md total removal, zero-loss.** OD-U15 executed in full with
  OD-U1's burden discharged by ledger, not assertion: 42 claims accounted, the
  barrel-inventory fork decided WITH its argument (barrels stay zone-summaries; a
  per-file list in a barrel is a new rot surface; each file documents its own
  contract — verified as actually true at every spot-checked anchor), the README
  single-owner split honored exactly, the demo orientation re-homed.
- **P5 — the transport trio recut, converged.** The ratified name landed
  (ChannelGroup / ChannelControls / ChannelOptions — no later lane re-litigates,
  per SPEC-B2 §4.5); G13 executed as law (types file-local, rows DIE BY FIXING,
  ratchet 26→16); both verdicts in-tree; the host scored KEEP on a real
  exclusive-cluster argument with gate anchors named; the options/playback clusters
  share ZERO data edges — proven, not claimed. The child-split pattern U.B needs is
  fully proven, named, and recorded.

**One P5 note, ruled moot (ruling 12):** `demo/CLAUDE.md:34` in P5's tree still says
"control-suite shells" — a stale line in a file P3's ratified charter DELETES. Under
SPEC-B2 §5.F's own merge-order logic this is a recorded note, not a defect: it can
never materialize in ratified work. Contrast P1's three stale refs (§4), which live in
SURVIVING source files and are real.

---

## 4. What remains, and WHY

Every remaining item is a **small, mechanical close** — no rework, no new design. The
residues, exhaustively:

### P1 (95%) — three stale refs + one template hole
- Three prose references (comments/docstrings — "prose refs": path mentions in text,
  not code) to the PRE-move path `compile/view-transition.ts` survive; the file is now
  `compile/emit/view-transition.ts`. Sites: `src/animation/compile/emit/index.ts:40`
  (a comment inside the renamed barrel itself — should read `./view-transition`),
  `src/animation/orchestration/view-transition/view-transition.ts:16`, and
  `src/animation/orchestration/view-transition/index.ts:11`. This is pass-1's named
  third failure mode (the anti-drift work minting fresh drift) in surviving source.
- `P1-REANCHOR-TEMPLATE.md` — P1's core deliverable — has seven site classes ("site
  class": a category of place a moved path can be written down, which the template's
  checklist sweeps) but NO doc/prose cross-reference class, despite this very carve
  having re-anchored two CLAUDE.md doc trees and having missed the three refs above.
  A later carve following it verbatim repeats the drift. Ruling 11 fixes this.

### P4 (96%) — two honest-form edges
- Three dead `eslint-disable` (`no-eval`/`no-new-func`) directives are KEPT verbatim in
  `scripts/proof-{accent,font}-census.mjs` + `proof-kf-differential.mjs`. P4 found them
  (better than the research, which counted 2 and never looked in `scripts/`) and made a
  reasoned documentary keep — but a directive addressed to a linter removed at Q.WA1 is
  dead tooling cruft under this tranche's own NO-legacy ethos. The converged form:
  convert each to a plain intent comment ("intentional: serialized fn into
  page.evaluate"), directive deleted.
- VJ-L2 version attribution drifts across surfaces ("≥ 1.0.0" / "0.14.0" / "3.1.0").
  Non-load-bearing but imprecise; ruling 13 sets one provenance.

### P6 (95%) — one factual error + one clause tension
- §Structure describes `internal/` as "the value.js-free leaf tier, shared across the
  **light zones**" — factually wrong: the import graph shows 6 HEAVY zones + 2 LIGHT
  zones consume it. One-word fix: "shared across zones". Fresh drift in anti-drift
  work; must not ship.
- MINOR: the §Structure intro's blanket "each behind an `index.ts` barrel" sits over a
  tree that lists barrel-less `internal/`; tighten the clause. (The §dynamic-engine vs
  §tree-shaking dual explanation is PRE-EXISTING dual-audience prose, ruled not a P6
  defect — recorded so pass 3 does not reopen it.)
- The **P6-after-P3 hard sequencing constraint stands unchanged** — P6's artifact
  already reflects P3's actual landed state (verified byte-exact), so the constraint is
  satisfied; it simply continues to govern merge order at ratification.

### SPEC-B2 (92%) — five errata
1. G12 application 1 cites the surviving types-purity sibling clause at
   `proof-boundary.mjs:232`; it lives at `:498` (doc block `:220-224`).
2. §6.2 "2 [dead eslint-disable] at last count" — the tree carries 3 (all in
   `scripts/`); re-measure or drop the count.
3. §5.B "wrong for nine of them" is loose arithmetic (the operative three-file
   conclusion is correct and was acted on; fix the prose).
4. LIGHT/HEAVY — the project's most central term of art — is absent from the §0
   glossary while rarer terms are glossed; add it (and optionally `stutter-trio`).
5. Add the one-line note that the canonical full library table = SPEC-B1 §2 as amended
   by §2 here (declared, but worth stating where a scorer reads).

Plus one content fold (from P4's verification): §8.4.2's two now-falsified research
premises — the `_diff/` vestige (already retired at T.M3) and "two eslint directives"
(five existed; three live in `scripts/`) — correct the wording so no future pass
re-hunts the phantom.

---

## 5. Agglomerator rulings (binding on pass 3) — numbering continues from PASS-1's 1–8

9. **The P5 `progress` supersession is RATIFIED.** Where a charter orders a signature
   tighten and fix-time measurement proves the edge DEAD end-to-end, deleting the edge
   whole beats the tighten — G11's measure-at-the-moment protocol extends from folds
   to prop/edge tightens. The supersession must be declared and evidenced in-tree (P5
   did both). No lane re-litigates the deleted `progress` chain.
10. **Critique-validity rule (new, general).** A step-4 critique that is not
    self-contained — placeholder analysis, unverifiable gaps, or absent entirely — is
    VOID; its score is discarded, never inherited or averaged. Step 5 verifies from
    the tree before scoring, and records the substitute basis in the pass record (§2
    is the form). Every future step-4 fleet's output is checked for self-containment
    before step 5 consumes it.
11. **The template gains its 8th site class.** `P1-REANCHOR-TEMPLATE.md` adds a
    doc/prose cross-reference class covering: doc files (CLAUDE.md-class trees while
    they exist), sibling-barrel comments, consumer docstrings, AND the moved file's
    own header/self-references (a renamed file must not cite its own old path — nor
    its old siblings'). The class is exercised by fixing P1's three live stale refs
    under it. This also covers the P3×P1 merge-order case: `format.ts`'s new
    docstring self-cites `compile/backward/format.ts`, correct on P3's tree; the
    ratified rename wave re-anchors it to `emit/` under this class.
12. **Moot-by-merge-order stale refs are RECORDED, not fixed** (SPEC-B2 §5.F
    generalized). A stale mention inside a file another prototype's ratified charter
    DELETES (P5's `demo/CLAUDE.md:34`) is a merge-order note and blocks nothing; a
    stale mention in SURVIVING source (P1's three) is a defect and blocks 100. The
    distinction is the file's ratified fate, not the mention's size.
13. **One version provenance per external claim.** When citing an external package's
    landed fix, state the installed version once — `package.json` is the authority —
    and let other surfaces point at that one statement. Applied to P4's VJ-L2
    citations (installed: value.js 3.1.0). Also carried as verified fact: value.js
    3.1.0's `parseLinearStops` still THROWS on the flat-comma form — parser tolerance
    was never VJ-L2's contract (the serializer fix was); no future lane re-litigates
    the un-skipped probe.
14. **SPEC-B3 is errata-thin.** At 97% overall, the spec revision for pass 3 is the
    five §4 errata + the §8.4.2 premise correction + these rulings folded — NO
    re-synthesis, no new rules, no charter rewrites. Spec churn at high convergence is
    itself a divergence risk.
15. **Exited items FREEZE.** P2, P3, P5 worktrees are read-only evidence from here;
    no pass-3 lane commits to them. Their residual cross-prototype notes (the
    orbital-drag single-`constants.ts` shape, the fold-map's pre-P5 demo paths, the
    `demo/CLAUDE.md` stale line) are reconciliation inputs to the WAVE SET, which
    executes on one tree where the merge-order questions dissolve.

---

## 6. Newfound context pass 3 carries (verified facts, not claims)

- **The §9 questions, answered:** Q5 (any-sweep schedule) — ANSWERED by P4's
  calibration: 13 seams typed, ceiling 99→86, exactly 35 `KeyframesAnimation<any>`
  lines remain in precisely the files U.B's recut opens; verdict = in-drive sweep on
  U.B, no standalone wave, tail floor for genuinely-dynamic Monaco/Vue-ref casts.
  Q6 — DESIGN.md KEEP + the `@`-dissolution still ride to the OWNER; **ChannelGroup
  does NOT join them** (converged and landed). Q7 (barrel laundering) — verified NO.
  Q8 (KEEP-flip counts) — spot-checked exact at the fold commits.
- The no-dead-export backlog is 16 rows with a defined G13 exit; the any-ceiling is 86
  with the ratchet's moved-anchor rule intact; `roundtrip-easing` is 8/8 with no
  conditional skip.
- P1's tree: 132 modules, zero circulars, full gate roster green including both plant
  self-checks; the zone-cohesion entries are RE-KEYED (not retired) pending U.C9 —
  exactly as SPEC-B2's corrected wording orders.
- P3's ledger arithmetic: 42 claims = 32 HOMED (18 prior / 8 new / 6 option-b) +
  10 DROPPED-for-cause; `warmEngine` has no surviving doc advertisement (U.C13
  unblocked).
- P5's trio: 334L group / 473L host / 383L options, host under the 500L ceiling with
  two gate anchors pinning content in place; six colocated composables already hold
  every reusable cluster.
- The pass-2 research fed two falsified premises (the `_diff/` phantom; the eslint
  undercount) — P4 refuted both against the tree rather than inheriting them. The
  loop's step-3 agents checking step-1 research against the tree is now demonstrated
  practice; keep it.

---

## 7. THE PASS-3 FOCUS — a close pass

**Four items iterate; three are frozen. All pass-3 work is mechanical; no research
fan-out is needed (step 1 is a no-op this pass — every fix is already located to the
line).** SPEC-B2 revises to SPEC-B3 as errata only (ruling 14).

| Item | Mode | Pass-3 work — exhaustive |
|---|---|---|
| P1 | **close** | Fix the three stale `compile/view-transition.ts` prose refs (`emit/index.ts:40` → `./view-transition`; the two orchestration sites → the `emit/` path). Add the 8th site class to `P1-REANCHOR-TEMPLATE.md` per ruling 11, with the self-reference caveat, exercised by these very fixes. |
| P4 | **close** | Convert the 3 dead `eslint-disable` directives in `scripts/` to plain intent comments (directive deleted). Unify the VJ-L2 provenance to one statement per ruling 13. |
| P6 | **close** | One-word fix: `internal/` "shared across zones". Tighten the §Structure barrel-blanket clause. Re-run both README gates. (Still LANDS AFTER P3 at ratification — constraint unchanged.) |
| SPEC-B2→B3 | **errata** | The five §4 errata + the §8.4.2 falsified-premise correction + rulings 9–15 folded. Nothing else. |
| P2 · P3 · P5 | **FROZEN** | Read-only evidence (ruling 15). |

**Exit condition (OD-U18, unchanged):** pass 3 re-runs step 4 on the four touched
items only (self-contained critiques, per ruling 10), then step 5 agglomerates. At
100% across all seven the loop TERMINATES and **the exact U wave-set development
begins** — with the six prototype branches + the loop/ records as its ratified
evidence base, and the standing lessons intact: score against the charter, execute
the hard half first, leave the record in-tree, and verify — never inherit — every
number.
