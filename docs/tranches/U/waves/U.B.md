# Tranche U — Band U.B — THE DEMO TRANSPOSITION

> **Status: CLOSED (2026-07-15).** The canonical-home keystone and the terminal
> component/editor/transport/scene recuts landed. `demo/@`,
> `components/custom`, `components.json`, and the app/dock component home are
> absent; shared facilities occupy their lowest honest consumer homes.
>
> **Terminal amendment.** The per-wave gate re-arm language below is historical.
> OD-U23 deleted the residual source-shape apparatus instead of retaining gates
> merely to certify the moved tree. Product behavior is held by Vitest and the six
> direct browser observations; component-level Glass residue is owner-re-deadlined
> to the Glass 6 artifact, not carried as a local U/V gate.
>
> **Charter sentence (U.md §2, the U.B row — RE-CUT per OD-U2, 2026-07-10).**
> Keystone-first move: the component CORE redesigned to the glass-ui post-BH idiom
> (`audit/glassui-idioms-post-bh.md` §1 — kebab-case component dirs, PascalCase SFCs,
> `index.ts` barrel, `constants.ts` types-through-the-barrel, per-component
> `composables/`, >500L renderer carves, README/skeleton if-needed) + dissolve
> `components/custom/` (STANDS); the `demo/@/` DISSOLUTION replaces the former
> `@→shared` rename as the REVISED RECO (hoist its five children to
> `demo/{components,composables,state,styles,utils}/`, alias SPELLINGS unchanged,
> 3-plane declaration — executes on the owner's one-word confirm; the ~34 gate
> path-literal re-anchor pass is IDENTICAL either way); then per-area recursive
> colocation to the ratified component-module skeleton (lane 24 §9) — transport's 15
> private composables into their owners;
> per-parent module dirs; editors unified on ONE keyframe-authoring core; shell
> purged of home-hero pieces + dead EditorHeader; scenes converged on
> `useManagedLoop`/`usePainterRegistry`/`useSweepScene`; the SceneFacility
> subsumption (dual-path `SceneExposedApi` deleted); `@/state` singleton hygiene;
> skeleton tier completed (T.F8) per module.
>
> **Provenance lanes owned.** 03 (t-verdict-trace — the #26 keystone falsification),
> 17 (transport), 18 (editors · shell · @/state), 19 (scenes), 20 (app · shared
> tier · alias/config change map), 24 (design-restructure — **§9 is THE shape
> authority every wave cuts to**), 26 (colocation-idiom-vue — barrel/async/CSS/test
> rulings), 32 (target-tree-synthesis — the move-once sequence). Cross-cited: 02/29
> (the SceneFacility subsumption F1, U.B7).
>
> **The shape authority — the house component shape, stated once.** Every move-wave
> below cuts to ONE skeleton (from audit lane 24 §9, anchored to the glass-ui post-BH
> idiom): each component is a **kebab-case directory** holding its **PascalCase
> `.vue`**, an **`index.ts` barrel** (re-export only), a **`constants.ts`** for its
> typed constants (types flow through the barrel), and a **`composables/`** folder
> for its hooks; renderers over ~500 lines get carved into composables; a member
> lives at the LOWEST directory containing all its consumers (the placement rule);
> props/emits/slots each follow ONE grammar. Below, "cut to the house component
> shape" means exactly this; (lane 24 §9.1/9.2/9.3, §9.4 = the timeline recut as the
> reference implementation) are the audit cites. U.G ratifies §9/§10 as the DESIGN
> CODEX; U.B EXECUTES the moves cut to it — grammar/lexicon conversions ride the
> import-touching moves (lane 24 §4: "the rename is free at the move, and NEVER
> cheaper later"), never as a standalone pass.
>
> **The anti-sprawl covenant (binding, U.md §6).** U.B introduces **ZERO** new
> standalone `proof-*.mjs`. Every new assertion is a CLAUSE on an existing gate
> (`proof:colocation`, `proof:style-file-ceiling`, `proof:styling-idioms`) or a rule
> in the already-running dependency-cruiser pass; several gates RETIRE
> (`proof:demo-no-oversize`, the tolerant-`SHARED`/`DEFERRED` machinery). The
> post-build reachability/weight assertion is **U.D-owned as ONE clause inside
> `proof:publish`** (OD-U11 — the standalone `proof:chunk-graph` gate is DROPPED,
> net NEW standalone gates in U = ZERO); U.B lands the module cuts it certifies (the
> facet-seam cure, OD-U12). Net gate count only goes DOWN.

---

## §0 — Why this band exists

Lane 03 is the indictment: T verdict #26 (the GRAND COLOCATION EDICT) is marked
LANDED, but the two ROOT structure waves **never executed** — `demo/@/` is still a
literal on-disk dir, `components/custom/` still wraps the whole facility, and the
keystone gate `proof:colocation` was **shaped to pass without them** (tolerant
`SHARED = ["demo/@","demo/shared"].find(exists)` at `proof-colocation.mjs:50`; a
`DEFERRED` map at `:69` that "passes whether the violation is still present OR
already-cured"). The good work (SceneFacility, instrument/ fold, scene prunes) is
REAL but INVISIBLE because it sits one level under the two un-dissolved shadcn
wrappers. The owner opens `demo/`, sees `@/` and `custom/`, and correctly reads
"unchanged."

So U.B's acceptance oracle is lane 03's: **"the owner can read the tree at the first
`ls`."** The keystone (U.B1) makes the invisible work visible by DELETING the two
wrapper concepts and re-forging the gate from tolerant to REQUIRING; every
subsequent wave recuts one area to lane 24 §9 so the tree's shape STATES its
ownership. Per **OD-U2** (2026-07-10) the keystone now goes further than the T-era
`@→shared` rename: the component CORE is redesigned to the glass-ui post-BH idiom
(`audit/glassui-idioms-post-bh.md` §1) and `demo/@/` DISSOLVES (its five children
hoist to `demo/{components,composables,state,styles,utils}/`, alias spellings
unchanged, declared across all three planes) — the owner reads a tree HOMOGENEOUS
with glass-ui's, not merely a renamed wrapper.

## §1 — The DAG (within U.B; edges to sibling bands)

```
U.B1  (KEYSTONE — component-core redesign to the glass-ui post-BH idiom + dissolve
       custom/ + @-DISSOLUTION (REVISED RECO, one-word confirm) as deletions;
       re-anchor + re-forge proof:colocation tolerant→REQUIRING) ── the root every
       U.B wave rides; co-scheduled with U.A's gate re-anchoring (one pass, lane 32)
   │
   ├── U.B2  (transport module recut — §9.4 shape)          ── rides B1
   │     └── U.B3  (transport de-tangle + the ONE demo ticker) ── rides B2 (hoist targets)
   ├── U.B4  (editors: ONE keyframe-authoring core)          ── rides B1
   │     └── U.B5  (shell purge + tier)                       ── rides B4 (CSSCodeEditor hoist)
   ├── U.B6  (@/state hygiene + controlSurfaceDFA split)     ── rides B1
   ├── U.B7  (SceneFacility subsumption + contract hoist)    ── rides B1; edge U.C2 (the seek/adoptClock codec), U.F (value.js plain-vars)
   ├── U.B8  (scenes convergence + app/runtime split)        ── rides B1,B7 (facility contract home); ↔ U.D5 (async contract, co-owned)
   └── U.B9  (app-shell honesty + shared-tier re-homing;
              empty DEFERRED residuals → delete tolerance)   ── rides B2,B4,B8 (hoist destinations settled)
   │
   U.B10 (skeleton tier per module — T.F8 completion)        ── rides each module recut (B2/B4/B5/B8)
   U.B11 (barrel discipline + async-seam facet cuts, OD-U12) ── rides B1; co-sched U.D's proof:publish reachability clause (OD-U11)
   U.B12 (grammar + CSS-split residual sweep + §10 clause)   ── LAST; sweeps what the move-waves didn't touch
   U.B13 (amiga suspend-contract cure, OD-U13 Fix B)         ── rides B8 (useManagedLoop); ↔ U.C14 (Fix A, the library freeze); ← U.H1 (net FIRST)
   U.B14 (small-module INLINE wave, OD-U16 inline — U.B half) ── rides B1; target-by-target from P2's fold-map (`287-11`); ↔ U.C constants fold (paired)
```

**The net underneath every move (U.H1).** Every U.B move rides on U.H1's
characterization net (green across the move = behavior preserved; red = drift),
authored BEFORE U.B per U.md §3 — a file relocation that reds the net is a behavior
regression, not a cosmetic move.

**Sequencing keystones (lane 32).** (1) `custom/` dissolution + the `demo/@/`
DISSOLUTION (OD-U2) FIRST so every import/gate re-points ONCE — it is the
HIGHEST-fanout path change; any inner recut before it re-points imports TWICE. (2) The CI-trim and the restructure touch
the SAME path-pinned gate scripts (~34 hardcode `demo/@/…`) — ONE coordinated pass
with U.A, never two. (3) Grammar/lexicon renames ride the move-waves.

---

## §2 — Wave index

| id | title | size | gate / oracle | edges |
|---|---|---|---|---|
| **U.B1** | KEYSTONE (OD-U2): component-core redesign to the glass-ui post-BH idiom (audit §1) + dissolve `custom/` (STANDS) + `@`-DISSOLUTION (REVISED RECO, one-word confirm) as DELETIONS; re-anchor gates; re-forge `proof:colocation` tolerant→REQUIRING | L | `proof:colocation` REQUIRING (asserts the hoisted `demo/{components,composables,state,styles,utils}/` homes exist, `demo/@`+`components/custom` do NOT) + `grep` 0-hit witness + vitest demo suite green through the alias repoint | ROOT; co-sched U.A (lane 32); precedes all U.B |
| **U.B2** | Transport module recut to §9.4: dissolve the flat `composables/` drawer, collapse `components/`+`controls/` into per-parent module dirs, carve `useMenubarMeasure`+`useIconSpin` from `TransportDock` | L | `proof:colocation` REQUIRING (inverse clause: single-owner member in a shared tier → RED) + transport vitest behavior tests + owner-golden | B1; retires `proof:demo-no-oversize` (U.A) |
| **U.B3** | Transport de-tangle + the ONE demo ticker: hoist shared playback widgets + reference-data, invert the 6-level `../` import, unify per-host rAF into one visibility-gated `useDemoTicker`, idiom-CSS → `design-idioms.css`, `DemoGlobalChrome` → document-singleton tier | M | vitest (transport + easing/spring scene tests unchanged) + a headless one-shot ticker-count witness (one rAF, gated) + owner-golden | B2 (hoist targets settled) |
| **U.B4** | Editors: ONE keyframe-authoring core — unify all 3 surfaces on `useKeyframesEditor`, fold `useKeyframeBrushApply`, collapse the duplicated `selectedKeyframesControl` store field, retire the `Ï` magic-char shortcut, hoist `CSSCodeEditor` to an `instrument/`-shared Monaco leaf | L | vitest editor round-trip (CSS ⇄ animation parity across surfaces) + `proof:control-surface-single-writer` green | B1 |
| **U.B5** | Shell purge + tier: DELETE dead `EditorHeader.vue`, move the home-hero trio (`HeroAurora`/`AnimatedText`/`TypingDots`) to `app/`, tier the residual editor-chrome shell to the §9 skeleton | M | `proof:no-dead-export` (EditorHeader row cleared) + `proof:colocation` (tier-once-peer-has-subcomponents+composable clause) + owner-golden (home hero) | B4 (CSSCodeEditor hoist) |
| **U.B6** | `@/state` hygiene: disciplined `applySharedState(patch)` writer (hashSharing stops reaching into `.value`); split the control-surface state machine (`controlSurfaceDFA.ts`) into its pure derivation logic (`controlSurfaces.ts` → `@/state`) vs its dock tab labels (`surfaceTabs.ts` → instrument facility) | M | `proof:control-surface-single-writer` + `scene-machine`/`control-surface-dfa`/`sharing` vitest suites | B1 |
| **U.B7** | SceneFacility subsumption: hoist the contract out of `app/scene/` → `demo/composables/scene-facility/` (the OD-U2 dissolution home); DELETE `animationGroup?`/`scenePlayback?` from `SceneExposedApi`, the `?? animationGroup` dead fallback, the `ownsPlayback` discriminator; stable `identity` + sole play authority; kill the `controlSurfaceDFA` structural duck-type | M | `proof:scene-facility` (re-armed to assert the SOLE seam) + vitest scene-machine binding tests + owner-golden (all 6 scenes play) | B1; edge **U.C2** (the seek/adoptClock codec), **U.F** (plain-vars re-pin) |
| **U.B8** | Scenes convergence: generalize `useRafScene`→`useManagedLoop`, route `sequence`+`square` through it (no bare `new RAFPlayback()` at scene scope); kill sequence's 60 Hz reactive-progress write; extract `usePainterRegistry`; charter `useSweepScene` recipe (easing+spring onto it); DELETE `num()`/`toRGB`/`sweepHue` for `ValueUnit`/`Color`; colocate the square tumble egg; `channelFromAnimation`; uniform `onScopeDispose`; split `app/runtime/` scene-recipes → `demo/composables/scene-runtime/` | L | scene vitest (`scene-raf-leak`, `scene-visibility-pause`, per-scene) + a headless dogfood witness (no hand-rolled unit/color parse) + owner-golden (each scene) | B1, B7 (facility contract home); ↔ U.D5 (F3 async-tolerant AnimationGroup contract, co-owned) |
| **U.B9** | App-shell honesty + shared-tier re-homing: guards → `app/lifecycle/`; instrument-only `iosTextEntry`+`toastGuard` → `instrument/utils/`; `kfEngine` → the demo root (`demo/kf-engine.ts`); `SceneSkeleton` → `app/`; `font-roles.json` → `scripts/`; `glass-ui-gaps.ts` → `demo/utils/`; EMPTY the `DEFERRED` residuals (`gestureSelectSuppression`, `kfEngine`) then DELETE the tolerance machinery | M | `proof:colocation` (`app/` shell-only; `@/utils` global-only; **tolerance-free** — DEFERRED map deleted) + `proof:shared-has-n-consumers` + vitest | B2,B4,B8 (destinations settled) |
| **U.B10** | Skeleton tier per module (T.F8 completion): `<Name>.skeleton.vue` colocated per lazily-delivered module, wired by the module barrel's `loadingComponent`; the Monaco panes get their skeletons; DELETE `@/components/skeletons/` | M | `proof:colocation` skeleton clause (lazy barrel w/o `loadingComponent` → RED) + owner-golden (no scene-swap text-flash) | rides B2/B4/B5/B8 |
| **U.B11** | Barrel discipline + async-seam FACET cuts (OD-U12 design authority): DELETE the zero-consumer umbrella barrel; barrels become explicit-named EAGER contracts (`defineAsyncComponent` leaves the barrels); every scene composes the multi-facet instrument (controls / keyframes-if-befitting / scene-specific facets — cube's matrix, etc.), heavy editors (Monaco/highlight) load ONLY when their facet is SHOWN; excise the NAMED violation — `SpringPhysicsFacet`→`KeyframesEditor` eager deep import (the inline spring editor) — via a facet-level async seam; depcruise demo ruleset (`no-cross-module-deep`/`no-self-barrel`/`no-star-export`) | M | the `proof:publish` reachability/weight clause (**U.D-owned**, OD-U11 — no standalone `proof:chunk-graph`; born-RED on the 906 KB leak) + depcruise (existing run) + `proof:published-surface` unaffected | B1; co-sched **U.D** |
| **U.B12** | Grammar + CSS-split residual sweep + the §10 standing clause: convert residual `withDefaults`/runtime-object props + manual `update:*` emits not covered by move-waves; split `SpringTarget.vue`'s 200 L `<style>` block; `@apply`-confinement; EXTEND `proof:colocation` + `proof:style-file-ceiling` + `proof:styling-idioms` per §10 | S | the extended existing gates (one AST pass, no browser) + `proof:styling-idioms` @apply clause | LAST; U.G codex authority |
| **U.B13** | The amiga suspend-contract cure (OD-U13 Fix B — the dossier at `audit/defect-amiga-suspend-resume.md` SPLIT the mandate: the dominant freeze is the LIBRARY bug U.C14 cures; this wave folds amiga's IntersectionObserver + visibility pause + machine TAB_HIDDEN into ONE symmetric suspend seam governing both the render loop AND the group clock — no drift-then-jump on tab return), owner-mandated in-U | M | the characterization net (U.H1) covers scene switch/visibility semantics BEFORE the fix (the cure is WITNESSED, not asserted) + owner-golden (amiga plays ≥3s; suspend/resume clean + jump-free) | ↔ U.C14 (Fix A — the library freeze, consumed here); B8 (useManagedLoop convergence); ← U.H1 (net FIRST) |
| **U.B14** | THE SMALL-MODULE INLINE WAVE (OD-U16 inline direction, U.B half): absurdly-small demo modules inlined-and-deleted, cut **target-by-target from P2's fold-map** (`287-11`), NEVER a bulk sweep; **paired with U.C's constants fold** (the OD-U16 both-directions ruling closes in one motion) | M | `proof:colocation` (a single-symbol / single-in-area-consumer module → RED, cured by the inline; no new clause) + demo vitest green through each inline + the fold-map complete (every target inlined or exempted BY NAME) | rides B1; ↔ **U.C** (OD-U16 inline direction, U.B+U.C paired); loop-chartered (OD-U18, U.L) |

---

## §3 — Per-wave detail

### U.B1 — THE KEYSTONE (OD-U2: component-core redesign + dissolve `custom/` + `@`-dissolution as deletions; re-anchor + re-forge)

- **substance.** The single highest-fanout first move, executed as DELETIONS not
  relocations (lane 03 F1/F5, lane 20 F1/F2, lane 32 F1), **RE-CUT per OD-U2
  (2026-07-10, two parts):**
  - **THE WAVE ORDER IS `N1-MOVE-SCRIPT.md` §①–⑤, EXECUTED VERBATIM from the frozen
    evidence** (the convergence loop's converged keystone — P7/N1 @ 100, worktree
    `d7f-11`; OD-U18, band U.L). **Steps ①/④ are owner-gated per the ride queue:**
    **①** dissolve `demo/@/` + `custom/` + the riders + **`git rm components.json`**
    (R21/E16 — the dead shadcn scaffold config, both its `demo/@` paths die with it, no
    re-point) + the six-bucket 72-file measured sweep (re-measured in-commit, ruling 24 —
    every hit disposed BY NAME, in-sweep-space ≠ disposed) + the `ci.yml:496` step-name
    re-word; **②** the dock re-home (already EXECUTED as evidence — replays from the
    manifest); **③** the singleton re-homes; **④** the interior recut (owner-gated
    point 2); **⑤** `demo/CLAUDE.md` deletes LAST (couples to U.E7). The two OD-U2 parts
    below ARE steps ①/④'s substance; the ~34-gate re-anchor pass is co-scheduled with U.A.
  - **The component CORE redesigned to the glass-ui post-BH idiom (OD-U2 part 1 —
    RULED terminal).** Per `audit/glassui-idioms-post-bh.md` §1, every component
    becomes a **kebab-case dir** with **PascalCase SFCs** + an **`index.ts` barrel**
    (re-export only) + **`constants.ts`** (typed constants/enums/pure helpers, with
    types flowing **through the barrel** — SFCs cannot re-export a type) + a
    per-component **`composables/useXxx.ts`** for any bound hook, with **>500L
    renderers carved** out of the SFC and an optional **`README.md`/skeleton "if
    needed."** This IS the homogeneity the owner demanded ("ensure homogenaity with
    our other repos") — the demo's component set now reads like glass-ui's
    `custom/`/`ui/` families. The per-area recut waves (B2/B4/B5/B8) cut each module
    to THIS shape; lane 24 §9 is the same skeleton, now ANCHORED to the glass-ui
    evidence rather than derived in isolation.
  - **`demo/@/` DISSOLVES (OD-U2 part 2 — REVISED RECO, executes on the owner's
    one-word confirm).** The literal on-disk `@/` is a shadcn-scaffold vestige
    (shadcn's `@/` is an ALIAS to the src root; the scaffold materialized it as a real
    directory — glass-ui has NO on-disk `@`, only a 3-plane `@glass` alias; audit §2).
    Rather than the T-era `@→shared` RENAME, **hoist `demo/@/`'s five children to
    `demo/{components,composables,state,styles,utils}/`** (the glass-ui root shape, no
    `shared/` wrapper). The alias SPELLINGS consumers import (`@components/`,
    `@composables/`, `@state/`, `@styles/`, `@utils/`) are **UNCHANGED** — repointed at
    the new homes and declared across **ALL THREE PLANES** per the glass-ui discipline:
    `tsconfig.json` `paths`, `vite.config.ts` `resolve.alias`, AND `vitest.config.ts`
    `resolve.alias` (none reads the others — the audit's twin-declaration law).
    `grep -rn 'from "@/' demo` → **0 hits** (verified, lane 20 F1), so this touches
    **ZERO `.vue`/`.ts` import statements**; the ~34 gate path-literal re-anchor pass
    is **IDENTICAL** whether the dir is renamed `shared/` or dissolved. S.D4's "a
    rename would touch every import" premise is factually false; the stale "ruled
    terminally to keep `@/`" paragraph dies with `demo/CLAUDE.md` itself (OD-U15 —
    ALL CLAUDE.md files delete wholesale; U.E7 owns the removal). **Toolchain integrity**
    (the 3-plane declaration builds + tests green) is an acceptance clause — the owner:
    "ensure we're not breaking our tool chaining, too." (Same vestige noted in
    value.js's demo — a letter row to its active tranche, U.F, not kf work.)
  - **Dissolve `components/custom/`.** `@/components/` holds exactly `{custom/,
    skeletons/}`; `custom/` holds exactly `{instrument/, CopyButton.vue}` — the
    shadcn "not-`ui/`" bucket whose sibling `ui/` died at S.C3b, now a
    single-facility-plus-one-leaf wrapper the "NO legacy" edict forbids. Hoist
    `custom/instrument/` → `components/instrument/`, `custom/CopyButton.vue` →
    `components/CopyButton.vue` (17 import sites `@components/custom/…` →
    `@components/…`; `@components` RHS now `demo/components` post-dissolution). CopyButton's
    shared legitimacy is preserved (cross-tier: easing/spring targets + four
    instrument files). Delete the empty `custom/`; delete
    `proof-shared-has-n-consumers.mjs:174`'s `if (parts[1] === "custom")`
    special-case in the same commit. This drops the deepest demo path **8 → 6**
    levels (`@/components/instrument/transport/controls/AnimationControls.vue`).
  - **Re-anchor ~34 gate path-literals** that hardcode `demo/@/…` /
    `components/custom/…` (`proof-decomposition.mjs:87-89,167`,
    `proof-hero-two-focal.mjs:58`, `proof-pp-logo-svg.mjs:141`,
    `proof-stage-within-docks.mjs:362`, …) — folded into THIS commit, co-scheduled
    with U.A's gate cull so the path sweep and the roster reduction do not
    double-touch the same files (lane 20 F8, lane 32 note).
  - **Re-forge `proof:colocation` from tolerant to REQUIRING** (lane 03 F2, lane 26
    Finding 3). Delete the tolerant `SHARED = ["demo/@","demo/shared"].find(exists)`
    (`:50`) → assert the canonical hoisted homes (`demo/components/`,
    `demo/composables/`, `demo/state/`, `demo/styles/`, `demo/utils/`) EXIST and
    `demo/@`/`custom` do NOT. The `DEFERRED` map (`:69-90`) was emptied by the
    B9 residue discharge; the proof now requires the canonical runtime home and
    has no deferred exception for the two former residuals. A keystone that cannot fail on the rejected tree is
    retired.
- **evidence.** `demo/@/` on-disk (verified `ls`); `custom/` = `{instrument/,
  CopyButton.vue}` (verified); `proof-colocation.mjs:50` tolerant `SHARED`, `:69`
  `DEFERRED` (verified); `grep 'from "@/' demo` → 0 (lane 20 F1).
- **shape authority.** lane 24 §9.2 placement rule (kind-appropriate shared tier);
  the acceptance oracle is lane 03's "owner reads the tree at first `ls`."
- **size** L. **edges.** ROOT; the CI-trim path sweep is the SAME pass as U.A (never
  two). Precedes every other U.B wave. The gate re-anchoring/re-forge is legal only
  AFTER U.A1 inverts the `ci-coverage` literal-step contract (U.A §A.0); co-scheduled
  with U.A in the ONE coordinated pass, A1 riding first.

### U.B2 — Transport module recut to §9.4

- **substance** (lane 17 F1/F3/F7, lane 24 §5). The transport facility is a
  three-drawer pile inverting the edict:
  - **Dissolve the flat `composables/` drawer** (18 files, no barrel). 15 of 18 are
    single-owner (lane 17 dependency table, verified against `import` lines): each
    moves INTO its owner's module dir (`useAnimationGroupPlayback`/`Actions`/
    `useAnimationProgress`/`useControlsKeyboardShortcuts` → `AnimationControlsGroup/`;
    `useControlsLayout`/`usePaneRegister`/`usePaneHover` → `ControlsPaneWrapper/`;
    `useSelectedControlSurface`/`useKeyframesPaneReveal`/`useTabStripScroll` →
    `AnimationControls/`; `useKfPillTabs` → beside `KfPillTabs.vue`;
    `usePlayActuation` → `TransportDock/`; `useAnimationSync`/`usePlaybackToggle`/
    `useTimingFunctionEditor` → `controls-body/`). The 3 genuine multi-consumer
    utilities (`useScrollFade`, `useDragCapture`, `useRafLoop`) hoist to
    `@/composables/` (their ≥2-area count earns the seat) — `useRafLoop` folds into
    the shared ticker (U.B3).
  - **Collapse `components/` + `controls/` into per-parent module dirs.** The two
    names encode nothing (RibbonBar renders INSIDE ControlsPaneWrapper yet sits in
    the `components/` peer bag). Recut to the §9.4 shape: each parent owns its
    `.vue` + scoped `.css` + private composables + child modules
    (`AnimationControlsGroup/{TransportDock/, ControlsPaneWrapper/{RibbonBar.vue,
    AnimationControls/{KfPillTabs.vue (renamed at B12 — the de-vanity sweep), controls-body/…}}}` — lane 17 target tree).
  - **Carve `TransportDock.vue` (464 L)** into `useMenubarMeasure.ts` (the
    resize-observer + monotonic peak `--menubar-measured-h` publisher, `:255-321`) +
    `useIconSpin.ts` (the reset-icon `CSSKeyframesAnimation`, `:398-434`) colocated
    in the TransportDock module — a COHESION carve (extract a real concern), the
    opposite of the line-count carve that manufactured the composables drawer.
  - Grammar conversion rides (lane 24 §4/§8): rename `AnimationControlsControls` →
    the ratified lexicon (e.g. `TransportChannelControls`); `defineModel` for
    `KfPillTabs`'s manual `update:modelValue`; named-tuple emits.
- **evidence.** `transport/composables/` = 18 files, no `index.ts` (verified);
  the extraction stamps on `useAnimationGroupActions.ts:31` et al. show these files
  were split out only to satisfy the now-retired line-count gate
  `proof:demo-no-oversize`, not by ownership (lane 17 F1); `TransportDock.vue:290-321,
  398-434` (lane 17 F7).
- **shape authority.** lane 24 §9.4 (the recut is the timeline's, generalized to
  transport) + §9.2 ownership rule + **P5's responsibility-recut TEMPLATE** (the
  convergence loop's demo-component-recut prototype, frozen evidence `287-17`: actuation
  hoisted to the host, the options form purified, the Channel-lexicon naming rule) — the
  TEMPLATE every U.B recut wave cuts to.
- **gate/oracle.** `proof:colocation` with the lane 24 §5 **inverse clause**
  (single-owner member inside a shared tier → RED) — turns the keystone into the
  skeleton's enforcement arm; transport vitest (`useAnimationGroupPlayback.test.ts`,
  `KfPillTabs.test.ts`) preserved through the moves; owner-golden.
  **Retires `proof:demo-no-oversize`** (U.A CI-trim): a `.vue`'s line count is not a
  defect and the gate manufactured finding-1's orphan drawer.
- **size** L. **edges.** Rides B1; the hoist destinations feed B3.

### U.B3 — Transport de-tangle + the ONE demo ticker

- **substance** (lane 17 F4/F5/F6/F8).
  - **Invert the six-level tangle.** `AnimationControlsControls.vue:264` imports
    `EASING_GROUPS` from `../../../../../../scenes/easing/easingGroups` (verified —
    a control panel reaching six `../` into a scene) while the easing scene reaches
    back into `transport/controls/` for `PlaybackRibbon`, `timingCurveUtils`,
    `animationDescriptions`. Hoist the genuinely-shared vocabulary to neutral tiers:
    `PlaybackRibbon`/`AnimationVisualizer` → `@/components/playback/` (the "standard
    ribbon" every scene mounts, `proof:scene-uses-standard-ribbon` exists);
    `animationDescriptions` + `timingCurveUtils` + `easingGroups` → `@/` shared
    reference-data. After the hoist both directions of the cycle vanish (a DAG).
  - **Unify the rAF polling into ONE `useDemoTicker`.** Today every mounted control
    host runs a `useAnimationSync` `useRafFn` poll (hosts are `v-show`, not `v-if`,
    so N channels spin N loops), plus `AnimationVisualizer` starts its sync loop
    unconditionally ("Always poll", no `isPlaying` guard, `:238-250`), plus
    `useAnimationProgress` reallocates a fresh `Record` every frame. Replace with one
    `RAFPlayback` driver in `@/composables/` gated on `documentVisible &&
    (anyPlaying || anyDragging)`; the markRaw→reactive bridge polls the SELECTED
    animation only; the visualizer ball becomes a subscriber that unsubscribes at
    rest. This is THE grand-performance-edict axis — many small rAF loops where one
    shared ticker belongs.
  - **Idiom-CSS to `design-idioms.css`.** `PlaybackRibbon.vue:82`
    (`import "./playback-button.css"`) + `AnimationControls.vue:214`
    (`import "./tab-trigger.css"`) inject NON-scoped global sheets whose
    `.btn-playback`/`.tab-trigger` classes are consumed by easing/spring scenes — an
    implicit mount-order coupling. Move both class families into
    `@/styles/design-idioms.css`; delete the side-effect imports.
  - **Re-home `DemoGlobalChrome`** (the rainbow `<defs>` paint-server + the teleported
    `<Toaster>`, self-labeled "resolve against the DOCUMENT") from
    `transport/components/` to a document-singleton tier beside the app shell chrome.
  - **Reconcile the `LayerConfigPanel` blend selector on multi-target groups (OD-U14
    behavior lane D4).** `LayerConfigPanel.vue:69` exposes the
    `["replace","add","weighted"]` blend selector (wired through
    `useAnimationGroupActions.ts:45` → `group.setLayerConfig`) for EVERY group — but blend
    modes are a NO-OP on `singleTarget=false` groups: `renderMultiTarget`
    (`group/entries.ts:87-96`) applies each child DIRECTLY to its own target with NO
    cross-child composite, so `add`/`weighted`/z-order do NOTHING there. The demo's `square`
    scene IS `singleTarget=false`, so a user picks "add", sees no change — a live control that
    does nothing (an appearance-axis blind spot, not identity). GATE the blend selector OFF
    for multi-target groups, or surface an explicit **"independent targets — no cross-layer
    blend"** note in its place. Provenance: `audit/assay-compositor-behavior.md` (the adjacent
    semantic gap found while driving the stacking matrix); the LIBRARY-side compositor
    re-charter is U.C14/U.C15, this is the demo control's honesty.
- **evidence.** `AnimationControlsControls.vue:264` (verified 6-level import);
  `useAnimationSync.ts:40`, `AnimationVisualizer.vue:236-250` (lane 17 F5);
  `PlaybackRibbon.vue:82` + `AnimationControls.vue:214` (verified side-effect imports);
  `LayerConfigPanel.vue:69` blend selector live on `square` (`singleTarget=false`) where
  `renderMultiTarget` has no blend arm (OD-U14 behavior lane D4).
- **shape authority.** lane 24 §9.2 rule 4 (cross-area shared → `@/` tier, kind-
  appropriate) + §6 CSS three-tier rule (cross-component idiom never lives in a
  module).
- **gate/oracle.** vitest (easing/spring scene tests unchanged post-hoist) + a
  headless one-shot witness asserting exactly ONE rAF driver at demo scope, gated on
  visibility+play; owner-golden (playback ribbon in every scene).
- **size** M. **edges.** Rides B2 (the recut settles the hoist source homes).

### U.B4 — Editors: ONE keyframe-authoring core

- **substance** (lane 18 F1/F4/F5/F6/F7). The editors carry the oldest demo code —
  THREE divergent CSS-`@keyframes` parse/serialize paths:
  - **Unify on `useKeyframesEditor`** (the existing state+parse+ops composable trio).
    `KeyframesStringControls.vue` (the ONLY editor `AnimationControls.vue:137`
    mounts) re-implements serialize+parse INLINE (`:100-209`) and does NOT use the
    trio at all; `KeyframeTimeline.vue:229-248` hand-parses declarations by raw `:`/`;`
    string-splitting. Elevate `useKeyframesEditor` to THE single `animation ⇄ CSS`
    engine; all three surfaces (Monaco string editor, card editor, timeline inline)
    become thin views calling `core.updateFromString(...)`. Delete the inline
    `updateCSS…`/`onEditorChange`/`syncStoredOptions` and the timeline's string-splitter.
  - **Fold `useKeyframeBrushApply`.** Both editors carry a near-identical
    `applyCSSStyles()` toggling `useApplyCSS` + a hand-built paintbrush
    `CSSKeyframesAnimation`, with DIVERGENT class-name derivation
    (`getTmpAnimationName()` vs `keyframesStyleId` — a latent apply-collision bug).
    One `useKeyframeBrushApply(animation, opts)` composable owns both; settle ONE
    class-name derivation; the paintbrush stays one asset.
  - **Collapse the duplicated `selectedKeyframesControl`** — declared at the store
    TOP level (`controlOptionsStore.ts:18`, default `"string"`) AND nested in
    `keyframeControls` (`:20-25`), the nested default component-seeded by
    `KeyframesStringControls.vue:71-80` (`??= defaultKeyframeControls` — a UI
    component owning the store's sub-shape). ONE store-owned authority; no component
    `??=` seeding; delete the field if the string/card toggle is vestigial (only
    StringControls mounts).
  - **Retire the `Ï` magic-char** format hotkey (hardcoded in
    `KeyframesEditor.vue:190`, `KeyframesStringControls.vue:135`,
    `KeyframesAddDialog.vue`) → glass-ui `registerShortcut` with a named binding.
  - **Hoist `CSSCodeEditor` (+ `monaco-themes/`)** to an `instrument/`-level shared
    Monaco leaf so `timeline` stops reaching into `keyframes/` internals
    (`KeyframeTimeline.vue:156` → `../keyframes/CSSCodeEditor.vue`); both peers
    consume it through one boundary, lazy-chunk split preserved.
  - **Drive `any`→zero at the editor boundaries (lane 05 ask 5).** Re-type the keyframes-editor /
    store / composable seams this unification touches so no `any` survives at the editor boundary
    — the precondition for **U.A3**'s fold of `any-ceiling` into `proof:lint-clean` (the ceiling
    gate retires only once the boundaries it counts are at zero).
- **evidence.** `KeyframesStringControls.vue:100-209` inline vs the composable trio
  (lane 18 F1); `KeyframesEditor.vue:231-290` vs `KeyframesStringControls.vue:211-249`
  brush dup (F4); `controlOptionsStore.ts:18,20-25,39` (F5).
- **shape authority.** lane 24 §9.3 (one authoring core = the module's declared
  contract) + §7 (context DECLARED, not `ReturnType`-derived) + **P5's
  responsibility-recut TEMPLATE** (`287-17`) — the editors converge on ONE keyframe-
  authoring core exactly as P5 hoisted actuation to the host and purified the options form.
- **gate/oracle.** vitest editor round-trip (CSS text ⇄ animation parity across all
  three surfaces — the unification's proof) + `proof:control-surface-single-writer`.
- **size** L. **edges.** Rides B1; CSSCodeEditor hoist feeds B5.

### U.B5 — Shell purge + tier

- **substance** (lane 18 F2/F3). `instrument/shell/` conflates three tenants:
  - **DELETE `EditorHeader.vue`** (108 L) — ZERO runtime importers (verified:
    consumers are `demo/CLAUDE.md` + `layout.css` comment + `shell/index.ts`
    re-export only; the barrel re-export is what hides it from
    `proof:no-dead-export`). Drop its `shell/index.ts` line. NO legacy code.
  - **Move the home-hero trio out of the shared shell.** `HeroAurora.vue`
    (`app/App.vue:44,139`), `AnimatedText.vue` + `TypingDots.vue` (imported only by
    `EditorStartScreen.vue`) are the app's landing identity, not the shared editor
    facility — colocate under `app/` beside `App.vue`.
  - **Tier the residual editor-chrome shell** (EditorShell, EditorStartScreen,
    SharePopover, KeyboardShortcutsModal, useShareState) to the §9 skeleton — give it
    `components/` + `composables/` to match its `keyframes/`+`timeline/` peers (today
    `shell/` is flat, escaping `proof:colocation` only by having no `composables/`
    tier — a structural gap, not compliance).
- **evidence.** `grep -rn EditorHeader` → no runtime consumer (verified);
  `HeroAurora` importer `app/App.vue:139`; `AnimatedText`/`TypingDots` importer
  `EditorStartScreen.vue` only (lane 18 F2); `shell/` flat vs tiered peers (F3).
- **shape authority.** lane 24 §9.1 (the module shape) + §2 (a member colocates with
  its sole owner; the app hero is an app-shell concern).
- **gate/oracle.** `proof:no-dead-export` (EditorHeader row cleared — a NO-DEFERRAL
  discharge, coordinates with U.E's 26-row sweep) + `proof:colocation` (the
  tier-once-a-peer-has-subcomponents-and-a-composable clause, lane 18 F3) +
  owner-golden (the home hero renders under `app/`).
- **size** M. **edges.** Rides B4 (shares the CSSCodeEditor/Monaco hoist).

### U.B6 — `@/state` hygiene + `controlSurfaceDFA` split

- **substance** (lane 18 F8 + colocation verdict). `@/state` is honestly global
  (machine, two option stores, storeUtils, hashSharing) — two refinements:
  - **Disciplined `applySharedState(patch)` writer.** `restoreStateFromParam` does
    `Object.assign(useAnimationGroupsOptionsStore().value, state.options)`
    (`hashSharing.ts:59-67`) — a second, undisciplined writer bypassing the
    single-writer/DFA boundary the rest of `@/state` enforces. Give the option stores
    an `applySharedState(patch)` entry that validates+assigns under the store's own
    authority; hashSharing calls THAT. Document (or fix) the shared-URL
    playback-position omission (it shares options+controls but ignores the
    `perScene` snapshot).
  - **Split `controlSurfaceDFA.ts` (309 L)** — two fused things: (a) the pure
    control-surface DERIVATION (`surfacesFor`, `selectedSurfaceFrom` — genuinely
    global) stays as `controlSurfaces.ts` in `@/state`; (b) the dock PRESENTATION
    (`SURFACE_META` tab labels/icons `:145-160`, `dockCardinality` `:266-309`,
    `ControlSurfaceTab` descriptors) moves to a facility-level `surfaceTabs.ts`. The
    derivation → state, the tab-metadata → the instrument facility.
  - **Drive `any`→zero at the store/composable boundaries (lane 05 ask 5).** Re-type the
    `@/state` store + composable seams this wave opens so no `any` survives — with U.B4's editor
    re-typing, the precondition for **U.A3**'s fold of `any-ceiling` into `proof:lint-clean`.
  - **Discharge BG-9 (the EasingPicker write-through gap, lane 30 ask 7).** Give the easing Curve
    facet's `EasingPicker` a controlled `modelValue`/`points`-in write-through so the demo drives
    it by `v-model`, retiring the per-selection `:key` REMOUNT re-seat. This is the kf-side
    DISCHARGE — dischargeable WITHOUT a glass-ui change; the U.F6 letter RECORDS it
    discharged-kf-side, not a glass-ui ask.
- **evidence.** `hashSharing.ts:20-27,51-72` (F8); `controlSurfaceDFA.ts:145-160,
  266-309` (state colocation verdict).
- **shape authority.** lane 24 §9.2 (a member lives at the lowest dir containing its
  consumers — the dock presentation belongs with the dock).
- **gate/oracle.** `proof:control-surface-single-writer` + the
  `scene-machine-reducer`/`control-surface-dfa`/`sharing` vitest suites (re-homed
  with U.B8's test mirror regroup).
- **size** M. **edges.** Rides B1; `surfaceTabs.ts` destination coordinates with B7's
  facility contract home.

### U.B7 — SceneFacility subsumption + contract hoist

- **substance** (lanes 02/29 F1 + lane 20 F3). The SceneFacility "unification" is a
  half-measure — the legacy duality survives:
  - **DELETE the dual path.** Every group scene exposes BOTH `facility` (whose
    `facilityFromGroup` always sets `.group`) AND a redundant `animationGroup`
    (`CubeScene.vue:249`, `AmigaScene.vue:237`, `SquareScene.vue:316`, each
    self-labeled "the legacy `animationGroup`"); `SceneExposedApi` keeps both and
    admits it (`sceneExposedApi.ts:24-31`); the shell branches on family in three
    seams, incl. the unreachable `facility?.group ?? sceneRef.value?.animationGroup`
    dead fallback (`useSceneMachineShellBinding.ts:67`). Give `SceneFacility` a stable
    `identity` (built once per entry from `facility.group ?? facility.playback`, not a
    churning `computed`); make scene "playing" a UNIFORM readonly projection of
    `facility.playback.isPlaying()` for both families; then delete `animationGroup?` +
    `scenePlayback?` from `SceneExposedApi`, the `??` fallback, and the `ownsPlayback`
    discriminator. One descriptor, one play authority, one identity.
  - **Hoist the contract out of the shell.** `app/scene/sceneFacility.ts` DEFINES the
    `SceneFacility`/`ChannelHandle`/`SceneFacet` contract that six scenes + the
    instrument transport + the state DFA depend UPWARD into — a dependency inversion
    (leaf → shell). Hoist to `demo/composables/scene-facility/` (contract + `facilityFromGroup` — the OD-U2 dissolution home);
    `app/` then DEPENDS on the contract instead of owning it, and
    `controlSurfaceDFA`/`controlSurfaces.ts` (U.B6) imports the REAL type instead of
    the `:58-59` structural duck-type dodge.
  - **The `SceneFacet` contract IS the OD-U12 multi-facet seam (design authority).**
    The `SceneFacet` this hoisted contract defines is the unit OD-U12 composes per
    scene — every scene exposes its facets (controls / keyframes-if-befitting /
    scene-specific: cube's matrix, spring's physics, etc.), the shell reveals one at a
    time, and heavy editors load ONLY when their facet is SHOWN. U.B7 fixes the
    CONTRACT so the facet is a single-authority descriptor; **U.B11** lands the
    async load seam over it and **U.D** the delivery.
- **evidence.** `sceneFacility.ts:4,112` (the "replaces the triple" claim vs the
  always-set `.group`); `sceneExposedApi.ts:24-31`;
  `useSceneMachineShellBinding.ts:67` (dead `??`); the three scene `animationGroup`
  exposures (lane 29 F1, verified cites).
- **shape authority.** lane 24 §9.2 rule 4 (a contract crossed by scenes ∪
  instrument ∪ state belongs in the shared tier) + §9.3 (`defineExpose` only against a
  NAMED interface — the `SceneExposedApi` precedent).
- **gate/oracle.** `proof:scene-facility` RE-ARMED (assert the SOLE seam: no
  `animationGroup`/`scenePlayback` field, no `ownsPlayback`) + scene-machine binding
  vitest + owner-golden (all six scenes play through the one seam).
- **size** M. **edges.** Rides B1. **Edge U.C2:** lane 29 F2's `serialize()/hydrate()`
  engine codec (the demo's eight-field hand-reseat) is a LIBRARY wave → **U.C2** (the
  `seek`/`adoptClock` codec), not here. **Edge U.F:** lane 29 F3's `plain-vars` retirement rides the value.js re-pin →
  **U.F**. U.B7 owns only the demo-side facility subsumption (F1).

### U.B8 — Scenes convergence + `app/runtime/` split

- **substance** (lane 19 F1–F8 + lane 20 F4). Six SOTA scenes, collectively
  un-converged — four different rAF-ownership idioms:
  - **Generalize `useRafScene` → `useManagedLoop`** (own the `markRaw(RAFPlayback)` +
    bound idempotent start/stop + `onScopeDispose` + `useSceneVisibilityPause`).
    `sequence` re-hand-wires the entire recipe (`useSequenceDemo.ts:188-450`) and
    `square` owns a bare `new RAFPlayback()` (`useSquareDemo.ts:217`); route both
    through `useManagedLoop` — no scene may type `new RAFPlayback()` at scene scope.
  - **Kill sequence's 60 Hz reactive-progress write** — `syncFromSequence` writes
    `progress.value` unconditionally every frame (`:179-195`), the exact anti-pattern
    easing/spring were refactored away from. Fold onto the hot/cold split
    (`useThrottledReadout(PROGRESS_READOUT_HZ)` cold path + a non-reactive position
    channel like spring's `scrubberPhase`); the redundant second mirror loop
    disappears.
  - **Extract `usePainterRegistry<P>()`** into the scene-runtime tier — easing's
    `dotPainters` (`useEasingDemo.ts:186-201`) and spring's `springPainters`
    (`useSpringHotPath.ts:101-117`) are byte-identical `Set<Painter>` + register +
    repaint trios; ONE audited primitive.
  - **Charter `useSweepScene({ sample, duration, onScrub })`** over `useManagedLoop`
    — easing and spring's Sweep channel are the SAME machine (~120 L of near-identical
    control-plane: phase clock + `startTime` rebase, machine-gate + reconcile-on-stop,
    `useSceneTransport`, throttled readout, painter registry, `RESET`). The recipe
    owns the control-plane; the scenes shrink to their distinct payload (easing's
    curve catalog; spring's solver+presets+heatmap), dropping both composables under
    the 500 L ceiling.
  - **DOGFOOD dogfood (NO legacy).** `useSquareDemo.ts` hand-writes `num()` (a
    `"42px"→42`/`"108%"→1.08` normalizer, `:75-87` — value.js `ValueUnit`'s job) and
    `toRGB`/`sweepHue` (naive sRGB parse + linear mix, `:187-214` — while
    `SpringHeatmap.vue:169` correctly uses `color-mix(in oklab, …)` and the library's
    headline is perceptual color). Delete `num()` for `ValueUnit`; replace
    `toRGB`/`sweepHue` with value.js `Color` perceptual mix. The demo cannot hand-roll
    primitives the constellation ships and exists to showcase.
  - **Colocate** the square tumble egg → `useSquareTumble.ts` (the derby precedent),
    localizing the F5 color cure; extract `channelFromAnimation(anim, name)` in
    `sceneFacility.ts` as the single clamp-by-duration channel authority; standardize
    teardown on `onScopeDispose` across all scene units (amiga included).
  - **Split `app/runtime/`** (lane 20 F4): the four scene-consumed recipes
    (`useRafScene`→`useManagedLoop`, `useSceneVisibilityPause`, `useSceneTransport`,
    `rafConstants`) → `demo/composables/scene-runtime/` (an encapsulated module);
    the two app-private guards (`loaf-observer`, `useMonacoCancellationGuard`) → B9's
    `app/lifecycle/`.
  - **The async-tolerant scene-machine contract.** The scene-machine's `AnimationGroup` prop
    becomes nullable/async-tolerant (Suspense-gated hydration on engine resolve) so `main.ts`
    mounts BEFORE the engine warm (the hero has zero engine dependency) — co-owned with
    **U.D5**-F3 (the LCP decouple); the group hydrates when `loadAnimationEngine()` resolves.
- **evidence.** `useRafScene.ts:14-22` docstring (the consolidation that stopped at
  2/4); `useSequenceDemo.ts:179-195,188-450`; `useSquareDemo.ts:75-87,187-214`;
  `useEasingDemo.ts:186-201` / `useSpringHotPath.ts:101-117` (lane 19 F1–F6).
- **shape authority.** lane 24 §9.4 (scene sub-modules by ownership — spring's 6
  components/6 composables → `target/`, `heatmap/`, `physics-facet/`) + §9.2 + **P5's
  responsibility-recut TEMPLATE** (`287-17`) — the scenes recut on the same host-hoist /
  options-purify pattern the loop proved.
- **gate/oracle.** scene vitest (`scene-raf-leak.test.ts`,
  `scene-visibility-pause.test.ts`, per-scene) preserved through the moves + a
  headless dogfood witness (grep: no hand-rolled unit/color parse in `scenes/`) +
  owner-golden per scene.
- **size** L. **edges.** Rides B1 and B7 (the facility contract home + `channelFromAnimation`);
  ↔ **U.D5** (F3 — the async-tolerant `AnimationGroup` contract / LCP decouple, co-owned).

### U.B9 — App-shell honesty + shared-tier re-homing; empty the DEFERRED residuals

- **substance** (lane 20 F4/F5/F7 + lane 26 Finding 3, lane 32 F3/F5).
  - **`app/` becomes the shell, not a scene utility belt.** The scene-recipes left
    `app/runtime/` at B8; the two app-lifetime guards fold into `app/lifecycle/`.
  - **Colocate the instrument-only utils** — `iosTextEntry.ts` (consumed only by
    `EditorShell.vue` + `CSSCodeEditor.vue`) and `toastGuard.ts` (only
    `KeyframesAddDialog.vue` + `CSSPasteDialog.vue`) → `instrument/utils/` (used
    across ≥2 instrument peers → the facility's own tier). Hoist `kfEngine.ts` (the
    widest fan-out — 13 consuming areas, the demo's one universal engine-loader seam)
    to the demo root as `demo/kf-engine.ts` (under OD-U2's dissolution the demo root
    IS the shared tier). Leave `clipboard.ts` with CopyButton.
    `@/utils/` shrinks to genuinely-global helpers.
  - **Relocate the mislocated singletons** — `SceneSkeleton.vue` (App-only Suspense
    fallback, one consumer) → `app/` beside `App.vue`, deleting the one-member
    `@/components/skeletons/` tier; `font-roles.json` (consumed only by two gates, a
    manifest not a style) → `scripts/`; `glass-ui-gaps.ts` (spans app/ + the hoisted tiers) off the
    demo root into `demo/utils/`.
  - **Discharge the former `DEFERRED` residuals.** `kfEngine` now lives at the
    demo root with a three-plane `@kf-engine` alias; the plain
    `gestureSelectSuppression` singleton lives in `demo/utils/`. The deferred map
    and tolerant branch are gone from `proof-colocation.mjs`; the gate is now
    tolerance-free (an exception changes the RULE, never enrolls in a list).
  - **`demo/CLAUDE.md` is DELETED, not reconciled (OD-U15).** Its stale composables
    inventory (omits `useDoubleTap`/`useThrottledReadout`; undocumented
    `sceneFacility.ts`; `useHeroSourceEgg` listed but deleted) is one more exhibit for
    the removal — the load-bearing content re-homes inline or into the README per
    U.E7's OD-U15 wave; this wave writes NOTHING into it.
- **evidence.** util consumer counts (lane 20 F5); `SceneSkeleton` sole consumer
  `App.vue:140` (lane 24 §2); `font-roles.json` gate-only (lane 20 F7);
  `proof-colocation.mjs:69-96` (verified DEFERRED machinery).
- **shape authority.** lane 24 §9.2 (single-owner satellites colocate into their
  owner; kind-bins only at facility/shared level with ≥2-consumer members).
- **gate/oracle.** `proof:colocation` (now tolerance-free: `app/` shell-only, `@/utils`
  global-only) + `proof:shared-has-n-consumers` + vitest.
- **size** M. **edges.** Rides B2/B4/B8 (all hoist destinations settled first).

### U.B10 — Skeleton tier per module (T.F8 completion)

- **substance** (lane 24 §2). The "skeleton tier" is ONE orphan
  (`@/components/skeletons/SceneSkeleton.vue`, one consumer) in the exact category-bin
  the edict forbids; the OTHER lazy seams have NO skeleton (the Monaco panes are
  `defineAsyncComponent` with no `loadingComponent` — `AnimationControls.vue:252-253`
  — so the pane renders NOTHING during the Monaco/highlight chunk fetch, a
  layout-shifting blank that is a PERFORMANCE-edict defect). Ratify the skeleton as a
  MEMBER OF THE MODULE (§9.1): every lazily-delivered module owns
  `<Name>.skeleton.vue` beside `<Name>.vue`, and the module's OWN barrel wires
  `defineAsyncComponent({ loader, loadingComponent: NameSkeleton, delay: 150 })` — the
  placeholder ships in the eager barrel chunk while the payload stays split.
  `SceneSkeleton.vue` re-homes to `app/` (B9); `@/components/skeletons/` is deleted;
  the house skeleton spec inherits SceneSkeleton's a11y contract (role=status,
  aria-busy, reduced-motion static plate, `:28-99`).
- **evidence.** `SceneSkeleton.vue:2-9` (one file, one consumer);
  `AnimationControls.vue:252-253` (no `loadingComponent`); `scenes.ts:226-227`
  (descriptors carry none, the shell owns the `<Suspense>` fallback) (lane 24 §2).
- **shape authority.** lane 24 §9.1 (the skeleton member) + §2.
- **gate/oracle.** `proof:colocation` SKELETON CLAUSE (lazy barrel without a
  `loadingComponent` → RED, lane 24 §10) + owner-golden (no scene-swap text-flash).
- **size** M. **edges.** Rides each module recut (B2/B4/B5/B8 — the modules must exist
  in §9 shape before their barrels can wire skeletons).

### U.B11 — Barrel discipline + async-seam FACET cuts (OD-U12)

- **substance** (lane 26 Finding 1/2/7, R1/R2; **OD-U12 the design authority**). The
  T.F5 lazy-barrel apparatus is decorative and one deep import bypasses it into a
  906 KB leak. **OD-U12 (verbatim design authority) governs this wave:** every scene
  composes the shared multi-facet instrument — controls / keyframes-if-befitting /
  scene-specific facets (cube's matrix, etc.) — and heavy editors (Monaco, highlight)
  load ONLY when their facet is SHOWN, never inline in a scene and never eagerly in a
  facet module graph. The moves:
  - **DELETE the zero-consumer umbrella** `instrument/index.ts` (`export *` ×4, ZERO
    importers — verified: only `demo/CLAUDE.md` references it). Its `export *`
    contradicts the house's own barrel policy (`proof:no-flat-siblings` clause 4
    mandates explicit-named barrels). An unconsumed export surface IS legacy.
  - **Barrels become explicit-named EAGER contracts** — `defineAsyncComponent`
    LEAVES the barrels (re-exporting async-ness makes the boundary invisible at the
    call site). Laziness lives at the CONSUMER's seam (scene/router entries;
    pane-reveal `defineAsyncComponent`; heavy-vendor bearers).
  - **Excise the OD-U12 NAMED violation — the spring inline-editor import.**
    `SpringPhysicsFacet.vue:133` eagerly deep-imports `KeyframesEditor` (verified) —
    the "inline monaco editor for spring" the owner named ("We should not have an
    inline monaco editor for spring") — which statically drags `vendor-highlight`
    (906 KB) into `SpringScene`'s entry chunk (built-graph trace, lane 26 Finding 1).
    The facet's editor card is a reveal boundary; the import becomes
    `defineAsyncComponent` at the facet's seam (the same class as
    `AnimationControls.vue:252`), so Monaco/highlight load ONLY when the keyframes
    facet is shown — never eagerly for the spring scene.
  - **Depcruise demo ruleset** — `no-cross-module-deep` (cross-module imports go
    through the module's `index.ts`; intra-module stays deep-relative),
    `no-self-barrel`, `no-star-export` — folded into the EXISTING depcruise run (no
    new script). The library half (RULING 1: cross-zone through the sibling barrel;
    `internal/` stays direct per C-5) is U.C's turf; U.B lands the demo rules.
- **evidence.** `SpringPhysicsFacet.vue:133` (verified eager import); umbrella
  `instrument/index.ts` zero runtime importers (verified); the 13 external consumers
  all deep-import (lane 26 §1.4).
- **shape authority.** lane 24 §9.1 (barrel = re-export-only contract) + lane 26 R1/R2.
- **gate/oracle.** the **`proof:publish` reachability/weight clause** (**U.D-owned**,
  OD-U11 — the standalone `proof:chunk-graph` gate is DROPPED; born-RED on the leak:
  no scene chunk statically reaches `vendor-monaco`/`vendor-highlight`/`html2canvas`;
  `vendor-three` only from AmigaScene) + depcruise + `proof:published-surface`
  unaffected. `highlight.js` retirement + Monaco on-demand delivery are **U.D**.
- **size** M. **edges.** Rides B1; co-scheduled with U.D's `proof:publish` reachability
  clause (OD-U11 — the facet cuts are what green it; DAG: "the clause lands with B's
  module cuts").

### U.B12 — Grammar + CSS-split residual sweep + the §10 standing clause

- **substance** (lane 24 §3/§4/§6/§8/§10, lane 26 R3/R4/Finding 4/8). The move-waves
  convert grammar as they touch imports; this LAST wave sweeps the residue and turns
  the codex into standing law:
  - **Residual API grammar** — convert any `withDefaults`/runtime-object props (the
    lone runtime-object holdout `CopyButton.vue:27`) + manual `update:*` emits
    (`TimelineTrack.vue:129`, any survivor) not already converted; add `defineSlots`
    to slotted components (EditorShell 9 sites, AnimationControlsGroup 4, …); complete
    the lexicon rename (facility/transport/channel/scene/target/facet).
  - **The Kf-vanity retirement** — `KfPillTabs`/`useKfPillTabs` et al. (the `Kf`-prefixed
    vanity names) rename to the ratified U.G lexicon, DROPPING the `Kf` prefix, and where the
    pill-tabs merely re-implement glass-ui's public tabs surface, ADOPT **`SegmentedTabs`**
    (glass-ui `custom/tabs/` — the PUBLIC tabs component per audit §1; `ui/tabs` is off the
    public surface), in the SAME import-touching moves — so U.R4's "Kf-vanity de-vanitized"
    clearing-oracle is satisfiable (RED until zero `Kf`-prefixed vanity survives).
  - **CSS split** — `SpringTarget.vue` keeps a 200 L inline `<style scoped>` block
    (the largest SFC, no split) while smaller SFCs split to `<Name>.css`. Split it via
    the house `<style scoped src="./SpringTarget.css">` idiom; make the split MECHANICAL
    (>100 L style block OR >300 L SFC → sibling; every demo `.css` ≤300 L).
  - **`@apply` confinement** — `@apply` legal only under `@/styles/`; `@reference`
    banned (per-SFC theme-graph import cost); scoped blocks consume tokens + plain CSS.
  - **The §10 standing clauses (extend, do not add):** `proof:colocation` gains the
    inverse-colocate + barrel-purity + skeleton clauses; `proof:style-file-ceiling`
    absorbs `proof:demo-no-oversize`'s survivors as the all-demo-`.css` + SFC
    style-block sweep (retiring the `@/styles`-only scope AND the retired oversize
    gate); `proof:styling-idioms` gains the `@apply`/`@reference` grep-clause. One AST
    pass, no browser — survives the CI trim.
- **evidence.** `CopyButton.vue:27` (the runtime-object holdout); `SpringTarget.vue`
  200 L block (lane 26 Finding 4); `@apply` ×11 all in `design-idioms.css:94-114`
  (lane 26 §1.5); zero `defineSlots` tree-wide (lane 24 §8).
- **shape authority.** lane 24 §9.3 (the API grammar) + §10 (gate posture) + lane 26
  R3/R4/R6/R7.
- **gate/oracle.** the extended existing gates (no new script) + `proof:styling-idioms`.
- **size** S. **edges.** LAST; U.G ratifies §9/§10 as the codex this wave enforces.

### U.B13 — The amiga animation + scene suspend/resume cure (OD-U13)

- **substance (OD-U13, owner-mandated in-U — "our amiga animation, and our scene
  suspend/resume facilities have been broken for many tranches … investigated from
  first principles and fixed within this tranche").** The amiga scene's animation and
  the demo's scene suspend/resume facilities have been broken across multiple tranches
  while the gates said green throughout (a vacuous-green exhibit for RC-1). The
  first-principles investigation COMPLETED in U-dev; the dossier is
  **`audit/defect-amiga-suspend-resume.md`**, and it SPLIT the mandate:
  **Defect A (the dominant bug) is a LIBRARY bug, not a demo bug** — the plain-vars
  projection caches a stale leaf reference and the amiga sphere freezes at the 25%
  pose ~2s into EVERY play (a live 5.2.0 product defect; only the
  `singleTarget`+`unflatten` path hits it, which is why cube/square dodge). Its cure
  is **U.C14** (the library composite-state cure — the D1 amiga instance of the
  widened three-instance stale-leaf class, with its born-RED past-the-segment-boundary
  characterization); this wave CONSUMES U.C14. **Defect B (the demo half, THIS
  wave):** the "resume broken" symptom is mostly Defect A wearing a mask — the
  machinery is otherwise sound (no orphaned rAF loops; cube/easing suspend/resume
  cleanly) — but ONE real gap remains: amiga's tab-visibility pause governs only its
  WebGL render loop, never its animation-group clock (machine `TAB_HIDDEN` is
  status-only), so the group drifts while hidden and jumps forward on return. The
  cure: ONE symmetric suspend contract — the IntersectionObserver + visibility pause
  + machine `TAB_HIDDEN` fold into the same seam governing BOTH the render loop and
  the group clock, converged onto the `useManagedLoop` recipe U.B8 charters, so
  suspend is jump-free exactly like the raw-rAF scenes.
- **The witnessed-cure discipline (← U.H1).** The characterization net (U.H1) must
  COVER scene-switch and visibility (suspend/resume) semantics BEFORE the fix lands,
  so the cure is WITNESSED (green-across-the-fix = behavior restored), not asserted —
  the exact discipline the vacuous-green history violated.
- **shape authority.** lane 24 §9 (the amiga scene recut to the module skeleton) +
  U.B8's `useManagedLoop` convergence (the shared loop the cure rides).
- **gate/oracle.** the U.H1 net (scene switch/visibility covered FIRST) + owner-golden
  (amiga plays; suspend→resume clean across every scene switch).
- **size** M. **edges.** Rides **U.B8** (useManagedLoop convergence — the amiga loop
  joins the one recipe); ↔ **U.C14** (Fix A — the library composite-state cure, the
  D1 amiga instance, consumed here); ← **U.H1** (the net covers scene
  switch/visibility BEFORE the fix). Owner-mandated in-U (OD-U13); the impl drive
  lands it IN U.

### U.B14 — THE SMALL-MODULE INLINE WAVE (OD-U16 inline direction, U.B half)

- **substance (OD-U16 the INLINE direction; loop-chartered, band U.L — P2's demo half,
  frozen evidence `287-11`).** OD-U16 rules module granularity BOTH ways: U.C7/U.C8
  execute the long-file CARVE direction; THIS wave (with U.C's constants fold) executes
  the OPPOSITE — **absurdly-small modules inlined-and-deleted.** The convergence loop's
  P2 prototype (small-module-inline-sweep, EXITED at 100 at pass 2) produced the demo-side
  **fold-map that IS this wave's work order**: each single-symbol / single-consumer demo
  module is inlined into its sole owner and deleted, **cut target-by-target from the
  fold-map — NEVER a bulk sweep** (the G3/G9 per-target adjudication form: a module is
  inlined only where the fold-map names it, with its one consumer, BY NAME — ruling 24's
  in-sweep-space ≠ disposed clause). A module with ≥2 genuine consumers KEEPS its file
  (the placement rule, unchanged).
- **paired with the U.C constants fold.** OD-U16's inline direction is **U.B+U.C paired**:
  the demo half (this wave) and the library half (U.C's constants back-compat barrel
  dissolution + P2's byte-clean LIBRARY fold-map, same `287-11` evidence) run the SAME
  target-by-target discipline and co-schedule, so the OD-U16 both-directions ruling closes
  in one motion.
- **evidence.** P2's demo fold-map (`287-11`) — the per-target inline ledger the loop
  minted and froze; each row = one small module + its sole consumer + the inlined home.
- **shape authority.** the placement rule (a member lives at the LOWEST dir containing all
  its consumers — inline where that dir IS the sole consumer) + OD-U16 (granularity both
  directions) + the fold-map zero-loss discipline (OD-U1 — a deletion proves its content's
  new home).
- **gate/oracle.** `proof:colocation` (a single-symbol module with one in-area consumer is
  a colocation defect the inline cures — no new clause) + the demo vitest suite green
  through each inline (a pure move); the fold-map's completeness is the deliverable (every
  named target inlined or exempted-by-name).
- **size** M. **edges.** Rides **B1** (the homes must settle first); ↔ **U.C** (the OD-U16
  inline direction, U.B+U.C paired — the library constants fold rides the same
  target-by-target discipline); loop-chartered (OD-U18, band U.L).

---

## §4 — The test mirror (OD-U7, R5) — a cross-wave discipline

Per **OD-U7 / lane 26 R5**, tests MIRROR (`test/demo/<area>/`) and never colocate into
`demo/` — a test is a CONSUMER of a module's surface, not a private satellite;
colocation would tax every structural gate with exclusions and put test-only edges in
the dev-server module graph. `test/demo/` is today a FLAT 24-file bucket spanning four
areas. As U.B moves each area, its tests regroup into `test/demo/{scenes,instrument,
state,app}/…` (rides B2/B4/B6/B7/B8), and the mirror gains its integrity clause (each
test's primary import resolves into the area it is filed under — foldable into
`proof:zone-cohesion`, no new script). This is a carve-out of the colocation edict,
owner-blessed via OD-U7, executed AS PART OF each move-wave, not a standalone wave.
**U.H3 is the ratifying wave** for this test↔demo mirror regroup (co-scheduled per
OD-U7): U.H3 ratifies R5/OD-U7 and lands the mirror invariant as
`test/support/mirror.test.ts`, and each U.B area move re-files its mirrored tests in
the SAME commit (← U.H1 (net), ↔ U.H3 (mirror regroup)).

---

## §5 — Risks + the re-arm map

The stale-era re-arm class is EXPECTED (U.md §5): every U deletion invalidates some
gate's path-literal or shape expectation. Disposition is re-arm-WITH-the-wave or
delete-citing-the-ruling.

| gate / test | invalidated by | disposition |
|---|---|---|
| `proof:colocation` | B1 (tolerant→REQUIRING), B2 (inverse clause), B5 (tier clause), B9 (DEFERRED map deleted), B10 (skeleton clause), B12 (barrel-purity) | **RE-ARM incrementally** — the keystone is progressively forged from the S/T-era tolerant blindspot into the REQUIRING enforcement arm; tolerance machinery DELETED at B9 (net simpler). The acceptance oracle shifts from import-count locality to "owner reads the tree at first `ls`" (lane 03). |
| `proof:demo-no-oversize` | B2 (it manufactured the orphan-composable drawer) | **RETIRE** (U.A CI-trim) — a `.vue`'s line count is not a defect; a mis-homed composable is. Survivors (all-demo-`.css` + SFC style-block) absorbed into `proof:style-file-ceiling` at B12. |
| `proof:style-file-ceiling` | B12 (all-demo-`.css` sweep + SFC style-block clause) | **RE-ARM + MERGE** — retire the `@/styles`-only scope; absorb the oversize survivors. Net −1 gate. |
| `proof:shared-has-n-consumers` | B1 (`custom` special-case `:174` deleted), B9 (re-homes) | **RE-ARM** — delete the `custom/` module-ID special-case in the same commit; counts unchanged. |
| `proof:scene-colocated` / `proof:decomposition` / `proof:hero-two-focal` / `proof:pp-logo-svg` / `proof:stage-within-docks` (~34 scripts, `demo/@/` literals) | B1 (path DISSOLUTION — `demo/@/` → `demo/{components,…}/`) | **RE-ANCHOR** the fs-path literals in the SAME commit, co-scheduled with U.A's cull (never two passes; the pass is IDENTICAL whether renamed or dissolved). `proof:hero-two-focal` re-anchors to `app/` when the hero moves (B5); several are CI-trim candidates (U.A). |
| `proof:scene-facility` | B7 (dual-path deleted) | **RE-ARM** to assert the SOLE seam (no `animationGroup`/`scenePlayback` field, no `ownsPlayback`). |
| `proof:no-dead-export` | B5 (EditorHeader excised) | **RE-ARM** — one deferred row cleared (coordinates with U.E's 26-row NO-DEFERRAL sweep). |
| `proof:control-surface-single-writer` | B4 (store-field collapse), B6 (`applySharedState`) | **RE-ARM** — the disciplined writer strengthens it; DFA split preserves the derivation it checks. |
| depcruise (existing run) | B11 (demo ruleset) | **EXTEND** — `no-cross-module-deep`/`no-self-barrel`/`no-star-export` folded in; no new script. |
| `proof:styling-idioms` | B3 (idiom-CSS moved), B12 (@apply clause) | **RE-ARM** — the `.btn-playback`/`.tab-trigger` classes resolve from `design-idioms.css`; add the @apply/@reference confinement clause. |
| `proof:transport-play-first-render` · `no-single-option-select` · `dfa-derived` · `stage-inventory` · `cursor-light-subtle` | B1 (path), B2/B3 (transport recut) | **RE-ANCHOR path; behavior PRESERVED** — verified by vitest + owner-golden (the T-blessed product surface is transposed, never redesigned). |
| the `proof:publish` reachability/weight clause (**U.D-owned**, OD-U11 — NOT a standalone gate) | born-RED on the 906 KB spring leak | **LANDS WITH B11** — the facet async-seam + umbrella delete are what green it; the standalone `proof:chunk-graph` is DROPPED into a `proof:publish` clause (net NEW standalone gates = ZERO). |
| `test/demo/*` (flat 24 files) | every area move (B2/B4/B6/B7/B8) | **RE-HOME** into `test/demo/<area>/` (OD-U7/R5 mirror), with the import-resolves-into-area integrity clause folded into `proof:zone-cohesion`. |

**The one standing hazard.** The keystone gate (`proof:colocation`) is being edited by
nearly every wave. The T-era failure was a gate SHAPED to pass on the rejected tree; the
discipline (U.md §5, T4/T5) is that the orchestrator INDEPENDENTLY re-runs the re-armed
gate on the merged tree and that each new clause is authored to RED on today's tree
FIRST (the born-RED plant), then green only after the move lands — never green by
loosening. A keystone that cannot fail on the rejected structure is retired, not shipped.
