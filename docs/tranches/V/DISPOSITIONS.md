# Tranche V — Terminal Disposition Ledger

**Date:** 2026-07-17 · **Scribe:** dispositions-scribe · **Tree:** `/Users/mkbabb/Programming/keyframes.js` @ `a59d3a22` (uncommitted K6/Glass-7 transaction live)

Every chronic, deferred, banked, partial, or refuted item carries **exactly one** terminal state: **BUILD** (named wave), **FOLD** (named wave), **RETIRE** (rationale), **BANKED** (concrete external re-trigger). Re-booking is forbidden. A row that rode ≥2 closes un-decided is marked **DISEASE** and its terminal decision cites the wave that kills it. Fresh build findings (FAM-05/06/07/09/10/14/15 non-chronic members) are BUILD rows owned by W4–W11 in the wave charter, not re-listed here — this ledger dispositions carried/chronic/refuted/partial rows only.

Wave map key: W1 Render Truth · W2 Glass-7 Consume · W3 Native Close & Deploy · W4 Structure Gate · W5 Module Carves · W6 Lib Encapsulation · W7 Demo Grammar · W8 Kind-Dir Dissolution · W9 Make-Real Quartet & Prune · W10 Doc Canon · W11 Proportion & Affordance · W12 Constellation & Ledger Terminal · W13 Close.

---

## A. Rail inheritance (FAM-01 — producer-gated, born RED; external re-trigger = the immutable Glass 7 packet)

| Row | Origin (tranche/lane) | Closes ridden | DISEASE? | Terminal state | Owner/rationale |
|---|---|---|---|---|---|
| CH-01 FINAL-U "terminal 5.3.4 / no V backlog" superseded within a day | R1-10 CH-01 (`FINAL-U.md:124-125,161-165`); npm `latest=6.0.0` | U close (1) | No (1 close) | **BUILD W2** | Handoff — not FINAL-U — is the real V inherit; reconstruct 65-path K6 consumer slice on immutable base, consume Glass 7 demo-only devDep, deploy |
| CH-02 Glass-6/6.0.0/Atlas-2.0 boundary reworded-not-decided | R1-10 CH-02 (`FINAL-U.md:114-122`); K6 shipped no glass edge, Atlas is 4.0.0 (`handoff §6:444`) | U close, reworded in K6 txn (1) | **Watch** — re-word-not-decide behavior; becomes DISEASE if V re-words again | **FOLD W10** | Rewrite the boundary to the real Glass-7/K6/Atlas-4 rail or annotate "SUPERSEDED — see AGENTIC-HANDOFF-2026-07-16 §0 DAG"; must re-decide, never re-word |
| CH2-02 T's four dock-crispness tripwires DE-TRIPWIRED (subject-legible/blur-not-resampled/dock-rest-crisp/dock-morph-continuity) | R2-03 CH2-02 (`T/FINAL.md:83-100`); apparatus deleted at U | T close → U close (2), ungated | **Yes** (T→U un-gated) | **FOLD W2** — killed here | Re-instate one dock-crispness live check on the Glass-7 consume, OR retire the four with a one-line "Glass 7 shipped the fix, verified on running demo" rationale. No silent ungated hope |
| PR-2 demo consumes an undeclared, unpublished, registry-absent Glass 7 (43 files import it) | AUDIT-REGISTRY FAM-01 (`AUDIT-REGISTRY.md:15`) | born at V formation | No | **BUILD W2** | Exact glass devDep + registry lock + one-physical-core; born RED until immutable Glass 7 packet |
| CT-01/CT-02/DD-6 manifest+lock carry NO glass edge → any `npm` op prunes the swapped linkage | AUDIT-REGISTRY FAM-01; live-confirmed (`GLASS-AUDIT-LINKAGE.md:28-35`) | born at V formation | No | **BUILD W2** | The consume must declare glass-ui in package-lock so npm stops reconciling it extraneous |
| XR-1 / AV-8 the 65-path manifest digest is unreproducible (recipe unpinned) | AUDIT-REGISTRY FAM-01 & R2 synthesis (`AUDIT-REGISTRY.md:15,57`) | born at V formation | No | **BUILD W2** | Pin the manifest script; reproducible 65-path slice digest |
| XR-4 HeaderRibbon Glass-7 consumer edits unapplied (drop `mode=`, delete `defineExpose`) | AUDIT-REGISTRY FAM-01 (`AUDIT-REGISTRY.md:15`) | born at V formation | No | **BUILD W2** | Apply the Glass-7 consumer edits during the consume slice |
| WT-01/WT-02/WT-03 worktree-transaction rail findings | AUDIT-REGISTRY FAM-01 (`AUDIT-REGISTRY.md:15`) | born at V formation | No | **BUILD W2/W3** | Fold into the consume + native-close transaction; V re-measures worktree counts (FAM-13) |
| CC-05 Glass-7 API-change watchlist of 8 queued changes | R2-09 (`AUDIT-REGISTRY.md:57`) | born at V formation | No | **BUILD W2** | Re-verify each of the 8 against the immutable Glass 7 packet before consume |
| DP-02 / FAM-02 blank-on-all-7-routes crash: `EditorShell.vue:30` `<Tooltip>` without root `<TooltipProvider>` | R1/R2 FAM-02 (`AUDIT-REGISTRY.md:16`); AV-1 sole-crash proven | born at V formation | No | **BUILD W1** | Demo-owned root `<TooltipProvider>` (documented Glass-7/reka pattern per CC-03); gate = pageerror==0 on 7 routes × 2 viewports |

---

## B. Chronics

| Row | Origin (tranche/lane) | Closes ridden | DISEASE? | Terminal state | Owner/rationale |
|---|---|---|---|---|---|
| CH-03 removed-feature doc residue, mischaracterized (README:322/428/433/764, published-surface:151, DESIGN.md:67) | R1-10 CH-03 | K6 txn (fresh) | No | **BUILD W10** | Remove only the `BlendMode` type + `"weighted"` op-keyword refs (the actual removed public symbol); correct `Card surface="cartoon"`; PRESERVE live `weight`/`weightSpring` copy; verify vs packed d.ts |
| CH-04 MbabbMenu Tailwind-literal reword | R1-10 CH-04 (`MbabbMenu.vue:6`) | pre-U → U reworded → dropped from handoff | Borderline (reworded once, then silently dropped) | **RETIRE** — killed now | Resolved-by-migration / no defect present; `:6` is a legitimate `min-w-[var(...)]` class, not a prose literal. Retire with :6 evidence so it cannot be re-booked a 3rd time |
| CH-05 "nightly roster" cadence mislabel (cron dow=1 = weekly) | R1-10 CH-05 (`ci.yml:16,50,54,81,92`) | U close (1) | No | **FOLD W10** | Relabel prose "weekly roster" or change cron to daily; also correct FINAL-U:53-55 and the deploy-ancestry freshness-window prose |
| CH-06 / CH2-04 dogfood-inversion:48 "K.WZ does the K-tranche republish" inside a "COMPLETE" doc | R1-10 CH-06 + R2-03 CH2-04 (`docs/dogfood-inversion.md:48`) | K → … → U (never updated), inside "COMPLETE" | **Yes** | **FOLD W10** — killed here | Past-tense or strike :48; `scroll-morph`/`color-fidelity`/all MIGRATION docs swept clean (CH-06 coverage gap discharged) |
| CH2-01 value.js `proof:no-collision-rename` (PropertyDescriptor→CSSPropertyDescriptor) | R2-03 CH2-01 (`dist/keyframes.d.ts:6,747,2563,2567`; value.js 4.0.0) | R→T | No | **RETIRE-DISCHARGED** | Resolved in the live tree; zero `PropertyDescriptor_2` mangle remains. Do NOT re-book from the T backlog table |
| CH2-03 published-surface EP-3 PATH-B (`flip`/`flipShared`/`drag`/`Draggable`/`DrawSVG`/`fromDrawSVG`) | R2-03 CH2-03 (`published-surface.md:196-210`) | J → now, decided-terminal | No | **RETIRE** | Decided-terminal BOOK per P-inv-28; self-maintaining — `proof:publish` machine-checks its coverage table. Flip to PATH A automatically if a befitting demo scene ever lands |
| K engine-seam split / `loadAnimationEngine()` chunk refinement | R2-03 (`K/FINAL.md:66`; landed `src/animation/load-engine.ts:123`) | K → R | No | **RETIRE-DISCHARGED** | `engine.ts` decomposed; the seam is realized. Discharged in R |
| A/B/C `--spring-*` token regen (VAL-9) + bucket-glassui label ASK | R2-03 (`A/B/C FINAL.md` §Deferrals) | A/B/C → absent S/T/U | No | **RETIRE-DISCHARGED** | Constellation-absorbed; absent from the last three tranches. Discard (FAM-11 residue) |
| H VJ-F1/F2/F3 + PT-4 packrat re-key next-slice | R2-03 (`H/FINAL.md:59-101`) | H → absent S/T/U | No | **RETIRE-DISCHARGED** | Constellation-absorbed; absent S/T/U. Discard |
| L Band-B un-consumed edges (value 0.13, glass BB, parse-that 0.9, keyframes-vue) | R2-03 (`L/FINAL.md:205-361`) | L → consumed M/O/Q | No | **RETIRE-DISCHARGED** | Consumed by the M/O/Q constellation drives; keyframes-vue KILLED (R). Discard |
| KILL rows: ScrollTimeline-native (B), Worker/OffscreenCanvas/Atomics (A), `Mod+K` palette FB-6 (J), P.W4 codegen tombstone | R2-03 negatives (`ci.yml`/FINAL corpus) | stayed KILLed A→U | No | **RETIRE** | None re-surface in S/T/U; permanent-KILL confirmed. Do not resurrect |

---

## C. Refuted premises (RETIRE-REFUTED — the false premise cannot re-book)

| Row | Origin (tranche/lane) | Closes ridden | DISEASE? | Terminal state | Owner/rationale |
|---|---|---|---|---|---|
| `compiled-frame.ts` split-as-defect premise | AUDIT-REGISTRY amendment 1 (`AUDIT-REGISTRY.md:31-32`; 8 cross-zone importers) | R1 charter | n/a | **RETIRE-REFUTED** | Correctly-extracted shared contract, kept as-is by W5; `numeric-plan.ts` is the real single-consumer fold. The split-defect premise is false |
| Handoff §3 blanket "delete weighted-blending prose" premise | AUDIT-REGISTRY amendment 2 (`AUDIT-REGISTRY.md:33-34`); CH-03 | U handoff | n/a | **RETIRE-REFUTED** | Weighted blending is a LIVE feature (`weight.ts`, `weightSpring`, `compositor.ts:257`); only the `BlendMode` type + `"weighted"` op were removed. Blind deletion would remove accurate copy — see CH-03 for the surgical build |
| Multi-target-blend "UI promises what the engine refuses" premise | R3-03 RG-N2 (`AnimationControlsGroup.vue:21`, `SquareScene.vue:180`, `LayerConfigPanel.vue` v-else) | R2 assay | n/a | **RETIRE-REFUTED** | The blend `<LabeledSelect>` is NOT rendered on a multi-target group (renders disabled "independent targets"); the defect does not exist. UI is honest |
| EE-02 anon-fn throw attributed to `useTransformState.ts:107` (`easeInBounce`) | R3-02 negative (`R3-02:229-234`) | R1/R2 registry | n/a | **RETIRE-REFUTED** | `easeInBounce` is reference-equal to the registry singleton and fed to a rAF `NumericAnimation` (never serialized). Real source is the transport panel css-less `{fn}` assignment — see EE-02 in §D |
| BV-1 settle-on-long-hide `spin` decay | R3-03 RG-N3 (`R3-03:108-122`); 0/9 reproductions | R2-02 probe | n/a | **RETIRE-REFUTED** | Artifact of the synthetic `visibilitychange` override; every genuine run resumed playing with continuous motion. Not a reproducible engine behavior |
| `internal/` grab-bag premise ("massive explosion") | AUDIT-REGISTRY amendment 4 (`AUDIT-REGISTRY.md:38-41`) | R1 charter | n/a | **RETIRE-REFUTED** | `internal/` is 7/9 multi-consumer; barrels pure; scenes/ colocation exemplary. The explosion was the apparatus (dissolved at U); the restructure is narrow and surgical |

**CT-05 parse-that node_modules residue — RETIRE** (terminal in R1-09;
self-resolves at W2's clean `npm ci`). Recorded here for no-silent-drop
completeness; a residue self-resolution, not a counted disposition row — the
52-row count holds.

---

## D. Behavior / product rows

| Row | Origin (tranche/lane) | Closes ridden | DISEASE? | Terminal state | Owner/rationale |
|---|---|---|---|---|---|
| FE-3 Spring keyframe cards render `[object Object]` (10 labels; DEV+PROD) | R3-01 FE-3 (`KeyframeCardList.vue:11`, `KeyframeCard.vue:5,37`) | born V (novel, shipped) | No | **BUILD W1** | Demo-owned; coerce `frame.start` to its numeric/percent scalar before binding; add smoke assertion no label matches `/\[object Object\]/` |
| EE-01 `CopyButton.vue:42` `timingFunction:"bounceInEase"` throws at construction → copy-feedback dead | R3-02 EE-01 / R3-01 FE-1 (`CopyButton.vue:42`) | born V (Value-4 migration regression) | No | **BUILD W1** | Demo-owned one-liner → `"easeInBounce"` (or `"ease-in-bounce"`/cubic-bezier). No value.js letter — curve exists. Gate: bounceInEase pageerror→0 on /#/easing,/#/spring |
| EE-02 css-less `{fn}` assign → Keyframes-string serialize throws (cube/square/amiga) | R3-02 EE-02 / R3-01 FE-2 (`useTimingFunctionEditor.ts:101`, `TimingFunctionPanel.vue:144`) | born V | No | **BUILD W1** | Demo-owned; attach faithful `.css` twin (`{fn, css:<literal>}`) at both assign seams. Library throw is the deliberate G.W4 contract — do NOT touch it. Gate: anon-fn pageerror→0 |
| EE-03 "Invalid watch source" flock = one `markRaw`-array watch | R3-02 EE-03 (`useKeyframesParsing.ts:96`) | born V | No | **BUILD W1** | Demo-owned; `watch(() => animation.templateFrames.length, …)`. Gate: watch-warn count→0 all scenes |
| FE-4 dev-server favicon 404 (`demo/app/index.html:31`) | R3-01 FE-4 | born V (novel, dev-only) | No | **BUILD W11** | P3 polish; move favicon under a served public dir or accept dev-only 404. Prod build hashes it correctly |
| BV-1 U.B13 "one suspend seam" not folded into source (`AmigaScene.vue:202-206,226`) | R2-02 BV-1 | U → V (charter-vs-code, no live symptom) | No | **RETIRE** | OD-U13 user-visible mandate discharged (no jump: firstPostShowJump 0.0308 « frame delta; resumes+moves). Settle-decay REFUTED (§C). Retire as satisfied-by-behavior |
| BV-2 `weighted`/`weight` composite grows monotonically past peak (t3000=825) | R2-02 BV-2 (`probe7.mjs` D2weight) | born V | No | **BUILD W9** — UNVERIFIED | Non-collapse confirmed (D2 cured); numeric correctness of the `weight` axis UNVERIFIED. Verify via a born-checked static-weight golden (symmetric composite). Pure `op:'add'` path already correct (BV-N2) |
| BV-3 no public layer-removal API; D3 fix demo-unreachable | R2-02 BV-3 (`AnimationGroup` prototype) | born V | No | **RETIRE** | Correct-when-triggered (BV-N3); removal only via private seam. Harmless; if V mints an OD-U14 gate it drives the private seam. No public defect |
| RG-1 desktop controls-close toggle not single-click reachable (stale dock box → click hits stage) | R3-03 RG-1 (`#/cube` 1280; matches memory `project_dock_doubleclick`) | glass-root chronic (dock double-click) | Chronic (glass-root, ≥2 closes) | **FOLD → formation-time glass batch packet; kf-side re-verify W2** | Glass-root double-layer swap; not a demo patch. Two-step succeeds. Killed by the glass batch fix + W2 re-verify |
| RG-2 mobile Open-controls toggle host layer hidden + button off-screen at rest | R3-03 RG-2 (`#/cube` 390; `dock-layer--full` hidden) | glass-root chronic | Chronic (glass-root) | **FOLD → glass batch packet; kf-side re-verify W2** | Same glass-root mechanism, mobile facet. Demo-side rest-affordance mitigation built at W11 (RG-2 narrow-dock affordance); root fix is glass |
| RG-3 occasional amiga play-start race (1/9; Play leaves spin pinned at 0) | R3-03 RG-3 | born V | No | **RETIRE** | P3 flake, distinct from settle; no live-breaking symptom. Re-observe under W9 MR1 pageerror smoke if it recurs — not a wave |
| CC-04 dock ACTIVATION facet unowned in Glass BI | R2-09 (`AUDIT-REGISTRY.md:57,66`) | born V (glass-root) | No | **FOLD → glass batch packet; kf-side re-verify W2** | Ownerless dock-activation facet; belongs to the glass batch packet, re-verify on consume |

---

## E. Blueprint amendments (XB-01..08 — each FOLD into its owning wave)

| Row | Origin (tranche/lane) | Closes ridden | DISEASE? | Terminal state | Owner/rationale |
|---|---|---|---|---|---|
| XB-01 DD-4 demo over-export sweep double-owned (LT-13/A33 schedules demo work in library Batch 5) | R3-04 XB-01 (`R2-05:353,495`, `R2-06:210`) | born V | No | **FOLD W8** | Assign entire DD-4 sweep to a single DEMO wave; strike "DD-4 (11 demo)" from LT-13/A33 |
| XB-02 `isIOSLikePlatform` is NOT same-file-only (live test consumer) | R3-04 XB-02 (`test/demo/instrument/ios-text-entry.test.ts:4,38,46,58,69,78`) | born V | No | **FOLD W8** | DD-4 wave must re-grep `test/`+`bench/` per symbol; `isIOSLikePlatform` stays exported. Whole DD-4 demotion list unverified against test/ — re-derive |
| XB-03 DM-18 ↔ DT ordering edge (rewords comments at paths DT relocates/renames) | R3-04 XB-03 (`R2-08` DM-18 ↔ `R2-06` DT moves) | born V | No | **FOLD W10** | Run M-18 before D, OR fold comment-rewords into the DT waves touching those files, OR re-grep the version-token post-DT |
| XB-04 `mirror.test.ts` prune double-scheduled (LT-02a AND GP) | R3-04 XB-04 (`R2-05:75,463`, `R2-07:133,237`) | born V | No | **FOLD W4** | Single owner = W4 structure gate (L·B0, must precede library moves); GP defers with a one-line note |
| XB-05 `taxonomy.json` prune orphans 4 bench prose comments | R3-04 XB-05 (`resolve.bench.ts:5-6`, `group-composite.bench.ts:29-31`, `cold-import.bench.ts:25`, `spring-tick.bench.ts:233`) | born V | No | **FOLD W9** | Route the 4 bench comment folds into the prune wave alongside PF-3 (`interpolate.ts:257`); else stale refs to a deleted file |
| XB-06 MR4 not named as a demo-restructure precondition in DT | R3-04 XB-06 / E4 (`R2-07:221`) | born V | No | **FOLD W9** | Name G·MR4 (test:demo CI) as an explicit precondition of the W7/W8 demo waves + the DD-4 sweep |
| XB-07 FENCE E "every batch re-emits d.ts rosters" echoed only in Batch 3 | R3-04 XB-07 (`R2-05:371`; LT-15) | born V | No | **FOLD W5/W6** | Belt-and-suspenders: run `build:lib` + d.ts-grep per library batch as FENCE E literally requires (batches can't move rosters, but honor the fence) |
| XB-08 DM-08 CHANGELOG numeric anchor off-by-one (`:22/:23` → actual `:21/:22`) | R3-04 XB-08 (`CHANGELOG.md:21-23`) | born V | No | **FOLD W10** | Correct the DM-08 line numbers; textual anchor is robust but a mechanical agent misplaces the insert |

---

## F. Partials / banked

| Row | Origin (tranche/lane) | Closes ridden | DISEASE? | Terminal state | Owner/rationale |
|---|---|---|---|---|---|
| Audit-copy AUDIT-PROBE patch (`App.vue` `<TooltipProvider>` in the COPY) | GLASS-AUDIT-LINKAGE (`:22-24,4`) | evidence-only | No | **RETIRE** | Evidence-only, never lands; the REAL fix is W1's demo-owned root `<TooltipProvider>` (see DP-02, §A). Retire with the GLASS-AUDIT-LINKAGE citation |
| 28+ design captures + 4 closed-state captures (40 PNGs in `design-captures/`) | R2-01/R2-02/R3-03 (`docs/tranches/V/audit/design-captures/`) | evidence baseline | No | **FOLD W11** (BEFORE-baseline input; consumed at W11 open) | The BEFORE baseline for W11 Proportion & Affordance π/DELTA before/after; consumed at W11 open (a hand-off inside the tranche, not a bank) |
| GCF-03 sci-report science-report, unsent | R1-10 coverage gap (`handoff §6:456`) | R→U (cross-repo) | No — **UNVERIFIED** | **BANKED** — re-trigger = integrated native reproduction | External-producer-gated (sci-report); status UNVERIFIED here (cross-repo, read-only). W12 confirms it is a decided SCI row, not a kf-side orphan. Verify: `grep GCF-03` over sci-report with correct naming |
| FAM-11 outbound packets: XR-6, IN-ATLAS-1..4 (exact-V4-pin rationale line), IN-GLASS-1 | R2-09/AUDIT-REGISTRY FAM-11 (`:25`) | formation housekeeping | No | **FOLD W12** | Consolidated outbound constellation packets owed at close; exact-V4-pin rationale line owed to atlas. XR-2/XR-3 already DONE 2026-07-17 |
| CC-01 value.js-V D-GAP-6 undeclined (ship-or-decline) | R2-09 CC-01 (`AUDIT-REGISTRY.md:66`) | born V | No | **FOLD W12** | Execution-time ship-or-decline decision on D-GAP-6; exact-V4-pin confirmed deliberate (CC-02) |

---

## Zero-deferral statement

**52 disposition rows, all terminal.** Group A rail 10 · Group B chronics 11 · Group C refuted 6 · Group D behavior/product 12 · Group E blueprint 8 · Group F partials/banked 5. **Zero rows left un-terminal.** Terminal-state tally: BUILD 15 · FOLD 16 · RETIRE 11 (incl. 3 RETIRE-DISCHARGED, 6 RETIRE-REFUTED) · BANKED 1 (GCF-03 only, external-producer-gated: re-triggered by integrated native reproduction; the design-capture baseline is FOLD W11, a BEFORE-baseline hand-off inside the tranche, not a bank). **DISEASE rows: 2** — CH2-02 (T→U ungated, killed by W2) and CH-06/CH2-04 (K→U inside a "COMPLETE" doc, killed by W10); one **watch** row (CH-02 re-word-not-decide, becomes DISEASE if V re-words again). V inherits **no formal chronic ledger** — the `proof:chronic-closure` machine was deleted at U and every prior ledger self-terminated; residual carried risk is bounded to the rows above.
