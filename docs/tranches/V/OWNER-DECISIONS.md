# Tranche V — OWNER DECISIONS

| Row | Date | Decision (verbatim anchor) | Encoding |
|---|---|---|---|
| OD-V1 | 2026-07-17 | "Ratify." | **THE FORMATION IS RATIFIED — IMPL AUTHORIZED** per V.md's ratification clause. The 13-wave plan, both blueprint decision sets (V.B/V.C headers), the prune/make-real quartet, and the disposition ledgers stand as formed. Still open by design: DP2-06 transport-home taste ruling and the W8 ceiling election (500→one extraction / 400→two) — both presented in-wave, never proxied. Execution begins on dispatch; W2/W3 remain producer-gated on the immutable Glass 7 packet. |
| OD-V2 | 2026-07-17 | "We'll have to audit the demo in the forthcoming tranche after glass-ui perfection." | Two encodings, no silent deferral: (a) IN-V — W11 already rides the rendered, real-Glass demo (Depends on: W1, W2, W8) and is the in-tranche demo design/a11y/proportion audit at full fidelity; (b) SUCCESSOR BOUNDARY — an owner-ordered TOTAL demo audit after Glass-ui perfection (their Q003/Q002/Q051 close + the 7.0.0 tag + our W2/W3 consume) is the named opening obligation of the successor tranche (W-formation mints it as its first audit band). Recorded in PROMPT-RECAP-V FM-5 and V.md's debt section as an owner-ordered future boundary, not a V deferral. |
| OD-V3 | 2026-07-17 | DP2-06 relay → "Decide per capture review" | **THE TRANSPORT-HOME RULING IS HELD FOR CAPTURE REVIEW.** W11's design-fix unit builds the options packet FIRST (fresh captures of both transport homes — in-panel card and floating pill — on all four duplicating scenes at 390 AND 1280, real Glass 7, both options mocked or annotated) and presents it; implementation of the winning home follows the ruling. If the ruling outlasts the band window, W11 closes `complete_with_misses` on that row per its own spec — never proxied, never defaulted. |
| OD-V4 | 2026-07-17 | W8 ceiling relay → "500 — one extraction (Recommended)" | **THE 500 RAW-LINE CEILING IS ADOPTED FOR THE DEMO SCOPE** — one unified ceiling with the library's LT-01 policy (cohesion is the axis; allowlists stay empty). W8 extracts `EasingField.vue` (~140L) from `ChannelOptions.vue` (609L pre-move) and STOPS: `ChannelLabelRows.vue` is NOT extracted. proof:structure's R4 demo scope enforces 500 at the W8 extension; the staged W4 red witness (ChannelOptions 609L) discharges against it. |
| OD-V5 | 2026-07-17 | RG-2 relay → "Defer to glass's dock fix" | **THE AT-REST REOPEN QUESTION IS DEFERRED PENDING GLASS'S DOCK MARK.** The glass-root dock repair (our batch rows G-1/G-2 in their BI inbox) may itself cure the hidden Open-controls toggle; the demo-side peek-sufficiency question is re-examined WHEN their mark returns. W11 verifies the 390 at-rest state in its π matrix either way and records the observation; if the glass mark is still absent at W11 close, the row closes `complete_with_misses` citing the pending external mark (W12 tracks the mark's arrival). |

---

## ADDENDUM 2026-09-18 — X.KF.W10 `.c` · THE OWNER BLOCK (G-3) AND THE NEVER-CITE REGISTER (G-4)

**SERVED MODEL** `claude-opus-5[1m]` · unit **`KF.W10.c`** of X.KF.W10 (Track B · X·KF), written under
`value.js docs/tranches/X/keyframes/waves/KF-W10.md` **§3.3 `:284-289`** and its two gates — **G-3
`:421-431`** and **G-4 `:433-457`**. §4 §Bounds grants this unit **this file alone** (*append-only,
conditional*), and §4 §Disjointness makes it the only unit that writes it. **E-3 honoured: the table
above is byte-unchanged.** The `OD-V3 :7` and `OD-V5 :9` rows keep their 2026-07-17 words exactly;
nothing below overwrites, re-words or re-keys them. Substrate of record: keyframes.js `origin/master`
**`69095552`** · value.js `tranche-u` at the current tree bytes. The close ledger (`value.js
docs/tranches/X/keyframes/close/FINAL-KF.md`, unit `.f`) carries both blocks **by reference to this
file**, which resolves under L-6.

---

### 1 · THE OWNER BLOCK (G-3) — three rows, one RULED and two `complete_with_misses`, never proxied

**The acceptance this block is written against**, quoted from G-3: *"three verbatim rulings, **or** three
`complete_with_misses` closures each citing its **exact** pending precondition (OD-V3's capture packet;
OD-V5's glass dock mark; OG-KF1's owner sitting). No inference, no default, no proxy."* This block is
**1 ruled-and-quoted + 2 `complete_with_misses`** — the shape COHESION **§0j.C KF-ODV3** authorizes in
advance, and the shape the wave record's sitting-2 baseline reads the gate as taking (*"the gate's
three-part acceptance is 1 ruled-quotable + 2 `complete_with_misses`-authorized, never 0"*). **No
transport home is implemented by this unit, anywhere, at any strength** — G-3's own falsifier makes that
act fail *G-3 and the wave*.

#### (1) OG-KF1 — **RULED**, 2026-09-17. Quoted verbatim; not paraphrased, not summarised.

**The question of record**, at `value.js
docs/tranches/V/megatranche/audit/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md:232` under *"Owner
rulings owed"*: *"**OG-KF1** | B21-18 | Does the Codex Keyframes B-lineage continue at all — and must any
continuation re-anchor to `origin/master` `81a56990` before its denominators may be cited? (Joins
OD-V3/OD-V5 in KF.W10's owner block.)"*

**The ruling**, read at `value.js docs/tranches/X/COHESION.md` **§0j.C `:926-929`** and transcribed here
character-for-character:

> **KF-OGKF1** — the Codex "Keyframes v8" B-lineage is **STALE-BY-SUBSTRATE and does not continue**; it
> is citable only as *"345 exact / 12 partial / 57 unresolved @ 8281638c"*, never as HEAD coverage; the
> five conditional TCC re-reads never open; KF.W0's re-count adopts 185-at-HEAD (X-2). Rationale: the
> lineage's pin is exactly the tree §0 already ruled non-authoritative, measured live.

**Hand**: the value.js orchestrator, under the owner's 2026-09-17 begin-word delegation recorded at
COHESION §0j. **Consequence carried into §2**: this ruling is the supersession that register entry **(4)**
attaches, and it is why entries (1)–(3) and (5) can be terminal rather than open.

#### (2) OD-V3 — **`complete_with_misses`**. The row at `:7` stays **HELD**. Nothing is ruled here.

**The exact pending precondition — a named artefact carrying a measured cell count, not prose.**
`value.js docs/tranches/V/megatranche/audit/visual/safari-real/SS-13-CAPTURE-RECEIPT.md` **`:35`**:

> ### (a) THE OD-V3 CAPTURE PACKET — **INCOMPLETE · 0 of 16 required cells**

Its axis table at `:44-48` reads **scenes 4 required / 0 produced · transport homes 2 / 0 · viewports 2 /
0 · cells (4 × 2 × 2) 16 / 0**, and its `:53-60` records that the seven shots the wave does hold — three
of them on `/#/easing`, an OD-V3 scene — are **not counted**, because *"a capture of one home, or of
either home at one viewport, does not satisfy it"* (§Sequencing S-8 (iii-a)). The two blocking limbs at
`:62-73`, each measured rather than asserted: **(i)** no cell on this host renders a real 390 Safari
surface — iOS is **UNREACHABLE-IN-CELL**, ruled at COHESION **§0m.2**; **(ii)** `safari-app/desktop`
closed mid-seat and needs *"'Allow Remote Automation' re-enabled by a hand that can answer an admin
prompt, or the owner's Safari restarted."* Limb **(iii)** — real Glass 7 under it — is **satisfied and is
explicitly not the blocker**.

**The authorization, quoted**, COHESION §0j.C `:946-949`: *"the transport-home ruling is taken by the
orchestrator **from the OD-V3 packet** at KF.W10 `.g`'s sitting under this delegation, and if the packet
does not exist by then KF.W10 closes `complete_with_misses` on that row citing its exact precondition."*
**PACKET-FIRST is a lock, not a preference** (KF-W10 §6.B): 0 of 16 cells exist, so the ask is not
assembled, so the ruling is not taken. `.g`'s sitting re-reads `:35` at its own clock; if the count is
still **0 of 16**, this closure stands as written.

#### (3) OD-V5 — **`complete_with_misses`**. The row at `:9` stays **DEFERRED**. Nothing is ruled here.

**The exact pending precondition — glass's dock mark, named with its repo, its letter and its row** (a
producer artefact, so it is named at the producer's own coordinates rather than described):

1. **The ask this repo filed**: glass-ui
   `docs/tranches/BI/coordination/keyframes-inbox-2026-07-17-v-formation-batch.md` **`:27`** — *"**G-2 —
   Mobile Open-controls toggle unreachable at rest (390×844).** The toggle's host layer
   `dock-layer--full` sits `pointer-events:none / opacity:0 / visibility:hidden` while the button's box
   is off-screen (`x=441` on a 390 viewport) … so the first tap expands and the second actuates."*
   (Tracked at glass-ui HEAD, verified `git ls-files` this seat; read-only, never written.)
2. **The observation OD-V5's own encoding at `:9` requires** (*"W11 verifies the 390 at-rest state in its
   π matrix either way and records the observation"*): SS-13-CAPTURE-RECEIPT.md **`:80`** — *"### (b) THE
   OD-V5 390 AT-REST OBSERVATION — **NOT OBSERVED · nothing ruled, and nothing may be**"*, **Produced:
   nothing**, on the same bound as (2)'s limb (i).
3. **The mark in its measurable form**: SS-13-CAPTURE-RECEIPT.md **`:97`** — the glass §4 dock-contract
   re-verify, **0 of 4 marks** (`BG-5` · `GU-1` · `GU-2` · `subject-legible`), *"no dock frame was
   photographed by any seat of this wave."*

**The authorization, quoted**, COHESION §0j.C `:950-952`: *"**OD-V5 stays DEFERRED** pending glass's dock
mark (standing edict: glass rows and OD-V5 route to SS-6, never demo-side hacks); the
`complete_with_misses` shape is authorized in advance."*

**A DATED OBSERVATION BESIDE THE CLOSURE — recorded because a block that hides a live producer letter
bearing on its own precondition is not a record.** glass-ui
`docs/tranches/BI/coordination/glass-outbound-2026-07-17-keyframes-marks-placed.md` **`:9-15`** (tracked;
read-only at this seat) reports: *"**The G-1/G-2 activation oracle is LANDED in-tree, ahead of the tag**
— `f1e88fe2` adds the single-click activation-at-rest acceptance arms: DOCK-SPINE desktop … and
DOCK-CROSSFADE mobile (390×844)."* **What landed there is the acceptance ORACLE — the arms that would
detect the defect — not the dock repair mark and not the 390 at-rest observation**, and the letter is
dated at the **7.0.0** boundary. **This seat adjudicates none of it.** glass-ui is READ-ONLY always, the
standing edict routes OD-V5 and every glass row to **SS-6**, and the ruling is three hands away from this
one (SS-13 `:78`: *"glass-ui (the dock mark) → the SS-6 relay → KF.W10 `.g`. Three hands, none of them
here."*). **Routed, not ruled**; `.g`'s sitting and SS-6 own the question of whether `f1e88fe2` is the
mark OD-V5 awaits.

---

### 2 · THE NEVER-CITE REGISTER (G-4) — seven entries, ordinals binding

**Scope**: no X·KF spec asserts a rejected Codex denominator or an unciteable corpus coordinate except in
its superseded form with the supersession attached. **The ordinals are G-4 acceptance criteria, so they
have exactly one value each** — §3.3.2's list, reproduced here identically and nowhere contradicted.
Every coordinate below was **re-resolved at the bytes by this seat before it was written** (KF-AT-28
re-anchor law), and each row says where the thing is *actually* filed rather than where it was remembered
to be.

| # | coordinate | where it is actually filed (verified at the bytes, this seat) | supersession / true source | terminal disposition |
|---:|---|---|---|---|
| **(1)** | **B18-12** | `value.js docs/…/codex-provenance/INTAKE-ADJUDICATION-2026-08-03.md:82` (*"3 are genuinely unlocatable external coordinates (B18-12/13/14)"*) + `:163-166` (CARRY → KF.W10) · `…/formation/keyframes/CENSUS-2026-08-03.md:326` | **None, and none can exist.** The adjudication's own law: *"an external coordinate that nothing reproduces cannot close a row."* No file on this machine reproduces it. | **UNRESOLVABLE-BY-CONSTRUCTION.** Closes no row, enters no X·KF spec as evidence, never re-opened. **Re-booking forbidden — the disposition is terminal.** |
| **(2)** | **B18-13** | same three coordinates | same | **UNRESOLVABLE-BY-CONSTRUCTION**, terminal, re-booking forbidden |
| **(3)** | **B18-14** | same three coordinates | same | **UNRESOLVABLE-BY-CONSTRUCTION**, terminal, re-booking forbidden |
| **(4)** | **the 357/414 block** (with its percentage spelling `86.23`) — filed as the triple **B18-23 + B19-14 + B20-14**, *"one disposition"* | `INTAKE-ADJUDICATION-2026-08-03.md:163-166` · `CENSUS-2026-08-03.md:326` (both name the triple explicitly) | **`345 exact / 12 partial / 57 unresolved @ 8281638c`** — the sole citable form, per **OG-KF1 as ruled in §1(1) above** (COHESION §0j.C `:926-929`): *"STALE-BY-SUBSTRATE and does not continue … never as HEAD coverage."* | **NEVER CITED BARE.** A spec may print `357/414` (or `86.23`) **only** with `345/12/57 @ 8281638c` beside it, on the same line. Verified compliant in all three live X·KF wave specs below. |
| **(5)** | **B18-27's slot count** (`0/5` vs the 08-03 delta's `0/4`) | `INTAKE-ADJUDICATION-2026-08-03.md:116`, conflict **C-8**, verbatim: *"keyframes slot denominator (Codex-internal) \| B18-27: '0/5' vs the 08-03 delta's '0/4' \| census silent \| **CONFLICT UNRESOLVED → CARRIED** (KF.W10)"* · + `:166` · `CENSUS:326` | **None — nothing external decides it.** The fresh census is silent; the disagreement is Codex-internal bookkeeping against itself. | **CONFLICT UNRESOLVED → CARRIED.** *"enters no keyframes spec until 4-vs-5 settles or the ledger is dropped."* **Settle-or-drop is ONE act; a third number is not an option and fails on sight.** Cross-lock: the slot count sits inside **OG-V1**'s enumeration (KF-W10 §6.C-F) — a settling here that contradicts a ruling there manufactures a new conflict, so **this register settles nothing and carries**. |
| **(6)** | **ChromeDock's `challenge-C-consumption.md` header tally** | `value.js docs/tranches/V/megatranche/registry/adjudicated/kf-ChromeDock.md` — **`## DISSENT — preserved, not ruled away` at `:142`, item 4 at `:147`**. **The record files NO id**: ⟨cmd⟩ `grep -oE 'DISSENT[ -]*[0-9]+' kf-ChromeDock.md` → **0 hits**, re-run this seat. Keyed **`CD-DISSENT-4`** — a **declared positional key**, never presented as the record's own coordinate (anti-rename law). The tally itself is at `…/audit/kf-components/ChromeDock/challenge-C-consumption.md:11` — *"Tally after Round 3: 25 defects (3 BLOCKER · 12 MAJOR · 8 MINOR · 2 INFO) · 6 superlatives"*, with its own Round-3 integrity note conceding the miscount at `:15`. | **No true source exists to substitute.** Verbatim from the dissent: the tally *"still counts eight defects and two superlatives that were never written (its own Round-3 note concedes it), and with S-2 and R3-1 both killed on the merits **the axis's arithmetic must be restated from scratch by any downstream consumer — never quoted**."* | **NEVER QUOTED.** The header tally enters no X·KF spec in any form. **Restating the arithmetic is the downstream consumer's act, not this register's** (KF-W10 §7): the register names it unquotable and stops there. |
| **(7)** | **EditorHeader's C-axis §0 citation** | `…/registry/adjudicated/kf-EditorHeader.md` **killed-claim #8 at `:86`**, filed with **no wave** (homed here by mechanism M-25). Its words: *"The cited `coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md` **does not exist** (my `ls`/`find`)."* | **The repo-scoped restatement below, NOT the transcribed kill** (G-4 acceptance, §3.3's lock). | **REPO-SCOPED, NOT ABSENT** — see the restatement immediately below. The **substance (EH-1) is untouched** and survives on its own witness. |

#### Entry (7) — the REPO-SCOPED restatement, carried here rather than transcribed (G-4's acceptance names this entry, not (6), as its bearer)

**REPO-SCOPED, NOT ABSENT** — dead at value.js, live at keyframes.js `origin/master`; the two letters are
different documents. The paraphrase-presented-as-quotation finding is re-scoped to the kf-local letter and
re-verified there before it may be asserted.

**Re-measured at this seat, at the landing substrate** (`origin/master` `69095552`), because the
restatement is only as good as its last measurement:

- ⟨cmd⟩ `git ls-tree -r --name-only origin/master | grep -i headerribbon` →
  `docs/tranches/V/coordination/GLASS-INBOUND-2026-07-16-headerribbon-consumer-updates.md` — **the path
  EXISTS in this repo.** ⟨cmd⟩ `git show origin/master:<that path> | wc -l` → **26** lines; its head reads
  *"# Glass inbound — HeaderRibbon consumer updates for the post-Glass-7 refresh"*, **Date: 2026-07-16**,
  **From: the glass-ui BI/P/Q execution session (team lead)**.
- ⟨cmd⟩ `ls -la value.js/docs/tranches/V/archive/GLASS-INBOUND-2026-07-16-headerribbon-persistent-only.md`
  → present, 2,878 B; its head reads *"ARCHIVED 2026-07-17 — historical; superseded by W40-S5 + W44"* and
  *"# Glass inbound — HeaderRibbon goes persistent-only (V-A92 supersession mark)"*, with the deletion
  claim at `:15-19`.
- **They are different documents in different repos**, both dated 2026-07-16, both from the same glass-ui
  session — exactly as `kf-EditorHeader.md:139` (D-1, repair round 3) already books them: *"a receipt
  naming one of the two is RED … **The 'NONEXISTENT path' reading is STRUCK at C-14**: the path is live —
  in the other repo."* The killed claim's `ls`/`find` was **value.js-scoped**; the register books the
  scope, not an absence.
- **The substance is not in question.** `kf-EditorHeader.md:40` (**EH-1**, MAJOR) stands on its own
  witness: installed HeaderRibbon **7.0.0 still ships** the `anchor` slot (`{pinned}` payload), the entire
  collapsible machinery and `--header-ribbon-actions-width`. **Only the citation was re-scoped; the defect
  was not.**

#### The witness field, re-run and re-partitioned at this seat (R2-5 — the executing seat never re-prints an inherited count)

⟨cmds, run in `value.js/docs/tranches/`, **double-run, run 1 ≡ run 2**, at the bytes **before** this
register was written⟩ `grep -rlE '357/414|86\.23|0/5 slots' X/ V/megatranche/ | wc -l` → **34 files** ·
`grep -rhoE … | wc -l` → **112 occurrences** · `grep -rcE … | awk -F: '{s+=$2} END {print s}'` → **78
lines**. **Counting rule stated at the enumeration**: occurrences are `grep -o` tokens, lines are `grep
-c` rows, one line can carry three tokens. Token split: **`357/414` 79 · `0/5 slots` 19 · `86.23` 14**.

| class | files | occ | lines | disposition |
|---|--:|--:|--:|---|
| **Live X·KF wave specs** — `KF-W0.md` 2 (2 L) · `KF-W3.md` 2 (2 L) · `KF-W10.md` 23 (14 L) | **3** | **27** | **18** | **The register's adjudicable scope. ZERO bare citations — verified line-by-line, not asserted** (see below). Membership and every per-file figure **reproduce the spec's round-5 cell exactly** (W0 2 · W3 2 · this file 23 = 27 / 18 L). |
| **X·KF conformance artifacts** — PASS-1 8 (5 L) · PASS-2 4 (2 L) · PASS-3 3 (3 L) · PASS-4 5 (3 L) · PASS-5 7 (4 L) · **PASS-6/KF-W10-CHECK 1 (1 L)** · **PASS-6/KF-W3-CHECK 2 (1 L)** | **7** | **30** | **19** | Dated read-only checks that quote the figure in order to adjudicate it. Not live citations. **TWO NEW MEMBERS entered at this write — both PASS-6 checks** — exactly as the class's own note predicts (*"this class is open by construction and every future pass adds a member"*). The class-membership falsifier is **satisfied by naming the files that moved**, which is what it exists to make possible. |
| **Megatranche substrate** — dated, sealed, E-3-protected evidence, 2026-07-31 → 2026-08-03 | **19** | **44** | **34** | **Out of the register's reach by E-3**: dated evidence is never rewritten, so these are quoted-in-context records. **Membership unchanged at 19 files, and every per-file LINE figure in the spec's round-5 enumeration reproduces term-for-term** (5·1·3·4·1·1·2·2·1·1·1·1·1·1·1·2·2·2·2 = 34). Eleven are dated **after** the 08-03 supersession — the adjudication that kills the figure necessarily quotes it; the caveat is stated, not hidden. |
| **X·KF execution + artefact records (CLASS DECLARED AT THIS WRITE, not smuggled into another row)** — `X/execution/B/KF-W10.md` 6 (2 L) · `X/execution/SITTING-DOSSIER-2026-09-17.md` 1 (1 L) · `X/keyframes/W0/V8-DISPOSITION.md` 1 (1 L) · `X/keyframes/artefacts/W0/v8-disposition.txt` 1 (1 L) · `X/parse-that/evidence/W3/equivalence-full-surface.json` 2 (2 L) | **5** | **11** | **7** | **Four of the five are COMPLIANT** — each carries `345 exact / 12 partial / 57 unresolved @ 8281638c` with *"never 357/414, never as HEAD coverage"* on the same line, i.e. they are the ruling being applied, not cited bare. **The fifth is NOT A CITATION AT ALL** — see the finding below. This class did not exist in the spec's round-5 table because these artefacts did not exist; it is declared here rather than folded into a neighbouring row. |

**`3 + 7 + 19 + 5 = 34` · `27 + 30 + 44 + 11 = 112` · `18 + 19 + 34 + 7 = 78`** — the partition
**checking itself against the totals the commands returned**, never a derivation of either. **No figure in
this block was produced by subtraction** (LAW D(3)).

**ZERO BARE CITATIONS IN THE LIVE-SPEC CLASS — verified per line, in this register's own voice, because
this is the one claim G-4 actually turns on.** ⟨cmd⟩ each `357/414` line in the three live specs tested
for `345` **and** `8281638c` on the same line → **4 of 4 carry both**: `KF-W0.md:378` (*"only as `345
exact / 12 partial / 57 unresolved @ 8281638c`, never 357/414, never as HEAD coverage"*) · `KF-W0.md:695`
(*"the v8 seal's terminal disposition (357/414 dead within Codex's own corpus → sole citable form `345
exact / 12 partial / 57 unresolved @ 8281638c`)"*) · `KF-W3.md:209` · `KF-W3.md:329` (**E-11**, *"never
357/414, never as HEAD coverage"*). **Neither `0/5 slots` nor `86.23` occurs in `KF-W0.md` or `KF-W3.md`
at all**; `KF-W10.md`'s 23 are the gate's own adjudication of the figure.

**A FINDING AGAINST THE GATE'S OWN WITNESS, recorded loud rather than absorbed.** The witness regex is
unanchored, and `86\.23` matches **any numeric substring**. `X/parse-that/evidence/W3/equivalence-full-surface.json`
**`:9765`** and **`:11567`** are hits solely because an X·P CSS-parser equivalence corpus contains the
input `oklch(88.720 86.234 -0deg)` — **`86.234`, an oklch chroma in a different sub-tranche's evidence
file, is not a citation of anything.** So **2 of the 112 occurrences (1.8%) are instrument artefacts**,
and the true citation field is **111**. This changes no verdict — the gate's subject is the **partition**,
and a non-citation falls outside the adjudicable class by construction — but a register that reports a
figure it knows to be inflated is doing the thing it exists to forbid. **The gate's command is NOT
edited** (E-3: the spec is immutable, and this file is this unit's only writable path): the correction is
booked here, dated, and **routed to `.g`'s adjudication** for a bounded regex (`86\.23%` or `\b86\.23\b`)
at whatever artefact next re-prints the field.

**Two properties of the witness, restated because a seat that re-runs this must not mis-run it.** **(i)
The field grows monotonically and its largest contributor is the spec itself** — 34 / 112 / 78 at this
seat's clock, against the sitting-2 baseline's **34 / 108 / 77** (the `+4 occ / +1 line` delta is `.b`'s
receipt landing in `X/execution/B/KF-W10.md`, a file inside the field) and the spec's round-5 **27 / 98 /
69**. Three clocks, three readings: **that is the property, not a defect in it**, and no figure here may
be re-printed without re-running the command. **(ii) The gate's subject is the PARTITION, not the count**
— G-4 closes when the live-spec class is bare-citation-free and every unciteable coordinate is registered,
whatever the other classes total. **A NOTE THIS ROUND CAN MAKE FOR THE FIRST TIME**: this register's own
bytes land in **keyframes.js**, which is **outside** the witness field (`value.js docs/tranches/X/` +
`docs/tranches/V/megatranche/`) — so **the act of writing the register does not move its own witness**.
Only the unit's receipt does, and that receipt names its own contribution.

**The falsifiers, carried intact so the next seat can fire them at this block.** A spec that cites
`357/414` without `345/12/57 @ 8281638c` beside it **fails, naming the file and line**. A **third** slot
number for B18-27 **fails on sight** — settle-or-drop, never a new figure. Re-booking any of
**B18-12/13/14** as evidence **fails: UNRESOLVABLE-BY-CONSTRUCTION is terminal.** A class row whose
membership no longer reproduces at the executing seat's grep **fails the partition, naming the file that
moved**. An ordinal in this register that takes a second value anywhere **fails**, because an ordinal that
is a gate criterion may have exactly one value.

---

**Gate readings at this write** — **G-3: RED → GREEN** (0 of 3 closed → 3 of 3: OG-KF1 **RULED** and
quoted verbatim with its date; OD-V3 and OD-V5 **`complete_with_misses`**, each citing a named artefact at
a measured coordinate, neither proxied, neither defaulted). **G-4: RED → GREEN** (the register did not
exist → it exists, **seven entries**, ordinals **(1)…(7)** binding and stated once, entry **(7)** carrying
the **REPO-SCOPED, NOT ABSENT** restatement rather than the transcribed kill; the witness re-run,
re-partitioned, and **zero bare citations in the live-spec class**, verified line-by-line). **Residual,
carried not cured**: the unanchored-`86.23` instrument artefact above, routed to `.g`.
