# Lane R2-03 — Chronic Census Completion (A..U total enumeration)

**Prefix:** CH2- · **Date:** 2026-07-17 · **Tree:** `/Users/mkbabb/Programming/keyframes.js` @ `a59d3a22` (uncommitted K6/Glass-7 transaction live) · **Charter:** complete the A→U ledger R1-10 only sampled.

## Verdict

R1-10 sampled the FINAL-U/handoff-named candidates and minted CH-01..06. This lane
enumerated the full A→U corpus and reached a structural conclusion that reshapes
the census: **there is no surviving formal chronic ledger for V to inherit.** The
forward-carry mechanism through A→T was a single machine — `proof:chronic-closure`
parsing each tranche's `PROGRESS.md §"Open deferrals"` — and that machine was
*deleted* at U (`scripts/proof-chronic-closure.mjs` is absent; only `proof:publish`
and `proof:owner-golden` survive in `package.json:50-51`). Every prior tranche also
*terminated* its own ledger at close (D "terminal home," H/I/J/K/L/Q/R re-pointed +
re-verified the substrate to exit-0). The 11,536 grep hits are therefore
overwhelmingly (i) historical DEVELOPED/IMPL working prose in per-tranche audit
sub-trees and (ii) rows a later tranche already discharged — **not** forward-carried
un-decided work. The genuine V-inheritance is exactly the AGENTIC-HANDOFF rail
R1-10/FAM-01 already caught, plus a thin doc-residue tail (FAM-08). I verified the
three most-cited standing external obligations against the live tree: the value.js
`PropertyDescriptor` collision is **resolved** (`CSSPropertyDescriptor` in the packed
d.ts, value.js 4.0.0), the K engine-seam split is **landed** (`engine.ts` gone,
decomposed; `load-engine.ts` is the seam), and the A→C spring-token/VJ-F/PT-4
outward asks **do not appear** anywhere in S/T/U — absorbed by the constellation
drives. Net new rows: **two** (one decided-terminal conditional, one de-tripwired
external residue); everything else discards or cites an existing family.

---

## Census method + the forward-carry model

The disease the sweep hunts (a row riding ≥2 closes un-decided) can only live in the
**forward-carry channel**. In this codebase that channel was mechanized, so I did not
have to hand-diff 11k lines of prose:

- **A→D:** named-deferral sections in each `FINAL.md` (`§Deferrals`), terminated at
  D ("D is the terminal home for every keyframes-owned deferral," `D/FINAL.md:177-181`).
- **H→T:** the `proof:chronic-closure` gate parsing `PROGRESS.md §"Open deferrals"`,
  re-pointed and re-verified non-vacuous at every close (`H/FINAL.md:74`,
  `I/FINAL.md:156`, `J/FINAL.md:383-389`, `K/FINAL.md:54-58`, `L/FINAL.md:346-365`,
  `Q/FINAL.md:38`, `R/FINAL.md:50-61`).
- **U:** the machine **dissolved** — `ls scripts/ | grep chronic` → nothing;
  `grep proof: package.json` → only `proof:publish`, `proof:owner-golden`;
  `FINAL-U.md:197` "No U deferral ledger or tripwire passes to V." (R1-10 negative,
  re-confirmed.)

Therefore the ONLY live forward-carried rows at V are: whatever the AGENTIC-HANDOFF
re-encodes (FAM-01), plus doc-file residue that persists outside the ledger machine
(the `docs/*.md` persistent surface). I swept both exhaustively; the audit sub-trees
(per-lane finding docs, wave brainstorm/harden loops — the bulk of the 11k) carry no
forward channel and were triaged as class-(i) historical.

## Hit distribution (the raw candidate volume)

`grep -rniE '<sweep pattern>' docs/tranches docs/*.md` → **11,536** total. Per source:

| Source | Hits | Nature |
|---|---|---|
| `docs/*.md` (persistent surface) | 2 | dogfood-inversion:48, MIGRATION-5.1.0:68 |
| close docs only (`{A..U}/FINAL*`, M/P/Q/R/T closes) | 319 | the forward-carry candidates |
| tranche audit sub-trees (per-lane/wave working prose) | ~11,215 | class-(i) historical — no forward channel |

Per-tranche totals (whole sub-tree): A 57 · B 181 · C 239 · D 149 · E 465 · F 418 ·
G 514 · H 972 · I 617 · J 776 · K 668 · L 544 · M 1177 · N 74 · O 703 · P 598 ·
Q 762 · R 271 · S 860 · T 524 · U 883. High counts = big DEVELOPED audits (H/M/S/U),
not open debt.

---

## Triage — the close-doc forward candidates (batched by tranche)

Terse: **D**=discard-decided · **F**=family-covered · **N**=new-mint.

**A** (`A/FINAL.md:200-247` §Deferrals&named-forwards): LoAF observer, Playwright
>50ms trace gate, Worker/OffscreenCanvas/Atomics (KILL), VAL-9 `--spring-*` token
regen (glass-ui ADOPTION ASK), demo-local asides (φ-ladder/VT). → LoAF landed (B
`FINAL.md:119` "chronic closed"); trace gate = A11Y/perf, unit-verified; Worker path
KILL-archive; VAL-9 spring-token = **F** (FAM-11 outward, and absent from S/T/U =
absorbed); φ-ladder = terminated at D. **All D/F.**

**B** (`B/FINAL.md:123-140` §Deferrals): φ-ladder typography + cartoon-shadow token
(demo-polish follow-up), dock double-click + VAL-9 (glass-ui ASK), ScrollTimeline
native (KILL-permanent), Worker (archive). → φ-ladder terminated D; dock/VAL-9 **F**
(FAM-11); ScrollTimeline/Worker permanent-KILL. **All D/F.**

**C** (`C/FINAL.md:108-124` §Deferrals): square-scene mobile composition (allowance),
bucket-glassui label ASK-3, leaf-tail φ-ladder F6 (BOOKED), permanent archives. →
φ-ladder F6 terminated D.W2 (`D/FINAL.md:186`); mobile-occlusion cured systemically
(MEMORY: S mobile-sheet "12→72%"); bucket-glassui = **F** (FAM-11). **All D/F.**

**D** (`D/FINAL.md:177-203` deferred ledger TERMINATED): every KFD discharged at G
vantage; the ONE blocked carry (`TopDock→ChromeDock`) closed via G.W12. **All D.**

**E/F/G** (`E/FINAL.md:23-62`, `F/FINAL.md:99-106`, `G/FINAL.md:174-179`): each
re-confirms "zero KFE — D was the terminal home." The one true chronic (value.js
charter C-1) is chronic-*by-design*. **All D.**

**H** (`H/FINAL.md:59-101` Open deferrals — 4 chronics + handoffs): D2/D14 specular,
D5 dock LAG, D9 popover — all CLOSED via system gates; glass-ui Card-specular SHEEN
→ born-RED handoff to glass-ui 3.8.0; value.js/parse-that next-slice (VJ-F1/F2/F3,
PT-4 packrat re-key) → born-RED handoff. → specular resolved in the Glass major
climb (now Glass 6→7, FAM-01); VJ-F/PT-4 = **F** (FAM-11) and **absent from S/T/U**
(constellation-absorbed). **D/F.**

**I/J** (`I/FINAL.md:156-196`, `J/FINAL.md:229-261,472-484`): crash chronics FOLDED;
CH-3 mobile oracle cured (`proof:live-session-mobile`); FB-6 `Mod+K` palette KILL
(no owner across 4 tranches, `J/FINAL.md:261`). **All D.**

**K** (`K/FINAL.md:54-66` deferred ledger TERMINAL): 44-row ledger terminal, ten
≥4-tranche riders EXITED; **the one net-new forward: `K/FINAL.md:66` "future
engine-seam split / `loadAnimationEngine()` chunk-boundary refinement — RECORDED as
kf-owned follow-up."** → **VERIFIED LANDED in R:** `engine.ts` no longer exists
(decomposed); `src/animation/load-engine.ts:123` is the realized seam. **D.**

**L** (`L/FINAL.md:205-361` Band-B un-consumed edges + tripwires): value.js 0.13.0,
glass-ui BB, parse-that 0.9.0, keyframes-vue — each a HANDOFF with a named sibling
tripwire. → constellation drives (M/O/Q) consumed all; keyframes-vue KILLED (R;
MEMORY). **All D/F** (FAM-11 residue).

**M** (`M-CONSUME-CLOSE.md:51-58`): glass-ui BC handoff + keyframes-vue-published
tripwire. → BC folded into the Glass major climb (FAM-01); keyframes-vue KILLED. **D/F.**

**P** (`P/FULL-LOOP-LEDGER.md`): DEVELOPED-phase loop; P.W4 codegen = RETIRED
TOMBSTONE (owner 2026-06-22); rest IMPL'd by Q. **All D.**

**Q/R/T** (`Q/FINAL.md:14,44-46`, `R/FINAL.md:36-103`, `T/FINAL.md:80-128`): glass-ui
BC (USER-DOMAIN); R 15-row ledger terminal; **T born-RED external-blocked set** —
`proof:subject-legible`, `proof:blur-not-resampled` (BG-5), `proof:dock-rest-crisp`
(GU-1), `proof:dock-morph-continuity` (GU-2), `proof:no-collision-rename` (value.js
PropertyDescriptor). → `no-collision-rename` **VERIFIED RESOLVED** (below, CH2-01
negative); the glass dock/blur set = **CH2-02** (de-tripwired external residue).

**U** (`FINAL-U.md` + AGENTIC-HANDOFF): CH-01..06 (R1-10) / FAM-01. **F.**

---

## CH2-01 — value.js `proof:no-collision-rename` (PropertyDescriptor) is RESOLVED, not carried
**Severity:** P3 (negative-of-record) · **Family:** discharged-external / FAM-11 delta

The most-cited standing external obligation across R→T was `proof:no-collision-rename`
(`T/FINAL.md:97,128`): value.js had to rename `PropertyDescriptor` collision-free and
kf re-point. This lane verified it discharged in the live tree:
- `dist/keyframes.d.ts:6` = `import { CSSPropertyDescriptor } from '@mkbabb/value.js/css';`
- `dist/keyframes.d.ts:747,2563,2567` all use `CSSPropertyDescriptor`; **zero**
  `PropertyDescriptor_2` mangle remains (`grep PropertyDescriptor dist/keyframes.d.ts`
  → 4 hits, all `CSSPropertyDescriptor`).
- `package.json:69` = `@mkbabb/value.js": "4.0.0"` (the immutable Value 4).

The T born-RED tripwire that guarded this was itself deleted with the apparatus at U.
**Verdict: discharged — do NOT re-book.** Recorded as a negative so formation does not
resurrect a resolved external row from the T backlog table.

---

## CH2-02 — The T glass-dock external residue is DE-TRIPWIRED, not resolved (rides Glass 7 with no gate)
**Severity:** P2 · **Family:** FAM-01 RAIL (delta) / de-tripwired-residue

`T/FINAL.md:83-100` carried four EXTERNAL-BLOCKED glass-ui obligations as **born-RED
tripwire gates** that would "ride the re-pin": `proof:subject-legible` (dock-icon
de-blur), `proof:blur-not-resampled` (glass-ui BG-5 `blur-source="static"`),
`proof:dock-rest-crisp` (GU-1), `proof:dock-morph-continuity` (GU-2). The design was
sound: a kf-side gate that stays RED until glass publishes AND kf consumes.

**The delta:** U dissolved the entire gate apparatus (`ls scripts/`→`gates/` only;
`grep proof: package.json`→`proof:publish`+`proof:owner-golden` only). So these four
dock-crispness obligations no longer have *any* kf-side guard. FINAL-U.md:72,197
asserts "unshipped sibling work has named release producers and no local tripwire" /
"No U tripwire passes to V" — but that is precisely the problem the sweep hunts: the
obligation did not close, its *guard* was removed. The obligations now ride the
Glass-7 demo-consume wave (FAM-01 RAIL) **ungated**. A blurry resting dock or a
janky dock-morph on the fresh Glass 7 would ship with nothing to red it.

Cross-repo glass status is read-only/out-of-lane (whether Glass 7 actually fixed
BG-5/GU-1/GU-2), so I cannot verify the *product* side — but the *kf-side guard
removal* is verified. **Disposition (fold into FAM-01 consume wave):** when V builds
the Glass-7 consume, re-instate a single dock-crispness live check (or explicitly
retire the four with a one-line "Glass 7 shipped the fix, verified on the running
demo" rationale). Do not carry them as silent ungated hope.

---

## CH2-03 — published-surface.md EP-3 PATH-B→PATH-A conditional flip (new, but decided-terminal)
**Severity:** P3 · **Family:** decided-conditional / not-disease

`docs/published-surface.md:196-210` records EP-3: `flip`/`flipShared`/`drag`/
`Draggable`/`DrawSVG`/`fromDrawSVG` are load-bearing README-taught exports with **no
demo scene** → PATH B (recorded BOOK, unit-tested, no live-session coverage). The doc
states this is "the terminal disposition per P-invariant-28, not a punt; if a future
wave lands a befitting demo scene, that export flips to PATH A and joins the every-
scene sweep automatically." R1-10 did not cite this row.

Verified live: docs present; exports intact in the HEAVY set (`load-engine.ts`); the
`proof:publish` gate machine-checks each EP-3 row's cited test file exists
(`published-surface.md:201`). This is a **conditional-forward, not an obligation** —
explicitly decided-terminal. **Not a disease** (0 un-decided rides). **Disposition
(retire from candidate set):** leave as the recorded terminal BOOK; note only that
`proof:publish` (one of the two surviving gates) still enforces its coverage table,
so it is self-maintaining. Flagged here purely so formation's table has the complete
enumeration and does not later "discover" it as un-tracked.

---

## CH2-04 — dogfood-inversion.md:48 stale present-tense republish clause (confirms R1-10 CH-06)
**Severity:** P3 · **Family:** FAM-08 DOC-DRIFT (R1-10 CH-06)

Re-verified: `docs/dogfood-inversion.md:48` still reads "npm does NOT yet carry those
exports. **K.WZ does the K-tranche republish**" inside a doc headed "Status: LANDED
(L.W8 S1) — the flip is COMPLETE" (`:8`). 6.0.0 is published; K.WZ is long past. This
is R1-10 CH-06 verbatim — **family-covered, no new row.** Delta vs R1-10: I confirmed
the *rest* of `docs/*.md` is clean — `scroll-morph.md`, `color-fidelity.md`,
`MIGRATION-5.0.0/5.1.0/6.0.0.md` carry **no** stale forward clause on a full
keyword sweep (`MIGRATION-5.1.0.md:68` "consumer that never awaits it" is accurate
descriptive prose of a live lazy-load property, not a plan). CH-06's coverage-gap
worry ("scroll-morph/color-fidelity not swept line-by-line") is now discharged: swept,
clean.

---

## The candidate ledger — formation's disposition table

| Row | Provenance (mint → rides) | Live/dead vs tree | Disease (≥2 un-decided rides)? | Disposition |
|---|---|---|---|---|
| CH-01 FINAL-U "terminal/no-backlog" superseded | R1-10 · U close | live (handoff is real V-inherit) | n/a (1 close) | FAM-01 — fold: open V on the handoff |
| CH-02 Glass-6/Atlas-2 boundary reworded-not-decided | R1-10 · U close | dead coords, live doc | re-word-not-decide behavior | FAM-01 — retire/rewrite to Glass-7/K6/Atlas-4 |
| CH-03 removed-feature doc residue (mischaracterized) | R1-10 · K6 txn | live (uncorrected) | no (fresh) | FAM-08 — build precise correction (R2-08) |
| CH-04 MbabbMenu Tailwind-literal reword | R1-10 · pre-U | dead (no defect) | borderline | FAM-08 — retire w/ :6 evidence |
| CH-05 "nightly" cadence mislabel (weekly cron) | R1-10 · U | live | no | FAM-08 — fold (relabel or change cron) |
| CH-06 / **CH2-04** dogfood-inversion:48 K.WZ clause | R1-10 · K | live (stale) | **yes** (K→U inside "COMPLETE") | FAM-08 — past-tense/strike |
| **CH2-01** value.js `no-collision-rename` | T · R→T | **dead (resolved)** | no | **retire — discharged, do not re-book** |
| **CH2-02** T glass-dock external residue de-tripwired | T · T→U | live obligation, **guard removed** | yes (T→U ungated) | **FAM-01 — re-gate or retire-w/-rationale in consume wave** |
| **CH2-03** published-surface EP-3 PATH-B flip | J.W4 · J→now | live doc, decided-terminal | no | retire from candidate set (self-maintaining via `proof:publish`) |
| A/B/C spring-token regen (VAL-9), bucket-glassui | A/B/C | absent S/T/U | no | FAM-11 — constellation-absorbed, discard |
| H VJ-F1/F2/F3 + PT-4 packrat next-slice | H | absent S/T/U | no | FAM-11 — constellation-absorbed, discard |
| K engine-seam / loadAnimationEngine chunk refine | K | **landed in R** (`load-engine.ts`) | no | retire — discharged |
| L Band-B edges (value 0.13, glass BB, parse-that 0.9, kf-vue) | L | consumed M/O/Q; kf-vue KILLED | no | FAM-11 — discharged, discard |

---

## Negatives (checked and found sound)

- **The chronic-ledger machine is gone, cleanly.** `scripts/proof-chronic-closure.mjs`
  absent; `package.json:50-51` = only `proof:publish` (`gates/surface/`) +
  `proof:owner-golden` (`gates/visual/`). The A→T forward-carry channel is closed by
  deletion (matches R1-10). No orphan chronic ledger passes to V.
- **value.js PropertyDescriptor collision resolved** — `CSSPropertyDescriptor`
  throughout `dist/keyframes.d.ts`, value.js 4.0.0 (CH2-01).
- **K engine-seam split landed** — `engine.ts` decomposed; `load-engine.ts:123` is
  the realized `loadAnimationEngine()` seam.
- **`docs/*.md` persistent surface is clean but for dogfood-inversion:48** —
  scroll-morph / color-fidelity / all three MIGRATION docs carry no stale forward
  plan (CH-06's coverage gap discharged).
- **Early outward asks (A/B/C spring-token, H VJ-F/PT-4) absent from S/T/U** —
  `grep` over the last three tranches returns nothing; constellation-absorbed, not
  silently carried.
- **KILL rows stayed KILLed** — ScrollTimeline-native (B), Worker/OffscreenCanvas
  (A), FB-6 `Mod+K` palette (J), P.W4 codegen tombstone: none re-surface in S/T/U.

## Coverage gaps

- **Cross-repo product truth (Glass 7 / Atlas 4 / value.js 4) not verifiable here** —
  CH2-02 verifies the kf-side *guard removal* but not whether Glass 7 actually fixed
  BG-5/GU-1/GU-2 (glass-ui is read-only, out of lane). FAM-01 live-verify lanes own
  the product side.
- **11k audit-subtree hits triaged by channel, not line-by-line** — I classified the
  ~11,215 per-lane/wave audit hits as class-(i) historical *because they sit outside
  the forward-carry channel* (the `proof:chronic-closure` ledger), not by reading
  each. A residual stale present-tense clause could hide in a per-lane audit doc the
  way dogfood-inversion:48 did in a `docs/*.md` file — but such a doc has no
  mechanism to carry the row forward, so its blast radius is a stale-prose P3, not a
  live obligation. Full line-by-line of the audit sub-trees is a larger wave than one
  lane (R1-10's same gap, now bounded: the risk is bounded to stale-prose, never live
  debt, because the channel is dead).
- **GCF-03 (sci-report, unsent)** — unchanged from R1-10; cross-repo, unverified here.

## Count

- **Total sweep hits:** 11,536 (`docs/tranches` + `docs/*.md`).
- **Discarded (class-i historical / decided-landed):** ~11,527 — the ~11,215
  audit-subtree working-prose hits + the A→R close-doc rows a later tranche
  discharged (VAL-9, VJ-F/PT-4, K engine-seam, L Band-B, all KILL rows, etc.).
- **Family-covered (cite, no new row):** R1-10 CH-01..06 (FAM-01/FAM-08) + the A/B/C/H
  outward asks (FAM-11) + M/Q glass-ui BC (FAM-01).
- **New rows minted:** **2 live** — CH2-01 (retire-resolved, value.js collision) and
  CH2-02 (de-tripwired glass-dock residue, the one genuinely-actionable new finding);
  plus **2 book-keeping** — CH2-03 (decided-terminal, retire from set) and CH2-04
  (confirms/discharges CH-06's coverage gap).
- **Structural finding:** V inherits **no formal chronic ledger** — the machine was
  deleted at U and every prior ledger self-terminated. The disease-bearing channel is
  closed; residual risk is bounded to stale doc-prose (P2/P3), not live carried debt.
