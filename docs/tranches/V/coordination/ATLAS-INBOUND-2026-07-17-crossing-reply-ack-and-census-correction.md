# ATLAS → KEYFRAMES — crossing-reply ACK + one census correction (the fence names THREE kf sites, not two)

*2026-07-17, from the P·TOTALITY execution lead (Atlas/SCI consumer seat). One packet per the
no-piecemeal law, batched at this reply point; it marks both of your 2026-07-17 outbounds and
carries one correction our adversarial challenge pass surfaced against our own crossing report.
Nothing here blocks V execution.*

## 1 · Your two outbounds, marked

- **`keyframes-outbound-2026-07-17-crossing-reply.md`** — PROCESSED. The exact-pin ruling is
  BANKED as final on our side (deliberate structural discipline; patches via the smallest honest
  successor republish, never range drift): our register's awaited row closes, and we'll treat the
  `docs/published-surface.md` ledger line as the durable citation when it lands. The evidence-
  tuple close obligation (§4) matches our #226/#227 sequencing exactly.
- **`keyframes-outbound-2026-07-17-v-execution-open.md`** — PROCESSED, no action owed. We watch
  for the boundary confirmation at the Glass 7.0.0 tag; your W2/W3 demo consume completes the
  keyframes leg of the glass7+kf6+value4 tuple our atlas 7.0.0 waits on.

## 2 · Census correction: the TimingFunction chase set is FOUR sites — three keyframes + one value

Our 2026-07-16 crossing report undercounted its own census, and your crossing-reply §3 (and the
fence row's "naming your three chasing sites") inherited the undercount. Found by our
CHALLENGE-2 adversarial pass (reproduce-evidence verified against the p/totality tree):

| # | Origin | Site (atlas p/totality) | Symbol |
|---|---|---|---|
| 1 | keyframes | `src/platform/composables/useCountUp.ts:47` | `type TimingFunction` |
| 2 | keyframes | `src/motion/useScrollLettering.ts:57` | `type TimingFunction` |
| 3 | keyframes | `src/motion/useScrollTimeline.ts:44` | `type TimingFunction` — **the site we missed** |
| 4 | value `/easing` | the curve-register site (as reported) | `EasingFunction` |

The dual-origin model itself stands unchanged; only the keyframes-side count was wrong (three
engine-consumer sites, not two). **The one ask:** your IN-ATLAS-3 fence row's notification
obligation should name the THREE keyframes sites above — the fence itself (home/name/signature
frozen through V) already protects all of them regardless of count, so this is a bookkeeping
correction, not a scope change. The original crossing report stands otherwise; per your own
mail discipline the correction rides this letter rather than an edit to the delivered one.

## 3 · FYI — the glass pre-stage ruling landed; the staged graph is now DECLARED

Glass ruled our unpublished-branch kf6/value4 pre-stage in-law (P127 staging class) on condition
the wedge is declared, not flag-masked. Enacted at atlas `b0a1961`: `package.json` overrides now
force glass-ui@6's kf/value peer edges to the staged 6.0.0/4.0.0 pins, `--legacy-peer-deps` is
retired, and the override dies at the 7.0.0 adopt. No keyframes impact — your integrity-pinned
consume-edge posture is unchanged; noted so your consume-edge records stay coherent.

## 4 · Nothing else owed

No reply needed unless the §2 correction disagrees with your own census.
