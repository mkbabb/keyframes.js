# Tranche V → successor — THE FOLD-FORWARD LEDGER (owner-ordered, 2026-07-17)

> The owner's directive at the Glass-7 adopt: *"ensure that all heretofore
> non-implemented waves are folded forward to the next begotten tranche;
> ensure that ALL heretofore features that were NOT audited and developed in
> this most recent series of tranches, those exhortations and marks, are
> folded forward to that tranche, too."* This ledger is that guarantee —
> complete, row-for-row, nothing silent. The successor's FORMATION mints its
> charter from §A + §B below plus OD-V2's total demo audit. V's own close
> ceremony (W13) executes as the successor formation's opening act.

## §A — Wave dispositions (against `V.md`'s 13-wave table)

| Wave | State at fold | What folds forward (exact) |
|---|---|---|
| W1 Render Truth | **CLOSED — landed with W2.** Born-RED witnesses + the GREEN witness on real registry Glass 7 (14/14, pageerror=0, objectObject=0) both archived in `audit/harness/`. | Nothing. (The nested-provider deviation is §B-7.) |
| W2 Glass-7 Consume | **CLOSED** (`add20b7e`/`ebb08948`): 65-path slice + W1 extension + XR-4 + exact glass `7.0.0` devDep, registry-only lock, one physical core, CC-05 watchlist green, landed-slice manifest `0d2c1cc0…` from the committed pinned script. Full battery GREEN — full check + full test are the honest battery again (FAM-01 CURED). | The **CH2-02 ×4 dock-crispness re-verifies and RG-1/RG-2 re-verifies** (W2 Scope 6–7) were NOT individually discharged — folded into the successor demo audit, not silently retired. |
| W3 Native Close & Deploy | **PARTIAL.** Demo build green (the K6 `/units` deploy-red cured); harness-level render matrix green; deploy round-trip through the Pages workflow in flight at fold (run IDs in PROGRESS). | The full §5.11 NATIVE interaction matrix (HeaderRibbon actionable, DarkModeToggle theme flip, cube Play→Pause motion, dock one-active-face, no-slab sweep) at 1280/390; the completed deploy-of-record table; the constellation consume-confirmation packets ride the fold boundary (sent at compaction). No kf version was cut — no atlas tuple is owed, only the boundary confirmation. |
| W4 Structure Gate & Openers | **CLOSED** (+AM-1 `drag/2d.ts`). | Nothing. |
| W5 Module Carves | **CLOSED** (6 modules, fences empty-diff). | Nothing. |
| W6 Encapsulation Sweep | **CLOSED** (R6 rule; 32 demotions + 3 dead deletes; divergence adjudicated). | Nothing. (The dead-delete × transaction collision was repaired in-wave at W2 — §B-8.) |
| W7 Demo Grammar & Chrome | **NOT IMPLEMENTED — folds whole.** | DT-02 (app/dock → `components/chrome/`), DT-03 (18-file transport moves), DT-09, DT-01 casing grammar; gated on the folded MR4. Blueprint `audit/R2-06` remains the mechanism of record; re-derive anchors against the LANDED slice tree (the R3-04 gap note). |
| W8 Kind-Dirs & Channel Module | **NOT IMPLEMENTED — folds whole.** | DT-04..08; the channel-options module + **EasingField-only** extraction under the **OD-V4-RATIFIED 500 ceiling**; DD-4 sweep (test/+bench/ grepped); **CT-04 remainder** (~8 resolving deep `@src/animation` imports incl. `css-text` ×3, `selector` ×1, `browser` ×2, `internal/helpers` camelCase ×1 — re-grep at execution); proof:structure demo-scope extension with the W4-staged R4/R5 red witnesses (ChannelOptions 609L + instrument kind-dirs). |
| W9 Make-Real Quartet & Prune | **AUTHORED + STAGED, landing folds.** Branch `v/w9-staging` (2 commits, pushed): MR1–4, relabel, TC-5 trio, BV-2 golden (closed as documentation), prune set + tombstones. | The LANDING: rebase over the slice-landed `deploy-pages.yml` (MR3 conflict — re-derive mechanically); MR4 is now GREEN-able (demo tests pass on the consumed tree — its CI red-once witness at landing); the four CI-run witnesses; TC-5 verification. |
| W10 Doc Canon | **Pre-rail subset LANDED.** | DM-16/17 (ci.yml relabel, after W9 lands); DM-18 demo-comment set token-grepped (+ NEW: `demo/styles/style.css:22` tokens-path prose accuracy); `demo/DESIGN.md:238,253`; CT-03 `kf-engine.ts` prose; the sync-step ×2 stale-comment residue (`test/physics/sync-step.test.ts:24`, `bench/sync-step.bench.ts:129`); Scope-10 memory re-pins. |
| W11 Proportion & Affordance | **NOT IMPLEMENTED — folds whole.** The owner's UI-fix corpus, blocked through V by the un-spawnable demo; the demo NOW SPAWNS. | DP2-04 (Select trigger name-only), DP2-05 (preset-card dead band), DP2-07 (spring lozenge), **OD-V3** (DP2-06 packet-FIRST capture review at 390/1280 — ruling held), **OD-V5** (RG-2 re-examined on glass's dock mark), FE-4 favicon, the **AY-1..5 a11y quintet**, π/DELTA with the banked 40-PNG BEFORE set (`audit/design-captures/` → `screenshots/before/` at open). |
| W12 Coordination | **Substantially discharged** (every inbound marked: IN-ATLAS-1..5, IN-GLASS-1..2, IN-VALUE-1..2 incl. the WL verdicts; GCF-03 confirmed SCI-owned; D-GAP-6 DECLINED-WITH-PATTERN — the value boundary is CLEAR). | G-1..G-4 glass batch-letter mark verification (their INBOUND-MARKS at their cadence); ledger terminalization at V-close (= successor opening). |
| W13 Close Ceremony | **NOT EXECUTED — folds as the successor formation's OPENING ACT**: ledger walk (PROMPT-RECAP 68 + DISPOSITIONS 52 + INBOUND), ι integrity sweep (incl. the worktree family + this clone), 3 read-only close-audit lanes, FINAL-V, π/DELTA archive verification, VM-4 byte-authority check (184,430 B). | All of it, as one bounded opening band. |

## §B — The unaudited features, exhortations, and marks (nothing silent)

1. **Glass §7 known defect — V-A95 aurora reverse-drag black slab.** LIVE EXPOSURE: `demo/components/instrument/shell/HeroAurora.vue` consumes the aurora substrate. Glass owns the cure (born-RED BJ wave GF-AURORA); the successor demo audit verifies our surface when their fix ships.
2. **Glass §7 known defect — Chip/Badge orphaned dist CSS.** LIVE EXPOSURE: 2 demo files compose Chip/Badge; glass treatment incomplete until their re-wire wave. Successor audit row.
3. **Glass §4 dock contract** (PEEK is NOT native; binary FSM + opacity crossfade; manual-mode suppression): our dock consumption re-verifies against THIS shipped contract — subsumes the folded CH2-02 ×4 (BG-5, GU-1, GU-2, subject-legible).
4. **G-1..G-4 batch rows** (dock double-click activation, modelValue rationale, a11y pointer) — delivered, awaiting glass marks; OD-V5's RG-2 re-examination keys off the same boundary.
5. **OD-V3 / OD-V5** — the two held owner rulings (capture-review packet; dock-mark re-examination). Never proxied.
6. **IN-ATLAS-5 census** — TimingFunction's THREE kf chase sites named in the fence obligation; the fence itself held through every V restructure (verified at every FENCE D echo).
7. **The nested `<TooltipProvider>`** at `AnimationControlsGroup.vue:2` — valid reka nesting, left in place by the bounded W1 patch; the demo settlement rules whether the clean shape drops it.
8. **The W6×transaction collision, repaired but instructive**: `debounce`/`hyphenToCamelCase`/`convertPixelsToCh` were src-dead but transaction-live; re-homed at `demo/utils/helpers.ts`. LESSON for the successor's gates: a src-scoped dead-sweep must also grep the LANDING consumer tree when a consume transaction is in flight.
9. **MR2 runner-parity watch item** (system Chrome vs pinned revision on the Linux runner) — recorded in the staged W9 branch; verified at W9 landing.
10. **CH-02 watch row** — becomes DISEASE if the successor re-words rather than decides. Carried verbatim.
11. **VM-4** — 6.0.0 tarball byte authority 184,430 B; close-audit doc-drift check item.
12. **The owner-checkout reconciliation** — SAFE NOW (W2 landed; the transaction is on master AND byte-preserved at `keyframes-v-w2-prestage/`). One act (`git fetch && git reset --hard origin/master` in the owner checkout, untracked V docs unharmed), owner's hand or the successor's opening; until then the checkout simply lags.
13. **Standing invariants carried into the successor charter**: frozen `.`+`./engine` surface (44-key mirror), `TimingFunction` at `constants/types.ts:45`, `scenes/` exemplar fence, `docs/precepts/` read-only, exact-pin consume-edges (now bilaterally ratified), no-legacy/no-masking, zero-deferral close, batches-of-3 agent law, Fable-deepest/Opus-fanout routing.
14. **OD-V2 — the successor's opening audit band**: the owner-ordered TOTAL demo audit after glass perfection, now concretely enriched by rows 1–5 above + the W11 corpus + the W3 native matrix. Glass's own Q003/Q002/Q051 close state gates "perfection"; the audit does not wait on it to BEGIN auditing what already ships.
15. **External watches, no kf action**: value SCI-1 (their possible 4.x into-variant); their terminal clean-pass corrections (none expected); speedtest — no open edges.

## §C — What the successor inherits GREEN

Master `ebb08948`: immutable K6 producer + the settled library (proof:structure
R1–R6, 159 modules, 0 depcruise violations) + the CONSUMED Glass-7 demo (real
registry 7.0.0, exact-pinned, one core) + full check/test/lint/build/
proof:publish green + the render-truth instrument and its paired RED/GREEN
witnesses. The demo SPAWNS. The UI-fix frontier is open and measured.
