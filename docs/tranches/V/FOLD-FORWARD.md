# Tranche V → successor — THE FOLD-FORWARD LEDGER (owner-ordered, 2026-07-17)

> The owner's directive at the Glass-7 adopt: *"ensure that all heretofore
> non-implemented waves are folded forward to the next begotten tranche;
> ensure that ALL heretofore features that were NOT audited and developed in
> this most recent series of tranches, those exhortations and marks, are
> folded forward to that tranche, too."* This ledger is that guarantee —
> complete, row-for-row, nothing silent. The successor's FORMATION mints its
> charter from §A + §B below plus OD-V2's total demo audit. V's own close
> ceremony (W13) executes as the successor formation's opening act.

## §A — Wave dispositions (against `V.md`'s 13-wave table)

| Wave | State at fold | What folds forward (exact) |
|---|---|---|
| W1 Render Truth | **CLOSED — landed with W2.** Born-RED witnesses + the GREEN witness on real registry Glass 7 (14/14, pageerror=0, objectObject=0) both archived in `audit/harness/`. | Nothing. (The nested-provider deviation is §B-7.) |
| W2 Glass-7 Consume | **CLOSED** (`add20b7e`/`ebb08948`): 65-path slice + W1 extension + XR-4 + exact glass `7.0.0` devDep, registry-only lock, one physical core, CC-05 watchlist green, landed-slice manifest `0d2c1cc0…` from the committed pinned script. Full battery GREEN — full check + full test are the honest battery again (FAM-01 CURED). | The **CH2-02 ×4 dock-crispness re-verifies and RG-1/RG-2 re-verifies** (W2 Scope 6–7) were NOT individually discharged — folded into the successor demo audit, not silently retired. |
| W3 Native Close & Deploy | **PARTIAL.** Demo build green (the K6 `/units` deploy-red cured); harness-level render matrix green; deploy round-trip through the Pages workflow in flight at fold (run IDs in PROGRESS). | The full §5.11 NATIVE interaction matrix (HeaderRibbon actionable, DarkModeToggle theme flip, cube Play→Pause motion, dock one-active-face, no-slab sweep) at 1280/390; the completed deploy-of-record table; the constellation consume-confirmation packets ride the fold boundary (sent at compaction). No kf version was cut — no atlas tuple is owed, only the boundary confirmation. |
| W4 Structure Gate & Openers | **CLOSED** (+AM-1 `drag/2d.ts`). | Nothing. |
| W5 Module Carves | **CLOSED** (6 modules, fences empty-diff). | Nothing. |
| W6 Encapsulation Sweep | **CLOSED** (R6 rule; 32 demotions + 3 dead deletes; divergence adjudicated). | Nothing. (The dead-delete × transaction collision was repaired in-wave at W2 — §B-8.) |
| W7 Demo Grammar & Chrome | **NOT IMPLEMENTED — folds whole.** | DT-02 (app/dock → `components/chrome/`), DT-03 (18-file transport moves), DT-09, DT-01 casing grammar; gated on the folded MR4. Blueprint `audit/R2-06` remains the mechanism of record; re-derive anchors against the LANDED slice tree (the R3-04 gap note). |
| W8 Kind-Dirs & Channel Module | **NOT IMPLEMENTED — folds whole.** | DT-04..08; the channel-options module + **EasingField-only** extraction under the **OD-V4-RATIFIED 500 ceiling**; DD-4 sweep (test/+bench/ grepped); **CT-04 remainder** (~8 resolving deep `@src/animation` imports incl. `css-text` ×3, `selector` ×1, `browser` ×2, `internal/helpers` camelCase ×1 — re-grep at execution); proof:structure demo-scope extension with the W4-staged R4/R5 red witnesses (ChannelOptions 609L + instrument kind-dirs). |
| W9 Make-Real Quartet & Prune | **AUTHORED + STAGED, landing folds.** Branch `v/w9-staging` (2 commits, pushed): MR1–4, relabel, TC-5 trio, BV-2 golden (closed as documentation), prune set + tombstones. | The LANDING: rebase over the slice-landed `deploy-pages.yml` (MR3 conflict — re-derive mechanically); MR4 is now GREEN-able (demo tests pass on the consumed tree — its CI red-once witness at landing); the four CI-run witnesses; TC-5 verification. |
| W10 Doc Canon | **Pre-rail subset LANDED.** | DM-16/17 (ci.yml relabel, after W9 lands); DM-18 demo-comment set token-grepped (+ NEW: `demo/styles/style.css:22` tokens-path prose accuracy); `demo/DESIGN.md:238,253`; CT-03 `kf-engine.ts` prose; the sync-step ×2 stale-comment residue (`test/physics/sync-step.test.ts:24`, `bench/sync-step.bench.ts:129`); Scope-10 memory re-pins. |
| W11 Proportion & Affordance | **NOT IMPLEMENTED — folds whole.** The owner's UI-fix corpus, blocked through V by the un-spawnable demo; the demo NOW SPAWNS. | DP2-04 (Select trigger name-only), DP2-05 (preset-card dead band), DP2-07 (spring lozenge), **OD-V3** (DP2-06 packet-FIRST capture review at 390/1280 — ruling held), **OD-V5** (RG-2 re-examined on glass's dock mark), FE-4 favicon, the **AY-1..5 a11y quintet**, π/DELTA with the banked 40-PNG BEFORE set (`audit/design-captures/` → `screenshots/before/` at open). |
| W12 Coordination | **Substantially discharged** (every inbound marked: IN-ATLAS-1..5, IN-GLASS-1..2, IN-VALUE-1..2 incl. the WL verdicts; GCF-03 confirmed SCI-owned; D-GAP-6 DECLINED-WITH-PATTERN — the value boundary is CLEAR). | G-1..G-4 glass batch-letter mark verification (their INBOUND-MARKS at their cadence); ledger terminalization at V-close (= successor opening). |
| W13 Close Ceremony | **NOT EXECUTED — folds as the successor formation's OPENING ACT**: ledger walk (PROMPT-RECAP 68 + DISPOSITIONS 52 + INBOUND), ι integrity sweep (incl. the worktree family + this clone), 3 read-only close-audit lanes, FINAL-V, π/DELTA archive verification, VM-4 byte-authority check (184,430 B). | All of it, as one bounded opening band. |

## §B — The unaudited features, exhortations, and marks (nothing silent)

1. **Glass §7 known defect — V-A95 aurora reverse-drag black slab.** LIVE EXPOSURE: `demo/components/instrument/shell/HeroAurora.vue` consumes the aurora substrate. Glass owns the cure (born-RED BJ wave GF-AURORA); the successor demo audit verifies our surface when their fix ships.
2. **Glass §7 known defect — Chip/Badge orphaned dist CSS.** LIVE EXPOSURE: 2 demo files compose Chip/Badge; glass treatment incomplete until their re-wire wave. Successor audit row.
3. **Glass §4 dock contract** (PEEK is NOT native; binary FSM + opacity crossfade; manual-mode suppression): our dock consumption re-verifies against THIS shipped contract — subsumes the folded CH2-02 ×4 (BG-5, GU-1, GU-2, subject-legible).
4. **G-1..G-4 batch rows** (dock double-click activation, modelValue rationale, a11y pointer) — delivered, awaiting glass marks; OD-V5's RG-2 re-examination keys off the same boundary.
5. **OD-V3 / OD-V5** — the two held owner rulings (capture-review packet; dock-mark re-examination). Never proxied.
6. **IN-ATLAS-5 census** — TimingFunction's THREE kf chase sites named in the fence obligation; the fence itself held through every V restructure (verified at every FENCE D echo).
7. **The nested `<TooltipProvider>`** at `AnimationControlsGroup.vue:2` — valid reka nesting, left in place by the bounded W1 patch; the demo settlement rules whether the clean shape drops it.
8. **The W6×transaction collision, repaired but instructive**: `debounce`/`hyphenToCamelCase`/`convertPixelsToCh` were src-dead but transaction-live; re-homed at `demo/utils/helpers.ts`. LESSON for the successor's gates: a src-scoped dead-sweep must also grep the LANDING consumer tree when a consume transaction is in flight.
9. **MR2 runner-parity watch item** (system Chrome vs pinned revision on the Linux runner) — recorded in the staged W9 branch; verified at W9 landing.
10. **CH-02 watch row** — becomes DISEASE if the successor re-words rather than decides. Carried verbatim.
11. **VM-4** — 6.0.0 tarball byte authority 184,430 B; close-audit doc-drift check item.
12. **The owner-checkout reconciliation** — SAFE NOW (W2 landed; the transaction is on master AND byte-preserved at `keyframes-v-w2-prestage/`). One act (`git fetch && git reset --hard origin/master` in the owner checkout, untracked V docs unharmed), owner's hand or the successor's opening; until then the checkout simply lags.
13. **Standing invariants carried into the successor charter**: frozen `.`+`./engine` surface (44-key mirror), `TimingFunction` at `constants/types.ts:45`, `scenes/` exemplar fence, `docs/precepts/` read-only, exact-pin consume-edges (now bilaterally ratified), no-legacy/no-masking, zero-deferral close, batches-of-3 agent law, Fable-deepest/Opus-fanout routing.
14. **OD-V2 — the successor's opening audit band**: the owner-ordered TOTAL demo audit after glass perfection, now concretely enriched by rows 1–5 above + the W11 corpus + the W3 native matrix. Glass's own Q003/Q002/Q051 close state gates "perfection"; the audit does not wait on it to BEGIN auditing what already ships.
15. **External watches, no kf action**: value SCI-1 (their possible 4.x into-variant); their terminal clean-pass corrections (none expected); speedtest — no open edges.

## §C — What the successor inherits GREEN

Master `ebb08948`: immutable K6 producer + the settled library (proof:structure
R1–R6, 159 modules, 0 depcruise violations) + the CONSUMED Glass-7 demo (real
registry 7.0.0, exact-pinned, one core) + full check/test/lint/build/
proof:publish green + the render-truth instrument and its paired RED/GREEN
witnesses. The demo SPAWNS. The UI-fix frontier is open and measured.

---

# ADDENDUM 2026-09-18 — X.KF.W10 `.a`: THE §B DISCHARGE TABLE (G-1) AND THE CARRY-FORWARD RECORD BLOCK

SERVED MODEL: claude-opus-5[1m]

**Authority**: `value.js docs/tranches/X/keyframes/waves/KF-W10.md` §3.1 (`:192-266`) · §5 **G-1** (`:384-394`) ·
§6.A ordering 3 · §6.B carried locks (`:513-524`) · §9.1 · §10 commit 1. Sub-tranche X·KF, wave **KF.W10**,
owned by **SS-2** (`COHESION.md` §0t). Sitting of record **2026-09-17** (the owner's begin-word, COHESION
§0j); this seat's clock **2026-09-18 23:2x EDT**.

**E-3 posture — this is an APPENDED DATED ADDENDUM, never a patch.** §A, §B and §C above are **byte-unchanged**.
Pre-addendum bytes, measured twice at these bytes before the write: **9,289 B · 54 L**, `shasum`
**`797029037559bbc64d935285065f83ae2fb958b9`**, identical to `git show origin/master:docs/tranches/V/FOLD-FORWARD.md`
(`git diff --stat origin/master -- <this file>` → empty). Substrate of record: kf `origin/master` **`69095552`**
(the authoring-time `81a56990` is superseded by KF.W0 §B-12's settle; §6.B's re-baseline law re-anchors it here).

**THE TWO DENOMINATOR LOCKS, ASSERTED EXPLICITLY (G-1 acceptance; R-14 extended by R2-2).**

1. **§B is 15 rows and ONLY 15.** Measured read-only at these bytes, double-run:
   ⟨cmd⟩ `awk 'NR>30 && NR<48 && /^[0-9]+\./' FOLD-FORWARD.md | wc -l` → **15** (run 1) · **15** (run 2).
   This addendum discharges 15 and creates none.
2. **The CARRY-FORWARD RECORD block is 4 records, and it sits BESIDE the table.** It contributes **nothing**
   to G-1's arithmetic and is never promoted into it: a §B row and a carry-forward record are different
   objects, and a close wave that inflates the denominator it exists to close has closed nothing. G-1 stays
   **15**; G-5 stays **4 addenda**, untouched by this block's row count.

**THE CLOSED VOCABULARY** is declared in exactly one place — `KF-W10.md` §3.1's alphabet table — and is
**cited, never restated as an alphabet** here, so a widening can never leave this addendum reading against a
stale word list: **`LANDED`** ⟨`<commit>`⟩ · **`KILLED`** ⟨`<tombstone path>`⟩ · **`RULED`** ⟨the ruling's own
text + its authority coordinate: an **owner quote** for owner-gated rows, a **rulings citation by path** for
rows the program has ruled as law⟩ · **`FOLDED-TO`** ⟨a banked id resolving in the 58-record registry, **or** a
sibling-wave §-anchor resolving at that wave's bytes, **or** a COHESION register row cited by its **declared
positional key**⟩ · **`STANDING-CARRIED`** ⟨the carried text — charter, watch row, or the standing surface that
carries it forward — **or** a NAMED GATE RECEIPT of this wave, with the arm that produces it⟩ ·
**`DISCHARGED-BY-CONSTRUCTION`** ⟨the receipt, cited⟩ · **`ENUMERATED-NOT-MINTED`** ⟨the C-17.R roster row +
its enumerated disposition — **inside the CARRY-FORWARD block only, never on a §B row**⟩.

**The dispositions below are CARRIED, not re-derived** (§2: *W10 consumes; it never cures*). Every figure this
addendum publishes was read from these settled bytes at this seat's clock and double-run.

## §B DISCHARGE — 15 rows, exactly one terminal verb each

| §B | row | **terminal verb** | evidence, in the form the verb requires — measured at this seat |
|---|---|---|---|
| **1** | aurora reverse-drag black slab (V-A95) | **STANDING-CARRIED** | **`COHESION.md` §4a — the SS-6 communiqué accretion register**, the standing surface that carries this row forward ⟨heading measured `:116`; its dispatch row `:129` states the register *"resumes accreting for the next boundary"* after **O-20 departed 2026-08-28** carrying **zero** aurora cargo⟩. Glass owns the cure (born-RED BJ wave **GF-AURORA**); the row rides the **SUCCESSOR SS-6 batch**, never O-20 — the dated sent-row is a receipt a later boundary produces **from** this register, and is the follow-on, never this verb's evidence. Surface-verify leg: **KF.W9 §Bounds' capture-receipt row** — **anchor-only (LAW C(3))**, re-resolved at that wave's bytes this seat ⟨`grep -c 'SS-13-CAPTURE-RECEIPT' KF-W9.md` → **5**, double-run⟩ |
| **2** | Chip/Badge orphaned dist CSS | **FOLDED-TO `COHESION-SC-1`** | The **DECLARED POSITIONAL KEY** for `COHESION.md` §4a's **first** register row (`:120` — *"glass-ui 7.0.0 ships `glass-chip.css` and never imports it anywhere … ALL Chip adoption in this repo BLOCKED-ON the producer cure"*), in the **third evidence form declared for this verb** (§3.1's alphabet table). **KEY DISCIPLINE — the collision is why the key is positional**: bare `SC-1` resolves **to a different subject** in the very 58-record corpus G-2 sweeps. Receipts re-run this seat, **counting rule stated at the enumeration (unit = LINE; the `ESC-1` substring class excluded by construction)**: ⟨cmd⟩ `grep -n 'SC-1' COHESION.md \| grep -v 'ESC-1'` → **`:120` `:129`** (2 lines, double-run); ⟨cmd⟩ the same over `registry/adjudicated/kf-SequenceScene.md` → **`:13` `:28` `:57` `:58` `:106` `:149` `:154` `:158`** (**8** lines, double-run), where `:58` is *"**SC-1** *(NET-NEW — reader-B M-1)* — the GETTER on line 30 mints and persists a whole control-options bucket on the READ"* — a live **NO-WAVE-OWNER** row — and `:149` reads *"**SC-1..SC-8 minted here for life**"*. **Two eight-member SC-1..SC-8 rosters, two meanings for one key.** ⟨The spec's round-4 five-coordinate reading is a **dated observation superseded at these bytes**, recorded not re-derived; the collision itself is unchanged — `:58` and `:149` both reproduce.⟩ **The registry ids are untouched and never re-keyed**: the positional key is minted on THIS file's side of the fold, exactly as `CARRY-C-*` is |
| **3** | Glass §4 dock contract re-verify (subsumes the folded CH2-02 ×4: BG-5, GU-1, GU-2, subject-legible — **quoted from §B-3's own sentence above, never asserted as banked ids**) | **FOLDED-TO** — the two sibling §Bounds anchors | **KF.W6 §Bounds' TransportDock rows** and **KF.W9 §Bounds' capture-receipt row**, each **re-resolved at its own wave's bytes by this seat** (the spec's standing instruction, sibling specs having moved within every round): ⟨cmd⟩ `grep -c 'TransportDock' KF-W6.md` → **12**, double-run — the `demo/components/instrument/transport/TransportDock.vue` modify-carve row carries the DOCK-CONTRACT RE-VERIFY cell naming **this end of the W10 seam** and the `--dock-margin` witnesses `:389`/`:395`; ⟨cmd⟩ `grep -c 'SS-13-CAPTURE-RECEIPT' KF-W9.md` → **5**, double-run — its §Bounds row creates the surface receipt and names cell **(c)** *"the glass §4 dock-contract re-verify cell that KF-W10 §B-3 re-homes here"*. **The seam is two-ended and agreeing.** The four subsumed names are **not** banked ids: ⟨cmd⟩ `grep -l 'CH2-02\|BG-5\|GU-1\|subject-legible' registry/adjudicated/kf-*.md \| wc -l` → **0 files** of **58**, double-run — which is exactly why this row folds to **anchors** and not to an id |
| **4** | G-1..G-4 batch marks | **STANDING-CARRIED** | **`COHESION.md` §4a's re-opened accretion register — the same standing surface as row 1** ⟨`:116` heading; `:129` dispatch row⟩. Delivered, awaiting glass marks; **OD-V5 keys off the same boundary**. The vehicle is the **SUCCESSOR SS-6 batch**, never the departed O-20 |
| **5** | OD-V3 / OD-V5 | **RULED** | **Owner quotes — the form G-3's three require** — read at kf `origin/master:docs/tranches/V/OWNER-DECISIONS.md`: **OD-V3 `:7`** *"Decide per capture review"* → *"**THE TRANSPORT-HOME RULING IS HELD FOR CAPTURE REVIEW** … never proxied, never defaulted."* · **OD-V5 `:9`** *"Defer to glass's dock fix"* → *"**THE AT-REST REOPEN QUESTION IS DEFERRED PENDING GLASS'S DOCK MARK.**"* **Authority coordinate for the closure shape**: `COHESION.md` **§0j.C KF-ODV3 / KF-ODV5 / KF-AT** — *"the transport-home ruling is taken by the orchestrator **from the OD-V3 packet** at KF.W10 `.g`'s sitting under this delegation, and **if the packet does not exist by then KF.W10 closes `complete_with_misses` on that row citing its exact precondition**. **OD-V5 stays DEFERRED** … the `complete_with_misses` shape is authorized in advance."* **`"Never proxied."` is carried verbatim.** The two closures are this wave's **owner block (G-3, unit `.c`)**, each citing its own exact precondition; **this row stamps the verb the owner's two rulings already carry and proxies no owner** — §6.B's lock (*§B-5 may take no terminal verb until the owner block carries its ruling, or `complete_with_misses`*) is satisfied **by the ruling-in-advance, never by a default** |
| **6** | IN-ATLAS-5 TimingFunction fence (3 kf chase sites) | **STANDING-CARRIED** | **The lane-docs row, quoted** — `value.js docs/tranches/V/megatranche/formation/keyframes/lane-docs.md` **row 14** ⟨`:378`⟩: *"\| 14 \| **IN-ATLAS-3 fence** \| K→(atlas) \| `TimingFunction`'s published home/name/signature **frozen surface**; 3 atlas chase sites named; the fence held through every V restructure \| `INBOUND-LEDGER.md:28`; `FOLD-FORWARD.md` §B-6, §B-13 \| **STANDING; verified held** \|"*. **No action.** ⟨**Dated observation, recorded and NOT re-derived** (§2 — W10 consumes): the lane-docs row keys the fence **IN-ATLAS-3** and §B-6 keys it **IN-ATLAS-5**; the row's own source column names `FOLD-FORWARD.md §B-6`, so the two spellings name **one** obligation. W10 records the divergence and **re-keys nothing**.⟩ |
| **7** | nested `<TooltipProvider>` at `AnimationControlsGroup.vue:2` | **FOLDED-TO `KF-APP-21`** | **A banked id resolving in the 58-record registry**, re-verified this seat: ⟨cmd⟩ `grep -n 'KF-APP-21' registry/adjudicated/kf-App.md` → **`:26` `:71` `:159`**; the routing cell at `:71` reads *"Three nested TooltipProviders, 700ms vs 100ms (7×) in one chrome; glass-ui forwards no defaults …; frontier keeps the root provider deliberately (DP-02/CC-03)"* → **`KF.W6`** *(one provider policy; the root provider is load-bearing at the frontier — **cure is config unification, not deletion**)*. **§B-7's open question — *"the demo settlement rules whether the clean shape drops it"* — is ANSWERED by the bank: it does not drop it.** |
| **8** | the W6×transaction collision LESSON | **RULED** | **The ruling's own text + its authority coordinate by path**: `value.js docs/tranches/X/keyframes/conformance/PASS-3/RULINGS-3.md`, **LAW A**, at `:12` — *"**LAW A · THE IMPORT-GRAPH LAW** — a delete/repoint/shim act's consumer census is derived from the import graph at the frontier, never from line counts, never from the wave tree's own text"* — **binding program-wide on every seat** that writes, carries or leaves standing a delete/repoint/shim act, which is strictly wider than the single-wave rule-list fold. ⟨The round-1 **fold-to-KF.W4 claim is STRUCK as a phantom fold** (correction (i) below); **the struck claim's verb token is deliberately NOT reprinted in this cell** — a §B row carries exactly one word from the alphabet, and this gate's falsifier convicts a row carrying two.⟩ |
| **9** | MR2 runner-parity watch (system Chrome vs pinned revision, Linux runner) | **STANDING-CARRIED** | **The NAMED GATE RECEIPT form, with the arm that produces it** — the watch is verified **AT** the `v/w9-staging` landing and not before, so it carries until **KF.W10's G-7** verb is stamped and **dies with it**. State measured at this seat's clock: ⟨cmd⟩ `git ls-remote --heads origin \| grep w9` → **`b920b1902b4854c1bc7c5778d1674436dd51dce6  refs/heads/v/w9-staging`**; ⟨cmd⟩ `git merge-base --is-ancestor b920b190 origin/master` → **UNMERGED**; **G-7 is RED and unstamped at this seat**. **LAND ⇒ G-7's landing CI witness** (the four run witnesses + TC-5); **KILL ⇒ the tombstone's MR2 unit**. Unit `.e` stamps the arm; this watch carries to that stamp and not one row past it |
| **10** | CH-02 watch row | **STANDING-CARRIED** — verbatim | **The verbatim carry**: *"becomes DISEASE if the successor re-words rather than decides."* A **decide-not-reword lock**: a re-worded CH-02 is a defect **by its own text**. This addendum carries the sentence and re-words nothing |
| **11** | VM-4 | **STANDING-CARRIED** | **The carried byte authority + the two reads, re-run at close.** Byte authority: the **6.0.0 tarball, 184,430 B**. Re-measured at these bytes this seat: ⟨cmd⟩ `grep -n '"version"' package.json` → **`:3    "version": "6.0.0",`**; ⟨cmd⟩ `grep -n '^## ' CHANGELOG.md \| head -1` → **`:6 ## 6.0.0`**, **no `Unreleased` section**. **No version cut since** — the doc-drift watch therefore carries **un-fired** rather than closing |
| **12** | owner-checkout reconciliation | **FOLDED-TO `KF.W0`** (census SCH-1/F-1) | **A sibling-wave §-anchor, resolved at KF.W0's bytes this seat**: `KF-W0.md` **§Carry** ⟨`:355`⟩, row **C-1** ⟨`:361`⟩ = the **`F-1 ≡ SCH-1`** re-anchor chain, and **§B-12**, the manifest-schism settle. **W10 ASSERTS the reconciliation happened; it never performs it.** The assertion, measured read-only in this repo this seat: branch **`master`** · `HEAD` **`69095552`** = `origin/master` **`69095552`** · ⟨cmd⟩ `git rev-list --left-right --count HEAD...origin/master` → **`0 0`** · snapshot ref present ⟨cmd⟩ `git branch --list 'kf-sacred-snapshot*'` → **`kf-sacred-snapshot-2026-09-17`**. **The checkout no longer lags**; §B-12's own act is KF.W0's, recorded here and not repeated |
| **13** | standing invariants | **STANDING-CARRIED** — verbatim into the successor charter | **The charter text, carried whole**: frozen `.` + `./engine` surface (**44-key mirror**) · `TimingFunction` at `constants/types.ts:45` · `scenes/` exemplar fence · `docs/precepts/` read-only · exact-pin consume-edges (**bilaterally ratified**) · no-legacy/no-masking · zero-deferral close · **batches-of-3 agent law** · Fable-deepest/Opus-fanout routing. **PLUS THE §9.1 COLLISION FLAG, STATED AND NOT SILENTLY RESOLVED (§6.B's explicit lock; KF-W10 §9 dissent 1)**: the inherited **batches-of-3 agent law** collides with the **value.js owner cap of MAX 4 CONCURRENT WORKFLOWS (2026-07-24)** — two **owner-issued** laws, unreconciled. **The successor charter carries BOTH and flags the collision; it may not silently adopt either.** X.KF.W10's own 6-serial-unit shape sidesteps the collision **by construction** and is therefore **no precedent** |
| **14** | OD-V2's owner-ordered TOTAL demo audit | **DISCHARGED-BY-CONSTRUCTION** | **The receipt, cited**: `COHESION.md` **§0a BOUNDARY 2026-08-25 — THE REGISTRY IS WHOLE** — *"**227/227 units banked** at `35fc8ebf` — value.js 88/88 (SS-7 CLOSED), **keyframes 58/58 (SS-10b CLOSED)**, fourier 66/66 …"*. Re-measured this seat: ⟨cmd⟩ `ls registry/adjudicated/kf-*.md \| wc -l` → **58**, double-run. **The megatranche adjudication program IS that audit**; re-verbing it as a landing or as a fold would **misdescribe** it, which is why this verb was declared into the alphabet rather than substituted (and the two rejected tokens are deliberately not reprinted in this cell) |
| **15** | external watches | **STANDING-CARRIED** | **The carried watch text + §6.C-G's PLAW-BIND routing.** §B-15's own *"no kf action"* is carried **verbatim** as the watch text. Value **SCI-1** (their possible 4.x into-variant) rides the 4.1 coordination event **CC-084** — *"no emergency 4.0.1 — ruled"* (`registry/CARRY-CUT-LEDGER.md:186`, cited in O-21 §C, which **WITHDREW** O-8's delivery-vehicle question on it) — through the **gate-keyed PLAW-BIND** chain: parse-that release condition → value (X·V L1/L5) → packed release → **KF.W3** repin **`4.0.0 → V`, where `V` is the registry coordinate `RC-P(V)` resolves to** (KF-W2 D-8; a predicate evaluated per `parse-that/RELEASE-CONDITION.md` §6a — **never a wave number and never a forecast version**). **W10 stamps the watch's terminal verb and does NOT schedule the cut. Direct parse-that→fourier remains FORBIDDEN.** |

**PER-VERB COUNTS, published from the settled table above (counting rule: the unit is a §B ROW; each row
carries exactly one verb, so the counts sum to the denominator or the table is wrong):**

| verb | rows | count |
|---|---|---|
| `LANDED` | — | **0** |
| `KILLED` | — | **0** |
| `RULED` | 5 · 8 | **2** |
| `FOLDED-TO` | 2 · 3 · 7 · 12 | **4** |
| `STANDING-CARRIED` | 1 · 4 · 6 · 9 · 10 · 11 · 13 · 15 | **8** |
| `DISCHARGED-BY-CONSTRUCTION` | 14 | **1** |
| `ENUMERATED-NOT-MINTED` | — (record block only, never a §B row) | **0** |
| **total** | **1–15, each exactly once** | **15 / 15** |

**2 + 4 + 8 + 1 = 15.** No row carries two verbs; no row carries none; no row carries a status word
(*"handled"*, *"noted"*, *"in flight"*) or a word outside §3.1's alphabet table. Every `FOLDED-TO` target was
resolved **at this seat** — the two §-anchors at their waves' bytes, the one banked id in the 58-record
registry, the one positional key at COHESION §4a. Every `RULED` coordinate is a **quote plus a path**, never a
paraphrase.

### THE THREE DATED CORRECTIONS, CARRIED WITH THE TABLE (carried, not re-derived)

**(i) §B-8's `FOLDED-TO KF.W4` is STRUCK as a phantom fold.** The claimed sentence is not at KF-W4's bytes;
G-1's own falsifier — *"a `FOLDED-TO` whose banked id does not resolve … fails on the id, not the prose"* —
convicts it. The true disposition is **larger** than the fold: the lesson is now **program law** at
`PASS-3/RULINGS-3.md` **LAW A**, which is why row 8 takes **`RULED`**.

**(ii) §B-3's evidence is RE-HOMED at two resolvable anchors**, not at a wave pair and not at ids. The four
subsumed names are **quoted from §B-3's own sentence** and are **0-of-58 in the registry** (re-run above); what
is load-bearing is the **anchor**, and both anchors resolve at this seat. The figures on that seam have read
five different true values at five clocks — that is the argument for anchoring, not a flaw in it.

**(iii) THE SS-6 VEHICLE CORRECTION — rows 1 and 4 (and OD-V5) ride the SUCCESSOR batch.** **O-20 has already
departed without them** (`DISPATCHED 2026-08-28`, `COHESION.md:129`; the letter carries **zero** aurora cargo),
and §4a's own dispatch row declares the register *"resumes accreting for the next boundary"*. **Naming a
departed vehicle as an evidence coordinate is an unproducible receipt**, which is what the rounds-1–2 spelling
was. The vehicle of record is **COHESION §4a's re-opened accretion register**, named by that anchor at every
site.

---

## CARRY-FORWARD RECORD BLOCK — **FOUR records**, BESIDE the table, never inside it

**This block has its own heading, its own row keys, and contributes NOTHING to G-1's 15/15 arithmetic.**
Its rows are **terminal records**, not §B discharges. **G-1's denominator stays 15 and G-5's stays 4 whatever
this block's row count becomes.**

**§6.A ordering 3 / §6.B — SATISFIED, MEASURED, NOT ASSUMED.** *"`.a`'s CARRY-FORWARD RECORD block runs AFTER
KF.W0's C-17 mint … W10 consumes; it never mints."* **The mint HAS run**, in ONE motion over the whole C-17.R
roster: value.js commit **`a331fae6`** (2026-09-17) — *"docs(kf-w0/counts): KF.W0.d — SCH-1..7 + X-2 re-run, the
cascade column, the PRM/z re-scope, and **the C-17 mint**"*, gate **G-0.6 GREEN**. The slots resolve at the
**only namespace where a slot id resolves**, `value.js docs/tranches/V/megatranche/formation/keyframes/lane-frontend.md`
**§5** ⟨heading `:260`⟩: ⟨cmd⟩ `grep -c '^#### S-'` → **12** (`S-9` … `S-20`, each citing its originating record)
and ⟨cmd⟩ `grep -c '^### S-'` → **8** (`S-1`..`S-8`, unchanged). **Therefore the `STANDING-CARRIED — pending the
KF.W0 C-17 mint` fallback does NOT apply, and no slot is guessed**: every id below is quoted from the mint's own
assignment. **This record mints, renumbers and re-keys nothing.**

| record | **terminal verb** | content |
|---|---|---|
| **`KF-W8-R-4-STRUCT-PAIR`** | **`DECLINED-FOR-X·KF, carried forward`** — **plus the THREE RE-OPEN TRIGGERS carried VERBATIM (trigger block below)** | Both structural instances — the **`src/` single-child flatten** and the **`src/animation/engine/animation.ts` stutter-instance** — declined **together, in KF.W8 R-4's one ruling**, for the duration of X·KF, reasons as stated there. KF.W5's **D-6** do-not-split lock is **SATISFIED by decline-whole**: the pair never separates and its terminus is this record. **KF.W5's G-STRUCT owns the 16-stutter rename denominator** and executes that programme whole in that wave; **KF.W8's G13 8-count is a narrower structural count, declared subordinate**, and never re-measures W5's rows. **W10 is the RECORD, not the receiving surface**: it performs no flatten, re-opens no ruling, schedules no successor act. A successor formation that wants the flatten re-opens it **against KF.W8 R-4**, not against this row. That posture stands **BESIDE** the triggers, never in place of them |
| **`CARRY-C-3`** — the census S-9/S-10 extensions | **one terminal verb PER CLAIMANT** — **`LANDED` ×12** (against the ids C-17 minted) · **`ENUMERATED-NOT-MINTED` ×2** · the fifteenth row carried as **C-17.R's own enumerated strike, which takes no verb from this record** | **Consumes KF.W0's C-17 single-motion mint; mints and renumbers nothing** (R-5: C-17 is the **only** census-slot authority — *"claims are inputs to the mint; a claim is never a slot"*). **The roster is consumed at KF-W0's post-round-4 bytes**, `KF-W0.md` **§Carry `C-17.R — the census-slot claimant roster`** ⟨`:457`⟩: *"the roster is **TEN S-9 claimants and FOUR S-10 claimants — fourteen LIVE rows, plus one row retained as an enumerated strike**"*. **The verb denominator, re-derived over the live roster and not carried**: **12 of the 14 LIVE rows take a minted-id verb; 2 of 14 take `ENUMERATED-NOT-MINTED` and never a slot; the fifteenth is the enumerated strike and takes no verb here.** *Counting rule, stated at the enumeration: the unit is a **C-17.R table row**; **14 live = 10 S-9 + 4 S-10**, of which the two enumerated rows are **both S-9** (`K-6`, `C·C-2`), so the split is **8 S-9 minted + 2 S-9 enumerated + 4 S-10 minted**. The round-2/3 spelling "11 of 13 / 2 of 13" is **struck**.* The enumeration is the record's whole product and is written out below |
| **`CARRY-C-4`** — the `.tap-floor` adopt-or-delete ruling | **`RULED DELETE, 2026-09-18, KF.W6`** — **the ruling's verb, never the deletion** | Files the **RULING RECORD** at fold-discharge, citing **`KF-W6.md` §Gates `G-W6-4`** as the **measurement of record**: `.tap-floor` → **2 hits / 1 file** (`demo/styles/design-idioms.css:81` its own comment + `:82` its selector) → **0 adopters**. **The act's bytes are KF.W6's**, not this record's — that file is in KF-W6's §Bounds and ADOPT-OR-DELETE is a **KF.W6 phase-1 ruling that precedes all spend**; the ruling was taken by **KF.W6 `.b`** (*"§2.1 `.tap-floor` — ADOPT-OR-DELETE, decided ONCE for the whole demo: **DELETE**"*, seat clock 2026-09-18) and **spent by KF.W6 `.e`** at `c6608042`. **Seven records depend on the one decision** ⟨KF-CB-25 · kf-CubeScene MISS-3 · KSC D-20 + RR-D missed-M4 · KF-KE-45 · KC-23 · KF-KC-33 · SP-11⟩. **PREMISE-CORRECTION LOCK, carried whole**: the *"demo's own 44px house practice"* premise is **DEAD** at every citing row; what survives is the **SC 2.5.8 boundary math (24×24 CSS px + the spacing exception)** and the **shipping 32px answer**. **Companion correction carried**: **KF-SST-30** — glass `@utility touch-hit-area` **cannot expand a target** (`pointer-events: none`), after which `.tap-floor` was *"the only working in-reach mechanism"* only in the sense that it existed — **with zero adopters it worked on nothing**, which is why DELETE was not the free option it looks like |
| **`CARRY-C-5`** — the `/command` decision record | **`RECORDED — evaluate-not-replace, 2026-09-18`** | Files the decision **so the census stops re-raising it**. Bank: `registry/adjudicated/kf-KeyboardShortcutsModal.md:68` — evaluate-not-replace **CONFIRMED**, on the combobox-semantics and second-stylesheet reasons. **Routing split declared (§2b)**: the **verdict** is KF.W6's, the **filed record** is W10's — *"a wave product, not a code change."* **SLOT-ID LOCK — DISCHARGED AGAINST THE MINT, NOT GUESSED**: the census id is **`lane-frontend.md §5 S-17`** ⟨`:918` — *"`S-17` · the `/command` family shadowed — **MINT**"*⟩, the slot C-17 assigned to kf-KeyboardShortcutsModal's **`C-9`**, at `a331fae6`. **The seat's own S-9→S-10 renumber is a claim-input, not a slot** (C-17's E-3 addendum demotes it; the mint records it *"superseded"*), and this record honours the demotion rather than quietly benefiting from it. **A third number is not an option** — the record cites what C-17 assigned |

### `CARRY-C-3` — THE CLAIMANT ROSTER, one terminal verb per claimant

**The twelve minted rows take `LANDED`** — evidence form `<commit>`, the commit being **`a331fae6`** (value.js,
2026-09-17), with the **full slot spelling** carried because a bare `S-n` elsewhere *"is a claim token, another
record's local roster index, or a substring — never a slot"* (C-17's own id-space rule, measured before the
first assignment):

**TEN S-9 claim-inputs** — 8 minted, 2 enumerated:

| # | claimant (record · claim id) | verb | evidence |
|---|---|---|---|
| 1 | kf-SpringHeatmap · **`C-M-1`** | **`LANDED`** | `lane-frontend.md §5 S-9` (*glass `./canvas` shadowed*) @ **`a331fae6`** |
| 2 | kf-EditorHeader · **`F1 ≡ D-1/L-B1/C-1 → kf-App KF-APP-41`** | **`LANDED`** | `lane-frontend.md §5 S-10` (*`EditorHeader → HeaderRibbon` — DEAD, replace-by-deletion*) @ **`a331fae6`**. ⟨`F1(c)` **RETIRED** at W0's repair round 3 (D-2/E-9); the round-2/3 spelling is struck and is **not** carried here⟩ |
| 3 | **`LP-8 ≡ KF-CO-35 + ME-18`** — the `/number-field` claim | **`LANDED`** | `lane-frontend.md §5 S-11` (*the number-field row, "consumed-but-mis-called"*) @ **`a331fae6`**. ⟨**RE-KEYED to the carry's head id** at W0's D-2/E-8; the kf-CubeScene `L-5/C-6` + kf-LayerConfigPanel ruling-8 legs are retained **beneath** it as provenance, never as the key⟩ |
| 4 | kf-AmigaScene · **`C-6`** | **`LANDED`** | `lane-frontend.md §5 S-12` @ **`a331fae6`** — the evaluation is **owned by KF.W6** |
| 5 | kf-AnimationVisualizer · **`KF-AV-28 · D-18 = C-13`** | **`LANDED`** | `lane-frontend.md §5 S-13` @ **`a331fae6`**, **carrying the standing supersession rider by name** (KF.W7 governs it) |
| 6 | kf-CSSPasteDialog · **`R-5`** | **`LANDED`** | `lane-frontend.md §5 S-14` @ **`a331fae6`** |
| 7 | kf-DemoGlobalChrome · **`D-5/L-3/C-2`** | **`LANDED`** | `lane-frontend.md §5 S-15` @ **`a331fae6`** |
| 8 | kf-KeyframesAddDialog · **`KAD-12`** | **`LANDED`** | `lane-frontend.md §5 S-16` @ **`a331fae6`**. ⟨**ADOPTED** at W0's repair round 3 (D-2/E-7) from this wave's own sole carry `KF-W6-CARRY.md:176`; the round-2/3 omission is cured⟩ |
| 9 | kf-SpringTrace · **`K-6 / D-§S-9 = C-S-B`** ⟨`kf-SpringTrace:89`⟩ | **`ENUMERATED-NOT-MINTED`** | **C-17.R's roster row and its enumerated disposition**, quoted: *"**ENUMERATED AS A DEAD OCCUPANT, NOT MINTED.** The row 'must be re-derived (0..1-range caveat included) before any census landing' — that re-derivation is not this wave's"*. **C-17 will never mint an id for it**, so *"pending the mint"* would be a forwarding address to a letter that never arrives |
| 10 | kf-SharePopover · **`C·C-2`**'s census amendment ⟨`kf-SharePopover:90`⟩ | **`ENUMERATED-NOT-MINTED`** | **C-17.R's roster row**, quoted: *"**ENUMERATED AS REFUSED, NOT MINTED** — the refusal is itself the anti-rename law working, and it is recorded so it is not re-litigated"* |

**FOUR S-10 claim-inputs** — all 4 minted:

| # | claimant (record · claim id) | verb | evidence |
|---|---|---|---|
| 11 | kf-KeyboardShortcutsModal · **`C-9`** (claim-input only, R-5) | **`LANDED`** | `lane-frontend.md §5 S-17` (*the `/command` family shadowed*) @ **`a331fae6`** — the same slot `CARRY-C-5` files against; its self-ruled renumber is recorded **superseded** |
| 12 | kf-KeyframesEditor · **`KF-KE-21`** | **`LANDED`** | `lane-frontend.md §5 S-18` @ **`a331fae6`** |
| 13 | kf-SquareInstrument · **`C-12`** | **`LANDED`** | `lane-frontend.md §5 S-19` @ **`a331fae6`** |
| 14 | kf-EasingTarget · **`KF-ET-27`**, with **`KF-ES-20`** ⟨kf-EasingSidebar:61⟩ as its **second provenance leg** | **`LANDED`** | `lane-frontend.md §5 S-20` (*the `design-idioms.css` census arm*) @ **`a331fae6`** — *"the design-idioms census arm is KF.W5's"*. ⟨**ADOPTED as the FOURTH S-10 claimant** at W0's repair round 4, D-3/E-2⟩ |

**Plus the fifteenth row, retained as C-17.R's ENUMERATED STRIKE** — *"~~the fifth record's original request~~ —
**NAMED AND STRUCK AS A DUPLICATE**"*, the row being kf-KeyframesEditor's own S-10 ask, i.e. a duplicate of
**`KF-KE-21`** (slot `S-18`). **This record counts it in no denominator and gives it no verb**: the strike is
carried *as the strike*, which is the whole reason W0 retained the row rather than deleting it — *"the
enumerated-strike row exists precisely so the superseded claim cannot be re-raised as new."*

**Arithmetic, re-derived at these bytes and not carried**: 10 S-9 + 4 S-10 = **14 live**; **12 `LANDED`** +
**2 `ENUMERATED-NOT-MINTED`** = 14; **+ 1 enumerated strike (no verb)** = **15 = C-17.R's row count**, and
**12 slots** were minted against them. The mint's own reading agrees: *"10 S-9 claimants (8 minted · 1 dead ·
1 refused) + 4 S-10 claimants (4 minted) = 14 live, + 1 enumerated strike = 15 … 12 slots minted, 3
dispositioned without one."* **This roster contributes nothing to G-1's 15/15** — the coincidence of the two
fifteens is arithmetic, not identity: one counts §B rows, the other counts C-17.R rows.

### THE THREE RE-OPEN TRIGGERS — carried VERBATIM

Quoted byte-exact from **`KF-W8.md` §Sequencing · the `Rulings this file is required to make` block** ⟨`:395`⟩
**· `R-4`, the *"TRIGGER, named"* clause** ⟨`:411`⟩; re-verified at that wave's bytes this seat: ⟨cmd⟩
`grep -c 'This decline re-opens on any ONE of three conditions' KF-W8.md` → **1**. ⟨*"KF-W8 §Rulings" is a
phantom §-heading; the block sits inside KF-W8's `§Sequencing`, and this record carries the corrected
spelling.*⟩

> **This decline re-opens on any ONE of three conditions, and on no other:** (1) **X·KF closes** — the decline is scoped *for the duration of X·KF*, so the successor programme inherits an open question, not a settled one; (2) **the anchor tax that grounds the decline disappears** — when no adjudicated record and no live wave spec cites an `src/animation/**:line` anchor (the churn cost is the whole reason, so its disappearance re-opens the ruling on its own terms); or (3) **`engine/animation.ts` stops being a live cure surface** — when KF.W5's B-6/B-8 obligations on it are discharged, the mid-cure half of the no-split lock lapses. **Each trigger is a condition a later seat can evaluate without re-litigating this wave**; the FOLD-FORWARD record at KF.W10 carries all three with the ruling.

**CARRYING IS NOT SCHEDULING** — *"carrying the conditions is not scheduling; a successor evaluates them
against KF.W8 R-4 via this record."* W10 performs no flatten, re-opens no ruling and schedules no successor
act; it hands the successor the instrument R-4 built for it. **Trigger (1) is notable and is stated rather
than left for a reader to notice**: **X·KF's close IS this wave**, so trigger (1) fires the moment this record
is written — which is precisely why the conditions had to travel **with** it rather than after it. The other
two are conditions on later trees and are evaluated there.

---

**G-1 reading at these settled bytes: 15 / 15 §B rows verbed, one verb each, every evidence coordinate
resolved at this seat. §A · §B · §C byte-unchanged. The record block holds 4 records beside the table and
enters no denominator.**
