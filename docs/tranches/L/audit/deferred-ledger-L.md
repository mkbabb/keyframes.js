# Tranche L — THE DEFERRED / CHRONIC LEDGER (K → L), TAGGED

**Lane:** deferred-ledger-L · **Date authored:** 2026-06-16 · **Tree at author:**
`tranche-l-dev` (L.W0 DEV phase — every wave DEVELOPED with a born-RED gate named; impl
opens on explicit authorization only; inv-16 holds throughout per `L.md:5`).

**Mandate.** THE consolidated K→L deferred/chronic substrate — the item-by-item terminal
ledger that the `PROGRESS.md §"Open deferrals"` board (13 DL-L cluster rows) summarizes.
Every CHRONIC-FOLD, CROSS-REPO-ASK, KILL, and BOOK surfaced by the **36-lane deep audit**
(`audit/audit-32-skeleton.txt` 31 lanes + 129 W-proposals + 34 ⚠-violations;
`audit/completion-lanes-32-36.txt` lanes 32/33/34/35/36) is parsed into ONE row, each
**TAGGED** with exactly one of four dispositions, its **L home** named, and its **named
tripwire** recorded. The K-inherited OUT/HANDOFF items (RF-17/DL-K9, GlassControlPoint/DL-K7,
MorphSVG/FB-3, parse-that/PT-2) and the NEW cross-repo asks (F-2 peer-cycle, SegmentedTabs
aria, the value.js-O grammar/perf set, the parse-that set) are carried with terminal homes.

**Method (inv ε).** Every row cites a `file:line`, an `audit-W#`, a `⚠#`, a sibling-lane
audit § that itself cites those, or a re-runnable command. Substrate parsed:
- `audit/audit-32-skeleton.txt` — the 31-lane skeleton: §L-WAVE PROPOSALS (W1–W129),
  §PRECEPT VIOLATIONS (⚠1–⚠34), §CROSS-REPO-ASK, §KILL, §CHRONIC-FOLD, §HIGH-severity (★).
- `audit/completion-lanes-32-36.txt` — the 5 completion lanes: 32 gesture/transport DX,
  35 the validate verb, 33 the gate blind-spots, 34 demo-UX, 36 supply-chain.
- `K/audit/deferred-ledger-k.md` — the 32-row J→K ledger (TERMINAL; all ten ≥4-tranche `‡`
  riders EXITED; `proof:chronic-closure` GREEN on the K substrate, `K/FINAL.md:56`).
- `K/FINAL.md:60-65` — the K-close OUT/sibling band (4 named-owner HANDOFF rows).
- `K/audit/prompt-recap-k.md:362-377` — the K §18 sibling-HANDOFF terminal band.
- `L.md`, the L wave specs `L/waves/L.W{0..10,Z}.md`, and `L/PROGRESS.md §"Open deferrals"`
  (the 13 DL-L cluster rows + the Band-B consume-edge table) — the L homes cited per row.

---

## §0 — THE TAG SET (the four terminal dispositions)

P-invariant-28 (`L.md:77`): **no item re-BOOKed without a tripwire; every ≥4-tranche rider
has a terminal home.** A bare BOOK on a fifth ride is forbidden. Every row below carries
exactly one tag:

| Tag | Meaning | Exit mechanism | When it greens |
|---|---|---|---|
| **Band-A-fold** | kf-internal cure (value.js 0.13.0 + glass-ui 4.0.0 sufficient; no new sibling gate) | a born-RED gate authored FIRST, GREENed by the kf cure | the L Band-A wave lands |
| **Band-B-gated-consume** | a defect at a published sibling; fixed AT the sibling, consumed via re-pin (inv-L-acyclic-purity, `L.md:67-70`) | a born-RED kf-side consume/deletion gate; STAGED-PENDING until the sibling publishes | the sibling publishes + kf re-pins (+ the workaround is deleted in the same commit) |
| **KILL** | researched negative result; non-re-litigable anti-charter | RECORD permanent; a value.js AST extension gates any future re-look | never (KILL-GUARD re-confirms at L.W-KILL-GUARD / W120) |
| **BOOK-with-tripwire** | net-new, deferred to a NAMED later home with a NAMED tripwire that FIRES it | the tripwire condition (a Baseline date, a measured workload, a prove-first dependency) | the tripwire fires; then the home wave opens |

**The chronicity column.** `★` = ≥2-tranche · `‡` = ≥4-tranche → **P-inv-28 EXIT-ONLY in L**.
The leading integer is the tranche-span count the `proof:chronic-closure` parser reads.

**The runtime-band citation contract (`PROGRESS.md:272-277`).** A Band-A-fold row's
backticked `` `proof:*` `` is a load-bearing RUNTIME oracle (must resolve to a `package.json`
key, run in `proof:correctness`, open a browser over the built dist). A HYGIENE/source-shape
gate is named in PLAIN PROSE; the row's terminal mechanism is the non-gate keyword.

---

## §1 — THE LEDGER (K → L)

Rows are numbered `DLL-N` (this ledger) and cross-link the `PROGRESS.md §"Open deferrals"`
cluster rows (`DL-Ln`) they refine. The ledger is partitioned by tag.

### 1a — Band-A-fold (kf-internal totality — the replay-equality floor, the compiler, the ingest, the gate-suite, the SOTA-DX, the perf, the publish/dogfood)

| # | Item (chronic / deferral) | Tag | Born | Chronicity | L home + named tripwire | Evidence oracle |
|---|---|---|---|---|---|---|
| **DLL-1** | **`!important` silently dropped** — `adapter.ts` `declsToVarMap` reads only `decl.name`/`decl.value`, throws away the value.js-parsed `Declaration.important`; `format.ts` never emits it. The same silent-drop class the `serializeEasing` THROW idiom (`format.ts:43-51`) forbids, unguarded. | **Band-A-fold** | L | 1 (L) | **L.W1** (the !important honor). Tripwire (gate-first): `proof:replay-equality` authored born-RED over an `opacity:0 !important` keyframe → RED on today's tree → GREEN on the honor+emit cure. | ⚠31; ★`audit:294`; W116; `adapter.ts` `declsToVarMap`; `stylesheet.d.ts:5` (`Declaration.important`) |
| **DLL-2** | **`@property` registrations never re-emit backward** — `engine.ts:1225/1318` registers `--prop` with the UA but neither `CSSKeyframesToString` nor `compileToCSS` calls value.js's `serializeStylesheetItem`; a compiled artifact animating a registered custom-prop loses its typing and breaks on re-ship. | **Band-A-fold** | L | 1 (L) | **L.W1** (wire value.js's existing `serializeStylesheetItem` into `compileToCSS`/`CSSKeyframesToString`). Tripwire: `proof:replay-equality` `@property` row born-RED. **Deepening gated on Band B** — typed `<syntax>` interpolation needs VJ-O5 (see DLL-26). | ⚠15; ★`audit:277`; W75; `engine.ts:1225` |
| **DLL-3** | **Per-stop `animation-composition` asymmetric** — adapter captures it (`ResolvedKeyframes.composition`), frame-compiler stores it (`templateFrame.composition`), but `declaredKeyframeBody` (`format.ts:81-103`) emits per-stop easing and NOT per-stop composition; `fromString(per-stop add)` → serialize → reparse loses the operator. | **Band-A-fold** | F (the FB-1 family; DL-K16 LAYER-level honored at K.W7) | 2 (F-layer, L-per-stop) | **L.W1** (close the per-stop emit asymmetry). Tripwire: `proof:replay-equality` per-stop-`add` row born-RED; the existing `roundtrip-easing.test.ts` locks per-stop EASING — the gate's composition arm is the sibling. | ⚠16; W76; `format.ts:81-103`; Lane 33 `[med]` "compile-replay clause(b) tests LAYER-level … the breach is per-STOP" |
| **DLL-4** | **Named keyframe selectors ingested-then-THROWN** — value.js parses `entry/exit/cover/contain` (`stylesheet.ts namedKeyframeSelector`), the adapter surfaces them literally (`adapter.ts:115`), but `frame-compiler.addFrame`'s guard (`SELECTOR_KEYWORD_RE`/`SELECTOR_PERCENT_RE`, `frame-compiler.ts:179-188`) accepts only `from/to/percent` and THROWS `AnimationOptionError`. What value.js produces, kf cannot ingest. | **Band-A-fold** | K (honest commented deferral ⚠32 at K; THROW is the L breach) | 1 (K→L) | **L.W1** (the SELECTOR-FLOOR: animate the named selectors; composes the shipped ScrollScene phase mapper). Tripwire: `proof:replay-equality` named-selector row born-RED. | ⚠17; ⚠32; W77/W118; `frame-compiler.ts:179-188`; `adapter.ts:111-116` |
| **DLL-5** | **OPERATOR-floor** — `composite`/`iteration-composite`/`animation-play-state` absent from `AnimationOptions`; mixed-operator animations on the WAAPI path are neither rejected nor per-keyframe-composited (the rAF↔WAAPI parity hole); the CSS-path `UNKNOWN_TIMING_FN` silent fallback has no diagnostics row. | **Band-A-fold** | L | 1 (L) | **L.W1** (the OPERATOR-floor on `AnimationOptions`; W5 reject-or-per-keyframe-composite on WAAPI; W6 mirror the fallback as a diagnostics row). Tripwire: `proof:replay-equality` operator arms born-RED. | W5/W6/W117; engine-internal (value.js-independent) |
| **DLL-6** | **The compiler is scroll-BLIND** — W9 (ScrollScene engine) and W10 (compile) do NOT compose; `compile.ts` has zero timeline emit. The round-trip headline gap. | **Band-A-fold** | K (W9/W10 shipped separately; the compose gap is the L headline) | 1 (K→L) | **L.W2** (CC-6 compile the scroll grammar; W119 emit `animation-timeline`/`animation-range`; value.js 0.13.0 `serializeTimelineOptions` already published+consumed). Tripwire: `proof:compile-replay` + NEW scroll-driven fixture (W114) — the gate bites the case it currently skips. | ★`audit:268`; W12/W36/W119; `compile.ts` (zero timeline emit) |
| **DLL-7** | **Multi-color densify ships a non-faithful sRGB block with `eligible:true`** — `compile-color.ts:188-190` emits a verbatim sRGB `@keyframes` block (measured ΔE=0.82 vs kf's own oklab playback) while a SINGLE-color drift at the same ΔE HARD-REFUSES (`compile.ts:329-340`). The honest-refusal clause (`compile.ts:35-43`) is violated by the lossy ship; the BOOK fallback is silently-lossy, not a safe deferral. | **Band-A-fold** | K (the asymmetric honest-refusal breach) | 1 (K→L) | **L.W2** (CC-3.5 densify EACH changing color key independently via `sampleColorRamp`/`deltaEOK` — already published 0.13.0 — OR REFUSE with a named reason; never the silent sRGB ship). Tripwire: NEW multi-color fixture (W114) makes `proof:compile-replay` BITE the lossy case (the corpus has NO multi-color fixture today — Lane 33 `[high]`). | ⚠28/⚠29; ★`audit:269,291,292,293`; W13/W113; `compile-color.ts:188-190` |
| **DLL-8** | **`reverseMs` re-authors value.js's `reverseCSSTime`** — `compile.ts:241` ships a bespoke time serializer (`reverseMs(1000)='1s'`) while the sibling `format.ts` already imports `reverseCSSTime` (`reverseCSSTime(1000)='1000ms'`); two divergent time serializers in one module. | **Band-A-fold** | K | 1 (K→L) | **L.W2** (unify on value.js's `reverseCSSTime`; delete `reverseMs`). Tripwire: `proof:compile-replay` time-serialize arm asserts ONE authority; non-gate mechanism: the `reverseMs` symbol is DELETED. | ⚠30; W115; `compile.ts:241` |
| **DLL-9** | **`seedAtTime` delay-paused FREEZE** — `adoptRunning` freezes when the source animation has `delay > 0`; `seedAtTime` does not reset delay to 0 for mid-flight continuity. | **Band-A-fold** | K.W8 (ingest) | 1 (K→L) | **L.W3** (seedAtTime resets delay to 0). Tripwire: `proof:ingest-replay` extended with a delay arm born-RED. | ★`audit:267`; W8; `ingest.ts:258-272` |
| **DLL-10** | **Live-stylesheet ingest depth gaps** — no recursive group-rule walk for nested `@keyframes` (W7); no `ADOPT_REFUSE` diagnostic for not-found/no-`getAnimations` (W9); no `adoptedStyleSheets`/Shadow-DOM constructable-stylesheet walk (W10); no `CSSNumberish currentTime` resolution in `adoptRunning` for scroll-driven (W11); the `OnParseError` sink is authored but INERT (`adapter.ts:173-176` — value.js parse failures don't flow to it). | **Band-A-fold** (the nested-walk SUBSTRATE is Band-B-gated — see note) | K.W8 | 1 (K→L) | **L.W3** (the ingest-deepening arms). Tripwire: `proof:ingest-replay` extended — NEW nested/shadow/refuse/scroll-time arms born-RED. **Note:** the recursive group-rule walk (W7) requires value.js O's typed recursive at-rule body (DLL-27/L.W10.S3) as substrate — that half is Band-B-gated; the kf walk consumes it. | W7/W9/W10/W11; `audit:227` (inert OnParseError); `L.W10.md:213-219` (the L.W10.S3 prerequisite) |
| **DLL-11** | **The gate-corpus blind-spot — 259 fixed-ms sleeps + serial-AND fail-fast CI + no Linux-container local-repro + source-shape round-trip gates.** The macOS-pass/Linux-fail render-race root; the settle-on-state cure exists but was applied to ONE function only (`demo-driver.mjs:654-713 navToScene`). | **Band-A-fold** | K (the CI-greenify session) | 1 (K→L) | **L.W4** (the `waitForRender/settle` lib primitive replacing all 259 sleeps — W28; report-all non-fail-fast posture — W27; Linux-container local-repro — W29; single-source device thresholds + observe-only CATEGORY taxonomy — W22/W30). Tripwire: the demo-smoke job runs report-all; ZERO fixed-ms sleeps in the render-wait seam; `grep waitForTimeout scripts/*.mjs` = 0. | ★`audit:270,271,272,273`; Lane 33 ⚠ device-honesty; W27/W28/W29/W30; inv-L-device-honesty `L.md:71-75`; ↔ `DL-L2` |
| **DLL-12** | **The missing born-RED CI gates** — `proof:no-single-option-select` (W2; the product fix is in place, the gate-only gap); `proof:live-session-mobile` M2 touch-event-only commit path for reka `SelectItem` (W19); `proof:subject-animates` demo arm (ARM-D, W4); the reason-string equality / "byte-for-byte" claim in `gate-taxonomy.md` (W21); the Monaco `Canceled` swallow relocation (W26). | **Band-A-fold** | K (gate-coverage gaps) | 1 (K→L) | **L.W4** (the missing-gate roster). Tripwire: each gate authored born-RED over its real input (a lone-option `<Select>`; a touch-only commit; the cold subject-animates oracle). | W2/W4/W19/W21/W26; Lane 33 `[med]`; `DL-L2` |
| **DLL-13** | **Draggable has no bounds / snap / rubber-band** — the GSAP-Draggable / Motion `dragConstraints` parity hole on the shipped `decay`/`spring` substrate; and the 2-D drag single-call sugar is absent. | **Band-A-fold** | L (competitive-SOTA, Lane 32) | 1 (L) | **L.W5** (W-GESTURE-BOUNDS over the shipped decay/spring; LIGHT, value.js-free). Tripwire: `proof:drag-gesture` extended (bounds/snap) born-RED. | Lane 32 `[med]` GAP(drag); LIGHT boundary |
| **DLL-14** | **Sequence/animate have no per-segment lifecycle callbacks** — no label-events, no segment-lifecycle channel; a LIGHT-path subscribe hook mirroring ScrollScene's `.on` is absent (orchestration-observability gap). | **Band-A-fold** | L (Lane 32) | 1 (L) | **L.W5** (W-TRANSPORT-EVENTS: segment-lifecycle + label-callback + LIGHT subscribe hook). Tripwire: `proof:transport-events` born-RED (segment callbacks fire). | Lane 32 `[med]` GAP(observability); LIGHT, reuses ScrollScene `.on` |
| **DLL-15** | **No agent-authoring VERB** — every PART of `validate(css)` exists (`resolveKeyframes` + typed `Diagnostic[]`; `compileToCSS` `{css, eligible, refusals}` with 4 named `CompileRefusal`; `isWAAPIEligible` + `waapiIneligibleReason`) but no read-only projection an LLM agent calls to ask "will this @keyframes ship faithfully?"; `llms.txt` lists capabilities but teaches no validate→fix→compile LOOP. | **Band-A-fold** (HEAVY; gated on L.W1+L.W2 so the refusal surface it projects is TOTAL) | L (Lane 35) | 1 (L) | **L.W6** (`validate(css)`/`explain(css)` over the EXISTING channels → one `{parseable, eligible, refusals, diagnostics, waapi}`; the `llms.txt` LOOP teaching). Tripwire: `proof:agent-validate` born-RED + `proof:agent-surface` admits the verb. **Sequenced after L.W1+L.W2.** | Lane 35 `[high]`; Lane 32 L.W-AGENT-AUTHOR; `compile.ts:74-134`; `adapter.ts:25-93` |
| **DLL-16** | **lerpArray published but un-consumed on the LIGHT tier** — `numeric.ts:138-143` (`NumericAnimation.at`) uses plain-array lerp, not value.js's `lerpArray(from,to,t,out)` (`value.js/src/math.ts:60`, published 0.13.0); `SpringProgress` lacks vector sugar over the same substrate. The one unclaimed perf edge (⚠34 names it as a missed OPPORTUNITY, not a violation). | **Band-A-fold** | K (the un-consumed published edge) | 1 (K→L) | **L.W7** (L-PERF-2 lerpArray consume on the LIGHT tier; `Float64Array` pack). Tripwire: `proof:zero-alloc` extended to the LIGHT tier born-RED (today covers ONLY the AnimationGroup compositor — Lane 33 `[med]`). | ⚠34; ★`audit:70`; W122; `numeric.ts:138-143` |
| **DLL-17** | **warmEngine() unimplemented + the granular dynamic boundary** — `loadAnimationEngine()` (`index.ts:330-363`) eagerly `Promise.all`s ALL 8 heavy chunks (one all-or-nothing door); no `warmEngine()` idle-warmer; no per-capability lazy accessors; `scheduler.postTask` priority bands un-probed; EPF-1 read/write phase separation un-measured. | **Band-A-fold** (W123 postTask + W40 EPF-1 are BOOK→born-RED probes within the wave — see DLL-30) | K.W12 (boundary recorded as kf-owned follow-up, `K/FINAL.md:66`) | 1 (K→L) | **L.W7** (warmEngine W121; granular `loadAnimationEngine` per-capability accessors W124; the budgeted bench taxonomy). Tripwire: `proof:boundary` enumerates `warmEngine`/`load*` as LIGHT with zero value.js edge; `proof:bench-taxonomy` budgeted (today `proof:bench-runs` is a RUN-CHECK not a budget — Lane 33 `[med]`). | ★`audit:295,296`; W121/W123/W124; `index.ts:330-363`; `K/FINAL.md:66` |
| **DLL-18** | **ED-3 dogfood inversion NOT landed** — 63 demo files import `@src/animation/*`; ZERO `@mkbabb/keyframes` barrel imports; the dogfood loop cannot close; `proof:demo-on-published-surface` exists but the demo is on `@src`. The demo's dogfood is SOURCE-level not PUBLISHED-BARREL-level (Lane 34 `[med/ALREADY-DONE]`). | **Band-A-fold** | K.W12 (STAGED not landed) | 2 (K staged, L open) | **L.W8** (the full barrel flip — W14/W125; extend `animate()` to dispatch the orchestration tier — W127; retire PKG-3 d.ts collision-aliases `Animation_2 as Animation` — W126; README prose for the 6 undocumented 4.3.0 exports — Lane 34). Tripwire: `proof:demo-on-published-surface` GREEN (`grep @mkbabb/keyframes demo/` non-zero, `grep @src/animation demo/` zero). | ★`audit:274,297`; Lane 34; W14/W125/W126/W127; `DL-L4` |
| **DLL-46** | **CC-6 `@supports` scroll() stagger EMIT** — the progressive-enhancement variant: a compiled scroll-driven `@keyframes` artifact should emit a `@supports (animation-timeline: scroll())` guard wrapping the scroll path with a non-scroll stagger fallback. DISTINCT from DLL-6's bare `animation-timeline` timeline-emit (that wave makes the compiler scroll-AWARE; this adds the `@supports` capability-query wrapper + the staggered fallback body). | **Band-A-fold** | K (W37; K.W9/K.W10 shipped the scroll engine + compile, the `@supports` variant is the L emit-deepening) | 1 (K→L) | **L.W2** (fold beside DLL-6's scroll-grammar compile; emit the `@supports` guard + fallback). Tripwire: `proof:compile-replay` `@supports`-variant arm born-RED (the compiler emits no `@supports` guard today) → GREEN on the emit cure. | W37; `audit:109`; value.js 0.13.0 (`serializeTimelineOptions` published); ↔ DLL-6 (distinct: timeline-emit vs `@supports`-wrap) |
| **DLL-47** | **Fold the three flatten/projection authorities onto ONE bidirectional codec** — `flatten`/`unflatten`/projection logic is spread across three value.js-HEAVY sites; a single named `flatten<->unflatten` codec seam removes the divergence risk (the three sites can drift). All three sites are already value.js-HEAVY, so no new publish gate. | **Band-A-fold** | L (W111; the architectural-cohesion fold) | 1 (L) | **L.W7** (the SOTA-perf + cohesion wave; unify the three sites onto one bidirectional codec module). Tripwire: a `proof:flatten-codec` born-RED asserting ONE flatten authority (the three sites are NOT yet unified today) → GREEN on the codec consolidation; non-gate mechanism: the two redundant projection sites are DELETED. | W111; `audit:183`; all-three-sites value.js-HEAVY (no new sibling gate) |
| **DLL-48** | **Memoize `parseAndFlattenObject` against template-frame identity** — an option-only re-parse (same template frames, changed `AnimationOptions`) re-flattens the full keyframe body needlessly; memoize on template-frame identity so the re-flatten is a no-op (mirrors the `renormalizeColors` targeted-rederive principle). | **Band-A-fold** | L (W112; the measure-first perf fold) | 1 (L) | **L.W7** (beside DLL-47; measure-first per the EPF-1 discipline DLL-30). Tripwire: a `proof:zero-alloc`/bench arm born-RED asserting an option-only re-parse re-flattens nothing (today it re-flattens fully) → GREEN on the memoized identity-skip. | W112; `audit:184`; mirrors `renormalizeColors` targeted-rederive |

### 1b — Band-B-gated-consume (the cross-repo dispatch — fixed AT the sibling, consumed via re-pin; inv-L-acyclic-purity)

The four K-terminal HANDOFF rows + the new L asks. Each carries a born-RED kf-side gate
(STAGED-PENDING until the sibling publishes, TRULY-RED if the sibling published but kf did
not consume — the three-state model `L.W9.md:511-515`). **None re-BOOKed** — the named
tripwire IS the exit (P-inv-28; a HANDOFF with a named publish-and-consume-edge is exit-shaped).

| # | Item | Tag | Born | Chronicity | Owner / L home + named tripwire | Evidence oracle |
|---|---|---|---|---|---|---|
| **DLL-19** | **RF-17 / DL-K9 GlassDock click-strand** — the kf interim (`onPlayPointerDown`/`pointerHandled`) is a workaround of a glass-ui primitive defect; REVERTED+BOOKED at K.W1 (3.13.0's `useDockClickIntegrity` did NOT subsume the kf twin); glass-ui 4.0.0 consumed one-behind. | **Band-B-gated-consume** | I (BLK-8 recurred J.WZ) | ★ 3 (I,J,K→L) | OWNER glass-ui BB. **L.W9 S2.** Tripwire: glass-ui 4.1.0 ships `W-DOCK-MORPH-FAMILY` + the RF-17 dock-layer collapse-crossfade strand fix → `proof:workaround-deletion` S2 (`grep -rn 'pointerHandled\|onPlayPointerDown' demo/` = 0) GREEN; the interim DELETED in the same commit as the re-pin. No interim carries to a 4th tranche under no-workaround. | ⚠5; ♾`audit:260`; `K/FINAL.md:63`; `K/audit/deferred-ledger-k.md DL-K9`; `DL-L6`; `PROGRESS §Band-B` |
| **DLL-20** | **GlassControlPoint / DL-K7** — the SVG control-point handle + pointer composable enabling the keyframes curve editor; 5-tranche net-new (E…J as ABSTRACT); exited K as HANDOFF; absent on the 3.13.0 AND 4.0.0 surfaces. | **Band-B-gated-consume** | E | ★‡ 6 (E,F,G,H,I,J,K→L) | OWNER glass-ui BB or a reasoned build-in-kf decision. **L.W9 S4.** Tripwire: glass-ui ships `GlassControlPoint` → `proof:control-point-live` (authored gate-first BEFORE impl; `grep GlassControlPoint node_modules/@mkbabb/glass-ui/dist/` = 0 today) GREEN on consume. **P-inv-28 (6-tranche):** if NOT shipped by L.WZ, the row does NOT re-BOOK — it exits as EXITED (shipped) or a named terminal KILL ("build-in-kf decision taken" with a concrete spec). The re-BOOK option is CLOSED. | ♾`audit:261`; `K/FINAL.md:64`; `K/audit/deferred-ledger-k.md DL-K7`; W34/W45; `L.W9.md:177-205`; `DL-L7`; `PROGRESS §Band-B` |
| **DLL-21** | **MorphSVG / FB-3 remainder** — the real competitor-feature gap (`fromMorphSVG`/`getPointAtLength`); the arc-length PARTIAL (VJ.W4) is in 0.13.0, the full sampler is the VJ.W4 remainder; absent in 0.13.0. | **Band-B-gated-consume** | C | ★ 6 (C,F,G,H,I,J,K→L) | OWNER value.js O. **L.W9 (KF-TO-VALUEJS-O dispatch).** Tripwire: value.js O (0.14.0) ships VJ.W4 remainder (`getPointAtLength`/`fromMorphSVG` arc-length sampler) → `proof:morphsvg-consume` born-RED (APIs absent in 0.13.0) GREEN when kf ships `fromMorphSVG` over them. | ♾ (FB-3); `K/FINAL.md:65`; `K/audit/deferred-ledger-k.md DL-K21`; `K/prompt-recap-k.md:362`; W44/W51; `DL-L8`; `PROGRESS §Band-B` |
| **DLL-22** | **parse-that packrat / PT-2 soundness** — Warth-Douglass-Millstein `(id,offset)` re-key; `packrat.ts` self-documents as UNSOUND (id-only key, "unsound across offsets") with ZERO production consumers; the gate-first BOOK. | **Band-B-gated-consume** | E (BOOK at G/LD-PT; 4-tranche) | ★‡ 5 (E,F,G,H,I,K→L) | OWNER parse-that. **L.W9 S11 (KF-TO-PARSE-THAT dispatch).** Tripwire: parse-that PT-WAVE-4b/6 ships the `(id,offset)` re-keyed packrat → `proof:packrat-sound` (authored gate-first in parse-that's suite) GREEN; value.js consumes parse-that, kf's gate is on the kf re-pin consuming the value.js that consumes the fixed parse-that. | ⚠27; `K/FINAL.md:65`; `K/prompt-recap-k.md:363`; W47/W93; `DL-L9`; `PROGRESS §Band-B` |
| **DLL-23** | **The kf-owned constellation workarounds (the inv-L-acyclic-purity sweep)** — (a) `FN_NAME` Symbol stamp on `ValueUnit` (`utils.ts:45-57,294-299`; dropped by `.clone()`, re-stamped per call); (b) `linear()` regex normalize (`utils.ts:185-196`; the `, 25%`→` 25%` fold over value.js's own serializer output); (c) direct `@mkbabb/parse-that` production dep (`utils.ts:1` `import { any as parseAny }`; `package.json` `^0.9.0`); (d) `:aria-orientation="undefined"` suppression (`SpringSidebar.vue:43` — INCOMPLETE: `AnimationControls.vue:66` carries the same pill strip un-suppressed, the defect fires fleet-wide). | **Band-B-gated-consume** | K (all carried named-but-un-retired through the K close) | 1 (K→L, born as named violations) | **L.W9 S1/S7/S8/S9** + `proof:workaround-deletion` (NEW, born-RED on every arm, three-state). Tripwires: (a) value.js VJ-L1 `FlatLeaf`/typed first-class API → S8 (`grep FN_NAME src/animation/utils.ts` = 0); (b) value.js VJ-L2 `linearStopsToCSS` → S7 (`grep LINEAR_PAREN_PREFIX` = 0); (c) value.js VJ-L3 `parseCSSSubValue` → S9 (`grep @mkbabb/parse-that src/animation/utils.ts` = 0 + the dep removed from `package.json` + `proof:boundary` W96 extension catches direct parse-that imports); (d) glass-ui BB SegmentedTabs pill-variant aria fix → S1 (the COMPLETE fleet sweep: BOTH `SpringSidebar.vue:43` AND `AnimationControls.vue:66`). | ⚠1-3/⚠7/⚠18/⚠19/⚠20/⚠24; ★`audit:282,283`; `audit:204-206,210,233`; W86/W87/W94/W96/W24/W50; `DL-L10`; `L.W9.md S1/S7/S8/S9` |
| **DLL-24** | **F-2 glass-ui peer-cycle (LIVE)** — glass-ui 4.0.0's `peerDependencies` `@mkbabb/value.js: ^0.10.0 || ^0.11.0` does NOT admit value.js 0.12.0/0.13.0 that both kf and glass-ui consume; ELSPROBLEMS on every install today; the no-workaround precept forbids papering with `overrides` / `peerDependenciesMeta optional`. | **Band-B-gated-consume** | K (the K.W1 consume-edge) | ★ 2 (K born, L live) | OWNER glass-ui BB. **L.W4** (NEW `proof:peer-satisfied` reading the full installed peer graph — born-RED today on ELSPROBLEMS) + **L.W9 S3** (the BB widen ask). Tripwire: glass-ui 4.1.x widens the value.js peer range to admit 0.13.0+ → `proof:peer-satisfied` (`npm ls --json` zero `peerMissing`/`invalid`) GREEN + kf re-pins. | ⚠8; `audit:211,212`; Lane 36 `[high]`; `K/CONSTELLATION-DAG.md §5`; W-PEER-SPINE; `DL-L3`; `L.W9.md:145-174` |
| **DLL-25** | **SegmentedTabs emits `aria-orientation` on `role=group` (pill)** — a genuine glass-ui defect (ARIA spec forbids it on `role=group`); the one-line conditional bind in glass-ui makes ALL consumers correct at once (vs the kf per-call-site suppress in DLL-23(d)). | **Band-B-gated-consume** | K | 1 (K→L) | OWNER glass-ui BB. **L.W9 S1.** Tripwire: glass-ui 4.1.x ships the conditional bind (emit `aria-orientation` only when variant ≠ pill) → `proof:workaround-deletion` S1 GREEN (zero `:aria-orientation="undefined"` in demo + the live rendered pill emits NO `aria-orientation`). The ROOT half of DLL-23(d). | `audit:204,205,206,210`; Lane 36; W24/W50; `L.W9.md:80-113` |
| **DLL-26** | **value.js O grammar/correctness set** — (a) comma-list forward-leg TRUNCATION: `CSSValues.Values` truncates multi-value declarations at the first top-level comma with `status:true` (`box-shadow: 0 0 0 red, 0 0 0 blue` → second shadow GONE; the loss is UPSTREAM of every kf gate — ⚠13); (b) partial-input dishonesty: `parseDeclarationValue` treats a partial parse as success (⚠10/⚠14); (c) transform axis WRONG: `rotate()`→rotateX+rotateY (rotate≡rotateZ; scale sets X+Y not Z — ⚠12); (d) color() not replay-equal: `color(` wrapper dropped on serialize (⚠10); (e) `FunctionValue.toString()` space-args→commas (linear()/scroll() — ⚠23); (f) `@property` `<syntax>` opaque/untyped (W73 — unblocks DLL-2's typed re-emit). | **Band-B-gated-consume** | K (detected at the K-close grammar audit) | 1 (K→L) | OWNER value.js O. **L.W9 S6/S7/S10 (KF-TO-VALUEJS-O dispatch).** Tripwires: VJ-O1 `ValueList` comma-list grammar → kf multi-value fixture in `proof:compile-replay` GREEN; VJ-O2 partial-input honesty (`status:false`/`OnParseError` on incomplete consume); VJ-O3 transform axis (`rotate→rotateZ`); VJ-O4 color() serialize; VJ-O5 typed `<syntax>` → DLL-2 deepens; consumed transparently via re-pin (correctness fixes need no kf code change once value.js corrects the output) + `proof:replay-equality` color/transform arms. | ⚠10/⚠12/⚠13/⚠14/⚠23; `audit:217-226`; ★`audit:278-281`; W71/W72/W73/W74/W94/W95; `L.W9.md S6/S7/S10` |
| **DLL-27** | **value.js O + parse-that COORDINATED grammar (true-CSS-parity)** — CSS Nesting (Baseline-2023, silently dropped — ⚠25); url-token mis-tokenized (W98); modern at-rules `@container`/`@layer`/`@scope`/`@page` degrade to opaque `genericAtRule` with recursive body LOST (W98/W101/W102); env()/attr()/system-colors fall through to generic `FunctionValue` (W107/W108); structured radial/conic gradients opaque (W109); NO serializer / `Span`-on-node in parse-that's CSS module (⚠26 — replay-equality structurally absent); TWO divergent CSS grammars in the spine (⚠25/W97). | **Band-B-gated-consume** (research-spike FIRST, then coordinated publish; no kf code until siblings publish) | K (detected at the K-close audit) | 1 (K→L) | OWNER value.js O + parse-that (coordinated). **L.W10** (S0 research-spike settles the ONE-grammar decision; S1-S7 grammar-gap closure; S8 incremental-parse research). Tripwire: `proof:css-parity` capability matrix (NEW) born-RED on ≥6 class inputs (nesting/url-token/@container/structured-gradient/env/system-color all RED today) → GREEN only on the coordinated value.js O (0.14.0) + parse-that publish + kf re-pin. **L.W10.S3 is the SUBSTRATE for DLL-10's recursive group-rule walk** (sequenced: value.js O first, then kf's L.W3 walk consumes it). | ⚠25/⚠26; ★`audit:284-290`; `audit:218,219,223`; W97/W98/W99/W100/W101-W110; `DL-L11`; `L.W10.md` |
| **DLL-49** | **value.js O `parseTransitionShorthand`/`reverseTransitionShorthand`** — value.js ships the `animation` shorthand pair (`parseAnimationShorthand`/`reverseAnimationShorthand`, consumed at `format.ts:268`) but NO `transition` mirror; kf can round-trip `animation` but has no inverse to call for `transition` (the FORWARD parity hole). DISTINCT from the DLL-26 grammar set (this is a missing serializer PAIR, not a lossy parse). | **Band-B-gated-consume** | L (W104; the animation-shorthand mirror, detected at the L grammar-parity lane) | 1 (L) | OWNER value.js O. **L.W9 (`KF-TO-VALUEJS-O-ASKS.md §12`).** Tripwire: value.js O ships `parseTransitionShorthand`/`reverseTransitionShorthand` (mirroring `parsing/animation-shorthand.ts`) → `proof:replay-equality` `transition`-shorthand arm born-RED (no inverse exists today) GREEN on the consume; `format.ts`/`compile.ts` consume the inverse beside `reverseAnimationShorthand`. | W104; `audit:177` (W104 line); `KF-TO-VALUEJS-O-ASKS.md §12`; value.js `dist/parsing/animation-shorthand.d.ts` (the animation twin) |
| **DLL-50** | **parse-that non-ASCII `dispatch()` fallback** — the `O(1)` `dispatch()` jump table is ASCII-byte-indexed; a non-ASCII first-char (Unicode ident-start, non-ASCII `attr()`/`env()`/custom-ident) falls off the fast path. Permutation-ADJACENT (same `dispatch()` core as W105) but DISTINCT: W105 adds an any-order combinator; W106 widens the dispatch index via a `Map` fallback. | **Band-B-gated-consume** | L (W106; the SOTA-grammar-totality gap, `audit-32-skeleton.txt:178`) | 1 (L) | OWNER parse-that. **L.W9 S6 (`KF-TO-PARSE-THAT-ASKS.md §6`).** Tripwire: parse-that PT-WAVE-5/6 extends `dispatch()` with a non-ASCII `Map` fallback (ASCII fast path untouched) → value.js ident/`attr()`/`env()` grammar admits non-ASCII → `proof:replay-equality` non-ASCII-ident arm born-RED (ident falls off `dispatch()` today) GREEN on the parse-that publish + value.js re-pin. | W106; `audit-32-skeleton.txt:178` (PT-UNICODE-DISPATCH); `audit:28` (the SOTA `dispatch()` core); `KF-TO-PARSE-THAT-ASKS.md §6` |

### 1c — BOOK-with-tripwire (net-new; deferred to a NAMED home with a NAMED tripwire that FIRES it)

| # | Item | Tag | Born | Chronicity | Named home + named tripwire | Evidence oracle |
|---|---|---|---|---|---|---|
| **DLL-28** | **KF-OSCILLATOR** — a NEW LIGHT `Oscillator` periodic phase-clock (frequency + waveform → phase ∈ [0,1)); BOOKED at the K close for the glass-ui BB speedtest idle-breath (W-EASING-PRIMITIVE) consume; value.js-free (a phase clock, not a CSS value parser). | **BOOK-with-tripwire** | K (BOOKED `KF-TO-GLASSUI-BB-ASKS.md §1`) | 1 (K→L) | **L.W9 S5 / L.W5** (ship `Oscillator` to `src/animation/` as a LIGHT named export beside `SmoothProgress`/`SpringProgress`). **Tripwire:** the BB `W-EASING-PRIMITIVE` wave schedule confirmed (the consume signal). Born-RED: `proof:boundary` assertion-3 enumerates `Oscillator` as LIGHT with zero value.js edge (no `Oscillator` class exists today). Not blocking the BB 4.1.0 cut. | W16/W57/W128; `K/FINAL.md:90`; `KF-TO-GLASSUI-BB-ASKS.md §1`; `L.W9.md:208-239` |
| **DLL-29** | **keyframes-react adapter** — `@mkbabb/keyframes-react` thin adapter with the on-brand `<Keyframes css>` primitive; no source, no scaffold, no `packages/keyframes-react/` today. | **BOOK-with-tripwire** | K.W12 (BOOK, `K.W12.md:86`) | 1 (K→L) | **L.W8 S5 / `L/waves/L.W8-react-book.md`** (the BOOK DISPOSITION — documentation only, no source). **Tripwire (prove-first):** keyframes-vue 0.1.0 PUBLISHED + demo-dogfooded (the pattern-proof). Once S2 closes, the BOOK records the tripwire FIRED; `proof:keyframes-react-published` is the future gate. `peerDependencies: { react: ">=18", "@mkbabb/keyframes.js": ">=5.0.0" }`. | W39/W129; `K.W12.md:86`; `L.W8.md:333-351` |
| **DLL-30** | **`scheduler.postTask` priority bands + EPF-1 read/write phase separation** — `scheduler.ts:41-48` probes `scheduler.yield` only, not `scheduler.postTask` (`"background"` for `warmEngine`'s pre-flight); EPF-1 read/write phase separation un-measured (the cq-unit forced-layout thrash is epoch-boundary-only; no real multi-computed workload existed at J). | **BOOK-with-tripwire** (BOOK→born-RED probe within L.W7 — measure-first) | J (EPF-1 K-SEED, `K/audit/deferred-ledger-k.md:97`) | ★ 2 (J,K→L) | **L.W7** (W123 postTask born-RED probe; W40 EPF-1 measure-first). **Tripwire:** `scheduler.postTask` Baseline-Newly (2024-01-15) available in the test env AND a measured win over `scheduler.yield` → only then does `warmEngine` adopt `postTask("background")`; EPF-1 fires when a real multi-computed workload exists (K.W8 ingest-cssom.ts made it real). No code ships without the probe gate GREEN. | ♾`audit:264`; W40/W123; `scheduler.ts:41-48`; `K/deferred-ledger-k.md:97`; `L.W7.md Finding 3/5` |
| **DLL-31** | **Incremental / streaming parse** — re-parse-on-edit, document-fragment reuse, as the SOTA-perf differentiator for kf live-ingest; parse-that AST carries NO `Span`/loc (structurally blocks the rope/positioned-input models at 0.9.0); the chunked-reparse-of-changed-`@keyframes`-block model is the only feasible path today. | **BOOK-with-tripwire** (research-and-challenge, no code) | L (Lane 10/W100) | 1 (L) | **L.W10 S8** (a one-page research note: the threshold calculation + the Span-availability verdict + a go/no-go). **Tripwire:** a REAL workload where a document-size threshold (e.g. 500-keyframe library-stylesheet) makes incremental parse necessary — if found, it becomes a value.js O ask; else a BOOK for a later tranche. No regression gate (the artifact is a recommendation). | W100; `L.W10.md:294-326`; `parsers/css/types.d.ts` (no Span — confirmed) |
| **DLL-32** | **T1 formal resolution** — the "collapse the lattice" language persists (`precepts-k.md:253` uncancelled); `K.WZ.md:524` claims T1 RESOLVED without naming the chosen option; FINAL.md silent on T1; the corpus grew 14 gates net-addition (⚠9, an inv ε overclaim). | **BOOK-with-tripwire** (documentation; dev-phase) | K.WZ (the K FINAL overclaim) | 1 (K→L) | **L.W0** (own the corpus; DELETE the "collapse the lattice" language from `precepts-k.md`; `proof:gate-is-runtime` is formally the T1 resolution — the gate set is DERIVED, not declared by a `WAVE_HARD_GATES` list). **Tripwire:** `proof:gate-is-runtime` meta-gate authored → T1 non-re-litigable thereafter. Non-gate mechanism: the language is DELETED. | ⚠9; W62; `K.WZ.md:524`; `precepts-k.md:253`; `DL-L13` |
| **DLL-51** | **VectorSpring convenience sugar** — a thin `SpringProgress` vector wrapper (an `at(t)→number[]` over a packed component spring) for the multi-axis drag/transport case; the audit tags it "S micro-wave, L-eligible". DISTINCT from the PHYS-A KILL (DLL-43): PHYS-A is COUPLED vector springs (rejected as mathematically equivalent to N independent springs); this is pure CONVENIENCE sugar over N independent `SpringProgress` instances — no coupling, no new physics. | **BOOK-with-tripwire** | L (W42; the SpringProgress foundation shipped at K.W11 c0482cb) | 1 (L) | **L.W5** (beside the gesture/transport DX; LIGHT, value.js-free). **Tripwire (prove-first):** a REAL multi-axis consumer (the 2-D drag single-call sugar DLL-13, or a transport consumer) that wants vector-spring sugar over the shipped `SpringProgress` — if a consumer materializes, ship it as a LIGHT named export; ELSE it stays BOOKed. **N-independent guard:** the sugar composes N independent springs (it does NOT couple them — that path is KILLed at DLL-43). | W42; `audit:114`; `SpringProgress` (K.W11 c0482cb); ↔ DLL-43 (PHYS-A KILL — coupled, distinct) · ↔ DLL-13 (the 2-D drag consumer) |

### 1d — KILL (the standing anti-charter — 12, re-confirmed; +1 new from Lane 35; non-re-litigable)

These carry as L's standing anti-charter (`L.md:124-135`). **L.W-KILL-GUARD / W120** re-confirms;
a value.js AST extension would gate any future re-look. **RECORD permanent; do not re-open.**

| # | KILL item | Reason | Re-confirm anchor |
|---|---|---|---|
| **DLL-33** | **VT-A** (flipShared→View-Transition dispatcher) | inv-16 / kf-owns-time; structural, permanent | `audit:240,253`; `L.md:126` |
| **DLL-34** | **VT-B** (parse `::view-transition` @keyframes) | architectural boundary; value.js's job, permanent | `audit:241,253`; `L.md:126` |
| **DLL-35** | **CE-2** (@property for compositing) | Chromium bug unshipped; platform physics unchanged | `audit:242`; `L.md:127` |
| **DLL-36** | **CE-3** (compositor-offloaded oklab ramp) | fundamental rendering architecture | `audit:243`; `L.md:127` |
| **DLL-37** | **EPF-2** (interpolation JIT / precompiled closures) | dispatch already monomorphic-per-iv (`utils.ts:339`); CSP/KISS-hostile for sub-25% predicted win | `audit:244,255`; `L.md:128` |
| **DLL-38** | **EPF-5** (LoAF adaptive-quality / shed blend layers) | sheds author-declared intent; the weighted-blend axis IS the product; kf already sheds latency-not-work (YIELD_BATCH) | `audit:245,256`; `L.md:129` |
| **DLL-39** | **K-T1** (kf-owned splitText) | a11y hazard; off all three axes | `audit:246`; `L.md:129` |
| **DLL-40** | **K-T3** (Custom-Highlight-API animation) | paint mask, not motion substrate | `audit:247`; `L.md:130` |
| **DLL-41** | **CC-7** (blanket @starting-style) | platform-incorrect framing; structural. (Ingest of `@starting-style` also absent — `audit:235` — covered by the same KILL framing.) | `audit:248`; `L.md:130` |
| **DLL-42** | **ED-6** (JSR publish) | loses the hand-tuned static/dynamic boundary | `audit:249`; `L.md:131` |
| **DLL-43** | **PHYS-A** (coupled vector springs) | mathematically equivalent to N independent springs | `audit:250`; `L.md:131` |
| **DLL-44** | **Worker / OffscreenCanvas / AnimationWorklet (Houdini) + GPU/WebGPU compute** | re-examined 2026, STILL-KILL: no DOM-style write path off-main-thread; AnimationWorklet abandoned; OffscreenCanvas canvas-only; no DOM-style write path from a compute pass | `audit:254,257`; `L.md:132-133` |
| **DLL-45** | **GEN-1 generate-from-intent verb** (NEW, Lane 35) | off-axis (KISS/inv-16/moat-gestalt): the LLM generates, kf validates+compiles. Adding it would be the ONE real precept tension. Added to L's anti-charter. | Lane 35 `[high/KILL]`; `audit:24-28` completion; `L.W6.md` (`generate()` KILLed); `DL-L §3 GEN-1` |

---

## §2 — THE P-INVARIANT-28 EXPOSURE ROLL-UP (the ≥4-tranche `‡` riders — terminal homes mandatory)

P-inv-28 (`L.md:77`): a ≥4-tranche deferral may not ride as a bare BOOK; it exits via a
born-RED gate, a published consume-edge, a measurement, or a reasoned KILL. The K close
EXITED all ten K `‡` riders (`K/FINAL.md:58`). L inherits **three ≥4-tranche `‡` riders**,
each carried below with its MANDATORY terminal home — **none re-BOOKed, each tripwired:**

| Rider (≥4-tranche) | Chronicity | Tag | The REQUIRED L exit form | Terminal home |
|---|---|---|---|---|
| **DLL-20 GlassControlPoint** | ‡ 6 (E…K→L) | Band-B-gated-consume | consume-on-glass-ui-publish (`proof:control-point-live` gate-first) OR a reasoned build-in-kf KILL with a concrete spec — **the re-BOOK option is CLOSED at L.WZ** | L.W9 S4; if absent at L.WZ → EXITED (shipped) or named terminal KILL |
| **DLL-21 MorphSVG / FB-3** | ‡ 6 (C…K→L) | Band-B-gated-consume | published-consume-edge: `proof:morphsvg-consume` born-RED greens on value.js O VJ.W4 remainder + kf `fromMorphSVG` | L.W9 (KF-TO-VALUEJS-O); sibling-owned, exit-shaped |
| **DLL-22 parse-that packrat / PT-2** | ‡ 5 (E…K→L) | Band-B-gated-consume | gate-first BOOK is exit-shaped: `proof:packrat-sound` authored first; greens on parse-that PT-WAVE-6 + the kf re-pin via value.js | L.W9 S11 (KF-TO-PARSE-THAT) |

**The other chronics carried** (DLL-19 RF-17 3-tranche ★, DLL-24 F-2 2-tranche ★, DLL-30
postTask/EPF-1 2-tranche ★, DLL-3 composition 2-tranche, DLL-18 dogfood 2-tranche) are
sub-4-tranche; each STILL carries a named tripwire (P-inv-28 universal clause: "every item
re-BOOKed [carries] a tripwire", `L.md:77`). **DLL-23 (the workaround sweep)** is 1-tranche
but is the inv-L-acyclic-purity headline — its four arms each carry a sibling-publish tripwire
and a born-RED `proof:workaround-deletion` arm; no arm carries to a second L tranche.

**The P-invariant-28 closure assertion for this ledger:** every row in §1 carries (a) a tag,
(b) a named L home, and (c) a named tripwire. **Zero rows are bare BOOKs.** The three ≥4-tranche
`‡` riders (DLL-20/21/22) each have a terminal home that exits the P-inv-28 belt on the first
impl/consume, not a 7th BOOK. This is the `proof:chronic-closure` re-point contract
(`L.md` L.WZ row; `PROGRESS.md:249-260`): the gate must RED on three planted malformed rows
(a FOLD citing a source-shape gate; a HANDOFF targeting an unpublished future version with no
consume-edge; a ≥4-tranche bare BOOK) before it greens on this clean terminal ledger.

---

## §FOLD (the disposition summary)

| Tag | Count | Rows | Home band |
|---|---|---|---|
| **Band-A-fold** | 21 | DLL-1…DLL-18 · DLL-46 · DLL-47 · DLL-48 | L.W1 (1-5) · L.W2 (6-8, **46**) · L.W3 (9-10) · L.W4 (11-12) · L.W5 (13-14) · L.W6 (15) · L.W7 (16-17, **47-48**) · L.W8 (18) |
| **Band-B-gated-consume** | 11 | DLL-19…DLL-27 · DLL-49 · DLL-50 | L.W9 (19-26, **49**, **50**) · L.W10 (27) — each born-RED kf-side, consume-on-publish |
| **BOOK-with-tripwire** | 6 | DLL-28…DLL-32 · DLL-51 | L.W9/W5 (28) · L.W8 (29) · L.W7 (30) · L.W10 (31) · L.W0 (32) · L.W5 (**51**) |
| **KILL** | 13 | DLL-33…DLL-45 | anti-charter (L.W-KILL-GUARD / W120); non-re-litigable |

**Ledger total: 45 DLL rows** (the original 18 Band-A-fold · 9 Band-B-gated-consume ·
5 BOOK-with-tripwire · 13 KILL) **PLUS the 6 audit-W# proposals the P-invariant-28 sweep
recovered** (DLL-46 W37 · DLL-47 W111 · DLL-48 W112 · DLL-49 W104 · DLL-50 W106 · DLL-51 W42)
— a corrected total of **51 DLL rows** that **REFINE the 13 `PROGRESS.md §"Open deferrals"`
DL-L cluster rows** (the 13 PROGRESS cluster rows are the SUMMARY; these 51 DLL rows are the
item-by-item terminal expansion — the 13-vs-51 framing: 13 clusters, 51 leaf items). The K-inherited
OUT items are present and tagged: **RF-17/DL-K9 → DLL-19**, **GlassControlPoint/DL-K7 → DLL-20**,
**MorphSVG/FB-3 → DLL-21**, **parse-that/PT-2 → DLL-22**. The NEW cross-repo asks are present:
**F-2 peer-cycle → DLL-24**, **SegmentedTabs aria → DLL-25**, **the value.js-O grammar/perf set
→ DLL-26/DLL-27**, **the transition-shorthand mirror (W104) → DLL-49**, **the non-ASCII
`dispatch()` fallback (W106) → DLL-50**, **the parse-that set → DLL-22/DLL-27/DLL-50**. The
**six recovered audit-W# proposals** (W37/W42/W104/W106/W111/W112 — previously dropped with no L
home, the P-invariant-28 breach) now each carry a row: **W37 → DLL-46**, **W111 → DLL-47**,
**W112 → DLL-48**, **W104 → DLL-49**, **W106 → DLL-50**, **W42 → DLL-51**. **P-invariant-28 HELD:**
every row carries a tag, an L home, and a named tripwire; zero bare BOOKs; every ≥4-tranche `‡` rider
(DLL-20/21/22) has a terminal home; **no W# proposal rides home-less.** The ledger terminates at L.WZ when Band A is green and
Band B's edges have consumed or honestly circled-back (`L.md:122`); `proof:chronic-closure`
re-points K→L in one motion, gated non-vacuous on the three planted malformed rows.
