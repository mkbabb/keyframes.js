# J.W7a — THE APPEARANCE-GRAMMAR SUFFUSION (the design directive's appearance half · the protagonist re-plated · the display voice carried from the doorway inward · the `--ball-tone` colour seam closing the two-palette drift · the math made the hero · the storyboard grammar unified + the substrate given depth · on J.W4's appearance-certification critical path · the visual-lock baseline RE-CAPTURED in this wave's close motion, never after)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** SHIP-in-J (the DESIGN boundary of the
  boundary-ORACLE extension — the user's J directive, `J.md §MANDATE`: *"How might we better structure and
  suffuse proper design hierarchy … suffuse our design language of glass, grid, math, large and audacious
  typography, with colorful audacious pops, like those found in our icons … within a sense of proportion."*
  The 14-lane design audit finds the language PRESENT but CONCENTRATED at ONE doorway; W7a suffuses it
  through every pane.) · **Scope (demo APPEARANCE only — NO engine, NO gate-source, NO CI):** the shared
  idiom layer (`demo/@/styles/design-idioms.css` — the `.progress-ball` parameterization, the stage-field +
  graph-paper substrate, the φ-spacing) + the eight scene `*.vue` / `*Target.vue` / `*Scene.vue` surfaces +
  the floating controls pane (`ControlsPaneWrapper.vue`) + `demo/app/index.html` (favicon/theme-color) +
  the `proof:visual-lock` baseline corpus (RE-CAPTURED in the close motion). The kf APPEARANCE delta is
  W7a's **alone** — W7b owns ZERO appearance delta and re-captures NO baseline (`J.md §invariants`,
  isomorphic-styling row; `waves/README.md §3`). · **DAG-deps:** **follows J.W2** (the SHARED demo files —
  the dock projection, the single-writer control surface, the drag seam, the spring-settle sheet — must be
  born-correct in BEHAVIOR before they are re-skinned; re-skinning a stage whose drag seam or single-writer
  is still mid-repair would force W7a to re-baseline twice — `waves/README.md §3`, the load-bearing
  file-overlap edge). **On J.W4's APPEARANCE-CERTIFICATION critical path:** W4's mobile-overlap-==0, dark
  `--ball-tone`-contrast, and ghost-rail-absent legs gate on **J.W7a + J.W3** and green ONLY on the post-W7a
  tree (`J.W4.md §S1/§S3/§S5`, the appearance-certification band). The longest serial path is
  **W0 → W2 → W7a → W4(appearance legs)**; W7b runs PARALLEL and off this path.

## §Provenance (the folded design findings + the W6-3 deferral homed here)

The 14 design lanes over the 48-screenshot corpus (`audit/design/screenshots/`, captured by the checked-in
`scripts/capture.mjs`, zero console errors) converge on **five suffusion bands** — every band's claim cites a
pane-lane finding-ID + a screenshot + a `file:line`. The cross-lanes set the thesis; the pane lanes carry the
per-scene evidence.

- **The cross-hierarchy synthesis (`cross-hierarchy.md`)** — the ONE lane that sees every scene. Verdict
  (`:16-44`): the shell chrome is *"genuinely single-voiced and strong"* (RE-AFFIRM — W7a AMPLIFIES, does
  not repair); the break is BELOW the chrome in the stage + controls grammar. TOP-10 (`:75-120`): the empty
  400px ghost rail (#1, P1), the storyboard control-placement schism (#2, P1), the amiga gray slab (#3,
  P2/P1), the top-band collision (#4, P1), the orphaned home hero voice (#5, P2), the subject vertical drift
  (#8, P2), the mobile sheet covers the subject (#9, P2). The three worst — *"empty ghost rail, storyboard
  control-placement schism, amiga gray-slab stage"* — *"most visibly break the single-product illusion."*
- **The cross-typography lane (`cross-typography.md`)** — *"Instrument Serif (the brand display face) appears
  at EXACTLY ONE moment in the entire app"* (`:28-31`, the start-screen `text-display-mega`); every per-scene
  stage name is *"bold native sans, not the display serif"* (`:33-36`). The display tier is *"used once,
  beautifully; the scenes never reach for it"* (`:63-65`). The amiga `anti-goal` (`:169-172`): *"Adding display
  type here would break the iconic minimal aesthetic. Leave headerless"* — the one scene correctly silent.
- **The cross-color-pops lane (`cross-color-pops.md`)** — *"TWO distinct colour systems … drifted apart"*
  (`:9-44`): the ICON palette is a *"full 6-colour rainbow + gold"*, the SUBJECT palette *"collapsed to ONE
  green"* via the shared `.progress-ball` / `--color-progress` idiom. *"The icon promises a spectrum the
  subject does not keep … this is the single largest pop defect in the app, and it is STRUCTURAL — it lives
  in the shared `.progress-ball` / `--color-progress` idiom"* (`:28`). The four moves (`:102-114`):
  parameterize `.progress-ball` with `--ball-tone` *"exactly as `.status-badge` already does with
  `--badge-tone`"*; map the Sequence rows onto the spectrum; retire the `aquamarine` literal; give the live
  readout the accent. The proportion police (`:61-69`): the chrome STAYS calm; *"the fix is NOT 'more colour
  everywhere'."*
- **The cross-grid-math lane (`cross-grid-math.md`)** — *"this is an easing/spring/bezier engine whose
  signature mathematical object — the EasingCurveCanvas … — is exiled to a ~280-360px sidebar, while the
  ~960px protagonist stage shows only a green ball sliding on a flat gray line"* (`:9`, C1 `:56-60`). The
  stage plates have NO ghosted axes/grid (C2 `:62-65`); the grid-paper substrate is *"too faint and uniform
  to read as intentional graph paper"* (C5 `:78-82`); the readouts are caption-scale, not function notation
  at display scale (C3 `:67-71`). The motion-path stage (geometry + handles + generating string, C7 `:89-92`)
  is *"the BEST math-made-visible moment … the reference pattern the other scenes should match."*
- **The eight per-pane lanes** carry the per-scene evidence: `pane-square.md` (SQ-1/3/5/11/12), `pane-amiga.md`
  (A-01/A-02/A-03/A-06/A-07), `pane-home.md` (H1/H3/H4/H6/H11), `pane-easing.md` (E1/E2/E3/E11/E13),
  `pane-spring.md` (SP-1/2/9/12), `pane-sequence.md` (SEQ-01/02/05/13), `pane-motion-path.md`
  (MP-H1/MP-H2/MP-I1/MP-OPP5), `pane-cube.md` (C1/C2/C3/C16).

**W6-3 — the substrate-depth deferral, HOMED HERE (the terminal verdict).** `audit/wave-I.W6.md §6` /
W6-3 (P1, FOLD): the I.W6 spec classified **S3 substrate depth** NON-BLOCKING (M-2) — *"give the page glass
a real thing to refract"* — and the bloom-removal correctness landed clause-(a)-only, but S3 was *"a
perpetual punt without a terminal home: deferred as non-blocking, never KILLed, and not assigned to a J
wave"* (`:124`). The verdict (`:160`): *"J must assign a terminal verdict: FOLD into a J wave (legibility
gate) or KILL."* **W7a is that home.** The glass plate reads flat/inert because the page substrate behind it
is a near-invisible 0.10α corner-tick (cross-grid-math C5; pane-home H4) — there is nothing for the glass to
refract. W7a's §S4 stage-field band gives the page substrate REAL depth (the two-tier graph-paper substrate +
the per-stage field-tint), so the glass plate finally reads AS glass against a substrate with structure. The
TERMINAL VERDICT: **FOLD into the §S4 stage-field band with a legibility assertion** (the §Hard gate's
substrate-depth clause — the page substrate's computed graph-paper layer is PRESENT and legible behind the
glass plate, the I.W6 S3 item finally landed and gated). W6-3 is no longer a punt; it lands as part of the
stage-field band and exits J GREEN-or-RED on a runtime clause, never deferred again.

## §The state, verified (file:line / screenshot anchors)

- **The `.progress-ball` colour seam is UN-parameterized today.** `design-idioms.css:399-409` —
  `.progress-ball { … background: var(--color-progress); box-shadow: 0 2px 10px color-mix(in srgb,
  var(--color-progress) var(--ball-glow, 35%), transparent) }`. There is a `--ball-glow` axis but NO
  `--ball-tone` — the fill is hardcoded `--color-progress` green. The MIRROR seam already exists one section
  below: `.status-badge` at `:435-440` defaults `--badge-tone: var(--color-progress)` and paints
  `color-mix(… var(--badge-tone) …)`, with the comment (`:428-431`) explicitly naming `--badge-tone` as *"the
  same parameterization seam `.progress-ball`'s `--ball-glow` exposes"* — the `--ball-tone` var is the planned
  twin that was never cut. (`cross-color-pops.md §5.1`; `design-idioms.css:399-440`.)
- **The aquamarine literal.** `demo/app/scenes/SquareScene.vue:162` — `background-color: aquamarine;` (a raw
  CSS named colour ≈ `#7FFFD4`, off the rainbow/gold token ladder, no dark variant). The one raw named-colour
  in the scene corpus (`cross-color-pops.md §1`, SQ-5). The tumble egg already names `EGG_HUES =
  ["#C462D8","#7E6BE8","#52E898"]` (SQ-13) — the green stop `#52E898` is the square's true identity hue.
- **The display rungs SHIP in glass-ui 3.9.0 (verified on disk).** `node_modules/@mkbabb/glass-ui/dist/styles/
  typography.css:141-237` — `@utility text-display-audacious / -hero / -mega / -5 / -4 / -3 / -2 / display`
  ALL present, `--font-display` = Instrument Serif. The demo uses `text-display-mega` once and NONE of the
  others (`cross-typography.md §rung-census`: `text-display-mega ×3, text-display-4 ×1, text-display ×1` — all
  on the start screen). The poster rungs are published-and-available; the suffusion CONSUMES them.
- **The published display COMPONENTS ship (verified on disk — the S2 published/unpublished partition).**
  `MetricBadge` (`@mkbabb/glass-ui` main entry; `metric-badge.d.ts` — `size: 'sm'|'md'|'lg'|'xl'`, the `xl`
  rung self-documents as *"the audacious-poster rung … for hero placements that need to read at chassis
  distance"*; `color`, `label`, `unit`, `labelPosition` props), `AnimatedDigit` (`@mkbabb/glass-ui/
  animated-digit` — damps toward target via `useAnimatedNumber`, ships tabular-nums + ss01 + lnum), and
  `FourierField` (`@mkbabb/glass-ui/fourier-field` — `variant: "hero"|"final"`, REQUIRED `colorResolver` seam,
  inherits the reduced-motion freeze) are ALL PUBLISHED in 3.9.0. **`MetricHeader` is ABSENT** (grep over
  `dist` = 0) — the *"name + live mono readout + status pill"* metric-header primitive the cross-typography
  lane proposes (`:195-198`) does NOT ship; it is a **W7b handoff edge**, not a W7a consume. **`GraphFrame` /
  the `CurveEditorCanvas` control-point primitive is ABSENT** (cross-grid-math G1; glassui-abstract A1) — also
  a W7b handoff edge. W7a assigns ONLY the published primitives; the unpublished ones are recorded as W7b
  edges (inv-16: kf consumes only what AX PUBLISHES).
- **The routed scene set.** `demo/app/scenes.ts:165` `allScenes = [homeScene, ...scenes]` — 8 routed scenes
  (home·cube·amiga·square·easing·spring·sequence·motion-path), three stage registers `STAGE_MODES` (`:190-207`)
  `subject | editor | storyboard`. The amiga stage is the lone slab NOT in the I5 `<Card surface="glass">`
  stage-card register (`cross-hierarchy.md §architectural-map`).
- **The substrate is a near-invisible corner-tick.** `EditorShell.vue:181-188` — the grid-background is a
  single inline `data:image/svg+xml` 1rem corner-tick at `fill-opacity='0.10'`, duplicated for `.dark` at
  `:186`. *"At display zoom it reads as off-white texture, not a designed grid"* (pane-home H4;
  cross-grid-math C5 `:78-82`). This is what the glass has nothing to refract against (W6-3).
- **The favicon is monochrome.** `demo/app/index.html:24` points at the monochrome line-art cube
  (`#4c3ee8` violet stroke, no fills); no `<meta name="theme-color">`, no `apple-touch-icon`. *"The brand's
  signature image — the colourful cube — is absent from browser chrome"* (`cross-color-pops.md §4`).

## §Goal

Suffuse the design language — glass, grid, math, the audacious Instrument-Serif display voice, and the
saturated rainbow the ICONS already promise — from the home doorway through every one of the 8 panes, **within
proportion** (every delta passes the calm-field/sharp-accent test; the chrome stays calm). The move is NOT a
re-theme; it is FIVE coherent appearance bands, each landing at the SHARED idiom seam where it belongs (not
eight per-scene overrides), each NAMED in the §S6 delta register, all re-baselined in W7a's own close motion.
Concretely: every stage SUBJECT becomes the unambiguous protagonist on a plated, fielded stage; the display
voice carries inward via the published poster rungs; the two-palette drift dies at the `--ball-tone`
parameterization seam (one var, ≤8 one-line consumers — `J.md §MANDATE`: *"the subject-palette drift dies at
the `.progress-ball` PARAMETERIZATION seam (`--ball-tone`) — NOT eight per-scene CSS overrides"*); the math
becomes the hero of its own stage; the storyboard grammar unifies and the substrate gains real depth. Six
bands, KISS · no-legacy · no-workaround · isomorphic-EXCEPT-the-named-deltas.

## §Scope — the six bands (S1 protagonist · S2 display voice · S3 colour · S4 math · S5 grammar · S6 the named-delta register + re-baseline)

### S1 — PROTAGONIST (every stage subject the unambiguous hero on a plated, fielded, centered stage) [APPEARANCE · the cross-hierarchy #3/#7/#8 cures]

**Locus:** the scene stage roots + the shared stage-card register (`design-idioms.css` §STAGE-CARD `:480-523`)
+ the floating controls pane (`ControlsPaneWrapper.vue`).

- **Square gains its plate (SQ-3, P1) + centers (SQ-1, P1).** `SquareScene.vue` `.square-stage` — wrap the
  drag arena in the I5 protagonist plate `<Card surface="glass" tier="resting" :shadow="false">` (the register
  every other contained stage uses; `pane-square.md §SQ-3`). Resolve the off-center drift (box at ~(60%,55%),
  SQ-1) with `place-items-center` on the stage cell. The plate's `rounded-card` resolves SQ-4 for free.
- **The amiga slab joins the rounded-glass register (XH-3, P1) + the sphere gains presence (A-02, P1).**
  `AmigaScene.vue` — the Three.js *"hard-edged unrounded desaturated gray slab"* (`cross-hierarchy.md` #3)
  joins the rounded-glass register: a glass-plate frame around the canvas (the `.amiga-canvas` gains the
  `rounded-card` + a 1px inset stage-boundary hairline per A-07) so it reads as a glass stage, not a bare
  room. The sphere — *"a ~90px ball in a 900×700 room"* (A-02) — gains protagonist scale via the camera
  reframe the lane names (`camera = PerspectiveCamera(50, …)` + `camera.position.z = BOX_SIZE * 0.7`,
  `pane-amiga.md §A-02`) so it fills ~20% of the FOV. **Subject = pivot = framing** (the I.W3 amiga discipline
  carried): the reframe is one consistent move, the sphere centered and framed, not three competing offsets.
- **Subject sizing/centering across the bare-subject + storyboard scenes (SEQ-02, SP-1).** The cross-hierarchy
  subject-vertical-drift (#8) and the recessive subjects (SEQ-02 *"~70% empty void … balls visually tiny"*;
  SP-1 *"protagonist ball reads as a footnote"*) resolve to a shared framing baseline: the stage subject sized
  + anchored to a deliberate position (the lane-named lower-third anchor for the rising ball, `pane-easing.md
  §E2`; the dominant master playhead at `--ball-size` for sequence, SEQ-12), so the SUBJECT is the first thing
  the eye reaches.
- **The controls pane recedes via `glass-wash` so the stage bleeds through (C2, C16, P1).** `ControlsPaneWrapper.vue`
  — when `stageMode="subject"` on desktop, the floating controls pane adopts the published `glass-wash` surface
  (`surface="glass" tier="wash"`, the translucent backdrop-blur idiom; `pane-cube.md §C16/C2`) so the stage
  bleeds through the chrome and the cube/subject wins hierarchy. The cube lane's verdict: *"The subject/chrome
  hierarchy inverts when the pane opens. The fix is making the chrome visually lighter (glass-wash), not
  smaller"* (`pane-cube.md §design-hierarchy`). **`glass-wash` is a published 3.9.0 surface — a W7a consume**
  (NOT a W7b handoff).
- **The mobile hero/subject collisions die (H3, TYP-1, A-01).** `EditorStartScreen.vue` — the mobile hero
  *"Select an / animation…"* physically overlaps the cube at 375w (H3, P1; cross-typography TYP-1
  `:71-78`): the mobile layout SEPARATES them vertically (the hero parks in the top band via
  `pt-[var(--work-area-top-offset)]`, the cube recedes below; `pane-home.md §H3`). The amiga mobile-open
  occlusion (A-01, P1 — *"only the top hemisphere is visible above the sheet"*) resolves via the sphere
  camera-space lift + the sheet-detent tightening the lane names (`pane-amiga.md §A-01`). **These are the
  exact appearance facts J.W4's mobile-overlap-==0 leg certifies** on the post-W7a tree (`J.W4.md §S1`,
  appearance-certification band).

**§S1 anti-goal (proportion police).** The amiga gains a glass FRAME and a present sphere — it does NOT gain
display type on its stage (cross-typography `:169-172`: the headerless silence is *"a deliberate, on-brand
silence"* — RE-AFFIRMED, the amiga stays headerless; the display voice does NOT land here). The plate recedes;
the SUBJECT pops.

### S2 — DISPLAY VOICE (the Instrument-Serif display register carried from the doorway to the scenes — via the PUBLISHED poster rungs ONLY) [APPEARANCE · the cross-typography #5/#2/#3 cures]

**Locus:** the four `*Target.vue` header spans + the live math readouts + the home subtitle ladder. **The
published/unpublished partition is BINDING (inv-16): W7a assigns ONLY 3.9.0-published primitives; unpublished
ones are recorded as W7b handoff edges.**

- **Scene identity at the display rungs (TYP-2, C3, E3, SP-2, SEQ-01, MP-H2).** Lift the per-scene stage names
  — `SpringProgress`, `ease`, `Sequence`, `MotionPath` — from `text-heading` native-sans (25.9px) to a real
  display rung in Instrument Serif (`text-display` / `text-display-2`, 42-53px; `cross-typography.md §3` —
  *"the highest-leverage single change for suffusing the display identity"*). These rungs are PUBLISHED
  (`typography.css:141-237`, verified on disk) — a direct kf-side class swap on the four `*Target.vue` header
  spans, no glass-ui ask. (`pane-easing.md §E3`, `pane-spring.md §SP-2`, `pane-sequence.md §SEQ-01`,
  `pane-motion-path.md §MP-H2`, `pane-cube.md §C3`.)
- **The readout promotion: 12px captions → confident mono displays (the §2 cross-typography move, the
  cross-color-pops §2 + cross-grid-math C3).** The live engine readouts — `F(0.95)=0.999`, `x=1.000·v=0.00`,
  `OFFSET-DISTANCE=0%·TANGENT-89°`, `STAGGER×5·PROGRESS 0%` — are the inv-ζ dogfood, *"the engine's live math
  … the thing the product DOES"* (`cross-typography.md §2`), today at `text-mono-caption` 12px muted. Promote
  the HEADLINE value via the PUBLISHED **`MetricBadge size="xl"`** (the self-documented audacious-poster rung)
  + **`AnimatedDigit`** (the damped tabular-nums display) — BOTH published in 3.9.0 (verified: `metric-badge.d.ts`,
  `animated-digit.d.ts`). The unit/label stays small; the NUMBER wins. This is *"exactly the
  `--metric-row-value-clamp-max` pattern glass-ui already ships"* (`cross-typography.md §2`,
  `tokens.css:1641`). (`pane-spring.md §SP-2`, `pane-motion-path.md §MP-OPP5`, `cross-grid-math.md §C3`.)
- **The home subtitle ladder + display continuity (H2, TYP-5).** Step the `text-title italic` subtitle down one
  φ-rung so the hero:subtitle ratio reads ≥5:1 (H2); reuse the `text-display-mega` Instrument-Serif treatment
  on the honest-404 + empty states for *"one-line continuity of the audacious voice"* (`cross-typography.md §5`).
- **The amiga stays headerless (the BINDING exception, cross-typography `:169-172`).** Per the anti-goal in §S1,
  the amiga scene receives NO display title — *"the one scene that correctly has no title."* The display voice
  carries to the SEVEN other scenes; the amiga's silence is preserved.

**§S2 W7b handoff edges (UNPUBLISHED — recorded, NOT consumed here).** The *"name + live mono readout + status
pill"* **`MetricHeader`** primitive the cross-typography lane proposes (`:195-198`) is ABSENT from 3.9.0 (grep
= 0); it is an **ABSTRACT W7b ledger edge** (the four byte-similar stage headers → one glass-ui primitive),
NOT a W7a consume. W7a uses the published `MetricBadge`/`AnimatedDigit` to compose the readout; the
abstract-it-into-one-`MetricHeader` ask is W7b's `glassui-AX-handoff.md` row. The mono-caption uppercasing
inconsistency (`cross-typography.md §handoff`, REFINE) is likewise a W7b edge.

### S3 — COLOUR (the `--ball-tone` parameterization seam closing the two-palette drift — one var, ≤8 consumers, every subject keeps its icon's hue) [APPEARANCE · the cross-color-pops §5 cure — the J.md headline colour move]

**Locus:** `design-idioms.css:399-409` (the `.progress-ball` seam) + the per-scene `.vue` token consumers +
`SquareScene.vue:162` + the readout spans + `index.html`.

- **Parameterize `.progress-ball` with `--ball-tone` (the structural seam, mirroring `--badge-tone`).** Add a
  `--ball-tone` var (default `var(--color-progress)`) to `.progress-ball` (`design-idioms.css:406-407`),
  EXACTLY as `.status-badge` already does with `--badge-tone` (`:436`, the proven twin one section above in the
  same file). `J.md §MANDATE` (binding): *"the subject-palette drift dies at the `.progress-ball`
  PARAMETERIZATION seam (`--ball-tone`) — NOT eight per-scene CSS overrides."* ONE new var; the per-scene
  consumers set ONE token each.
- **The per-scene hue map FROM the icon palette (cross-color-pops §5.1).** Each scene's ball/traveller sets
  `--ball-tone` to match its ICON glyph: easing ball → `--rainbow-violet` (matches `easing.svg`); motion-path
  traveller → `--rainbow-cyan` (matches `motion-path.svg`, MP-OPP4); spring ball → `--rainbow-green` /
  `--color-progress` (the icon's rest dot IS green — already consistent); cube/amiga/square keep their 3D/teal
  identity. *"Every subject finally keeps its icon's promise. No new colours, no ad-hoc CSS"* (`cross-color-pops.md §5.1`).
- **Sequence rows onto the spectrum (CP-2, SEQ-13).** The Sequence icon is violet→blue→cyan→green ascending;
  the five staggered ROWS each draw the corresponding `--rainbow-*` stop (row 0 violet … row 4 green) instead
  of one green at 55% opacity (`SequenceTarget.vue:374,391,402`). *"The stagger storyboard becomes a literal
  rainbow — exactly what its glyph advertises … which the uniform green actively hides"* (`cross-color-pops.md §5.2`).
- **The aquamarine literal DIES (SQ-5, the no-legacy delta).** `SquareScene.vue:162` `background-color:
  aquamarine` → a named token (`--subject-teal` / `--rainbow-cyan`, the square icon's teal; the EGG_HUES green
  stop `#52E898` is the candidate identity hue, SQ-5/SQ-11). The one raw named-colour off the owned ladder —
  retired in the same motion (`cross-color-pops.md §5.3`).
- **Readouts gain the scene accent (CP-4).** The LIVE computed value in each mono readout (`= 0.686`, the
  settling `x`, `TANGENT`) wears `--ball-tone` (the scene accent); labels stay `--muted-foreground`. *"The
  number that proves the engine runs is the number that carries the colour"* (`cross-color-pops.md §5.4`) —
  the proportionate way to add colour to readouts without tinting the panel.
- **The colourful cube favicon + `theme-color` (CP-4.1).** `index.html:24` — promote the colourful cube to
  the favicon (a simplified 3-face flat-colour version surviving 16px) + add `<meta name="theme-color">` keyed
  to the cube's magenta/violet + an `apple-touch-icon`. *"The most-seen surface of the brand (the browser tab)
  should carry the brand's most-seen colour"* (`cross-color-pops.md §4.1`).
- **The GOLD RESERVE RULE (the proportion invariant).** `--color-gold` stays RESERVED for celebration —
  the Format-button, the motion-path active sparkle, the *"achievement/settled"* accent (`cross-color-pops.md
  §5` *"the sparkle/celebration … the gold stays reserved for celebration"*). NO band may spend gold on a
  resting subject or a readout; gold is the singular celebration note, policed.

**§S3 anti-goal (proportion police, cross-color-pops §3).** The two-card left rail, the dock/menubar
surfaces, the collapsed dock-pill LABEL, the glass card borders/backdrop STAY calm `--muted-foreground`. *"The
fix is NOT 'more colour everywhere' — it is 'let the SUBJECT keep the promise its ICON made,' and 'let the one
live number wear the accent.'"* The group-play rainbow disc stays *"the loudest note"* — the ONE orchestrated
motion-colour moment, un-rivalled.

### S4 — MATH (the easing stage projects its OWN bezier · the coordinate-grid stage fields · FourierField where befitting · W6-3 substrate depth folds in) [APPEARANCE · the cross-grid-math C1/C2/C5 + the W6-3 deferral]

**Locus:** `EasingTarget.vue` (the projected curve) + the shared stage-field substrate (`design-idioms.css` +
`EditorShell.vue:181-188`) + the calm empty fields.

- **The easing stage's projected live bezier (E1/E2, P1 — the ball traverses its visible curve).** Today the
  signature bezier specimen is *"exiled to a ~280-360px sidebar; the 960px stage shows a ball on a plain
  line"* (cross-grid-math C1). PROJECT the current bezier path across the full stage floor — a scaled-up
  low-opacity SVG of `bezierPathD` (`--ppmycota-primary` at 6-8%) so the ball traverses its OWN curve, not a
  flat rail (`pane-easing.md §E1/E2/E7`). The ball's x-position already equals `fn(progress)·maxX`; the ghost
  curve behind it MAKES THAT RELATIONSHIP VISIBLE. This is the §Hard gate's *"projected curve PRESENT and
  MUTATING with a handle drag"* clause (`J.W7a` row). **The motion-path stage (C7) is the reference pattern**
  the easing stage now matches — geometry + the live readout, the math on the stage at scale.
- **The designed coordinate-grid stage fields (C1/MP-I1 — a kf-LOCAL token-clean implementation).** Ghost a
  faint coordinate frame into each stage plate (a 0→1 t-axis baseline + value gridlines at 0.25/0.5/0.75,
  reusing the curve canvas's own gridline treatment so sidebar specimen + stage share ONE visual language;
  cross-grid-math C2). The motion-path stage stops rendering its traveller against the raw global crosshatch
  (MP-I1) and gains a clean stage field (`color-mix(… --color-progress 4% …)`, MP-OPP1). **Implemented
  kf-LOCAL + token-clean** (no magic literals — a `--graph-*` / field-tint token in `design-idioms.css`);
  **the glass-ui `<GraphFrame>` / `.bg-graph-paper` promotion is RECORDED in the W7b ledger** (cross-grid-math
  G1/G3 — ABSENT from 3.9.0, an ABSTRACT/REFINE handoff edge), consumed only when AX publishes (inv-16). W7a
  ships the kf-local field NOW; W7b books the promotion.
- **FourierField adoption where befitting — IF published (it IS — verified).** `FourierField`
  (`@mkbabb/glass-ui/fourier-field`) is PUBLISHED in 3.9.0 (verified on disk: `variant: "hero"|"final"`,
  REQUIRED `colorResolver` seam — pass `defaultBlobColorResolver` from `@mkbabb/glass-ui/color`, inherits the
  reduced-motion freeze). Adopt it for the EMPTY CALM FIELDS where befitting — the home left-half vacancy
  (pane-home H1), the under-filled stage negative space (cross-hierarchy §e) — at low presence as a
  mathematical motif in the calm field, NOT over the moving subject. **Because it is published, this is a
  W7a CONSUME** (not a W7b edge); the colour resolves through `--ball-tone` / the scene accent via the
  injected resolver, dark-mode-retinting for free.
- **W6-3 SUBSTRATE DEPTH folds into this stage-field band (the terminal verdict).** The page substrate —
  today the near-invisible 0.10α corner-tick (`EditorShell.vue:181-188`; cross-grid-math C5, pane-home H4) —
  gains REAL depth: a deliberate two-tier graph paper (fine 1rem lines + bolder 5rem major lines, *"like
  engineering graph paper"*) or a stage-region-concentrated grid, tokenized via `--graph-pitch` /
  `--graph-opacity` / `--graph-major` (cross-grid-math C5). NOW the glass plate has a structured substrate to
  REFRACT against — the I.W6 S3 *"give the page glass a real thing to refract"* item finally landed
  (`wave-I.W6.md §6`, W6-3). **This is the substrate-depth legibility CLAUSE of the §Hard gate** — W6-3 exits
  J on a runtime assertion, never deferred again.

**§S4 anti-goal (proportion police, cross-grid-math §proportion-notes + cross-typography `:174-181`).** The
math AMPLIFIES in the STAGE region only — the substrate stays CALM in the field, concentrating near the
stage. The motion-path stage interior does NOT gain a display watermark behind the live geometry
(cross-typography: it *"would compete with the live geometry"* — confine amplification to the HEADER name +
the readout NUMBER). FourierField goes in EMPTY fields, never over a moving subject.

### S5 — GRAMMAR (the ghost rail dies · the storyboard register converges · the top-center band contract · the playback affordance converges) [APPEARANCE · the cross-hierarchy XH-1/XH-2/XH-4 cures]

**Locus:** `AnimationControlsGroup.vue` (the rail track) + the storyboard scene roots + the top-center band.

- **XH-1 — the ghost rail DIES (cross-hierarchy #1, P1).** The hollow 400px glass shell that floats top-left
  on sequence/motion-path (*"nothing in it … a vast asymmetric empty column"*) is removed: where a scene's DFA
  control-surface set is `[]` (sequence/motion-path — `CONTROL_SURFACES = []`), the `[rail]` track COLLAPSES
  to 0 (the stage reflows to fill) — the rail does not hold open at 400px showing a vacant card
  (`cross-hierarchy.md §1/§d`, `cross-grid-math.md §A3`). **This is the §Hard gate's `ghost rail ABSENT`
  clause** (`J.W7a` row; J.W4 §S5 certifies it on the `home`/storyboard sweep). The freed space either
  collapses or (cross-hierarchy suffusion) becomes a typographic moment (the freed track hosts the large serif
  scene-title) — the IMPL chooses collapse-or-fill, but the GHOST (the hollow pill over a void) is GONE in
  ONE motion (no legacy beside the replacement).
- **XH-2 — the storyboard register CONVERGES (cross-hierarchy #2, P1).** The three `storyboard` scenes
  (spring/sequence/motion-path) today disagree on where playback lives (full left-rail panel vs in-stage text
  row vs bottom-dock-only — *"same declared register, three different control architectures"*). Converge to
  ONE storyboard control grammar: the playback affordance lives in ONE consistent place across the three
  (`cross-hierarchy.md §2/§c`), so the storyboard register reads as a system, not three per-scene decisions.
- **XH-4 — the top-center band contract (cross-hierarchy #4, P1).** The stage-level floating chrome (easing's
  `ease F(t)=… Singular` header; spring's `Live solver / Discrete` toggle) overlaps the centered
  scene-switcher pill on mobile — *"two chrome layers stacked in the same top-center band."* The contract: the
  top-center safe band belongs to the scene-switcher; stage-level chrome relocates OUT of that band (the metric
  header into the stage plate's own header strip; the spring toggle into the rail) so the band has ONE
  occupant. (The glass-ui `GlassDock` reserved-band query is a W7b REFINE edge — cross-hierarchy §handoff;
  W7a does the kf-side relocation, W7b books the durable band-query.)
- **The playback affordance convergence (cross-hierarchy §c).** The playback action forks THREE ways today
  (left-rail pill buttons / in-stage text row / bottom-dock rainbow) — *"three playback registers for one play
  action."* W7a converges the SCENE-LEVEL forks onto the canonical pair (the rail playback card + the bottom
  dock's rainbow group-play, the model the rest rhymes against — `cross-color-pops.md §0`), retiring the
  in-stage flat text row (sequence) into the convergent register.

### S6 — THE NAMED-DELTA REGISTER (every visual change enumerated — the isomorphic exception) + the visual-lock RE-CAPTURE (in THIS wave's close motion) + the proportion rule

**The isomorphic exception, enumerated (the BINDING register).** Per `J.md §invariants` (isomorphic-styling
row): *"EXCEPT the named J.W7a design deltas, which are DELIBERATE, enumerated, befitting appearance changes …
each is named in the wave spec and re-baselined in the same motion."* EVERY visual delta W7a ships is in the
register below — nothing un-enumerated changes appearance:

| # | Delta (the appearance change) | Band | Evidence anchor |
|---|---|---|---|
| D1 | Square gains the I5 glass protagonist plate + centers | S1 | `pane-square.md §SQ-3/SQ-1`, `square-desktop.png` |
| D2 | Amiga canvas gains a rounded glass-plate frame + 1px stage hairline | S1 | `pane-amiga.md §XH-3/A-07`, `amiga-desktop.png` |
| D3 | Amiga sphere reframed to protagonist scale (camera FOV/z). **Fix-round 1 — bounce-aware framing:** the D3 frustum frames the REST pose only; the bounce group translates the sphere ±BOUNCE about home and the PLAYING subject left the tightened frame entirely (390w portrait worst — frustum half-width ≈2.2 units vs the ±6 bounce+radius envelope). ONE move at the same subject=pivot=framing seam: the bounce amplitude is scaled INTO the live frustum (per-axis fit factors from the canonical camera fov/aspect/distance, recomputed on resize; `AmigaScene.vue refreshBounceFraming()` → the `getBounceScale` seam in `useAmigaAnimations`) so home ± scale·BOUNCE + r stays in frame at the simultaneous-worst corner. Rest pose + protagonist framing untouched. | S1 | `pane-amiga.md §A-02`, `amiga-desktop.png` |
| D4 | Subjects sized/anchored to a shared framing baseline (seq/spring/easing) | S1 | `pane-sequence.md §SEQ-02/12`, `pane-spring.md §SP-1`, `pane-easing.md §E2` |
| D5 | Controls pane recedes via published `glass-wash` (stageMode=subject, desktop) | S1 | `pane-cube.md §C16/C2`, `cube-laptop-open.png` |
| D6 | Mobile hero/subject separated vertically (home + amiga occlusion) | S1 | `pane-home.md §H3`, `pane-amiga.md §A-01`, `*-mobile.png` |
| D7 | Seven scene titles → Instrument-Serif display rungs (amiga EXCLUDED) | S2 | `cross-typography.md §3`, all `*-desktop.png` |
| D8 | Live readouts → `MetricBadge size="xl"` + `AnimatedDigit` (published) | S2 | `cross-typography.md §2`, `cross-grid-math.md §C3` |
| D9 | Home subtitle steps one φ-rung (LANDED, `EditorStartScreen.vue`). The 404/empty display-mega half is **DISPOSED — no such surface exists** (fix-round 1): the hash router's catch-all redirects every unknown route home (`demo/app/router.ts:31` — there is no in-app 404 page to carry the voice); the I.W5 "honest 404" is the dev server's asset-extension-miss policy (`vite.config.ts`, a network-tab fact, not a rendered page); and the demo has no text-bearing empty state (the start screen IS the resting state and already carries `text-display-mega`). A register row may not claim a delta the tree cannot witness — the half is recorded as surfaceless, not deferred; it revives only if a real 404/empty surface is ever born. | S2 | `pane-home.md §H2`, `cross-typography.md §5`; `demo/app/router.ts:31` |
| D10 | `.progress-ball` parameterized with `--ball-tone` (the seam) | S3 | `cross-color-pops.md §5.1`, `design-idioms.css:399-409` |
| D11 | Per-scene ball/traveller hue = icon hue (easing violet, mp cyan, …) | S3 | `cross-color-pops.md §5.1`, `easing/motion-path-desktop.png` |
| D12 | Sequence five rows onto the violet→green spectrum | S3 | `cross-color-pops.md §5.2`, `sequence-desktop.png` |
| D13 | `aquamarine` literal → owned token (DIES) | S3 | `pane-square.md §SQ-5`, `SquareScene.vue:162` |
| D14 | Live readout value wears the scene accent | S3 | `cross-color-pops.md §5.4` |
| D15 | Colourful cube favicon + `theme-color` + apple-touch-icon | S3 | `cross-color-pops.md §4.1`, `index.html:24` |
| D16 | Easing stage projects its own bezier across the floor | S4 | `pane-easing.md §E1`, `cross-grid-math.md §C1` |
| D17 | Coordinate-grid stage fields (kf-local, token-clean) + mp clean field | S4 | `cross-grid-math.md §C2`, `pane-motion-path.md §MP-I1` |
| D18 | FourierField in the empty calm fields (published consume) | S4 | `pane-home.md §H1`, `cross-hierarchy.md §e` |
| D19 | Substrate two-tier graph-paper depth (W6-3 landed) | S4 | `wave-I.W6.md §6 W6-3`, `cross-grid-math.md §C5` |
| D20 | The ghost rail DIES (rail collapses to 0 for empty-DFA scenes) | S5 | `cross-hierarchy.md §1`, `sequence/motion-path-desktop.png` |
| D21 | Storyboard playback register converges to one grammar | S5 | `cross-hierarchy.md §2` |
| D22 | Stage chrome relocates OUT of the top-center scene-switcher band | S5 | `cross-hierarchy.md §4`, `*-mobile.png` |
| D23 | Playback affordance forks converge to the canonical pair | S5 | `cross-hierarchy.md §c` |

**The visual-lock RE-CAPTURE — in THIS wave's close motion, NEVER after (the T3 honesty, `waves/README.md §3`).**
W7a re-captures the `proof:visual-lock` baseline corpus as the LAST act of the SAME wave that authors D1–D23.
Per `J.md §invariants` (T3 resolved): `proof:visual-lock` is *"re-labeled an appearance-drift tripwire whose
self-baseline is acknowledged in its own header (and re-captured at J.W7a, the appearance-grammar half)."* The
re-capture is honest by construction BECAUSE it is the closing motion of the authoring wave — a baseline
captured by a SEPARATE later motion *"would certify drift it did not author"* (`waves/README.md §3`). The
baseline lands WITH the wave; no later wave re-baselines (J.W7b changes no kf appearance and re-captures
nothing — `J.md §invariants` isomorphic-styling row).

**The proportion rule — every item passes the calm-field/sharp-accent test.** Each D-item is tested against
the design directive's *"within a sense of proportion"*: the SUBJECT and the live NUMBER pop; the CHROME stays
calm. The anti-goals — where pops/type would be WRONG — are enumerated from the lane docs and BIND W7a:

- **The amiga stays HEADERLESS** — no display type on its stage (cross-typography `:169-172`); the silence is on-brand.
- **The chrome STAYS calm `--muted-foreground`** — the two-card rail, the dock/menubar, the collapsed
  dock-pill LABEL, the glass borders/backdrop receive NO colour (cross-color-pops §3).
- **GOLD stays RESERVED** for celebration only — never on a resting subject or readout (cross-color-pops §5).
- **No display watermark behind live geometry** — the motion-path/easing path canvas keeps the math LIVE,
  not ornamented (cross-typography `:174-178`).
- **FourierField/grid AMPLIFY in the STAGE region only** — the field stays calm; amplification concentrates
  near the subject (cross-grid-math §proportion-notes).
- **The group-play rainbow disc stays the LOUDEST note** — un-rivalled; no band adds a competing saturated
  chrome accent (cross-color-pops §0).

## §Hard gate (the proof:* that BITES — born-RED on the pre-suffusion tree, GREEN-on-fix · RUNTIME/INTERACTION · the per-finding appearance oracles on the LIVE page)

**`proof:appearance-suffusion`** — a Playwright session over the BUILT `dist/gh-pages/`, on J.W3's
industrialized harness (`withPage()`/`withBrowser()`), navigating scenes through J.W0's `navToScene(page,
sceneId, expected)` per-expected-state primitive. Each clause asserts a PRODUCT-FACING computed property on
the live page (the gate-ORACLE precept — the property a human's eye would check), not a source-shape grep.
The clauses are the per-finding RUNTIME assertions the `J.W7a` charter row names:

- **clause (a) — the `--ball-tone` == icon hue per scene (the S3 colour oracle, the J.md headline move).**
  `navToScene` into each accented scene; read the subject's COMPUTED `--ball-tone` (via
  `getComputedStyle(progressBall).getPropertyValue('--ball-tone')`) and assert it RESOLVES to the scene's
  icon hue token (easing → `--rainbow-violet`; motion-path → `--rainbow-cyan`; spring → `--rainbow-green` /
  `--color-progress`). **BITE:** reds on the pre-suffusion tree — `.progress-ball` has NO `--ball-tone`
  var (`design-idioms.css:399-409`, verified), every accented subject computes the hardcoded
  `--color-progress` green; greens on S3 (the seam + the per-scene consumers). **Born-RED witness plan:**
  on the cured tree, plant a single per-scene consumer set to the WRONG token (e.g. easing ball
  `--ball-tone: var(--color-progress)` instead of `--rainbow-violet`) → the clause reds for that scene
  (the hue-map can BITE per scene, not just on the var's presence).
- **clause (b) — the display register's computed font-family resolves Instrument Serif at the named moments
  (the S2 voice oracle).** `navToScene` into each of the SEVEN display-voice scenes (amiga EXCLUDED by the
  binding exception); read the scene-title element's COMPUTED `font-family` and assert it resolves Instrument
  Serif (the `--font-display` face), NOT native sans. Assert the amiga stage carries NO display-title element
  (the headerless exception holds). **BITE:** reds on the pre-suffusion tree — every stage name is
  `text-heading` native sans (`cross-typography.md §3`, verified); greens on S2 (the display-rung swap).
  **Born-RED witness:** revert one `*Target.vue` title to `text-heading` → that scene's clause reds; the
  amiga-has-no-title clause reds if a title is planted on the amiga stage (the exception is enforced, not
  assumed).
- **clause (c) — the 390×844 hero/subject overlap == 0px (the S1 H3/TYP-1/A-01 oracle — the APPEARANCE-
  CERTIFICATION fact J.W4 §S1 also certifies).** On a `390×844` context, `navToScene` into `home` (and the
  amiga mobile-open path); measure the hero text element rect and the subject (cube/sphere) rect and assert
  their intersection AREA == 0 (no overlapping pixels). **BITE:** reds on the pre-suffusion tree — at 375w the
  hero *"animation"* word is physically behind the cube face (H3, `home-mobile.png`); greens on S1's mobile
  vertical separation. **Born-RED witness:** force the hero host's `pt-[var(--work-area-top-offset)]` back to
  the fixed `mt-28` overlap → the intersection area > 0 → clause reds. (This clause is shared with J.W4 §S1's
  appearance-certification band; both gate on J.W7a + J.W3.)
- **clause (d) — the easing stage's projected curve PRESENT + MUTATING on handle drag (the S4 math oracle).**
  `navToScene` into `easing`; assert the stage-floor projected-bezier SVG path is PRESENT (a non-empty `d`
  spanning the stage, distinct from the sidebar canvas). Then DRAG a curve handle and assert the projected
  path's `d` MUTATES (the stage curve re-renders the edited shape — the ball traverses its OWN visible curve,
  not a flat rail). **BITE:** reds on the pre-suffusion tree — the stage shows a ball on a flat gray line, no
  projected curve (cross-grid-math C1, `easing-desktop.png`); greens on S4 (E1's projected ghost curve wired
  to `bezierPathD`). **Born-RED witness:** stub the projected path to a static (non-`bezierPathD`-bound)
  literal → the drag does not mutate it → clause reds (a no-op projection that does not track the live curve
  still REDs; the mutation, not mere presence, is asserted).
- **clause (e) — the ghost rail ABSENT (the S5 grammar oracle — the APPEARANCE-CERTIFICATION fact J.W4 §S5
  also certifies).** `navToScene` into `sequence` and `motion-path` (the empty-DFA scenes); assert the
  `[rail]` track computed width is 0 (collapsed) and there is NO hollow rail-width card element in the
  top-left void. **BITE:** reds on the pre-suffusion tree — the rail holds open at `--rail-width: 400px`
  showing a vacant card (`cross-hierarchy.md §1`, `sequence-desktop.png`); greens on S5 (the rail collapses
  for empty-DFA scenes). **Born-RED witness:** revert the empty-DFA rail-collapse rule → the 400px track
  reappears with its hollow card → clause reds.
- **clause (f) — the amiga rounded-glass computed style (the S1 XH-3 oracle).** `navToScene` into `amiga`;
  read the `.amiga-canvas` stage element's COMPUTED `border-radius` and assert it resolves the
  `--radius-card` value (the rounded-glass register), NOT 0 / square. **BITE:** reds on the pre-suffusion
  tree — the amiga slab is an unrounded gray rectangle (`cross-hierarchy.md §3`, `amiga-desktop.png`); greens
  on S1's glass-plate frame. **Born-RED witness:** revert the `rounded-card` on `.amiga-canvas` → the computed
  radius returns to 0 → clause reds.
- **clause (g) — the substrate-depth legibility (the S4 / W6-3 oracle — the I.W6 S3 item, finally gated).**
  On the home/stage view, read the page substrate's graph-paper layer (`EditorShell` grid-background) and
  assert the two-tier graph paper is PRESENT and legible behind the glass plate (the computed
  `--graph-opacity` / the major-line layer resolves above the near-invisible 0.10α floor; the substrate has
  structure for the glass to refract). **BITE:** reds on the pre-suffusion tree — the substrate is the
  single near-invisible 0.10α corner-tick (`EditorShell.vue:181-188`, cross-grid-math C5); greens on S4's
  two-tier graph-paper depth. **Born-RED witness:** revert the substrate to the single 0.10α tile → the
  two-tier major layer is absent / below the legibility floor → clause reds. **This clause discharges W6-3**:
  the I.W6 S3 substrate-depth item exits J on a runtime assertion, never deferred again (`wave-I.W6.md §6`).
- **clause (h) — `proof:live-session` budget stays 0 across the re-skinned tree (the regression gate).** The
  full `proof:live-session` battery (inherited by reference, the structured allowlist — NEVER re-stated) runs
  on the post-W7a tree and accumulates budget == 0 across PLAY + SWITCH + DRAG. **BITE:** the re-skin must
  not introduce a `pageerror`/`_gen`/`"......"` regression (a re-skinned stage that throws on play REDs);
  greens when the suffusion is purely appearance, behavior-clean.

**The §spine bar — MUST bite.** Clauses (a)-(g) each read a COMPUTED product-facing property on the live page
(the resolved `--ball-tone` hue, the resolved display font-family, the measured mobile overlap area, the
mutating projected curve, the collapsed rail track, the rounded amiga radius, the legible substrate) — the
exact appearance facts the design lanes measured against the design intent, never a source-shape grep. Each is
born-RED on the pre-suffusion tree against a recorded screenshot defect and greens only when the named band
lands; each carries a SELF-CONTAINED plant-a-defect witness (the wrong-token consumer, the reverted title, the
restored overlap, the static projection, the restored 400px rail, the un-rounded canvas, the single-tile
substrate) so every clause can actually BITE. **The re-captured `proof:visual-lock` baseline lands WITH the
wave** (the close motion, never after) — the appearance-drift tripwire's self-baseline is honest by
construction because the re-capture is the authoring wave's last act (T3, `waves/README.md §3`).

**Three-tier taxonomy (the charter invariant, inherited).** Per the three-tier taxonomy (gate-ORACLE +
on-device, `J.md §invariants`, `waves/README.md §5`) and `proof:gate-is-runtime`, clauses (a)-(h) are ALL
CORRECTNESS-tier RUNTIME/INTERACTION clauses — each navigates the running product through `navToScene` and
asserts a product-facing COMPUTED property. This wave carries NO source-shape / static-lint clause; its GREEN
depends on the runtime clauses ALONE. **The on-device boundary (P6):** the per-finding COMPUTED-STYLE asserts
(hue token, font-family, border-radius, overlap geometry) are DEVICE-INDEPENDENT facts and HARD-gate;
ABSOLUTE-pixel appearance (cross-OS render) remains owned by `proof:visual-lock` (observe-only in CI per P6,
re-captured here) — the two are partitioned by the P6 boundary, not duplicated. `proof:gate-is-runtime` passes
for J.W7a because every §Hard clause is interaction-driven through `dist/gh-pages/`.

## §Folds (every J.md W7a-assigned fold item, with its evidence citation)

- **The protagonist deficit** (cross-hierarchy #3/#7/#8; `J.md §finding-cluster` "the design suffusion" →
  J.W7a) — S1: square's plate (SQ-3) + off-center (SQ-1); the amiga slab → rounded-glass register (XH-3) +
  sphere presence (A-02); subject sizing/centering (SEQ-02, SP-1); glass-wash on the floating controls pane
  (C2/C16); the mobile collisions (H3, TYP-1, A-01). **The mobile-overlap-==0 / dark-`--ball-tone`-contrast /
  ghost-rail-absent facts are J.W4's appearance-certification band** — W7a AUTHORS the cure, J.W4 CERTIFIES it
  (`J.W4.md §S1/§S3/§S5`).
- **The display-voice concentration** (TYP-2; cross-typography `:28-65`) — S2: the scene-identity display
  moments (TYP-2/C3/E3/SP-2/SEQ-01/MP-H2) via the PUBLISHED glass-ui poster rungs + `MetricBadge`/
  `AnimatedDigit` (verified 3.9.0); the readout promotion from 12px captions to confident mono displays; the
  amiga stays headerless (the binding exception). The unpublished `MetricHeader` is a W7b ABSTRACT edge.
- **The two-colour-system drift** (CP-1; cross-color-pops `:9-44`) — S3: the `--ball-tone` parameterization of
  `.progress-ball` mirroring `--badge-tone`; the per-scene hue map FROM the icon palette; Sequence rows onto
  the spectrum; the aquamarine literal DIES (SQ-5); readouts gain the scene accent; the colourful cube favicon
  + `theme-color`; the gold reserve rule.
- **The math-made-visible band** (cross-grid-math C1/C2/C5; `J.md §finding-cluster`) — S4: the easing stage's
  projected live bezier (E1/E2); the designed coordinate-grid stage fields (C1/MP-I1, kf-local token-clean,
  the glass-ui `<GraphFrame>` promotion RECORDED in the W7b ledger); FourierField adoption where befitting
  (published — a W7a consume); **W6-3 substrate depth folds into this stage-field band with a terminal
  verdict** (`wave-I.W6.md §6`, W6-3 FOLD).
- **The grammar unification** (XH-1/XH-2/XH-4; cross-hierarchy `:75-120`) — S5: the ghost rail dies (XH-1); the
  storyboard register converges (XH-2); the top-center band contract (XH-4); the playback affordance convergence.
- **W6-3 (the substrate-depth deferral, P1)** — HOMED in §S4 with a TERMINAL VERDICT (FOLD + legibility gate
  clause g); the I.W6 S3 item *"give the page glass a real thing to refract"* finally lands as part of the
  stage-field band, gated, never deferred again (`audit/wave-I.W6.md §6`, W6-3).

## §The W7b boundary (what W7a does NOT own — inv-16 honesty)

W7a owns EVERY kf APPEARANCE delta and the visual-lock baseline; W7b owns ZERO appearance delta and
re-captures NO baseline (`J.md §invariants` isomorphic-styling row; `waves/README.md §3`). The clean partition,
recorded so no band smuggles a W7b edge into W7a:

- **W7a CONSUMES the 3.9.0-published primitives** (verified on disk): the display poster rungs
  (`typography.css:141-237`), `MetricBadge`/`AnimatedDigit`/`FourierField`, `glass-wash`. These are PUBLISHED
  — consuming them IS W7a (a kf appearance delta).
- **W7b BOOKS the UNPUBLISHED edges** (ABSENT from 3.9.0 — recorded as `glassui-AX-handoff.md` rows, consumed
  only when AX publishes, inv-16): `MetricHeader` (the four stage headers → one primitive, cross-typography
  ABSTRACT); the `CurveEditorCanvas` / `GlassControlPoint` control-point primitive (glassui-abstract A1,
  cross-grid-math G1); the `<GraphFrame>` / `.bg-graph-paper` substrate utility (cross-grid-math G1/G3); the
  `GlassDock` reserved-band query (cross-hierarchy §handoff); the mono-caption case-preserving variant
  (cross-typography REFINE). W7a ships the kf-LOCAL token-clean field NOW and records the glass-ui promotion in
  the W7b ledger — never a kf-side patch of a sibling.

## §Design decisions (trade-offs RESOLVED)

- **The colour drift dies at ONE seam, not eight overrides — RESOLVED (the J.md headline).** The `--ball-tone`
  parameterization (one var defaulting to `--color-progress`, ≤8 one-line per-scene consumers) is the
  architectural transposition the mandate names — it mirrors the proven `--badge-tone` seam one section above
  in the same file (`design-idioms.css:436`). NOT eight per-scene CSS overrides (`J.md §MANDATE`, binding).
- **W7a assigns ONLY published primitives; unpublished ones are W7b edges — RESOLVED (inv-16).** Verified on
  disk: MetricBadge/AnimatedDigit/FourierField/the poster rungs/glass-wash SHIP in 3.9.0 → W7a consumes;
  MetricHeader/GraphFrame/the control-point primitive are ABSENT → W7b handoff. The graph-paper stage field
  is implemented kf-LOCAL token-clean now, with the glass-ui promotion booked in W7b — kf never patches a
  sibling.
- **The amiga stays headerless — RESOLVED (the binding exception).** Cross-typography names the amiga's
  silence as on-brand (`:169-172`); the display voice carries to the OTHER seven scenes; clause (b) ENFORCES
  the exception (reds if a title is planted on the amiga stage). The amiga DOES join the rounded-glass register
  (D2) and gains a present sphere (D3) — frame + subject, not type.
- **W6-3 substrate depth is FOLDED, not KILLed — RESOLVED.** The deferred I.W6 S3 item gets its terminal home
  in §S4: the substrate gains real two-tier graph-paper depth (so the glass has something to refract), gated
  by clause (g). The alternative (KILL) was rejected — the substrate depth is the load-bearing reason the glass
  reads flat/inert; it is a real legibility delta the design directive calls for, not a punt to retire.
- **The visual-lock re-capture is the wave's LAST act, never a separate motion — RESOLVED (T3).** A baseline
  captured by a later wave would certify drift it did not author (`waves/README.md §3`). The re-capture is the
  closing motion of the wave that authored D1–D23, so the appearance-drift tripwire's self-baseline is honest
  by construction. J.W7b re-baselines nothing.
- **Proportion is a RULE, not a vibe — RESOLVED.** Every D-item passes the calm-field/sharp-accent test; the
  anti-goals (amiga headerless, chrome calm, gold reserved, no watermark over live geometry, field amplifies
  only near the stage, the group-play disc the loudest note) are enumerated from the lane docs and BIND W7a.
  The directive's *"within a sense of proportion"* is enforced by the anti-goal register, not assumed.
- **Behavior FIRST, appearance SECOND — RESOLVED (the W2→W7a edge).** W7a follows J.W2 because re-skinning a
  stage whose drag seam / single-writer / spring-settle sheet is still mid-repair would force a double
  re-baseline (`waves/README.md §3`). The shared demo files land their BEHAVIOR (J.W2) before their
  APPEARANCE (W7a); the appearance-certification legs of J.W4 then certify the suffused result.
