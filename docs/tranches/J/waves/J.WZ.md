# J.WZ — THE CLOSE (the boundary FINAL held to inv ε · the prompt-recap terminal · the chronic-closure SUBSTRATE TRANSITION · the honest-minor cut + publish · the close-merge round-trip RE-observed · the T5 memory change)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** BOOK (the close docs —
  `docs/tranches/J/FINAL.md`) + **USER-DOMAIN** (the version cut + npm publish + the close-merge
  deploy — version owner **Mike Babb** `mike@babb.dev`, confirm-first) + **RECORD** (the T5
  memory narrowing — a user-visible change to `MEMORY/feedback_props_destructuring.md`). ·
  **Scope (docs + the substrate-transition wiring + USER-DOMAIN release; NO new behaviour
  source):** `docs/tranches/J/FINAL.md` (NEW — the J boundary close report) + the prompt-recap
  reconciliation (every MANDATE row + every fold row + the 10 PARTIAL lineage rows dispositioned)
  + the chronic-closure SUBSTRATE TRANSITION (the meta-gate's parse target moves
  `I/PROGRESS §"Open deferrals"` → `J/PROGRESS §"Open deferrals"` in ONE motion, the gate re-run
  proving non-vacuity on the new substrate) + the `changeset version` cut (the honest MINOR
  consuming both pending patches, per J.W5) + the npm publish (USER-DOMAIN, confirm-first) + the
  close-merge auto-deploy RE-observation (the J.W0 round-trip oracle re-witnessed on the close
  merge itself) + the AFTER screenshot capture (`scripts/capture.mjs` vs the J-open 48-shot
  corpus) + the T5 memory update + the W7b OPEN-edge sibling-HANDOFF ledger disposition rule. **ZERO
  `src/**` / `test/**` / `.github/**` / `demo/**` BEHAVIOUR edit** — the only non-doc motions are
  the chronic-closure parse-target re-point (a one-line `proof-chronic-closure.mjs` path constant,
  itself gated by the non-vacuity re-run) and the USER-DOMAIN `changeset version`/`npm publish`
  the wave AUTHORS the trigger for and the owner FIRES. · **DAG: LAST in J** — the close runs after
  every SHIP/DECIDE wave (J.W0–J.W7a/W7b) lands + green CI; it consumes `proof:all == CI`
  (J.W3), the OBSERVED green-CI→auto-deploy round-trip primitive (J.W0), the `proof:published-surface`
  GREEN (J.W5), and the re-captured visual-lock baseline (J.W7a). The close's own arbiter is the
  RUNNING product across every boundary J taught it to cross — never a prior FINAL.

**Title.** *The boundary-honest close. J extended the gate-ORACLE precept to every boundary the
product crosses — DEPLOY, PUBLISH, DOCS, AXES, the design language — closed the latent seam
classes I left half-repaired, industrialized the estate to net-deletion, terminated every
≥4-tranche rider, and suffused the design from the doorway through every pane. The close writes
the J FINAL held to inv ε (each boundary claim cites its OBSERVED oracle), dispositions every
MANDATE row and every fold row and the 10 PARTIAL lineage rows to a terminal verdict,
TRANSITIONS the chronic-closure parse substrate from I's board to J's in one non-vacuous motion,
RE-observes the J.W0 auto-deploy round-trip on the close merge itself, books the USER-DOMAIN
honest-minor cut + publish, captures the AFTER screenshot corpus against the J-open 48 shots, and
records the T5 memory narrowing as a user-visible change.*

This is the §MANDATE's **P-invariant close** at the boundary scale: every carry exits with a
terminal disposition, no perpetual punt survives a fifth tranche, and inv ε is mechanical — the
FINAL may not overclaim because the close's own evidence is the boundary battery GREEN
(`proof:all == CI` GREEN where `proof:all` IS the CI roster; the close-merge auto-deploy OBSERVED;
the tarball passes `proof:published-surface`; the axes legs bite on planted defects; the
re-skinned tree holds the live-session budget at 0). The catastrophe I exists to end was a FINAL
that overclaimed at the product surface; the catastrophe J exists to end is the SAME overclaim
shape displaced one boundary out — a FINAL whose deploy/publish/docs/axes claims could not be
true when written (`final-vs-tree-inv-epsilon.md` INVE-1/INVE-2). The J FINAL is held to the
arbiter J installed: the running product at every boundary, OBSERVED.

## §Provenance (the close mandate + the two inv-ε FINAL gaps J exists to cure)

- **`J.md §WAVE MAP · J.WZ`** — the close's owned items, verbatim: *"FINAL.md (held to inv ε —
  the boundary claims must each cite their observed oracle); the prompt-recap (this charter's
  MANDATE + every fold row dispositioned); the chronic-closure SUBSTRATE TRANSITION (the
  meta-gate's parse target moves from I's PROGRESS to J's ledger in the same motion the J ledger
  becomes authoritative); the changeset version cut + npm publish (USER-DOMAIN, Mike Babb,
  confirm-first — the honest minor); the T5 memory update; the auto-deploy round-trip RE-observed
  on the close merge."* The §Hard gate the charter names: *"`proof:all` GREEN where proof:all ==
  CI; the close merge's own CI run auto-deploys (the J.W0 oracle, re-witnessed on the close
  itself); the published tarball passes `proof:published-surface`; the J ledger terminal (zero
  un-dispositioned rows)."*
- **`J.md §ENFORCEMENT (inv ε)`** — the close's binding standard: *"J's FINAL is held to the same
  standard I's was — and the two inv-ε gaps the audit found in I's FINAL (the deploy-mechanism
  claim that could not be true when written; the breadth-of-human-surface claim) are exactly what
  J.W0 and J.W4 exist to make true."* The close CITES J.W0's observed round-trip and J.W4's
  planted-defect axes battery as the cures, not a re-assertion.
- **`final-vs-tree-inv-epsilon.md` INVE-1 / INVE-2** — the two named gaps in I's own FINAL the J
  close must explicitly CURE-or-CARRY: INVE-1 (the "merge → green CI → CF auto-deploys" claim was
  STRUCTURALLY IMPOSSIBLE when written — ci.yml YAML-invalid since H; the deploy was a manual
  `wrangler` bypass) → CURED by J.W0's OBSERVED auto-deploy round-trip, RE-witnessed on the J.WZ
  close merge (clause (c)); INVE-2 (the headline correctness claim was desktop-1440-mouse-light-only)
  → CURED by J.W4's mobile/touch/reduced-motion/dark/keyboard battery. The J FINAL records each as
  CURED with its observed oracle, never CARRIED-silent.
- **`wave-I.WZ-postclose.md §E`** — the changeset/SemVer facts the close consumes: two PENDING
  changesets both `patch`; `package.json` version `4.1.0`; changesets COALESCE same-type bumps
  (`patch + patch → 4.1.1`, not 4.1.2); the I changeset's own body flags the unresolved
  patch-vs-minor decision (the value.js `^0.11.1 → ^0.11.2` floor advance is minor-worthy);
  `release.yml` triggers on `push: tags: v*.*.*` INDEPENDENT of the `ci` workflow but has **never
  run a single time**. The honest MINOR consuming both patches is J.W5's authored changeset; the
  CUT + PUBLISH are this wave's USER-DOMAIN motion.
- **`wave-I.WZ-postclose.md §G`** — the supersession-coherence pattern to AVOID: I's close docs
  CONTRADICTED each other on the `d469e69` disposition (`I.WZ.md:90` `DEFERRED-BY-USER` vs
  `FINAL.md:244` `SUPERSEDED-BY-FIX-SHIP`) — a two-narrative drift the I tranche was founded to
  forbid, benign only because the tree disambiguated. **The J close MUST be single-voiced:** the J
  FINAL is the sole authoritative narrative; the J wave specs and `J/PROGRESS.md` are frozen DEV
  artifacts whose dispositions the FINAL may supersede, and where it does, the FINAL says so
  EXPLICITLY (a pointer, not a silent re-write) — never leaving two live narratives a future
  reader must adjudicate against the tree.
- **`docs/tranches/I/FINAL.md`** — the close-doc shape to MATCH-and-extend (the I FINAL records
  what LANDED with `file:line` + gate evidence across I.W0–I.W7, numbered §§, the catastrophe-it-
  ended framing, the chronic ledger re-verified live). The J FINAL extends it from the PRODUCT
  surface to the FIVE boundaries — the same evidence discipline, pointed outward.
- **`J.md §chronic fold` USER-DOMAIN band + `J/PROGRESS.md §4f`** — the version owner is **Mike
  Babb (`mike@babb.dev`)**, confirm-first; the cut is the honest MINOR off 4.1.0 consuming both
  pending changesets (H's patch subsumed — never published); the npm publish and the close-merge
  deploy are USER-DOMAIN at this wave.
- **MEMORY `feedback_props_destructuring.md`** (the T5 narrowing target) + **`J.md` T5** /
  **`J/PROGRESS.md §3` props-destructure row** — the rule NARROWS to its true kernel (only a
  destructured prop passed INTO a composable is gated; template/watchEffect destructure is
  platform-idiomatic post-Vue-3.5 and no longer policed). The memory file is updated AT J close
  — a USER-VISIBLE memory change, flagged here.

## §The state, verified (file:line / command / live anchors — re-checked first-hand on `tranche-j-dev`)

The load-bearing facts for the close, each re-checked against the tree at `4072af9`-base:

1. **`docs/tranches/J/FINAL.md` does NOT yet exist** — it is the close report this wave authors
   (after the SHIP/DECIDE waves land). The J tranche dir carries `J.md` + `PROGRESS.md` +
   `PATH-FORWARD.md` + `audit/` (32 systemic + `design/` 14 lanes + 48 screenshots) +
   `waves/J.W{0,3,4,5,6}.md` on disk (`find docs/tranches/J/waves -name '*.md'`) + the in-flight
   `J.W{1,2,7a,7b,WZ}.md` (this file), but no `FINAL.md`. The I tranche's `FINAL.md` is the shape
   exemplar (numbered §§, `file:line` + gate evidence per wave, the chronic ledger re-verified
   live).

2. **The chronic-closure parse substrate is STILL `I/PROGRESS.md` — verified, and the transition
   is the close's named motion.** `J/PROGRESS.md §"Open deferrals"` carries the binding
   SUBSTRATE-TRANSITION NOTE (`PROGRESS.md:213-223`): *"Until J.WZ, this section is the
   AUTHORED-but-not-yet-active substrate; `I/PROGRESS.md §"Open deferrals"` remains the
   authoritative parse target for `proof:chronic-closure` through the J development + implementation
   phases. The substrate TRANSITION happens at J.WZ: the meta-gate's parse target moves from I's
   PROGRESS to THIS J ledger in the SAME motion the J ledger becomes authoritative."* The J ledger
   carries the machine-readable Chronicity-integer column shape the gate parses (`PROGRESS.md:225-237`).
   This wave EXECUTES the transition + proves non-vacuity (S3).

3. **The version freeze is REAL — `4.1.0`, never published, the honest minor AUTHORED by J.W5.**
   `package.json:3` `"version": "4.1.0"`; `npm view @mkbabb/keyframes.js version` → `4.1.0`;
   `git tag -l 'v*'` tops at `v4.1.0`; `release.yml:21 tags: v*.*.*` (tag-triggered, INDEPENDENT of
   the dead `ci` workflow) has NEVER fired (`wave-I.WZ-postclose.md §F(g)`). J.W5 AUTHORS
   `.changeset/tranche-j.md` (`minor`, naming the E→I orchestration tier export-by-export, the two
   patches consumed/deleted in the same motion — `J.W5 §S5`). This wave runs the CUT + PUBLISH
   (USER-DOMAIN, confirm-first).

4. **The close-merge auto-deploy round-trip is the J.W0 oracle, RE-observed.** `deploy-pages.yml:42-46`
   auto-fires iff `workflow_run.conclusion == 'success' && head_branch == 'master' && event ==
   'push'`; the J.W0 §Hard gate clause (c) requires the observed deploy run's `event_name` read
   `workflow_run` with `workflow_run.event == 'push'` — the AUTO arm, NOT a `workflow_dispatch`
   substitute (`J.W0.md:390-413`). The CLOSE MERGE is itself a real master push: this wave
   RE-witnesses the same round-trip on the close merge's own CI run (`J.md` J.WZ row, `J.W0
   §Hand-off → J.WZ`).

5. **`scripts/capture.mjs` is the checked-in 48-shot corpus engine.** `ls scripts/capture.mjs` →
   present; the J-open audit corpus is 48 screenshots (8 scenes × 3 viewports × 2 panel states,
   zero console errors — `J.md §finding-cluster ledger`, `audit/design/*.md`). The AFTER capture
   re-runs `scripts/capture.mjs` on the post-W7a re-skinned tree and diffs the corpus against the
   J-open 48 shots (S5) — the visual record that the design suffusion LANDED and the isomorphic
   exceptions are the ONLY deltas.

6. **The T5 memory narrowing target exists.** MEMORY `feedback_props_destructuring.md` carries the
   un-narrowed rule (*"NEVER destructure defineProps() in script setup"*); `J.md` T5 +
   `J/PROGRESS.md §3` (the props-destructure row: *"UNPOLICED + 6 live Vue-3.5 sites — NARROWED to
   its true kernel in J … only a destructured prop passed INTO a composable is gated; template/
   watchEffect destructure is platform-idiomatic post-3.5"*) own the narrowing. This wave updates
   the memory file — a USER-VISIBLE change, flagged.

## §Goal

Close Tranche J with a FINAL that tells the truth at every boundary it crossed — proven, not
asserted — and a terminal ledger with ZERO un-dispositioned rows. The boundary FINAL is held to
inv ε: each of the five boundary claims (DEPLOY / PUBLISH / DOCS / AXES / DESIGN) cites its
OBSERVED oracle, and the two inv-ε gaps the audit found in I's own FINAL are each CURED-or-CARRIED
explicitly. The prompt-recap dispositions EVERY MANDATE row, EVERY `J/PROGRESS.md` fold row, and
the 10 PARTIAL lineage rows to a terminal verdict. The chronic-closure substrate TRANSITIONS from
I's board to J's in ONE motion with the gate re-run proving it BITES on the new substrate (no
vacuous-green hand-off). The honest minor is CUT and PUBLISHED (USER-DOMAIN, confirm-first), the
close-merge auto-deploy round-trip is RE-observed (the J.W0 oracle on the close itself), the AFTER
screenshot corpus is captured and diffed against the J-open 48, the T5 memory rule is narrowed to
its composable-boundary kernel (a user-visible change), and the W7b OPEN-edge disposition rule is
recorded so no un-consumed AX edge becomes a perpetual punt. Six moves, each at the gestalt
altitude the mandate demands; nothing rides a fifth tranche; no boundary is certified by hand, by
paperwork, or at one viewport.

## §Scope

- **S1 — `docs/tranches/J/FINAL.md` (the boundary-honest close report; inv ε mechanical).** The
  authoritative J close. Records what LANDED across J.W0–J.W7a/W7b with `file:line` + gate
  evidence, structured by the FIVE boundaries (matching and extending the I FINAL's numbered-§
  shape from the PRODUCT surface outward):
  - **The DEPLOY boundary (J.W0).** The `navToScene` per-EXPECTED-state primitive landed in
    `scripts/lib/demo-driver.mjs`; both `scene-control-dfa` + `scene-transition-perf` migrated onto
    it; the dock trigger projection made born-correct from the DFA (the I.W2 single-authority
    extended to the control-surface projection); the 8-commit post-close tail formally adopted with
    its terminal disposition table. **inv-ε evidence — the OBSERVED oracle, not the assertion:**
    the green-CI → `deploy-pages.yml` auto-fire → live-bytes round-trip OBSERVED end-to-end on the
    Linux runner (the recorded CI run id, the deploy run's `event_name == 'workflow_run'` /
    `workflow_run.event == 'push'`, the served-build hash); born-RED on CI run `27228309606`'s
    `trigger='null'`-under-load shape before the cure. **This is the INVE-1 cure recorded with its
    observed witness** — the I FINAL's structurally-impossible deploy claim becomes TRUE because
    the AUTO round-trip is OBSERVED, never asserted (`J.W0 §Hard gate clause (c)`,
    `final-vs-tree-inv-epsilon.md` INVE-1).
  - **The PUBLISH boundary (J.W5).** `proof:published-surface` GREEN (the tarball surface == the
    source's public exports == the README's taught API == the runtime `loadAnimationEngine`
    surface); `_redirects` out of the tarball at the `publicDir: false` seam; the README §Beyond
    CSS grown from 4 to the full ~13-primitive roster with every taught snippet EXECUTING against
    the built dist. **inv-ε evidence:** the gate witnessed born-RED on the CURRENT tree (the
    `_redirects` leak + the 9 untaught exports red it today — `J.W5 §Hard gate`), GREEN only when
    the tarball, the exports, and the README agree. The honest minor cut + published (S4).
  - **The DOCS boundary (J.W5).** The three CLAUDE.md files REWRITTEN to the tree (the real
    28-module `src/animation`, the real demo dirs, the real 69-file/685-test count, the
    static/dynamic `loadAnimationEngine` boundary) — never annotated; the executable-README runner
    is the runtime DOCS oracle. **inv-ε evidence:** each structural claim verifiable against the
    tree (the doc-rot tripwire corroborates; the executable snippets are the runtime oracle).
  - **The AXES boundary (J.W4).** `proof:live-session` taught the human axes — mobile (390×844,
    touch), reduced-motion (`emulateMedia`), dark, keyboard/focus, the widened scene sweep; CH-3
    re-certified on a MOBILE oracle (`sheet.bottom ≤ menubar.top` ON the 390×844 viewport);
    `proof:lighthouse-mobile` given a tier under its P6 posture. **inv-ε evidence — the breadth
    claim made true:** each new leg witnessed born-RED-able against a PLANTED defect, then the full
    battery GREEN with budget 0 per leg. **This is the INVE-2 cure recorded with its observed
    witness** — the headline correctness claim no longer means "a desktop-1440-mouse-light human";
    it means a human on a phone, in the dark, with motion stilled, on a keyboard
    (`J.W4 §Hard gate`, `final-vs-tree-inv-epsilon.md` INVE-2).
  - **The DESIGN boundary (J.W7a ∥ J.W7b).** The protagonist made unambiguous on every stage; the
    Instrument Serif display voice suffused from the doorway through every pane; the `--ball-tone`
    parameterization (every subject keeps its icon's hue); the easing stage projecting its own
    bezier; the ghost rail dead, the storyboard grammar unified; the consume-to-delete glass-ui
    edges; the 46-item glass-ui HANDOFF ledger complete. **inv-ε evidence:** the per-finding RUNTIME
    asserts on the live page (computed `--ball-tone` == icon hue per scene; mobile hero/subject
    overlap == 0 px; the ghost rail ABSENT; the amiga rounded-glass computed style); the
    re-captured visual-lock baseline lands WITH J.W7a; the live-session budget stays 0 across the
    re-skinned tree. The AFTER corpus (S5) is the visual record.
  - **The seam-totality + estate + terminations layer.** The one serialization authority
    (J.W1 ENG-1); the total guards + the unit pyramid restored (J.W1); the B6 drag seam at zero
    residue + the `selectedControl` single-writer + the `AnimationMenuBar → TransportDock` rename
    (J.W2); the estate collapsed onto the lib under the net-deletion rule (J.W3 — gate COUNT and
    estate LoC measured strictly DOWN, the before/after numbers RECORDED); every ≥4-tranche rider
    EXITED via a measurement artifact or a reasoned KILL (J.W6 — the close ledger carries ZERO rows
    tagged MEASURE-FIRST without a measurement).
  - **inv ε is MECHANICAL at the close (the §Design-decisions binding):** the J FINAL's evidence is
    the boundary battery GREEN on the fixed tree — `proof:all == CI` GREEN, the close-merge
    auto-deploy OBSERVED, `proof:published-surface` GREEN, the axes legs biting on planted defects,
    the live-session budget 0 on the re-skinned tree. NO chain-of-trust over a prior FINAL; the
    catastrophe proves that is worthless. The close CANNOT overclaim because its oracle is the
    running product at every boundary, OBSERVED — and where J could not make a boundary fully true
    (a CARRIED row), it says so with the carry's named mechanism, never a silent gap.
  - **The single-voice discipline (the §G supersession-coherence pattern to AVOID):** the J FINAL
    is the SOLE authoritative narrative. Where it supersedes a frozen DEV-phase disposition in a J
    wave spec or `J/PROGRESS.md`, it says so EXPLICITLY (a pointer to the FINAL's §), never leaving
    two live narratives. The exact two-narrative drift `wave-I.WZ-postclose.md §G` caught in I's
    close (`I.WZ.md` `DEFERRED-BY-USER` vs `FINAL.md` `SUPERSEDED-BY-FIX-SHIP`) is forbidden here:
    one tranche, one authoritative close voice.

- **S2 — the prompt-recap reconciliation (every MANDATE row + every fold row + the 10 PARTIAL
  lineage rows — terminal).** The close reconciles THREE strata to a terminal disposition, none
  dropped:
  - **Every charter MANDATE row** (`J.md §The MANDATE` — the verbatim development directive + the
    design directive + the precept spine): the deep parallel audit RAN (32 systemic + 14 design
    lanes); NO quick solutions / NO workarounds is HELD (the scene-control-dfa flake died via the
    per-expected-state predicate + the born-correct projection, NOT a longer settleMs /
    continue-on-error / IN_CI escape — `J.W0 §No-workaround prohibitions`; the publish gap closed
    with the honest minor + the real documented surface, NOT a rubber-stamp patch; the
    subject-palette drift died at the `--ball-tone` parameterization seam, NOT 8 per-scene
    overrides; the doc-rot died by REWRITING to the tree, NOT annotating); NO legacy beside its
    replacement is HELD (the 43 serveDist copies died WITH the consolidation; `proof:repin-safe`
    KILLED; stale docs rewritten); architectural transpositions for ELEGANCE/SIMPLICITY/PERFORMANCE
    landed at the right seams (the lib harness, `navToScene`, the `--ball-tone` seam, the serializer
    unification, the Three.js tree-shake — measure-first where perf-claimed); MEASURE-FIRST is HELD
    (every rider exited with a measurement artifact or a reasoned KILL); inv-16 is HELD (glass-ui/
    value.js/parse-that consumed PUBLISHED; every glass-ui item a HANDOFF or a published consume-edge,
    never a kf-side patch; the permanent engine rule honored — `src/animation` in scope when
    correctness/elegance required); isomorphic styling is HELD except the named J.W7a deltas
    (deliberate, enumerated, re-baselined in the same motion; J.W7b owns NO appearance delta).
  - **Every `J/PROGRESS.md §"Open deferrals"` fold row** — each of the bands §4a–§4i carries its
    closure cell citing the gate/evidence; the close confirms every row reached a terminal verdict
    (FOLD landed via its wave; RE-AFFIRM not re-litigated; VERIFY-ONLY re-run; HANDOFF paired with a
    born-RED kf gate or a published consume-edge; BOOK with a terminal home; RECORD historical; KILL
    permanent; USER-DOMAIN fired). The net-new `scene-control-dfa` chronic the I FINAL could not
    enumerate is closed P0 via J.W0's observed round-trip (`J/PROGRESS.md §2` net-new row).
  - **The 10 PARTIAL/GATED lineage rows** (`J/PROGRESS.md §4g`, `audit/recap-{AB,CD,EF,GH,I-session}.md`
    — 55/65 ADDRESSED, 10 PARTIAL) — each RESOLVED with its J home or sibling gate, enumerated to a
    terminal verdict: **CD-1** (`AnimationMenuBar → TransportDock` — the D FINAL overclaim) →
    J.W2 EXECUTED (grep 0 for the old name); **the CI-on-Linux follow-up** → J.W0 (the observed
    round-trip); **the changeset/publish** → J.W5 authored / J.WZ cut+published (this wave);
    **F.W8 composition-honoring** → the FB-1 BOOK re-affirmed (SHIP-if-elected); **the four
    sibling-gated rows** (VJ-1..9 next-slice / PT-1 packrat / FB-3 MorphSVG / the glass-ui ledger)
    stay HANDOFF with their born-RED kf gates or published consume-edges — never re-opened as kf
    work. **No prompt is dropped; every PARTIAL has a J home or a sibling gate** (`J.md §chronic +
    deferred fold`). The standing development mandate + the standing orchestration directive
    RE-ENTERED J's charter VERBATIM (the durable project spine, not I-specific).
  - **WHY a recap, not a tally:** the catastrophe was a FINAL that asserted "all requests resolved"
    against a proxy. The J recap reconciles each row to a TERMINAL verdict with its gate/evidence —
    the recap is itself held to inv ε.

- **S3 — the chronic-closure SUBSTRATE TRANSITION (one motion · the gate re-run proving
  non-vacuity on the new substrate).** The meta-gate `proof:chronic-closure` parses a single
  authoritative `§"Open deferrals"` ledger. Through J's dev + impl phases the authoritative
  substrate is `I/PROGRESS.md §"Open deferrals"` (`J/PROGRESS.md:213-223` SUBSTRATE-TRANSITION
  NOTE). At THIS close, in ONE motion:
  - **The parse target moves `I/PROGRESS.md` → `J/PROGRESS.md` §"Open deferrals"** — the single path
    constant in `proof-chronic-closure.mjs` re-points to the J ledger, which becomes authoritative
    in the SAME motion. The J ledger carries the machine-readable Chronicity-integer column
    (`J/PROGRESS.md:225-237`): every row leads with an INTEGER tranche-span the gate reads, the
    ≥4-tranche EXIT-ONLY mandate (P-invariant-28) enforced mechanically off that integer.
  - **The transition is itself GATED — non-vacuity proven on the new substrate (the binding bite).**
    Per the chronic-closure meta-invariant (a closure must cite a runtime/boundary gate that BIT; a
    HANDOFF row may target ONLY a PUBLISHED version or a kf-owned consume-edge, never a future
    version number / unreleased commit — the B7 vaporware lesson), the gate is RE-RUN against the J
    ledger and MUST BITE: a deliberately-malformed probe row (a FOLD row citing a SOURCE-SHAPE gate;
    a HANDOFF row targeting an UNPUBLISHED version; a ≥4-tranche row with no EXIT disposition) is
    planted and the gate REDs on it — proving the J substrate is parsed and policed, not a
    rubber-stamp swap. Then the planted row is removed and the gate GREENs on the true J ledger.
    **The born-RED witness:** the gate RED on the planted malformed J-ledger row BEFORE the
    transition is accepted; GREEN on the clean J ledger after. **NO vacuous transition** — a swap
    that re-points the path without re-proving the gate bites on the new substrate is the exact
    paperwork-auditor failure mode the meta-gate exists to forbid (the I FINAL's
    `proof:chronic-closure` was itself once a source-shape gate auditing a markdown table — the
    deepest expression of the blindspot; the J transition proves the NEW substrate is policed by a
    biting gate, not re-asserted).
  - **WHY in the close, not earlier:** the J ledger is AUTHORITATIVE only when J's waves have
    landed and their closures are real (each cell cites a gate that BIT on a defect tree). Moving
    the substrate before the closures are real would make the gate parse aspirational rows — the
    transition is correctly the LAST motion, in lockstep with the J ledger becoming authoritative
    (`J.md §invariant set` — the chronic-closure meta-invariant; `J/PROGRESS.md §6` — *"the J ledger
    becomes the parse target in the SAME motion it becomes authoritative"*).

- **S4 — the version cut + npm publish (USER-DOMAIN · Mike Babb · confirm-first · the honest
  minor).** J.W5 AUTHORED `.changeset/tranche-j.md` (`minor`, the E→I orchestration tier named
  export-by-export, both pending patches consumed/deleted in the same motion — `J.W5 §S5`). This
  wave runs the CUT + PUBLISH, USER-DOMAIN, confirm-first:
  - **The cut:** `changeset version` consumes the single `tranche-j` minor → `package.json`
    `4.1.0 → 4.2.0` (the honest minor: the E→I additions are purely additive new exports ⇒ minor,
    and the value.js `^0.11.1 → ^0.11.2` floor advance is a contractual tightening minor honestly
    signals — `J.W5 §S5`, `wave-I.WZ-postclose.md §E(g)`, `scope-adversary §4`). The two `patch`
    changesets are already consumed (J.W5 deleted `.changeset/tranche-{h,i}.md` into the minor —
    leaving them would stack `4.1.0 → 4.1.1 → 4.1.2 → 4.2.0` and re-paper the dishonesty the wave
    closed). H's patch is SUBSUMED (H never published).
  - **The publish (PRE-CONDITION — the boundary gates GREEN first):** the publish fires ONLY after
    `proof:all == CI` is GREEN, `proof:published-surface` is GREEN on the cut tree (the tarball is
    `4.2.0`, `_redirects`-free, the d.ts complete, the README executable), and the close-merge
    auto-deploy round-trip is OBSERVED (S6). The publish leg rides `release.yml` (tag-triggered:
    `check:lib → build:lib → test → proof:boundary → npm publish --provenance` — `release.yml:21`,
    INDEPENDENT of the `ci` workflow, never run before this) on the pushed `v4.2.0` tag. **This is
    the first time `release.yml` runs** — the close RECORDS the run id + the published version as
    the OBSERVED publish oracle (the PUBLISH boundary's round-trip, the analogue of the deploy
    round-trip).
  - **The release.yml-gates-on-correctness decision (RECORD + decide — `J/PROGRESS.md §4d`):**
    `release.yml` re-runs the library-scoped `proof:boundary`, NOT the full `proof:correctness`
    suite (defensible — the published library surface is glass-ui-free). The close RECORDS the
    decision: the library tarball is correct by `proof:boundary` + the `ci` workflow's
    `proof:all == CI` having gated the merge that produced the tagged tree; whether `release.yml`
    SHOULD additionally gate on `proof:correctness` is a recorded USER-DOMAIN decision, not a silent
    gap (`wave-I.WZ-postclose.md §F(g)`).
  - **USER-DOMAIN, confirm-first — NO autonomous registry mutation.** The version owner is **Mike
    Babb (`mike@babb.dev`)**; the close AUTHORS the cut trigger + the publish pre-conditions and
    PRESENTS them; the owner FIRES `changeset version` + the `v4.2.0` tag push, confirm-first
    (`J.md §chronic fold` USER-DOMAIN, `J/PROGRESS.md §4f`). The wave's deliverable is the honest
    cut + the gated publish, not an autonomous npm push.

- **S5 — the AFTER screenshot capture (`scripts/capture.mjs` vs the J-open 48-shot corpus · the
  design-suffusion visual record).** Re-run `scripts/capture.mjs` (the checked-in corpus engine,
  zero-console-error harness — `J.md §finding-cluster ledger`) on the post-W7a re-skinned tree to
  capture the AFTER corpus (8 scenes × 3 viewports × 2 panel states = 48 shots, the same matrix as
  the J-open corpus under `audit/design/screenshots/`). DIFF the AFTER corpus against the J-open 48:
  - **The expected deltas ARE the named isomorphic exceptions** (`J.md §invariants` — *"the named
    J.W7a design deltas, which are DELIBERATE, enumerated, befitting appearance changes"*): the
    plated stage grammar, the `--ball-tone` per-subject hue, the Instrument Serif display register
    at the scene-identity moments, the easing stage's projected bezier, the dead ghost rail, the
    amiga rounded-glass register, the mobile hero/subject overlap resolved. EVERY delta is one J.W7a
    named in its wave spec; an UNNAMED delta (a regression outside the enumerated suffusion) is a
    RED — the AFTER corpus is the visual proof the suffusion is the ONLY change, the isomorphic-
    exception discipline made visible.
  - **The re-captured visual-lock baseline (J.W7a's own close motion) is the gate-side companion;
    this corpus is the human-readable record** — the AFTER shots are committed alongside the J FINAL
    as the design boundary's evidence (the design language suffused from the doorway through every
    pane, OBSERVED, not asserted). W7b changes no kf appearance and contributes NO delta to the
    corpus (it re-captures NO baseline — `J.md` J.W7b, `J/PROGRESS.md §1`).
  - **WHY the capture at the close:** the design directive's success criterion is the RENDERED
    RESULT measured against the design intent (`J/PROGRESS.md §2` design row — *"the rendered RESULT
    was never measured against the design intent"*). The AFTER-vs-48 diff is that measurement,
    closing the design boundary with an observed visual oracle rather than prose.

- **S6 — the close-merge auto-deploy round-trip RE-observed (the J.W0 oracle on the close itself).**
  The J close merge to `master` is itself a real master push — the EXACT event the J.W0 §Hard gate
  clause (c) certifies. This wave RE-witnesses the round-trip on the close merge's own CI run:
  - **The observed chain:** the close-merge push → the `ci` workflow concludes `success` (BOTH
    `gates` and `demo-smoke` GREEN on the Linux runner, `scene-control-dfa` included via
    `navToScene`, ZERO escapes, the ~60-gate tail running to completion within the validated
    wall-clock) → `deploy-pages.yml`'s `workflow_run`-on-`push` arm evaluates true (`:42-46`) → the
    `deploy` job runs `scripts/pages-deploy.sh` → the live site (keyframes.babb.dev) serves the
    close-merge `head_sha`'s bytes. **RECORD the CI run id, the deploy run's TRIGGER
    (`event_name == 'workflow_run'`, `workflow_run.event == 'push'` — the AUTO arm, NOT a
    `workflow_dispatch` substitute), and the served-build hash** (`J.W0 §Hard gate clause (c)`,
    §No-workaround prohibitions — NO `workflow_dispatch` substitute, NO manual `wrangler` bypass).
  - **WHY re-observe on the close merge:** J.W0's first observed round-trip proved the chain works
    on A master push; the close-merge re-observation proves the J CLOSE itself ships through the
    honest auto-chain — the boundary the I FINAL claimed-but-could-not-actuate (INVE-1) is true OF
    THE J CLOSE, not merely demonstrated once earlier. This is the deploy boundary's terminal
    inv-ε observation: the certified bytes are the bytes the live site serves, OBSERVED on the very
    merge that closes the tranche.

- **S7 — the T5 memory update (the props-destructure rule narrowed to its composable-boundary
  kernel · a USER-VISIBLE memory change · flagged).** Update MEMORY
  `feedback_props_destructuring.md` (and the auto-memory `MEMORY.md` pointer) to the NARROWED rule
  (`J.md` T5, `J/PROGRESS.md §3`):
  - **The narrowing:** the rule was *"NEVER destructure defineProps() in script setup"* — a
    pre-Vue-3.5 blanket. The TRUE kernel: **a destructured prop passed INTO a composable loses
    reactivity; THAT is gated.** Template destructure and `watchEffect`/computed-body destructure
    are platform-idiomatic post-Vue-3.5 (reactive-props-destructure is a stable language feature)
    and are NO LONGER policed. The 6 live Vue-3.5 destructure sites the audit found
    (`audit/precepts.md §2-F3`) are idiomatic and correct; only the composable-passed prop is the
    hazard.
  - **The USER-VISIBLE flag:** this is a CHANGE to a standing memory rule the user relies on across
    conversations — flagged HERE as a user-visible memory change (per the J charter's standing
    memory rules + T5: *"the memory file is updated at J close (user-visible memory change, flagged
    at WZ)"* — `J.md` T5, §invariant set). The memory edit is the close's motion; the close FINAL
    records it as a RECORD-tier disposition (the rule's narrowing is permanent, reasoned, and
    user-facing).
  - **WHY at the close:** the narrowing is a J-resolved precept tension (T5 — `audit/precepts.md
    §3`); resolving it mid-tranche risks a stale rule beside its replacement (no-legacy applies to
    memory too). The close is the single motion that lands the narrowed rule with the tranche's
    full justification on the record.

- **S8 — the W7b OPEN-edge disposition rule (each un-consumed AX edge exits as a sibling-HANDOFF
  ledger row consumed by the next glass-ui re-pin — a named mechanism, never a perpetual punt).**
  J.W7b authored `glassui-AX-handoff.md` (the 25 REFINE + 21 ABSTRACT glass-ui items, each
  dispositioned with its evidence anchor + the consuming kf seam named — `J.md` J.W7b,
  `J/PROGRESS.md §1`). At the close, the AX edges fall into two states, and the close RECORDS the
  binding disposition rule for the OPEN ones:
  - **CONSUMED-this-tranche:** the consume-to-delete edges whose published glass-ui version arrived
    within J — each deleted kf hand-rolled surface + its consumed published primitive landed in ONE
    motion (the hand-rolled-surface-GONE grep is the hygiene corroborator — `J.W7b` boundary
    oracle). These are TERMINAL (RE-AFFIRM / consumed).
  - **OPEN-edge (un-consumed at close — the disposition RULE):** each AX edge whose glass-ui
    publish did NOT land within J exits as a **sibling-HANDOFF ledger row in `J/PROGRESS.md
    §"Open deferrals"`** with its NAMED consuming mechanism: *consumed by the NEXT glass-ui re-pin*
    — the kf-side consume-edge is specified (the published primitive name, the kf surface it
    replaces, the grep that proves the hand-rolled surface gone the instant the consume lands), and
    the row targets the published consume-edge, NEVER a future version number / unreleased commit
    (the B7 vaporware lesson; the chronic-closure meta-invariant). **This is NOT a perpetual punt:**
    the row carries a NAMED mechanism (the next re-pin's consume-edge) and a born-RED-able kf gate
    (the grep flips the instant the publish lands), so a future reader sees exactly HOW and WHEN the
    edge closes — not "deferred again." The OPEN edges are inv-16 sibling-coordination (kf consumes
    only what AX PUBLISHES, never a kf-side patch); they ride the next re-pin as a STRUCTURED
    handoff, the same band as VJ-1..9 (value.js next-slice, chronic-by-design, rides the next
    re-pin) — a named, terminal-at-the-next-edge disposition, P-invariant-28-compliant.
  - **WHY a RULE, not a row-by-row punt:** the design directive's glass-ui half is genuinely
    cross-repo (inv-16); some AX publishes will not land within J's window. The close's duty is to
    make EACH open edge a NAMED sibling-HANDOFF with its consuming mechanism — the opposite of the
    perpetual punt P-invariant-28 forbids — so no AX edge becomes an un-rowed "we'll get to it"
    (the exact shape the J audit caught in the ≥4-tranche riders).

## §Hard gate (the close's terminal oracle — the boundary battery GREEN, OBSERVED · zero un-dispositioned rows)

The close's §Hard gate is NOT a new gate — it is the COMPOSITION of the J waves' boundary oracles,
re-witnessed on the close tree + the close merge, plus the substrate-transition non-vacuity bite.
The close is GREEN iff ALL of the following hold, each citing its OBSERVED oracle (inv ε
mechanical — the close cannot overclaim because its arbiter is the running product at every
boundary):

- **clause (a) — `proof:all == CI` GREEN, where `proof:all` IS the CI roster (CORRECTNESS).** The
  full `proof:all` suite GREEN, and the J.W3 two-way `proof:ci-coverage` equivalence proves
  `proof:all` == the CI gate roster both ways (no gate hard-gates CI without a named tier; no tier
  gate absent from CI). **BITE:** reds if any correctness gate reds, OR if the two-way equivalence
  finds a roster mismatch. **The orphaned deploy-gating gates can no longer hide** — they entered a
  tier in J.W3 (`J.W3` estate motion, `wave-I.WZ-postclose.md §E`).
- **clause (b) — the close-merge auto-deploy round-trip OBSERVED (CORRECTNESS · the J.W0 oracle
  RE-witnessed).** The close merge's own `ci` run concludes `success` on Linux,
  `deploy-pages.yml`'s `workflow_run`-on-`push` arm fires, and the live site serves the close-merge
  `head_sha` bytes — the deploy run's `event_name == 'workflow_run'` / `workflow_run.event ==
  'push'` (the AUTO arm), the CI run id + served hash recorded (S6). **BITE:** reds if the chain is
  blocked (any `demo-smoke` red), or if the deploy fired via `workflow_dispatch` / a manual
  `wrangler` bypass rather than the auto arm. **This is the INVE-1 terminal cure** — the deploy
  claim is true OF THE CLOSE MERGE, observed (`J.W0 §Hard gate clause (c)`).
- **clause (c) — `proof:published-surface` GREEN on the CUT (`4.2.0`) tree (CORRECTNESS · the
  PUBLISH oracle).** The packed tarball surface == the source's public exports == the README's
  taught (and EXECUTING) API == the runtime `loadAnimationEngine` surface; `_redirects` absent; the
  d.ts complete; the version `4.2.0`. **BITE:** reds on the current tree (the `_redirects` leak +
  the 9 untaught exports — `J.W5 §Hard gate`); GREEN only when the tarball, the exports, and the
  README agree on the cut tree. The publish (S4) fires only on this GREEN.
- **clause (d) — the chronic-closure SUBSTRATE TRANSITION is non-vacuous on the new substrate
  (CORRECTNESS · the meta-gate's own bite).** `proof:chronic-closure` re-pointed to `J/PROGRESS.md
  §"Open deferrals"` REDs on a planted malformed J-ledger row (a FOLD row citing a source-shape
  gate; a HANDOFF targeting an unpublished version; a ≥4-tranche row with no EXIT), then GREENs on
  the clean J ledger (S3). **BITE:** the born-RED witness is the planted malformed row; a swap that
  greens vacuously (the gate not actually parsing/policing the J substrate) is the paperwork-auditor
  failure mode this clause exists to forbid. **The substrate is policed by a BITING gate on the new
  ledger, not re-asserted.**
- **clause (e) — the J ledger is TERMINAL: zero un-dispositioned rows (CORRECTNESS · the
  P-invariant close).** Every row in `J/PROGRESS.md §"Open deferrals"` carries a terminal
  disposition (FOLD-landed / RE-AFFIRM / VERIFY-ONLY-rerun / HANDOFF-with-born-RED-gate-or-published-
  consume-edge / BOOK-with-home / RECORD / KILL / USER-DOMAIN-fired); every ≥4-tranche rider EXITED
  via a measurement artifact or a reasoned KILL (the close ledger carries ZERO rows tagged
  MEASURE-FIRST without a measurement — `J.W6 §Goal`); every W7b OPEN-edge carries its named
  sibling-HANDOFF mechanism (S8). **BITE:** reds if any row is un-dispositioned, or any MEASURE-FIRST
  row lacks its measurement, or any AX OPEN-edge is an un-rowed punt.
- **clause (f) — the prompt-recap dispositions every MANDATE row + every fold row + the 10 PARTIAL
  rows (CORRECTNESS · inv ε on the recap).** Every charter MANDATE row HELD-or-CARRIED; every
  `J/PROGRESS.md` fold row terminal; the 10 PARTIAL lineage rows each RESOLVED to a J home or a
  sibling gate (S2). **BITE:** reds if any prompt-derived request is unstatused (the M-4 status-gap
  shape the I close caught — `I.WZ.md §S2`).
- **clause (g) — the AFTER corpus diff is the named-exceptions-only (HYGIENE corroborator —
  labeled).** The `scripts/capture.mjs` AFTER corpus diffed against the J-open 48 shows ONLY the
  enumerated J.W7a deltas; an unnamed delta reds (S5). *(Labeled HYGIENE — it CORROBORATES the
  design boundary's runtime asserts (the J.W7a per-finding `--ball-tone`/overlap/ghost-rail/glass
  computed-style gates) + the re-captured visual-lock baseline; it may NOT substitute for a red
  correctness clause. The design correctness oracle proper is J.W7a's live-page asserts + the
  re-captured baseline; this corpus is the human-readable visual record.)*

**The §spine bar — MUST bite.** Clauses (a)–(f) are CORRECTNESS: they actuate the running product
at every boundary J taught it to cross — `proof:all == CI` GREEN (the regime), the close-merge
auto-deploy OBSERVED (deploy), `proof:published-surface` GREEN on the cut tree (publish/docs), the
substrate transition non-vacuous (the meta-gate's own honesty), the ledger terminal (the
P-invariant), the recap dispositioned (inv ε on the recap). Clause (g) is HYGIENE-corroborating
(the visual record). **The close is GREEN iff (a)–(f) hold against the close tree + the close
merge.** The inv-ε arbiter is the boundary battery itself — the close CANNOT overclaim because its
evidence is the OBSERVED oracle at each boundary, never a chain-of-trust over a prior FINAL. **The
two inv-ε FINAL gaps are CURED, recorded with their observed witnesses:** INVE-1 by clause (b) (the
close-merge auto-deploy OBSERVED via the auto arm); INVE-2 by the J.W4 axes battery cited in S1
(the breadth claim made true on planted-defect-biting mobile/touch/reduced-motion/dark/keyboard
legs). **NO CARRIED gap is silent** — where J could not fully cure a boundary (an AX OPEN-edge, the
`release.yml`-on-correctness decision), the carry's NAMED mechanism is recorded (S8, S4).

## §No-workaround prohibitions (BINDING — the mandate's named forbiddings for the close)

- **NO overclaim in the FINAL.** Every boundary claim cites its OBSERVED oracle (the deploy
  round-trip's recorded run id + auto-arm trigger; `proof:published-surface` GREEN; the axes legs
  biting on planted defects; the live-session budget 0 on the re-skinned tree). A claim that cannot
  cite an observed oracle is CARRIED with its named mechanism, never asserted. The I FINAL's
  INVE-1/INVE-2 gaps are the precise shape forbidden — the J FINAL's deploy/axes claims are
  OBSERVED, not structurally-impossible-when-written.
- **NO two-narrative drift.** The J FINAL is the SOLE authoritative close voice; where it
  supersedes a frozen J wave spec / `J/PROGRESS.md` disposition it says so EXPLICITLY (a pointer,
  not a silent re-write). The `wave-I.WZ-postclose.md §G` pattern (`I.WZ.md` `DEFERRED-BY-USER` vs
  `FINAL.md` `SUPERSEDED-BY-FIX-SHIP`) is the exact incoherence forbidden.
- **NO vacuous substrate transition.** The parse-target re-point (S3) is accepted ONLY after the
  re-run proves the gate BITES on a planted malformed J-ledger row — a swap that greens without
  re-policing the new substrate is the paperwork-auditor failure mode the meta-gate was re-founded
  to forbid.
- **NO autonomous registry mutation.** The version cut + npm publish are USER-DOMAIN (Mike Babb,
  confirm-first); the close AUTHORS the trigger + the gated pre-conditions and PRESENTS them, the
  owner FIRES. The publish fires ONLY on `proof:all == CI` GREEN + `proof:published-surface` GREEN +
  the close-merge round-trip OBSERVED — never ahead of the boundary gates.
- **NO `workflow_dispatch` / manual-`wrangler` substitute for the close-merge round-trip.** The S6
  re-observation is discharged ONLY by the AUTO `workflow_run`-on-`push` arm
  (`deploy-pages.yml:42-46` second disjunct), the `event_name`/`workflow_run.event` recorded — a
  manual dispatch proves bytes CAN ship, never that the auto-chain fired on the close merge
  (`J.W0 §No-workaround prohibitions`).
- **NO perpetual punt for the W7b OPEN-edges.** Each un-consumed AX edge exits as a NAMED
  sibling-HANDOFF row with its consuming mechanism (the next glass-ui re-pin's consume-edge) + a
  born-RED-able kf gate (the grep that flips on consume) — never an un-rowed "deferred again"
  (S8, P-invariant-28).
- **NO stale memory rule beside its replacement.** The T5 narrowing (S7) REPLACES the blanket
  props-destructure rule in one motion (the narrowed composable-boundary kernel), flagged
  user-visible — no-legacy applies to memory.

## §Folds (every J.md-assigned close item, with its evidence citation)

- **FINAL.md held to inv ε** — S1 (the five-boundary close report; each claim cites its OBSERVED
  oracle; the §G single-voice discipline). `J.md §WAVE MAP · J.WZ`, `final-vs-tree-inv-epsilon.md`
  INVE-1/INVE-2.
- **INVE-1 (the I FINAL deploy-claim gap)** — CURED, recorded with its observed witness: S1 (the
  DEPLOY boundary §) + clause (b) (the close-merge auto-deploy OBSERVED via the auto arm).
  `final-vs-tree-inv-epsilon.md` INVE-1, `J/PROGRESS.md §4d`.
- **INVE-2 (the breadth-of-human-surface gap)** — CURED, recorded with its observed witness: S1
  (the AXES boundary §, citing J.W4's planted-defect-biting mobile/touch/reduced-motion/dark/
  keyboard legs). `final-vs-tree-inv-epsilon.md` INVE-2.
- **The prompt-recap (every MANDATE row + every fold row + the 10 PARTIAL lineage rows)** — S2 +
  clause (f). `J.md §The MANDATE`, `J/PROGRESS.md §4g`, `audit/recap-{AB,CD,EF,GH,I-session}.md`
  (55/65 ADDRESSED, 10 PARTIAL each owned).
- **The chronic-closure SUBSTRATE TRANSITION (one motion · non-vacuity on the new substrate)** —
  S3 + clause (d). `J/PROGRESS.md:213-237` (the SUBSTRATE-TRANSITION NOTE + the Chronicity-integer
  column), `J.md §invariant set` (the chronic-closure meta-invariant).
- **The changeset version cut + npm publish (USER-DOMAIN, the honest minor)** — S4 + clause (c)
  (the publish gated on `proof:published-surface` GREEN). `J.W5 §S5` (the authored minor),
  `wave-I.WZ-postclose.md §E(g)`, `J/PROGRESS.md §4f`.
- **The close-merge auto-deploy RE-observation** — S6 + clause (b). `J.W0 §Hand-off → J.WZ`,
  `J.md` J.WZ row.
- **The AFTER screenshot capture (`scripts/capture.mjs` vs the J-open 48)** — S5 + clause (g). The
  design-suffusion visual record; the named-exceptions-only diff. `J.md §finding-cluster ledger`,
  `audit/design/*.md`, `J.W7a` (the re-captured visual-lock baseline companion).
- **The T5 memory update (the props-destructure rule narrowed to its composable-boundary kernel;
  USER-VISIBLE)** — S7. `J.md` T5, `J/PROGRESS.md §3`, MEMORY `feedback_props_destructuring.md`.
- **The W7b OPEN-edge disposition rule (each un-consumed AX edge → a named sibling-HANDOFF row
  consumed by the next glass-ui re-pin)** — S8 + clause (e). `J.md` J.W7b, `J/PROGRESS.md §1`/`§4c`
  (the glass-ui REFINE+ABSTRACT ledger band), `glassui-AX-handoff.md` (J.W7b's deliverable).
- **The `release.yml`-gates-on-correctness decision** — S4 (RECORD + the recorded USER-DOMAIN
  decision). `wave-I.WZ-postclose.md §F(g)`, `J/PROGRESS.md §4d` (release.yml never run).
- **The supersession-coherence pattern (the §G drift to AVOID)** — S1 (the single-voice
  discipline; the J FINAL is the sole authoritative narrative, supersessions explicit).
  `wave-I.WZ-postclose.md §G`.

## §Hand-off / cross-wave boundaries (BINDING)

- **← J.W0 (the deploy oracle, BINDING):** the close RE-observes J.W0's auto-deploy round-trip on
  the close merge (S6); the `navToScene` primitive + the green-Linux CI J.W0 landed are the
  precondition that the close-merge `demo-smoke` runs to completion. The close consumes; J.W0
  authored.
- **← J.W3 (the estate, BINDING):** the close's clause (a) consumes `proof:all == CI` (the two-way
  `proof:ci-coverage` equivalence) + the chronic-closure substrate the transition (S3) re-points;
  `proof:chronic-closure` derives its roster honestly from the J ledger J.W3 made the CI contract.
- **← J.W5 (the publish/docs, BINDING):** the close consumes the AUTHORED `tranche-j` minor (S4
  cuts it) + `proof:published-surface` GREEN (clause (c)) + the rewritten docs; J.W5 authored the
  changeset + the gate + the doc rewrites, the close FIRES the cut + publish (USER-DOMAIN).
- **← J.W4 (the axes, BINDING):** the close cites the J.W4 mobile/touch/reduced-motion/dark/
  keyboard battery as the INVE-2 cure (S1); the planted-defect-biting legs are the observed oracle
  the breadth claim rests on.
- **← J.W7a (the design baseline, BINDING):** the close captures the AFTER corpus on the post-W7a
  re-skinned tree (S5); the re-captured visual-lock baseline (J.W7a's own close motion) is the
  gate-side companion to the corpus.
- **← J.W7b (the AX ledger, BINDING):** the close records the OPEN-edge sibling-HANDOFF disposition
  rule (S8) over `glassui-AX-handoff.md`'s 46 dispositioned items; the consumed edges are terminal,
  the open ones ride the next re-pin as named handoffs.
- **USER-DOMAIN (Mike Babb, confirm-first — the close FIRES on confirmation):** the `changeset
  version` cut → `4.2.0`; the `v4.2.0` tag push triggering `release.yml`'s first-ever run; the
  npm publish; the close merge to master (which RE-fires the auto-deploy round-trip, S6). The close
  AUTHORS the triggers + the gated pre-conditions; the owner CONFIRMS and FIRES.
- **OUT / sibling (do NOT touch):** the value.js next-slice (VJ-1..9, rides the next re-pin); the
  glass-ui AX OPEN-edges (S8's named sibling-HANDOFFs, consumed on AX PUBLISH); the DEP-1 CNAME
  confirm (J.W6 verify-only, deploy-repo-owned) — all inv-16 sibling-coordination, never kf-patched.

## §Design decisions (trade-offs RESOLVED)

- **inv ε is MECHANICAL at the close — RESOLVED.** The J FINAL's evidence is the boundary battery
  GREEN on the fixed tree (`proof:all == CI` GREEN, the close-merge auto-deploy OBSERVED,
  `proof:published-surface` GREEN, the axes legs biting on planted defects, the live-session budget
  0 on the re-skinned tree). The running product at every boundary is the arbiter — NO chain-of-
  trust over a prior FINAL. The close cannot overclaim because its oracle is the OBSERVED boundary
  battery; where a boundary is CARRIED, the carry's named mechanism is on the record (S8, S4).
- **The two inv-ε FINAL gaps are CURED-with-witnesses, not re-asserted — RESOLVED.** INVE-1 (deploy)
  is cured by the close-merge auto-deploy OBSERVED via the AUTO arm (clause (b), S6) — the I FINAL's
  structurally-impossible claim becomes TRUE of the J close itself. INVE-2 (axes) is cured by the
  J.W4 planted-defect-biting battery cited in S1. Each is recorded WITH its observed oracle, the
  opposite of the assert-when-impossible shape J was founded to end.
- **The substrate transition is GATED, not a swap — RESOLVED.** The parse-target re-point
  (`I/PROGRESS` → `J/PROGRESS`) is accepted ONLY after the gate re-run BITES on a planted malformed
  J-ledger row — non-vacuity on the new substrate proven, not assumed. A vacuous swap is the exact
  paperwork-auditor failure mode the meta-gate was re-founded to forbid; the transition proves the
  J ledger is POLICED by a biting gate, in lockstep with it becoming authoritative.
- **The honest minor cut + publish stay USER-DOMAIN, gated on the boundary GREEN — RESOLVED.** The
  cut is `4.2.0` (additive new exports ⇒ minor; the value.js floor advance is a contractual
  tightening minor honestly signals); the publish fires only on `proof:all == CI` GREEN +
  `proof:published-surface` GREEN + the close-merge round-trip OBSERVED. The owner (Mike Babb)
  confirms and fires; the close authors the gated trigger, never an autonomous npm push. The first
  `release.yml` run is RECORDED as the PUBLISH boundary's observed oracle.
- **The close-merge round-trip is RE-observed on the close itself, not demonstrated once earlier —
  RESOLVED.** J.W0 proved the chain on A master push; the close re-witnesses it on the close merge,
  so the J CLOSE ships through the honest auto-chain — the deploy boundary's terminal inv-ε
  observation. The auto arm is required (NO `workflow_dispatch` substitute); the run id + served
  hash + trigger are recorded.
- **The AFTER corpus is the design boundary's observed oracle — RESOLVED.** The design directive's
  success criterion is the rendered RESULT measured against the intent; the
  `scripts/capture.mjs` AFTER-vs-48-shot diff is that measurement, with ONLY the named J.W7a deltas
  permitted (an unnamed delta reds). The corpus is the human-readable companion to J.W7a's
  re-captured visual-lock baseline; W7b contributes no delta.
- **The T5 memory narrowing is a user-visible change, landed at the close — RESOLVED.** The blanket
  props-destructure rule narrows to its composable-boundary kernel in ONE motion (no stale rule
  beside its replacement), flagged user-visible. Resolving it at the close lands the narrowed rule
  with the tranche's full justification on the record; the 6 idiomatic Vue-3.5 sites are no longer
  policed.
- **The W7b OPEN-edges exit via a NAMED mechanism, not a perpetual punt — RESOLVED.** Each
  un-consumed AX edge becomes a sibling-HANDOFF ledger row whose consuming mechanism (the next
  glass-ui re-pin's consume-edge) + born-RED-able kf gate (the grep on consume) are specified — the
  opposite of the un-rowed "deferred again" P-invariant-28 forbids. inv-16: consumed on AX PUBLISH,
  never kf-patched.
- **The close is single-voiced — RESOLVED.** The J FINAL is the sole authoritative narrative;
  supersessions of frozen DEV artifacts are explicit pointers, never silent re-writes that leave two
  live narratives. The `wave-I.WZ-postclose.md §G` two-narrative drift the I tranche was founded to
  forbid does not recur — one tranche, one close voice, every boundary telling the same true story.
