# Q.WE2 — the `leaves.ts` externalization (Arm B terminal): consume a value.js `/math` tree-shakeable subpath + DELETE the kf `internal/leaves.ts` math duplicates (the no-legacy violation 4.4.0 left) + `proof:no-cross-realm-cast` + the stale-comment correction (GATED on the value.js `/math` publish)

**Band:** E — no-legacy → 5.0.0 (the explicit owner NO-legacy directive; this band is its terminal).
**Phase:** **GATED** — the Arm-A externalize-or-delete leg fires atomically on the value.js `/math` tree-shakeable subpath publish (it ships TODAY at `dist/subpaths/math.d.ts`, but the boundary-gate clearance that makes a LIGHT-source consume legal is Q.WG2's `/math` arm + the W97 `math-subpath-clean` clause this wave authors). The NOW legs — the stale-comment correction, the `leaves-parity` test affirmation, and the `proof:no-cross-realm-cast` structural gate over the MOVED witnesses — fire on today's installed tree, zero sibling gate.
**Sequence (DAG edges):** `Q.WA3 master-merge-reconcile (NOW) ─► parse-that 0.13.0 (Q.WG1) ─► value.js 1.1.1/1.2.0 (Q.WG2, the /math subpath arm) ─► Q.WG4 kf GATED consumes ─► **Q.WE2** (the leaves externalize fires on the /math subpath + the W97 clearance) ─► Q.WZ (the 5.1.x additive cut absorbs the consume re-pin)`. The structural-gate legs (S1 `proof:no-cross-realm-cast`, S2 the W97 clause) are authored NOW, born-RED on today's tree; the Arm-A externalize (S3) fires on the GATED edge.
**Owning DM / idea:** the **no-legacy DUPLICATION violation the impl drive LEFT** (B2-pw1-lint-pw10-leaves: "P.W10 S3/S4/S6 is HALF-DONE and left in an INCONSISTENT state — a no-legacy violation introduced BY the impl drive") + the **P.W10 Arm-B terminal** (the `leaves.ts`→`/math` BUNDLE-EXTERNALIZATION TRAP, DP-6, recorded as Arm-B-expected, never landed) + **B4-precept §5** (the `internal/leaves.ts` math duplication, the isomorphic/no-legacy math debt). Audit substrate: **B2-pw1-lint-pw10-leaves** (the primary lane), **B4-precept-reckoning** (the VIOLATION + the GATED-on-`/math` wave). The `proof:no-cross-realm-cast` arm folds **P.W10 §S1** (authored-never-landed) re-anchored to the MOVED witnesses.

---

## Context

The owner directive is absolute: **NO legacy code; NO deferrals in Q.** `internal/leaves.ts` is a no-legacy violation on two axes — a **duplication** (it re-implements value.js's `clamp`/`scale`/`lerp`/`lerpArray` byte-for-byte) AND a **stale-comment lie** (its docstring asserts a structural claim that is FALSE on the shipped tree). B2-pw1-lint-pw10-leaves found the impl drive made this WORSE, not better: it landed the Arm-B *substance* (the `leaves-parity.test.ts` byte-equivalence sweep) but NOT the Arm-B *honesty* (the comment correction) — leaving "an INCONSISTENT state, a no-legacy violation introduced BY the impl drive." Q is the terminal; this wave finishes Arm B cleanly AND opens the Arm-A externalize on the `/math` GATED edge.

**The stale-comment lie (verified 2026-06-23, the no-legacy violation the drive left).** `internal/leaves.ts`'s `lerpArray` docstring (the `:50-64`-region comment) still asserts:

> *"value.js exposes ONLY its barrel export (`@mkbabb/value.js` → `dist/value.js`) — no tree-shakeable `./math` subpath — so a static `import { lerpArray } from "@mkbabb/value.js"` here would pull value.js's CSS-grammar static init into the LIGHT bundle and red `proof:boundary`."*

The **"no tree-shakeable `./math` subpath"** sub-clause is FALSE: value.js SHIPS `@mkbabb/value.js/math` today (`node_modules/@mkbabb/value.js/dist/subpaths/math.d.ts` exports exactly `clamp, scale, lerp, lerpArray, logerp, deCasteljau, cubicBezier, …` with a `parse-that-FREE` docstring). The comment is a stale structural claim shipped INTO 4.4.0 — **a stale comment is legacy too** (P.W10 precept). The OPERATIVE invariant of the comment (a LIGHT module must carry zero static `@mkbabb/value.js*` edge) is STILL TRUE — only the "no subpath exists" sub-clause is stale.

**The TRAP — why a naive `/math` re-export is NOT the cure (the P.W10 precept correction, grounded NOW).** O.W9 §S3's original idea was a delete: `export { clamp, scale, lerp, lerpArray } from "@mkbabb/value.js/math"`. That is FALSIFIED by the boundary gate's OWN regex. `proof-boundary.mjs:102` `const SPEC = String.raw`@mkbabb\/value\.js(?:\/[^"']*)?`;` — the `(?:\/[^"']*)?` arm matches `@mkbabb/value.js/math` exactly, so a static subpath specifier in a LIGHT module (reached by ~11 light entries: `numeric`, `smooth`, `playback`, `spring`, `drag`, `stagger`, `sequence`, `timeline`, `motion-path`, `waapi`, …) would RED **assertion 4** (the source-grep complement). And the full-loop W97 probe found a SECOND red: the externalized math chunk lives under `node_modules/@mkbabb/value.js/`, so the `isValueJs` per-entry filter (`proof-boundary.mjs:79-81`, assertion 1) flags it and reds EVEN when its graph is grammar-clean. **The leaves are STRUCTURALLY FORCED, not legacy** — UNTIL the boundary gate learns to distinguish "a subpath that pulls grammar" from "a parse-that-FREE math leaf." That is the W97 clause (S2). The cure is a **gate-VERIFIED bundle-externalization**, never an asserted delete.

**The moved cross-realm casts (P.W10 §S1, re-anchored — B2-pw1 finding).** P.W10 authored `proof:no-cross-realm-cast` but the impl drive never landed it, and the witnesses MOVED: the S9 parse-that retire DID land (`utils.ts` no longer imports `@mkbabb/parse-that`; the two `as any` casts over `parseAny` are GONE), but the cross-realm-cast class persists in `compile-color.ts` over value.js `Color` types — verified live at `compile-color.ts:59` (`normalizeColorUnit(vu as never).value as unknown as Color`), `:63` (`color2(c, "oklab") as unknown as {...}`), `:97-99` (`{...} as never) as unknown as Color`). These are the single most insidious workaround class (invisible to `tsc`, caught only by a bespoke scan). The gate must bite a NEW un-reasoned cross-realm cast while allow-listing the genuinely-irreducible `Color`-realm seam (value.js and kf each bundle their own nominal `Color`) via a `// @cross-realm:` annotation.

### Audit evidence

| Ref | Source location | Fact (verified 2026-06-23) |
|-----|-----------------|----------------------------|
| the stale-comment lie | `src/animation/internal/leaves.ts` `lerpArray` docstring (`:50-64`-region) | "value.js exposes ONLY its barrel export … no tree-shakeable `./math` subpath" — FALSE on the shipped tree; the no-legacy violation 4.4.0 left (the Arm-B substance landed, the honesty did not) |
| the math subpath SHIPPED | `node_modules/@mkbabb/value.js/dist/subpaths/math.d.ts:7` | `export { clamp, scale, lerp, lerpArray, logerp, deCasteljau, cubicBezier, interpBezier, cubicBezierToSVG, cubicBezierToString } from '../math'` — `parse-that-FREE` (`:1-2` docstring), grammar-free by construction |
| the boundary subpath ban | `scripts/proof-boundary.mjs:102` | `SPEC = `@mkbabb\/value\.js(?:\/[^"']*)?`` — the `(?:\/[^"']*)?` arm matches `@mkbabb/value.js/math`; a static subpath specifier in LIGHT source reds assertion 4 |
| the assertion-1 thread | `scripts/proof-boundary.mjs:79-81` (`isValueJs`) | the math chunk lives under `node_modules/@mkbabb/value.js/`, so the per-entry filter flags it and assertion 1 reds even when its graph is grammar-clean — the W97 allow-list must thread BOTH assertions |
| the published external (bare only) | `vite.config.ts:306` | `external: ["vue", "prettier", "@mkbabb/parse-that", "@mkbabb/value.js"]` — value.js externalized by BARE name; `@mkbabb/value.js/math` NOT covered (exact-string match, not prefix) |
| the boundary non-external | `scripts/proof-boundary.mjs:256` | `bundleEntry` external `["vue", "prettier"]` ONLY — value.js stays NON-external so a static edge bundles the grammar (the gate's whole point) |
| the Arm-B substance landed | `test/leaves-parity.test.ts` | EXISTS — the byte-equivalence sweep (`clamp`/`lerp`/`scale`/`lerpArray` vs `@mkbabb/value.js` across a 14-point grid). The Arm-B test is present; the Arm-B comment honesty is NOT |
| the moved cross-realm casts | `src/animation/compile-color.ts:59, :63, :97-99` | `normalizeColorUnit(vu as never).value as unknown as Color`; `color2(c,"oklab") as unknown as {...}`; `{...} as never) as unknown as Color` — un-annotated `as unknown as` over value.js `Color` types (the moved P.W10 §S1 witnesses) |
| S9 landed (witnesses moved) | `src/animation/utils.ts:1` | NO `@mkbabb/parse-that` import (the S9 retire DID land at 495484a); the two `as any` casts over `parseAny` are GONE — the cross-realm-cast witnesses moved to `compile-color.ts` |
| the absent artifacts | `ls scripts/proof-no-cross-realm-cast.mjs scripts/leaves-externalization-decision.json` | both ABSENT — the structural gate + the decision JSON are this wave's authored deliverables |
| the GATED edge | `package.json:3` value.js pin `^1.1.0`; Q.WG2 the 1.2.0 `/math` arm | the `/math` subpath ships today, but the W97 boundary-clearance + the re-pin that makes a LIGHT consume legal is Q.WG2/Q.WG4 — the GATED atomic edge |
| precedent gate | `scripts/proof-no-deprecated-guard.mjs` | the comment-aware grep-over-a-surface idiom the structural gate mirrors |

---

## Scope

Each S-clause is a concrete, falsifiable deliverable. The no-legacy cuts ride a GATE-VERIFIED transposition, never an asserted delete. The structural-gate legs (S1/S2) are authored NOW born-RED; the leaves cut (S3/S4) fires on the GATED `/math` edge.

### S1 — author `proof:no-cross-realm-cast` born-RED FIRST (the keystone structural gate, re-anchored to the moved witnesses)

**Breach.** No gate bans `as any` / `as unknown as` over an imported library type — the single most insidious workaround class (invisible to `tsc`). P.W10 §S1 authored this gate but the impl drive never landed it, and the witnesses MOVED from the now-gone parse-that casts (`utils.ts`) to the value.js `Color` casts (`compile-color.ts:59,63,99`).

**Cure (gate-first — author the RED before any cut).** Author `scripts/proof-no-cross-realm-cast.mjs`: scan `src/animation/**` for `as any` / `as unknown as` / `as never` over an identifier IMPORTED from `@mkbabb/value.js` (or, defensively, `@mkbabb/parse-that` — though that import is now gone); assert ZERO **un-annotated** violators. The escape hatch: a cast carrying a `// @cross-realm: <reason>` line-or-trailing annotation is allow-listed (a DECLARED, reasoned exception — the genuinely-irreducible cross-realm `Color` seam: value.js and kf each carry their own nominal `Color`, irreducible until value.js exposes a structural-clone/typed accessor). The `compile-color.ts:59,63,99` casts gain the annotation (the `Color` realm-bridge is genuinely irreducible on the densify path — K.W10); they GREEN as declared exceptions. A NEW bare `as unknown as Color` over a value.js import reds. Wire into `proof:hygiene` (beside `proof:no-deprecated-guard`, `package.json:200`). CI posture: **HARD** — a correctness/precept invariant, structural and device-independent.

### S2 — author the `proof:boundary` W97 `math-subpath-clean` clause (the TRAP-resolution decision oracle, threading BOTH assertions)

**Breach (the precept correction).** `proof:boundary`'s source-grep bans the `@mkbabb/value.js/math` subpath from LIGHT source (`:102`), and the `isValueJs` per-entry filter (`:79-81`) flags its chunk under `node_modules/@mkbabb/value.js/` — so the naive `leaves.ts`→`/math` re-export reds BOTH assertion 4 (the grep) AND assertion 1 (the per-entry filter). The gate cannot today tell a grammar-pulling subpath from a parse-that-FREE math leaf.

**Cure.** Add the W97 `math-subpath-clean` clause: bundle `@mkbabb/value.js/math` as its OWN rolldown entry (the `bundleEntry` pattern, `proof-boundary.mjs:244`) and assert its STATIC module graph contains ZERO CSS-grammar / parse-that / `engine.ts` modules. GREEN iff the subpath is structurally grammar-free (the W97 probe at P.W10 confirmed the graph at 2 modules / 533 bytes / 0 parse-that-grammar-engine — the gate VERIFIES it, never assumes). On GREEN, the subpath is added to an ALLOW-LIST that threads **BOTH** boundary assertions (the full-loop RE-SCOPE correction, P.W10 ledger line 239):
- **assertion 4 (the source-grep complement, `:353-380`):** add `@mkbabb/value.js/math` to the allow-list so the specifier is PERMITTED in a LIGHT source module IFF W97 confirms its graph is clean.
- **assertion 1 (the per-entry `isValueJs` filter, `:79-81`):** the W97 allow-list must ALSO externalize the verified-clean subpath in the gate's OWN `bundleEntry` external — promoting the math chunk from "a value.js module that must be inlined-and-banned" to "a verified-clean runtime edge the gate externalizes too." Without this second thread, Arm A reds assertion 1 across the ~11 light entries that pull `leaves.ts`.

**W97 is the decision oracle for S3** — it measures whether the externalization is safe across BOTH assertions; it asserts neither arm.

### S3 — the leaves cut: externalize-if-clean (Arm A) OR document-the-keep (Arm B) — the GATED no-legacy terminal

**Breach.** The four `leaves.ts` math leaves are byte-duplicates of value.js's `src/math.ts` — KISS debt IF the subpath can be safely consumed; STRUCTURALLY-FORCED clean locals IF it cannot. The drive left the substance (the parity test) but not the resolution (the comment + the decision JSON).

**Cure (the transposition — gate-VERIFIED, not asserted). The owner's NO-legacy directive favors Arm A (DELETE the duplicates) where mechanically clean; Arm B is the gate-forced contingency.**

- **Arm A (externalize — the no-legacy DRY win, the OWNER-FAVORED terminal IF W97 GREEN + the H4 smoke-test passes):** widen `vite.config.ts:306` `external` to cover `@mkbabb/value.js/math` (add the subpath string AND/OR the predicate `/^@mkbabb\/value\.js(\/|$)/`); **DELETE** the four local math functions (`clamp`/`scale`/`lerp`/`lerpArray`) in `leaves.ts` and replace with `export { clamp, scale, lerp, lerpArray } from "@mkbabb/value.js/math"`; delete the stale `:50-64` comment. The published `dist/keyframes.js` carries a bare runtime edge to the subpath (resolved at the consumer); `proof:boundary` stays GREEN because W97's allow-list permits the verified-clean subpath on BOTH assertions. **The rAF shim stays local** (`requestAnimationFrame`/`cancelAnimationFrame`/`FRAME_RATE`, `leaves.ts:87+`) — NOT in `./math` (it is environment-shim, not pure math).
  - **The H4 rolldown-external SMOKE-TEST (verify BEFORE the widening commits).** A bare `"@mkbabb/value.js"` external string does NOT match the `/math` subpath (rolldown does exact-string / package-boundary matching, not prefix). So a precondition smoke-test gates Arm A: build the LIGHT lib with the WIDENED external and assert the published `dist/keyframes.js` carries `@mkbabb/value.js/math` as a BARE `import`/`from` runtime edge (the specifier appears verbatim in the emitted ESM, resolved at the consumer) AND the subpath's math source is NOT INLINED into the entry chunk. The P.W10 H4 probe found: bare string → 533B inlined (edge=false); subpath string + predicate → 113B externalized (edge=true). If the widened external does NOT externalize the subpath, Arm A is MECHANICALLY FRAGILE → fall to Arm B. Proven on the built dist, never asserted from the `vite.config.ts` edit.
- **Arm B (document-the-keep — the gate-forced contingency IF W97 finds a grammar edge OR the externalization is mechanically fragile):** KEEP the four byte-equivalent leaves; **correct the stale `:50-64` comment** to the TRUE rationale: "the boundary gate bans the subpath specifier from LIGHT source (the regex matches `@mkbabb/value.js/math`) AND the `isValueJs` per-entry filter flags the math chunk; these are the gate-forced clean local, parity-tested against value.js `src/math.ts` (`test/leaves-parity.test.ts`) — NOT legacy." This is the equally-honest no-legacy outcome: the duplication is not legacy when the gate structurally forbids the import.

**The decision is W97's verdict + H4's smoke-test, recorded in a decision JSON** (S4). **NOTE on the P.W10 precedent.** P.W10's full-loop named Arm B as the EXPECTED outcome on the value.js-1.0.2 tree (the assertion-1 cost + the ~11-light-entry blast radius outweighed the ~30-LOC DRY win). Under Q's explicit **owner NO-legacy directive**, Arm A (the DELETE) is the FAVORED terminal — but only IF the GATED `/math` boundary-clearance (S2's W97 on BOTH assertions) AND the H4 smoke-test BOTH pass, making the externalization mechanically clean. If either fails, Arm B fires as the gate-forced honest keep (not a punt — the corrected comment + the parity test + the decision JSON make it a terminal). The wave specifies BOTH arms NOW so the GATED leg cannot fork mid-tranche.

### S4 — record the verdict in `leaves-externalization-decision.json` (the P-inv-28 terminal home)

Record the W97 + H4 verdict in `scripts/leaves-externalization-decision.json` (beside `spring-vector-decision.json`, mirroring its verdict+evidence schema): **ARM-A** (externalized + the four duplicates DELETED — the subpath graph clean across BOTH assertions, the H4 external mechanically clean, the no-legacy DRY win) OR **ARM-B** (documented-keep — the leaves retained with the corrected rationale, the gate-forced honest local). The JSON captures the probe numbers as the audit trail — W97 graph (modules / bytes / grammar-parse-that-engine count), H4 external (bare → inlined/bytes; subpath/predicate → externalized/bytes), the assertion-1 finding, the light-entry blast radius. The `leaves.ts` debt (DP-6) gets a terminal home — never a bare deferral. Whichever arm fires, the stale comment is corrected (Arm A deletes it; Arm B rewrites it to the TRUE rationale) — the no-legacy violation 4.4.0 left is CLOSED either way.

### S5 — affirm `test/leaves-parity.test.ts` as the drift guard (both arms)

**Breach.** A byte-copy (Arm B) can silently drift from its canonical value.js source; an externalize (Arm A) could silently consume a drifted/renamed subpath export.

**Cure.** `test/leaves-parity.test.ts` (ALREADY shipped — the Arm-B substance the drive landed) is affirmed as the drift guard for BOTH arms:
- **Arm B:** the parity sweep catches a local-copy drift from value.js `src/math.ts` (its existing role).
- **Arm A:** the SAME test, now importing the four functions through the externalized `@mkbabb/value.js/math` subpath, catches a value.js refactor that drops/renames a `/math` export (the consume would fail to resolve, or the values would drift). The test is re-pointed in Arm A to assert the subpath consume is value-identical to the value.js barrel — closing the consume-edge.

No new test is authored (the parity sweep exists); the wave affirms its role across both arms so the drift guard survives whichever terminal fires.

---

## Born-RED gate

**Gate:** `proof:no-cross-realm-cast` (NEW — `scripts/proof-no-cross-realm-cast.mjs`; this wave authors it gate-first per S1) + the `proof:boundary` W97 `math-subpath-clean` clause (EXTENDED, S2) + the `leaves-externalization-decision.json` verdict (S4). Every clause is STRUCTURAL (a source-grep or a bundle-graph assertion), device-independent by construction.

**The REAL observable (observable-truth — a source/build-graph defect, not a perf proxy):**

| Clause | Witness on today's (2026-06-23) tree | The REAL observable | GREEN condition |
|---|---|---|---|
| `no-cross-realm-cast` (S1 — **KEYSTONE structural**) | `grep -n "as unknown as\|as never" src/animation/compile-color.ts` → `:59`, `:63`, `:97-99` (over value.js `Color` / `normalizeColorUnit` / `color2` imports), UN-annotated | un-reasoned `as unknown as Color` over imported value.js types — the insidious workaround class invisible to `tsc`; a NEW one would drift unproven | every `as any`/`as unknown as`/`as never` over a value.js import carries a `// @cross-realm:` reason annotation; the `compile-color.ts:59,63,99` casts annotated (declared exceptions); zero un-annotated violators |
| `W97` math-subpath-clean (S2 — the TRAP decision oracle) | `node scripts/proof-boundary.mjs` has no clause that bundles `@mkbabb/value.js/math` and verifies its graph | the gate cannot distinguish a grammar-pulling subpath from a parse-that-FREE math leaf — so it bans the subpath wholesale (assertion 4 grep + assertion 1 per-entry filter), FORCING the `leaves.ts` duplication | W97 bundles `@mkbabb/value.js/math` as its own entry, asserts ZERO grammar/parse-that/`engine.ts` modules in its static graph, and threads the allow-list through BOTH assertions (4 source-grep + 1 `isValueJs` per-entry filter) — promoting "banned specifier" to "verified-clean externalizable edge" |
| `leaves-no-legacy-resolved` (S3/S4) | the `leaves.ts:50-64` comment claims "no `./math` subpath" (FALSE) + four local math duplicates; no decision JSON | the four math leaves are byte-duplicates held by a comment that mis-states the reason — the no-legacy violation 4.4.0 left, UNDECIDED | **Arm A (owner-favored, IF W97 GREEN + H4 clean):** the four duplicates DELETED, `leaves.ts` re-exports from the W97-verified subpath, `vite.config.ts:306` external widened, `proof:boundary` GREEN on BOTH assertions, the H4 smoke-test confirms `@mkbabb/value.js/math` is a bare runtime edge (not inlined). **Arm B (gate-forced contingency):** leaves kept, the stale comment corrected to the TRUE rationale, the parity test re-affirmed. Either way the stale comment is GONE and the verdict recorded (S4) |

**How it is born-RED via a planted failure.**
- **`no-cross-realm-cast` is born-RED with NO plant needed** — `compile-color.ts:59,63,99` are live un-annotated `as unknown as`/`as never` over value.js `Color` imports the instant the gate runs. **The discriminating bite (the proxy-trap guard):** a cure that DELETES the `as unknown as` text but replaces it with a laundered `as never` double-cast (the SAME workaround obfuscated) STILL reds — the gate matches the `as never` / `as any` / `as unknown as` shapes together, and the genuine escape is the `// @cross-realm:` annotation (a reasoned declaration), never a syntactic dodge. The observable is "an un-reasoned cross-realm cast," not "the literal text `as unknown as`."
- **`W97` is born-RED via a PLANTED failure** that proves the gate measures the GRAPH, not the name: temporarily add `import "@mkbabb/value.js"` (the BARE grammar-pulling root, NOT `./math`) to a fixture the W97 entry bundles → W97 reds with grammar modules in the graph, proving the clause bites the genuine "this subpath pulls grammar" observable. On the real `./math` subpath (grammar-free by construction), W97 greens — and ONLY then is the subpath allow-listed on BOTH assertions. A clause that merely greps "does `leaves.ts` import `./math`" would be the proxy mistake (it greens on a subpath that secretly pulls grammar); the bundle-the-graph witness bites the real edge.
- **`leaves-no-legacy-resolved` is born-RED** because the four local defs + the stale comment are live today. **Discriminating bite #1:** a cure that does the NAIVE re-export WITHOUT the W97 allow-list reds `proof:boundary` assertion 4 (the subpath specifier in LIGHT source) — proving the cure is the GATE-VERIFIED externalization, not the asserted delete. **Discriminating bite #2 (the assertion-1 thread):** even WITH the source-grep allow-list, an Arm-A cure that does NOT externalize the math chunk in the W97 `bundleEntry` external still reds assertion 1 (the `isValueJs` per-entry filter flags the math chunk) across the ~11 light entries pulling `leaves.ts` — proving the allow-list must thread BOTH assertions. **Discriminating bite #3 (H4):** a cure that widens `vite.config.ts` external with the BARE `"@mkbabb/value.js"` string ONLY (not the `/math` subpath string or the predicate) reds the H4 smoke-test because the subpath is INLINED (rolldown's exact-string match does not cover the subpath) — proving the smoke-test verifies the widening actually externalizes the subpath on the built dist. **Discriminating bite #4 (the comment-honesty bite):** an Arm-B cure that keeps the leaves but leaves the stale "no `./math` subpath" comment reds the comment-content check — the no-legacy violation is the stale lie, not just the duplication.

**Portable / structural gates (not perf floors).** Every clause here is STRUCTURAL (a source-grep or a bundle-graph assertion), device-independent by construction — no absolute thresholds, no CI device-dependence (unlike a perf-ratio gate). CI posture: HARD — these are correctness/precept invariants.

**Green condition.** `proof:no-cross-realm-cast` GREEN (the `compile-color.ts` casts annotated, zero un-annotated violators); the W97 `math-subpath-clean` clause GREEN on BOTH assertions; the leaves cut resolved (Arm A delete-and-externalize on the GATED `/math` clearance, OR Arm B keep-with-corrected-comment) with the verdict in `leaves-externalization-decision.json`; the stale comment GONE; `test/leaves-parity.test.ts` affirmed as the drift guard. The no-legacy violation 4.4.0 left is CLOSED.

---

## Dependencies

- **value.js `/math` tree-shakeable subpath — the GATED enabling edge (Q.WG2 → Q.WG4).** The `@mkbabb/value.js/math` subpath ships TODAY (`dist/subpaths/math.d.ts`, parse-that-FREE), but the boundary-gate clearance that makes a LIGHT-source consume legal is the W97 clause (S2, authored this wave) + the Q.WG2 dispatch confirming the `/math` subpath's `parse-that`-free contract holds across the value.js 1.2.0 publish + the Q.WG4 re-pin. **The Arm-A externalize (S3) fires atomically on this edge** — no kf wave consumes an unpublished/uncleared surface (charter §3 friction-chain 4). The structural-gate legs (S1, S2) are authored NOW born-RED on today's tree, independent of the GATED edge.
- **Q.WA3 (master-merge-reconcile) — SEQUENCE precondition.** The leaves cut lands on a reconciled master (the deploy-of-record), not a divergent tranche tip.
- **Q.WG1 (parse-that 0.13.0) → Q.WG2 (value.js 1.2.0) — the cross-repo publish chain that precedes the GATED consume.** The `/math` arm of Q.WG2 confirms the subpath contract; Q.WE2's Arm A consumes it. The dispatches carry a terminal-or-KILL so the GATED edge cannot become a perpetual punt.
- **Q.WE1 (the alias drop) — Band-E sibling, DISJOINT.** Q.WE1 touches the alias sites + demo/test consumers; Q.WE2 touches `internal/leaves.ts` + `compile-color.ts` + the boundary gate. No region collision. Both are no-legacy terminals; they compose into the 5.0.0/5.1.x cut at Q.WZ (Q.WE1's breaking alias drop → 5.0.0; Q.WE2's additive externalize → 5.1.x).
- **Q.WZ (the 5.1.x cut) — the honest home for the additive consume re-pin.** The Arm-A externalize is additive (a bundle-internal edge change, no public-surface break); the value.js `^1.2.0` consume re-pin is absorbed by the 5.1.x minor at Q.WZ.
- **The S9 retire is DONE (the witness moved, not a dependency).** P.W10's `proof:no-cross-realm-cast` was anchored to the `utils.ts` parse-that casts; those vanished with the S9 dep retire (495484a). Q.WE2 re-anchors the gate to the MOVED `compile-color.ts` `Color` casts — no parse-that dependency.

---

## dev→impl boundary

This file is the Tranche Q DEVELOPMENT spec for Q.WE2 — **DOCS ONLY** (inv-16: kf writes only keyframes.js; the `./math` subpath is CONSUMED, value.js owns it; the W97 boundary-clearance + the consume re-pin are kf-side). The IMPLEMENTATION (the `proof:no-cross-realm-cast` gate, the W97 `proof:boundary` clause, the `leaves.ts` cut + the `vite.config.ts` external widen OR the documented keep, the decision JSON, the parity-test affirmation) opens ONLY on the owner's explicit authorization. The structural-gate legs are phase NOW (born-RED on today's tree); the Arm-A externalize is GATED on the value.js `/math` clearance. When it opens it is:

- **gate-first** — `proof:no-cross-realm-cast` (S1) + the W97 `proof:boundary` clause (S2) authored born-RED BEFORE any cut; the W97 + H4 decision oracle ratified BEFORE the S3 leaves transposition.
- **observable-truth** — W97 bundles the math subpath and verifies its GRAPH (never assumes the subpath is clean); the H4 smoke-test verifies the external widening RESOLVES the subpath as a bare runtime edge on the built dist; the cross-realm gate bites an un-REASONED cast (the `// @cross-realm:` annotation is a declaration, not a syntactic dodge).
- **no-legacy** — the precept terminal: the duplication is RESOLVED (Arm A DELETES the four byte-copies where mechanically clean — the owner-favored cut — OR Arm B keeps them as the gate-forced clean local with the TRUE rationale); the stale "no `./math` subpath" comment — the no-legacy violation 4.4.0 left — is GONE either way; the boundary gate is SHARPENED (W97 learns to distinguish a grammar-pulling subpath from a clean math leaf), not relaxed.
- **no-deferral** — both arms are specified NOW (the GATED leg cannot fork mid-tranche); the verdict has a terminal home (`leaves-externalization-decision.json`); the `proof:no-cross-realm-cast` gate is a HARD CI invariant, not a hoped precept. A KEEP (Arm B) is not a punt — it is the gate-forced decision with the corrected comment + the parity test + the JSON.
- **gestalt (the transposition, not the band-aid)** — the `leaves.ts` cut is the bundle-externalization (resolve the specifier AT the consumer, never inline) — a structural transposition; the gate LEARNS the subpath/grammar distinction (W97), so the boundary discipline is sharpened.

**Mid-tranche-friction pre-emption.** This wave could spawn TWO frictions, each pre-empted NOW:
1. **The GATED-consume perpetual-punt** — "the Arm-A externalize waits on a value.js `/math` clearance that never lands" (the exact P-inv-28 failure the two GATED Band-E/G waves risk). **Pre-empted by binding the GATED edge to the Q.WG2 dispatch's terminal-or-KILL** (charter §3 friction-chain 4): the `/math` subpath ships today; the W97 clause (authored NOW) IS the clearance oracle; if the W97/H4 probe finds the externalize mechanically fragile, Arm B fires as the gate-forced honest keep — the wave terminates either way, never a perpetual GATED stall.
2. **The W97 two-assertion blast radius** — "an Arm-A cure clears assertion 4 but reds assertion 1 across the ~11 light entries that pull `leaves.ts`." **Pre-empted by the S2 dual-thread spec + the gate's discriminating bite #2**: the W97 allow-list threads BOTH assertion 4 (the grep) AND assertion 1 (the `isValueJs` per-entry filter, externalizing the math chunk in the gate's own `bundleEntry`), so the externalize clears every light entry atomically; the gate bites a cure that threads only the grep.

The born-RED witness (the un-annotated `as unknown as Color` casts at `compile-color.ts:59,63,99`; the absent W97 clause; the four local `leaves.ts` math duplicates + the stale "no `./math` subpath" comment; the absent decision JSON) stands on today's tree; the cure opens on authorization. Gate-first, observable-truth (W97 bundles the graph, H4 verifies the built dist), no-legacy, no-deferral, gestalt throughout.
