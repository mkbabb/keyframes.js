# Tranche Q — FINAL (the no-deferral terminal tranche, IMPLEMENTED + SHIPPED)

**Closed 2026-06-24.** The constellation impl drive over Tranche Q completed in totality on `tranche-q-impl`
→ merged to `master` (`186acec`, tag `v5.0.0`). Every band discharged; the P-invariant-28 ledger TERMINATED.

## The version narrative (shipped)

| Repo | Version | Status |
|---|---|---|
| **parse-that** | **0.13.0** | PUBLISHED + tagged `v0.13.0` (dead-API delete, packrat re-entrancy try/finally + key widen, *Span deprecate, subTable retract) |
| **value.js** | **1.2.0** | PUBLISHED + tagged (contrast-color, if-multibranch, the color out-param family + ColorChannelPlan in-leaf fold, VJ-Q4 `.fnName`, `/math` subpath, dashed-call) |
| **keyframes.js** | **5.0.0** | PUBLISHED (registry-confirmed) + tagged `v5.0.0` + merged to master. The BREAKING alias-drop major absorbing the full Q payload |
| **keyframes-vue** | **0.1.0** | PUBLISHED (first-ever; write-confirmed E403-on-republish) — **DM-7 P-inv-28 belt EXITS, no 6th carry** |
| **glass-ui** | BC | **USER-DOMAIN** — owner WIP on `prototype/liquid-dock`; the kf S1/S2 deletes + N-Stage unshelf stay GATED-PENDING (correctly, per plan) |

**Deploy:** keyframes.babb.dev redeployed (CF Pages, `index-DVJqnozg.js`) — the round-trip oracle VALIDATED
live-byte equality (the served bundle == the built bundle). **DM-20 re-observed.**

## The bands (all discharged)

- **A (apparatus, `ac40f72`):** SLIM dep-cruiser lint tier (eslint KILLED); drag2D LIGHT cert; CI-green (6-gate
  wiring + F-7 device-harden + report-all hygiene + the deploy round-trip oracle + decision-JSON determinism);
  wave-charter contrivance gate + the machine-readable DAG manifest; **the master-merge reconcile** (the FIRST motion).
- **B (engine-perf, `090c7b0`):** emerging-CSS Phase-2 element-aware arm + **@function call-inlining** (`4d6683e`,
  consuming value.js 1.2.0); processFrame Float64 SoA fold (bit-identical); WAAPI curvature-adaptive densify.
- **C (demo-fleet, `090c7b0`+`46b92bc`):** **DemoControlPoint** — the DM-2 NINTH-carry chronic GENUINELY EXITS
  (browser-verified pointer-drag re-shapes the bezier + re-times the subject); the easing curve-editor dogfood;
  the mobile scroll-snap N-Stage carousel + typed VT; the **MorphSVG** scene + the on-DOM render contract +
  orient-along-path; amiga `decay()` telemetry.
- **D (correctness, `090c7b0`):** the NaN-frame proper cure (attach-time deferred-resolution + a play-time guard,
  NEVER a parse-throw — the S4 ingest round-trip held); grammar-fuzz fast-check + the kf-vs-browser differential.
- **E/F (the breaking-cut spine, `9a24bd1`/`aab1a6f`/`b246872`):** the @deprecated alias DROP + the 39-consumer
  migration + MIGRATION-5.0.0.md; the group.ts SoA decomposition → `group-soa.ts`; the engine.ts split → 
  `engine-playback.ts` + **the decomposition close** (every Q-grown library file cured — `proof:decomposition` FULLY GREEN).
- **G (consume, `4d6683e`):** value.js 1.2.0 GATED consumes — @function inline, S8 `.fnName` (WeakMap retired),
  WE2 leaves externalize (onto value.js/math), the principled WB3-color DECLINE (the fold ships in-leaf), the
  VJ-Q9 grammar-fuzz promotion, the S1/S2 gate-hygiene retarget (content-probes, glass-ui-BC-gated).
- **Z (close, `186acec`):** `proof:chronic-closure` re-pointed L→Q + the 3 gate-code co-edits + NON-VACUOUS
  (3 plants → RED on 3 clauses → GREEN) — **the P-inv-28 ledger TERMINATED, zero ≥4-tranche rows un-exit-shaped**;
  the 5.0.0 breaking cut; the keyframes-vue belt exit; the live-byte deploy round-trip.

## The no-deferral law, honored

Every chronic discharged with a gate that BIT: DM-2 (9th carry) BUILD-IN, DM-22 BUILD-IN, DM-7 USER-DOMAIN
(published), DM-3/5/16/20 et al. The Q perf waves' decomposition regressions were CURED (not deferred). The
only PENDING items are USER-DOMAIN by design (the glass-ui BC publish — owner WIP — gating the kf S1/S2 deletes).
No "ABSOLUTE FINAL" without a system-gate exit.

**The constellation is shipped, live, and verified.**
