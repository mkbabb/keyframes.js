# A.W5 visual-runtime — 2026-06-02 retroactive archival

Two stale demo-dock captures (`dock-closeup.png`, `dock-fixed.png`, Mar 16
2026 — pre-dating tranche A) archived here per the constellation
visual-evidence protocol's archive-not-delete rule
(`audit/constellation-adoption-2026-06-02.md` §2). They imaged a `dock-*`
demo surface, not engine output, and had no owning tranche; relocated from
the gitignored `.playwright-mcp/` scratch dir into a tracked baseline leaf.

This is a retroactive archival run (`-Aarchive`), distinct from a tranche
open/close π capture. A is an engine tranche — its deltas are static-graph
(boundary bytes), behavioural (reduced-motion snap), and perf (long-task
break), not pixel-layout — so no paired open/close π baseline was warranted
(adoption fold §3). See `FINAL.md` for the π-lane disposition.
