# Tranche H — the consolidated cross-repo HAND-OFF charter (glass-ui · value.js · parse-that · deploy)

**What this is.** The single cross-repo HAND-OFF artifact for Tranche H: the charter that
collects EVERY item H does NOT author in the keyframes.js tree into its sibling-owned
surface (glass-ui · value.js · parse-that · deploy). Each item carries the sibling-repo
`file:line` (or live anchor), its disposition, the falsifiable gate, the sequencing (who
lands first, what gates the kf consume-leg), AND — the H discipline — **the born-RED kf
gate it PAIRS with**. H reorders the charter from G: **glass-ui leads** (the dock LAG is
the live, AW-ACTIVE chronic and the heaviest H cross-repo surface), value.js is the
next-slice principal riding the next re-pin, parse-that is the transitive sibling with one
withheld re-key, and deploy carries the P0 CNAME drift.

**Phase:** IMPL spec-authored-in-DEV-awaits-auth. This is TRANCHE DEVELOPMENT — DOCS ONLY,
ZERO source/test/CI/demo edits. Implementation awaits explicit authorization (the
D/E/F/G dev/impl boundary). This lane wrote ONLY this file.

**Branch.** `tranche-h-dev` (kf **`4.1.0`** — all of Tranche G live at `:5174`). The
re-pin LANDED in G and is verified live this pass: `package.json` `@mkbabb/value.js
^0.11.1` (`:99`) · `@mkbabb/parse-that ^0.9.0` (`:99`) · `@mkbabb/glass-ui ^3.4.0`
(`optionalDependencies`, `:103`) — NO `file:`/`link:`/`git:` protocol. Installed:
value.js `0.11.1`, parse-that `0.9.0`, glass-ui `3.4.0` (`node_modules/@mkbabb/*/
package.json`, re-verified). The demo's `@mkbabb/glass-ui → src` dedup self-alias STAYS
(AW-endorsed, `vite.config.ts:142-146`).

**The id.** `handoffs`. **The deliverable.** This file only.

---

## §THE BINDING MANDATE (travels WITH this charter — verbatim-in-substance)

NO quick solutions / NO workarounds — idiomatic, gestalt; no escape-hatch beside the real
fix. NO legacy beside its replacement (a replaced surface replaced in ONE motion). NO god
modules (MEASURE-FIRST). KISS · DRY · no nested imports · no test-in-src. Styling
ISOMORPHIC unless a NAMED befitting delta. MEASURE-FIRST (every perf claim behind a biting
bench or recorded-withheld WITH the number). **inv-16 (H posture): consume glass-ui /
value.js / parse-that PUBLISHED — AUDIT + SUGGEST + HAND-OFF, do NOT author them inside
kf.** Per the user's standing MEMORY rule, **all glass-ui / dock changes live in the
glass-ui repo, never patched or re-masked in the demo** — this rule is CORRECT and is NOT
relaxed for H. inv ε: every claim cites a phase-1 lane or a live `file:line`. inv ζ: the
chrome dogfoods the engine.

**THE CHRONIC-CLOSURE DISCIPLINE (the one new H rule this charter is built around,
`_SYNTHESIS-gap-scorecard §0/§6`, `_SYNTHESIS-deferred-ledger §2`):** a chronic exits the
ledger ONLY with (a) a passing SYSTEM-property gate, or (b) a **HANDOFF tag PAIRED with a
born-RED kf-side gate** that fails until the upstream fix lands and is consumed. **A bare
HANDOFF tag is no longer a terminal.** The P-invariant policed the COLUMN, not the PRODUCT
— H repairs it. Every HANDOFF row below therefore names its paired **born-RED kf gate**;
that pairing IS the terminal. (The dock — CH-4 — is the canonical case, and its TWO
concerns carry TWO distinct gates with ONE canonical name each — never the conflated
`proof:dock-live`: (i) the SPRING/settle half (D5) is **`proof:dock-morph-settled`**,
born-RED today on the +16.3% token, GREEN when kf bumps to the published glass-ui `^3.5.1`;
(ii) the @mbabb POPOVER half (D9) is **`proof:dock-popover-opens`**, a kf SHIP gate owned by
**H.W1** that greens when the App.vue double-wrap is undone — D9 is kf-patched, NOT a
glass-ui handoff. The legacy `proof:dock-live` name is RETIRED — it dangled against the
committed substrate and conflated the two halves.)

**The §ALREADY-SOTA record is BINDING.** Manufacture NO work where the engine / parse /
color / re-pin kernels lead (`_SYNTHESIS-gap-scorecard §5`). The single-dispatch
`lerpValue` seam (`engine.ts:18` import, `:779` the per-frame `lerpValue(eased, iv)` call
in `processFrame`, off `:657 interpFrames` — re-anchored this pass; the published
CLAUDE.md `engine.ts:516,576` dev-bundle lines and the `iv._lerp` form are stale), the
value.js color science + parse breadth, the parse-that leaf
tier + the four landed F waves, and glass-ui's spring-token cascade + motion-core SCC
boundary + the specular `::before` BUILD (its defect is tuning/seam, NOT perf) are at or
ahead of SOTA and are left alone. The §ALREADY-SOTA tail of each section is the refusal —
binding, not optional.

---

**Provenance (the lanes consolidated).** The load-bearing `_SYNTHESIS-gap-scorecard` (the
§4 HANDOFFS table + §3 wave→gate map H.W0..H.W8 + §1 gap map) ·
`_SYNTHESIS-deferred-ledger` (§1 the four chronics + §2 the meta-gate + §3.4-3.7 the
sibling-HANDOFF roll-up) · `a-deferred-ledger` (the row-level whole-history ledger §2-§7) ·
`a-historical-dock` (the dock git-archaeology + the AW.W2 retune + the D9 root-cause +
`proof:dock-morph-settled` / `proof:dock-dropdown-opens`) · `a-glass-ui-consumption` (the
specular SEAM root-cause + D9 double-trigger + D5 collapse-delay + D13 drawer + D14
`--specular-intensity`). Every claim cites a phase-1 lane or a live `file:line`
re-verified against the trees on `tranche-h-dev` (2026-06-07). The companion charter this
charter SUCCEEDS: `G/valuejs-parsethat-glassui-handoff.md`.

---

## ⟢ THE SPINE THIS CHARTER ENFORCES — a HANDOFF is born paired or it is not a terminal

G's charter was a re-pin charter: one re-pin, three deps, a tight additive set. **H's
charter is a chronic-closure charter.** The G re-pin DISCHARGED the value.js/parse-that
consumer chronic (`a-deferred-ledger §0/§1`: the whole pre-G open ledger collapsed; the
0.11.1 slice is live kf behaviour through the seam). So H inherits a SMALL, well-homed
residual — but it inherits ONE structural failure G's gate regime was blind to: **four
user-visible chronics (cartoon-shadow, φ-typography, mobile, dock) "exited" the ledger on
PAPER** (`_SYNTHESIS-deferred-ledger §1`). Two of those four are cross-repo (the dock LAG
and the Card-specular SEAM); the other two are kf-demo waves (H.W2/H.W4). For the
cross-repo two, the chronic-closure discipline is the whole point of this charter: **a
glass-ui HANDOFF that ships no born-RED kf gate is exactly the M3 "column-migration to
HANDOFF" escape that let the dock stay broken B→C→D→G** (`_SYNTHESIS-deferred-ledger §1
CH-4`). So every glass-ui / value.js / parse-that / deploy row below is written as a
PAIR: the sibling-owned anchor + disposition, AND the kf-side born-RED gate that converts
a silent forever-punt into a *watched* one.

**This charter's four sections, ordered by the gap-scorecard §4 table.** glass-ui leads
(the dock is the live AW-ACTIVE chronic + the Card-specular root + the drawer + the VT
helper); value.js is the next-slice principal (E1/E2 · VJ-F1 · F2/F2b · MCI-5 · VJ-F2 ·
VJ-F4 · F3 · F6, all riding the next re-pin, ZERO kf edit); parse-that carries the one
withheld packrat re-key; deploy carries the P0 CNAME drift.

---

## §1 — glass-ui (`/Users/mkbabb/Programming/glass-ui`, the AW tranche, ACTIVE; consumed PUBLISHED `^3.4.0`)

The demo's motion + dock + dialog + drawer substrate. **The AW tranche is ACTIVE NOW** —
the dock is being reworked in glass-ui this very cycle (`a-historical-dock` HEAD
`53c1b07`), which is exactly why the dock items are AUDIT + SUGGEST + glass-ui-HANDOFF and
NOT patched in kf. The four glass-ui HANDOFFs: the dock LAG (the chronic), the Card
specular SEAM (the D2/D14 root), the mobile drawer spring, and the `{types}` VT helper.

### GH-1 (= §4 row 1 / D5-b / CH-4 / H-dock-2) — the dock LAG: the pre-AW.W2 `--spring-dock` (the CHRONIC; now a **kf SHIP-in-H consume-leg BUMP** PAIRED with born-RED `proof:dock-morph-settled`)

> **HARDEN-RESOLVED (BLK-5, this DEEP pass).** The original framing of GH-1 as a
> *glass-ui-HANDOFF awaiting release of `53c1b07`* is **STALE**. Registry + repo re-check
> this pass: `npm view @mkbabb/glass-ui versions` = `… 3.5.0, 3.5.1, 3.6.0` (ALL
> PUBLISHED); `git merge-base --is-ancestor 53c1b07 v3.5.0` → **YES** (the AW.W2 dock
> retune commit IS shipped). The work is NOT release-blocked. kf still pins
> `@mkbabb/glass-ui ^3.4.0` (`package.json:103`) and the installed token is still the OLD
> bouncy register, so the gate is HONESTLY born-RED — but the FIX is a **one-line
> `package.json` bump `^3.4.0 → ^3.5.1` (or `^3.6.0`)**, a kf consume-leg that SHIPs in H,
> NOT a wait. Treating it as a wait would PUNT a shippable fix = the exact M3
> silent-forever-punt the chronic-closure discipline exists to prevent. The rest of GH-1
> below is corrected accordingly; "NO kf fork of the spring" still holds (a bump consumes a
> published token, it does not fork it).

- **The chronic, named once.** The dock is CH-4 — the chronic that escaped B→C→D→G by
  **column-migration to HANDOFF** (M3): the kf-side rename/barrel chore CLOSED (D.W5/G.W12),
  but the BEHAVIOURAL lag was perpetually routed glass-ui-HANDOFF and **never gated**, so a
  correct handoff became a silent forever-punt (`_SYNTHESIS-deferred-ledger §1 CH-4`). H's
  repair is this PAIRED row.
- **glass-ui side — the work is DONE and PUBLISHED.** glass-ui replaced its
  CSS-transition dock morph with a `SpringProgress`-driven engine that dogfoods keyframes.js
  (`useLayerTransition.ts`, 397L: a native `startViewTransition` path + a FLIP fallback off
  ONE `SpringProgress` clock, velocity-continuity on retarget — the iOS interruptible-spring
  contract, `a-historical-dock §1.3`). The morph spring is mirrored to the build-time CSS
  token `--spring-dock` so the JS-driven and CSS curves cannot drift
  (`proof:spring-tokens-synced`). The **AW.W2 retune** (glass-ui `53c1b07`, dated Jun 7
  13:48 — response `0.32`, ζ `0.7`, ~+4.6% overshoot, settling ~167ms / 11 rising frames)
  landed **AFTER the 3.4.0 cut** (`7306fa2`, Jun 7 04:07) but IS now **published in glass-ui
  `3.5.0` / `3.5.1` / `3.6.0`** (verified: `53c1b07` is an ancestor of `v3.5.0`). kf has
  simply **not yet bumped** off the `^3.4.0` pin — the consume-leg is the whole remaining
  motion.
- **LIVE, re-verified this pass (the BITE).** kf consumes glass-ui 3.4.0
  (`node_modules/@mkbabb/glass-ui/package.json` → `3.4.0`). The installed token is the OLD
  bouncy register: `node_modules/@mkbabb/glass-ui/dist/styles/tokens.css:163` —
  `--spring-dock: linear(0, 0.10932 2.041%, 0.35412 4.082%, … 1.16292 14.286%, …)` = the
  **(0.5, 0.5)** register, **ramp peak `1.16292` = +16.3%** (the sampled `linear()` stop
  maximum, the value a token-peak gate parses; the analytic spring overshoot for (0.5,0.5)
  is ~+18.5%, the value the AW.W8 note cites). Confirmed by the AU.W8 authoring note at
  `tokens.css:1297` (*"`--spring-dock` ((0.5, 0.5), ~+18.5%) sits in the springy 15-30%
  band — the user-felt iOS bounce"*) and `--dock-resize-spring: var(--spring-dock)`
  (`tokens.css:1301`). The 3.3.0 `container-type: inline-size` sliver regression
  (`5c0f529`) is **already healed** by 3.4.0 (`a-historical-dock §2`: live
  `.glass-dock containerType: "normal", contain: "none"`, collapsed rect 106×55px,
  content-sized) — so D5-as-layout-collapse is DONE; only the spring FEEL remains.
- **Disposition: kf SHIP-in-H consume-leg BUMP — `@mkbabb/glass-ui ^3.4.0 → ^3.5.1` (or
  `^3.6.0`).** The retune is published (3.5.0+); the consume is a one-line `package.json`
  bump + `npm install`. **NO kf-side patching/fork of the spring** (the token + the JS
  `DOCK_SPRING` const are paired + gate-locked `proof:spring-tokens-synced`; a bump consumes
  the published token, it does not fork it — `a-historical-dock §4`,
  `a-glass-ui-consumption D5`). The memory rule (dock changes live in glass-ui, never
  patched in kf) is CORRECT, not relaxed — and a version bump is the idiomatic CONSUME, not
  a kf-side patch. **MEASURE-FIRST blast-radius:** the 3.4.0→3.5.x bump crosses dock-layering
  polish — re-run `proof:dock-dropdown-opens` + a dock slot-shape check post-bump; the bump
  is not free of other glass-ui deltas. **Demo self-alias scope:** confirm whether the live
  demo reads `node_modules/@mkbabb/glass-ui` or the `vite.config.ts:142-146` dedup alias to a
  checked-out glass-ui — live evidence is that the demo reads the OLD `+16.3%` register, so
  the installed package path IS effective and the bump WILL land in the demo.
- **The PAIRED born-RED kf gate — `proof:dock-morph-settled` (THE TERMINAL):** **adopt the
  TOKEN-PEAK form** — parse the installed `--spring-dock` `linear()` stops and assert ramp
  peak ≤ **~+6%**. **Born-RED today (peak `1.16292` = +16.3%); GREEN the instant kf bumps to
  `^3.5.1`** (the retuned token's peak is ~+4.6%). This is the trivially-measurable,
  flake-free born-RED form. The morph-geometry form (sample the VT pseudo-elements OR the
  `SpringProgress` clock — NOT live `getBoundingClientRect`, per the §H-dock-4 caveat below;
  peak overshoot ≤ ~6% AND settle ≤ ~200ms) is **secondary/RECORD-WITHHELD**: the live morph
  is NOT reliably driveable (a deep harden lane captured 181 samples with no morph captured),
  so the token-peak parse is the gate that actually bites. This pairing is the
  chronic-closure terminal — it converts the silent HANDOFF into a *watched* one that
  cannot become another forever-punt (`a-historical-dock H-dock-2`,
  `_SYNTHESIS-deferred-ledger §1 CH-4`).
- **The H.W8 ledger meta-gate enforces the pairing:** a bare HANDOFF/consume-leg row with no
  born-RED gate REDS the ledger (`_SYNTHESIS-gap-scorecard §3 H.W8`).
- **Sequencing:** kf bumps `@mkbabb/glass-ui ^3.4.0 → ^3.5.1` (the published retune) → the
  `--spring-dock` ramp peak drops to ~+4.6% → `proof:dock-morph-settled` (token-peak form)
  flips GREEN. Independent of every other H wave; no upstream wait remains.
- **MEASURE-FIRST honesty (the §H-dock-4 caveat, carried so H does not false-GREEN):** a
  rAF `getBoundingClientRect` loop on `.glass-dock` UNDER-reads the morph. **The dock has TWO
  distinct morph arms and the instrument must name which it samples** (the demo forks on
  `'startViewTransition' in document`, `useLayerTransition.ts`): (a) the **native VT arm** —
  the morph runs on browser View-Transition *snapshots* (`::view-transition-group/old/new`),
  invisible to live-element geometry reads, so this arm is sampled via the
  `::view-transition-*` pseudo-elements (or not at all on this path); (b) the **FLIP
  fallback arm** — the morph runs in pixel space off the `SpringProgress` clock, sampled via
  that clock directly. The token-peak gate (above) sidesteps BOTH arms (it parses the static
  `--spring-dock` token, not a live morph) — which is precisely why it is the gate that
  bites (`a-historical-dock §2`). (The demo-side `collapse-delay`
  2500→~1000ms tune is a kf-demo SHIP, H.W2-adjacent, MEASURE-FIRST — NOT this charter;
  `a-glass-ui-consumption D5`, `ChromeDock.vue` `:collapse-delay="2500"` — re-anchored live
  to `ChromeDock.vue:116`.)
- **The demo-side D5 contributors are NOT this charter's to gate, but they MUST be homed in a
  kf-demo wave (latent M3 if left charter-only).** Two demo-owned lag contributors travel
  with the dock chronic and are currently named ONLY in `H.md` with no wave anchor: (i) the
  `collapse-delay` 2500→~1000ms tune (`ChromeDock.vue:116`) — gate it with
  `proof:dock-actions-reachable` (a re-armed dock stays reachable within the shortened
  window); (ii) the ~1M px² backdrop-over-moving-scene cost (`a-perf-dock-lag` F1) — gate it
  with `proof:demo-backdrop-budget` (MEASURE-FIRST @dpr=2). Both belong in the kf-demo
  dock-adjacent wave (H.W2-adjacent / H.W7 mobile-dock), NOT this cross-repo charter. Recorded
  here so the kf-demo wave authoring picks them up and they do not silently fall through
  (`a-historical-dock` / `a-perf-dock-lag` F1).
- **Secondary lag suspect to hand off for verification (glass-ui MEASURE-FIRST):** is the
  native VT morph path actually faster than the spring-FLIP on this dock? VT snapshots the
  whole container; a heavy backdrop-filter or sibling capture can jank first expand. glass-ui
  has the detector (`tests/components/custom/dock/dock-animation-live.detect.test.ts`).
  Tag it (`a-historical-dock H-dock-6`). NOT a kf gate.

### GH-2 (= §4 row 2 / D2/D14 / CH-1 root / H.W2 fold) — the Card specular SEAM: wire-or-omit + calmer default (glass-ui-HANDOFF PAIRED with born-RED `proof:no-orphan-specular`)

- **The chronic, named once.** Cartoon-shadow is CH-1 — the chronic that escaped A→C→G by
  **issue-close masquerading as system-close** (M1): C.W2 migrated ONE site
  (`CSSCodeEditor.vue:6`, the only `cartoon` site live), the ledger read "cartoon-shadow —
  CLOSED," and every later tranche treated the SYSTEM as done while the panels regressed to
  glass+specular (`_SYNTHESIS-deferred-ledger §1 CH-1`). The kf-demo BULK (flip panels to
  `surface="cartoon"`) is SHIP-in-H.W2; only the glass-ui Card-component half is this HANDOFF.
- **glass-ui side — the SEAM root-cause (verified live).** glass-ui's `<Card surface="glass">`
  (the default — every demo Card omits `surface=`) emits `glass-specular-track`, whose
  `::before` paints a mouse-tracked radial: `node_modules/@mkbabb/glass-ui/dist/styles/
  glass-specular-track.css` — `background: radial-gradient(circle at var(--specular-x,50%)
  var(--specular-y,50%), hsl(40 30% 100% / 0.55) 0%, … transparent 55%)`,
  `opacity: var(--specular-intensity, 0.35)` (rest 0.35 → hover 0.6 → active 0.85),
  `mix-blend-mode: screen`. **The defect is a SEAM, not perf:** the radial is mouse-tracked
  by `--specular-x`/`--specular-y` but **no host wires the pointer** — live 13 hosts,
  **0 pointer-wired** → it renders as a STATIC centered white bloom, not a catch-light
  (`_SYNTHESIS-gap-scorecard §1.1 D2`, `a-glass-ui-consumption D2`: computed `::before`
  `background = radial-gradient(circle, rgba(255,255,255,0.55) …)`, `opacity 0.35`,
  `mix-blend-mode screen`). The build itself is GPU-cheap + SOTA — **the defect is tuning +
  the unwired seam** (`_SYNTHESIS-gap-scorecard §1.2 ALREADY-SOTA`).
- **The glass-ui-HANDOFF ask (TWO legs).** (a) The Card should **wire-or-omit** its
  specular seam: a glass surface either writes `--specular-x`/`--specular-y` from pointer
  itself (a `useSpecularPointer` internal) OR does not emit `glass-specular-track` — never
  a mouse-tracked radial with no mouse writer. (b) A **calmer DEFAULT intensity** — the rest
  0.35 / hover 0.6 floor is too hot for `resting` glass panels; propose rest ≤ 0.25, radius
  ≤ 40% (`_SYNTHESIS-gap-scorecard §4 row 2`, `a-glass-ui-consumption D14`).
- **Disposition: glass-ui-HANDOFF (Card wire-or-omit + calmer default, BOOK) + kf-demo SHIP
  (H.W2, NOT this charter).** The kf bulk is `surface="cartoon"` on the panel Cards (the
  radial dies at SOURCE — the surface map stops emitting the class, no `!important`/
  `display:none`; the manual `.glass-card` plate is deleted in the SAME motion —
  `_SYNTHESIS-gap-scorecard §2.2`). Where `surface="glass"` is deliberately KEPT (D14), the
  kf demo wires the pointer seam locally (a demo `useSpecularPointer` composable) AND/OR
  tames `--specular-intensity` (a typed, `inherits:false` custom property — owned, gate-able,
  zero cross-repo coupling, `a-glass-ui-consumption D14`). The glass-ui default-softening is
  the BOOK that lands if the harsh default reads as broken in MANY consumers.
- **The PAIRED born-RED kf gate — `proof:no-orphan-specular` (THE TERMINAL):** ZERO
  `.glass-specular-track` on panels OR (if retained) a `--specular-x`/`--mouse-x` writer
  present; a hover-screenshot lock = offset cartoon stamp, no centered radial bloom; paired
  with `proof:cartoon-is-panel-depth` (≥4 panel Cards resolve `box-shadow:
  var(--shadow-cartoon-md)` at rest). **Born-RED today** (13 specular hosts, 0 wired, 0
  panels on cartoon). The kf-demo half greens it; the glass-ui default-softening cannot
  silently re-take the panels without reddening it — the chronic-closure terminal
  (`_SYNTHESIS-gap-scorecard §3 H.W2`, `_SYNTHESIS-deferred-ledger §1 CH-1`,
  `a-glass-ui-consumption` `proof:no-stray-radial`).
- **Sequencing:** the kf-demo `surface="cartoon"` flip ships NOW (the token is published in
  3.4.0 — `--shadow-cartoon-sm/md` live) and greens the gate independent of glass-ui. The
  glass-ui default-softening is the slower BOOK that, once consumed, must keep the gate green.

### GH-3 (= §4 row 3 / D13 / H.W7 fold) — the mobile drawer spring (glass-ui DrawerContent `spring` prop = BOOK; the demo SHIPs a bespoke sheet — PAIRED with born-RED `proof:drawer-spring`)

- **glass-ui side — the vaul-vue constraint.** The demo's mobile drawer historically rode
  `<Drawer>` from glass-ui, whose open/close is **not the demo's to set**: vaul-vue sets
  `transition: transform .5s cubic-bezier(.32,.72,0,1)` and glass-ui documents *"we do NOT
  override `transition`"* (`drawer.css:30`, `a-glass-ui-consumption D13`). So that path is a
  500ms non-springy curve — exactly the "too slow, not springy" the user reports.
- **THE ADJUDICATION (gap-scorecard §4 row 3 — this is the one row where the charter says
  SHIP-in-kf, not HANDOFF).** `a-mobile-architecture` proves the demo drawer is **BESPOKE**
  (not vaul/Sheet). Therefore the springy mobile sheet is **SHIP-in-H.W7** — the demo
  dogfoods `SpringProgress` for its OWN bottom-sheet (snappy preset, <350ms, PRM single-frame
  snap), replacing the 550ms CSS `grid-template-rows` ease (inv ζ, the chrome dogfoods the
  engine). The glass-ui `DrawerContent` `spring` prop ask (a springy
  `SpringProgress`/`linear(spring)` curve at ~240-300ms, OR a `transition`/`spring` prop so
  consumers opt in) is a **glass-ui-HANDOFF BOOK** — only for any glass-ui-owned drawer the
  demo still consumes, not the bespoke sheet (`_SYNTHESIS-gap-scorecard §4 row 3`,
  `a-glass-ui-consumption D13`).
- **Disposition: SHIP-in-H.W7 (the bespoke sheet, kf-demo) + glass-ui-HANDOFF (the
  `DrawerContent spring` prop, BOOK — for any retained glass-ui drawer).** A demo-side
  `transition` override fights vaul's internal write → not idiomatic → that is why the
  glass-ui-owned path is a HANDOFF and the demo path is a bespoke SHIP.
- **The PAIRED born-RED kf gate — `proof:drawer-spring` (THE H.W7 terminal):** the sheet
  motion is a `SpringProgress` subscription (NO `transition: grid-template-rows`, NO 500ms
  `cubic-bezier`), settle < 350ms, spring-shaped trace, single-frame PRM snap. **Born-RED
  today** (the live drawer is a 550ms CSS ease). This is a kf-demo gate; the glass-ui BOOK
  rides alongside (`_SYNTHESIS-gap-scorecard §3 H.W7`, `a-glass-ui-consumption`
  `proof:drawer-springy`).
- **Sequencing:** the bespoke `SpringProgress` sheet ships in H.W7 (no glass-ui dependency);
  the glass-ui `DrawerContent spring` prop is a BOOK that lands on glass-ui's cadence.

### GH-4 (= §4 row 4 / GG-3 / H-1 / FB-4 enabler) — the `{types}` directional VT helper (glass-ui-HANDOFF, AW; PAIRED with born-RED demo-smoke VT-types assertion)

- **glass-ui side.** Grow `startViewTransition` from the bare-callback form to ALSO accept
  the object form `{ update, types? }`, and ship the paired
  `:active-view-transition-type(forward|backward)` CSS recipe. The platform half is Baseline
  (Active view transition Baseline 2026-01-13 — PAST). The directional-VT recipe is the
  enabler for FB-4 (typed/directional scene transitions); the demo VT consumer
  (`useSceneTransition.ts`) WAITS on it (`_SYNTHESIS-gap-scorecard §4 row 4`,
  `a-deferred-ledger §6 GG-3`). glass-ui's AW tranche owns it.
- **Disposition: glass-ui-HANDOFF (AW). The demo VT consumer (FB-4 / GG-4) lands when the
  helper publishes** — a kf-demo BOOK that derives `dir` from the scene-index delta and
  passes `{types}`. Per the H posture, AUDIT + SUGGEST + TAG; do NOT author the helper in kf.
  The current crossfade is functional + Baseline-degrade-clean (low urgency).
- **The PAIRED born-RED kf gate:** a browser-driven VT-types assertion in the EXISTING
  `demo-smoke` Chromium job (NOT a new gate) — born-RED until the glass-ui helper lands AND
  the demo consumer passes `{types}` (`_SYNTHESIS-deferred-ledger §3.3 FB-4`, paired-gate per
  the M3 discipline). Ties to D11 (scene interactivity).
- **Sequencing:** glass-ui lands the helper + CSS → the demo derives `dir` from scene order
  and passes `{types}` → the born-RED demo-smoke assertion flips GREEN. The kf vitest
  motion-core stub already `satisfies`-tracks the real types (GG-2, done in G.W12), so the
  signature drift is auto-caught.

### GH-5 (= GG-6, carried) — the one direct `reka-ui` `SelectIcon` reach — **HARDEN-RESOLVED: ALREADY-RETIRED (RECORD)**

> **HARDEN-RESOLVED (HS-HIGH-2, this DEEP pass).** Re-verified live:
> `grep -rn 'from "reka-ui"' demo/` returns **0 matches in demo source** (the only hits are
> in built `demo/app/dist/*.js` artifacts, not authored code). The named `SelectIcon`
> direct-reka reach is **already retired** — the demo consumes glass-ui's `Select`/`Dock*`
> trigger families which own the chevron slot. So the original GH-5 gate as authored
> (`grep ZERO from "reka-ui"`, born-RED on a live reach) does **not bite** — there is
> nothing to red against in source. The gate as written would also red on legitimate
> vendored shadcn `ui/` imports if scoped repo-wide. GH-5 is therefore downgraded to RECORD,
> with the gate reframed to a NO-NEW-reach invariant.

- **glass-ui side.** glass-ui consumes `SelectIcon` internally but does not re-export the raw
  reka primitive from its `./select` subpath. The demo HISTORICALLY reached PAST glass-ui to
  reka-ui for one chevron-slot primitive; that reach is **gone** (the demo now consumes
  glass-ui's `Select`/`Dock*` trigger families). The optional glass-ui re-export of the reka
  primitives its `Select` family composes remains a LOW-urgency nicety, not a kf need
  (`a-deferred-ledger §6 GG-6`, `G/handoff GG-6`).
- **Disposition: RECORD / ALREADY-RESOLVED.** No kf action owed (the reach is retired); the
  glass-ui re-export is an optional convenience HANDOFF only.
- **The PAIRED kf gate (reframed to bite something real):** a `grep` clause in
  `proof:boundary`/`proof:decomposition` asserting **no NEW raw-reka reach** — `from
  "reka-ui"` is forbidden OUTSIDE `demo/@/components/ui/` (vendored shadcn) AND outside any
  glass-ui-owned family. The bare repo-wide `grep ZERO from "reka-ui"` is INFEASIBLE (it
  reds on legitimate vendored `ui/menubar/` shadcn) — scope it to the new-reach guard. Green
  today; bites only on a regression.
- **Sequencing:** none required — the reach is already retired. If glass-ui ever re-exports
  the reka primitives, the demo would import from glass-ui; not a precondition for anything.

### glass-ui §ALREADY-SOTA (BINDING — manufacture NO work)

- **The dock's `SpringProgress`-driven morph engine** (`useLayerTransition.ts`) — it
  dogfoods keyframes.js, runs ONE clock with velocity-continuity on retarget (the iOS
  interruptible-spring contract), and pairs the JS `DOCK_SPRING` to the `--spring-dock` token
  gate-locked. The dock is NOT "broken many versions ago and lost" — its LOCAL animation
  engine was deliberately replaced by glass-ui's SUPERIOR spring/VT engine; the two
  regressions are pure consume-leg gaps (the unretuned spring + the unwired @mbabb popover),
  not an architecture loss (`a-historical-dock TL;DR/§6`). Do NOT re-introduce the old local
  dock.
- **The specular `::before` BUILD** — GPU-cheap, SOTA-built; the typed `--specular-intensity`
  (`inherits:false`) is a clean knob. Its defect is tuning + the unwired SEAM, NOT perf or
  authoring (`_SYNTHESIS-gap-scorecard §1.2`, `a-glass-ui-consumption D14`).
- **glass-ui CONSUMPTION hygiene** — the LabeledField/forms/subpath consumption is exemplary
  (`LabeledSelect/Input/Slider/Switch`, the tree-shaken subpaths); `design-idioms.css` OWNS
  the demo's playful vocabulary (`--rainbow-*`/`--color-gold`/`.scale-on-hover`). The
  spring-token cascade, `springLinearStops()` value.js-free enabler, motion-core SCC
  boundary, and the VT a11y focus-route are idiomatic. No kf action
  (`a-glass-ui-consumption Verdict`, `a-deferred-ledger §9`).
- **The mobile affixed-dock + safe-area scaffolding** — both docks `position:fixed` honoring
  `--work-area-*-offset` + `@supports not (dvh)` fallbacks. The mobile SKELETON is correct;
  only the COMPOSITION (stack vs overlay, H.W7) is GAP (`_SYNTHESIS-gap-scorecard §5.7`).

---

## §2 — value.js (`/Users/mkbabb/Programming/value.js`, version `0.11.1`; CHRONIC-by-design C-1)

The next-slice principal. `0.11.1` is consumed (the re-pin LANDED in G; the F slice
A1/A2·B1b/B3+B5·C1·C5·D2·F7 is live kf behaviour through the seam — `a-deferred-ledger §1`).
**C-1 (the value.js cross-repo charter) is CHRONIC-by-design and CORRECT** — value.js is
dirty+active, a slice ships every tranche, and the test for legitimate chronic vs papered
punt is *"did the PRODUCT move?"* — for C-1, YES (the re-pin lit the F wins this very
cycle, `_SYNTHESIS-deferred-ledger §3.9`). The rows below are the value.js NEXT-wave slice,
each re-verified OPEN in `0.11.1` this pass, each **value.js-HANDOFF (OPEN)** with the kf
consumer-half riding the next re-pin through the unchanged `lerpValue` seam
(`engine.ts:779`) with ZERO kf edit.

### VJ-1 (E1/E2) — the `linear()` / `steps()` PARSER → `LinearStop[]` (kf's `parseLinearStops` shim RETIRES on land) — value.js-HANDOFF (= C-4)

- **value.js side:** value.js grows the `linear()` / `steps()` easing parsers → `LinearStop[]`.
  **OPEN** — re-verified live: value.js exports `cssLinear` (the EVALUATOR) but
  `parseLinearStops === undefined` / `parseLinear === undefined`. kf's LOCAL reader shim
  already landed (`utils.ts:106 parseLinearStops`, `:192` call-site, `a-deferred-ledger §2`).
  `linear()` Baseline-WA 2026-06-11 (PAST).
- **The no-legacy collapse (the H discipline):** when value.js E1 lands, kf's
  `parseLinearStops` shim RETIRES ONTO it in the SAME motion — no compat alias kept beside it
  (a replaced surface replaced in ONE motion). The shim is NOT a blocker today (it works); it
  rides until the publish.
- **Disposition: value.js-HANDOFF (OPEN, MED-HIGH) + kf-RETIRE-on-land.** kf consume-leg:
  DELETE the shim, import value.js's parser, on the publish.
- **The PAIRED kf gate:** `proof:roundtrip-easing` (already in tree, G.W4) stays green
  through the swap; AND after the publish, `grep parseLinearStops src/` = 0 (the shim is
  gone, not aliased). value.js side: the parser round-trips `linear(0, 0.25 75%, 1)` /
  `steps(4, jump-end)` byte-identically.
- **Sequencing:** value.js publishes E1/E2 → kf retires the shim on the next re-pin.

### VJ-2 (VJ-F1) — the path-geometry sampler (unblocks MorphSVG / numeric MotionPath) — value.js-HANDOFF (OPEN) + kf BOOK (= FB-3 / C-5)

- **value.js side:** a path-`d` → length-parametrized bézier sampler (`getPointAtLength`/
  `getTotalLength`/arc-length curve sampling). **OPEN** — re-verified:
  `v.getPointAtLength === undefined`, `v.samplePath === undefined`. kf shipped the CSS-native
  sliver (`motion-path.ts`) + `fromDrawSVG` (one `getTotalLength()` DOM read, G.W13) but NO
  arc-length curve sampler (`a-deferred-ledger §2`). The one real persisting
  competitor-feature gap.
- **Disposition: value.js-HANDOFF (OPEN) + kf BOOK.** Sequence VJ-F1 (value.js) FIRST, then
  the kf `fromMorphSVG` / numeric-MotionPath consumer wave. `DrawSVG` is INDEPENDENT (it
  needs no sampler).
- **The PAIRED kf gate:** `proof:morphsvg` is the BOOK gate — carried, not built until the
  sampler publishes (an arc-length-uniform-spacing parametrization test). value.js side: the
  sampler resolves N points within ε of a closed-form circle/line reference.
- **Sequencing:** value.js publishes the sampler → the kf MorphSVG BOOK un-BOOKs.

### VJ-3 (F2/F2b) — `currentColor` / `light-dark()` / `contrast-color()` color sentinels — value.js-HANDOFF (OPEN, HIGH)

- **value.js side:** the color sentinels. **OPEN** — re-verified:
  `parseCSSColor('light-dark(red,blue)')` **THROWS** `Parse error at offset 0`. kf has 0
  policy because they don't parse (`a-deferred-ledger §2`).
- **Disposition: value.js-HANDOFF (OPEN; HIGH for `currentColor`/`light-dark` — Baseline
  2024-05-13; `contrast-color` BOOK — Baseline Apr-2026, black/white-only, must NOT alias to
  `safeAccentColor`).**
- **The PAIRED kf gate:** a kf `interpolate-anything` corpus row over `light-dark(...)` /
  `currentColor` endpoints — currently absent (would throw); greens when value.js parses
  them. value.js side: the sentinels parse + resolve against a known theme.
- **Sequencing:** value.js publishes the sentinels → kf adds the corpus rows + any
  eligibility policy in the same motion.

### VJ-4 (MCI-5) — the identity-aware fn-arity pad (the `it.fails` witness IS the consume signal) — value.js-HANDOFF (OPEN)

- **value.js side:** the identity-aware function-arity pad (e.g. `brightness` holds `1` at
  t=0, not `0`). **OPEN.**
- **The witness, LIVE this pass (the born-RED gate ALREADY in tree).** kf's witness
  `it.fails(...)` is GREEN today — verified at `test/interpolate-anything.test.ts:256-262`
  (*"`it.fails` is GREEN. When MCI-5 lands, the slot holds 1, the inner PASSES, and
  `it.fails` FLIPS RED → remove this wrapper"*). This is the model paired gate: **the
  `it.fails` witness IS the consume-leg signal — it FLIPS RED the instant value.js lands the
  pad**, at which point H deletes the wrapper. No NEW H gate is owed; the born-RED gate is
  already authored (`a-deferred-ledger §2`, `_SYNTHESIS-gap-scorecard §4`).
- **Disposition: value.js-HANDOFF (OPEN). The `it.fails` witness IS the signal.**
- **The PAIRED kf gate:** `test/interpolate-anything.test.ts:256-262` (the existing
  `it.fails` witness — flips RED on land, then the wrapper is deleted and the inner assertion
  stands).
- **Sequencing:** value.js publishes MCI-5 → the witness flips RED → H deletes the wrapper in
  the SAME motion (no-legacy).

### VJ-5 (VJ-F2) — the structured parse-error SINK + `tryParse` `furthest` swap — value.js-HANDOFF (OPEN, HIGH) + kf BOOK (= LD-DIAG / the `ResolvedKeyframes.diagnostics` channel)

- **value.js side, TWO legs:** (a) `tryParse` reads `state.offset` (the backtrack-RESTORED
  point), NOT `state.furthest` (the real derail point parse-that `0.9.0` now exposes) — the
  one-field swap is strictly-more-correct, isomorphic for non-backtracking parses; (b)
  value.js exposes a structured `parseResultWithDiagnostics` returning the parse-that `0.9.0`
  `Diagnostic[]` under `enableDiagnostics()`. **OPEN** — the producer half (parse-that PT-1)
  is ALREADY real + root-exported in `0.9.0`; both consumers (value.js + kf) are dark
  (`a-deferred-ledger §2/§3`, `_SYNTHESIS-gap-scorecard §4`).
- **kf consume-leg (BOOK, GATED on the value.js sink):** the kf adapter carries
  `diagnostics?: Diagnostic[]` on `ResolvedKeyframes`, populated on a malformed parse — the
  editor demo's parse-error channel replacing the silent collapse / bare throw. Do NOT
  half-wire — populated end-to-end or not declared.
- **Disposition: value.js-HANDOFF (OPEN, HIGH) + kf BOOK (gated).**
- **The PAIRED kf gate:** a kf `fromString` test over a malformed `@keyframes`
  (`@keyframes x { 50% { transform: } }`) asserts a non-empty `diagnostics` array with the
  `furthest`-correct offset / `expected` set — reds today (no field), greens when wired;
  bite-control: well-formed keyframes → empty/absent.
- **Sequencing:** parse-that (DONE) → value.js (swap + sink) → kf
  (`ResolvedKeyframes.diagnostics` seam, BOOK). Gated on the value.js sink.

### VJ-6 (VJ-F4) — the buffer-reusing `unflattenObjectToString(flat, out?)` serializer (the per-frame DOM-write garbage) — value.js-HANDOFF (OPEN, = G-3, ZERO kf edit on consume)

- **value.js side:** a caller-owned out-buffer overload that null-fills a compile-stable
  string buffer instead of minting `{}` + per-key `.split(".")` + concat garbage EVERY
  frame. **OPEN** — only the byte-identical allocating form exists; live call-sites still
  allocate per-frame (`utils.ts:370`, `waapi.ts:267`, `format.ts:113,150` — the real
  per-frame DOM-write garbage, `a-deferred-ledger §2`).
- **Disposition: value.js-HANDOFF (OPEN) + kf-consume-on-re-pin (ZERO kf edit when the
  overload lands — thread a per-animation string buffer through `transformTargetsStyle`).**
- **The PAIRED kf gate:** fold the DOM-write into the existing `proof:zero-alloc` contract.
  **NB the write path is currently OUTSIDE the zero-alloc lock** (a named gate gap worth
  closing on consume). value.js side: a `%HasFastProperties` + alloc-count bench on the
  threaded buffer (the C1/F.W4 template).
- **Sequencing:** value.js publishes the overload → kf threads the buffer in the DOM-write
  fold on the next re-pin.

### VJ-7 (F3) — the bounded LRU on the value.js result cache (lives ONCE in value.js, = C-3) — value.js-HANDOFF (OPEN)

- **value.js side:** an LRU eviction bound on the result cache. **OPEN** — no `maxCacheSize`
  cap; `tryParseCache` wholesale-clear (`a-deferred-ledger §2/§5 C-3`).
- **Why ONCE in value.js:** the re-pin makes a kf-side SECOND eviction policy a DRY violation
  — the bound belongs where the cache lives. kf must NOT add a parallel policy.
- **Disposition: value.js-HANDOFF (OPEN). kf-side: NOTHING (consumes the bound on re-pin).
  Re-open trigger UNCHANGED: a measured editor footprint (none exists).**
- **The PAIRED kf gate:** NONE (kf has no second policy by design — RECORD that no kf
  eviction policy is owed). value.js side: a cache-size bench under an unbounded-key workload
  showing the bound holds + evicts LRU.
- **Sequencing:** independent of the kf consume-leg; value.js sequences against its own LRU
  discipline.

### VJ-8 (F6) — parser-free easing + leaf-math sub-path (paired kf FOLD) — value.js-HANDOFF (OPEN)

- **value.js side:** the parser-free easing + leaf-math sub-path. **OPEN**
  (`a-deferred-ledger §2`).
- **Disposition: value.js-HANDOFF (OPEN); paired kf FOLD** — on land, DELETE the
  `internal/leaves.ts` shadow and static-re-export the canonical `clamp`/`scale`/`lerp` (the
  no-legacy collapse). Re-verify post next re-pin.
- **The PAIRED kf gate:** after the publish, `grep` the `internal/leaves.ts` shadow = gone,
  canonical re-export in place; `proof:boundary` stays green (the light modules carry zero
  static value.js edge).
- **Sequencing:** value.js publishes → kf folds the shadow on the next re-pin.

### VJ-9 (LD-PT-2 / G-HANDOFF-1) — value.js re-pins its OWN parse-that (the realm convergence) — value.js-HANDOFF (OPEN, the hard predecessor)

- **value.js side:** value.js re-pins its OWN parse-that `^0.8.2 → ^0.9.0` to collapse the
  dual realm (kf resolves parse-that `0.9.0`; value.js still declares `^0.8.2`). **OPEN.**
  Surfaced **fail-explicit non-gating** by `proof:deps-current` (G.W2). The `as any`
  cross-realm cast (`utils.ts`) compiles + the round-trip is green today
  (`a-deferred-ledger §3 LD-PT-2`).
- **Disposition: value.js-HANDOFF (OPEN). The hard PREDECESSOR of a clean future parse-that
  bump.** NOT a kf-side shim — sequence value.js-first only when the realm actually bites.
- **The PAIRED kf gate:** the `proof:deps-current` dual-realm convergence clause (the
  parse-that realm kf resolves and the realm value.js resolves are the SAME minor) — converts
  the silent `as any` hazard into a gated invariant; non-gating until it bites.
- **Sequencing:** value.js re-pins its own parse-that → the realms converge → the
  `proof:deps-current` clause stays GREEN.

### value.js §ALREADY-SOTA (BINDING — manufacture NO work)

- **The single-dispatch interp seam** (`lerpValue`, the H live anchors `engine.ts:18`
  import / `:779` the `processFrame` call site) — the structural reason every value.js row above is zero-kf-edit. No
  refactor. **All kf-consumed value.js names survive `0.11.1`** — the re-pin was non-breaking
  (`a-deferred-ledger §9`, `_SYNTHESIS-gap-scorecard §5.1`).
- **The light/heavy boundary** — light modules (`NumericAnimation`/`SmoothProgress`/
  `SpringProgress`/`Timeline`/`ElementMorph`) carry ZERO static value.js edge
  (`proof:boundary` self-enforces); the heavy surface imports value.js directly. **The color
  science** (oklab/oklch perceptual lerp, gamut mapping, CSS-Color-4 hue short-way) + **parse
  breadth** + **interp dispatch** at or ahead of SOTA — "the gaps are churn + memo-key cost +
  a handful of spec parsers, not science." `0.11.1` closed the churn + the memo-key cost.
- **The D1 frozen-shape monomorphization** is a measured non-win (mono≈mega), NOT shipped in
  0.11.1; **SUP-7 bit-packing** is a KILL three ways (no headroom). Do NOT re-litigate
  (`a-deferred-ledger §8`).

---

## §3 — parse-that (`/Users/mkbabb/Programming/parse-that`, version `0.9.0`)

The transitive sibling. `0.9.0` landed the four F waves clean (PT-1/PT-2/PT-3), consumed
via the G re-pin RP-4 (**done**). kf reaches parse-that through exactly ONE direct import
(`any`, `utils.ts:1`, still root-exported by `0.9.0`); the rest is value.js-transitive. One
item remains the carried WITHHELD.

### PT-1 (LD-PT-1 / PT-4) — the `(id,offset)` packrat re-key (`proof:packrat-position` FIRST, THEN re-key) — parse-that-HANDOFF (internal, NOT a re-pin predecessor)

- **parse-that side:** the packrat `MEMO` is still `id`-only-keyed, NOT `(id,offset)`
  (`packrat.ts:61,82` — `MEMO.get(p.id)`). PT-WAVE-2 documented this as a KNOWN LIMITATION
  (`packrat.ts:16-26`) and BOOKED the sound Warth-Douglass-Millstein `(id,offset)` re-key as
  a dedicated packrat-soundness item (`a-deferred-ledger §3 LD-PT-1`,
  `_SYNTHESIS-deferred-ledger §3.5`).
- **The HARD sequencing constraint:** **build `proof:packrat-position` FIRST, THEN re-key.**
  The re-key is a soundness change to a hot primitive — it MUST land behind a falsifiable
  position-correctness gate, not asserted (the F discipline: advisory→hard,
  no-ship-on-assertion).
- **Why it is NOT a consumer-facing defect:** packrat is OFF the default path; NEITHER kf NOR
  value.js opts into it (zero production consumers, isolated opt-in BBNF left-recursion path).
  So it is parse-that-internal and does NOT block the re-pin or any kf wave.
- **Disposition: parse-that-HANDOFF (internal soundness; NOT kf/H scope, NOT a re-pin
  predecessor).** Recorded so no H lane mistakes it for a consumer-facing defect.
- **The PAIRED gate (parse-that-owned, no kf gate):** `proof:packrat-position` — a
  backtracking grammar memoizes the SAME `id` at two distinct offsets; the re-keyed `MEMO`
  returns the offset-correct cached result; the `id`-only key returns the WRONG one (the gate
  reds pre-re-key, greens post). No kf gate (zero production consumers).
- **Sequencing:** `proof:packrat-position` lands FIRST, THEN the `(id,offset)` re-key. Owned
  by the parse-that owner against its own discipline; independent of all H waves.

### PT-2 (PT-3b) — the span-first core unification — parse-that-HANDOFF (BOOK)

- **parse-that side:** span-first core unification — a dedicated parse-that tranche, not an H
  ship (`a-deferred-ledger §3 PT-3b`).
- **Disposition: parse-that-HANDOFF (BOOK).** No kf gate; recorded for completeness.

### parse-that §ALREADY-SOTA (BINDING — manufacture NO work)

The leaf tier — `string()` charCode fast path, `regex()` sticky-`y` zero-alloc `test()`,
the `Int8Array(128)` first-char `dispatch`, the `trimStateWhitespace` charCode loop — at/
beyond the JS-combinator frontier. PT-WAVE-1/2/3a all LANDED CLEAN; the §1.5
`parseSingleValue`/`parseFunctionArgs` expose LANDED (the producer half of value.js's
deepest adoption + the parse-that PT-1 diagnostics producer). **PT-5** (per-combinator
closure alloc, build-time) is RECORD — ALREADY-SOTA; do NOT chase the un-portable Rust
`SmallBox`. Manufacture no parse-that work here (`a-deferred-ledger §3/§9`).

---

## §4 — deploy (deploy/fourier-owned; named for completeness, NOT a kf write)

These are constellation/deploy items the kf tree touches but that the deploy spine
(fourier) owns — recorded so the sequencing is explicit, NOT written here.

### DEP-1 (G-HANDOFF-3, P0) — the `dns-cf-sync.sh` CNAME drift — deploy-HANDOFF (P0, OPEN)

- **deploy side:** fix `dns-cf-sync.sh` `keyframes.pages.dev → keyframes-8uq.pages.dev` (DNS
  drift). **A live-correctness fix, P0** by the grand-audit rating
  (`a-deferred-ledger §7 G-HANDOFF-3`, `_SYNTHESIS-gap-scorecard §4`). **The authoritative
  target is kf's `deploy-pages.yml:4-5`** — a blind sync REGRESSES the live CNAME, so the
  authoring is kf's, the WRITE is deploy's.
- **Disposition: deploy-HANDOFF (P0, OPEN).** Owner: deploy (fourier).
- **The PAIRED gate:** a post-deploy live-CNAME assertion (deploy-owned); kf authors the
  authoritative target, deploy writes + verifies. No kf-tree gate.

### DEP-2 (G-HANDOFF-2) — the CF-Pages deploy template — deploy-HANDOFF (kf AUTHORS, deploy WRITES)

- **deploy side:** distil kf's green-CI-gated `deploy-pages.yml` into the deploy spine's
  `templates/deploy-pages.yml` CF-Pages template. kf AUTHORS the content + rationale; deploy
  WRITES (`a-deferred-ledger §7 G-HANDOFF-2`).
- **Disposition: deploy-HANDOFF (carried from G; not a kf write).** Owner: deploy (fourier).

### DEP-3 (G-HANDOFF-4 / G-CONST-5/6) — the constellation roster docs-lag — RECORD (fourier-hub)

- The CONSTELLATION roster lags published reality (kf is now `4.1.0`; the spine should bump
  to match). G-CONST-4 phantom submodule = KILL (asymmetry-is-intent). **RECORD
  (fourier-hub / kf-ahead; not a kf write)** (`a-deferred-ledger §7`).

---

## §5 — the cross-repo sequencing DAG (who lands first; what gates the kf consume-leg)

```
THE PAIRING RULE (binding on every row): a HANDOFF is a terminal ONLY when paired with a
born-RED kf gate that flips GREEN on consume. A bare tag REDS the H.W8 ledger meta-gate.

glass-ui (AW tranche, ACTIVE — LEADS)
────────────────────────────────────
 GH-1 dock LAG: 53c1b07 PUBLISHED in 3.5.0+ ─→ kf BUMPS ^3.4.0→^3.5.1 (SHIP-in-H) ─→ proof:dock-morph-settled (token-peak ≤+6%)  [BORN-RED on +16.3% token]
 GH-2 Card specular SEAM: wire-or-omit + calmer default ─┐
   kf-demo surface="cartoon" (H.W2, SHIPs NOW) ──────────┴─→ proof:no-orphan-specular        [BORN-RED: 13 hosts, 0 wired]
 GH-3 DrawerContent spring prop (BOOK) ─ // ─ kf-demo bespoke SpringProgress sheet (H.W7) ─→ proof:drawer-spring  [BORN-RED: 550ms CSS ease]
 GH-4 startViewTransition({types}) + directional CSS ─→ kf demo {types} consumer (FB-4) ─→ demo-smoke VT-types  [BORN-RED]
 GH-5 reka SelectIcon reach ALREADY RETIRED (0 `from "reka-ui"` in demo/ src) ─→ RECORD/ALREADY-RESOLVED + proof:boundary no-NEW-raw-reka clause

value.js (CHRONIC-by-design C-1 — next slice, ALL ride the next re-pin, ZERO kf edit)
─────────────────────────────────────────────────────────────────────────────────
 VJ-1 E1/E2 linear()/steps() ─→ kf RETIRES parseLinearStops shim ─→ grep=0
 VJ-2 VJ-F1 path sampler ─→ kf fromMorphSVG (BOOK) ─→ proof:morphsvg (BOOK gate)
 VJ-3 F2/F2b color sentinels ─→ kf interpolate-anything corpus row
 VJ-4 MCI-5 identity pad ─→ test/interpolate-anything.test.ts:256-262  [it.fails GREEN today; FLIPS RED on land]
 VJ-5 VJ-F2 furthest+sink ─→ kf ResolvedKeyframes.diagnostics (BOOK) ─→ fromString malformed test
 VJ-6 VJ-F4 unflatten out-buffer ─→ kf DOM-write fold ─→ proof:zero-alloc (write-path fold)
 VJ-7 F3 LRU (once in value.js)  [no kf policy by design]
 VJ-8 F6 parser-free easing ─→ kf folds internal/leaves.ts shadow
 VJ-9 LD-PT-2 realm convergence ─→ proof:deps-current dual-realm clause

parse-that (transitive)                          deploy (fourier)
───────────────────────                          ────────────────
 PT-1 (id,offset) re-key                          DEP-1 dns-cf-sync.sh CNAME (P0)
   [proof:packrat-position FIRST; internal,        DEP-2 CF-Pages template (kf AUTHORS)
    zero production consumers, NOT a predecessor]   DEP-3 roster docs-lag (RECORD)
 PT-2 span-first core (BOOK)
```

**The binding orderings.**
1. **glass-ui LEADS** — the dock (GH-1) is the live AW-ACTIVE chronic, but its glass-ui
   half is **already published** (the retune `53c1b07` is in `3.5.0`+), so GH-1 is now a kf
   **consume-leg BUMP** `^3.4.0 → ^3.5.1` that SHIPs in H independent of any upstream wait —
   the single largest cross-repo motion collapses to a one-line `package.json` bump +
   MEASURE-FIRST post-bump re-check. The Card-specular kf-demo half (GH-2, H.W2) SHIPs
   NOW and greens its gate independent of glass-ui; the glass-ui default-softening rides
   behind.
2. **Every value.js row rides the NEXT re-pin** through the unchanged `lerpValue` seam
   (`engine.ts:779`) with ZERO kf edit; the consume-legs are paired gates (VJ-1 grep=0, VJ-4 `it.fails`
   flip, VJ-5 malformed-keyframes test, VJ-6 zero-alloc write fold).
3. **VJ-4 MCI-5 is the MODEL paired gate** — the `it.fails` witness
   (`test/interpolate-anything.test.ts:256-262`) is already born-RED-on-land in the tree; no
   new gate is owed.
4. **PT-1 is parse-that-INTERNAL, NOT a re-pin predecessor** — `proof:packrat-position`
   FIRST, then the re-key; zero production consumers, blocks nothing.
5. **DEP-1 is P0** (the live CNAME drift); kf authors the target, deploy writes + verifies.
6. **The chronic-closure terminal:** GH-1/GH-2/GH-3/GH-4 each carry a born-RED kf gate — that
   pairing is what stops a correct HANDOFF from becoming the M3 silent forever-punt the dock
   suffered B→G. The H.W8 ledger meta-gate enforces it: a bare HANDOFF tag REDS the ledger.

---

## §6 — inv-16 / inv ε compliance

This charter wrote ONLY `docs/tranches/H/valuejs-parsethat-glassui-handoff.md`. ZERO
source/test/CI/demo edits to keyframes.js, glass-ui, value.js, OR parse-that. Every item is
a HAND-OFF the respective sibling owner sequences; the kf-side consume-legs (the glass-ui
bump, the cartoon flip, the bespoke sheet, the value.js folds, the diagnostics seam) are kf
SHIP/BOOK waves authored in `H/waves/`, NOT written here. **inv-16 (H posture): glass-ui /
value.js / parse-that are consumed PUBLISHED — AUDIT + SUGGEST + HAND-OFF; the dock memory
rule is CORRECT and NOT relaxed.**

Every claim cites a phase-1 lane or a live `file:line` re-verified this pass (2026-06-07):
the pins (`package.json:99` `@mkbabb/value.js ^0.11.1` / `@mkbabb/parse-that ^0.9.0`;
`:103` `@mkbabb/glass-ui ^3.4.0` in `optionalDependencies`; the dedup self-alias
`vite.config.ts:142-146`; installed `0.11.1`/`0.9.0`/`3.4.0`); the dock spring
(`node_modules/@mkbabb/glass-ui/dist/styles/tokens.css:163` `--spring-dock: linear(0,
0.10932 … 1.16292 14.286% …)` = the (0.5,0.5) register, ramp peak **+16.3%** / analytic
~+18.5%, confirmed by the AU.W8 note `:1297` and `--dock-resize-spring: var(--spring-dock)`
`:1301`); the AW.W2 retune now **PUBLISHED** (glass-ui `53c1b07`, response 0.32 / ζ 0.7;
`npm view @mkbabb/glass-ui versions` = `3.5.0/3.5.1/3.6.0`, `53c1b07` an ancestor of
`v3.5.0` — so GH-1 is a kf consume-leg BUMP `^3.4.0→^3.5.1`, not a wait); the @mbabb
double-trigger (`demo/app/App.vue:18-21`
`<DropdownMenuTrigger as-child>` over `<DockDropdownTrigger>` — the D9 root, fixed
SHIP-in-H not HANDOFF); the Card specular SEAM (`glass-specular-track.css`, 13 hosts 0
pointer-wired); the value.js OPEN slice (`parseLinearStops`/`getPointAtLength`/a parsing
`light-dark` all `undefined`/THROW; `unflattenObjectToString` allocating-only); the MCI-5
born-RED witness (`test/interpolate-anything.test.ts:256-262`, GREEN today); the parse-that
`id`-only packrat key (`packrat.ts:61,82` + the documented limitation `:16-26`); the
single-dispatch seam (`engine.ts:18` import / `:779` `lerpValue(eased, iv)` in
`processFrame`).

**The §ALREADY-SOTA record is BINDING: this charter manufactures NO work where the engine /
parse / color / re-pin kernels lead.** The whole charter is the chronic-closure discipline
applied to the cross-repo surface: a SMALL, well-homed, sibling-owned set of landings, each
**PAIRED with a born-RED kf gate** so no correct HANDOFF can become a silent forever-punt —
the P-invariant repaired to police the PRODUCT, not the column.
