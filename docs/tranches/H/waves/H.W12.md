# H.W12 — COMPONENT STANDARDIZATION · DECOMPOSITION · ENCAPSULATION · BRITTLENESS/STYLING AUDIT · SEQUENCE+PATH ENRICHMENT + EASTER EGGS (the consolidated round-3 user-feedback fold · the DRY/encapsulation/de-brittle sweep + the affordance enrichment that dogfoods it · I8/I9/I10/I11/I12/I3)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** CORRECTIVE SHIP-in-H (the THIRD user-feedback refinement round, second of its two waves; net-deletion-leaning on the share/de-brittle side — collapse 3–4 hand-rolled drag-scrub copies into ONE `useDragScrub`, lift the motion-path gesture engine out of the Target, resolve the 61 no-op `icon-*` classes into ONE idiom area, kill magic numbers / brittle calc chains; additive on the I3 enrichment side — draggable sequence rows + editable motion-path + a copy artifact + a tangent readout + one easter egg per scene; pure demo CSS/markup/Vue + ONE shared composable + ONE design-idiom area — DOGFOODS public `Sequence.add`/`ManualTimeline`/`setChildTime().render()` (inv ζ, NO hand-rolled rAF), NO engine source (the engine is FENCED, inv ζ / `H.md §Mandate`), NO glass-ui patch in kf — inv-16, NO dist build) · **Scope:** `useDragScrub({ el, onScrub, project })` extracted into `demo/@/composables/` (the I9 encapsulation seam; consumed by `SpringTarget.vue:88-93`'s `positionFromEvent`, `SequenceTarget.vue`'s master-scrub `progressFromEvent`, `MotionPathTarget.vue:124-147`'s `projectPointer`) + share the `.btn-playback` SKIN for sequence Play/Reverse (`PlaybackRibbon.vue:26,73-76` vs sequence's bespoke `grid-cols-4` transport) + the motion-path projection/scrub/`ManualTimeline` gesture engine LIFTED from `MotionPathTarget.vue:79-220` into `useMotionPathDemo.ts` (the thin 49L composable) + the W1 store single-writer / pure-getter audit (`scenePlayback.ts`, `controlOptionsStore.ts`, `animationOptionsStore.ts`) + the demo-decomposition VERIFY + colocate (no demo file >500L today; the engine FENCED) + the brittleness audit (`MotionPathTarget.vue:118-147` the `SAMPLE_STEP=5` magic + the implicit-square-viewBox client→user scale `:131-134`; any residual `.closest(".class")`/`querySelector(".class")` DOM walks; brittle `:deep()` chains) + the styling-idiom localization (`design-idioms.css` gains the `icon-(xs|sm|md|lg)` idiom area — 61 silent no-op refs, e.g. `TimingFunctionPanel.vue:30` `icon-sm`, `:41` `icon-md`; the OWNED-IDIOMS contract extended to the full referenced set; magic-number/brittle-calc cleanup in scene styles) + SEQUENCE+PATH GREATLY REFINED (draggable storyboard rows re-authoring each child's `at:` — `SequenceTarget.vue:19-38`, `useSequenceDemo.ts:117-120`; editable motion-path control points re-emitting `PATH_D` — `MotionPathTarget.vue:18-24,42`, `motionPathGeometry.ts:1-11`; the copy-`offset-path` artifact; the tangent-angle readout) + ONE easter egg per scene (EE-SEQ-1 "the reel"; EE-MP-2 "the emoji winks"; one per other scene from the frontend-design pass) + the gate set (NEW `proof:dragscrub-single`/`proof:composable-encapsulation`/`proof:demo-no-oversize`/`proof:no-brittle-selector`/`proof:styling-idioms`/`proof:sequence-rows-draggable`/`proof:motion-path-editable`/`proof:motion-path-copy`/`proof:easter-egg`) · **DAG-deps:** depends on **H.W11** (the I5 glass-card is the CONTAINER the W12 enrichments live INSIDE — the draggable rows + editable path render within the stage `<Card>`; the I2 DFA must enumerate sequence/path as `{stage-only}` BEFORE W12 enriches their self-contained transport), **H.W5** (RE-OPENS the W5-BOOKed `useDragScrub` extraction — `H.W5.md:66` BOOKed at 2 consumers / MEASURE-FIRST threshold; now at 3–5 consumers, over threshold — and the W5-BOOKed H-MI-4 sequence row-drag + the F4 editable-path elevation), **H.W10** (EXTENDS G3/G6 component normalization to the last two un-normalized scenes — sequence/path — WITHOUT forcing them onto `PlaybackRibbon`), **H.W9 / G.W10 / D.W2** (the styling-idiom localization extends the OWNED-IDIOMS contract; the design cohesion is co-authored with I5's new card register). Sequenced **AFTER H.W11, BEFORE H.W8's GOLDEN `proof:visual-lock` baseline** (the convergent order: **… W9 → W10 → H.W11 → H.W12 → H.W8 golden capture** — the enriched sequence/path + the localized idioms are the FINAL state H.W8 locks). Every browser gate settle-gates on **H.W1**'s FSM resting. The engine is FENCED (inv ζ); the glass-ui LabeledField-subgrid (W11 I1) is the only sibling HANDOFF this wave composes with — W12 emits NONE of its own (it CONSUMES public engine primitives + the in-tree shared seam).

## §Provenance (the folded lanes)

This wave is the SPEC-AUTHOR lane of `audit/feedback/i-_PLAN.md` (the COMPLETE, authoritative round-3 plan — read in full), the third SPEC-DEVELOPMENT triumvirate. The plan folds `i-r5` in full for I3-sequence/path and grounds I8/I9/I10/I11/I12 on direct source reads + git history + the deep-audit lane notes:

- `audit/feedback/i-_PLAN.md` (the BINDING synthesis — read in full) — §1 the two-wave structure (W12 = standardize/decompose/audit gestalt), §3 the per-item W12 clauses (I8/I9/I10/I11/I12/I3), §4 the consolidated gate list, §5 the supersede-map (the RE-OPENed W5 BOOKs), §6 the W5/W10 reconciliation, §7 the I3-rung fork, §8 the convergence sequencing.
- `audit/feedback/i-r5-sequence-path.md` (the BINDING research note for I3 — sequence + path, the ONLY round-3 research note LANDED at plan-author time) — §1.3/§2.3 the headline refinements (draggable rows; editable path; copy artifact; tangent readout), §3.2/R-SEQ-F the `useDragScrub` extraction at the 3–5-consumer threshold (the I8 share-win), §W-MP-5 the thin `useMotionPathDemo` anomaly (the I9 lift), §4 the disposition summary + the gate hooks (`proof:sequence-rows-draggable`/`proof:motion-path-editable`/`proof:motion-path-copy`/`proof:dragscrub-single`), the easter-egg recommendations (EE-SEQ-1 "the reel"; EE-MP-2 "the emoji winks").
- The deep-audit lanes — `a-styling-idioms.md:43-87` (the 61-site `icon-*` no-op idiom — I12), `a-demo-architecture` (the demo decomposition state — I10), `a-store-architecture` (the W1 store consistency tail — I9), `a-scene-spring-sequence.md:273-277` (the W5-BOOKed H-MI-4 sequence row-drag — I3), `a-scene-path-discrete.md:135-137` (the F4 editable-path elevation — I3).
- Direct source reads (cited `file:line` below) on `tranche-h-impl` HEAD `f064cc1` + the W10 working-tree `M` files + the W11 landed forms — every gate's born-RED state re-verified at spec-author time.

**Research-lane note (honest provenance).** Of the five round-3 research notes, only `i-r5` had LANDED at plan-author time; `i-r1..i-r4` were MISSING (written concurrently). This wave grounds I3 on `i-r5` in full and I8/I9/I10/I11/I12 on direct source reads + git history + the deep-audit lanes. When `i-r1..i-r4` land, the IMPL lead reconciles them; nothing here contradicts i-r5 (the I3/I8 clauses are co-authored with it).

Synthesis home: `audit/feedback/i-_PLAN.md` (entire). This wave RE-OPENS the W5-BOOKed `useDragScrub` + H-MI-4 + the F4 editable-path elevation (now over their MEASURE-FIRST thresholds) — the §supersede-map below is the honest ledger.

## §The state, verified (file:line / live anchors from the lanes)

- **I8 — sequence/path/spring/easing each hand-roll the SAME pointer-drag scrub dance.** The rect→ratio + pointer-capture + window-listener pattern is copied: `SpringTarget.vue:88-93` (`positionFromEvent`), `SequenceTarget.vue` (the master-scrub `progressFromEvent`), `MotionPathTarget.vue:124-147` (`projectPointer`). Verified live: `grep -ln 'setPointerCapture\|getBoundingClientRect' demo/spring demo/sequence demo/motion-path` → all three scene targets. The transport diverges too: sequence's bespoke `grid-cols-4` transport vs the standard `.btn-playback` skin (`PlaybackRibbon.vue:26,73-76`). The biggest share-win is the drag seam (i-r5 §3.2). (`i-_PLAN.md §3 I8`)
- **I9 — `useMotionPathDemo.ts` is anomalously thin (the gesture engine lives in the Target).** `useMotionPathDemo.ts` is 49L; ALL the drag/projection/`ManualTimeline` logic lives in `MotionPathTarget.vue:79-220`, unlike sequence/spring whose composables own the engine + transport (i-r5 §W-MP-5). The W1 stores (`scenePlayback.ts`, `controlOptionsStore.ts`, `animationOptionsStore.ts`) carry the single-writer / pure-getter discipline the W1 ST-7/ST-9 findings began. (`i-_PLAN.md §3 I9`)
- **I10 — NO demo file exceeds 500L (the only >500L files are the FENCED engine).** Verified: max demo file `useSpringDemo.ts` 418, `AnimationControlsGroup.vue` 417, `useEasingDemo.ts` 389, `EasingCurveCanvas.vue` 373, `useSequenceDemo.ts` 372, `AnimationControlsControls.vue` 371, `EasingTarget.vue` 339. The engine files (engine.ts 1375, animations.ts 870, group.ts 772, sequence.ts 628, spring.ts 491) are FENCED — do NOT touch (`H.md inv ζ`; the I10 NOTE). So I10 is VERIFY + colocate, NOT manufacture splits — the demo is already D-tranche-decomposed. (`i-_PLAN.md §3 I10`)
- **I11 — the motion-path projection is magic-number + coupling-brittle.** `MotionPathTarget.vue:118-147` uses a bare `SAMPLE_STEP = 5` linear nearest-point search; `:131-134` the client→user-unit scale ASSUMES a square viewBox (an implicit coupling — a future non-square stage would silently mis-project). `:deep()` chains and `.closest(".class")`/`querySelector(".class")` DOM walks are selector-brittle (the W3/W10 era already replaced some with owned `useTemplateRef`s). (`i-_PLAN.md §3 I11`)
- **I12 — the OWNED-IDIOMS contract membership is under-enumerated (the `icon-*` no-op is ALREADY CLOSED by W4).** `a-styling-idioms.md:43-87` found `icon-sm`/`icon-md`/`icon-lg`/`icon-xs` were 61 SILENT NO-OP classes (defined NOWHERE — every icon paints Lucide's 24px default). **THAT FINDING IS NOW STALE: W4 (`084feb9`) ALREADY DEFINED all four `@utility icon-(xs|sm|md|lg)` in `design-idioms.css:209-232` AND authored the live gate `scripts/proof-icon-idiom.mjs` (`proof:icon-idiom`, already GREEN — resolve-or-red + strict-monotonicity + SVG-cascade). Verified live: the four utilities resolve; the icon-no-op is CLOSED.** The genuinely-open I12 remainder is (a) the OWNED-IDIOMS contract MEMBERSHIP — `proof:icon-idiom` only polices the four `icon-*`; the demo references OTHER idiom-shaped classes (`depth-text`/`text-mono-caption` are glass-ui-grace rents that resolve transitively, NOT through the kf-owned contract) — so the contract membership should EXTEND to the full referenced-idiom set (a referenced-but-undefined idiom-shaped class reds); (b) the magic-number/brittle-calc cleanup in the scene styles. The localized idiom area is `design-idioms.css` (the OWNED-IDIOMS contract). **The impl lane MUST MEASURE-FIRST whether any referenced idiom-shaped class beyond the resolved `icon-*` is actually undefined — if NONE, clause (a) reduces to a regression guard, recorded honestly (see `i-_HARDEN.md` FORK-I12).** (`i-_PLAN.md §3 I12`; `a-styling-idioms.md:43-87` — pre-W4 finding, W4-closed)
- **I3 — sequence rows are READ-ONLY; the motion-path AUTHOR PATH is FIXED; no copy artifact.** Sequence: the `stagger`/`at:` distribution is invisible-as-tunable — the storyboard rows (`SequenceTarget.vue:19-38`) are read-only (i-r5 §W-SEQ-4); the `at:` re-sort home the row-drag re-emits into is `useSequenceDemo.ts:117-120`. Motion-path: the AUTHOR PATH is FIXED (control points not editable — i-r5 §W-MP-2, the BOOKed F4 elevation); the SVG guide host `MotionPathTarget.vue:18-24`, the traveller `:42`; the single-source `PATH_D` the editable control-points re-emit is `motionPathGeometry.ts:1-11`; no copy-the-`offset-path` artifact (i-r5 §W-MP-3). i-r5 is the binding research note for this item. (`i-_PLAN.md §3 I3`; i-r5 §1.3/§2.3/§4)
- **The engine is FENCED.** The only >500-line files are `src/animation/` — ALREADY-SOTA, NOT touched (`H.md §Mandate inv ζ`). The I3 affordances DOGFOOD public `Sequence.add`/`ManualTimeline`/`setChildTime().render()` — NO hand-rolled rAF (inv ζ). (`i-_PLAN.md §0.5`)

## §Goal

Land the DRY/encapsulation/de-brittle sweep + the affordance enrichment that dogfoods it — the round-3 D/E-scale refinement layer, inside the I5 glass cards W11 lands. **The spine is the share-seam:** extract `useDragScrub({ el, onScrub, project })` (I8) FIRST so I3's new affordances are BORN on it (no churn-then-delete); each scene supplies only its `project` (rect-ratio for rails; nearest-point-on-path for motion-path). Around that spine: lift the motion-path projection + scrub-seam + `ManualTimeline` gesture engine from the Target into `useMotionPathDemo.ts` so the composable owns the engine (matching sequence/spring) and audit the W1 stores for single-writer / pure-getter consistency (I9); VERIFY the D-tranche decomposition holds + colocate the I8/I9 extractions (I10 — no manufactured splits; the engine is FENCED); de-brittle the motion-path projection (name `SAMPLE_STEP`, document the square-viewBox invariant) + replace any class-string DOM walk with owned refs (I11); localize the styling idioms — define ONE `icon-(xs|sm|md|lg)` idiom area in `design-idioms.css` so the 61 no-op callsites resolve to real differentiated sizes (a NAMED visual delta — the uniform 24px is the accident), extend the OWNED-IDIOMS contract to the full referenced set, kill magic numbers / brittle calc chains in the scene styles (I12, ISOMORPHIC unless a HIGHLY befitting NAMED delta); and GREATLY REFINE sequence + path on the shared seam (I3) — draggable storyboard rows that re-author each child's `at:` live (dogfooding `useDragScrub` + `Sequence.add`/re-sort, inv ζ), an editable motion-path via draggable SVG control points re-emitting `PATH_D` (the F4 elevation, the single-source invariant guaranteeing no drift), a copy-`offset-path` artifact, a tangent-angle readout, a swept master-playhead line, and ONE easter egg per scene (EE-SEQ-1 "the reel"; EE-MP-2 "the emoji winks"; one per other scene from the frontend-design pass). Net-deletion-leaning on the share/de-brittle side, additive on the enrichment side, every fix idiomatic and gestalt — NO workaround, NO legacy beside its replacement (the 3–4 drag copies COLLAPSE to one, the gesture engine LIFTS out of the Target in one motion, the icon idiom RESOLVES not suppressed), KISS·DRY (share the idioms, keep the scene-specific structure that earns its difference — do NOT force sequence onto `PlaybackRibbon` which lacks timeScale), styling ISOMORPHIC unless a HIGHLY befitting NAMED delta (the icon-size differentiation IS such a delta), MEASURE-FIRST (the I3-rung FLOOR is the scope-guard), the engine FENCED + DOGFOODED (inv ζ), the demo verified-decomposed not over-split (I10). This is the chronic-closure discipline in ACTION: the affordances are born on the shared seam, the idioms resolve in one localized area, the enrichments dogfood the engine.

## §Scope (each S = WHAT + WHY)

- **S1 — I8: STANDARDIZE components — extract `useDragScrub` + share the `.btn-playback` skin (extends W10 G3/G6).** WHAT: extract `useDragScrub({ el, onScrub, project })` into `demo/@/composables/` (the I9 encapsulation seam) — it owns pointer-capture + window `pointermove`/`pointerup` + clamp; each scene supplies ONLY its `project` (rect-ratio for rails; nearest-point-on-path for motion-path). Spring (`SpringTarget.vue:88-93`), sequence (the master-scrub `progressFromEvent`), and path (`MotionPathTarget.vue:124-147`) all consume it; I3's new affordances (sequence row-drag, path control-points) are BORN on it. Share the `.btn-playback` SKIN for sequence Play/Reverse (keep its domain-specific timeScale/Reset as `.btn-interactive` extras — the cube model: domain verbs beside the standard transport; do NOT force sequence onto `PlaybackRibbon` which lacks timeScale). WHY: sequence/path/spring/easing each hand-roll the SAME pointer-drag scrub dance; the transport buttons diverge. Share the idioms, keep the scene-specific structure that earns its difference. (`i-_PLAN.md §3 I8`; i-r5 §3.2/R-SEQ-F)

- **S2 — I9: ENCAPSULATION audit (composables, useX's, state + store management).** WHAT: move the motion-path projection + scrub-seam + `ManualTimeline` logic from `MotionPathTarget.vue:79-220` into `useMotionPathDemo.ts` (or into the shared `useDragScrub` of S1 with a path `project`) — the Target holds refs + markup, the composable holds the gesture engine, matching sequence/spring's shape. Audit the W1 stores (`scenePlayback.ts`, `controlOptionsStore.ts`, `animationOptionsStore.ts`) for the single-writer / pure-getter discipline (the exported surface is `dispatch()` + readonly refs; no read-with-write-side-effect getters). KISS — consistency, not a rewrite (W1 already landed the FSM; this is the consistency tail). WHY: `useMotionPathDemo.ts` (49L) is anomalously thin — the gesture engine lives in the Target, unlike sequence/spring whose composables own it. Composes with S1 (the `useDragScrub` home is the I9 seam) and S6 (the gesture engine moves to the composable BEFORE I3 enriches it). (`i-_PLAN.md §3 I9`; i-r5 §W-MP-5)

- **S3 — I10: DECOMPOSITION (verify the decomposition holds; colocate; modern Vue patterns).** WHAT: VERIFY (do NOT manufacture splits — the demo is already D-tranche-decomposed; no demo file >500L; the engine is FENCED). Where BEFITTING: colocate a scene's component + composable + constants into its dir (sequence/path already have `useXDemo.ts` + `*Keys.ts` + geometry beside the Target — confirm coherent); a complex component → a sub-component dir (components/composables/constants) ONLY if a genuine cohesion ceiling is crossed. Colocate the S1/S2 extractions (`useDragScrub` beside its consumers' shared home). KISS — no contrivance, no over-engineering. This is largely a CONFIRM-GREEN lane with targeted colocation, not a split campaign. WHY: the I10 NOTE — the only >500-line files are `src/animation/` ENGINE files (FENCED, ALREADY-SOTA); this targets DEMO components, which D-tranche largely decomposed. (`i-_PLAN.md §3 I10`)

- **S4 — I11: BRITTLENESS audit (deeply-nested / brittle selectors in CSS OR reactivity).** WHAT: name `SAMPLE_STEP` (`MotionPathTarget.vue:118-147`) as a documented coarse-search resolution OR replace the linear walk with coarse-then-refine; assert the square-viewBox invariant explicitly (`.mp-stage` is `aspect-ratio: 1` — document the coupling at `:131-134` so a future non-square stage cannot silently mis-project). Replace any class-string DOM walk (`.closest(".class")`/`querySelector(".class")`) with owned `useTemplateRef`s (the established W3/W10 pattern). De-brittle, don't rewrite. WHY: the `SAMPLE_STEP=5` magic + the implicit-square-viewBox scale coupling is brittle; `:deep()` chains and `.closest()` DOM walks are selector-brittle. Composes with S1's `useDragScrub` which absorbs the projection math. (`i-_PLAN.md §3 I11`)

- **S5 — I12: STYLING audit (isomorphic unless HIGHLY befitting; localize idioms; idiomatic Tailwind).** WHAT: **NB — the icon-sizing idiom is ALREADY OWNED (W4 `084feb9`: `@utility icon-(xs|sm|md|lg)` in `design-idioms.css:209-232`, gated by `proof:icon-idiom`; the four sizes resolve to differentiated `size-3.5/4/5/6` — the "61 no-op" was the PRE-W4 state, now CLOSED).** So S5 does NOT re-author the icon idiom. The open work: EXTEND the OWNED-IDIOMS contract MEMBERSHIP from the four `icon-*` (already owned) to the FULL referenced-idiom set (a referenced-but-undefined idiom-shaped class reds — MEASURE-FIRST whether any such class exists beyond the resolved `icon-*`). Audit scene styles: colocate component-scoped idioms, hoist truly-shared ones to the localized `design-idioms.css` area, kill magic numbers / brittle calc chains / viewport-unit traps. Idiomatic Tailwind `@apply` for the shared style/anim/color idioms. ISOMORPHIC unless a HIGHLY befitting NAMED delta. WHY: `a-styling-idioms` found the OWNED-IDIOMS contract under-enumerated (the `icon-*` instance W4 already closed); the contract should police EVERY referenced idiom-shaped class, not just the hand-enumerated set. Co-authored with the design-cohesion of I5's new card register (W11). (`i-_PLAN.md §3 I12`; `a-styling-idioms.md:43-87` — pre-W4 finding, W4-closed; see `i-_HARDEN.md` FORK-I12)

- **S6 — I3: SEQUENCE + PATH greatly refined + frontend-design usability pass + EASTER EGGS (folds i-r5).** WHAT — the headline refinements, from i-r5 §1.3 / §2.3, BORN on the shared seam (S1) inside the I5 cards (W11):
  - **Sequence:** draggable storyboard rows that re-author each child's `at:` offset live (the GSAP-timeline gesture, H-MI-4 re-opened — `SequenceTarget.vue:19-38` storyboard rows + `useSequenceDemo.ts:117-120` the `at:` re-sort the row-drag re-emits into) — dogfooding `useDragScrub` (S1) + the engine `Sequence.add`/re-sort (inv ζ). FLOOR (MEASURE-FIRST): a single stagger-amount slider re-deriving all five `at:` from one `each`. PLUS a swept master-playhead line across the rows (the stagger SEEN as a swept playhead, pure CSS). PLUS share the `.btn-playback` skin (S1).
  - **Motion-path:** editable path via draggable SVG control points (the F4 elevation, landed at last — `MotionPathTarget.vue:18-24,42` the SVG guide host + traveller; `motionPathGeometry.ts:1-11` the single-source `PATH_D`) — a handle drag re-emits `PATH_D` and BOTH the guide `<path>` `d` AND the traveller's `offset-path` re-read it in lockstep (the single-source invariant guarantees no drift). PLUS a "copy `offset-path`" artifact (the second copy-paste output beside Discrete's `linear()`). PLUS a tangent-angle readout. FLOOR (MEASURE-FIRST): 2–3 named preset paths. Dogfood `useDragScrub` (S1) with a path `project`.
  - **Frontend-design pass:** the `frontend-design` skill audits EVERY scene (not just sequence/path) for usability/affordance/interactability — recorded as a per-scene usability checklist within the wave, the refinements folded where befitting.
  - **EASTER EGGS (one per scene; i-r5 recommends the primary; lead selects):** Sequence → **EE-SEQ-1 "the reel"** (a hidden trigger replays the five balls as a cascading Mexican-wave overshoot, reusing the engine children); Motion-path → **EE-MP-2 "the emoji winks"** (a full-lap drag — 0%→100% on the closed loop — swaps the 🙂‍↔️ glyph). One egg per other scene divined in the frontend-design pass.
  WHY: sequence rows are READ-ONLY (the `stagger`/`at:` distribution is invisible-as-tunable); the motion-path author path is FIXED; no copy artifact. The user wants sequence + path "greatly refined", a frontend-design pass auditing every scene, and a few easter eggs per scene. The affordances are born AFTER S1's extraction so they live on the shared seam — no churn-then-delete; they DOGFOOD the engine (inv ζ). (`i-_PLAN.md §3 I3`; i-r5 §1.3/§2.3/§4)

- **S7 — J: the easing sidebar → MINIMAL, controls-like (round-4 feedback, `j-easing-minimalism.md`).** WHAT — strip the easing sidebar (`demo/easing/EasingSidebar.vue`, post-W11) to its essence so the bezier visualizer dominates: **(J1+J2)** DELETE the `<LabeledInput label="value">` CSS-value text input + its "value" label (`EasingSidebar.vue:41-48`) — the `<EasingSelect>` dropdown (`:53-57`) is the SOLE easing selector; relocate or drop the trailing `<CopyButton>` (`:50`) (keep a copy affordance only if it earns its place beside the dropdown, else drop — the input it served is gone). **(J5)** DELETE the `<h2 class="text-title">{{ currentEasingName }}</h2>` scene title (`:24`) — the easing sidebar reads like the standard controls (no big per-scene title; the dropdown already names the curve). **(J3)** the `duration` slider spans the FULL WIDTH of the card (the LabeledRange/slider row goes full-bleed within the `CardContent`, not the short track). **(J4)** collapse any remaining DOUBLE CONTAINER (one flat `CardContent` — verify no nested wrapper survives the W10/W11 normalization). **(J6)** grow the `<EasingCurveCanvas>` (`:26-33`) BIGGER still, reclaiming the vertical space J1/J5 freed — extend H.W11's `proof:bezier-grown`/the `clamp(...cqi...)` ceiling for the IN-SIDEBAR canvas (MEASURE-FIRST the max that fits without scroll now that the input + title are gone). WHY: the user wants the easing controls minimal and controls-like — the dropdown IS the selector, the curve IS the subject; the text input + "value" label + scene title are redundant chrome stealing the bezier's space. ISOMORPHIC deletions + a full-width slider + a bigger canvas; composes with W11's `proof:easing-sidebar-normalized` (J removes a row the subgrid no longer needs) + `proof:label-subgrid` (one fewer labeled row) + `proof:bezier-grown` (pushed further). (`j-easing-minimalism.md`; round-4 user feedback 2026-06-08)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

The 9 H.W12 gates, each BITES + cites a `file:line`/live anchor, each born-RED on `tranche-h-impl` today (except `proof:demo-no-oversize`'s 500L clause, born-GREEN as a REGRESSION guard — its bite is a future over-split). Gate-authoring home: per `H.W8.md §Scope` the gate-regime wave OWNS gate authoring; H.W12's NEW born-RED clauses are wired into the `proof:*` set + `proof:all`, and the H.W8 `proof:visual-lock` named-region matrix captures the enriched sequence/path + localized-idiom GOLDEN state AFTER H.W12 lands. Every browser gate settle-gates on the H.W1 FSM resting.

| Gate | Item(s) | Disposition | BITE (born-RED today → green on fix) |
|------|---------|-------------|--------------------------------------|
| `proof:dragscrub-single` | I8 | **NEW** | ≤1 hand-rolled `getBoundingClientRect()`-ratio + `setPointerCapture` + `window.addEventListener('pointermove')` drag block across spring/sequence/motion-path (the shared `useDragScrub` is the single home). Reds TODAY (3–4 hand-rolled copies — `SpringTarget.vue:88-93`, `SequenceTarget.vue` master-scrub, `MotionPathTarget.vue:124-147`); greens on the single extraction. Static grep across the scene targets. |
| `proof:composable-encapsulation` | I9 | **NEW** | `useMotionPathDemo` owns the project/scrub/`ManualTimeline` logic (the Target's `<script>` holds NO `getBoundingClientRect`/`getTotalLength` projection math); AND no store getter outside `useSceneMachine`/the store modules has a write side-effect (pure reads). Structural + grep. Reds TODAY (the gesture engine in `MotionPathTarget.vue:79-220`); greens on the lift. |
| `proof:demo-no-oversize` | I10 | **NEW** (regression guard; 500L clause born-GREEN) | every DEMO `.vue`/`.ts` (excluding `src/`, `node_modules/`, `dist/`) is ≤500L; AND each scene dir colocates its Target + composable + keys coherently (no orphan composable in a wrong dir). Reds if any demo file crosses 500L OR a manufactured split leaves an orphan; greens on the verified-decomposed + colocated state. Born-GREEN on the 500L clause TODAY — the bite is the REGRESSION guard: a future I3 enrichment must not push a Target over 500L without a colocated split. |
| `proof:no-brittle-selector` | I11 | **NEW** | ZERO `.closest("…class…")` / `querySelector(".…")` class-string DOM walks in the scene targets (owned refs instead); AND the motion-path projection has a NAMED constant + a documented viewBox invariant (no bare magic `5` step + implicit square coupling). Reds TODAY (the `SAMPLE_STEP=5` magic + scale coupling — `MotionPathTarget.vue:118-147`); greens on the named constant + documented invariant. |
| `proof:styling-idioms` | I12 | **NEW** | **NB — the 4 `icon-(xs\|sm\|md\|lg)` utilities ALREADY resolve via `design-idioms.css:209-232` (W4 `084feb9`; the live `proof:icon-idiom` gate `scripts/proof-icon-idiom.mjs` already GREEN). The `a-styling-idioms §1` "61 silent no-op" finding was TRUE pre-W4 and is now CLOSED — this gate does NOT re-litigate it (see `i-_HARDEN.md` FORK-I12).** The W12 bite is the genuinely-open remainder: (a) the OWNED-IDIOMS contract membership covers the FULL referenced-idiom set — every idiom-shaped class the demo references beyond the W4-owned `icon-*` four resolves to a definition (a referenced-but-undefined idiom-shaped class reds); (b) no NEW magic-number/brittle-calc regressions in the scene styles. Born-RED on (a) requires the impl lane to MEASURE-FIRST the referenced-idiom set against the live `design-idioms.css` definitions and identify ≥1 referenced-but-undefined idiom-shaped class — if NONE exists beyond the resolved `icon-*`, this gate REDUCES to the regression-guard clause (b) (born-GREEN, bites a future un-owned idiom), and that reduction is RECORDED honestly, NOT papered as a born-RED that does not bite. EXTENDS the W4 `proof:icon-idiom` resolve-or-red plumbing from the 4 `icon-*` (already green) to the full referenced-idiom set. |
| `proof:sequence-rows-draggable` | I3 | **NEW** | drag a sequence row handle → its `at:`/`delays[i]` changes AND the `Sequence` re-sorts (`useSequenceDemo.ts:117-120`). Reds TODAY (rows read-only — `SequenceTarget.vue:19-38`); greens on the draggable rows (FLOOR: the stagger-amount slider re-derives all five `at:`). Settle-gated on H.W1; the drag dogfoods `useDragScrub` (S1) + `Sequence.add` (inv ζ). |
| `proof:motion-path-editable` | I3 | **NEW** | drag a control handle → the guide `<path>` `d` AND the traveller's `offset-path` BOTH change to the SAME `d` (the single-source `PATH_D` invariant — `motionPathGeometry.ts:1-11`). Reds TODAY (fixed `PATH_D` — control points not editable); greens on the editable path (FLOOR: 2–3 named preset paths). Settle-gated on H.W1; the drag dogfoods `useDragScrub` (S1) with a path `project`. |
| `proof:motion-path-copy` | I3 | **NEW** | a copy affordance emits `offset-path: path(…)` (the second copy-paste output beside Discrete's `linear()`). Reds TODAY (no copy affordance); greens on the artifact. Mirrors the existing Discrete `linear()`-copy plumbing. |
| `proof:easter-egg` | I3 | **NEW** | EACH scene has its egg: a hidden trigger produces an observable, off-the-normal-path effect (sequence reel EE-SEQ-1, path emoji-wink EE-MP-2, + one per other scene). Reds TODAY (no eggs); greens when each scene's trigger fires its effect. Settle-gated on H.W1; the eggs reuse the engine children (inv ζ). |
| `proof:easing-sidebar-minimal` | J | **NEW** | the easing sidebar carries ZERO CSS-value text `<input>` + ZERO "value" label + ZERO `<h2>` scene title (`EasingSidebar.vue`); the `<EasingSelect>` dropdown is the sole easing selector; the `duration` control resolves a FULL-WIDTH track (its rendered width ≈ the `CardContent` inner width, ±8px); a SINGLE container (no nested wrapper); AND the `<EasingCurveCanvas>` grew taller than the H.W11 in-sidebar size (the reclaimed J1/J5 space) while still fitting without scroll. Reds TODAY (the `<LabeledInput label="value">` + the `<h2>` title + the short duration track are live — `EasingSidebar.vue:24,41-48`); greens on the minimal strip. Settle-gated on H.W1; composes with `proof:easing-sidebar-normalized` + `proof:label-subgrid` + `proof:bezier-grown`. |

**KEPT UNCHANGED:** `proof:scene-parity` (the survivor-set + per-mode interactivity — the I3 affordances ADD interactivity, they do not remove any), `proof:icon-idiom` (W4 — its 4-`icon-*` resolve-or-red plumbing is the substrate `proof:styling-idioms` EXTENDS to the full referenced set; the W4 gate stays GREEN), `proof:scene-uses-standard-ribbon` (W10 — I8 EXTENDS G3/G6 to sequence/path WITHOUT forcing them onto `PlaybackRibbon`, so the W10 gate's cube/easing/spring subjects are unchanged), the W1 `proof:scene-machine-irrefragable`/`proof:scene-isolation` plumbing (REUSED by the I3 settle-gates).

**The §Mandate bar — MUST bite, no vacuity.** Each H.W12 gate asserts an EXACT live measurement or static fact the lanes captured: the 3–4 hand-rolled drag copies, the gesture engine in the motion-path Target, the `SAMPLE_STEP=5` magic + square-viewBox coupling, the read-only sequence rows, the fixed `PATH_D`, the absent copy artifact, the no-eggs state. **`proof:styling-idioms` is the ONE gate whose original born-RED premise was STALE (the `icon-*` no-op was W4-closed) — corrected at HARDEN: its bite is the contract-MEMBERSHIP extension (born-RED only if a referenced idiom-shaped class beyond the resolved `icon-*` is undefined, MEASURE-FIRST) + the magic-number/brittle-calc regression guard; if the membership probe finds none, the gate honestly REDUCES to a born-GREEN regression guard, NOT a papered born-RED.** The rest pass non-vacuously — `proof:demo-no-oversize` is born-GREEN on the 500L clause but BITES a future over-split (the regression guard); the I3 gates each assert an observable interaction effect (the `at:` changes + re-sorts; the `d` changes in lockstep; the copy emits `path(…)`; the egg fires). No gate is satisfied by a `display:none`/`!important` suppression — the drag copies COLLAPSE (one extraction), the gesture engine LIFTS (subtree moved, not hidden), the rows DRAG (real affordance, not a stub).

## §Folds

- `audit/feedback/i-_PLAN.md` (the BINDING synthesis — §1 the W12 gestalt; §3 the per-item W12 clauses I8/I9/I10/I11/I12/I3; §4 the gate list; §5 the supersede-map with the RE-OPENed W5 BOOKs; §6 the W5/W10 reconciliation; §7 the I3-rung fork; §8 the convergence) — every clause re-verified against live source (the 3–4 drag copies, the thin `useMotionPathDemo`, the no-demo-file-over-500L decomposition, the `SAMPLE_STEP` magic, the 61 no-op `icon-*`, the read-only rows + fixed path — all CONFIRMED).
- `audit/feedback/i-r5-sequence-path.md` (the BINDING I3 research note — §1.3/§2.3 the headline refinements; §3.2/R-SEQ-F the `useDragScrub` extraction at the 3–5-consumer threshold; §W-MP-5 the thin-composable I9 lift; §4 the gate hooks `proof:sequence-rows-draggable`/`proof:motion-path-editable`/`proof:motion-path-copy`/`proof:dragscrub-single`; the easter-egg recommendations EE-SEQ-1/EE-MP-2) — folded in full for I3 + I8.
- `a-styling-idioms.md:43-87` (the 61-site `icon-*` no-op idiom — the I12 localized idiom area + the OWNED-IDIOMS contract extension).
- `a-scene-spring-sequence.md:273-277` (the W5-BOOKed H-MI-4 sequence row-drag) + `a-scene-path-discrete.md:135-137` (the F4 editable-path elevation) — the W5 BOOKs I3 RE-OPENS now over threshold.
- `H.W5.md:66` (the W5-BOOKed `useDragScrub` extraction at the 2-consumer / MEASURE-FIRST threshold) — RE-OPENED by I8 now at 3–5 consumers, over threshold.

## §supersede-map (which W5 BOOK / landed decision each item re-opens or extends + why precept-consistent)

H.W12 is a NEW corrective wave that RE-OPENS specific W5 BOOKs (now over their MEASURE-FIRST thresholds) and EXTENDS W10's normalization. Most items are consistency tails on W1/W5/W10 with no landed decision to supersede. (`i-_PLAN.md §5`)

| Item | W5 BOOK / landed decision RE-OPENED or EXTENDED | The revision | Why consistent with the precepts |
|------|-------------------------------------------------|--------------|----------------------------------|
| **I8** | **W10 G3/G6** (EXTENDS) + the **W5-BOOKed `useDragScrub`** (`H.W5.md:66`, BOOKed at 2 consumers / MEASURE-FIRST threshold) | extract `useDragScrub` (now at 3–5 consumers, over threshold); share the `.btn-playback` skin to sequence | RE-OPENS the BOOK now over its 3-consumer threshold (MEASURE-FIRST satisfied — i-r5 §3.2); share idioms, keep earned differences (do NOT force sequence onto `PlaybackRibbon`) |
| **I9/I10/I11/I12** | (no landed decision) | verify/tidy/de-brittle/localize | consistency tails on W1/W5/W10; ISOMORPHIC unless a NAMED delta (the icon-size differentiation is such a delta) |
| **I3** | **W5-BOOKed** H-MI-4 (sequence row-drag — `a-scene-spring-sequence.md:273-277`) + the **F4 editable-path elevation** (`a-scene-path-discrete.md:135-137`) | land both (the headline refinements) + the copy artifact + the tangent readout + easter eggs | RE-OPENS the deferred enrichments under the user's "greatly refined" mandate; dogfood the engine (inv ζ) + the shared seam (I8) — no churn-then-delete (the affordances are born on `useDragScrub` AFTER its extraction) |

## §USER-DECISION fork — I3-rung (the ONE genuine fork; default the FULL target)

**FORK I3-rung (the enrichment floor vs full).** Sequence: full per-row drag (I3-faithful) vs the stagger-amount-slider FLOOR. Motion-path: full editable cubic control-points (I3-faithful) vs the 2–3 preset-paths FLOOR. RECOMMENDED: the FULL target for both (the user said "greatly refined"); the FLOOR is the MEASURE-FIRST scope-guard if the wave slips. Lead picks the rung; documented as the explicit guard.

**No other W12 item needs a user decision** — I8/I9/I10/I11/I12 are decisive (the source-grounded idiomatic fix is unambiguous). The easter-egg selection (i-r5 recommends EE-SEQ-1 / EE-MP-2 as the primaries; one per other scene from the frontend-design pass) is a lead-tuning call set during impl, not a user fork. (`i-_PLAN.md §7`)

## §sequencing (the convergent landing within H.W12)

1. **I8 (the share seam) — S1.** Extract `useDragScrub` FIRST so I3's new affordances are BORN on it (no churn-then-delete); share the `.btn-playback` skin to sequence.
2. **I9 (encapsulation) — S2.** Lift the motion-path gesture engine into `useMotionPathDemo` (BEFORE I3 enriches it); audit the W1 stores for pure getters.
3. **I10 (colocate-verify) — S3.** VERIFY the D-tranche decomposition holds; colocate the S1/S2 extractions; NO manufactured splits.
4. **I11 (de-brittle) — S4.** Name `SAMPLE_STEP` + document the square-viewBox invariant; replace class-string DOM walks with owned refs.
5. **I12 (style-idioms) — S5.** Localize the `icon-*` idiom area in `design-idioms.css`; extend the OWNED-IDIOMS contract; kill magic numbers / brittle calc chains.
6. **I3 (sequence/path enrichment + easter eggs) — S6.** GREATLY REFINE sequence/path on the shared seam (S1) inside the I5 cards (W11) — draggable rows + editable path + copy artifact + tangent readout + the frontend-design pass + one egg per scene.

**All AFTER H.W11, BEFORE H.W8's GOLDEN `proof:visual-lock` baseline capture** (so the enriched sequence/path + the localized idioms are the FINAL state the named-region matrix locks). The born-RED clauses (the 9 NEW gates, except `proof:demo-no-oversize`'s born-GREEN 500L regression-guard clause) red TODAY on `tranche-h-impl`, green on the H.W12 fixes — every gate BITES and cites a `file:line`/live anchor.

## §Design decisions (trade-offs RESOLVED)

- **I8's `useDragScrub` is the spine; I3's affordances are born on it — RESOLVED.** Extract `useDragScrub` FIRST (now at 3–5 consumers, over the W5 BOOK's 2-consumer MEASURE-FIRST threshold) so I3's sequence row-drag + path control-points are BORN on the shared seam — no churn-then-delete. Share the idioms, keep the scene-specific structure that earns its difference (sequence keeps its timeScale/Reset domain verbs; do NOT force it onto `PlaybackRibbon`). (`i-_PLAN.md §3 I8`; i-r5 §3.2)
- **I9 lifts the gesture engine BEFORE I3 enriches it — RESOLVED.** The motion-path projection/scrub/`ManualTimeline` logic moves from the Target into `useMotionPathDemo` (matching sequence/spring) BEFORE I3 makes the path editable — the composable owns the engine, the Target holds refs + markup. KISS — consistency, not a rewrite. (`i-_PLAN.md §3 I9`; i-r5 §W-MP-5)
- **I10 is VERIFY, not a split campaign — RESOLVED.** No demo file exceeds 500L; the only >500L files are the FENCED engine (inv ζ). This is a CONFIRM-GREEN lane with targeted colocation (the S1/S2 extractions beside their consumers), NOT manufactured splits — KISS, no contrivance. The 500L gate clause is a REGRESSION guard (born-GREEN, bites a future over-split). (`i-_PLAN.md §3 I10`)
- **I12 is the OWNED-IDIOMS contract-MEMBERSHIP extension, NOT the (already-closed) icon-size differentiation — RESOLVED (corrected at HARDEN).** The icon-size differentiation already LANDED in W4 (`084feb9`: `@utility icon-(xs|sm|md|lg)` → `size-3.5/4/5/6`, gated by `proof:icon-idiom`) — the "61 no-op classes / uniform 24px accident" was the PRE-W4 state, now CLOSED. W12's I12 EXTENDS the OWNED-IDIOMS contract MEMBERSHIP from those four to the full referenced-idiom set (so a future un-owned idiom-shaped class reds) + kills magic numbers / brittle calc chains. Whether ≥1 referenced-but-undefined idiom-shaped class exists beyond `icon-*` is MEASURE-FIRST during impl; if none, `proof:styling-idioms` clause (a) reduces to a born-GREEN regression guard, recorded honestly. ISOMORPHIC unless a HIGHLY befitting NAMED delta. Co-authored with I5's new card register (W11). (`i-_PLAN.md §3 I12`; `a-styling-idioms.md:43-87` — W4-closed; `i-_HARDEN.md` FORK-I12)
- **I3 dogfoods the engine + the shared seam — RESOLVED.** The draggable rows re-author `at:` via `Sequence.add`/re-sort; the editable path re-emits `PATH_D` with the single-source invariant guaranteeing no drift; the eggs reuse the engine children — all dogfood public engine primitives (inv ζ, NO hand-rolled rAF) on the `useDragScrub` seam (I8). The FLOOR (stagger slider / preset paths) is the MEASURE-FIRST scope-guard (FORK I3-rung, default FULL). (`i-_PLAN.md §3 I3`; i-r5 §1.3/§2.3)
- **This wave RE-OPENS deferred BOOKs, it does not REWRITE — RESOLVED.** H.W12 is a NEW corrective wave AFTER H.W11, BEFORE H.W8's golden baseline; the §supersede-map records the W5 BOOKs (`useDragScrub`, H-MI-4, the F4 elevation) now over their MEASURE-FIRST thresholds + the W10 G3/G6 extension. The chronic-closure discipline is in ACTION — the BOOKs re-open under the user's "greatly refined" / "share more" / "better encapsulation" mandate, dogfooding the engine + the shared seam, no churn. No engine touched (inv ζ / FENCED); all DEMO-side (I10); the only sibling HANDOFF is W11's glass-ui LabeledField-subgrid (W12 emits none of its own).
