# H.W4 — the easing editor + hero φ-typography + the icon-sizing idiom (the visual-fidelity rungs) (Band 3 · SOURCE — appearance fidelity)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (three coupled visual-fidelity rungs — pure demo CSS/markup + one design-idioms `@utility`; no engine, no glass-ui patch; every rung needed already ships in `@mkbabb/glass-ui`, probed live) · **Scope:** `demo/@/components/custom/EasingCurveCanvas.vue:266-273` (the uncapped `aspect-ratio:1` square) + `demo/easing/EasingSidebar.vue:2` (the uncapped root, no container, no header) + `demo/@/components/custom/animation-controls/controls/TimingFunctionPanel.vue:17-19,37-45` (the double-chrome + `text-heading` header) + `demo/app/EditorStartScreen.vue:3,6,10,15-21` (the hero rung + the orphaned `...` + the `mt-28`/`h-0` overlay) + `demo/@/styles/design-idioms.css` (the NEW icon-sizing `@utility` family) + the φ-ladder leaf-tail (`AnimationMenuBar.vue:102`, `MotionPathTarget.vue:119`) + a new `proof:easing-canvas-bounded` / `proof:hero-rung` / `proof:icon-idiom` gate set · **DAG-deps:** **independent of H.W3/H.W5 surface** but BEST SEQUENCED AFTER H.W3 (the hero lives in the `[stage]` empty-state slot the H.W3 frame reserves — `a-demo-architecture` F7; the easing canvas sizes off the H.W3 rail-width container) and the orphaned-`...` fold COORDINATES with H.W6 (the typing-dots primitive — this wave owns the hero LAYOUT coupling, H.W6 owns the dot-fade MECHANISM); the H.W0 H-A2 engine guard (bare-text leaf → discrete-hold) is the belt-and-suspenders that keeps the hero ellipsis out of a lerp path regardless. MEASURE-FIRST gates the exact hero rung (`proof:hero-balance` + CLS) and the unmemoized `viewBox` recompute (RC-5, gated, not asserted).

## §Provenance (the folded lanes)

- `a-easing-editor` **D3/RC-1–RC-5** (the PRIMARY — the canvas has NO block ceiling: `aspect-ratio:1` off a `width:100%` SVG inside a `max-width:none` 724px card = a measured **680×680px** square eating 77% of an 883px panel, in NO container context; the gestalt fix = a `container-type:inline-size` container + `block-size: clamp(160px,38cqi,280px)`, plus the double-chrome collapse + the `text-heading`→`text-title` header bump + the EasingSidebar parity header — `a-easing-editor.md:36-72,75-170`).
- `a-hero-typography` **D7/F1/F3 + L1/L2** (the hero IS golden-rung'd but on the WRONG middle rung — `text-display-4` = 86.1px live, while glass-ui ships the audacious tier `text-display-mega` 177px built precisely for poster heroes; the φ-ladder mechanism + Capsize fallback are ALREADY-SOTA; the orphaned `...` fragments the optical block; the 2 residual raw rungs L1/L2 are the leaf-tail to sweep — `a-hero-typography.md:19-101,108-166`).
- `a-styling-idioms` **§1 (the NEW icon-sizing find) + §5/§6** (the SINGLE largest idiom-ownership gap: `icon-sm`×34/`icon-md`×13/`icon-lg`×11/`icon-xs`×3 = **61 callsites** defined NOWHERE — live `anyIconRuleInStylesheets:false`, all compute identical 24px (Lucide default); the fix = own ONE icon-sizing `@utility` family in `design-idioms.css`; D7 = a one-rung bump, D3 = layout deltas not idiom drift — `a-styling-idioms.md:43-87,240-293`).
- `a-design-language` §2/§3 (the audacious-tier rationale + the φ-ladder altitude), `a-scene-square-easing` §2 (the EasingCurveCanvas sizing leaf-depth — `EasingCurveCanvas.vue:269-273` `min-height:140px` no max; `:4` `p-2`; the bare-SVG-no-header artifact `:6-101,215`), `a-scene-path-discrete` §6 (the MotionPathTarget header rung leaf-tail).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §1.1` (easing-editor, hero/φ-typography, icon-sizing rows `:59,63,65`) + **§3 H.W4** (`:142-145`, the headline + the biting proof set) + §5 ALREADY-SOTA #2 (the φ-ladder mechanism).

## §The state, verified (file:line / live anchors from the lanes)

- **The easing canvas is a 680×680px square — 77% of the panel (live, Playwright, 1440×900, `/#/easing`).** `glass-card` (EasingSidebar root) = 724×883, `cardMaxWidth: none`, `cardMinWidth: 0px` (nothing caps it); `easing-curve-canvas-wrapper` = 698×698; `easing-curve-canvas` (the SVG) = **680×680**, `aspect-ratio: 1/1`, `min-height: 140px`; `container-type` on EVERY ancestor = `normal` (NO container context anywhere). `width:100%` + `aspect-ratio:1` + an uncapped card ⇒ width→square→viewport, no block ceiling. (`a-easing-editor.md:42-62,75-87`)
- **The "container-query-friendly" comment is aspirational, not implemented.** `grep -rn 'container-type|@container|cqi|cqw' demo/easing/ EasingCurveCanvas.vue TimingFunctionPanel.vue` → NONE, yet `EasingCurveCanvas.vue:266-268` claims it. No container, no container unit, no `@container` rule. (`a-easing-editor.md:55-62`)
- **The inner wash border touches the header (editor B, double chrome).** `TimingFunctionPanel.vue:17-45`: a `<Card plain>` whose `<CardHeader class="...pb-1">` (CardTitle `text-heading`, `:19`) is followed IMMEDIATELY by `<EasingCurveCanvas>`, itself a `GlassPanel variant="wash"` carrying `border: 1px solid var(--glass-border-wash)` (glass-ui `glass.css:48-52`) + `rounded-card` (`EasingCurveCanvas.vue:4`); with only `pb-1` header + `gap-2` content the wash panel's 1px top border sits hard against the header baseline — a bordered GlassPanel nested inside a bordered Card (double chrome) with near-zero separation. (`a-easing-editor.md:90-98`)
- **EasingSidebar (editor A) has NO header — the canvas just floats.** `EasingSidebar.vue:2` `<div class="glass-card p-3 grid gap-3">`, measured 724×883, `max-width: none`, no `container-type`, no title element ("ease" only appears in the value input). (`a-easing-editor.md:85-89,165-170`)
- **The hero is on the WRONG (middle) φ rung (live, 1440×900, `/`).** `<h1>` (`EditorStartScreen.vue:5-21`) classes `text-display-4 grid p-0 lg:flex`; computed `font-size` = **86.112px** (`--type-display-4` = φ^(7/2), `clamp(3.33rem, 2.5rem+4vw, 5.382rem)`); `line-height` 94.72px (1.1); `font-family` Instrument Serif (+ Capsize fallback); `text-wrap: balance`; host rect top-left `mt-28 h-0 w-screen`. ALL φ rungs resolve correctly and the audacious tier IS compiled: `--type-display-mega` = φ^(9/2) = **177.4px @1440** with `.text-display-mega` ✔ (glass-ui `typography.css:111-123,116-120,201-210`). The hero is the textbook poster-hero consumer and is NOT reaching the tier glass-ui built for it. (`a-hero-typography.md:19-90`)
- **The orphaned `...` fragments the hero block (live `hero-home-1440.png`).** `EditorStartScreen.vue:15-20` renders a SECOND `<AnimatedText :text="ellipsis='...'">` in its own `<div>`; with `lg:flex` on the `<h1>` it is meant to sit inline but live it WRAPS to a second line below the headline (the "..." floats on its own row above the subtitle), mid-fade (`opacity 0.68`). At the larger mega rung this worsens. (`a-hero-typography.md:108-124`)
- **`icon-sm/md/lg/xs` are 61 SILENT no-op classes (live, Playwright, `/#/easing`).** `anyIconRuleInStylesheets: false` — NO rule with a `.icon-(sm|md|lg|xs)` selector exists (not demo, not glass-ui, git-confirmed never authored); all three sizes compute IDENTICALLY `24px × 24px` (Lucide's default `<svg width="24" height="24">`). 61 callsites across 4 size names (`icon-sm`×34/`icon-md`×13/`icon-lg`×11/`icon-xs`×3); e.g. `TimingFunctionPanel.vue:10` `<ArrowLeft class="icon-sm" />`, `:28` `<CopyButton class="scale-on-hover icon-md" />`. `proof:idioms` clause-1 did not catch it because its membership is the hand-enumerated `--rainbow-*`/gold/`scale-on-hover` set, NOT "every idiom-shaped class the demo references." (`a-styling-idioms.md:43-87`)
- **The φ-ladder leaf-tail residual (the new sweep targets).** L1: `AnimationMenuBar.vue:102` `text-xl` (play/pause glyph) — a raw Tailwind rung the sweep forbids; L2: `MotionPathTarget.vue:119` `font-size: 1.25rem` — route to `--type-subheading` (1.272rem, the √φ rung it's eyeballing). (L3 `MenubarLabel.vue:10 text-sm` = RECORD/KILL-candidate vendored; L4 `EasingCurveCanvas.vue:343` `0.055px` = NOT-A-DEFECT SVG user-space unit; L5 `SquareScene.vue:54` = ALREADY-SOTA φ token.) (`a-hero-typography.md:147-166`)
- **ALREADY-SOTA, do NOT touch (inv ε):** the φ-ladder MECHANISM (genuine √φ-derived rungs) + the Instrument-Serif Capsize metric-matched fallback (`style.css:39-87`, 0 CLS, E.W11.S5 — bumping the rung does NOT disturb it, the fallback geometry scales); the `depth-text` cartoon-shadow text idiom the hero already dogfoods (`EditorStartScreen.vue:10,17` → glass-ui `utilities.css:231-246` — the D2/D14 restoration should MIRROR this, not invent a new one); the bezier-drag interaction (`EasingCurveCanvas.vue:167-262`, cited as the D11 bar — H.W5's concern, not touched here); the φ-ladder hygiene across surviving scenes (zero leaf-tail beyond L1/L2). (`a-hero-typography.md:126-145,170-178,210-211`, `a-styling-idioms.md:240-247,297-327`)

## §Goal

Land the three coupled visual-fidelity rungs that make the demo's appearance match its φ-ladder intent: (1) the easing canvas gets a CONTAINER CEILING — a `container-type:inline-size` context + `block-size: clamp(160px, 38cqi, 280px)` so the 680px square becomes a ≤280px square (panel height ~883→~470px) — the idiomatic Baseline-2023 modern-web transposition (size-aware-styling + fluid-scaling, BOTH Baseline Widely available since 2023-02-14, NO fallback owed); (2) the hero bumps ONE φ rung into the audacious tier (`text-display-4` → `text-display-mega`, 86→177px, keep `text-wrap: balance`) and folds the orphaned `...` into one balanced run; (3) the 61-site `icon-*` idiom becomes ONE real demo-owned `@utility` family in `design-idioms.css` (a NAMED visual delta — the icons finally differentiate `xs<sm<md<lg`, where the current uniform 24px is the accident). Plus the TimingFunctionPanel double-chrome collapse + header φ-rung bump + the EasingSidebar parity header, and the L1/L2 leaf-tail sweep. KISS · DRY · every size on the φ ladder or a named-`@utility` size — no px literals, no new abstraction, no legacy fallback owed.

## §Scope (each S = WHAT + WHY)

- **S1 — make the easing editor a container, cap the canvas (D3; `a-easing-editor` Fix-1, RC-1/RC-2).** WHAT: declare the editor root a container — `EasingSidebar.vue:2` (and the `TimingFunctionPanel` Card wrapper) `container-type: inline-size; container-name: easing-editor`; replace the open-ended square at `EasingCurveCanvas.vue:269-273` with `display:block; inline-size:100%; aspect-ratio:1; block-size: clamp(160px, 38cqi, 280px); max-block-size: 280px; margin-inline: auto`. WHY: `38cqi` ties the canvas to the CONTAINER'S inline size (not the viewport), `clamp` holds it in `[160px,280px]` — a deliberate bounded canvas regardless of sidebar width; `aspect-ratio` keeps the square LAW, `margin-inline:auto` centers it. ~4 lines, deletes the unbounded behavior in one stroke; `38cqi` is the ONE magic number (a NAMED φ-derived delta landing the canvas at the cube/square scene-target proportion), everything else is a token. (`a-easing-editor.md:120-148`)
- **S2 — collapse the double chrome + bump the header rung + add the EasingSidebar parity header (D3; `a-easing-editor` Fix-2/Fix-3/Fix-4, RC-3/RC-4).** WHAT: in `TimingFunctionPanel.vue` drop the redundant `variant="wash"` border on the nested canvas when it is already inside a Card (one surface owns the border — DRY collapse the double chrome); bump `TimingFunctionPanel.vue:19` `class="text-heading"` → `class="text-title"` (25.9px → 32.9px, the next φ rung — a ladder step, no px literal); add a `text-title` header to `EasingSidebar.vue` (it already knows `demo.currentEasingName`) for gestalt parity with editor B. WHY: the canvas's wash IS the surface; the Card's job is layout, not a second frame; with the canvas now ≤280px the title legitimately leads the panel; editor A floating headerless is the parity gap. **`TimingFunctionPanel.vue:78` has its OWN `grid-cols-[auto_1fr]`** (the cubic-bézier detail panel) — folded into the H.W3 single-column treatment via the `proof:demo-shell-grid` grep clause; this wave only sizes/headers the panel. (`a-easing-editor.md:150-170`, `a-controls-sidebar.md:193-197`)
- **S3 — bump the hero one φ rung into the audacious tier + fold the orphaned `...` (D7; `a-hero-typography` D7-main/F1).** WHAT: `EditorStartScreen.vue:6` `text-display-4` → `text-display-mega` (86→177px @1440 / ~94px mobile, inherits `--font-display-weight` 400, tracking-tight, leading 1.1, `font-optical-sizing:auto` — ZERO new declarations, a one-class swap through the existing golden substrate); keep `text-wrap: balance` so "Select an / animation" balances to two lines; fold the second `<AnimatedText :text="..."` `<div>` (`:15-20`) into the SAME run as the title (append the ellipsis as a trailing inline `<span>`/word of the single AnimatedText run, drop the second `<div>`, collapse the `grid p-0 lg:flex` host to a plain block); optional scoped `line-height: 0.92` on the hero `<h1>` ONLY (a demo-local rule mirroring `.start-screen-prose` at `EditorStartScreen.vue:67`, NOT a glass-ui override of the shared `.text-*` family). WHY: the hero is the textbook poster-hero consumer reaching the tier glass-ui built for it; the orphaned `...` row fragments the poster (worse at mega) — folding it makes the hero ONE balanced display run; the scoped leading tightens the two-line block at poster scale. **Do NOT use `.text-hero`** (it is `white-space: nowrap` for single-token numeric heroes — a 3-word English hero overflows). (`a-hero-typography.md:69-101,108-124`)
- **S4 — own the icon-sizing idiom + sweep the L1/L2 leaf-tail (NEW + D7-leaf; `a-styling-idioms` §1, `a-hero-typography` L1/L2).** WHAT: define ONE icon-sizing idiom in `design-idioms.css` beside the other owned utilities — `@utility icon-xs { @apply size-3.5 } / icon-sm { @apply size-4 } / icon-md { @apply size-5 } / icon-lg { @apply size-6 }` (pick the sizes the design WANTS — the uniform 24px is the accident); re-rung `AnimationMenuBar.vue:102` `text-xl` to a φ class (or an `icon-*` size, it is a control glyph); route `MotionPathTarget.vue:119` `font-size: 1.25rem` → `--type-subheading` (gate behind the H.W5 motion-path survival verdict, but the scene KEEPS per `a-modes-pertinence`, so SHIP). WHY: a 61-site idiom resolving to LITERALLY NOTHING is a strictly-worse instance of the exact rent `design-idioms.css` exists to retire; owning it differentiates the icons (a NAMED befitting delta) and the L1/L2 sweep re-arms the C.W2 φ-sweep so the leaf-tail can't regrow — the sweep is the zero-raw-rungs half of `proof:phi-leaf-zero` (the φ-hero chronic SYSTEM-property gate), now a first-class named gate (not a buried `proof:icon-idiom` sub-clause) so the chronic-closure meta-gate can cite it by name. (`a-styling-idioms.md:70-87`, `a-hero-typography.md:156-157`)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

Four new gates (Playwright DOM/visual + grep), wired into the `proof:*` set:

- **`proof:easing-canvas-bounded` (the canvas ceiling lock).** At 1440×900 on `/#/easing`, assert `getComputedStyle('.easing-curve-canvas').blockSize <= 280` AND the SVG bounding box `width === height` (the square LAW held) AND `<= 280`; plus `getComputedStyle(editorRoot).containerType === 'inline-size'` (the container context exists) AND a panel-height clause (`.glass-card offsetHeight` such that the canvas is `<= 0.55` of panel height) AND a header-clearance clause (`TimingFunctionPanel` `CardTitle` bottom → `.easing-curve-canvas-wrapper` top gap `>= 8px`). **BITE:** reds TODAY — `blockSize` is 680, the canvas is 77% of the panel, the container type is `normal`, the header gap is near-0; greens only after the container + clamp + double-chrome collapse. (`a-easing-editor.md:176-188`)
- **`proof:hero-rung` (the LARGER + golden lock).** Assert the hero `<h1>` computed `font-size` ≥ `--type-display-mega` resolved at the test viewport (≥170px @1440) AND its class is a φ `text-display-*` rung (regex: no `text-\d?xl`, no arbitrary `text-[Npx]`); plus `proof:hero-balance` (the hero renders as a BALANCED ≤2-line run with the `...` on the SAME optical block — no orphaned ellipsis row; screenshot diff at 1440 + 390) and a `proof:hero-cls` companion (the `<h1>` LCP node CLS contribution ≈ 0 after the mega bump — the Capsize fallback still neutralizes the swap). **BITE:** reds TODAY — `font-size` is 86px on the middle `text-display-4` rung and the `...` floats on its own row; greens only on the mega bump + the folded run. (`a-hero-typography.md:183-193`)
- **`proof:phi-leaf-zero` (the φ-hero chronic SYSTEM-property gate — CH-2/M1, the citable closure gate).** The single named gate the chronic-closure meta-gate (H.W8 S3) cites for the φ-hero chronic. Asserts BOTH halves of the M1 system property in one gate (per `a-deferred-chronic.md:81-82,113` + `_SYNTHESIS-deferred-ledger.md:220`): (1) the leaf-tail SWEEP — zero raw rungs `grep -rno "text-(xs|sm|base|lg|xl|2xl|4xl|6xl|8xl)"` + zero bare `font-size:\s*\d` in `demo/{@,app,easing,spring,sequence,motion-path}/**/*.{vue,css}` (excl. `/dist/`, vendored `ui/`, `.svg` viewBox) = **0**; AND (2) the hero on the TOP φ rung — the hero `<h1>` carries a `text-display-*` rung at `≥ --type-display-mega` (the positive half it shares with `proof:hero-rung`). **BITE:** reds TODAY — L1 `text-xl` / L2 `font-size:1.25rem` are live (the sweep half) AND the hero is on the middle `text-display-4` rung (the rung half) — the 37 surviving raw rungs that "exited" the ledger at issue-close red it; greens ONLY once S3's mega bump AND S4's L1/L2 sweep BOTH land. Strip either half (re-introduce a raw rung, or revert the hero) → it reds (BOTH are load-bearing — this is precisely what stops the M1 re-paper the `proof:hero-rung`-alone close left open). (`a-deferred-chronic.md:113`, `_SYNTHESIS-deferred-ledger.md:220`, `a-hero-typography.md:147-166,186-188`, `a-styling-idioms.md:79-83`)
- **`proof:icon-idiom` (the 61-no-op lock — the §1 NEW find).** Grep every `\bicon-(xs|sm|md|lg)\b` reference in `demo/**` `.vue` and assert each resolves to a `design-idioms.css` definition (the same resolve-or-red shape `proof:idioms` clause-1 uses for `--rainbow-*`); plus a computed assert that the four sizes differentiate (`size(icon-xs) < size(icon-sm) < size(icon-md) < size(icon-lg)`). **BITE:** reds TODAY — 61 refs resolve to ZERO definitions and all compute identical 24px; greens only once the `@utility` family is owned. (The φ-leaf-tail sweep that used to ride this clause is now its own first-class `proof:phi-leaf-zero` gate above — it is the φ-hero chronic's SYSTEM-property gate and must be citable by name, not buried in the icon lock.) (`a-styling-idioms.md:79-83`)

**The §Mandate bar — MUST bite.** Every clause asserts an EXACT live measurement (680px canvas, `containerType:normal`, 86px hero, the orphaned `...` row, the 37 surviving raw rungs, `anyIconRuleInStylesheets:false` + three identical 24px) — none passes vacuously. `proof:phi-leaf-zero` is the φ-hero chronic's SYSTEM-property gate (CH-2/M1) — it asserts BOTH the zero-raw-rungs sweep AND the hero-on-top-rung, so the M1 escape (the issue closes while raw rungs linger) is structurally locked, not papered (the `proof:hero-rung`-alone close would have left it open). The MEASURE-FIRST items are GATED, not asserted: the exact hero rung lands behind `proof:hero-balance` + CLS (fall to `text-display-5` (110px) only if mega crowds the cube), and the RC-5 `viewBox` 17×-sample recompute lands behind a `proof:bezier-drag-frame-budget` baseline median (if baseline <0.5ms it is ALREADY-SOTA — leave it; no perf assertion ships without the number first).

## §Folds

- `a-easing-editor` **RC-1/RC-2** (the uncapped square + non-container root — S1, Fix-1) + **RC-3** (the double chrome — S2, Fix-2) + **RC-4** (the relative-too-small header — S2, Fix-3) + **A** (EasingSidebar parity header — S2, Fix-4) + **B** (the aspirational CQ comment — made TRUE by S1). **RC-5** (the unmemoized `viewBox` recompute) = MEASURE-FIRST (gated by `proof:bezier-drag-frame-budget`, NOT a clause). **C** (`demo/CLAUDE.md` lists the non-existent `CubicBezierControls.vue`) = RECORD (doc fix). **D** (`text-display` clobbered to 16px) = D7's lane, folded here as S3 (a demo-local override; the mega bump routes through the correct substrate). **E** (the `/#/easing`→`#/spring` redirect, transient 0×0) = cross-ref D12 (H.W1).
- `a-hero-typography` **D7-main** (the rung bump — S3, `proof:hero-rung`) + **F1** (the orphaned `...` — S3, COORDINATE with H.W6) + **L1/L2** (the leaf-tail sweep — S4, the zero-raw-rungs half of `proof:phi-leaf-zero`). **D7-not.hero** (do NOT use `.text-hero`) = RECORD-guard (encoded in `proof:hero-rung`). **The φ-hero chronic (CH-2/M1) SYSTEM-property gate is `proof:phi-leaf-zero`** (BOTH the rung bump AND the leaf-tail sweep, in one citable gate) — this wave AUTHORS it (the H.W8 S3 chronic-closure meta-gate cites it by name; `a-deferred-chronic.md:113`, `_SYNTHESIS-deferred-ledger.md:220,248`). **F2** (`depth-text` = the cartoon-shadow text idiom the hero already dogfoods) = RECORD / cross-ref D2/D14 (the H.W2 restoration mirrors it). **F3** (the `mt-28`/`h-0` overlay vs optical-balance tokens) = MEASURE-FIRST · BOOK→H.W7 (the hero re-anchors onto the `--work-area-top-offset` rhythm when the H.W3 `[stage]` slot + H.W7 mobile land — verify mega placement at 1440/390/768 first). The Capsize fallback substrate = ALREADY-SOTA (verify CLS still ≈0 at mega — `proof:hero-cls`).
- `a-styling-idioms` **§1** (the 61 no-op `icon-*` classes — S4, the headline NEW find) + **§5** (the hero rung — folds into S3) + **§6** (D3 easing sizing/header are layout deltas — S1/S2). **§2/§3/§4** (the specular seam, `depth-text`/`text-mono-caption` rents, the dot-fade primitive) are **H.W2/H.W6's** — not this wave; the `proof:idioms`-clause-1 membership EXTENSION (from a hand-list to "every referenced idiom resolves to a demo-local def OR a gated glass-ui carve") is the shared instrument those waves complete.
- **RECORD (no separate wave) — the un-touched ALREADY-SOTA:** the φ-ladder mechanism + Capsize fallback (`_SYNTHESIS-gap-scorecard §5 #2`); the bezier-drag interaction (the D11 bar — H.W5); the φ-ladder hygiene across surviving scenes (zero leaf-tail beyond L1/L2). H.W4 touches NONE of these beyond the named one-rung/one-utility deltas.

## §Design decisions (trade-offs RESOLVED)

- **Container + clamp, not a magic px height — RESOLVED.** The defect is the MISSING ceiling, not the technique (`aspect-ratio` + `min-height` was a reasonable first cut). The modern-web-guidance idiom (size-aware-styling + fluid-scaling, Baseline 2023 — NO fallback owed) is: make the panel a container, size the canvas off the container, clamp it. `38cqi` is the single NAMED magic number (φ-derived, lands at the scene-target proportion); everything else is a token. (`a-easing-editor.md:114-148,216-228`)
- **One φ-rung bump, not `.text-hero`, not an ad-hoc px — RESOLVED.** D7 is ALTITUDE, not adoption: the hero is already on the ladder (`text-display-4`, C.W2's close of the A-grand-audit finding) but on the middle rung; the audacious tier exists precisely for "when the consumer wants the headline to win." `text-display-mega` is the decisive +φ^½ jump that keeps `text-wrap: balance` (so it balances to two lines, not a 1392px run). `.text-hero` is `nowrap` (single-token numeric posters) — the load-bearing distinction. The exact rung is MEASURE-FIRST (mega vs the conservative `text-display-5`). (`a-hero-typography.md:52-90`)
- **The icon family is a NAMED delta, not isomorphic — RESOLVED.** The current uniform 24px is the ACCIDENT (61 no-op classes), not the intent; owning the `@utility` family makes the icons finally differentiate `xs<sm<md<lg` — a befitting named delta per the Mandate, not an isomorphic change. The instrument is the SAME `proof:idioms` resolve-or-red shape, membership completed — the D.W2 contract's enforcement extended to the idiom it never enumerated. (`a-styling-idioms.md:70-87`)
- **The double chrome collapses to one surface — RESOLVED.** The canvas's `GlassPanel variant="wash"` IS the surface; the surrounding `<Card plain>` is redundant double-framing. The gestalt is (a) drop the wash border when inside a Card (one border, not two) over (b) merely adding header padding — DRY: the canvas carries depth via its wash, the Card's job is layout. (`a-easing-editor.md:150-159`)
- **Independent of the engine bands; the H-A2 guard is belt-and-suspenders — RESOLVED.** The hero ellipsis fold (S3) and the H.W6 typing-dots primitive both keep `...` out of a `CSSKeyframesAnimation` value position; the H.W0 H-A2 engine guard (bare non-numeric/non-color text leaf → discrete hold-snap) is the orthogonal belt-and-suspenders so a stray ellipsis never aborts a frame. This wave owns the hero LAYOUT coupling; H.W6 owns the dot-fade mechanism — the shared seam is tagged. (`a-hero-typography.md:120-124`, `_SYNTHESIS-gap-scorecard §3 H.W0/H.W6`)
