# H.W0 — KILL THE LIVE CRASHES (Band 0 · prerequisite — the demo must not throw)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (HIGH; two LIVE, reproducible uncaught engine exception FAMILIES ship in 4.1.0 — they poison every other lane's "clean console" measurement) · **Scope:** the demo scenes that pass a closure/`CSSCubicBezier` timing function with no `Easing.css` twin — the easing `contractAnim` (`demo/easing/useEasingDemo.ts:268`, built from a bare `currentEasingFn` closure on a `cubic-bezier`/`steps` selection) + amiga (`demo/amiga/useAmigaAnimations.ts:31,74`, which pass `CSSCubicBezier(0.2,0.65,0.6,1)` directly as `timingFunction`) + the ~12 `src/animation/animations.ts` presets built with `CSSCubicBezier(...)` — give each a faithful `Easing.css` twin · `src/animation/engine.ts` (the `processFrame` interp dispatch — classify a bare non-numeric/non-color text leaf as a discrete hold-snap) + `demo/@/components/custom/animation-controls/keyframes/KeyframesStringControls.vue` (the unguarded readout consumer — the CO-EQUAL primary floor) + a new `test/discrete-leaf.test.ts` + a zero-console-error CLAUSE added to the existing `proof:demo-usability` Playwright gate; NO new machinery, NO CI surgery beyond registering the new clause · **DAG-deps:** **leads the tranche** — H.W0 and H.W1 are prerequisites (`_SYNTHESIS-gap-scorecard.md §3` "the live crashes poison every other measurement"); the H-A2 engine guard is the belt-and-suspenders the H.W6 typing-dots fix sits behind (the discrete-leaf row folds forward to H.W6); the H-A1 `.css`-twin landing flows through `adoptCompiled` (G.W19, already landed) intact.

## §BLK-1 RESOLVED — the crash SEAMS re-targeted (DEEP-harden adjudication, BINDING)

The first authoring MIS-ROOT-CAUSED both crash families at byte/live level (3 deep lanes + a feasibility lane). The corrections below are BINDING over the original lane prose:

1. **H-A1 throw is NOT in the Cube presets.** Cube `Rotations`/`Matrix` resolve `ease-in-out` (a string/registry easing) and serialize CLEAN — `CSSKeyframesToString(cubeRotations)` returns a string today (proven in unit + live). The REAL throw surfaces are (a) the easing-scene `contractAnim` (`useEasingDemo.ts:268`, built from a bare `currentEasingFn` closure when the selection is `cubic-bezier`/`steps`) and (b) the amiga scene (`useAmigaAnimations.ts:31,74` pass `CSSCubicBezier(0.2,0.65,0.6,1)` directly as `timingFunction`) + the ~12 `animations.ts` presets built with `CSSCubicBezier(...)`. So S1 targets those seams; the Cube-preset edit is DELETED (it was a no-op on a file that does not throw). Gate (d) re-points at a preset that actually throws.
2. **H-A2 `"......"` root cause is wrong** — `AnimatedText.vue` is pure CSS (0 engine symbols), so "the hero ellipsis reaches a `CSSKeyframesAnimation` lerp" is FALSE. The live `"......"` parse-error IS real but its TRIGGER is a narrower value.js path tied to route-storm-restored cross-scene state, NOT a bare `{label:a}→{label:b}` leaf (proven no-throw in unit). S3 ships the kf-side guard as the belt; the PRIMARY classification is re-classified toward a value.js-HANDOFF (the `parseCSSValueUnit('...')` throw lives in value.js), PAIRED with a MEASURE-FIRST isolate of the actual leaf.
3. **Path re-anchors (the published CLAUDE.md tree predates the single-`animation/` consolidation; `src/` = only `animation/` + `env.d.ts`):** `serializeEasing` is `src/animation/format.ts:30` (throw at `:36`), NOT `src/parsing/format.ts:24`. The interp dispatch is `engine.ts:769 processFrame` / `:779 lerpValue` (via `:657 interpFrames`), NOT `:516,576` (those are dev-bundle map lines). The readout call is `KeyframesStringControls.vue:~95` invoked from `onMounted :222-224`, NOT a watcher at `:140`.
4. **The "4× per Cube load" count is wrong** — a clean cold `#/cube` throws 0–3, state-dependent (the route storm may navigate away before the throw on an un-pinned load); the count target is "0 console errors," not a fixed 4.

## §Provenance (the folded lanes)

- `a-engine-regressions` **H-A1** (the SHIP — `serializeEasing` THROWS uncaught on a closure/`CSSCubicBezier` timing function with no `.css` twin: `src/animation/format.ts:36` ← `CSSKeyframesToString` ← the readout call `KeyframesStringControls.vue:~95` invoked from `onMounted :222-224` with **no `try/catch`**; the THROW SEAMS are the easing `contractAnim` (`useEasingDemo.ts:268`) + amiga (`useAmigaAnimations.ts:31,74`) + the `animations.ts` `CSSCubicBezier` presets — NOT the Cube presets [see §BLK-1]; G.W4's fail-explicit policy is correct *as engine policy* but the demo consumers were not adapted at the same altitude — `a-engine-regressions.md:30-91`).
- `a-engine-regressions` **H-A2** (the SHIP — the `"......"` lerp parse-error: `value.js _lerp` ← `CSSKeyframesAnimation.processFrame` `engine.ts:769` ← `lerpValue` `engine.ts:779` ← `interpFrames` `engine.ts:657`; the live throw is real but its trigger is a route-storm-restored cross-scene value.js path, NOT the hero ellipsis [the `AnimatedText.vue` hero is pure CSS — see §BLK-1]; the engine's `processFrame` trusts the compiled `iv` and calls `lerpValue(eased, iv)` with **no guard** for a bare non-numeric/non-color text leaf; the gestalt engine fix classifies a text leaf as a discrete hold-snap, NOT routed into value.js's numeric/color `_lerp` — `a-engine-regressions.md:94-153`).
- `a-scene-state-machine` **§3** (the same two error families confirmed live as state-isolation leaks: §3(a) the placeholder `contractAnim` built with a raw JS easing reaching the keyframes-string serializer; §3(b) the six-dot `"......"` reaching the CSS-value lerp path — `a-scene-state-machine.md:112-134`). H.W0 closes the engine/serializer half; the *leak* half (the placeholder reaching a UI surface at all) is H.W1's state-isolation deliverable.
- Cross-lane: `a-engine-regressions` records the deeper string-discrete-classification as a possible **value.js-HANDOFF** if the parse happens entirely inside value.js's `_lerp`/`flattenObject` (`a-engine-regressions.md:345-348`); the kf-side `processFrame` guard lands in H regardless (the belt-and-suspenders).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §1.1` (the "live console crashes" GAP row, `:71`) + `§3 H.W0` (`:122-125`) + `§THESIS` (the two crashes "ship in 4.1.0 and must die first", `:32-33`).

## §The state, verified (file:line / live anchors from the lanes)

- **H-A1 reproduces 0–3× on a Cube load that RESTS on a closure-easing readout (state-dependent — NOT a fixed 4×, see §BLK-1).** When a mounted Keyframes-string editor targets a `cubic-bezier`-closure animation (the easing `contractAnim` / amiga), the console throws: `AnimationOptionError: Invalid value for animation option "timingFunction": [function anonymous] — a custom TimingFunction has no CSS animation-timing-function representation — attach a faithful Easing.css twin, …` at `serializeEasing (src/animation/format.ts:36)` ← `CSSKeyframesToString` ← `KeyframesStringControls.vue:~95` ← `onMounted (KeyframesStringControls.vue:222-224)`. The route storm may navigate away before the throw on an un-pinned load — PIN the route to reproduce (`a-engine-regressions.md:33-45`).
- **The readout consumer has NO `try/catch`.** `KeyframesStringControls.vue:~95` — `cssKeyframesString.value = await CSSKeyframesToString(animation, getTmpAnimationName())` — the only `try` in the file is the editor *apply* path, NOT this readout. The throw aborts the `cssKeyframesString` assignment → the Keyframes-string editor tab renders stale/empty for any closure-easing animation, and uncaught errors land in the session (`a-engine-regressions.md:52-72`).
- **The root is G.W4's correct policy meeting an un-adapted consumer.** G.W4 (commit `3d352a3`, `src/animation/format.ts`) changed `serializeEasing` from a silent `"linear"` degrade to a typed THROW on a custom-closure easing with no `Easing.css` twin — correct as engine policy. `proof:roundtrip-easing` tested the *engine* in isolation, not the demo's serializer consumer → the regression was invisible to the gate (`a-engine-regressions.md:47-66`). The closure timing functions originate at the easing `contractAnim` (`useEasingDemo.ts:268`), the amiga `CSSCubicBezier` callsites (`useAmigaAnimations.ts:31,74`), and the `animations.ts` `CSSCubicBezier` presets — VERIFIED at source; NOT the Cube `Rotations`/`Matrix` presets (which resolve a registry easing and serialize clean).
- **H-A2 reproduces on a route-storm-restored cross-scene hop (NOT a clean home↔scene transition — see §BLK-1).** During scene transitions that restore stale cross-scene state, the engine throws: `Error: Parse error at offset 0: "......"` at `_lerp (value.js)` ← `CSSKeyframesAnimation.processFrame (engine.ts:769)` ← `lerpValue (engine.ts:779)` ← `interpFrames (engine.ts:657)` (`a-engine-regressions.md:97-105`). A clean single-scene load = 0 errors; the throw is on the corrupted multi-hop path (the D12 face).
- **The `"......"` trigger is a value.js path — NOT the hero ellipsis leaf (proven no-throw in unit).** `AnimatedText.vue` is pure CSS (0 engine symbols, VERIFIED) — the hero ellipsis never reaches a `CSSKeyframesAnimation`. A bare `{label:"a"}→{label:"b"}` leaf does NOT throw in unit. The live `"......"` fires through `_lerp`←`processFrame` from a route-storm-restored value position whose ACTUAL leaf must be instrumented (the MEASURE-FIRST isolate, S3a) before S3 is specified (`a-engine-regressions.md:107-117`).
- **The engine has no fail-soft for a bare text leaf.** `engine.ts:779` (`lerpValue`, reached via `:769 processFrame` ← `:657 interpFrames`) trusts the compiled `iv` carriers and calls `lerpValue(eased, iv)` with NO guard for a leaf whose start/stop is a bare non-numeric / non-color text string — so a textual leaf reaches value.js and aborts the frame. "Interpolate ANYTHING" is the library's headline and `proof:interpolate-anything` (G.W15) locks multi-arg transform/filter/gradient/color midpoints, but **a bare non-interpolable text leaf is NOT in the corpus** (`a-engine-regressions.md:124-132,149-152`).
- **The CSS-correct behaviour is a discrete hold-snap.** A text leaf should be a discrete/hold leaf (snap at the segment midpoint — the CSS discrete-animation rule for `content`/keyword strings), NOT handed to value.js's numeric/color `_lerp`. This turns a hard crash into spec-faithful motion (`a-engine-regressions.md:139-147`).
- **ALREADY-SOTA around the defect — do NOT touch.** The blend leaf (G.W17, H-A4), the orbital rotate3d (G.W18, H-A5), `.finished`/DrawSVG (G.W13, H-A6), `adoptCompiled` (G.W19, H-A7) are all sound, proof-green, NOT regressions (`a-engine-regressions.md:240-291`). `adoptCompiled` is in fact the *correct landing* for the H-A1 `.css` twins — once the Cube presets carry `.css` twins they flow through `adoptCompiled`'s atomic transplant intact (`a-engine-regressions.md:288-290`).

## §Goal

Make the demo produce **zero console errors** on a pinned `#/cube` load, on a closure-easing scene (easing/amiga), and across cross-scene hops — the prerequisite that lets every visual H lane measure a clean console. Two contained fixes plus a co-equal consumer floor, each at the higher altitude the spine demands:

1. **H-A1 (engine-faithful, the DRY answer):** the demo animations that use closure / `CSSCubicBezier` timing functions — the easing `contractAnim` (`useEasingDemo.ts:268`), amiga (`useAmigaAnimations.ts:31,74`), the `animations.ts` `CSSCubicBezier` presets — get faithful `Easing.css` twins (a `cubic-bezier()` / `linear()` literal). The demo already has the CSS twin in hand: `useEasingDemo.ts`'s `cssValue`, and amiga passes the `"cubic-bezier(0.2,0.65,0.6,1)"` STRING which already twins. Pass a typed `Easing {fn, css}` OR take the value.js-HANDOFF that makes `CSSCubicBezier` return a twinned `Easing`. Then `serializeEasing` round-trips losslessly — G.W4's fail-explicit policy is **satisfied, not swallowed** (the W4 design intent). [NOTE: the Cube-preset edit is DELETED per §BLK-1 — the Cube presets serialize clean.] Do NOT re-introduce the silent `"linear"` degrade (that was the bug W4 fixed).
2. **S2 — the readout floor is CO-EQUAL primary, not a fallback (§BLK-1 promotion).** Because a future closure-preset can always re-introduce the throw and the value.js-HANDOFF arm of S1 lands on a slower cadence, the readout `try/catch` is PROMOTED to a co-equal primary: catch at the `CSSKeyframesToString` call (`KeyframesStringControls.vue:~95`, invoked from `onMounted :222-224`), render a `/* timing-function: custom */` comment placeholder, NEVER a silent `linear`. S1 is the gestalt move; S2 is the standing floor that makes the readout total regardless.
3. **H-A2 (engine belt + value.js-HANDOFF primary, §BLK-1 reclassification):** the kf-side `processFrame` guard classifies a leaf whose value is a non-numeric, non-color, non-unit STRING as a **discrete (hold-then-snap)** leaf — interpolate by snapping at the segment midpoint rather than handing it to value.js `_lerp`. This is the BELT. The PRIMARY classification is re-classified toward a **value.js-HANDOFF** (the `parseCSSValueUnit('...')` throw lives in value.js), PAIRED with a born-RED kf corpus row that bites the ACTUAL reproduction (instrumented via the S3a MEASURE-FIRST isolate), NOT `{label:a}→{label:b}` (which proves no-throw in unit).

KISS · DRY · no new machinery. The crashes die; the engine becomes "interpolate ANYTHING" in fact, not just in headline. This is a **shipped-product-correctness** fix: 4.1.0 throws on its own default scene.

## §Scope

- **S1 — the closure-easing demo callsites get faithful `Easing.css` twins (H-A1, the engine-faithful fix · §BLK-1 re-targeted).** For each demo animation whose `timingFunction` is a closure / `CSSCubicBezier` with no `.css` twin — the easing `contractAnim` (`useEasingDemo.ts:268`), amiga (`useAmigaAnimations.ts:31,74`), the `animations.ts` `CSSCubicBezier` presets — pass a typed `Easing { fn, css }`: the demo HAS the twin in hand (`useEasingDemo.ts`'s `cssValue`; amiga's `"cubic-bezier(0.2,0.65,0.6,1)"` string already twins). `serializeEasing` then round-trips losslessly (`a-engine-regressions.md:74-83`). **WHY:** this *satisfies* G.W4's fail-explicit contract instead of swallowing it — the curve is now CSS-representable through the type system, the DRY answer the W4 design intended, and it flows through `adoptCompiled`'s atomic transplant intact (`a-engine-regressions.md:288-290`). [§BLK-1: the Cube-preset edit is DELETED — the Cube `Rotations`/`Matrix` presets resolve a registry easing and serialize clean; editing them is a no-op on the wrong seam.] If the cleanest twin is to make `CSSCubicBezier` return a twinned `Easing`, RECORD that as a value.js-HANDOFF (S2 is the standing floor either way).

- **S2 — the readout never throws (H-A1, the CO-EQUAL primary floor · §BLK-1 promotion).** The single readout call site `CSSKeyframesToString` (`KeyframesStringControls.vue:~95`, invoked from `onMounted :222-224` — NOT a watcher at `:140`) is wrapped so a non-serializable easing renders a `/* timing-function: custom */` comment placeholder instead of aborting. **WHY:** §BLK-1 promotes this from fallback to co-equal primary — the keyframes-string readout is a *display*, not a contract surface, and a future closure-preset (or the not-yet-landed value.js twin arm of S1) can always re-introduce the throw; the floor must stand permanently (`a-engine-regressions.md:84,68-72`). NOT the silent `"linear"` degrade (forbidden — that was W4's bug).

- **S3 — the engine classifies a bare text leaf as a discrete hold-snap (H-A2, the BELT · §BLK-1: PRIMARY is the value.js-HANDOFF).** The kf-side guard lands at compile time, NOT as a per-frame `typeof` sniff in the hot loop (WV-W0-MED-1): stamp a `discrete` flag in `createInterpVarValue` (`src/animation/utils.ts:283`, honoring zero-alloc) so a leaf whose start/stop value is a non-numeric, non-color, non-unit bare STRING is interpolated by **snapping at the segment midpoint** (the CSS discrete-animation rule) rather than handed to `lerpValue → _lerp`. Do NOT add a `typeof` check at `engine.ts:779` (wrong altitude for the hot loop). **WHY:** the two seams are distinct — (a) the kf compile-time `discrete` stamp in `createInterpVarValue` is the belt that lands in H; (b) the PRIMARY classification lives in value.js's `_lerp`/`prepareInterpVar`/`flattenObject` (where the `parseCSSValueUnit('...')` parse actually throws) and is a **value.js-HANDOFF** (`a-engine-regressions.md:139-147,144-147`).

- **S3a — MEASURE-FIRST: isolate the actual `"......"` leaf before specifying the corpus row (§BLK-1).** Before authoring the S4 corpus row, INSTRUMENT the live `"......"` reproduction (the route-storm-restored cross-scene hop) to capture the ACTUAL leaf that reaches `_lerp` — a `{label:a}→{label:b}` leaf proves no-throw in unit, so the corpus row must bite the real value position, not a strawman. **WHY:** specifying a gate against a leaf that does not reproduce the throw is a born-GREEN no-op; the isolate names the real reproduction so S4 bites it (`a-engine-regressions.md:107-117`).

- **S4 — the discrete-leaf corpus row + the no-throw unit lock.** Add to the `proof:interpolate-anything` corpus the bare-text-leaf row identified by S3a (the ACTUAL reproduction) asserting a **discrete snap at the midpoint and NO throw**; plus a serializer unit lock `CSSKeyframesToString(contractAnim)` (the easing scene's closure anim — the seam that ACTUALLY throws, NOT `cubeRotationsAnimation`) resolves without throw (H-A1) (`a-engine-regressions.md:88-90,149-152`). **WHY:** the corpus's blind spot (no bare-text leaf — `a-engine-regressions.md:130-132`) is the un-assayed dimension that let H-A2 ship; locking it closes the dimension permanently. The unit lock proves S1's `.css` twins make the serializer total over a seam that throws TODAY.

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

A zero-console-error CLAUSE added to the EXISTING `proof:demo-usability` Playwright gate (DRY — it already drives `#/cube`+`/` live; prefer extending it over a parallel `proof:demo-console-clean` script · WV-W0-LOW-1) + a `test/discrete-leaf.test.ts` engine lock + the `proof:interpolate-anything` corpus row + the serializer unit lock, wired into `npm test` and the `proof:*` set — FOUR clauses, each with a STATED bite:

- **`proof:demo-usability` console clause (a) — pinned `#/cube` load = 0 console errors.** A pinned `#/cube` load asserts **zero** console errors. **BITE:** reds on a load that RESTS on a route whose mounted Keyframes-string editor targets a closure-easing animation — `AnimationOptionError` throws **0–3× (state-dependent, NOT a fixed 4 — §BLK-1)**; the route storm may navigate away before the throw on an un-pinned load, so the route is PINNED to reproduce (`a-engine-regressions.md:33-45,371`); greens when S1's `.css` twins + S2's readout floor make the serializer total. Re-runnable as part of `npm run proof:demo-usability`.
- **`proof:demo-usability` console clause (b) — closure-easing scene + cross-scene hop = 0 console errors.** A pinned `#/easing` (the `contractAnim` seam) + an `#/easing` → `#/spring` hop asserts zero console errors. **BITE:** the `"......"` `Parse error at offset 0` fires on the route-storm-restored cross-scene path (the corrupted multi-hop, NOT a clean transition — §BLK-1); greens on S3's discrete-leaf belt + the value.js-HANDOFF. (Note: this clause overlaps H.W1's `proof:no-route-storm` measurement window; H.W0 owns the *engine throw*, H.W1 owns the *route storm* that triggers extra transitions — both must be green for a truly clean console.)
- **`proof:interpolate-anything` corpus row (c) — the ACTUAL `"......"` leaf snaps discretely, no throw (born-RED on the S3a-isolated reproduction, NOT `{label:a}→{label:b}` — §BLK-1).** The corpus row identified by the S3a isolate asserts `interpFrames` yields a discrete snap to one endpoint (the CSS midpoint rule) AND throws NOT. **BITE:** the strawman `{label:"a"}→{label:"b"}` row proves no-throw in unit and would pass VACUOUSLY (born-GREEN) — so this row is the S3a-isolated reproduction that fires through `_lerp`←`processFrame` TODAY; greens on the kf-side `discrete` stamp (belt) and stays green when the value.js classification publishes. Revert the stamp → the row throws again (load-bearing).
- **`proof:roundtrip-easing` unit lock (d) — `CSSKeyframesToString(contractAnim)` resolves (a seam that THROWS today — §BLK-1, NOT `cubeRotationsAnimation`).** The serializer over the easing scene's closure `contractAnim` (or an `animations.ts` `CSSCubicBezier` preset) resolves without throwing. **BITE:** reds TODAY — the closure easing has no `.css` twin and `serializeEasing` throws `AnimationOptionError` (`a-engine-regressions.md:88-90,374`); greens on S1's `.css` twin. [§BLK-1: `cubeRotationsAnimation` does NOT throw today — using it would make the gate born-GREEN, witnessing nothing.] Strip the `.css` twin → the lock reds (S1 load-bearing; S2's floor, if relied on, must render the placeholder not throw).

**The §spine bar — MUST bite.** Clauses (a)+(d) RED on a PINNED closure-easing load TODAY (the serializer over the `contractAnim` / amiga / `CSSCubicBezier`-preset seam), GREEN on S1's `.css` twins + S2's floor; clauses (b)+(c) RED on the route-storm-restored cross-scene path TODAY (the `"......"` parse error), GREEN on S3's `discrete` belt + the value.js-HANDOFF. **§BLK-1 demotion:** the original "reds 4× on every Cube load" / "`{label:a}→{label:b}` reds today" claims were born-GREEN no-ops — gates (c)+(d) are RE-POINTED at seams that ACTUALLY throw. No clause passes vacuously: (a)/(b) assert an EXACT console-error count (zero) on a PINNED route, (c) asserts an EXACT discrete-snap value AND no-throw on the S3a-isolated reproduction, (d) asserts the serializer is total over a seam that throws today. These are the falsifiable proofs the crashes are real and the fixes land them — exactly the gap H-A1/H-A2 left (`proof:roundtrip-easing` tested the engine in isolation, never the demo's serializer consumer — `a-engine-regressions.md:64-66`).

## §Folds

- `a-engine-regressions` **H-A1** (S1 the engine-faithful `.css` twins at the easing/amiga/`CSSCubicBezier` seams + S2 the CO-EQUAL readout floor + `proof:roundtrip-easing` unit lock (d) over `contractAnim`).
- `a-engine-regressions` **H-A2** (S3 the kf-side `discrete`-stamp belt + S3a the MEASURE-FIRST isolate + S4 the corpus row + `proof:demo-usability` console clause (b) + `proof:interpolate-anything` (c)) — the engine belt of D6; the PRIMARY classification is the value.js-HANDOFF. The *demo* typing-dots visual fix is H.W6's charge (`a-typing-dots`); H.W0 ships the engine belt-and-suspenders so a corrupted hop never aborts a frame regardless of the dots fix.
- `a-scene-state-machine` **§3** (the two error families recorded as state-isolation symptoms) — H.W0 closes the *engine/serializer throw*; the *leak* (the placeholder `contractAnim` reaching a serialize surface AT ALL, the start-screen and scene group not cleanly separated) is H.W1's state-isolation deliverable. Cross-linked so the two are never split.
- **value.js-HANDOFF (PRIMARY for H-A2, born-RED-paired · §BLK-1):** the bare-string discrete-classification lives inside value.js's `_lerp`/`prepareInterpVar`/`flattenObject` (where `parseCSSValueUnit('...')` throws) — this is the PRIMARY classification and a value.js-HANDOFF, not a mere RECORD fallback. The kf-side `discrete` stamp in `createInterpVarValue` (S3) lands in H regardless as the belt (`a-engine-regressions.md:144-147,345-348`). Author the corpus row (c) — biting the S3a-isolated reproduction — as the consume-signal: it greens on the kf belt and stays green when the value.js classification publishes. (The S1 "`CSSCubicBezier` returns a twinned `Easing`" arm is the SECOND value.js-HANDOFF; S2's readout floor is the standing belt for it.)
- **RECORD (no work) — the ALREADY-SOTA engine waves:** G.W17 blend, G.W18 orbital, G.W13 `.finished`/DrawSVG, G.W19 `adoptCompiled` are all sound and proof-green (H-A4..A7) — H.W0 touches NONE of them; the live errors are in `format.ts`/`processFrame`, not those seams (`a-engine-regressions.md:240-291`).

## §Design decisions (trade-offs RESOLVED)

- **S1's `.css` twin AND S2's readout floor are CO-EQUAL primaries — RESOLVED (§BLK-1).** The original framing made the readout `try/catch` a mere fallback; §BLK-1 promotes it to co-equal. S1 gives the closure-easing CALLSITES (easing `contractAnim` / amiga / `animations.ts` `CSSCubicBezier` presets — NOT the Cube presets, which serialize clean) faithful `.css` twins so the curve is CSS-representable through the type system and W4's policy is *satisfied* (`a-engine-regressions.md:74-83`). S2 wraps the readout call (`KeyframesStringControls.vue:~95`) so it NEVER throws into the render path — the standing floor for any residual non-representable curve and for the slower-cadence value.js twin arm (placeholder comment, never silent `"linear"`).
- **The discrete-leaf classification is the CSS-correct behaviour, not a swallow — RESOLVED.** S3 does NOT silently drop the leaf or coerce it to `"linear"`; it stamps a `discrete` flag at compile time in `createInterpVarValue` (the zero-alloc seam, NOT a per-frame `typeof` sniff in the hot loop) implementing the CSS discrete-animation rule (hold-then-snap at the midpoint) for non-interpolable strings (`a-engine-regressions.md:139-147`). The gate (c) asserts the EXACT discrete snap on the S3a-isolated reproduction, so "discrete" is a tested contract, not a hand-wave.
- **value.js-HANDOFF is the PRIMARY for H-A2; the kf belt lands now — RESOLVED (§BLK-1, both, sequenced).** The classification belongs in value.js (where `parseCSSValueUnit('...')` parses + throws). H.W0 lands the kf-side `discrete` stamp NOW (the belt, demo unblocked immediately) and the value.js classification is the PRIMARY HANDOFF that rides the next re-pin; the corpus row (c) — biting the S3a-isolated reproduction, not the no-throw strawman — is the consume-signal that stays green either way (`a-engine-regressions.md:144-147,345-348`). The chronic-closure discipline (H.W8): this HANDOFF is paired with the born-RED corpus row (c) — no bare HANDOFF tag.
- **HIGH, not MED — RESOLVED.** Two LIVE, reproducible uncaught engine exception FAMILIES ship in 4.1.0 (the closure-easing serializer throw on the easing/amiga scenes 0–3× state-dependent, and the `"......"` `_lerp` parse-error on the route-storm-restored cross-scene path — §BLK-1: NOT 4× on the default Cube route), invisible to the existing gate regime because G.W4's `proof:roundtrip-easing` tested the engine in isolation, never the demo consumer (`a-engine-regressions.md:21-27,64-66`). A shipped-product throwing uncaught on a live route is the highest-priority correctness bite — it leads the tranche.

- **Sequenced FIRST, parallel to H.W1 — RESOLVED.** H.W0 is a prerequisite: the live crashes poison every other lane's clean-console measurement (`_SYNTHESIS-gap-scorecard.md §3`). S1/S2/S3 are self-contained correctness fixes that CAN land in parallel with H.W1 — but the FULL clean console requires both (H.W0 kills the engine throw, H.W1 kills the route storm that triggers the extra transitions). The `proof:demo-usability` console clause (b) is shared: it is GREEN only when both land.
