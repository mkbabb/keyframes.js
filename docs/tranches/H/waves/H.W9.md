# H.W9 — DESIGN-LANGUAGE REFINEMENT ROUND 2 (the consolidated user-feedback fold · the calm glass+cartoon register · F1–F9)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (CORRECTIVE — the user-feedback refinement round, net-deletion-leaning: F3/F6 remove the specular subsystem; pure demo CSS/markup + one composable deletion + one `useIdle` wire + two glass-ui HANDOFFs; NO engine source, NO glass-ui patch in kf — inv-16, NO dist build) · **Scope:** the ~14 `surface="cartoon"` panel Card sites (→ add `tier="quiet"` — `AnimationControlsControls.vue:3`, `RibbonBar.vue:3`, `KeyframesEditor.vue:3`, `KeyframeTimeline.vue:3`, `MatrixEditor.vue:2`, `AssetViewport.vue:12`, `AssetLayerPanel.vue:2`, `EasingSidebar.vue:19,40`, `SpringSidebar.vue:4,60,81`, `TimingFunctionPanel.vue:30,87`) + the tracked-specular subsystem DELETED (`TimingFunctionPanel.vue:17-30,175,198`, `useSpecularPointer.ts` whole file, `design-idioms.css:245-282`) + the controls rows (`AnimationControlsControls.vue:4,9,133`, `LayerConfigPanel.vue:3,19,46`) + the bezier panel fit + header bake (`AnimationControlsControls.vue:331-334`, `TimingFunctionPanel.vue:5-12,30-32,51-58`) + the controls-content shadow clearance (`ControlsPaneWrapper.vue:182,207-208`) + the dock menu (`App.vue:38-44,48-61`) + the idle-fade restoration (`usePaneHover.ts`, `useControlsLayout.ts:50`, `ControlsPaneWrapper.vue:11,16-17,159-161,190-193`, a `useIdle(10_000)` wire + a `design-idioms.css` token+rule) + the gate set (AMEND `proof:single-column-pack`; NEW `proof:bezier-no-scroll`/`proof:glass-and-cartoon`/`proof:pp-logo-svg`/`proof:darkmode-row-toggle`/`proof:cartoon-shadow-unclipped`/`proof:idle-fade`; INVERT `proof:no-orphan-specular`; RETIRE `proof:cartoon-specular-coexist`+`proof:specular-calm`) · **DAG-deps:** depends on **H.W1** (the FSM resting — every browser gate settle-gates on it), **H.W2** (the surface flip it refines — `surface="cartoon"` is the substrate `tier="quiet"` tunes + the specular it removes), **H.W3** (the `rail·stage·rail` grid + the row shape F1 corrects + the load-bearing `overflow:hidden` F7 reconciles), **H.W4** (the bezier panel it re-fits — coordinates with `proof:easing-canvas-bounded`); **H.W6** (the `EditorStartScreen` hero lane is untouched — no collision). Sequenced **AFTER W2/W3/W4/W6, BEFORE H.W8's GOLDEN `proof:visual-lock` baseline capture** (the H.W9 calm register / one-column row / un-clipped shadow MUST be the rendered state H.W8 locks).

## §Provenance (the folded lanes)

This wave is the SYNTHESIS lane of `_PLAN.md` (the COMPLETE, authoritative H.W9 plan), grounded on the feedback triumvirate's three research lanes:

- `r1-source-rootcause.md` — the `file:line` root-cause of each of the user's nine live-observed feedback items F1–F9 (the per-item §F1..§F9 source anchors, the ~14 `surface="cartoon"` site inventory in §F6, the "no missing asset" pp-logo finding in §F4).
- `r2-git-archaeology.md` — the "previous correct" forms: F1's `cfea657:AnimationControlsControls.vue:4,252` parent `grid-cols-[auto_1fr]` + subgrid rows (the OLD correct row shape W3 collapsed by accident); F9's `git show 11550cd` idle-fade (`opacity:0.85; transition .5s ease-out 2s` + `--hovered → opacity:1`) and the JS-linger successor `3b8b468` (the direct ancestor of today's `usePaneHover.ts`); F4's committed brand asset `assets/ppmycota-logo-3.svg` (kf-owned locally, ∅ in the value.js/glass-ui/parse-that siblings).
- `r3-design-reconcile.md` — the design + modern-web reconcile: the F3+F6+F8 → ONE register decision (the headline — REMOVE the tracked specular decisively), the settings-row label-left/value-right idiom for F1, the symmetric shadow-clearance for F7, the `useIdle` modern idiom + `:focus-within` a11y improvement for F9.
- `_PLAN.md` (the BINDING synthesis — read in full) — §0 the one-wave structure decision, §1 the cross-cutting gestalt, §2 the per-item scope clauses, §3 the gate list, §4 the supersede-map, §5 the glass-ui HANDOFFs, §6 the F6 USER-DECISION fork, §7 the convergent sequencing.

Synthesis home: `_PLAN.md` (entire). This wave SUPERSEDES specific landed W2/W3/W4 decisions — the §supersede-map below is the honest ledger.

## §The state, verified (file:line / live anchors from the lanes)

- **THE HEADLINE — glass and cartoon ALREADY coexist; F3+F6+F8 collapse into ONE register move (R3, decisive).** `cartoon-surface` is a DECORATION-ONLY utility that composes ON TOP of the `glass-${tier}` class the Card already emits (`cards.css:22-48`). So the `surface="cartoon"` panels W2 landed ARE glassy today — what reads as "less glassy" (F8) is the Card's default `tier="resting"` (0.65 α) reading more opaque than the old `.glass-card` plate (which was `glass-quiet`, 0.50 α), and the absence of the catch-light shimmer (which survives ONLY on the one composite bezier card from W2's S2-COMPOSITE). **F3 + F6 + F8 are therefore ONE move:** keep `surface="cartoon"`, drop the panel Cards to `tier="quiet"` (recover the EXACT 0.50 α/10px the user remembers — F8), and REMOVE the tracked specular entirely (F3 too-dramatic + F6 consistency → the user's own "perhaps we just remove it"). Net DELETION + a NAMED prop. The W2 "one composite exception" dissolves; one calm register: glass + cartoon, broad, no tracked catch-light. (`_PLAN.md` headline / §1)
- **F1 — W3 collapsed the per-row split by accident.** `ece4743` collapsed the `CardContent grid-cols-[auto_1fr]` + the `grid-cols-[subgrid]` chain → `flex flex-col` (W3.S1), which made each field STACK label-over-value. W3 conflated TWO axes: (a) the OUTER lopsided two-column sidebar (rail 212px + stage 466px — the real D1 defect, correctly collapsed) and (b) the per-row INNER `[auto_1fr]` label|value split (collapsed by accident). The in-tree LIVING precedent that survived W3: `AssetPropertiesPanel.vue:6` (`grid grid-cols-[auto_1fr] items-center gap-x-3 gap-y-1.5` — exactly the shape F1 restores at the row level). (`r1`/`r2`, `_PLAN.md §2 F1`)
- **F2 — the bezier panel scrolls; the back button is an external top-LEFT element.** The bezier panel's intrinsic height (header + 280px easing canvas + readout + preset select + the standalone back `<Button>`) exceeds the detail-panel cap `min(50vh,480px); overflow-y:auto` (`AnimationControlsControls.vue:331-334`), producing a scrollbar; W4 placed the back affordance as a top-LEFT `<Button>` ABOVE the card (`TimingFunctionPanel.vue:5-12`, `justify-self-start mb-2 @click="emit('exitDetailPanel')"`). The bezier stack: `:30-32` (`CardHeader … <CardTitle>cubic-bézier</CardTitle>`, `grid gap-0`), `:51-58` (the `<EasingCurveCanvas>`, `38cqi` clamped `[160,280]` — the W4 ceiling, the dominant term), `:60-79` (the preset `<Select>`), `:245-248` (`.easing-editor { container-type: inline-size }`). (`r1`, `_PLAN.md §2 F2`)
- **F3/F6 — the tracked specular is a MOVING, white-cored, screen-blended radial on ONE card.** Magnitude single-sourced at `useSpecularPointer.ts:42` (`rest=0.22, hover=0.4`), written `:54-55`, the cursor write `:62-72`; projected by `design-idioms.css:266-282` (`hsl(40 30% 100% / 0.55)` core, `screen` blend, the `inherits:false` typed `@property` projection). The composite is on the bezier card ALONE (`TimingFunctionPanel.vue:30` manual `glass-specular-track`, `:175,198` the `useSpecularPointer` wire) — ~13 other cartoon Cards do NOT carry it. The `proof:specular-calm` ceiling (rest ≤0.25 / hover ≤0.4) is MET yet the user still reads it as overpowering (F3) and inconsistent (F6). The W2 `proof:no-orphan-specular` exception set is `{TimingFunctionPanel bezier}`; `proof:cartoon-specular-coexist` + `proof:specular-calm` police that one exception. (`r1`, `_PLAN.md §2 F3/F6`)
- **F7 — a STRUCTURAL TENSION: W3's load-bearing `overflow:hidden` × W2's left-casting cartoon shadow.** TWO facts compose: (1) the cartoon offset shadow casts bottom-LEFT — `--shadow-cartoon-md: -4px 3px …`, `--shadow-cartoon-lg: -6px 4px …` (`tokens.css:543-551`, negative-x left + positive-y down); the hover lift adds `translate: -1px -1px` (`--lift-sm`, `tokens.css:830`) growing the left lobe. (2) The clipping ancestor: `ControlsPaneWrapper.vue:182` desktop `overflow:hidden` (load-bearing for the `[rail]`-track collapse, `:171-173`); `.controls-content` budgets clearance only RIGHT + BOTTOM (`:207-208` `padding-right:12px; padding-bottom:12px`) — but the shadow goes LEFT. The bottom-LEFT lobe has ZERO clearance → sliced by the `overflow:hidden` edge (a straight sharp cut). The Card itself is `overflow-visible` (`:3`) but a child cannot escape an ancestor's clip. (`r1`/`r3`, confirmed from source, `_PLAN.md §2 F7`)
- **F4 — the pp logo SVG ALREADY renders; the defect is the markup AROUND it.** `App.vue:50` `<div class="ppmycota-logo-sm w-7 h-7 …">` → `.ppmycota-logo-sm` (`@styles/brand.css`) → `url("@assets/ppmycota-logo-3.svg")` (the committed brand asset at `assets/ppmycota-logo-3.svg`; siblings `-2.svg` LARGE, `.svg` original); tinted via `--filter-brand-color` (`style.css:175`). NOT in the value.js/glass-ui/parse-that siblings (`grep -rli ppmycota` = ∅) — keyframes.js owns it locally. The defect is the emoji `<p>` line at `App.vue:58` (HTML entities `🙂‍↔️ 🌱 🍄‍🟫`) garnishing the brand mark; `:57` the typed "ppmycota" word, `:59` the ppmycota.com link, `:235` the whole-row `@click="togglePpMode"` (PRESERVE). (`r1`/`r2`, `_PLAN.md §2 F4`)
- **F5 — only the dark-mode ICON toggles; the row does not.** `App.vue:38-44` — the dark-mode `DropdownMenuItem` carries `@select.prevent` (suppresses default select so the menu stays open) and NO row `@click`; only `<DarkModeToggle … class="aspect-square w-5">` (a glass-ui `<button>` owning `toggleDark` internally) flips the theme; the `<span>Dark mode</span>` is inert. Contrast `App.vue:49` (ppmycota) which carries `@click="togglePpMode"` on the whole row. `toggleDark`/`DarkModeToggle` from glass-ui; `DarkModeToggle.vue:79` `@click="!passive && toggleDark()"` (the `passive?: boolean` prop exists). (`r1`/`r2`, `_PLAN.md §2 F5`)
- **F8 — the W2 surface flip silently RAISED the opacity tier.** Flipping `.glass-card` (tier `quiet`, 0.50 α/10px) → `surface="cartoon"` (default tier `resting`, 0.65 α/12px) raised the opacity; the 2px border + crisp offset stamp reinforce the "lost the glass" read. glass-ui material model: `cards.css:22-48`, `glass.css:48-106,367-383`, `tokens.css:572-583,600-601`, `Card.vue.d.ts` (default `tier="resting"`). Reduced-transparency already honored (`glass.css:367-383` maps `--glass-blur-*`→none + α→1 — the tier choice still governs the floor). `quiet` 0.50 α is the documented "missing rung" the prior plate used. (`r1`/`r3`, `_PLAN.md §2 F8`)
- **F9 — the idle-fade CSS is gone; the class is vestigial.** A D-era refactor (`6f5a421`/`905a8c3^` already open/closed-only; the W3 wrapper rewrite did not re-author it) dropped the opacity rule that consumed `.controls-pane--hovered` while keeping the `usePaneHover` plumbing (`usePaneHover.ts:12,25-40`, the 2s `useTimeoutFn` linger) + `useControlsLayout.ts:50` (wires `isPaneHovered`) + the applied class (`ControlsPaneWrapper.vue:11`, `:16-17` `@mouseenter/@mouseleave`, `:159-161`/`:190-193` the `opacity:1` rest with NO `--hovered` consumer — the dead class). Proof-it-was-extant: `git show 11550cd` ("Idle opacity 0.85 with 2s delayed restore, instant on hover"). `@vueuse/core@^14.3.0` `useIdle` is installed (`index.d.ts:2930`). (`r1`/`r2`, `_PLAN.md §2 F9`)

## §Goal

Land the calm glass+cartoon panel register the user's nine feedback items collectively ask for — the user audited the running demo and named where W2/W3/W4 over-corrected or left a seam open. **The headline is ONE register move (F3+F6+F8):** keep `surface="cartoon"`, add `tier="quiet"` broadly (the glass returns at the exact 0.50 α/10px of the pre-cartoon plate), and REMOVE the tracked specular entirely (the lead has adopted REMOVE as the default per the user's "perhaps we just remove it" lean + R3's decisive recommendation; the CONSISTENT alternative is documented as the §USER-DECISION fork). Around that headline: restore label-LEFT/value-RIGHT rows while keeping ONE column (F1); fit the bezier panel and bake the back into the header right (F2); give the cartoon shadow symmetric clearance inside the load-bearing clip (F7); lead the ppmycota menu with the existing SVG mark and drop the emoji (F4); make the whole dark-mode row the click target (F5); and re-author the idle-fade dogfooding `@vueuse/core useIdle` (F9). Net-deletion-leaning (F3/F6 delete the specular subsystem — the manual `glass-specular-track`, the `.cartoon-specular ::before` projection, `useSpecularPointer` wire + file), one register, every fix idiomatic and gestalt — NO workaround, NO legacy beside its replacement, styling ISOMORPHIC unless a NAMED delta (the `quiet` tier recovers the isomorphic glass read; the cartoon stamp is the W2 named delta retained). This is the chronic-closure discipline in ACTION: the user caught a re-paper (the W2 composite, the W3 row collapse) BEFORE the H.W8 golden baseline locked it, and F6's invert keeps the D2 cartoon chronic closed via a STRONGER system property.

## §Scope (each S = WHAT + WHY)

- **S1 — F8 + F3 + F6 TOGETHER: the calm glass+cartoon register (revises W2 CORE).** WHAT: (a) add `tier="quiet"` to every kf-owned `surface="cartoon"` panel Card (the ~14 sites in the Scope inventory — `AnimationControlsControls.vue:3`, `RibbonBar.vue:3`, `KeyframesEditor.vue:3`, `KeyframeTimeline.vue:3`, `MatrixEditor.vue:2`, `AssetViewport.vue:12`, `AssetLayerPanel.vue:2`, `EasingSidebar.vue:19,40`, `SpringSidebar.vue:4,60,81`, `TimingFunctionPanel.vue:30,87`) — 0.50 α + 10px blur, recovering the old glass plate (F8); (b) DELETE the tracked-specular subsystem entirely — the manual `glass-specular-track` class (`TimingFunctionPanel.vue:30`), the `.cartoon-specular ::before` projection (`design-idioms.css:262-282`), the `useSpecularPointer` wire (`TimingFunctionPanel.vue:175,198`), and `useSpecularPointer.ts` itself if no other consumer remains; the `.cartoon-specular` NAME folds to bare `surface="cartoon" tier="quiet"` (F3+F6). The bezier card becomes a plain `tier="quiet"` cartoon Card like its siblings. WHY: it is ALREADY the architecture (cartoon = decoration over a glass tier) — `quiet` restores the EXACT 0.50 α the user remembers (isomorphic glass read + the named cartoon delta); REMOVE is the strongest calm (a moving white-cored screen-blended radial reads dramatic at ANY visible opacity — lowering further = invisible-but-complex, anti-KISS) and the coherent gestalt (track on EVERY panel — a per-surface `pointermove` + `::before` across ~14 surfaces, the glassmorphism-perf audit's most expensive idiom, re-opening the very "radial blur everywhere" W2 killed — or on NONE). No new backdrop CSS (inv-16); net deletion. **These MUST land together** — quiet glass without specular-remove leaves the lone dramatic composite jarring; specular-remove without quiet glass leaves the panels opaque. (`_PLAN.md §2 F3/F6/F8 · §7.1`)

- **S2 — F7: symmetric shadow-clearance so the cartoon stamp clears the load-bearing clip.** WHAT: BASE FIX (works everywhere) — add `padding-left` (keep `padding-bottom`) to `.controls-content` (`ControlsPaneWrapper.vue:207-208`) to clear the shadow's actual bottom-LEFT throw; the shadow then renders INSIDE the padded box and the wrapper's clip never reaches it. PROGRESSIVE ENHANCEMENT (non-base, nice-to-have): `overflow: clip` + `overflow-clip-margin` on the NON-scrolling outer wrapper only (modern-web `overflow-clipping-control`) — CAVEAT limited-availability (Firefox 148+ as of 2026, not Chrome/Edge/Safari) AND `overflow:clip` disables scrolling → a PE layer, NOT the base. DO NOT remove the wrapper's `overflow:hidden` (load-bearing for the rail-track collapse). WHY: "no workaround; reconcile, don't fork" — keep the load-bearing clip, give the shadow room INSIDE it; the demo ALREADY clears right/bottom (it just chose the wrong sides). The crisp Memphis stamp (0–2px blur) is INTENDED — the SLICE is the defect, not the crispness. Lands cleanly with S1 (the cartoon shadow is now broad-register; the clearance protects it everywhere). (`_PLAN.md §2 F7 · §7.2`)

- **S3 — F1: restore label-LEFT/value-RIGHT per row, ONE column of rows (revises W3.S1).** WHAT: two paths, the HANDOFF is the durable home — (A, durable glass-ui HANDOFF) add `orientation="horizontal"` (or `inline`) to `LabeledField` → `.labeled-field { display:grid; grid-template-columns:auto 1fr; align-items:center; column-gap:… }`, error region `grid-column:1/-1` (the reusable home — every `Labeled*` consumer inherits label-left by one prop); (B, demo-side born-GREEN, lands today) wrap the advanced panel's fields in `grid grid-cols-[auto_1fr] items-center gap-x-3 gap-y-1` mirroring `AssetPropertiesPanel.vue:6` — the `.labeled-field` is block flow, so its `<label>` + control auto-flow into the two grid cells. Targets: `AnimationControlsControls.vue:4,9,133`; `LayerConfigPanel.vue:3,19,46` (the `advanced` pane — blend/z-index/enabled). WHY: W3 conflated the OUTER lopsided sidebar (correctly collapsed) with the INNER row split (collapsed by accident) — F1 reverts ONLY the inner; an intra-row split does NOT re-introduce a second column of fields (every row keeps ONE left edge); reconcile, don't fork — the OLD correct form returns at the ROW level (the `AssetPropertiesPanel` precedent), the single-column-pack invariant STAYS TRUE. (R3: the macOS/iOS settings-row idiom; `modern-web forms §2` "labels above" targets data-entry FORMS, not compact settings panels — `gap-y > gap-x` preserves Gestalt proximity.) (`_PLAN.md §2 F1 · §7.3`)

- **S4 — F2: fit the bezier panel (no scroll) + bake the back into the CardHeader right (revises the W4.S1/S2 bezier-panel sizing band — the standing-height outcome it left under the pre-existing detail-cap + external back button).** WHAT: (a) make the `CardHeader` a `flex items-center justify-between` row — `<CardTitle>cubic-bézier</CardTitle>` LEFT + a right-aligned ghost icon-button (`<ArrowLeft/>` or `<X/>` close) `@click="emit('exitDetailPanel')"`; DELETE the standalone top-LEFT `<Button>` (`TimingFunctionPanel.vue:5-12`) — no legacy beside its replacement; (b) fit the height via (i) a TIGHTER in-panel canvas ceiling (smaller than the full-rail 280px — coordinated with H.W4's `proof:easing-canvas-bounded` which DEMOTES `blockSize<=280` to a post-fix full-rail ceiling), AND/OR (ii) the header move removing one row, AND/OR (iii) relax `overflow-y:auto`→`visible` once the content fits within the cap (`AnimationControlsControls.vue:331-334`). MEASURE-FIRST: confirm the fit at the standing detail-panel cap before relaxing the overflow. **NAMED delta vs H.W4:** the in-panel bezier ceiling is a panel-context clamp TIGHTER than W4's full-rail 280 — NOT a contradiction (W4's 280 is the full-rail ceiling); the square LAW (`width===height`) is PRESERVED. WHY: KISS — a panel should fit, not scroll; the header-right dismiss is the idiomatic detail-panel pattern (title left, dismiss right); folding the standalone back row into the header reduces standing height. (`_PLAN.md §2 F2 · §7.4`)

- **S5 — F4 + F5: the dock menu (App.vue markup + wiring).** WHAT: (F4) DROP the emoji `<p>` line (`App.vue:58`); lead with the existing `.ppmycota-logo-sm` SVG mark as the brand identity (it already is the leading mark); optionally retain a single caption (the ppmycota.com link) and consider whether the typed "ppmycota" word (`:57`) is redundant beside the SVG wordmark; PRESERVE the whole-row `@click="togglePpMode"` (`:235`). (F5) give the dark-mode `DropdownMenuItem` a row-level `@click="toggleDark()"` (mirroring the ppmycota row at `:49`) and make `<DarkModeToggle passive>` (its `passive?: boolean` prop) so the inner button becomes a pure INDICATOR that reflects state WITHOUT double-toggling when the row fires. WHY: KISS — the brand mark IS the identity (the emoji is clutter; no asset hunt, the SVG already renders + is wired); the row is the affordance (discoverability; the fix precedent sits RIGHT below it — the ppmycota row already carries a row-level `@click`); demo-local, idiomatic glass-ui consumption of the existing `passive` prop, no glass-ui change. (`_PLAN.md §2 F4/F5 · §7.5`)

- **S6 — F9: restore the controls idle-fade via `@vueuse/core useIdle` (restoration; inv ζ).** WHAT: `useIdle(10_000)` (global window idle — tracks mousemove/mousedown/keydown/touchstart/wheel/visibilitychange, so scrubbing a slider / typing / scrolling all RESET it); drive ONE class off `idle && !isPaneHovered`; CSS owns opacity + transition (token the magnitude `--controls-idle-opacity` ~0.35 in `design-idioms.css`, do NOT go below ~0.25 — controls must stay legible while they capture pointer/focus):
  ```css
  @media (min-width: 1024px) {
    .controls-pane-wrapper { transition: opacity var(--duration-normal) var(--ease-standard); }
    .controls-pane--idle:not(.controls-pane--hovered) { opacity: var(--controls-idle-opacity, 0.35); }
    .controls-pane-wrapper:hover, .controls-pane-wrapper:focus-within { opacity: 1; }
  }
  @media (prefers-reduced-motion: reduce) { .controls-pane-wrapper { transition: none; } }
  ```
  Targets: `usePaneHover.ts` (the 2s linger stays as the immediate hover tracker), `useControlsLayout.ts:50`, `ControlsPaneWrapper.vue:11,16-17,159-161,190-193`; token home `design-idioms.css`. **NAMED a11y IMPROVEMENT over the historical form:** `:focus-within` in the override (the old code had no focus override — a keyboard user tabbing in must never be left on a ghosted surface) + a MANDATORY PRM guard (snap, not animate). Reconcile the user's "~10s" with the historical "hover + 2s linger": `useIdle(10_000)` for WHEN, the `:hover`/`:focus-within` override for the instant lift; `usePaneHover`'s 2s linger stays the immediate hover-state tracker. WHY: "this used to be extant" + the spine dogfoods the installed primitive (`@vueuse/core useIdle`, no hand-rolled `setTimeout`); `opacity` is a compositor-thread property (cheap fade — `modern-web css §9`); the infra is intact, only the consuming CSS rule is gone — the class is vestigial. (`_PLAN.md §2 F9 · §7.6`)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

The 10 H.W9 gates, each BITES + cites a `file:line`/live anchor, each born-RED on `tranche-h-impl` today. Gate-authoring home: per `H.W8.md §Scope` the gate-regime wave OWNS gate authoring; H.W9's NEW born-RED clauses are wired into the `proof:*` set + `proof:all`, and the H.W8 `proof:visual-lock` named-region matrix captures the H.W9 calm-register / one-column-row / un-clipped-shadow GOLDEN states AFTER H.W9 lands. Every browser gate settle-gates on the H.W1 FSM resting.

| Gate | Item | Disposition | BITE (born-RED today → green on fix) |
|------|------|-------------|--------------------------------------|
| `proof:single-column-pack` | F1 | **AMEND** (add label-left clause) | the existing clause measures the `.controls-content .labeled-field` ROW box (one left-edge x, one width ±2px) — STAYS GREEN under an intra-row split (it measures the row element, NOT the label/control inside). ADD a born-RED clause: for each visible leaf row, `labelRect.right ≤ controlRect.left` (label LEFT of value). Reds on the current label-ABOVE stack (`labelRect.right > controlRect.left`); greens on the intra-row `[auto_1fr]`; the one-column row invariant stays GREEN. Non-vacuity: re-uses the existing `fieldCount >= 6` guard, `#/cube` pin, settle-gate. |
| `proof:bezier-no-scroll` | F2 | **NEW** | at `#/cube` desktop, open controls → cubic-bézier detail panel: assert the `.panel-content` host `scrollHeight ≤ clientHeight` (no vertical overflow) AND `getComputedStyle(...).overflowY !== 'scroll'`; PLUS a header-shape clause — the back/close control's `getBoundingClientRect().left` is to the RIGHT of the `CardTitle` ("cubic-bézier") AND inside the CardHeader row (a header sibling, not an external pre-card button). Reds today (content overflows the `min(50vh,480px)` cap on a short viewport; the back button is an external top-LEFT element); greens on the fit + header-right bake. Settle-gated on H.W1; mirrors `proof:easing-canvas-bounded`'s panel-measurement plumbing. |
| `proof:no-orphan-specular` | F3+F6 | **INVERT** (exception set → ∅) | INVERT clause 1: the enumerated exception set `{TimingFunctionPanel bezier}` → the EMPTY set. New invariant: ZERO `.glass-specular-track` on ANY kf-owned `<Card>`. Reds on the bezier composite TODAY; greens on the removal. STRONGER than the W2 form (it still bites a re-introduced `surface="glass"`) — the chronic-closure-discipline SYSTEM-property gate that replaces the retired ones ("no panel paints a tracked catch-light"). |
| `proof:cartoon-specular-coexist` | F3+F6 | **RETIRE** | subject (the W2 composite) deleted — drop from `proof:all` + `package.json`. The S2-COMPOSITE no longer exists, so the gate's `pointermove`+`circle at <x≠50%>` assertion has no live subject. |
| `proof:specular-calm` | F3+F6 | **RETIRE** | no retained `::before` to cap — drop from `proof:all` + `package.json`. The calm is now by ABSENCE, not a lower ceiling. |
| `proof:glass-and-cartoon` | F8 | **NEW** | at a panel-bearing route desktop, assert each kf-owned panel Card resolves BOTH `box-shadow` from `--shadow-cartoon-md` (the existing `proof:cartoon-is-panel-depth` covers this, tier-agnostic, stays GREEN) AND a TRANSLUCENT background (`backdrop-filter !== 'none'` AND background-color α < 1) in the non-reduced-transparency case. Reds if a panel resolves an OPAQUE background (α ≥ 1 / no backdrop-filter) — born-RED TODAY only IF the resting tier trips the α threshold; bind the α threshold MEASURE-FIRST (resting 0.65 vs quiet 0.50 delta) so the gate bites the "panels went opaque" regression. Guard the non-PRM-reduce case (under PRM-reduce the panels SHOULD be opaque — exclude that media). The F8 "glass is back" lock. |
| `proof:pp-logo-svg` | F4 | **NEW** (static) | assert the ppmycota menu item (`App.vue`) renders the `.ppmycota-logo-sm` SVG-backed mark AND contains ZERO emoji codepoints in its text (grep the item's markup for the `🙂↔️🌱🍄` entity range → 0) AND `.ppmycota-logo-sm` resolves to a real `assets/ppmycota-logo-*.svg` `background-image`. Reds today (the emoji `<p>` at `:58` present); greens on the emoji removal. Cheap static parse — no browser needed; mirrors `proof:idioms` clause-1 resolve-or-red. |
| `proof:darkmode-row-toggle` | F5 | **NEW** | open the `@mbabb` dock menu; click the dark-mode row at its LABEL/PADDING gutter (NOT the icon) → assert `<html>` `.dark` class flips; click again → flips back (exactly ONCE per click — no double-toggle from the passive icon). Reds today (clicking the label does nothing — no row `@click`); greens on the row-level wire + passive icon. Settle-gated on H.W1; cross-ref `proof:dock-popover-opens` plumbing (H.W1) for the menu-open driver. |
| `proof:cartoon-shadow-unclipped` | F7 | **NEW** | at `#/cube` desktop, the active panel Card's rendered bounding box minus the cartoon shadow's `|x|` left extent (the shadow's leftmost pixel) is ≥ the `.controls-pane` content-box left edge (the shadow's left lobe lands inside the pane). Reds today (the bottom-LEFT shadow lobe falls outside the padded box → clipped); greens once `.controls-content` clears the left. Mirrors `proof:stage-not-clipped`'s viewport-containment plumbing (must bite on the LIVE corner, not a source grep — F7 is a STRUCTURAL pixel artifact). |
| `proof:idle-fade` | F9 | **NEW** | at `#/cube` desktop, pane OPEN: fire no events for >10s (advance via `useIdle`'s clock / a Playwright wait), assert the pane's computed `opacity` drops to `--controls-idle-opacity` (< the active 1.0); then `page.hover` the pane → assert it returns to 1. Reds today (no idle dim exists — the class is dead, opacity rests at 1); greens on the restored `useIdle`-driven rest-dim. Non-vacuity: the active/resting opacity must be 1 so the dim is a real delta. Settle-gated on H.W1. |

**KEPT UNCHANGED:** `proof:cartoon-is-panel-depth` (tier-agnostic — F8's tier flip leaves it GREEN), `proof:specular-handoff` (glass-ui-owned Button/dock-icon tracks, still born-RED HANDOFF), `proof:no-dup-utility`, `proof:timeline-rail-width`, `proof:demo-shell-grid`, `proof:stage-not-clipped`, `proof:easing-canvas-bounded` (F2's in-panel bezier ceiling is a NAMED panel-context clamp, not a contradiction — the full-rail 280 ceiling + square law hold).

**The §Mandate bar — MUST bite.** Each H.W9 gate asserts an EXACT live measurement or static fact the lanes captured: the label-ABOVE stack (`labelRect.right > controlRect.left`), the bezier scroll overflow + external back button, the live bezier composite track, the opaque-resting α, the emoji `<p>` at `:58`, the dead dark-mode label click, the sliced bottom-LEFT shadow lobe, the dead idle class. None passes vacuously — the AMEND re-uses the `fieldCount >= 6` guard, the INVERT asserts the surface INVARIANT (exception=∅), the RETIREs remove now-subjectless gates from `proof:all`, and the six NEW gates each red TODAY and green only on the H.W9 fix. No gate is satisfied by a `display:none`/`!important` suppression — the specular dies because the subsystem is DELETED, the glass returns because the tier is `quiet`.

## §supersede-map (which landed W2/W3/W4 decision each item revises + why precept-consistent)

H.W9 is a NEW corrective wave that SUPERSEDES specific landed W2/W3/W4 decisions. Editing the landed wave docs' §Goal would rewrite the record of what was DECIDED vs what was REVISED — this map is the honest ledger. (`_PLAN.md §4`)

| Item | Landed decision SUPERSEDED | The revision | Why consistent with the precepts |
|------|----------------------------|--------------|----------------------------------|
| **F1** | **W3.S1** — collapsed per-row `[auto_1fr]` + subgrid → `flex flex-col` (label-over-value) | intra-row `[auto_1fr]` (label-left/value-right) at the ROW level, one-column STACK preserved | W3 conflated the OUTER lopsided sidebar (correctly collapsed) with the INNER row split (collapsed by accident) — F1 reverts only the inner; reconcile, don't fork; the OLD correct form returns at the right altitude (`AssetPropertiesPanel` precedent) |
| **F2** | **W4.S1/S2** — W4 owns the bezier-panel SIZING band (S1 sets the 280px full-rail canvas ceiling; S2 sizes/headers the panel — "this wave only sizes/headers the panel") yet left the panel under the pre-existing `min(50vh,480px); overflow-y:auto` detail-cap (`AnimationControlsControls.vue:331-334`) + the external top-LEFT back `<Button>` (`TimingFunctionPanel.vue:5-12`) — the standing-height outcome W4's sizing band did NOT resolve | fit the panel (no scroll); back baked into the CardHeader right | KISS — a panel should fit, not scroll; the header-right dismiss is the idiomatic detail-panel pattern; the in-panel ceiling becomes a NAMED tighter panel-context clamp UNDER W4's full-rail 280 (square law preserved — coordinated with `proof:easing-canvas-bounded`'s demoted post-fix ceiling, NOT a contradiction) |
| **F3** | **W2.S3** — specular rest 0.22 / hover 0.4 + cursor tracking; `proof:specular-calm` ceiling | REMOVE the tracked specular (collapses into F6) | a moving white-cored screen-blended radial reads dramatic at any visible opacity; lowering further = invisible-but-complex (anti-KISS); removal is the real calm |
| **F6** | **W2.S2-COMPOSITE** — the "one composite exception" (`{bezier}`) | REMOVE the tracked specular entirely; cartoon + quiet glass is the calm material | binary choice (every panel or none); the composite was a CONCESSION the user retracted; honoring the latest authoritative direction (spine); no-legacy/DRY net-deletion; the W2 specular gates retire/invert to a STRONGER system property; the D2 cartoon chronic stays closed via the inverted `proof:no-orphan-specular` (exception=∅) — a chronic exits via a SYSTEM-property gate |
| **F8** | **W2.S1** — `surface="cartoon"` implicitly took the `resting` (0.65 α) tier | keep `surface="cartoon"`, add `tier="quiet"` (0.50 α) broadly | glass+cartoon ALREADY coexist (cartoon = decoration over a glass tier); `quiet` restores the EXACT pre-cartoon glass plate the user remembers — isomorphic glass read + named cartoon delta; MEASURE-FIRST; the S2-COMPOSITE already proved coexistence feasible |
| **F7** | (no wrong decision) **STRUCTURAL TENSION**: W3's load-bearing `overflow:hidden` × W2's left-casting shadow | symmetric `.controls-content` left clearance (keep the clip) | reconcile, don't fork — keep the load-bearing rail-collapse clip, give the shadow room inside; no workaround; the crisp Memphis stamp is intended (the slice is the defect) |
| **F4** | (new — no landed decision) the ppmycota menu garnished the existing SVG with an emoji `<p>` | drop the emoji; lead with the existing pp logo SVG | KISS — the brand mark is the identity; no asset hunt (the SVG already renders + is wired) |
| **F5** | (new — no landed decision) only the `DarkModeToggle` icon flips the theme | row-level `@click="toggleDark()"` + passive icon | KISS / discoverability; mirrors the ppmycota row precedent; idiomatic glass-ui `passive` prop; demo-local, no glass-ui change |
| **F9** | (restoration) a D-era refactor dropped the idle-fade CSS while keeping the `usePaneHover` plumbing → dead class | re-author the rest-dim via `useIdle(10_000)` consuming the live plumbing | "this used to be extant"; dogfood the installed primitive (`@vueuse/core useIdle`, inv ζ); compositor-thread opacity; NAMED a11y improvement (`:focus-within` + PRM guard) over the historical form |

## §glass-ui HANDOFFs (inv-16: consumed PUBLISHED, sibling = HANDOFF, born-RED kf gate)

Two glass-ui-owned asks surface from H.W9 — each PAIRED with a born-RED kf gate per the chronic-closure discipline (a HANDOFF is never a bare tag; NOT kf-authored). (`_PLAN.md §5`)

1. **F1 — `LabeledField orientation="horizontal"`** (the durable home for label-left/value-right). glass-ui owns `.labeled-field`'s display (`utilities.css:62`, block flow). The first-class fix is a prop toggling `.labeled-field` to `display:grid; grid-template-columns:auto 1fr; align-items:center` (error region `grid-column:1/-1`) — every `Labeled*` consumer + EditorHeader inherits label-left by ONE prop, not each re-authoring a grid. Vehicle: a published bump (installed 3.4.0, source 3.6.0). kf-side born-RED pairing: the AMENDED `proof:single-column-pack` label-left clause (greens on the demo-side wrapper TODAY via path B, durably on the prop). Same class of gap as the W3-BOOKed label-action slot.
2. **F8 — (OPTIONAL) a `surface="cartoon" tier="quiet"` preset / alias** if the ~14 explicit `tier="quiet"` props read as repetitive. NOT required (the demo-side prop is born-GREEN); BOOK only if the demo wants a single named "glass+cartoon panel" register. kf-side born-RED pairing: `proof:glass-and-cartoon`.

**NOT a HANDOFF (kept demo-local, idiomatic today):** F2 (header bake — pure demo), F3/F6 (net deletion — demo), F4 (markup — demo), F5 (`passive` prop + row `@click` — demo consumes glass-ui's existing `passive`), F7 (padding clearance — demo), F9 (`useIdle` wire + CSS — demo).

## §USER-DECISION fork — F6 REMOVE vs CONSISTENT (the ONE genuine fork; default REMOVE)

R3 recommends REMOVE decisively, and the user leaned that way ("perhaps we just remove it?"), so this wave **adopts REMOVE as the default** (it collapses F3+F6+F8 into one coherent calm register and is net-deletion — S1 above). The user's "consistent OR remove" phrasing leaves a fork:

- **DEFAULT (adopted) — REMOVE.** Delete the tracked specular subsystem entirely (S1b). The W2 specular gates `proof:cartoon-specular-coexist` + `proof:specular-calm` RETIRE; `proof:no-orphan-specular` INVERTS to exception=∅.
- **ALTERNATIVE (only if the user rejects REMOVE) — CONSISTENT.** A glass-ui HANDOFF to emit `glass-specular-track` on ALL panel Cards (the prop API is cartoon-XOR-specular today, `CardFooter:37`) + `useSpecularPointer` per panel + rest dialed to ~0.12–0.15; then `proof:specular-calm`'s ceiling DROPS and `proof:no-orphan-specular` is RE-SCOPED to require the track everywhere. NOT recommended — more moving parts, the per-pane `pointermove` listener tax across ~14 surfaces, fights F3.

**This fork needs the user's call ONLY if they reject REMOVE.** No other item needs a user decision — R1/R2/R3 are decisive on F1, F2, F4, F5, F7, F8, F9. (F2's in-panel canvas ceiling magnitude and F9's exact idle-opacity/threshold are tuning values, set MEASURE-FIRST during impl, not user forks.) (`_PLAN.md §6`)

## §sequencing (the convergent landing within H.W9)

1. **F8 + F3 + F6 TOGETHER (the register) — S1.** `tier="quiet"` on the panel Cards + DELETE the specular subsystem. The calm glass+cartoon register lands as ONE move (either half alone leaves the demo incoherent — opaque-but-specular or glassy-but-still-dramatic).
2. **F7 (un-clip) — S2.** Independent; the `.controls-content` left clearance. Lands cleanly with S1 (the cartoon shadow is now broad-register; the clearance protects it everywhere).
3. **F1 (rows) — S3.** The intra-row `[auto_1fr]`; the `proof:single-column-pack` amendment.
4. **F2 (bezier panel) — S4.** Fit + header bake; coordinates with H.W4's `proof:easing-canvas-bounded`.
5. **F4 + F5 (dock menu) — S5.** `App.vue` markup + wiring; independent.
6. **F9 (idle-fade) — S6.** The `useIdle` wire + CSS; independent.

**All BEFORE H.W8's GOLDEN `proof:visual-lock` baseline capture** (so the calm register / one-column row / un-clipped shadow are what the named-region matrix locks). The born-RED clauses (F2/F4/F5/F7/F8/F9 new gates; the F1 amendment; the F6 invert/retire) red TODAY on `tranche-h-impl`, green on the H.W9 fixes — every gate BITES and cites a `file:line` / live anchor.

## §Design decisions (trade-offs RESOLVED)

- **F3+F6+F8 collapse into ONE register move — RESOLVED (the headline).** glass and cartoon already coexist (`cartoon-surface` decorates a `glass-${tier}`, `cards.css:22-48`), so F8 is a tier flip (`resting`→`quiet`, 0.65→0.50 α), and F3/F6 is a deletion (the lone composite). Doing them apart leaves the demo incoherent; they MUST land together. (`_PLAN.md §1 headline`)
- **REMOVE the tracked specular, NOT a lower ceiling — RESOLVED (default; the §USER-DECISION fork is the escape).** A moving white-cored screen-blended radial reads dramatic at ANY visible opacity; lowering further = invisible-but-complex (anti-KISS); REMOVE is the strongest calm + the coherent binary (every panel or none), and the net-deletion no-legacy dividend (delete the class, the `::before` projection, the wire, the file). The CONSISTENT alternative is documented for the user's pivot only.
- **F1 reverts ONLY the inner row split — RESOLVED.** The one-column-pack invariant (W3 D1) is PRESERVED; an intra-row `[auto_1fr]` keeps ONE left edge per row. `AssetPropertiesPanel.vue:6` is the in-tree precedent; the durable home is the glass-ui `LabeledField orientation` HANDOFF.
- **F2's in-panel canvas ceiling is a NAMED panel-context clamp, NOT a W4 contradiction — RESOLVED.** W4's `proof:easing-canvas-bounded` asserts the full-rail 280 ceiling + the square law (`width===height`); the bezier panel takes a TIGHTER ceiling in its detail-panel context, and the square law holds.
- **F7 keeps the load-bearing clip — RESOLVED.** Do NOT remove `ControlsPaneWrapper.vue:182` `overflow:hidden` (it is load-bearing for the rail-track collapse); give the shadow room INSIDE via symmetric `.controls-content` left clearance (the base fix); `overflow-clip-margin` is a PE layer only (limited availability + disables scrolling).
- **F9 dogfoods `@vueuse/core useIdle` with a NAMED a11y improvement — RESOLVED.** `useIdle(10_000)` (no hand-rolled `setTimeout`, inv ζ) + the `:focus-within` override (absent in the historical form) + the MANDATORY PRM guard; compositor-thread opacity; `usePaneHover`'s 2s linger stays the immediate hover tracker.
- **This wave SUPERSEDES, it does not REWRITE — RESOLVED.** H.W9 is a NEW corrective wave AFTER W2/W3/W4/W6, BEFORE H.W8's golden baseline; the §supersede-map is the honest ledger of which landed W2/W3/W4 decision each item revises + why precept-consistent. The chronic-closure discipline is in ACTION — the user caught a re-paper before the golden baseline locked it; F6's invert keeps the D2 cartoon chronic closed via a STRONGER system property (`proof:no-orphan-specular` exception=∅).
