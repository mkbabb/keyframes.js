# H.W10 — SCENE NORMALIZATION + EXPRESSIVE ICONS + the STAGE LAYOUT-PRIMITIVE (the consolidated user-feedback fold · the easing/spring scenes adopt the standard controls component · the colorful icon reversal · G1–G8)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (CORRECTIVE — the second user-feedback refinement round, observed live on `tranche-h-impl` AFTER W5 landed; net-deletion-leaning on the scene side (delete the bespoke `ribbonContent`, the duplicate stage `EasingCurveCanvas`, the per-scene `dock-inset`, ~2 container levels per sidebar field), restorative+additive on the icon side (re-instantiate the 4 originals 1:1, author 3 new); pure demo CSS/markup/Vue — NO engine source (it CONSUMES public `NumericAnimation`/`SmoothProgress`/`AnimationVisualizer` for the inv ζ dogfood), NO glass-ui patch in kf — inv-16, NO dist build) · **Scope:** the 4 ORIGINAL icons RE-INSTANTIATED 1:1 (cube/amiga/square `-icon-sm.png` + the original colorful `easing-icon-sm.svg`, restored verbatim from `084feb9` — the W5 monochrome stroke geometry DISCARDED for these four; raster→SVG only if 1:1-faithful) + 3 NEW colorful SVGs for the primitives that LACKED one (`spring`/`sequence`/`motion-path`, drawn to MATCH the re-instantiated four); the W5 `SceneDescriptor.icon` + `vite-svg-loader` `?component` substrate KEPT — `demo/app/scenes.ts:8-14,40,88-141`, `demo/@/components/custom/dock/ChromeDock.vue:172,195,211`, `vite.config.ts:190-205` `convertColors:false`) + the easing/spring scenes NORMALIZED onto the standard controls component (delete the bespoke `ribbonContent` — `EasingScene.vue:56-90,108` / `SpringScene.vue:95-…`; surface the easing animation into the group so `AnimationControlsControls.vue:163-177` mounts the standard `PlaybackRibbon.vue:10-60`) + the easing `singular` stage becomes ONE large engine-driven ball (delete the duplicate `EasingCurveCanvas` at `EasingTarget.vue:47-59`; the editable curve lives in the sidebar only) + the easing/spring sidebars FLATTENED onto the standard `Card surface="cartoon" tier="quiet"` + `Labeled*` rows (delete the ×3 inner `<Card>`/ad-hoc grids + `text-admin-label`/`size="sm"` rung — `EasingSidebar.vue:2-97`, `SpringSidebar.vue`) + the `[stage]`-track dock-safe containment PRIMITIVE (reserve the dock bands on the grid rows via the EXISTING `--work-area-*-offset`/`--dock-band-reserve` tokens — `AnimationControlsGroup.vue:336-341,360-390`; delete the per-scene `dock-inset` — `design-idioms.css:498-500`, `EasingTarget.vue:2`/`SpringScene.vue:2`/`SpringTarget.vue:2`/`StartingStyleTarget.vue:3`) + the easing/spring stage drops its bare-class `glass-resting cartoon-surface` card → full-bleed like cube/amiga (`EasingTarget.vue:4`, `SpringTarget.vue:4`, `StartingStyleTarget.vue:9`, `SpringScene.vue:8`) + the gate set (REVISE `proof:scene-icons`; NEW `proof:scene-card-rounded`/`proof:scene-uses-standard-ribbon`/`proof:easing-stage-is-ball`/`proof:easing-sidebar-normalized`; NEW/AMEND `proof:stage-within-docks` over `proof:stage-not-clipped`) · **DAG-deps:** depends on **H.W1** (the FSM resting — every browser gate settle-gates on it; the easing/spring scenes route through the machine via `scenePlayback`), **H.W3** (the `rail·stage·rail` grid + the `[stage]` track G8 reconciles + the row shape G6 inherits), **H.W5** (the `SceneDescriptor.icon` + `vite-svg-loader` substrate G1 reuses; the stage-curve-promote G4 reverses; the monochrome icon family G1 replaces — re-instantiating the originals + 3 new), **H.W9** (the normalized rows G6 consumes + the `tier="quiet"` register G6/G2 inherit + the bezier-panel-fit F2 G4 reconciles with). Sequenced **AFTER H.W9, BEFORE H.W8's GOLDEN `proof:visual-lock` baseline capture** — the colorful icons, the normalized easing/spring scenes, and the full-bleed contained stage are the FINAL rendered state the named-region matrix locks (the convergent order: **H.W9 → H.W10 → H.W8 golden capture**). NO glass-ui patch in kf (inv-16), NO dist build.

## §Provenance (the folded lanes)

This wave is the SYNTHESIS lane of `audit/feedback/g-_PLAN.md` (the COMPLETE, authoritative H.W10 plan — read in full), grounded on the feedback triumvirate's three research lanes:

- `audit/feedback/g-r1-source-rootcause.md` — the `file:line` root-cause of each of the user's eight live-observed feedback items G1–G8 on the CURRENT committed source (HEAD `db90cbb`); the through-line verdict (the easing/stage-card scene family DIVERGED from the standard component + the rail·stage·rail layout); the standard-component spine (`PlaybackRibbon`/`AnimationControlsControls`); the gate ledger.
- `audit/feedback/g-r2-git-archaeology.md` — the "previous correct" forms: the killed COLORFUL Playwright-screenshot PNGs (`74abd2b` — the magenta/yellow/red cube, the red-white checker amiga, the periwinkle "heyyyy" square) + the original VIOLET easing vector (`db90cbb^:assets/icons/easing-icon-sm.svg`, stroked `hsl(248,88%,71%)`); the easing scene's born-bespoke lineage (`f1d4fe6`); NO prior full-bleed easing form exists (the precedent to follow is the full-bleed cube/amiga siblings).
- `audit/feedback/g-r3-reconcile.md` — the idiomatic + modern-web reconcile: the `PlaybackRibbon` reuse path; the fixed-vivid-palette icon idiom (reusing the demo's `--rainbow-*`/`--accent-*`/`--color-progress` tokens); the engine-ball stage (inv ζ); the `[stage]`-track containment primitive; the H.W9 composition seams.
- `audit/feedback/g-_PLAN.md` (the BINDING synthesis — read in full) — §0 the one-wave structure decision (compose with, do not merge into, H.W9), §1 the cross-cutting gestalt (how the 8 collapse), §2 the per-item scope clauses, §3 the gate ledger, §4 the supersede-map, §5 the H.W9 reconciliation, §6 the USER-DECISION forks, §7 the convergent sequencing, §8 the anchor index.

Synthesis home: `audit/feedback/g-_PLAN.md` (entire). This wave SUPERSEDES specific landed W5/W3/W2 decisions (and the H.W9 pending spec carries a NAMED amendment) — the §supersede-map below is the honest ledger.

## §The state, verified (file:line / live anchors from the lanes)

- **THE THROUGH-LINE — G2/G3/G4/G5/G6/G7/G8 are ONE root cause wearing six faces (R1, the gestalt verdict).** The easing scene (and the stage-card scene family — spring, starting-style) DIVERGED from the standard cube/amiga controls component + the rail·stage·rail layout. Born bespoke at `f1d4fe6`, the easing scene forks the entire standard sidebar chain (`ControlsPaneWrapper → AnimationControls → AnimationControlsControls + PlaybackRibbon`), hand-rolls a bespoke `EasingSidebar`/`EasingTarget`, hand-rolls a bespoke `ribbonContent` (Play + Reset, not the standard Play/Reverse ribbon), wraps its stage in a bare-class `glass-resting cartoon-surface` `<div>` (which carries NO `rounded-card` — the literal G2/G4 square-corner defect), and offsets it with a `dock-inset` that pads only the BOTTOM band (the G8 top-clip). **NORMALIZATION onto the standard component + a real `[stage]`-track containment primitive is the gestalt fix — REUSE, do not fork a second sidebar/ribbon (KISS·DRY, G3/G6 explicit).** G1 is a SEPARATE axis: a W5 course-correction to recover the killed colorful icons as scalable vector.
- **G1 — the icon family is MONOCHROME `stroke="currentColor"` today (verified live).** All 7 assets are `fill="none" stroke="currentColor"` (e.g. `assets/icons/easing.svg:1` `stroke="currentColor"` + the endpoint `<circle fill="currentColor" opacity="0.4">`; `spring.svg` the same). W5 (`db90cbb`) cured a REAL defect (D8 — the theme-blind `<img :src>` raster registry) by moving to a per-descriptor inline-`<svg>` family — but threw the COLOR out with the bathwater. The W5 commit body ITSELF flags this: *"the inline-SVG icon STYLE (monochrome currentColor) is superseded by H.W10 (the user's G1 feedback — the expressive/colorful icons were correct); the SceneDescriptor.icon family + vite-svg-loader infrastructure this wave built is the substrate H.W10 refines."* The substrate to KEEP: `scenes.ts:8-14,40,88-141` (`?component` imports + `SceneDescriptor.icon`), `ChromeDock.vue:172,195,211` (`<component :is="scene.icon">`), `vite.config.ts:190-205` (svgLoader `convertColors:false` + `removeViewBox:false` — faithfully preserves any baked hue/`var()` through the `?component` seam). The demo's `--rainbow-*` palette is theme-validated + in-tree: `design-idioms.css:46-58` (red/orange/yellow/green/blue/violet/cyan), `:339-353` (the `#rainbow-gradient` stops), `--color-progress` (`:374-377`). (R1/R2)
- **G2 — `cartoon-surface` carries NO `border-radius` by design (R1).** `cartoon-surface` is a glass-ui DECORATION-ONLY `@utility` (`@mkbabb/glass-ui/dist/styles/cards.css:30-48`) — a 2px border + an offset-stamp `box-shadow` + a hover-lift, and explicitly does NOT set `border-radius` (the comment `:30-32`). The radius lives on the glass-ui `<Card>` ROOT's `rounded-card` class (`CardFooter-C390imy7.js:37`). The easing/spring stages assemble the cartoon look from RAW classes on a bare `<div>` (`glass-resting cartoon-surface`) — so they get the depth but MISS the radius. Un-rounded sites: `EasingTarget.vue:4` (the main stage card), `EasingSidebar.vue:2` (the bespoke sidebar root — square outer panel with rounded inner Cards = the visible mismatch), `SpringTarget.vue:4`, `StartingStyleTarget.vue:9`, `SpringScene.vue:8`. (R1)
- **G3/G7 — the bespoke `ribbonContent` is a PARTIAL fork of the standard ribbon (R1/R2).** `EasingScene.vue:56-90` hand-rolls a `ribbonContent` render-fn — a `grid grid-cols-2` of a `Pause/Play` `<Button class="btn-playback btn-playback-accent">` (`:63`, borrows the standard accent skin) + a `Reset` `<Button class="h-8 w-full rounded-full gap-2 text-body btn-interactive">` (`:84`, a DIFFERENT class string, a DIFFERENT verb), exposed at `:108`. NO scrubber, NO `Reverse`, NO `AnimationVisualizer` ball. The two divergent class strings → unequal box metrics under inspection (the G7 unequal-w/h). `SpringScene.vue:95-…` carries the same fork shape. The STANDARD target (`PlaybackRibbon.vue`): a `Slider` scrubber (`:10`), a `grid grid-cols-2 gap-2 w-full` (`:24`) of `Play/Pause` (`.btn-playback btn-playback-accent`, `:26`) + `Reverse` (`:34-43`, `aria-pressed`), and the `AnimationVisualizer` ball — mounted via teleport from `AnimationControlsControls.vue:163-177`. The bottom-dock rainbow play/reset/trash (`AnimationMenuBar`/`AnimationControlsGroup.vue:79-90`) is ALREADY shared by every scene — the bespoke part is ONLY the in-sidebar/ribbon Pause/Reset. (R1/R2)
- **G4 — `singular` renders a SECOND copy of the curve editor (R1/R2).** `EasingTarget.vue:146` defaults `viewMode = ref("singular")`; at `:47-59`, when `singular && isBezierEditable`, the stage renders a SECOND `EasingCurveCanvas` (`:editable="true"`) — the EXACT component the sidebar already shows at `EasingSidebar.vue:9-16`. Two copies of the curve editor on screen. For named/steps curves, `singular` shows a bare `<Slider>` (`:64-80`). W5.S4 PROMOTED the curve to the stage (`db90cbb`, +42 lines) — exactly the duplicate the user now calls useless. The "ONE large ball" already exists: the standard `AnimationVisualizer.vue` (`bg-accent-red rounded-full h-12 w-12` on a rail, driven by `SmoothProgress`/`RAFPlayback`), and the easing multi-track rows ALREADY animate a ball via `getBallX(fn,…) = fn(progress)*maxX` (`EasingTarget.vue:113,236-241`). The kit has the ball; the singular surface just doesn't use it. The W5-era audit `a-scene-square-easing.md:236-252` caught this BEFORE the user and prescribed option-b ("a real animated subject — dogfooding") — IDENTICAL to G4. (R1/R2)
- **G5/G6 — the easing sidebar uses the TIGHTEST rung + ×3 inner Cards (R1).** `EasingSidebar.vue:2` is a bare-class root `<div class="easing-editor glass-resting cartoon-surface p-3 grid gap-3">` → `<h2 text-title>` + `<EasingCurveCanvas>` + TWO inner `<Card surface="cartoon">/<CardContent>` (`:19,40`) + ad-hoc `grid grid-cols-2`/`grid gap-1`/`flex gap-2` row containers + a steps Card — depth/forks the FLAT standard `Card>CardContent>panel-stack>panel-row>panel-content` (`AnimationControlsControls.vue:3-9`) of uniform `Labeled*` rows. The rung is the TIGHTEST in the system: `text-admin-label` labels (`:43,54,79`), `text-mono-caption` values (`:23,49,59,67`), `size="sm"` sliders (`:82` → 4px track / 12px thumb), `p-2`/`p-3` padding, `gap-1`/`gap-3` — vs the standard `text-mono-small` labels, default controls, `px-4 py-3`, `gap-2`. Bespoke since `f1d4fe6`. `SpringSidebar` is the parallel fork. (R1)
- **G8 — the `[stage]` track does NOT reserve the dock bands; `dock-inset` pads BOTTOM ONLY (R1/R2).** The geometry chain: (1) the TOP `ChromeDock` (`fixed`, `ChromeDock.vue:108-109`) and the BOTTOM `AnimationMenuBar` (`fixed bottom: var(--work-area-bottom-offset)`, `AnimationMenuBar.vue:7`) each occupy a `--dock-band-reserve` band. (2) The `[stage]` track (`AnimationControlsGroup.vue:364`, `grid-template-rows: [top] auto [stage] 1fr [bottom] auto`, sized to `--work-area-max-height` `:336-341`) has NO dock inset — the `[top]` auto row collapses to 0 when the dock is `fixed` (out of flow), so `[stage] 1fr` spans nearly the full viewport UNDER both docks. (3) `dock-inset` (`design-idioms.css:498-500`) pads BOTTOM ONLY; the easing wrapper (`EasingTarget.vue:2`, `dock-inset`) has a `flex-1 min-h-0` child card (`:4`) that STRETCHES to fill — its TOP edge runs under the top dock. Spring is identical (`SpringScene.vue:2`/`SpringTarget.vue:2`). (4) cube/amiga DON'T clip because their subjects are CENTERED full-bleed in `.stage-cell` (`CubeScene.vue:2-15`, `AmigaScene.vue:6-9` — bare `canvas h-full w-full`), no card edge to clip. **Gate blind-spot:** `proof:stage-not-clipped` measures `.stage-cell` (the GRID CELL), NOT the easing CARD inside it, so the clip slips past today's gate (`scripts/proof-stage-not-clipped.mjs:198-242`). (R1/R2)

## §Goal

Normalize the new stage-card scenes (easing, spring, starting-style) onto the SAME standard controls component + rail·stage·rail layout the original cube/amiga scenes use, and recover the killed expressive/colorful icons — the gestalt the original W5 gates never checked for (REUSE). **The through-line is ONE normalization decision** (G3+G6+G7+G5 → the easing/spring scenes adopt the standard `PlaybackRibbon` + the standard `AnimationControlsControls`/`Labeled*` sidebar): reusing the standard `PlaybackRibbon` makes the buttons match cube/amiga EXACTLY (G3) AND equal-size by the `grid-cols-2` + shared `.btn-playback` skin (G7) automatically; reusing the standard sidebar component flattens the nesting (G6) AND lifts the control sizing to the standard rung (G5) automatically. Around that spine: the easing `singular` stage hosts ONE large engine-driven ball under the selected easing — the curve in MOTION, not a second copy of the editor (G4, the inv ζ dogfood); the `[stage]`-track becomes the SINGLE dock-safe containment PRIMITIVE (reserve the dock bands via the existing tokens, delete the per-scene `dock-inset`) and the easing/spring stage drops its card bg to full-bleed like cube/amiga (G8) — which dissolves G2's un-rounded card (no card to round); and the 7 monochrome scene icons recover their expressive COLOR as scalable inline SVG painting from the demo's dual-theme palette (G1, reversing the W5 monochrome decision + the `proof:scene-icons` theming clause). Net-deletion-leaning on the scene side, additive on the icon side; every fix idiomatic and gestalt — NO workaround, NO legacy beside its replacement (the bespoke `ribbonContent`/duplicate curve/`dock-inset` delete in one motion), styling ISOMORPHIC unless a NAMED delta (the easing/spring scenes become BYTE-identical to cube's transport because they ARE cube's component), MEASURE-FIRST (the control-rung parity, the dock-band reserve), NO overfit/hardcoded magic numbers (G8 is a layout PRIMITIVE whose every term resolves to an existing token, not a per-scene patch). This is the chronic-closure / gate-blindspot discipline in ACTION again: the user caught the bespoke divergence the original W5 gates never asserted REUSE for, BEFORE the H.W8 golden baseline locked it — the running demo is the truth.

## §Scope (each S = WHAT + WHY)

- **S1 — G1: RE-INSTANTIATE the original expressive icons; new ONLY for those that lacked one (reverses W5.S2 + `proof:scene-icons`).** **The binding constraint (the user's exact clarification — NOT a redesign):** *"I don't want the icons re-created. I want them re-instantiated. The only new icons should be for those that lack them. If they are to be converted to SVG, they should be done so 1-1."* So the W5 hand-authored monochrome stroke geometry is NOT the basis — it is DISCARDED for the four primitives that already had an icon; their ORIGINALS return verbatim. **The original-vs-lacked split (verified from `084feb9`, W5's parent — `git ls-tree` + the pre-W5 `sceneIcons` map):**
  - **RE-INSTANTIATE the 4 originals (1:1, verbatim — restore from git `084feb9`):** `cube` (`assets/icons/cube-icon-sm.png`), `amiga` (`amiga-icon-sm.png`), `square` (`square-icon-sm.png`) — the colorful rasters; and `easing` (`easing-icon-sm.svg` — already vector; restore its ORIGINAL colorful form, e.g. the `hsl(248,88%,71%)` violet stroke + colored endpoint, NOT W5's `currentColor` monochrome flip). These four are the icons the user calls "correct" — bring them back EXACTLY, not re-drawn. (`git checkout 084feb9 -- assets/icons/{cube,amiga,square}-icon-sm.png assets/icons/easing-icon-sm.svg`, then wire the descriptor to them.)
  - **NEW icons ONLY for the 3 that LACKED one (wore `<Home>` pre-W5): `spring`, `sequence`, `motion-path`** (`starting-style` is merged into `spring` — no separate icon). Author these three NEW, in the SAME expressive/colorful style + visual weight as the four re-instantiated originals (so the family reads coherent) — colorful, not monochrome.
- **WHAT — the raster→SVG question (1:1 ONLY).** The 3 re-instantiated rasters (`cube`/`amiga`/`square` `-sm.png`) MAY stay as their original PNG files (the most literal re-instantiation) OR be converted to SVG **1:1 FAITHFUL** — a true conversion that preserves the exact original pixels (an `<svg><image href="data:image/png;base64,…"/></svg>` embed, or a high-fidelity auto-trace verified pixel-equivalent), **never a hand-redrawn approximation**. `easing` is already SVG → re-instantiate as-is. The IMPL picks the cleaner faithful mechanism (see the gate). KEEP the W5 `SceneDescriptor.icon` + `vite-svg-loader` `?component` substrate; the descriptor accommodates the re-instantiated originals (a raster keeps an `<img>`/`url()` route OR the 1:1-SVG-embed `?component`; the gate allows the original-raster exception by name) + the 3 new inline-`<svg>` colorful files. WHY: the originals ARE the design the user approved — re-instate them, don't re-imagine them; only the genuine gaps (spring/sequence/motion-path) earn a new mark, drawn to match. (`g-_PLAN.md §2 G1`; user clarification 2026-06-08)

- **S2 — G3 + G7: the easing/spring playback buttons REUSE the standard `PlaybackRibbon` (revises the born-bespoke fork).** WHAT (path 1 PREFERRED — the gestalt close): the easing demo ALREADY owns a `CSSKeyframesAnimation` + `NumericAnimation` + `AnimationGroup` + a `scenePlayback` adapter (`useEasingDemo.ts:12-15`, exposed at `EasingScene.vue:105`) — SURFACE the easing ball-traversal animation into the group under a `selectedAnimation`, so `AnimationControlsControls` renders the SAME `PlaybackRibbon` (Play/Reverse + scrub + visualizer) it renders for cube — the transport becomes BYTE-identical to cube's because it IS cube's component. DELETE the hand-rolled PLAYBACK transport from `ribbonContent` (the Play/Pause + Reset cells — no legacy beside its replacement). Pairs with S3 (the surfaced animation's visualizer IS the stage ball — one transport, one ball, all engine). **`ribbonContent` is NOT itself the defect (harden):** it is the STANDARD scene-extension slot (`RibbonBar.vue:96-101` `<slot :selected-control>`) that cube ITSELF uses for DOMAIN extras (`CubeScene.vue:157` — a Matrix Reset + a Lock/fixed toggle, ALONGSIDE the standard `PlaybackRibbon`, not replacing it). The defect is that easing/spring put their PRIMARY playback transport INSIDE `ribbonContent` instead of surfacing an animation so the standard ribbon mounts. So what deletes is the PLAYBACK-transport portion; spring's legitimate DOMAIN verbs (Reveal/Dismiss for the discrete sub-view, Re-seat — `SpringScene.vue:104-165`) MAY REMAIN as `ribbonContent` domain extras (the cube model — domain controls beside the standard transport). **FLOOR** (if surfacing slips the wave): consume `PlaybackRibbon` directly from `ribbonContent`, OR at minimum put BOTH transport buttons on the shared `.btn-playback` skin (`playback-button.css` is already a shared non-scoped partial — the intended reuse seam). **Reverse vs Reset:** the standard ribbon is Play/**Reverse**; the fork is Play/**Reset**. The bottom-dock menubar ALREADY owns Reset/Clear for every scene (`AnimationControlsGroup.vue:79-90`), so the ribbon Reset is DUPLICATIVE — normalize to Play/Reverse in the ribbon (matching cube/amiga) and let the dock own Reset (the easing demo's `reset()` stays wired to the dock, not a second ribbon button). The same normalization extends to `SpringScene` (surface its spring animation → standard ribbon transport; keep Reveal/Dismiss/Re-seat as domain extras). WHY: "Why are these buttons different, and do not match the controls panel of the standard cube/amiga/animation controls? They should match EXACTLY, and RE-USE that component." G7 ("same width + height") DISSOLVES into G3 — the standard ribbon's `grid-cols-2` + shared `.btn-playback` skin makes the buttons equal-size by the LAYOUT IDIOM, NOT a per-button magic number. KISS·DRY — REUSE the standard component, do not re-skin a fork. (`g-_PLAN.md §2 G3/G7`)

- **S3 — G4: the easing `singular` stage = ONE large engine-driven ball (revises W5.S4).** WHAT: replace the `singular` stage (currently a SECOND copy of the curve editor at `EasingTarget.vue:47-59`) with ONE large animated ball traversing under the selected easing — the curve in MOTION. **Path 1 PREFERRED (true dogfood):** drive the singular ball off a REAL engine animation under the selected easing — the ball's `translateX` is the engine's `effectiveT` mapped through the selected timing function (the easing function BECOMES the animation's timing function and the ENGINE samples it — the strongest inv-ζ statement, reusing the same `AnimationVisualizer`/`NumericAnimation` primitives cube/amiga use). Pairs with S2 path 1: the easing animation in the group → `PlaybackRibbon` mounts AND the stage ball IS the visualizer. **FLOOR:** keep `demo.progress` as the linear time axis and position one large `.progress-ball` via `fn(progress)*maxX` at hero size (the existing `getBallX` math). DELETE the duplicate stage `EasingCurveCanvas` (`:47-59`); the editable curve lives in the SIDEBAR only. The view dropdown: `singular` → the big ball; the comparison views (a family / All) MAY stay (genuinely additive — many curves at once), or reduce to Singular(ball) + All for max KISS (lead's MEASURE-FIRST call). WHY: the "Singular" view-mode is "useless and duplicative" — it re-draws the SAME bezier the sidebar already shows. "Singular should NOT be a duplicated bezier view — it should be ONE large ball." The curve EDITOR has one home (the sidebar); the STAGE shows the RESULT (the curve in motion) — de-duplication + the inv ζ dogfood; the W5-era audit prescribed this exact option-b. (`g-_PLAN.md §2 G4`)

- **S4 — G6 + G5: FLATTEN the easing/spring sidebars onto the standard NORMALIZED component (revises the W5 easing layout; NAMED H.W9 S1 amendment).** WHAT: replace the bespoke `EasingSidebar` nested assembly (bare-class root + ×3 inner `<Card>` + ad-hoc grids) with ONE `Card surface="cartoon" tier="quiet"` (the H.W9 register) carrying `LabeledInput`/`LabeledSelect` label-left rows (the H.W9 F1 row idiom — see §H.W9-reconciliation) for css-value / steps / duration — DELETE the per-field sub-Cards (folding their fields into the parent Card; ~2 container levels per field gone). The editable bezier curve (`EasingCurveCanvas`) stays as the lone HERO element in the sidebar (the H.W9 F2 bezier-panel idiom is the precedent — title + tight canvas); the duplicate STAGE curve dies (S3). G5 ("controls LARGER") is CLOSED by G6 automatically — adopting the standard `Labeled*` rows + default control sizes (drop `size="sm"`, drop `text-admin-label`) lifts the controls to the standard scale by construction (DRY: same components → same sizes; NO per-control size override). G2's sidebar square-corner closes here too: the standard `Card` COMPONENT inherits the `rounded-card` token by construction (never a hand-classed `<div>` + ad-hoc `rounded-*`). The same normalization extends to `SpringSidebar` (the parallel fork). WHY: "There are too many inner-containers here — FLATTEN this — match more of the normal controls sidebar — this should use a NORMALIZED COMPONENT." KISS·DRY — REUSE the standard sidebar, do not fork a second one; flatten by adopting the standard row primitive; the control sizing + radius are inherited, not re-authored. (`g-_PLAN.md §2 G5/G6`)

- **S5 — G8 + G2: the `[stage]`-track dock-safe containment PRIMITIVE + the full-bleed stage (revises the W3 `[stage]`-track form; default = land BOTH per FORK A).** WHAT, at TWO altitudes: **(1) the binding PRIMITIVE (no magic numbers)** — make the `.stage-cell` / `[stage]` track the SINGLE dock-safe containment envelope for ALL scenes: promote the dock-band reserve onto the `[top]`/`[bottom]` grid rows (or onto a `--stage-*-inset` on `.stage-cell`) DERIVED from the EXISTING `--dock-band-reserve` / `--work-area-top-offset` / `--work-area-bottom-offset` tokens (`style.css:108-131`, `AnimationControlsGroup.vue:336-341,360-390`), then DELETE `dock-inset` from `EasingTarget.vue:2` + `SpringScene.vue:2`/`SpringTarget.vue:2`/`StartingStyleTarget.vue:3` — the band is reserved by the grid, once, for every scene (DRY; no per-scene class; ZERO hardcoded numbers — every term resolves to an existing token). **(2) the FULL-BLEED surface consequence** — the easing/spring stage drops its `glass-resting cartoon-surface` card → a transparent full-bleed subject in `.stage-cell` (`h-full w-full`, no card, the ball floats like the cube floats — `EasingTarget.vue:4`/`SpringTarget.vue:4`/`StartingStyleTarget.vue:9`/`SpringScene.vue:8`); this matches "how cube/amiga works" (the user's preferred lean), dissolves G2/G4's "main area card not rounded" (no card to round), and composes with S3 (the ball is the full-bleed subject) + S4 (the controls moved to the standard sidebar, so the stage no longer needs to BE a panel). The `max-w-3xl mx-auto` MAY stay as an optical max-measure for the comparison LIST only; the singular ball uses the full width. WHY: "The easing area CLIPS INTO THE DOCKS — reconcile this cleanly … contain the card between the two docks … OR remove the card bg to better match cube/amiga … a proper LAYOUT-LEVEL PRIMITIVE change." Land BOTH at their right altitude: (1) is the charter's "layout-PRIMITIVE, not a patch" (no future scene can clip the docks); (2) is the easing/spring scene CONSUMING it idiomatically (full-bleed like its siblings). modern-web grounding (R3): the affixed-dock safe-area idiom = reserve via named rows + `env(safe-area-inset-*)` (already in `--dock-band-reserve`), `dvh`/`100%` for dock-aware containers (never `100vw`), NOT a per-component padding patch. (`g-_PLAN.md §2 G8/G2 · §6 FORK A`)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

The 6 H.W10 gates (5 NEW + 1 REVISE; `proof:scene-uses-standard-ribbon` carries the G7 equal-dims clause and `proof:easing-sidebar-normalized` carries the G5 sizing clause, so the 8 items map to 6 gates), each BITES + cites a `file:line`/live anchor, each born-RED on `tranche-h-impl` today. Gate-authoring home: per `H.W8.md §Scope` the gate-regime wave OWNS gate authoring; H.W10's NEW born-RED clauses are wired into the `proof:*` set + `proof:all`, and the H.W8 `proof:visual-lock` named-region matrix captures the H.W10 colorful-icon / normalized-easing / full-bleed-stage GOLDEN state AFTER H.W10 lands. Every browser gate settle-gates on the H.W1 FSM resting.

| Gate | Item(s) | Disposition | BITE (born-RED today → green on fix) |
|------|---------|-------------|--------------------------------------|
| `proof:scene-icons` | G1 | **REVISE** (G1-SHAPE invert: drop the no-baked-hue clause → assert expressive color; G4-THEMING replace: drop "stroke==host color"/dark≠light → assert colorful-carries-own-identity; KEEP coverage/favicon-404; RELAX no-raster to allow the re-instantiated originals by name) | (1) RE-INSTANTIATION FAITHFULNESS — the 4 originals (`cube`/`amiga`/`square`/`easing`) byte-match (or, for a 1:1 raster→SVG embed, pixel-match) their `084feb9` source (re-instantiated, NOT redrawn — a hand-authored monochrome-derived stroke FAILS). (2) MONOCHROME INVERSION (non-vacuous) — every descriptor icon is COLORFUL: a `stroke="currentColor"`-ONLY icon now FAILS (the very thing the user rejected); each of the 3 NEW icons (`spring`/`sequence`/`motion-path`) resolves ≥1 colored stroke/fill (a `var(--rainbow*/accent*/progress)` token or a baked vivid hue). (3) COVERAGE — all non-home descriptors carry an icon; (4) no-raster RELAXED — `assets/icons/` permits the enumerated re-instantiated original rasters (`{cube,amiga,square}-icon-sm.png`) + the favicon, else SVG. The monochrome family TODAY reds (1)+(2); greens only on the re-instantiation + the 3 colorful new. **EXECUTABLE-ASSERTION anchors the REVISE MUST flip (not the doc-comment `:17-23`/`:40-57`):** the SHAPE invert rewrites the baked-color REJECTION at `proof-scene-icons.mjs:226-238` (`bakedAttr`/`bakedGlobal` today FAIL a colored icon — must become a colored-or-tokened REQUIREMENT, `currentColor`-only now the failing case) + relax the `fill="none"`/`stroke="currentColor"` shape probes `:218-225`; the THEMING replace rewrites the equality clauses `:540-569` (today REQUIRE host-color-tracking — must become "carries its own color"); KEEP the `<img>`-vs-inline-`<svg>` structural bite `:512-538` for the 3 NEW SVGs (the D8 theme-blind defense) while ADDING the re-instantiated-original-raster allow-list + the faithfulness byte/pixel check. |
| `proof:scene-card-rounded` | G2 | **NEW** (static + computed) | reds on `EasingTarget.vue:4`, `EasingSidebar.vue:2`, `SpringTarget.vue:4`, `StartingStyleTarget.vue:9`, `SpringScene.vue:8` (computed `border-radius: 0` — bare `cartoon-surface`, no `rounded-card`); greens on the full-bleed stage (no card to round) + the Card-component sidebar (the `rounded-card` radius token resolves non-zero). Every scene surface carrying `cartoon-surface`/`surface="cartoon"` resolves a non-zero computed `border-radius` OR is the full-bleed bg-less stage. |
| `proof:scene-uses-standard-ribbon` | G3 + G7 | **NEW** | reds on `EasingScene.vue:56-90` / `SpringScene.vue:95-183` (no `PlaybackRibbon`, no scrubber `Slider`, no `AnimationVisualizer`, the PRIMARY Play/Pause transport hand-rolled INSIDE `ribbonContent` as Play/Reset not the standard Play/Reverse, divergent class strings → unequal box metrics); greens when the scene's PRIMARY transport IS the standard `PlaybackRibbon` (surfaced via the group): a scrubber `Slider` IS present, the two transport cells are an EQUAL `grid-cols-2` of `.btn-playback`-skinned Play/Reverse buttons with equal `getBoundingClientRect()` width AND height (within tolerance — the G7 equal-dims clause), an `AnimationVisualizer` ball IS present, and the hand-rolled Play/Pause+Reset transport NO LONGER lives in `ribbonContent`. **NOT over-constrained (harden):** `ribbonContent` is the STANDARD scene-extension slot (`RibbonBar.vue:96-101` `<slot :selected-control>`) that cube ITSELF uses for DOMAIN extras (`CubeScene.vue:157` Matrix Reset + Lock toggle), so the gate does NOT forbid `ribbonContent` per se — it forbids the PRIMARY playback transport living there instead of in the standard `PlaybackRibbon`. Spring's legitimate domain verbs (Reveal/Dismiss for the discrete sub-view, Re-seat — `SpringScene.vue:104-165`) MAY remain as `ribbonContent` domain extras (like cube's), the same way cube keeps its Matrix controls; what must move out is the Play/Pause+Reset PLAYBACK transport. |
| `proof:easing-stage-is-ball` | G4 | **NEW** | reds on `EasingTarget.vue:47-59` (a second `EasingCurveCanvas` in the stage subtree); greens when the stage's primary element is a `.progress-ball`/`AnimationVisualizer` whose x-position TRACKS `fn(progress)` over time (sample two frames, assert the position changes) AND there is NO `EasingCurveCanvas` in the STAGE subtree (the editable canvas exists ONLY in the sidebar). |
| `proof:easing-sidebar-normalized` | G5 + G6 | **NEW** | reds on `EasingSidebar.vue` (`text-admin-label`, `text-mono-caption`, `size="sm"`, `p-2`/`gap-1`, ×3 `<Card>` + ad-hoc `grid gap-1` rows); greens on the standard rung: (a) label rung `text-mono-small` (not `text-admin-label`) + content padding `px-4 py-3` + no `size="sm"` slider (the G5 sizing clause — the Slider track height / input font-size equals the `AnimationControlsControls` value within tolerance); (b) BOUNDED nesting depth from the sidebar root to a leaf field (≤ the standard sidebar's depth — no ×3 sub-Card chain); (c) every field is a `Labeled*` row, not a hand-classed `grid gap-1` div. Covers spring too. |
| `proof:stage-within-docks` | G8 | **NEW/AMEND** (`proof:stage-not-clipped` → bite the CARD, not just `.stage-cell`) | reds today (the easing/spring `flex-1` stage card top runs under the top `ChromeDock`; `dock-inset` is present + pads bottom only — `design-idioms.css:498-500`; the gate currently measures `.stage-cell` `:198-242`, missing the card); greens when the stage SUBJECT's `getBoundingClientRect()` top is BELOW the top dock's bottom edge AND bottom is ABOVE the bottom dock's top edge (contained in the dock-safe band) at 1280/1440 + mobile, AND a STATIC clause: ZERO `dock-inset` class remains in the easing/spring scene markup (the containment comes from the grid, not a per-scene patch). |

**KEPT UNCHANGED:** `proof:scene-parity` (the survivor-set + per-mode interactivity — the easing curve drag stays a real affordance via the SIDEBAR canvas), `proof:scene-perf-budget` (cube/amiga), `proof:easing-canvas-bounded` (the editable canvas stays in the sidebar/panel under its bounded ceiling — H.W10 moves the STAGE to a ball, not the canvas; composes, no contradiction), `proof:stage-not-clipped`'s `.stage-cell` plumbing (REUSED by the amended `proof:stage-within-docks`).

**The §Mandate bar — MUST bite, no vacuity.** Each gate asserts an EXACT live measurement or static fact the lanes captured: the monochrome `stroke="currentColor"` icons, the `border-radius: 0` bare cartoon stages, the bespoke `ribbonContent` (no `PlaybackRibbon`/scrubber/visualizer + unequal buttons + a Reset verb), the second `EasingCurveCanvas` in the stage, the `text-admin-label`/`size="sm"` tightness + ×3 sub-Card nesting, the `dock-inset`-bottom-only clip. None passes vacuously — the REVISE INVERTS the icon assertions (monochrome reds, colorful greens), the NEWs each red TODAY and green only on the H.W10 fix. No gate is satisfied by a `display:none`/`!important` suppression — the duplicate curve dies because the subtree is DELETED, the buttons match because they ARE the standard component, the stage is contained because the layout primitive reserves the bands.

## §Folds

- `g-r1-source-rootcause` (the per-item §G1..§G8 `file:line` root-cause on HEAD `db90cbb`; the through-line verdict — S1–S5; the standard-component spine `PlaybackRibbon`/`AnimationControlsControls` — S2/S4; the gate ledger — §Hard gate) — every clause re-verified against live source at spec-author time (the bespoke `ribbonContent` Play/Reset, the duplicate `EasingCurveCanvas`, the bare-class `glass-resting cartoon-surface` stage, the `dock-inset`, the monochrome icons, the `text-admin-label`/`size="sm"` rung, the `[stage]` track without dock reserve — all CONFIRMED present).
- `g-r2-git-archaeology` (the killed COLORFUL forms — the `74abd2b` PNGs + the `db90cbb^` violet easing SVG — S1; the easing scene's born-bespoke `f1d4fe6` lineage — S2/S4; NO prior full-bleed easing form, so the precedent is the full-bleed cube/amiga siblings — S5).
- `g-r3-reconcile` (the `PlaybackRibbon` reuse path — S2; the fixed-vivid `--rainbow-*` palette idiom — S1; the engine-ball stage / inv ζ — S3; the `[stage]`-track containment primitive + the affixed-dock safe-area modern-web idiom — S5; the H.W9 composition seams — §H.W9-reconciliation).
- `a-scene-square-easing §236-252` (the W5-era audit that caught the duplicate-curve stage BEFORE the user and prescribed option-b = "a real animated subject (a box eased by the selected curve — dogfooding)" — IDENTICAL to G4/S3; RECORD that the prescription was authored but W5.S4 chose the curve-promote instead).
- The W5 `db90cbb` commit body (the PRE-AUTHORIZED supersede — it ITSELF names H.W10 / the user's G1 feedback as the superseder of the monochrome icon STYLE, KEEPING the `SceneDescriptor.icon` + vite-svg-loader infrastructure as the substrate H.W10 refines) — S1.

## §supersede-map (which landed W5/W3/W2 decision each item revises + why precept-consistent)

H.W10 is a NEW corrective wave that SUPERSEDES specific landed W5/W3/W2 decisions (and carries one NAMED amendment to the pending H.W9 spec). Editing the landed wave docs' §Goal would rewrite the record of what was DECIDED vs what was REVISED — this map is the honest ledger. (`g-_PLAN.md §4`)

| Item | Landed decision SUPERSEDED | The revision | Why consistent with the precepts |
|------|----------------------------|--------------|----------------------------------|
| **G1** | **W5.S2** — the survivor-SVG family authored `fill="none" stroke="currentColor"` monochrome (the easing `hsl(248…)`→`currentColor` flip; the cube/amiga/square `-sm.png` originals KILLED) + the WV-W5-MED-2 "G4 is the authority, a baked hue FAILS" clause | RE-INSTANTIATE the 4 originals 1:1 (DISCARD the W5 stroke geometry for cube/amiga/square/easing — restore them verbatim from `084feb9`; raster→SVG only if 1:1-faithful) + author 3 NEW colorful SVGs for the lacking primitives (spring/sequence/motion-path) in the matching style; KEEP the `SceneDescriptor.icon` + `?component` substrate | the user's exact direction — *"re-instantiated, not re-created; new only for those that lack them; SVG conversion 1:1"*; W5 ITSELF named G1 as the superseder (the `db90cbb` commit body); the SUBSTRATE (inline-`<svg>`/raster-via-descriptor, coverage, favicon-resolve) is KEPT — the W5 monochrome ASSETS + the gate's monochrome-enforcement clauses reverse; `convertColors:false` carries the re-instantiated colorful SVGs unchanged (zero infra change) |
| **G2** | (no landed decision) the bare-class `cartoon-surface` stages miss `rounded-card` | full-bleed stage (no card to round — G8) / Card-component sidebar (inherits the radius token — G6) | REUSE the glass-ui `<Card>` primitive (one radius token), no ad-hoc `rounded-*` literal; dissolves into G6/G8 |
| **G3** | the born-bespoke `ribbonContent` fork (`f1d4fe6` lineage; W1 `256f6fe` introduced the scene wrapper) — Play/Reset, a partial copy of `PlaybackRibbon` | surface the easing/spring animation → mount the standard `PlaybackRibbon` (Play/Reverse + scrub + visualizer); delete the `h(...)` fork; Reset owned by the dock | KISS·DRY — REUSE the standard component, do not re-skin a fork; no legacy beside its replacement; Reset de-duplicated against the dock that already owns it |
| **G4** | **W5.S4** — the easing curve PROMOTED to the stage (the `:bezier-points`/`@update:bezierPoints` two-way wire, the `:47-59` duplicate-curve-on-stage) | the `singular` stage = ONE large engine-driven ball (`AnimationVisualizer`/`NumericAnimation`, inv ζ) under the selected easing; the editable curve stays in the sidebar | the EDITOR has one home (the sidebar); the STAGE shows the RESULT (the curve in motion) — de-duplication + the inv ζ dogfood; reconcile, don't fork; the W5-era audit (`a-scene-square-easing.md:236-252`) already prescribed this option-b |
| **G5** | the bespoke `EasingSidebar` `size="sm"` + `text-admin-label`/`text-mono-caption` rung (`f1d4fe6`) | adopt the standard `Labeled*` rows + default control sizes (closed by G6) | DRY — same components → same sizes; no per-control size override; REUSE the standard sizing tokens |
| **G6** | the born-bespoke `EasingSidebar`/`SpringSidebar` nested assemblies (`f1d4fe6`) — ×3 / ×3 `<Card>`-in-`<div>` + ad-hoc grids | ONE `Card surface="cartoon" tier="quiet"` per sidebar with `Labeled*` label-left rows; the editable curve the lone hero; delete the per-field sub-Cards | KISS·DRY — REUSE the standard "NORMALIZED COMPONENT" the user named; **NAMED amendment to H.W9 S1** (the FIVE inner Cards it set `tier="quiet"` on — `EasingSidebar.vue:19,40` + `SpringSidebar.vue:4,60,81` — are deleted → the H.W9 EasingSidebar+SpringSidebar site-lists shrink to one parent Card each; the `tier="quiet"` decision survives on the surviving parents, the count drops) |
| **G7** | the fork's two divergent button class strings (`EasingScene.vue:60-74` vs `:79-88`) | equal w/h via the standard ribbon's `grid-cols-2` + shared `.btn-playback` skin (closed by G3) | NO-overfit — equal-size via the layout idiom (the grid track + the one shared class), not a per-button magic number |
| **G8** | the **W3 `[stage]`-track form** (`AnimationControlsGroup.vue:360-390`, dock bands not reserved on the track; the per-scene `dock-inset` patch — bottom only) | the `[stage]` track is the SINGLE dock-safe containment envelope (reserve via the EXISTING work-area/dock tokens, delete `dock-inset`) + the easing/spring stage drops its card bg → full-bleed like cube/amiga | a LAYOUT PRIMITIVE (the charter's "layout-LEVEL primitive, not a patch"), no hardcoded magic numbers (every term resolves to an existing token); the full-bleed stage matches the sibling primitive; reconcile with the `[stage]` track, don't fork |

## §H.W9-reconciliation (COMPOSE, do not contradict — the two waves change the SAME surfaces)

H.W10 lands AFTER H.W9 and changes the SAME rendered surfaces H.W9 tunes (the easing scene, the controls sidebar, the icons, the stage). The composition seams (`g-_PLAN.md §5`):

- **H.W9 F1 (rows label-LEFT/value-RIGHT, ONE column) ⟂ H.W10 G6/S4 (flatten + normalize the easing sidebar) — SAME DIRECTION, G6 CONSUMES F1.** F1 restores the intra-row `grid-cols-[auto_1fr]` split in the STANDARD `AnimationControlsControls`/`LayerConfigPanel` rows (H.W9 S3, the `AssetPropertiesPanel.vue:6` precedent), keeping ONE column. G6 says the easing sidebar should BE that standard sidebar — so normalizing easing onto the standard component (G6) makes it INHERIT F1's row shape for free (no second authoring). H.W10 EXTENDS F1's row idiom to `EasingSidebar`/`SpringSidebar` (which F1's site-list did not cover). **State: G6's normalization is the vehicle; F1 is the row idiom the normalized sidebar carries.**

- **H.W9 S1 (`tier="quiet"` on `EasingSidebar.vue:19,40` AND `SpringSidebar.vue:4,60,81` — the FIVE inner Cards across the two bespoke sidebars) ⟂ H.W10 G6/S4 (delete all five) — NAMED AMENDMENT.** The G6 flatten REMOVES the two inner easing Cards AND the three inner spring Cards (folding their fields into one parent Card per sidebar). So the H.W9 EasingSidebar site-list SHRINKS from 2 inner Cards to 1 parent Card AND the H.W9 SpringSidebar site-list SHRINKS from 3 inner Cards to 1 parent Card. The `tier="quiet"` DECISION SURVIVES (each surviving parent Card carries it); only the container COUNT drops. **State as a named amendment to H.W9 S1's site inventory — H.W10's normalized surfaces INHERIT H.W9's quiet-cartoon register; the H.W9 S1 `EasingSidebar.vue:19,40` (2) + `SpringSidebar.vue:4,60,81` (3) entries collapse to one parent Card each.** (NOTE for the IMPL lead: since H.W10 demolishes all five of these inner Cards, the IMPL may add `tier="quiet"` directly to the surviving parents in the SAME motion rather than first applying H.W9 S1 to the five doomed Cards — DRY, no churn-then-delete.)

- **H.W9 F2 (fit the bezier PANEL + bake the back into the header right) ⟂ H.W10 G4/S3 (the easing STAGE = a ball) — DISTINCT SURFACES.** F2 fits the `TimingFunctionPanel`'s in-panel `EasingCurveCanvas` (the detail Card in the RAIL); G4 changes the easing SCENE STAGE (`EasingTarget`) from a duplicate curve to a ball. Different components, different fixes, no collision. The bezier CANVAS lives in (1) the standard controls detail panel (F2's tight in-panel ceiling) and (2) the easing SIDEBAR (the editable curve, G6). The STAGE shows the BALL (G4), NOT a bezier canvas. **State explicitly: ONE editable bezier canvas per host (panel + sidebar), ZERO on the stage — no third bezier-canvas instance.**

- **H.W9 F7 (cartoon shadow clearance inside the load-bearing CONTROLS-pane clip) ⟂ H.W10 G8/S5 (the STAGE-track containment) — COMPATIBLE, different clipping ancestors.** F7 is the `ControlsPaneWrapper` clip (the rail); G8 is the `[stage]` track (the stage). No conflict.

- **H.W9 + H.W10 ordering (the SAME pre-golden-baseline window).** Both land BEFORE the H.W8 golden capture, H.W10 AFTER H.W9. **State the sequencing: H.W9 → H.W10 → the H.W8 golden capture**, so the lock captures the FINAL (colorful-icon, normalized-easing, full-bleed-stage) state. H.W9 KEEPS `proof:stage-not-clipped` + `proof:easing-canvas-bounded` unchanged — H.W10 AMENDS `proof:stage-not-clipped` to bite the easing CARD (G8) and composes with `proof:easing-canvas-bounded` (the editable canvas stays in the sidebar/panel, the stage is the ball).

## §USER-DECISION fork (the genuine forks; both have a RECOMMENDED default the IMPL wave adopts unless the user pivots)

Two genuine forks (everything else R1/R2/R3 are decisive on); both have a RECOMMENDED default. (`g-_PLAN.md §6`)

- **FORK A — G8: contained-card-with-bg vs full-bleed-no-bg.** The user offered both ("contain the card between the docks" OR "remove the card bg to better match cube/amiga"). **RECOMMENDED default (adopted): land BOTH at their right altitude** — the `[stage]`-track containment is the binding PRIMITIVE (it fixes the clip for ALL scenes, the charter's "layout-LEVEL primitive"), AND the easing/spring stage drops its card bg to full-bleed (the surface consequence — matches cube/amiga, the user's preferred lean, dissolves G2/G4 rounding, composes with the G4 ball). The pure contained-card-with-bg alternative is documented for the user's pivot only (it keeps a card to round — G2 then needs the Card-component route — and a card edge that can still graze the bands under content growth). **This fork needs the user's call ONLY if they reject full-bleed.**

- **FORK B — G1: RESOLVED by the user's clarification (no longer a palette fork).** The user directed: *"I don't want the icons re-created. I want them re-instantiated. The only new icons should be for those that lack them. If they are to be converted to SVG, they should be done so 1-1."* So the 4 originals (cube/amiga/square/easing) are RE-INSTANTIATED VERBATIM from `084feb9` — there is NO palette choice for them (their original colors return as-is). The ONLY open tuning is the palette/style of the 3 NEW icons (spring/sequence/motion-path), drawn to MATCH the re-instantiated four (the same expressive/colorful register — token-referenced `var(--rainbow-*)` or baked vivid, IMPL's call to match the originals' weight). The raster→SVG mechanism for the 3 re-instantiated PNGs (keep-as-PNG vs 1:1-faithful-embed/trace) is an IMPL mechanism gated by the faithfulness clause, not a user fork. **No user decision pending on G1** beyond the now-binding "re-instantiate, not recreate."

No other item needs a user decision — R1/R2/R3 are decisive on G2/G3/G4/G5/G6/G7 (the component/stage normalization is the unambiguous KISS·DRY gestalt fix). The G4 stage-ball implementation (engine-true path 1 vs the `getBallX` floor path 2), the S2 ribbon path (surface-into-group path 1 vs the `PlaybackRibbon`-direct floor), and the view-dropdown reduction (keep Singular+family+All vs Singular+All) are MEASURE-FIRST/lead tuning calls set during impl, not user forks.

## §sequencing (the convergent landing within H.W10)

1. **S1 / G1 (expressive icons) — independent.** RE-INSTANTIATE the 4 originals 1:1 from `084feb9` (cube/amiga/square `-sm.png` + the original `easing-icon-sm.svg`) + author 3 NEW colorful SVGs (spring/sequence/motion-path) to match; REVISE `proof:scene-icons` (the re-instantiation faithfulness clause + the monochrome inversion + the no-raster allow-list). The standalone axis; no dependency on the scene-normalization spine.
2. **S2 / G3 + G7 (the standard ribbon) — the spine, first among the scene cluster.** Surface the easing/spring animation into the group → mount the standard `PlaybackRibbon`; delete the bespoke `ribbonContent`. Closes G3 (match) + G7 (equal-size) together.
3. **S3 / G4 (the stage ball) — pairs with S2.** The surfaced animation's visualizer IS the full-bleed stage ball; delete the duplicate stage `EasingCurveCanvas`.
4. **S4 / G6 + G5 (the normalized sidebar) — composes with H.W9 F1/S1.** Flatten `EasingSidebar`/`SpringSidebar` onto the standard `Card surface="cartoon" tier="quiet"` + `Labeled*` label-left rows (inheriting H.W9's row idiom + quiet register); the control sizing lifts (G5) automatically. Closes G2's sidebar square-corner (the Card component carries `rounded-card`).
5. **S5 / G8 + G2 (the stage layout-primitive) — the binding primitive, lands last among scenes.** The `[stage]`-track containment envelope (reserve via the existing tokens, delete `dock-inset`) + the easing/spring stage drops its card bg → full-bleed. Closes G2's stage square-corner (no card to round).

**All AFTER H.W9, BEFORE H.W8's GOLDEN `proof:visual-lock` baseline capture** (so the colorful icons / normalized easing+spring / full-bleed contained stage are what the named-region matrix locks). The born-RED clauses (the 5 NEW gates + the 1 REVISE) red TODAY on `tranche-h-impl`, green on the H.W10 fixes — every gate BITES and cites a `file:line`/live anchor.

## §Design decisions (trade-offs RESOLVED)

- **ONE new corrective wave H.W10, COMPOSE with (do not merge into) H.W9 — RESOLVED.** These eight items are course-corrections on LANDED waves (W5 icons/stage-curve-promote, W3 grid/row, W2 cartoon surface) observed AFTER the visual waves landed. H.W9's headline is the MATERIAL register (F3/F6/F8); H.W10's headline is COMPONENT + STAGE NORMALIZATION + the EXPRESSIVE ICON reversal — distinct gestalts at distinct altitudes. Two waves keeps each coherent and each supersede-map honest. The ONLY genuine H.W9 overlap is the `tier="quiet"` register the G6 sidebar inherits — handled as the NAMED H.W9 S1 amendment, NOT a merge. (`g-_PLAN.md §0`)
- **The deepest collapse is G3+G6+G7+G5 → ONE normalization decision — RESOLVED (the spine).** Reusing the standard `PlaybackRibbon` makes the buttons match cube/amiga EXACTLY (G3) AND equal-size by `grid-cols-2` + shared `.btn-playback` (G7) automatically. Reusing the standard sidebar component flattens the nesting (G6) AND lifts the control sizing (G5) automatically. G2+G8 collapse together (the full-bleed stage removes the un-rounded card). G4+G8 collapse together (the ball IS the full-bleed subject). Only G1 is a standalone axis. (`g-_PLAN.md §1`)
- **The easing stage = a ball (G4), the editor stays in the sidebar — RESOLVED.** The curve EDITOR has one home (the sidebar); the STAGE shows the RESULT (the curve in motion via the engine-driven `AnimationVisualizer`/`NumericAnimation`, inv ζ). De-duplication + the strongest inv-ζ dogfood; the W5-era audit prescribed this exact option-b BEFORE the user named it. (`g-_PLAN.md §2 G4`)
- **G8 is a LAYOUT PRIMITIVE, not a per-scene patch — RESOLVED.** The `[stage]`-track dock-band reserve derives from the EXISTING `--dock-band-reserve`/`--work-area-*-offset` tokens (ZERO hardcoded numbers); `dock-inset` deletes from every scene; the easing/spring stage goes full-bleed like cube/amiga. The charter's "layout-LEVEL primitive, not an overfit/overcomplex hardcoded item" — no future scene can clip the docks. (`g-_PLAN.md §2 G8`)
- **G1 reverses the W5 monochrome decision; the substrate survives — RESOLVED.** W5 cured the theme-blind raster (a REAL defect) but over-corrected to monochrome `currentColor`. The inline-`<svg>` on the descriptor + the vite-svg-loader `?component` seam (`convertColors:false`) is the SUBSTRATE — KEPT unchanged; only the asset COLOR + the gate's monochrome-enforcement clauses reverse. The icon palette IS the demo palette (the `--rainbow-*`/`--accent-*`/`--color-progress` tokens) — colorful AND dual-theme for free, ZERO new color decisions. W5 ITSELF pre-authorized this supersede. (`g-_PLAN.md §2 G1 · §4`)
- **This wave SUPERSEDES, it does not REWRITE — RESOLVED.** H.W10 is a NEW corrective wave AFTER H.W9, BEFORE H.W8's golden baseline; the §supersede-map is the honest ledger of which landed W5/W3/W2 decision each item revises + why precept-consistent. The chronic-closure / gate-blindspot discipline is in ACTION again — the user caught the bespoke divergence the original W5 gates never asserted REUSE for (they checked icon coverage/shape and per-mode interactivity, NEVER that the new scenes REUSE the standard controls component), BEFORE the H.W8 golden baseline locked it; W10 normalizes the new scenes onto the standard component the original gates were blind to. (`g-_PLAN.md §0/§5`)
