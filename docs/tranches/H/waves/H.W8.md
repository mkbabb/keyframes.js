# H.W8 — THE GATE-REGIME UPGRADE (Band 5 · GATE/CI · the LOAD-BEARING wave · close the blind spots so this is the LAST re-paper of these chronics)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (the gate lattice A→G locks SOURCE-SHAPE + NOT-BLANK; it has NO appearance axis, NO interaction axis, and a DRIFTED `SCENES` manifest — three structural additions plus the chronic-closure meta-gate; this is the wave that makes "CLOSED" mean "rendered + behaves," not "parsed") · **Scope:** `scripts/lib/demo-driver.mjs` (the hand-maintained 6-entry `SCENES` array `:40-59` — re-sourced from `demo/app/scenes.ts`) + a NEW `scripts/proof-visual-lock.mjs` + a committed `docs/tranches/H/audit/visual-baseline/` pixel baseline + NEW `proof:scene-parity`/`proof:dock-live` runners + extensions to the EXISTING `scripts/proof-idioms.mjs` (the layout-idiom + φ-hero locks, one-line extensions of clause-8c/clause-2 shape) and `scripts/occlusion-gate.mjs` (the mobile-architecture lock + retire the `square/mobile` allowance) + the chronic-ledger meta-gate clause + the `proof:*` set wiring; NO engine source, NO demo source (this wave authors GATES, the sibling waves author the fixes the gates police) · **DAG-deps:** **I-1 (re-source the manifest) is the prerequisite** — it makes D8/D11/mobile checkable AT ALL (the 3 new scenes are never gate-visited today); `proof:visual-lock` and `proof:scene-parity` depend on the re-sourced manifest. The PER-DEFECT gate clauses are authored HERE but BITE against the sibling waves' fixes (H.W2..H.W7) — so the visual-lock baseline is captured AFTER the fix waves land (the baseline IS the fixed render); the BORN-RED clauses (the chronic meta-gate's HANDOFF gates, the manifest stale-guard) red TODAY independent of the fix waves.

## §Provenance (the folded lanes)

- `a-gate-blindspots` **(the master deficit map)** — the two ROOT blind spots: ROOT-A (every gate is a STATIC GREP or a NARROW RUNTIME ASSERTION — ZERO pixel/visual baseline in the repo; D1/D2/D3/D4/D6/D7/D10 are all appearance/layout regressions no gate can see — `a-gate-blindspots.md:21-44`); ROOT-B (the `SCENES` manifest is STALE — `demo-driver.mjs:40-59` knows 6, the demo ships 9; `sequence`/`motion-path`/`starting-style` are NEVER navigated by any runtime gate — `a-gate-blindspots.md:46-63`). The 9 proposed instruments I-1..I-9 (`a-gate-blindspots.md:91-218`); the meta-lesson "the lattice has no APPEARANCE axis and no INTERACTION axis, and its manifest drifted" + the 3 priority additions (`a-gate-blindspots.md:220-243`).
- `a-deferred-chronic` **§3 (the chronic-closure discipline)** — the meta-gate: a chronic exits ONLY with (a) a passing SYSTEM-property gate, OR (b) a HANDOFF tag PAIRED with a born-RED kf gate; a bare tag is no longer a terminal — the P-invariant repaired to police the PRODUCT, not the column (`a-deferred-chronic.md:103-121`). The four escape mechanisms M1/M2/M3 the meta-gate targets (`a-deferred-chronic.md:68-101`); the `proof:dock-live` born-RED gate for the glass-ui dock HANDOFF (`a-deferred-chronic.md:115-116,166`); the `proof:chronic-closed` gate family (`a-deferred-chronic.md:110-115`).
- `a-changes-vs-plan` — the 31/6 source/browser gate ratio (the suite is structurally SOURCE-biased — `a-changes-vs-plan.md:141-153`); the browser gates assert NAMED PIXELS, not fidelity (`proof:demo-usability` is point-wise: 3 clauses where D1–D14 needed 30 — `a-changes-vs-plan.md:154-166`); the browser gates are DARK by default (`KF_REQUIRE_BROWSER=1` + dist build; `proof:all` never renders the demo — `a-changes-vs-plan.md:167-177`); "CLOSED / verifies no regression" asserted on un-rendered claims (the cartoon-shadow ledger row — `a-changes-vs-plan.md:178-202`); the 4 instruments H needs (`a-changes-vs-plan.md:226-267`).
- `a-precept-sweep` — `proof:precept-sweep` should bundle the spine gates (`proof:scene-fsm`, `proof:scene-identity`, `proof:no-legacy` regrep, `proof:docs-tree`) so the spine is irrefragable through H (`a-precept-sweep.md:218-223`); the `SceneDescriptor.icon` required + no-fall-through-to-generic-glyph fail-explicit-at-the-type-layer (F-03, `a-precept-sweep.md:152-170`).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §3 H.W8` (`:162-165`) + §0 the meta-lesson (the deferred-chronic spine — issue-level close M1, scope-narrowing M2, column-migration M3; "the P-invariant policed the COLUMN, not the PRODUCT" — `:36-43`) + §5 record 9 (the gate lattice at what it covers; the GAP is the missing appearance + interaction axes — `:195`) + §6 the chronic-closure discipline (`:211-213`).

## §The state, verified (file:line / live anchors from the lanes)

- **ROOT-A: ZERO pixel/visual baseline in the repo.** `proof:idioms` (`scripts/proof-idioms.mjs`) + `proof:brittleness` (`scripts/proof-brittleness.mjs`) are 100% `fs.readFileSync` + regex over `demo/` — they assert a token/selector is DEFINED and not re-forked, never that it renders the RIGHT pixels (`proof-idioms.mjs:80-181` proves `.scale-on-hover` EXISTS, not that the hover looks like a cartoon shadow vs a radial blur). The runtime gates (`occlusion-gate`, `lighthouse-gate`, `demo-smoke`, `proof:demo-usability`) probe geometry/scores/mount/3 named usability clauses — never appearance (`demo-smoke.mjs:175-211` checks "#app has children + hero text exists"). `grep -rlE 'toMatchSnapshot|toHaveScreenshot|pixelmatch|visual.?regression|baseline' scripts/ test/` returns ONLY scoring scripts — NO pixel-diff anywhere (`a-gate-blindspots.md:21-44`).
- **ROOT-B: the manifest is STALE.** `scripts/lib/demo-driver.mjs:40-59` declares `SCENES` = **6** (`home, cube, amiga, square, easing, spring`) — the single truth for `occlusion-gate`, `lighthouse-gate`, AND `capture.mjs`. The demo ships **9** (`scenes.ts:48,56,62,68,74,80,90,103,115` adds `sequence`, `motion-path`, `starting-style`). So those 3 are NEVER occlusion-checked, lighthouse-scored, or captured (re-verified at source: `demo-driver.mjs:40` `export const SCENES = [` with 6 keys; `scenes.ts` 8 non-home ids + home). `proof:demo-usability` clause 1 (`proof-demo-usability.mjs:56-86`) checks route-reachability STATICALLY only — "the gate that lists them never looks at them; the gates that look never list them" (`a-gate-blindspots.md:46-63`).
- **The browser gates are DARK by default.** The browser halves SKIP unless `KF_REQUIRE_BROWSER=1` AND `dist/gh-pages` is built (`proof-demo-usability.mjs:94-104` `skipOrFail`; the lighthouse gate is honest-WITHHELD off-CI, `E/FINAL.md:54`). `proof:all` runs the 31 source gates + vitest; the felt-quality instruments run only in CI under an env flag — the loop agents iterate against NEVER renders the demo (`a-changes-vs-plan.md:167-177`).
- **The chronic escape mechanisms (M1/M2/M3) the meta-gate must catch.** M1 (issue-level close masquerading as system-level — cartoon-shadow "CLOSED" on ONE site `CSSCodeEditor.vue:6`, never the panel register; φ-ladder "swept" but 37 raw rungs survive); M2 (scope-narrowing — "perfect mobile" → "no dock occlusion" → "KFD-TERMINATED-2"); M3 (column-migration to HANDOFF/OUT — the dock leaves the kf column while broken; a HANDOFF item is BY CONSTRUCTION never a P-invariant violation no matter how long it stays broken) (`a-deferred-chronic.md:74-101`). The P-invariant policed the COLUMN, not the PRODUCT (`a-deferred-chronic.md:34-43`).
- **The `occlusion-gate` mobile axis is known-weak and TOLERATED.** `occlusion-gate.mjs:56-60` runs `375×667` but checks only overflow/subject/dock-cover — never the mobile ARCHITECTURE; it carries a standing `PENDING_OCCLUSION` allowance for `square/mobile` (`occlusion-gate.mjs:91-94`) — proof the mobile axis is known-weak and tolerated; the gate's own stale-guard reds if a pending allowance starts passing (`occlusion-gate.mjs:347-353`) (`a-gate-blindspots.md:79,208-216`).
- **The right instrument GENUS already exists — it is UNDER-POPULATED.** `proof:demo-usability` is a BROWSER gate that catches 3 specific rendered defects (dead route, collapsed word-gap, dupe aria) — the right SHAPE, reactive and point-wise (each clause authored AFTER the user reported that exact defect in G); clause-8c (`proof-idioms.mjs:545-580`/`:560-564`) locks coupled tokens to call-sites — the extensible shape; clause 2 (`proof-demo-usability.mjs:179-237`) already reads `getComputedStyle(h1)` — the φ-hero floor is a one-line extension (`a-changes-vs-plan.md:154-166`; `a-gate-blindspots.md:125-170`).
- **ALREADY-SOTA — do NOT re-author.** The gate lattice at what it COVERS: source-shape (`proof:idioms`/`proof:brittleness`), not-blank/not-occluded/score-floor (`demo-smoke`/`occlusion-gate`/`lighthouse`/the 3 `demo-usability` clauses) — exemplary; the GAP is the missing appearance + interaction axes, NOT the existing gates (`_SYNTHESIS-gap-scorecard.md:195`; `a-gate-blindspots.md:222-243`). The `proof:idioms` clause-8c shape, the `occlusion-gate` stale-guard, the `proof:brittleness` `LISTENER_ALLOWLIST` stale guard (`proof-brittleness.mjs:75-77,422-430`) are the TEMPLATES H extends — H does NOT rewrite them.

## §Goal

Upgrade the gate REGIME so the four chronics (cartoon-shadow D2, φ-hero D7, mobile D10, dock D5) can NEVER be re-papered — making H the LAST tranche these chronics can be closed-on-paper. Three structural additions plus the chronic-closure meta-gate: **(I-1) re-source the `SCENES` manifest from `scenes.ts`** — delete the hand-maintained 6-entry array; derive `SCENES` from the single source the router already trusts, with a STALE-KEY guard (a manifest key with no `scenes.ts` id reds), so adding a scene AUTO-enrolls it in every runtime gate (this is the load-bearing fix — it makes D8/D11/mobile checkable at all). **(I-2) a `proof:visual-lock` pixel baseline** — one Playwright harness (reuse `serveDist` + the re-sourced `SCENES`) screenshots each scene × {mobile 375, desktop 1440} × {controls closed, open} to a COMMITTED named-region baseline, diff via `pixelmatch` with a measure-first tolerance — the single broadest lever (converts D1/D3/D4/D6/D7/D10 appearance defects into ONE re-runnable diff). **(I-3) the chronic-closure meta-gate** — a chronic exits ONLY with a SYSTEM-property gate OR a HANDOFF paired with a born-RED kf gate (`proof:dock-live` born-RED for the glass-ui HANDOFF); a bare HANDOFF tag with no born-RED gate REDS the ledger. Plus the appearance + interaction axes: the φ-hero size floor (I-5), motion-liveness (I-6), scene-parity (I-7), the mobile-architecture lock (I-9), the layout-idiom locks (I-3-static, one-line clause-8c extensions). The browser gates become LIT in the dev loop (not env-dark) — `proof:visual-lock` wired into the browser CI job, not skippable-silent. Every per-defect clause is authored from a RENDERED probe, never a grep; each BITES on the exact regression it forbids. NO new god-scripts — the static locks are one-line extensions of `proof:idioms`/`proof:demo-usability`; the visual-lock is the missing peer of the existing `capture.mjs`. MEASURE-FIRST the visual tolerance (3 identical runs anti-flake) before binding HARD. This is the appearance+interaction axes the lattice never had — so "CLOSED" means "rendered + behaves," not "parsed."

## §Scope

- **S1 — I-1: re-source the `SCENES` manifest from `scenes.ts` (kills ROOT-B, the prerequisite).** Delete the hand-maintained 6-entry array in `demo-driver.mjs:40-59`; derive `SCENES` from `demo/app/scenes.ts` (the single source the router already trusts). The `subjectSelector` + `dockFloatAllowed` become a small per-scene manifest KEYED BY the `scenes.ts` id, with a STALE-KEY guard (a manifest key with no `scenes.ts` id reds — mirrors the `proof:brittleness` `LISTENER_ALLOWLIST` stale guard `proof-brittleness.mjs:75-77,422-430`). Add a clause to `proof:demo-usability` (or the gate preflight): every `scenes.ts` id appears in the runtime `SCENES` manifest. **WHY:** the 3 new scenes (`sequence`/`motion-path`/`starting-style`) are never gate-visited today — this is the load-bearing fix that makes D8/D11/mobile checkable AT ALL; adding a scene to `scenes.ts` without a manifest entry must red (`a-gate-blindspots.md:98-109`).

- **S2 — I-2: the `proof:visual-lock` pixel baseline (kills the bulk of ROOT-A).** One Playwright harness (reuse `serveDist` + the S1 re-sourced `SCENES`): screenshot each scene × {mobile 375, desktop 1440} × {controls closed, open} to a COMMITTED baseline under `docs/tranches/H/audit/visual-baseline/`, diff via `pixelmatch` with a named per-region tolerance. Scope to NAMED REGIONS (controls pane, hero, ribbon, easing editor) — NOT full-page — so anti-aliasing noise does not flap it. The baseline is captured AFTER the fix waves (H.W2..H.W7) land (the baseline IS the fixed render). Wire into the browser CI job (`demo-smoke`), NOT skippable-silent. **WHY:** the single broadest lever — converts six appearance defects (D1/D3/D4/D6/D7/D10) into ONE re-runnable named-region diff; it is the missing peer of `capture.mjs` (which already produces the frames; H promotes them to a gated diff). MEASURE-FIRST the tolerance from 3 consecutive identical runs (anti-flake) before binding HARD (`a-gate-blindspots.md:111-123`).

- **S3 — I-3 (meta-gate): the chronic-closure discipline as a ledger gate.** Encode the binding rule: a chronic ledger row may exit ONLY with (a) a passing SYSTEM-property gate, OR (b) a HANDOFF tag PAIRED with a born-RED kf-side gate. A bare HANDOFF tag with no born-RED gate REDS the ledger. **The parseable substrate (so the gate is implementable, not aspirational):** the COMMITTED chronic→gate table in `H/PROGRESS.md` (the `Chronic | Prior false-close mode | H closure (the SYSTEM-property gate or paired born-RED HANDOFF)` table, mirrored in `H/FINAL.md` at H.WZ) — the canonical source is `audit/_SYNTHESIS-deferred-ledger.md §1` (CH-1..CH-4 + the `proof:*` column at `:219-223,247-249`). The gate parses each chronic row and asserts: (i) every named SYSTEM-property `proof:*` gate in the "H closure" column RESOLVES to an authored gate in the `proof:*` set (no dangling name — the φ-hero row names `proof:phi-leaf-zero`, authored in H.W4) AND was green in the last `proof:all` run; (ii) every row whose closure is a HANDOFF carries a born-RED `proof:*` gate name that likewise resolves (the dock row names `proof:dock-live`/`proof:dock-morph-settled`); (iii) a row with NEITHER a resolvable SYSTEM gate NOR a HANDOFF-paired born-RED gate REDS. This is the same resolve-or-red mechanism `proof:idioms` clause-1 uses for token names + the `LISTENER_ALLOWLIST` stale-guard shape — a static parse of a committed table, not a new runtime probe. The four chronic SYSTEM-property gates land in their sibling waves (`proof:cartoon-is-panel-depth` H.W2, `proof:hero-rung`/`proof:phi-leaf-zero` H.W4, `proof:mobile-single-page` H.W7); the HANDOFF born-RED gate (`proof:dock-live`: the `@mbabb` DockDropdownTrigger popover OPENS on click; dock expand/collapse settles ≤1 frame of its spring) is authored HERE, born-RED, green ONLY when the consumed glass-ui (AW tranche) version fixes D5/D9 — do NOT patch in kf. **WHY:** the four chronics escaped by M1 (issue-level close), M2 (scope-narrowing), M3 (column-migration to HANDOFF) — each because the P-invariant checked tag-presence, not defect-resolution. The meta-gate makes a bare tag NON-terminal: it polices the PRODUCT, not the column (`a-deferred-chronic.md:103-121`; `a-changes-vs-plan.md:178-202`).

- **S4 — the appearance + interaction axis clauses (I-5/I-6/I-7/I-9 + the I-3-static layout locks).** Author the per-defect clauses that bite against the sibling fix waves: **(I-5 φ-hero, extend `proof:demo-usability` clause 2)** — the LCP `h1` computed `font-size` ≥ the golden display rung (read the rung from the `text-display-mega` token, not a magic px), AND a positive clause requiring a `text-display-*` rung class (extend the leaf-tail ban at `proof-idioms.mjs:183-220` with a POSITIVE requirement). **(I-6 `proof:motion-liveness`)** — for each named perpetual decorative animation (the hero dots, `.lift-down`), sample `getComputedStyle` opacity/transform across ≥5 rAF frames and assert it CHANGES (and settles under PRM) — the dual of `proof:dogfood`'s rAF discipline (catches D6's silent `v-bind("duration")` drop). **(I-7 `proof:scene-parity`, after I-1 + the modes-pertinence verdict)** — for every SURVIVING scene, assert icon parity (a non-empty designed inline-SVG, not a fallback/blank) + interactivity parity (≥1 pointer-interactive affordance). **(I-9 mobile-architecture lock, extend `occlusion-gate`, after I-1)** — top+bottom docks `position: fixed`/affixed, the scene area fills the viewport as background, per-mode single-column; RETIRE the `square/mobile` allowance (`occlusion-gate.mjs:91-94`). **(I-3-static layout locks, extend `proof:idioms` clause-8c)** — the one-column lock (D1) + the ribbon-width lock (D4: extend `:560-564` to require `PlaybackRibbon.vue` reads `var(--controls-pane-width)`/`--rail-width`). **WHY:** these are the appearance + interaction axes the lattice never had; each is a one-line extension of an EXISTING proven gate (DRY, no new god-script) biting on the exact regression it forbids (`a-gate-blindspots.md:125-218`; `a-changes-vs-plan.md:226-267`).

- **S5 — bundle `proof:precept-sweep` (the spine lock).** Bundle the spine gates so the spine is irrefragable through H — composing the ACTUAL authored gate names (no alias drift): `proof:scene-machine-irrefragable` (H.W1's FSM restore+suspend — the precept-sweep lane's `proof:scene-fsm` ≡ this), `proof:scene-icons` + `proof:scene-parity` (H.W5's `SceneDescriptor.icon` required + no-fall-through-to-generic-glyph — the lane's `proof:scene-identity` ≡ these), `proof:no-legacy` regrep (no resurrected alias), `proof:docs-tree` (every `demo/CLAUDE.md` path exists — the F-04 doc-drift lint). **WHY:** the precept-sweep lane proves the spine is largely intact A→G except the scene-state seam (owned by H.W1); bundling the four into one gate makes "the spine held through H" a single green check (`a-precept-sweep.md:218-223`). **(S5 is a WIRING clause — it composes existing/sibling-wave gates into one bundle, authoring only the `proof:docs-tree` + `proof:no-legacy`-regrep lints net-new here.)**

## §Hard gate (falsifiable · re-runnable · MUST bite — the gate-of-gates)

The gates this wave AUTHORS are themselves the falsifiable instruments; the meta-gate's BITE is that the regime now reds where it was blind. Wired into `npm test`/the `proof:*` set + the browser CI job (not env-dark).

- **`proof:manifest-sourced` (S1, I-1) — BORN-RED today.** Adding a scene to `scenes.ts` WITHOUT a runtime-`SCENES` entry reds; a manifest key with no `scenes.ts` id reds (stale-key guard). **BITE:** reds TODAY — `scenes.ts` has 9 ids, `demo-driver.mjs:40-59` has 6 (the 3 new scenes are unmanifested — `a-gate-blindspots.md:46-63`); greens when `SCENES` is derived from `scenes.ts`. Re-runnable: `npm run proof:manifest-sourced`.
- **`proof:visual-lock` (S2, I-2) — the appearance axis.** Reverting any of D1/D3/D4/D6/D7 (re-introducing the two-column grid, the 680px easing canvas, the full-width ribbon, the broken dot-fade, the mid-rung hero) trips the affected named-region diff beyond tolerance. **BITE:** after the fix waves land + the baseline is committed, revert any one defect's fix → the region diff exceeds tolerance → reds (`a-gate-blindspots.md:111-123`). MEASURE-FIRST: tolerance bound from 3 identical runs before binding HARD. This is the broadest lever — six appearance defects, one re-runnable instrument.
- **The chronic meta-gate (S3, I-3) — a bare HANDOFF tag with no born-RED gate REDS the ledger.** Parsing the COMMITTED chronic→gate table (`H/PROGRESS.md` / `H/FINAL.md`, canonical `_SYNTHESIS-deferred-ledger.md §1`), the ledger gate asserts: every chronic row's "H closure" cell names a `proof:*` gate that RESOLVES to an authored gate (no dangling name) and is EITHER a passing SYSTEM-property gate OR a HANDOFF tag PAIRED with a born-RED kf gate. **BITE:** (1) add a chronic row tagged HANDOFF with no born-RED gate → reds (catches M3 — column-migration-to-HANDOFF-and-forget structurally); (2) name a SYSTEM gate that no wave authors (e.g. the φ-hero row citing a gate H.W4 never produces) → reds on the dangling reference (catches the M1 paper-close where the cited gate does not exist) — `a-deferred-chronic.md:117-121`.
- **`proof:dock-live` (S3, I-3 the HANDOFF born-RED gate) — BORN-RED today.** The `@mbabb` DockDropdownTrigger popover OPENS on click; dock expand/collapse settles ≤1 frame of its spring (MEASURE-FIRST the budget). **BITE:** reds TODAY (D5 lag + D9 popover-not-opening ship in 4.1.0 — the consumed glass-ui 3.4.0 carries the pre-AW.W2 dock); greens ONLY when the consumed glass-ui (AW tranche) version fixes it — the kf-side WATCH on the HANDOFF; do NOT patch in kf (`a-deferred-chronic.md:115-116,166`; `_SYNTHESIS-gap-scorecard.md:173`).
- **`proof:hero-rung` / `proof:phi-leaf-zero` (S4, I-5) — the φ-hero floor + the chronic SYSTEM-property gate.** `proof:hero-rung`: the LCP `h1` computed `font-size` ≥ the `text-display-mega` φ rung AND carries a `text-display-*` class. The φ-hero CHRONIC (CH-2/M1) SYSTEM-property gate the meta-gate (S3) cites is `proof:phi-leaf-zero` (authored in H.W4) — it asserts BOTH the hero rung AND zero raw `text-*` rungs across the demo, so the M1 escape (the issue closes while raw rungs linger) is structurally locked, not merely the hero rung. **BITE:** `proof:hero-rung` reds TODAY (the hero is `text-display-4` = 86.1px, a MID rung — `_SYNTHESIS-gap-scorecard.md:63`); greens on the H.W4 one-class bump. `proof:phi-leaf-zero` additionally reds on the 37 surviving raw rungs (`a-deferred-chronic.md:113`), so locking D7 against re-shrinking AND against the leaf-tail re-growing.
- **`proof:motion-liveness` (S4, I-6) — decorative animations ADVANCE.** Each named perpetual decorative animation's sampled opacity/transform CHANGES across ≥5 rAF frames (and settles under PRM). **BITE:** break `v-bind("duration")` (`AnimatedText.vue:95`) so the shorthand drops → opacity never changes → reds (catches D6's exact silent-drop failure mode — `a-gate-blindspots.md:171-180`).
- **`proof:scene-parity` (S4, I-7, after the pertinence verdict) — icon + interactivity parity.** Every SURVIVING scene renders a non-empty designed inline-SVG nav icon AND exposes ≥1 pointer-interactive affordance. **BITE:** add a scene without an icon, or a non-interactive surviving mode → reds; an `<img>` icon FAILS by construction (theme-blind — `_SYNTHESIS-gap-scorecard.md:150`). MEASURE-FIRST: binds AFTER `a-modes-pertinence` decides survivors (gating interactivity on a KILL-slated mode is wasted rent — `a-gate-blindspots.md:182-193`).
- **The layout-idiom + mobile-architecture locks (S4, I-3-static + I-9).** One-column lock (D1: the field grid computed `grid-template-columns` is single-track) + ribbon-width lock (D4: `PlaybackRibbon.vue` reads `var(--rail-width)`) + mobile-architecture lock (D10: docks affixed, scene fills viewport, single-column; the `square/mobile` allowance RETIRED). **BITE:** re-introduce the `auto_1fr` two-up pairing, or author the ribbon full-bleed, or un-affix a dock → reds; the retired allowance's own stale-guard (`occlusion-gate.mjs:347-353`) reds if it starts passing again.
- **`proof:precept-sweep` (S5) — the spine bundle.** `proof:scene-machine-irrefragable` (H.W1) + `proof:scene-icons`/`proof:scene-parity` (H.W5) + `proof:no-legacy` regrep + `proof:docs-tree` all green (the actual authored gate names; the lane's `proof:scene-fsm`/`proof:scene-identity` are aliases of these). **BITE:** a resurrected alias, a `demo/CLAUDE.md` path that doesn't exist, a `SceneDescriptor` without `icon`, or an FSM round-trip that loses state → reds.

**The §Mandate bar — MUST bite.** `proof:manifest-sourced` and `proof:dock-live` are BORN-RED on the live tree TODAY (the 3 unmanifested scenes; the broken dock) — the falsifiable proof the blind spots are real. The chronic meta-gate reds on a bare HANDOFF tag (M3 caught structurally). `proof:visual-lock` + the per-defect clauses (`proof:hero-rung`, `proof:motion-liveness`, the layout/mobile locks) bite when their sibling-wave fix is reverted — locking the D1–D11 fixes against regression as a CLASS, not one-by-one. No clause passes vacuously: each asserts a RENDERED fact (pixel region, computed font-size, frame-to-frame opacity delta, grid track count, affixed position, popover-opens, settle budget) or a LEDGER property (every chronic has a SYSTEM gate or a born-RED HANDOFF gate) the current source-shape-only lattice cannot see.

## §Folds

- `a-gate-blindspots` **I-1** (S1 — re-source the manifest, ROOT-B, the prerequisite) + **I-2** (S2 — `proof:visual-lock`, the bulk of ROOT-A) + **I-3-static** (S4 — the layout-idiom locks, one-line clause-8c extensions) + **I-5** (S4 — the φ-hero floor `proof:hero-rung`; the φ-hero chronic SYSTEM-property gate `proof:phi-leaf-zero` is authored in H.W4 and CITED by the S3 meta-gate) + **I-6** (S4 — `proof:motion-liveness`) + **I-7** (S4 — `proof:scene-parity`, after pertinence) + **I-9** (S4 — the mobile-architecture lock, retire the allowance). **I-4** (`proof:hover-depth` radial-ban + cartoon-lock) is folded into H.W2 (the design-language wave owns the cartoon gate + `proof:cartoon-is-panel-depth`/`proof:no-orphan-specular`); H.W8's visual-lock matrix captures the hover-state screenshot that catches a residual radial. **I-8** (`proof:dock-perf` budget) is the MEASURE-FIRST half of `proof:dock-live` (S3) — the budget gate is kf's; the lag FIX is glass-ui-HANDOFF.
- `a-deferred-chronic` **§3** (S3 — the chronic-closure meta-gate; the `proof:dock-live` born-RED HANDOFF gate; M1/M2/M3 caught — the four SYSTEM-property gates land in their sibling waves, the meta-gate composes them into the ledger discipline).
- `a-changes-vs-plan` (S2/S4 — the 4 instruments: `proof:visual-fidelity` ≡ S2's `proof:visual-lock` viewport-matrix; `proof:scene-state-machine` ≡ H.W1's `proof:scene-machine-irrefragable`; `proof:motion-timing` ≡ H.W7's `proof:drawer-spring` + S4's `proof:motion-liveness`; the cartoon-shadow visual-lock ≡ H.W2 + S2's matrix). The browser gates move from DARK-by-default to LIT in the dev loop (S2 wires `proof:visual-lock` into the browser CI job).
- `a-precept-sweep` (S5 — `proof:precept-sweep` bundles `proof:scene-fsm`/`proof:scene-identity`/`proof:no-legacy`/`proof:docs-tree`; the F-04 `demo/CLAUDE.md` doc-drift lint authored here).
- **RECORD (process, in H's FINAL):** the gate-genus gap — the demo's runtime-fidelity gate regime did NOT exist before H; the next tranche inherits it. The chronic-closure meta-gate is the standing repair so a future "the home" claim must ship the SYSTEM gate, not a sliver (`a-deferred-chronic.md:162,167`; `a-changes-vs-plan.md:300-303`).
- **ALREADY-SOTA (do NOT re-author):** the gate lattice at source-shape + not-blank/not-occluded/score-floor; the `proof:idioms` clause-8c shape, the `occlusion-gate`/`proof:brittleness` stale-guards, the `proof:demo-usability` clause-2 hero probe — H EXTENDS these, never rewrites them (`a-gate-blindspots.md:222-243`; `_SYNTHESIS-gap-scorecard.md:195`).

## §Design decisions (trade-offs RESOLVED)

- **This wave is why H is the LAST re-paper — RESOLVED (the load-bearing claim).** The four chronics (cartoon D2, φ-hero D7, mobile D10, dock D5) "exited" the A→G ledger by re-classification (M1 issue-level close, M2 scope-narrowing, M3 column-migration), NOT by being solved — "the P-invariant policed the COLUMN, not the PRODUCT" (`_SYNTHESIS-gap-scorecard.md:36-43`). H.W8's meta-gate makes a bare tag non-terminal: a chronic exits ONLY with a SYSTEM-property gate or a HANDOFF-with-born-RED-gate. With the appearance axis (visual-lock), the interaction axis (motion-liveness/scene-parity/dock-live), and the re-sourced manifest, these four chronics become STRUCTURALLY un-re-paperable — that is precisely why this is the last tranche they can be closed-on-paper.
- **Re-source the manifest FIRST — RESOLVED (the prerequisite, cheap, unblocks everything).** I-1 is the load-bearing fix: until `SCENES` derives from `scenes.ts`, the 3 new scenes are invisible to every runtime gate, so D8/D11/mobile are not checkable at all. It is one motion (delete the array, derive + stale-guard) and unblocks `proof:visual-lock` and `proof:scene-parity` (`a-gate-blindspots.md:231-234`).
- **The visual-lock is the broadest lever — but scoped to NAMED REGIONS, measure-first — RESOLVED.** A full-page pixel diff would flap on anti-aliasing noise; scope to named regions (controls pane, hero, ribbon, easing editor) and bind tolerance from 3 identical runs before HARD. It is the missing PEER of `capture.mjs` (which already produces frames) — H promotes frames to a gated diff, not a new god-script. It converts six appearance defects into ONE re-runnable instrument (`a-gate-blindspots.md:111-123,235`).
- **Static locks are one-line extensions, NOT new god-scripts — RESOLVED (DRY).** I-3-static (layout) extends `proof:idioms` clause-8c (`proof-idioms.mjs:545-580`); I-5 (φ-hero) extends `proof:demo-usability` clause 2 (`proof-demo-usability.mjs:179-237`); I-9 (mobile) extends `occlusion-gate`. The right instrument GENUS (`proof:demo-usability`, browser, biting on rendered defects) ALREADY exists — it was under-populated (3 clauses where D1–D14 needed 30); H POPULATES it, it does not invent a parallel suite (`a-changes-vs-plan.md:154-166`; `a-gate-blindspots.md:239-243`).
- **The HANDOFF gates are BORN-RED, kf-side, never a kf patch — RESOLVED (inv-16 + M3 repair).** `proof:dock-live` is born-RED and green ONLY when the consumed glass-ui (AW tranche) version fixes D5/D9 — the kf-side WATCH that converts a silent HANDOFF into a gated one. The dock memory rule ("all dock changes in glass-ui, never patched in demo") is CORRECT and stays — the gate is COMPLEMENTARY to the rule, not a relaxation of it. The budget half (I-8 `proof:dock-perf`) is MEASURE-FIRST (establish the budget from a clean baseline before binding) (`a-deferred-chronic.md:206-211`; `a-gate-blindspots.md:195-205`).
- **The browser gates must be LIT in the dev loop, not env-dark — RESOLVED.** The felt-quality instruments running only in CI under `KF_REQUIRE_BROWSER=1` is the mechanism by which D1–D14 lived un-seen — the loop agents iterate against never rendered the demo. `proof:visual-lock` is wired into the browser CI job (`demo-smoke`), NOT skippable-silent — so the appearance axis is exercised, not dark (`a-changes-vs-plan.md:167-177`).
- **This wave authors GATES, not fixes — RESOLVED (the boundary).** H.W8 touches NO engine/demo source — the per-defect fixes live in H.W2..H.W7; the gates here BITE against those fixes. The visual-lock baseline is captured AFTER the fix waves land (the baseline IS the fixed render); the BORN-RED clauses (manifest stale-guard, `proof:dock-live`, the chronic meta-gate) red TODAY independent of the fix waves. This keeps the gate regime a clean, separable deliverable that the next tranche inherits whole.
