# H.W6 — TYPING-DOTS + CHROME DOGFOOD (Band 3 · DEMO-SIDE · inv ζ · the hero's first-paint animation must BE the engine)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (a genuine perceptual BREAK — measured 43% of every cycle near-invisible, 0 cadence — plus an inv-ζ violation: the demo hand-rolls the ONE animation literally named after the library's domain; net-deletion of two `@keyframes` and a class-collision) · **Scope:** `demo/@/components/custom/AnimatedText.vue` (the dual-`animation`-shorthand `.lift-down`/`.dot-fade` collision `:72-107`, the title-sized `duration` formula `:66-68` mis-applied to the 3-glyph ellipsis, the `split(/\s+/)` one-span substrate `:62-64`) + the call site `demo/@/components/custom/editor-shell/EditorStartScreen.vue:16-19` (`class="dot-fade depth-text" :text="ellipsis"`) + a NEW tiny `demo/@/components/custom/TypingDots.vue` dot primitive (the dogfood seam) + a new `test/proof-typing-dots` browser harness + `scripts/proof-typing-dots.mjs` runner wired into the `proof:*` set · **DAG-deps:** the belt-and-suspenders engine guard (H.W0 H-A2: a bare non-numeric/non-color text leaf classified as a discrete hold-snap, `engine.ts:516,576`) SHOULD land first so the hero ellipsis can NEVER abort a frame even on a corrupted hop (`a-animations-quality` F6); the dots primitive itself is independent of the spine (a kf-local demo-side fix) and can land in parallel with H.W2/H.W3.

## §Provenance (the folded lanes)

- `a-typing-dots` **(primary, root-caused live)** — the ellipsis is ONE word (`split(/\s+/)` leaves `"..."` whitespace-free → ONE span), so the whole `...` fades as a unit; the 2.6s duration was authored for the WHOLE TITLE (`3*0.2 + 0.2*10 = 2.6s`) and mis-applied; `@keyframes dotFade` is a full vanish (`0%/100% opacity:0`) not a typing blink; `.lift-down` + `.dot-fade` both set the `animation` shorthand on one node → silent cascade loser. NOT dogfooded (`a-typing-dots.md:33-50,80-125`). The gestalt fix (per-dot spans + short fixed cycle + low-rest blink + dogfood `steppedEase`/`NumericAnimation` + decouple from `lift-down`) and the `proof:typing-dots`/`proof:dogfood-hero` instruments (`a-typing-dots.md:127-184`).
- `a-animations-quality` **F1** — the same three compounding bugs through the inv-ζ lens (one span not three; double-bound shorthand; wrong primitive); `stagger()` (`stagger.ts:127`) + `NumericAnimation` is the canonical staggered primitive, ALREADY imported by the sequence scene (`useSequenceDemo.ts:6`) — the demo dogfoods stagger on its SUBJECT but hand-rolls + breaks it on its CHROME (`a-animations-quality.md:59-107`). **F6** — the live `Parse error at offset 0: "......"` (`engine.ts:516,576`): the hero ellipsis text leaking into a `_lerp` value position; fixing F1/F3 (drive by opacity, never the glyph string) removes the class of value that can throw, and the H.W0 engine guard is the floor (`a-animations-quality.md:241-278`). The reference dogfood pattern `CopyButton.vue:52-92` (`new CSSKeyframesAnimation(...).fromString(...)` in an `AnimationGroup`) — the template every chrome animation should follow (`a-animations-quality.md:32-37,334-339`).
- `a-styling-idioms` **§4** — the cascade-collision is a latent trap regardless of which idiom "wins": two rules setting the `animation` shorthand on one element; the structural lint is "no single element class-set carries two `animation`-shorthand rules" (folded into the gate's cascade clause).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §1.1 typing-dots row` (`:62`) + `§3 H.W6` (`:152-155`) + `§1.1 live-console-crashes H-A2` (`:71`).

## §The state, verified (file:line / live anchors)

- **One span, not three — the substrate structurally cannot stagger.** `AnimatedText.vue:62-64`: `words = props.text.split(/\s+/).filter((w) => w.length > 0)`. The ellipsis `"..."` has no whitespace → ONE token → ONE `<span>`. The three dots share one opacity; there is no per-dot delay (`a-typing-dots.md:39-40,90-94`; re-verified at source `AnimatedText.vue:62-65`).
- **The duration formula targets the TITLE, not the dots.** `AnimatedText.vue:66-68`: `` `${props.text.length * props.offset + props.offset * 10}s` `` → for `"..."` (length 3, offset 0.2) = `2.6s`; for the 19-char title ("Select an animation") ≈ 5.8s — the formula was authored for the title lift and mis-applied to the 3-glyph ellipsis (`a-typing-dots.md:37-38,82-88`; re-verified at source `AnimatedText.vue:66-68`).
- **`@keyframes dotFade` is a VANISH, not a typing blink.** `AnimatedText.vue:93-107` (re-verified at source `:99-107`): `0%,100% { opacity: 0 } 50% { opacity: 1 }` — the dots fully disappear each cycle; live opacity sampled over one 2.6s cycle (325 rAF samples): `min 0.000`, `max 1.000`, **43% of cycle < 0.3 (near-invisible)**, only **40% > 0.7 (clearly visible)** (`a-typing-dots.md:53-78`). No `. → ·· → ···` rhythm — a single slow ghost-swell.
- **The dual-`animation`-shorthand collision (re-verified at source).** The `...` span carries BOTH `.lift-down` (`AnimatedText.vue:74` `animation: liftDown 3s var(--ease-standard) infinite`) AND `.dot-fade` (`:95` `animation: dotFade v-bind("duration") var(--ease-standard) infinite`) — both set the `animation` shorthand; `.dot-fade` is declared later (`:93` > `:72`) so `dotFade` wins WHOLLY and silently drops `liftDown` (live: `animationName: dotFade-…` on the ellipsis span, `liftDown-…` on the title words) (`a-typing-dots.md:41-42,102-110`; re-verified at source `AnimatedText.vue:72-76,93-95`).
- **The call site.** `EditorStartScreen.vue:16-19`: `<AnimatedText class="dot-fade depth-text" :text="ellipsis" />`, `ellipsis` default `"..."` (`:49`); `depth-text` is a glass-ui utility (text-shadow only, `node_modules/@mkbabb/glass-ui/dist/styles/utilities.css:231`) — orthogonal to the animation, NO handoff (`a-typing-dots.md:43-45,185-187`; re-verified at source `EditorStartScreen.vue:16-19,49`).
- **inv ζ violated — and the dogfood template is IN-TREE, cheap, correct.** `AnimatedText.vue` imports NOTHING from the library; the engine already ships the exact idiom: `steppedEase(n, jump)` for a discrete dot cadence (`animations.ts:218,487`), `typingCursor` (`animations.ts:484-490`, `steppedEase(2,"jump-start")`), `spinner` (`animations.ts:553-559`, `steppedEase(40,"jump-end")`). The reference pattern `CopyButton.vue:52-92` drives icon feedback through `new CSSKeyframesAnimation(options).fromString(...)` in an `AnimationGroup` (re-verified at source `CopyButton.vue:52-91`) — the template to follow (`a-typing-dots.md:112-125`; `a-animations-quality.md:32-37`).
- **The live `"......"` parse-error is the same leak.** Rapid scene-hopping after `localStorage.clear()` throws `Error: Parse error at offset 0: "......"` at `engine.ts:576` (`processFrame`) ← `engine.ts:516` (`interpFrames`): the hero ellipsis `"..."` doubled (two keyframe endpoints) reaching value.js's `_lerp` as if interpolable (`a-animations-quality.md:241-256`). A clean single-scene load = 0 errors; the throw is on the corrupted multi-hop path — the animations-quality face of D12.
- **ALREADY-SOTA — do NOT touch.** The PRM guard (`AnimatedText.vue:113-121`): under `prefers-reduced-motion` the dots settle to a fully-opaque resting frame — correct, must be preserved (`a-typing-dots.md:165-166`; re-verified at source `:113-121`). The TITLE word-split (the per-word `AnimatedText` granularity the hero needs for `text-wrap: balance`, F.W16 rationale) is sound for the title — the dots merely need a DIFFERENT granularity (a befitting delta) (`a-typing-dots.md:137-139`). The `CopyButton` dogfood and the scene-subject dogfood (`useSceneSwap` SpringProgress, the easing/spring/sequence subjects) are exemplary and untouched (`a-animations-quality.md:334-339`).

## §Goal

Make the ellipsis a **first-class, kf-driven, staggered three-dot blink** — ONE motion that replaces the broken surface (no compat alias kept beside it). The dots become their OWN tiny substrate (`TypingDots.vue`): three (N) explicit `<span>·</span>` dots, each driven by the engine — a `NumericAnimation` over `{opacity}` per dot seated at `stagger(N)` delays, or a per-dot `CSSKeyframesAnimation` with a per-dot `delay`, `timingFunction: steppedEase(...)` — mirroring `typingCursor`/`spinner`/`CopyButton`. Rest opacity ~0.2 (the dots NEVER fully vanish, killing the 43% ghost window), short fixed cycle (~1.4s total, ~0.16s per-dot step — NOT `text.length`-derived). The dots interpolate ONLY a numeric property (opacity) — never the text content — so no string ever reaches `_lerp` (folds out F6's leak class; the H.W0 engine guard is the floor). The title keeps its WORD-granular `AnimatedText` + `liftDown` lift untouched (the befitting delta: title needs word-granularity for balance; dots need per-dot granularity for cadence). Because the dots are their own substrate, NO element ever carries two `animation` shorthands — the cascade collision dies WITH the split (no-legacy: replaced in one motion). The shared engine reduced-motion authority replaces the hand-mirrored `@media (prefers-reduced-motion)` block for the dots (the PRM resting-frame behavior preserved). KISS · DRY · zero new machinery — this is a transposition onto in-tree primitives, not a green-field add.

## §Scope

- **S1 — own the `TypingDots.vue` staggered dot primitive (the dogfood seam).** Author a tiny dedicated component rendering N explicit dot `<span>`s, driving each dot's `opacity` through the engine: `NumericAnimation` per dot at `stagger(N)` delays (the `stagger.ts:127` + `NumericAnimation` canonical primitive the sequence scene already dogfoods) OR a per-dot `CSSKeyframesAnimation` with per-dot `delay` and `timingFunction: steppedEase(...)` — mirroring `typingCursor` (`animations.ts:484`)/`spinner` (`animations.ts:553`)/`CopyButton` (`CopyButton.vue:52`). Rest opacity ~0.2 → peak 1 → back (NEVER 0). Total cycle ≤1.4s, per-dot step ~0.16s — a FIXED short cycle, NOT `text.length`-derived. The component imports a kf engine symbol (the inv-ζ seam). **WHY:** a typing indicator IS a staggered, per-element animation; the per-dot substrate is the ONLY shape that can produce the `. → ·· → ···` cadence a reader expects, and it makes the hero a live keyframes.js demonstration from first paint (`a-typing-dots.md:127-156`; `a-animations-quality.md:92-101`).

- **S2 — re-point the call site; decouple the dots from `lift-down` (kill the collision in one motion).** `EditorStartScreen.vue:16-19` renders `<TypingDots />` (or `<AnimatedText>` with an ellipsis render-path that delegates to the dots primitive) instead of `<AnimatedText class="dot-fade depth-text" :text="ellipsis" />`. The `.dot-fade` rule + `@keyframes dotFade` (`AnimatedText.vue:93-107`) are DELETED — the ellipsis no longer routes through the word-split or the title-sized duration. The title span keeps `liftDown`; the dot spans get only the dot animation. **No element carries two `animation` shorthands** because the two idioms now live on disjoint substrates. **WHY:** the cascade collision is a latent trap regardless of which idiom wins (`a-styling-idioms §4`); decoupling the substrates removes it STRUCTURALLY, and deleting `dotFade` + the broken duration in the same motion is the no-legacy "replaced surface replaced once" (`a-typing-dots.md:158-163`; `a-animations-quality.md:93-101`).

- **S3 — the dots interpolate numeric opacity only; never the glyph string (folds out F6's leak).** The dots' engine animation drives `{opacity}` (and optionally `translateY`) — the literal `"..."` text is rendered as static `<span>` content, NEVER a keyframe VALUE. **WHY:** the live `Parse error "......"` (`engine.ts:516,576`) is the hero text reaching `_lerp` as an interpolable value; driving by opacity removes the class of value that can throw. The PRIMARY fix is upstream (don't feed it text); the H.W0 H-A2 engine guard (a bare non-numeric/non-color leaf → discrete hold-snap) is the belt-and-suspenders so even a corrupted hop never aborts a frame (`a-animations-quality.md:241-278`; `_SYNTHESIS-gap-scorecard.md:71,124,154`). **(This S3 is a CONSTRAINT on S1, not a separate edit — the dots are opacity-driven by construction; F6's engine guard is H.W0's deliverable, recorded here as the dependency.)**

## §Hard gate (falsifiable · re-runnable · MUST bite — born-RED today, GREEN on fix)

A new `proof:typing-dots` (browser, reusing the lane's rAF-sampling harness) + `proof:dogfood-hero` (static) + a cascade lint, wired into `npm test` and the `proof:*` set. Load `/#/` after `localStorage.clear()` (the true home start screen), sample each dot span's opacity across one full cycle.

- **`proof:typing-dots` (a) — three distinct animated dot spans.** `n_dots === 3` distinct animated `<span>` elements (not one clump). **BITE:** reds TODAY (the `split(/\s+/)` substrate yields ONE span — `a-typing-dots.md:172-176`); greens ONLY on the per-dot split (S1/S2). Re-runnable: `npm run proof:typing-dots`.
- **`proof:typing-dots` (b) — the cadence exists (monotone delays).** Per-dot `animation-delay` (or the seated `stagger` delay) is STRICTLY INCREASING across the three dots. **BITE:** revert to a single shared `dotFade` → all dots share one delay → the monotonic assertion reds (catches a regression to the one-span clump). 
- **`proof:typing-dots` (c) — the dots NEVER vanish (kills the 43% ghost window).** `min opacity over the cycle >= 0.15`. **BITE:** reds TODAY (the `0%/100% opacity:0` vanish floors at 0.000 — 43% < 0.3); greens on the ~0.2-rest blink (S1).
- **`proof:typing-dots` (d) — short fixed cycle.** Total cycle `<= 1.6s`. **BITE:** reds TODAY (the title-sized 2.6s duration — `a-typing-dots.md:62-64`); greens on the fixed ~1.4s cycle (S1).
- **`proof:dogfood-hero` (inv ζ) — the dots import the engine.** Static assert: `TypingDots.vue` (the dots substrate) imports a kf engine symbol — grep `from "@src` / `CSSKeyframesAnimation` / `steppedEase` / `NumericAnimation` (`a-typing-dots.md:177-180`; `a-animations-quality.md:102-107`). **BITE:** reds TODAY (zero library imports in `AnimatedText.vue`'s dot path); greens when the dots route through the engine. Mirrors the `CopyButton.vue:52` dogfood template.
- **Cascade lint — no double-`animation` shorthand on one node.** Grep/runtime gate: no single element class-set carries two rules that BOTH set the `animation` shorthand (`getComputedStyle` resolves a single `animation-name` per dot) (`a-typing-dots.md:181-183`; `a-styling-idioms §4`). **BITE:** re-stack `.lift-down` + `.dot-fade` on one node → reds (catches the collision class structurally).

**The §Mandate bar — MUST bite.** Clauses (a)/(c)/(d) RED on the live tree TODAY (the CURRENT build fails exactly (a), (c), (d) per `a-typing-dots.md:174-176`), GREEN on the per-dot dogfooded blink — the falsifiable proof the break is real and the fix lands it. `proof:dogfood-hero` reds TODAY (zero imports) and greens on the inv-ζ transposition. The cascade lint is the structural lock against the collision returning. No clause passes vacuously: each asserts an EXACT rendered fact (span count, monotone delays, opacity floor, cycle bound, engine import) the current hand-rolled surface fails.

## §Folds

- `a-typing-dots` **(primary)** — S1 (the per-dot dogfooded blink + `proof:typing-dots` (a)/(b)/(c)/(d)) + S2 (the call-site re-point + the cascade decouple + `proof:dogfood-hero`).
- `a-animations-quality` **F1** — folded INTO S1 as the stagger/`NumericAnimation` primitive choice (the `CopyButton` template) + **F6** — folded INTO S3 as the "don't interpolate text content" invariant (the `"......"` leak class removed by opacity-driving; the engine guard is H.W0's). **F3** (the hero word-LIFT hand-rolled `liftDown`) is RECORDED as a sibling dogfood opportunity but NOT folded here — H.W6 owns the DOTS; the title lift's transposition to `CSSKeyframesAnimation` + stagger is a sibling `a-animations-quality` BOOK (the title's φ-typography rung is H.W4's). **F4** (cube idle-bob hand-rolled) is OUT of this wave (a cube-scene fold). **F7/F8** (gold shimmer + tab `enter` + `.scale-on-hover` are JUSTIFIED CSS) are RECORD — do NOT over-dogfood for dogma.
- `a-styling-idioms` **§4** — S2's cascade lint (the no-double-`animation`-shorthand structural gate).
- **RECORD (no separate wave) — the un-touched ALREADY-SOTA:** the PRM resting-frame guard (`AnimatedText.vue:113-121`), the title word-split granularity (F.W16 balance rationale), the `CopyButton`/scene-subject dogfood. H.W6 touches NONE of them — the defect is PURELY the dots' substrate, duration, keyframe, and the collision.

## §Design decisions (trade-offs RESOLVED)

- **Per-dot OWN component, not a special-case inside `AnimatedText` — RESOLVED.** The audit names two paths: (a) special-case the ellipsis inside `AnimatedText`, or (b) give the dots their OWN tiny render path. (b) is cleaner and the gestalt move: the title and the dots have genuinely DIFFERENT granularity needs (word-granular for `text-wrap: balance` vs per-dot for cadence — a befitting delta), and a `TypingDots.vue` keeps `AnimatedText` single-purpose (KISS) while the dots get a substrate that CAN stagger. The special-case path would re-entangle two granularities in one component — the opposite of the decouple S2 buys (`a-typing-dots.md:133-139`).
- **Dogfood the engine, NOT pure CSS — RESOLVED (inv ζ).** A demo whose entire reason for existing is to showcase the library MUST NOT hand-author the one animation named after the library's domain. If a pure-CSS LCP-purity argument is raised for the hero, MEASURE-FIRST: the `.dot-fade` node already re-keys constantly via the start-screen `<Transition appear>` (`EditorShell.vue:46-52`), so an LCP-purity claim for it is weak (`a-typing-dots.md:151-156`). The engine path is the strongest "this is what the library does" statement from first paint.
- **Rest opacity ~0.2, not 0 — RESOLVED.** The classic typing idiom keeps a low REST opacity and pulses to 1 — it never disappears; the user perceives "broken" precisely because the current keyframe BLANKS out (43% ghost). The ~0.2 floor is the perceptual fix AND the `proof:typing-dots (c)` bite (`a-typing-dots.md:96-100,145-148`).
- **Fixed short cycle, NOT `text.length`-derived — RESOLVED.** The blink is a fixed ~1.4s cycle (~0.16s per-dot step); the `length`-based formula stays ONLY for the title lift. Mis-applying a title-sized duration to a 3-glyph ellipsis is the headline bug; the fixed cycle is `proof:typing-dots (d)` (`a-typing-dots.md:141-143`).
- **The collision dies WITH the split — no double-shorthand band-aid — RESOLVED (no-legacy).** The tempting band-aid is to switch `.dot-fade`/`.lift-down` to `animation-name`/`-duration` longhands so both can coexist on one node. The gestalt fix removes the shared node entirely (the dots are their own substrate), so NO element carries two animations — the structural cause is gone, not papered. The longhand workaround stays ONLY as the named fallback if both idioms are ever forced onto one node elsewhere (`a-typing-dots.md:158-163`).
- **Opacity-driven, never text-interpolated; the engine guard is the floor — RESOLVED.** S3 makes the dots interpolate numeric opacity; the literal `"..."` is static content. This removes the `"......"` leak class at the SOURCE (don't feed text to `_lerp`). The H.W0 H-A2 engine guard (bare non-numeric/non-color leaf → discrete hold-snap) is the belt-and-suspenders for any OTHER corrupted-hop path — but the primary fix is upstream and demo-owned; the engine guard is H.W0's deliverable, sequenced before this wave (`a-animations-quality.md:268-272`; `_SYNTHESIS-gap-scorecard.md:124,154`).
- **Independent of the spine; H.W0-guard-dependent — RESOLVED.** The dots primitive is a kf-local demo-side fix with ZERO dependency on the FSM (H.W1) or the layout grid (H.W3) — it can land in parallel. Its ONE upstream dependency is the H.W0 engine guard (the F6 floor), which should land first so no corrupted hop can abort a frame regardless of the demo fix.
