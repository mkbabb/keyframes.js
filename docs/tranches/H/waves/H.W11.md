# H.W11 — THE STAGE-CARD REGISTER + THE CONTROL-SURFACE DFA + THE LAYOUT REFINEMENTS (the consolidated round-3 user-feedback fold · the four stage scenes converge to ONE standard glass `<Card>` · I5 REVERSES W10 G8 full-bleed · the per-scene control-VISIBILITY DFA extends W1 · I1/I2/I4/I5/I6/I7)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** CORRECTIVE SHIP-in-H (the THIRD user-feedback refinement round, observed live on `tranche-h-impl` while W10 is LANDING CONCURRENTLY in the working tree — the I-items SUPERSEDE specific W10 decisions whose impl is still settling, most importantly **I5 REVERSES W10 G8 full-bleed**; net-deletion-leaning — drop the inner bezier Card (I6), drop the "editing:" subtitle (I7), drop the per-row `auto` label widths + the `w-20` magic literal (I1), drop the reka-tab-fallback hacks (I2); pure demo CSS/markup/Vue + ONE orthogonal table on the W1 machine + ONE named transition bench — NO engine source (the engine is FENCED, inv ζ / `H.md §Mandate`), NO glass-ui patch in kf — inv-16, NO dist build (a W10 impl workflow concurrently owns dist)) · **Scope:** the FOUR stage SUBJECT roots → ONE standard non-cartoon glass `<Card>` (REVERT the full-bleed on `EasingTarget.vue:2-10` / `SpringTarget.vue:2-8` / `StartingStyleTarget.vue`; SWAP the bare `glass-resting cartoon-surface` divs on `SequenceTarget.vue:3` / `MotionPathTarget.vue:3` → `<Card>`; the card sits in the `[stage]` track, dock-contained by the surviving `.stage-cell` primitive — `AnimationControlsGroup.vue:58,364-366`) + the per-scene control-VISIBILITY DFA (a `controlSurfaces: Record<SceneId, ControlSurface[]>` table on the W1 `useSceneMachine`; `AnimationControls.vue:8-9,20-22` renders the tab triggers FROM the table not the hard-coded `controls|keyframes|timeline` list; the reka-fallback hacks DIE — `EasingScene.vue:26-32`, `SpringScene.vue:62-74`, `MotionPathScene.vue:25`, `CubeScene.vue` matrix-controls) + the uniform label SUBGRID (the per-row `grid-cols-[auto_1fr]` → a parent `subgrid` track — `EasingSidebar.vue:144-150`, the standard `AnimationControlsControls.vue` W9 F1 rows, `LayerConfigPanel.vue`; the `SequenceTarget.vue:25-27` `w-20` magic literal dies) + the bezier panel de-nest + grow (DROP the inner `<Card>` — `TimingFunctionPanel.vue:15` rendered inside the controls host at `AnimationControlsControls.vue:119`; GROW the in-panel canvas ceiling above the W9 220px — `TimingFunctionPanel.vue:251-252`; DELETE the "editing:" subtitle — `TimingFunctionPanel.vue:33`) + the in-demo card-rounding-by-consumption (I4 closes FOR FREE alongside I5 — the `<Card>` carries `rounded-card` by construction; the deeper primitive default is a glass-ui HANDOFF) + the gate set (NEW `proof:stage-glass-card`/`proof:card-rounded-primitive`/`proof:label-subgrid`/`proof:scene-control-dfa`/`proof:scene-transition-perf`/`proof:bezier-single-card`/`proof:bezier-panel-taller`; the named `bench:scene-transition` MEASURE-FIRST budget) · **DAG-deps:** depends on **H.W1** (the keystone FSM — I2 EXTENDS its `useSceneMachine` with a third orthogonal axis; every browser gate settle-gates on it resting), **H.W9** (I1 REFINES F1's per-row split → a parent subgrid; I6/I7 REFINE F2's bezier-panel form — the inner card + the 220px ceiling + the "editing:" subtitle; the calm cartoon+quiet PANEL register is UNTOUCHED — I5's glass-card is the orthogonal STAGE register), **H.W10** (I5 SUPERSEDES G8's full-bleed SURFACE consequence — treat W10's full-bleed as the immediately-superseded baseline; the `.stage-cell` dock-band-reserve LAYOUT PRIMITIVE half of G8 SURVIVES; I2's DFA enumerates the W10-normalized scenes; I6/I7 compose with W10's bezier panel). Sequenced **AFTER H.W10, BEFORE H.W12 and H.W8's GOLDEN `proof:visual-lock` baseline** (the convergent order: **… W9 → W10 (full-bleed baseline, superseded) → H.W11 → H.W12 → H.W8 golden capture**). W11 lands the STRUCTURE (the glass-card register all four stage scenes converge to, the uniform label grid, the control-surface DFA that gates what renders, the bezier panel form); W12 fills it.

## §Provenance (the folded lanes)

This wave is the SPEC-AUTHOR lane of `audit/feedback/i-_PLAN.md` (the COMPLETE, authoritative round-3 plan — read in full), the third SPEC-DEVELOPMENT triumvirate. The plan grounds I1/I2/I4/I5/I6/I7 on direct source reads + git history + the landed W9/W10 specs + the deep-audit lane notes:

- `audit/feedback/i-_PLAN.md` (the BINDING synthesis — read in full) — §0 the binding state at plan time (the three-way stage split converging to ONE register; the W1 keystone I2 extends; the engine fence), §1 the TWO-wave structure decision (W11 = stage-and-layout gestalt; W12 = standardize/decompose/audit gestalt) + rationale, §2 the per-item W11 clauses (I5/I4/I1/I2/I6/I7), §4 the consolidated gate list, §5 the supersede-map + the I5/G8 reversal, §6 the W1/W9/W10 reconciliation, §7 the USER-DECISION forks, §8 the convergence sequencing.
- `audit/feedback/i-r5-sequence-path.md` — the round-3 research note (sequence + path, I3 focus; LANDED at plan-author time). Its §0 verified the three-way stage split live (easing/spring full-bleed; sequence/path bare `cartoon-surface`); §3.2 the `useDragScrub` extraction at the 3-consumer threshold (folded into W12's I8, but the shared seam I3 consumes is named here). Nothing in i-r5 contradicts this wave; the §I5/I8 clauses are co-authored with it.
- Direct source reads (cited `file:line` below) on `tranche-h-impl` HEAD `f064cc1` + the W10 working-tree `M` files — every gate's born-RED state re-verified against live source at spec-author time.

**Research-lane note (honest provenance).** Of the five round-3 research notes, only `i-r5` had LANDED at plan-author time; `i-r1..i-r4` were being written concurrently by the other research lanes and were MISSING. This wave therefore grounds I1/I2/I4/I5/I6/I7 on direct source reads + git history + the landed W9/W10 specs + the deep-audit lane notes (`a-scene-state-machine`, `a-styling-idioms`, `a-easing-editor`, `a-demo-architecture`, `a-scene-square-easing`). When `i-r1..i-r4` land, the IMPL lead reconciles them against this wave; nothing here contradicts i-r5.

Synthesis home: `audit/feedback/i-_PLAN.md` (entire). This wave SUPERSEDES specific W10 decisions whose impl is still settling (most importantly W10 G8's full-bleed) and REFINES landed W9 F1/F2 — the §supersede-map below is the honest ledger.

## §The state, verified (file:line / live anchors from the lanes)

- **THE HEADLINE — the four STAGE scenes are in THREE different states today, converging to ONE standard glass-card register (the load-bearing fact, from `i-_PLAN.md §0.2` + verified live).** (a) **easing/spring → FULL-BLEED, no card** (W10 G8 just applied): `EasingTarget.vue:1-10` (the `<!-- G8 (H.W10.S5) — FULL-BLEED stage … card is DROPPED -->` comment + the `easing-target … h-full w-full` div, no `<Card>`), `SpringTarget.vue:1-8` (the same G8 comment + a bare `flex … h-full w-full` div), `StartingStyleTarget.vue`. (b) **sequence/path → `glass-resting cartoon-surface` bare-class card** (W10 G8 NEVER reached them): `SequenceTarget.vue:3` + `MotionPathTarget.vue:3` — both `<div class="glass-resting cartoon-surface w-full flex-1 min-h-0 flex flex-col overflow-hidden">`, which carries **NO `border-radius`** (the glass-ui `cartoon-surface` `@utility` is decoration-only — 2px border + offset-stamp shadow + hover-lift, NO radius; the radius lives on the `<Card>` root's `rounded-card`, `node_modules/@mkbabb/glass-ui/dist/styles/cards.css:33-48`). So sequence/path are SQUARE-corner cartoon panels — the exact I4 defect. (c) **I5's target → ALL FOUR on ONE standard non-cartoon glass `<Card>`** (glass, resting, `rounded-card` by construction). **This three-way split converging to ONE card register is the I5/I8/I12 isomorphism win** — confirmed at the verified source heads above.
- **The control PANELS keep cartoon+quiet (W2/W9 — the orthogonal register survives).** `surface="cartoon" tier="quiet"` on the ~14 sidebar/ribbon Cards is UNTOUCHED by I5. Two registers, cleanly separated: cartoon = the control chrome (the direct-manipulation panels); glass-resting = the protagonist plate (the stage subject). (`i-_PLAN.md §0.3`)
- **I2 — the control-surface VISIBILITY has no formal owner today.** `AnimationControls.vue:20-22` hard-codes THREE tab triggers (`<TabsTrigger value="controls">Controls`, `value="keyframes">Keyframes`, `value="timeline">Timeline`) for EVERY scene; the model is `storedControls.selectedControl` (`:8-9`). Each scene then poke-sets `selectedControl`/`isControlsPanelOpen` ad-hoc with reka-tab-fallback HACKS **(the Scene files live in `demo/app/scenes/`, NOT beside their Targets — cite the full path)**: `demo/app/scenes/EasingScene.vue:26-32` (`selectedControl = "easing"` + the comment "reka falls back to its first built-in tab (`controls`)" + a `nextTick` re-set at `:38`); `demo/app/scenes/SpringScene.vue:62-74` (the same fallback shape); `demo/app/scenes/MotionPathScene.vue:25` (`isControlsPanelOpen = false`); `demo/app/scenes/SequenceScene.vue:28` (`isControlsPanelOpen = false` — the FOURTH poke-set, completing the self-contained-stage set with motion-path); `demo/app/scenes/CubeScene.vue:78-80` (the matrix-controls tab). So the easing scene renders the hard-coded keyframes/timeline tabs that mean nothing for it. **This is the per-scene control-surface VISIBILITY that the W1 FSM owns the scene+playback axes for but has no third axis for.** (`i-_PLAN.md §2 I2`)
- **I1 — W9 F1's per-row split derives a PER-ROW `auto` label width, not a uniform column.** The W9 F1 fix restored label-LEFT/value-RIGHT rows via per-row `grid-template-columns: auto 1fr` — verified live at `EasingSidebar.vue:144-150` (`.panel-content :deep(.labeled-field) { … grid-template-columns: auto 1fr }`), the standard `AnimationControlsControls.vue` rows (the W9 F1 site), `LayerConfigPanel.vue`. Each row derives its OWN `auto` label width → labels are NOT uniform across rows. AND the sequence labels are uniform-by-MAGIC-LITERAL: `SequenceTarget.vue:25-27` (`<span class="… shrink-0 w-20 text-right pr-2">`) — a `w-20` fixed literal, not a derived track. (`i-_PLAN.md §2 I1`)
- **I6 — the cubic-bézier panel nests a Card inside a Card.** The bezier detail editor is rendered as `<Card surface="cartoon" tier="quiet" class="easing-editor …">` (`TimingFunctionPanel.vue:15`) INSIDE the controls host (`AnimationControlsControls.vue:119` renders `<TimingFunctionPanel>` within the controls-pane Card) — card-in-card. The steps variant `TimingFunctionPanel.vue:87` is the same shape. (`i-_PLAN.md §2 I6`)
- **I7 — the in-panel bezier canvas is capped at 220px + a redundant "editing:" subtitle.** W9 F2 TIGHTENED the in-panel canvas ceiling to `block-size: clamp(160px, 38cqi, 220px); max-block-size: 220px` (`TimingFunctionPanel.vue:251-252`) to fit-without-scroll, which made it SMALL. The "editing: {{ editingCurveName }}" subtitle is `TimingFunctionPanel.vue:33` (`<p v-if="editingCurveName" class="text-mono-caption …">editing: {{ editingCurveName }}</p>`). (`i-_PLAN.md §2 I7`)
- **I4 — the un-rounded cartoon div is impossible to fix safely at the callsite.** The DEFECT is `SequenceTarget.vue:3` + `MotionPathTarget.vue:3` (bare `cartoon-surface` divs, `border-radius: 0`). The PRIMITIVE is the glass-ui `cartoon-surface` `@utility` (`cards.css:33-48` — no `border-radius` by design). Leaving a bare-class card square is too easy because the radius lives on the `<Card>` root, not the utility. (`i-_PLAN.md §2 I4`)
- **The engine is FENCED.** The only >500-line files are `src/animation/` (engine.ts 1375, animations.ts 870, group.ts 772, sequence.ts 628, spring.ts 491) — ALREADY-SOTA, NOT touched (`H.md §Mandate inv ζ`; the I10 NOTE targets DEMO only). This wave is 100% demo-side. (`i-_PLAN.md §0.5`)

## §Goal

Land the stage-card register the user's round-3 feedback asks for — observed live on `tranche-h-impl` while W10 is still settling. **The headline is the I5 reversal of W10 G8** (the load-bearing supersede): the easing, spring, sequence, AND path STAGE scenes converge from THREE states (full-bleed / cartoon-square / target) to ONE standard, NON-cartoon glass `<Card>` (`tier="resting" surface="glass"`, rounded by construction). The control PANELS stay cartoon+quiet (W2/W9). Around that headline: bake card-rounding by CONSUMPTION not patch (I4 — the `<Card>` carries `rounded-card`, so sequence/path round FOR FREE alongside I5; the deeper primitive default is a glass-ui HANDOFF); promote the per-row `[auto_1fr]` label rules to a parent `subgrid` so the label column is UNIFORM across rows by construction (I1, the `w-20` magic literal dies); EXTEND the W1 FSM with a per-scene control-VISIBILITY DFA (I2 — a `controlSurfaces` table keyed by scene, the third orthogonal axis; the easing scene shows ONLY easing; the reka-fallback hacks DIE; NO undefined behavior across the (scene→scene) navigation matrix; state suspends-and-fully-resumes on `SCENE_READY`; a NAMED transition perf bench MEASURE-FIRST, not asserted); drop the inner bezier Card so the bezier editor flows into the parent controls Card directly (I6); GROW the in-panel canvas above the W9 220px ceiling + DELETE the "editing:" subtitle (I7, which composes with I6 — the freed vertical space). Net-deletion-leaning, the four stage protagonists converge to ONE register, every fix idiomatic and gestalt — NO workaround, NO legacy beside its replacement (the full-bleed div / the bare cartoon div / the inner card / the per-row `auto` width / the reka hacks DELETE in one motion), styling ISOMORPHIC unless a HIGHLY befitting NAMED delta (the glass-card stage IS the isomorphic convergence; the `subgrid`-derived uniform label width is the structural delta), MEASURE-FIRST (the I2 perf budget names `bench:scene-transition`, does not assert), the W1 keystone EXTENDED not re-authored, the engine FENCED (inv ζ), the glass-ui rounded-primitive an inv-16 HANDOFF. This is the chronic-closure discipline in ACTION: the user caught the W10 full-bleed BEFORE the H.W8 golden baseline locked it — the running demo is the truth.

## §Scope (each S = WHAT + WHY)

- **S1 — I5: GLASS-CARD ENCAPSULATION (REVERSES W10 G8 full-bleed) — the headline.** WHAT: the easing, spring, sequence, AND path STAGE scenes get a STANDARD, NON-cartoon glass `<Card>` (default `tier="resting" surface="glass"`). REVERT the full-bleed on `EasingTarget.vue:2-10` (the `easing-target` div → wrap in / become `<Card>`; delete the `<!-- G8 — FULL-BLEED stage -->` comment), `SpringTarget.vue:2-8`, `StartingStyleTarget.vue`; SWAP `glass-resting cartoon-surface` → `<Card>` on `SequenceTarget.vue:3`, `MotionPathTarget.vue:3`. Consume the glass-ui `<Card>` primitive (rounded by construction). KEEP each scene's inner flex column + header/body/footer structure EXACTLY — only the OUTER container changes (full-bleed div / bare cartoon div → `<Card>`). The card sits in the `[stage]` track, dock-contained by the SURVIVING `.stage-cell` primitive (`AnimationControlsGroup.vue:58,364-366` — `padding-block: var(--dock-band-reserve)`). The control PANELS stay cartoon+quiet (W2/W9 — UNTOUCHED). WHY: the user observed the W10 full-bleed easing stage live and wants a card background — "a standard, non-cartoon glass one" — on all four stage scenes. This is a SIMPLER state than today's three-way split: one card register for all four protagonists (the I8/I12 isomorphism). **NAMED delta to consider (IMPL/MEASURE-FIRST, FORK I5-shadow):** the stage card may earn `shadow={false}` since it is the protagonist plate, not nested chrome — lead's call set MEASURE-FIRST against the live render, not asserted here. **These four MUST converge to ONE register** — any scene left full-bleed or left bare-cartoon-square breaks the isomorphism. (`i-_PLAN.md §2 I5 · §5`)

- **S2 — I4: card-rounding by CONSUMPTION (in-demo) + the deeper primitive default (glass-ui HANDOFF).** WHAT, at TWO altitudes: **(in-demo, lands here)** — the S1 swap to `<Card>` makes sequence/path rounded with ZERO ad-hoc `rounded-*` literal (consume the primitive, don't patch the demo). I4 closes FOR FREE alongside I5 for the stage scenes. **(the deeper ask — a glass-ui HANDOFF, inv-16)** — bake rounding so a `cartoon-surface`-only div is IMPOSSIBLE to leave square (e.g. `cartoon-surface` gains a `border-radius: var(--radius-card)` default, OR a `rounded-card`-carrying `cartoon-card` primitive). This is glass-ui-owned (consumed published, sibling = HANDOFF) — RECORD it as a born-RED-paired HANDOFF, do NOT patch in kf. WHY: "motion-path's card is NOT rounded — it should be impossible." A `cartoon-surface`-only `<div>` is square because the radius lives on the `<Card>` root, not the utility — leaving a bare-class card square is too easy. Composes into S1. (`i-_PLAN.md §2 I4 · §glass-ui HANDOFFs`)

- **S3 — I1: uniform label width via a CLEAN GRID + SUB-GRID (REFINES W9 F1).** WHAT: promote the per-row `grid-cols-[auto_1fr]` rules to a PARENT `grid` whose rows share ONE label track via CSS `subgrid` — parent `display: grid; grid-template-columns: [label] auto [value] 1fr`, each `.labeled-field` row `display: grid; grid-template-columns: subgrid; grid-column: 1 / -1`. Targets: `EasingSidebar.vue:144-150` (the per-row `auto 1fr` rule), the standard `AnimationControlsControls.vue` W9 F1 rows, `LayerConfigPanel.vue`; AND the fixed-width sequence labels `SequenceTarget.vue:25-27` (the `w-20 text-right` magic literal — replaced by subgrid participation). The label column then derives from the WIDEST label across all rows — uniform by construction, the `w-20` magic literal DIES. **The durable home is a glass-ui HANDOFF** (the W9-BOOKed `LabeledField orientation="horizontal"` extended to subgrid-participation); the demo-side parent-subgrid wrapper lands TODAY (born-GREEN path B, mirroring `AssetPropertiesPanel.vue:6`). WHY: W9 F1 restored label-left rows but each row derives its OWN `auto` label width — labels are NOT uniform across rows. The user wants a UNIFORM label column across all rows (one derived width, not per-row auto). The W9 F1 row-shape direction (label-left/value-right, one column) is PRESERVED and STRENGTHENED; only the per-row-auto-width is replaced by the subgrid-derived uniform width. (`i-_PLAN.md §2 I1`)

- **S4 — I2: THE SCENE-STATE DFA (the per-scene control-VISIBILITY machine; EXTENDS W1).** WHAT: EXTEND the W1 FSM with an orthogonal **control-surface axis** — a static per-scene table `controlSurfaces: Record<SceneId, ControlSurface[]>` where `ControlSurface ∈ {controls, keyframes, timeline, easing, spring, matrix-controls, …}`. The DFA enumerates EXACTLY which surfaces are VALID per scene: easing → `[easing]`; cube → `[controls, keyframes, timeline, matrix-controls]`; sequence → `[]` / `[transport-only]` (self-contained, `isControlsPanelOpen = false`); path → `[]`. `AnimationControls.vue:8-9,20-22` renders the tab triggers FROM this table (NOT the hard-coded `controls|keyframes|timeline` list), so the easing scene shows ONLY easing — the reka-fallback hacks DIE (the table is the explicit owner the hacks stood in for): `EasingScene.vue:26-32`, `SpringScene.vue:62-74`, `MotionPathScene.vue:25`. The transition through the table routes through the W1 `SCENE_READY` event (state suspend on leave, full resume on the explicit ready event — the W1 contract, NOT re-authored). **MEASURE-FIRST (the I2 perf budget — name a bench, do not assert):** a NAMED transition bench `bench:scene-transition` (vitest bench OR a Playwright `performance.measure` harness gated in `proof:scene-transition-perf`) measures the cross-scene navigate + control-surface re-render cost and asserts a BUDGET (e.g. transition settle < N ms at 1280/1440 — the budget bound MEASURE-FIRST from the current W1 baseline during impl, NOT guessed). **This is the W1 keystone EXTENDED, not duplicated:** W1 owns scene+playback (two orthogonal axes); I2 adds the control-surface DFA keyed by scene (the third). Do NOT touch `useSceneMachine`'s reducer beyond adding the orthogonal table + the surface-projection. WHY: today the easing page shows keyframes/timeline tabs that mean nothing for it; the per-scene poke-sets are ad-hoc reka-fallback HACKS with no formal owner. The user wants every scene's valid states + which controls it shows fully MAPPED via a state machine/DFA, NO undefined behavior navigating between scenes AND all controls, proper STATE SUSPENSION + FULL RESUME, performance MONITORED. (`i-_PLAN.md §2 I2 · §6 W1-reconciliation`)

- **S5 — I6: CUBIC BÉZIER double card (remove the INNER-MOST card; REFINES W9 F2 / W10).** WHAT: drop the INNER `<Card>` wrapper (`TimingFunctionPanel.vue:15`) — the bezier editor's `CardHeader`/`CardContent` flow into the parent controls Card's content directly (the detail panel is a VIEW within the controls Card, not its own card). The steps variant `:87` is the same shape — drop its inner Card too. Verify the host already provides the framed surface (`AnimationControlsControls.vue` is itself a `Card surface="cartoon"`) so the inner card is pure duplication. NO legacy beside the replacement (delete the inner Card, don't `display:none` it). WHY: the bezier panel nests a card inside a card. KISS·DRY — no card-in-card. The W9 F2 "title-left / dismiss-right" header pattern is PRESERVED, just re-homed onto the parent flow (the header/back move to the parent content). (`i-_PLAN.md §2 I6`)

- **S6 — I7: the EASING PANEL grows (make the bezier controls BIG; remove the "editing:" subtitle; REFINES W9 F2).** WHAT: RAISE the in-panel canvas ceiling (`TimingFunctionPanel.vue:251-252`, the `:deep(.easing-curve-canvas)` `clamp(160px, 38cqi, 220px); max-block-size: 220px`) so the editor fills the container height — the S5 inner-card removal frees vertical space (the two compose: remove the inner card → more room → grow the canvas to fill it). The square LAW (`aspect-ratio: 1` from `EasingCurveCanvas`) is PRESERVED — only the `block-size`/`max-block-size` ceiling rises. DELETE the "editing: …" subtitle row (`TimingFunctionPanel.vue:33`) — it is redundant (the curve + the live `cubic-bezier(...)` readout already say what is being edited). MEASURE-FIRST: confirm the grown canvas still fits the detail-panel cap without re-introducing scroll (compose with W9's `proof:bezier-no-scroll`). The exact grown ceiling is a tuning value set MEASURE-FIRST during impl. WHY: the easing panel is too small — the user wants the bezier controls as BIG as possible within the container, TALLER; and the "editing: ease-in-out" sub-title removed. The W9 F2 fit-without-scroll invariant is PRESERVED (the grown canvas must still fit); the ceiling rises within that constraint. (`i-_PLAN.md §2 I7`)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

The 7 H.W11 gates, each BITES + cites a `file:line`/live anchor, each born-RED on `tranche-h-impl` today (with W10's full-bleed as the immediately-superseded baseline). Gate-authoring home: per `H.W8.md §Scope` the gate-regime wave OWNS gate authoring; H.W11's NEW born-RED clauses are wired into the `proof:*` set + `proof:all`, and the H.W8 `proof:visual-lock` named-region matrix captures the H.W11 glass-card-stage / uniform-subgrid / de-nested-bezier GOLDEN states AFTER H.W11+H.W12 land. Every browser gate settle-gates on the H.W1 FSM resting.

| Gate | Item(s) | Disposition | BITE (born-RED today → green on fix) |
|------|---------|-------------|--------------------------------------|
| `proof:stage-glass-card` | I5 | **NEW** | for EACH of {easing, spring, sequence, path} the stage SUBJECT root resolves a non-cartoon glass `<Card>`: computed `border-radius` is non-zero (the `rounded-card` token resolves) AND `backdrop-filter !== 'none'` AND the root is NOT `cartoon-surface` AND NOT the bare full-bleed div. Reds TODAY on easing/spring (full-bleed, no card → no radius / no backdrop — `EasingTarget.vue:2-10`, `SpringTarget.vue:2-8`) AND on sequence/path (`cartoon-surface` → `border-radius: 0` — `SequenceTarget.vue:3`, `MotionPathTarget.vue:3`). Greens only when all four resolve a standard glass card. Settle-gated on H.W1; mirrors the W10 `proof:scene-card-rounded` computed-style plumbing (it is the I5 SUPERSESSION of that gate's full-bleed branch). |
| `proof:card-rounded-primitive` | I4 | **NEW** (+ glass-ui HANDOFF, born-RED) | (kf demo half) ZERO bare-class `cartoon-surface` `<div>` stage roots remain (static grep: no `class="…cartoon-surface…"` on a stage `*Target.vue` root); every stage card resolves non-zero computed `border-radius`. Reds TODAY on `SequenceTarget.vue:3`/`MotionPathTarget.vue:3`; greens on the I5 `<Card>` swap. (glass-ui HANDOFF half, born-RED) — a `cartoon-surface`-only element resolves a non-zero `border-radius` FROM the primitive itself; reds until glass-ui ships the default, stays born-RED until the published bump (the kf half greens now on the swap). |
| `proof:label-subgrid` | I1 | **NEW** | within each `Labeled*`-bearing panel, ALL leaf-row label cells resolve the SAME computed width (within ±1px tolerance) AND the parent uses `subgrid` (the rows participate in a shared track, not independent `auto`). Reds TODAY (per-row `auto` → labels of different widths across rows — `EasingSidebar.vue:144-150`; sequence `w-20` is a magic literal not a derived track — `SequenceTarget.vue:25-27`); greens on the subgrid. **Non-vacuity:** assert ≥3 rows with differing label text so the uniform width is a real constraint, not a one-row trivial pass. Settle-gated on H.W1; mirrors the W9 `proof:single-column-pack` row-measurement plumbing (the F1 one-column invariant stays GREEN; this ADDS the uniform-width clause). |
| `proof:scene-control-dfa` | I2 | **NEW** | for EVERY scene the rendered control-surface set EXACTLY equals the DFA table entry (no extra, no missing): easing renders ONLY the easing surface (NO `keyframes`/`timeline` tab node exists — the `AnimationControls.vue:20-22` triggers come from the table); sequence/path render NO control panel (`isControlsPanelOpen=false`); cube renders its full set + matrix-controls. AND the navigation matrix is TOTAL: for every (scene → scene) pair the resulting control-surface set is DEFINED (no undefined behavior — every cell in the scene×control-surface table resolves). Reds TODAY (easing shows the hard-coded keyframes/timeline triggers via `AnimationControls.vue:20-22`; the reka-fallback hacks `EasingScene.vue:26-32` etc. stand in for the missing owner); greens on the table-driven render. Builds on the W1 `proof:scene-isolation` plumbing. |
| `proof:scene-transition-perf` | I2 | **NEW** (MEASURE-FIRST budget) | the NAMED `bench:scene-transition` budget: the cross-scene navigate + control-surface re-render settles within the measured budget (the bound set MEASURE-FIRST from the current W1 baseline during impl, NOT guessed); AND state suspends on leave and FULLY resumes on `SCENE_READY` (round-trip identity, reusing the W1 `proof:scene-machine-irrefragable` identity-field-set, EXTENDED with the `{selectedControl, control-surfaces}` projection). Reds if the transition exceeds budget OR loses control-surface state across a round-trip. Settle-gated on H.W1; EXTENDS the W1 identity gate (it does not duplicate it — the W1 field set GAINS the control-surface projection). |
| `proof:bezier-single-card` | I6 | **NEW** | the cubic-bézier detail panel has EXACTLY ONE `<Card>` ancestor between the bezier canvas and the controls-pane root (no card-in-card). Reds TODAY (the inner `TimingFunctionPanel.vue:15` Card nested inside the `AnimationControlsControls.vue:119` controls Card → 2 cards); greens on the inner-card removal. Static parse + computed-depth check. |
| `proof:bezier-panel-taller` | I7 | **NEW** | the in-panel `.easing-curve-canvas` resolved `block-size` is GREATER than the W9 220px ceiling (the panel grew) AND no "editing:" text node exists in `TimingFunctionPanel` (grep: the `editing:` subtitle at `:33` removed) AND the panel still fits (`scrollHeight ≤ clientHeight`, reusing W9 `proof:bezier-no-scroll` plumbing). Reds TODAY (canvas capped at 220px — `TimingFunctionPanel.vue:251-252`; the "editing:" `<p>` at `:33` present); greens on the grow + subtitle removal. |

**KEPT UNCHANGED / SUPERSEDED:** `proof:scene-card-rounded` (W10) — its full-bleed branch is SUPERSEDED by `proof:stage-glass-card` (the stage now resolves a card, not a full-bleed); the IMPL lead folds the W10 gate's "OR is the full-bleed bg-less stage" disjunct OUT (the disjunct's subject — full-bleed — no longer exists; this is the I5/G8 reversal at the gate level). `proof:single-column-pack` (W9 F1) stays GREEN (the one-column invariant holds — the subgrid keeps ONE left edge per row); `proof:bezier-no-scroll` (W9 F2) stays GREEN (the grown canvas must still fit — `proof:bezier-panel-taller` reuses its plumbing); the W1 `proof:scene-machine-irrefragable`/`proof:scene-isolation`/`proof:single-writer`/`proof:no-route-storm` plumbing is REUSED, not duplicated.

**The §Mandate bar — MUST bite, no vacuity.** Each H.W11 gate asserts an EXACT live measurement or static fact the lanes captured: the full-bleed easing/spring stage (no card → no radius/backdrop), the `border-radius: 0` cartoon sequence/path divs, the per-row `auto` label widths + the `w-20` magic literal, the hard-coded 3-tab list, the inner bezier card nested in the controls card, the 220px canvas cap + the "editing:" subtitle. None passes vacuously — `proof:label-subgrid` asserts ≥3 differing-text rows so the uniform width is a real constraint; `proof:scene-control-dfa` asserts the navigation matrix is TOTAL (no undefined cell); `proof:scene-transition-perf` is a NAMED bench (MEASURE-FIRST budget, not an assertion). No gate is satisfied by a `display:none`/`!important` suppression — the inner card DIES (subtree deleted), the full-bleed div BECOMES a `<Card>`, the reka hacks DIE (replaced by the explicit table), the canvas GROWS (not a measured-but-suppressed value).

## §Folds

- `audit/feedback/i-_PLAN.md` (the BINDING synthesis — §0 the binding state / the three-way stage split / the W1 keystone / the engine fence; §1 the two-wave decision — W11 = stage-and-layout gestalt; §2 the per-item W11 clauses I5/I4/I1/I2/I6/I7; §4 the gate list; §5 the supersede-map + the I5/G8 reversal; §6 the W1/W9/W10 reconciliation; §7 the forks; §8 the convergence) — every clause re-verified against live source at spec-author time (the full-bleed easing/spring stage, the bare-cartoon sequence/path divs, the hard-coded 3-tab list, the per-row `auto` labels, the inner bezier card, the 220px ceiling + the "editing:" subtitle — all CONFIRMED present).
- `audit/feedback/i-r5-sequence-path.md §0` (the live verification of the three-way stage split — easing/spring full-bleed, sequence/path bare `cartoon-surface`; the I5 target = one card register) — the I5 headline's ground truth. §3.2 names the `useDragScrub` seam (folded into W12's I8) the W11 DFA enumerates sequence/path as `{stage-only}` BEFORE.
- The W10 `H.W10.md §Scope G8/S5` (the full-bleed-no-bg SURFACE consequence — the immediately-superseded baseline I5 reverses) + the W10 `proof:scene-card-rounded` (whose full-bleed branch I5 supersedes) — RECORD that W10's full-bleed was a decided form (FORK A default) the user has now reversed; the `.stage-cell` dock-band-reserve PRIMITIVE half SURVIVES.
- The W9 `H.W9.md §Scope S3/S4` (F1's per-row `[auto_1fr]` rows that I1 promotes to subgrid; F2's bezier-panel form — the inner card + the 220px ceiling + the "editing:" subtitle — that I6/I7 refine) — the landed forms this wave REFINES, not forks.

## §supersede-map (which landed/in-flight W10/W9 decision each item revises + why precept-consistent)

H.W11 is a NEW corrective wave that SUPERSEDES specific W10 decisions whose impl is still settling (most importantly W10 G8's full-bleed) and REFINES landed W9 F1/F2. Editing the W10/W9 wave docs' §Goal would rewrite the record of what was DECIDED vs what was REVISED — this map is the honest ledger. (`i-_PLAN.md §5`)

| Item | Landed/in-flight decision SUPERSEDED or EXTENDED | The revision | Why consistent with the precepts |
|------|--------------------------------------------------|--------------|----------------------------------|
| **I5** | **W10 G8's full-bleed-no-bg SURFACE consequence** (`EasingTarget.vue:2-10`, `SpringTarget.vue:2-8` full-bleed; `SequenceTarget.vue:3`/`MotionPathTarget.vue:3` un-rounded cartoon) — W10 FORK A's full-bleed default | ALL FOUR stage scenes → ONE standard non-cartoon glass `<Card>` (rounded by construction) | the user's direct round-3 direction (observed live); W10 FORK A explicitly flagged "needs the user's call ONLY if they reject full-bleed" — they did; consume the `<Card>` primitive (KISS·DRY, no ad-hoc `rounded-*`); the four scenes converge to ONE register (the I8/I12 isomorphism); **the `.stage-cell` LAYOUT PRIMITIVE half of G8 SURVIVES** (only the surface consequence reverses; `dock-inset` stays deleted — no legacy beside the replacement) |
| **I4** | (no landed decision) the bare `cartoon-surface` divs miss `rounded-card` | sequence/path consume `<Card>` (rounded by construction, via I5); the deeper primitive default is a glass-ui HANDOFF | REUSE the primitive (one radius token); the cross-repo half is inv-16 HANDOFF (sibling = handoff), born-RED-paired |
| **I1** | **W9 F1** — per-row `grid-cols-[auto_1fr]` (each row its own `auto` label width) | parent `subgrid` → ONE uniform label column across rows; the `w-20` literal dies | REFINES, doesn't fork — the F1 label-left/one-column direction is PRESERVED + strengthened; the durable home is the glass-ui `LabeledField` HANDOFF |
| **I2** | **W1 FSM** (EXTENDS, not supersedes) + the per-scene reka-tab-fallback HACKS (`EasingScene.vue:26-32` etc.) | a per-scene control-VISIBILITY DFA (a 3rd orthogonal axis on W1) + a transition perf budget; the table-driven render REPLACES the hard-coded 3-tab list + the fallback hacks | the W1 keystone is EXTENDED not re-authored (the spine: I2 deepens W1); the hacks are SUPERSEDED by the explicit owner they stood in for; MEASURE-FIRST (the named bench, not an assertion) |
| **I6** | **W9 F2 / W10** bezier-panel form (the inner `<Card>` with the baked-in header) | drop the inner Card; the header/back flow into the parent controls Card | KISS·DRY — no card-in-card; the F2 title-left/dismiss-right pattern is PRESERVED, re-homed; no legacy beside the replacement |
| **I7** | **W9 F2** (the 220px in-panel ceiling) + the "editing:" subtitle (W4/W9-era) | grow the canvas within the fit-without-scroll constraint; delete the subtitle | REFINES F2 (the fit invariant holds; the ceiling rises); the subtitle is redundant clutter (the curve + readout already say it) |

**THE I5/G8 REVERSAL (stated explicitly, the load-bearing supersede):** W10 G8 = the `.stage-cell` dock-band-reserve PRIMITIVE (altitude a) + the full-bleed-no-bg SURFACE (altitude b). **I5 reverses ONLY altitude (b):** the full-bleed easing/spring stage GAINS a standard glass `<Card>` back; the un-rounded cartoon sequence/path cards SWAP to the same standard glass `<Card>`. **Altitude (a) — the `.stage-cell` primitive — SURVIVES untouched** (it contains any stage subject, card or not, between the docks; the `dock-inset` deletion STAYS — no legacy beside the replacement). The four stage scenes converge from THREE states (full-bleed / cartoon-square / target) to ONE standard glass-card register. Treat W10's full-bleed as the immediately-superseded baseline. FORK A of W10 (full-bleed vs contained-card-with-bg) is now RESOLVED by the user toward the contained card.

## §USER-DECISION fork — I5-shadow (the ONE NAMED-delta fork; default MEASURE-FIRST)

**FORK I5-shadow (NAMED delta) — the stage `<Card>` with `shadow` vs `shadow={false}`.** RECOMMENDED: MEASURE-FIRST against the live render — the protagonist plate may read cleaner without a nested shadow. Lead's call set during impl, NOT asserted here. Not a hard user fork.

**No other W11 item needs a user decision** — I1/I2/I4/I5/I6/I7 are decisive (the source-grounded idiomatic fix is unambiguous; the I5 card register is the user's direct round-3 direction). The I2 perf-budget NUMBER, the I1 subgrid column-gap, and the I7 canvas ceiling are MEASURE-FIRST / lead-tuning calls set during impl, not user forks. (FORK W — the two-wave-vs-one structure — is documented in `i-_PLAN.md §7.1` as a lead-tuning call, not a hard user fork; this wave adopts TWO.) (`i-_PLAN.md §7`)

## §sequencing (the convergent landing within H.W11)

1. **I5 (the glass-card register) — S1.** REVERT the full-bleed (easing/spring) + SWAP the bare cartoon divs (sequence/path) → ONE standard glass `<Card>`. The headline; the container all the W12 enrichments live inside. Closes I4's stage half FOR FREE (S2 — the `<Card>` is rounded by construction).
2. **I4 (rounding) — S2.** The in-demo half rides I5 (consume the primitive); the glass-ui rounded-primitive default is RECORDED as a born-RED-paired HANDOFF.
3. **I1 (the label subgrid) — S3.** Promote the per-row `[auto_1fr]` → a parent `subgrid`; the `w-20` magic literal dies; `proof:label-subgrid` adds the uniform-width clause (the W9 F1 one-column invariant stays GREEN).
4. **I2 (the control-surface DFA) — S4.** EXTEND the W1 FSM with the `controlSurfaces` table; render the tabs from the table; the reka-fallback hacks DIE; name `bench:scene-transition` (MEASURE-FIRST budget).
5. **I6 (de-nest the bezier) — S5.** Drop the inner Card; the bezier editor flows into the parent controls Card.
6. **I7 (grow the bezier) — S6.** RAISE the in-panel canvas ceiling (composes with S5's freed space) + DELETE the "editing:" subtitle.

**All AFTER H.W10, BEFORE H.W12 + H.W8's GOLDEN `proof:visual-lock` baseline capture** (so the glass-card stage / uniform subgrid / de-nested-grown bezier are carried by H.W12's enrichment and locked by H.W8). The born-RED clauses (the 7 NEW gates) red TODAY on `tranche-h-impl` (with W10's full-bleed as the superseded baseline), green on the H.W11 fixes — every gate BITES and cites a `file:line`/live anchor.

## §Design decisions (trade-offs RESOLVED)

- **I5 REVERSES W10 G8's full-bleed SURFACE — RESOLVED (the headline; the load-bearing supersede).** The four stage scenes converge from THREE states (full-bleed / cartoon-square / target) to ONE standard non-cartoon glass `<Card>`. W10's full-bleed is the immediately-superseded baseline (FORK A resolved toward the contained card by the user). The `.stage-cell` dock-band-reserve LAYOUT PRIMITIVE half of G8 SURVIVES — only the surface consequence reverses; `dock-inset` stays deleted (no legacy beside the replacement). (`i-_PLAN.md §5`)
- **TWO registers, cleanly separated — RESOLVED.** cartoon+quiet = the control chrome (the direct-manipulation PANELS, W2/W9 UNTOUCHED); glass-resting = the protagonist plate (the STAGE subject, I5). I5's glass-card is orthogonal to the panel register — the W9 calm cartoon+quiet panel register is not touched. (`i-_PLAN.md §0.3`)
- **I2 EXTENDS the W1 keystone, never re-authors — RESOLVED.** W1 owns scene+playback (two orthogonal axes) + the route reconcile + the one-way projections. I2 adds the control-VISIBILITY axis (a per-scene `controlSurfaces` table) as a THIRD orthogonal axis on the SAME machine — the W1 `SCENE_READY` suspend/resume contract carries the control-surface state; the round-trip identity EXTENDS the W1 `proof:scene-machine-irrefragable` field set with `{selectedControl, control-surfaces}`. The W1 plumbing is REUSED, not duplicated. Do NOT touch `useSceneMachine`'s reducer beyond the orthogonal table + the surface-projection. (`i-_PLAN.md §6 W1-reconciliation`)
- **The I2 perf budget is MEASURE-FIRST, not asserted — RESOLVED.** `bench:scene-transition` is a NAMED bench (vitest bench OR a Playwright `performance.measure` harness gated in `proof:scene-transition-perf`); the budget bound is set from a measured W1 baseline during impl, NOT guessed in the spec. (`i-_PLAN.md §2 I2`)
- **I4 closes for free alongside I5 in the demo; the deeper primitive is a glass-ui HANDOFF — RESOLVED.** The S1 `<Card>` swap rounds sequence/path with ZERO ad-hoc `rounded-*` literal (consume the primitive). The deeper "impossible to leave square" ask (a `cartoon-surface` default radius) is glass-ui-owned (inv-16, sibling = HANDOFF) — born-RED-paired, NOT patched in kf. (`i-_PLAN.md §2 I4`)
- **I6+I7 compose — RESOLVED.** Removing the inner bezier Card (I6) frees vertical space; growing the in-panel canvas (I7) fills it. The square LAW is preserved (only the ceiling rises); the W9 fit-without-scroll invariant holds (the grown canvas must still fit). The "editing:" subtitle is redundant clutter (the curve + readout already say it). (`i-_PLAN.md §2 I6/I7`)
- **This wave SUPERSEDES, it does not REWRITE — RESOLVED.** H.W11 is a NEW corrective wave AFTER W10, BEFORE H.W12 + H.W8's golden baseline; the §supersede-map is the honest ledger of which W10/W9 decision each item revises + why precept-consistent. The chronic-closure discipline is in ACTION — the user caught the W10 full-bleed (I5), the W9 per-row-auto labels (I1), the W9/W10 bezier double-card + tightness (I6/I7), and the un-rounded cartoon cards (I4) BEFORE the H.W8 golden baseline locked them; the running demo is the truth. No engine touched (inv ζ / FENCED); all DEMO-side (I10); the glass-ui rounded-primitive + LabeledField-subgrid are inv-16 HANDOFFs.
