# H.W5 — scene icons (inline-SVG currentColor family) + mode pertinence (MERGE Discrete→Spring) + per-mode interactivity + the cube/amiga scene-quality + demo-perf budget (the new AND original modes finished) (Band 3 · SOURCE — the demo modes)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (one themable inline-SVG icon family on the descriptor + a pertinence MERGE that removes a nav entry in one motion + the per-mode interactivity floor + the cube/amiga scene-quality/perf budget — A3 a real amiga-tessellate bug, A2/G1/G5 MEASURE-FIRST scene-perf, A5 the amiga pertinence call; all kf-owned demo code — no glass-ui internals, no engine edit beyond consuming public `ManualTimeline`/`SpringProgress`/`getPointAtLength`/`decay`) · **Scope:** `demo/@/components/custom/dock/ChromeDock.vue:20-30,171-172,180,194,210-211` (the dock-keyed `sceneIcons` `Record<string,string>` + the `<img :src>` render + the overloaded `<Home>` sentinel) → moved onto `demo/app/scenes.ts:7-14,54-123` (`SceneDescriptor.icon`) + `assets/icons/*` (the 3 `-sm` PNG + 3 orphan `-lg` PNG KILLed, the easing SVG hue→`currentColor`, the survivor SVGs authored) + `vite.config.ts` (the `?component` / `vite-svg-loader` inline-SVG seam) + the Discrete→Spring merge (`demo/spring/StartingStyleTarget.vue`, `demo/app/scenes/StartingStyleScene.vue`, `router.ts`, the nav) + per-mode interactivity (`demo/easing/EasingTarget.vue` + `EasingCurveCanvas` to stage; `demo/motion-path/MotionPathTarget.vue` + `ManualTimeline`; `demo/app/scenes/SquareScene.vue` KILL-candidate) + the cube/amiga scene-perf (`demo/amiga/utils.ts:9,17-24` the tessellate tile-loop, `demo/app/scenes/AmigaScene.vue:47` the dpr cap + the sphere-drive-or-KILL, the `scene-host` `contain: paint` + inactive-root `content-visibility`) + the de-dup of `springLinearStops` to ONE composable + a new `proof:scene-icons` / `proof:scene-parity` / `proof:scene-perf-budget` gate set · **DAG-deps:** **depends on H.W1** (the pertinence + interactivity reads are POISONED by the D12 route storm — every scene-deeplink lane observed `/#/sequence`→`/#/?anim=Rotations`, `/#/spring`→cube→square, `/#/square`→home; the FSM is a prerequisite, and the per-mode `proof:*-drag` locks gate behind it so they fail on interaction logic not routing churn); **depends on H.W0** (the StartingStyle contract-anim `serializeEasing` throw + the `"......"` lerp must die so the Discrete merge lands on a clean console); icons designed ONLY for survivors AFTER the pertinence verdict.

## §Provenance (the folded lanes)

- `a-scene-icons` **D8/F1–F6** (the PRIMARY — `sceneIcons` is a dock-keyed `Record<string,string>` of imported image URLs mapping ONLY cube/amiga/square/easing; 5-of-9 scenes wear `<Home>`; TWO unreconciled idioms (3 PNG screenshots + 1 hand-SVG); the gestalt = ONE inline-SVG `currentColor` family on `SceneDescriptor.icon` via `<component :is>`, KILL the PNGs — `a-scene-icons.md:14-43,139-217,279-309`).
- `a-icon-pipeline` **P1–P9** (the STAGE-4 truth: `<img :src>` is theme-blind BY CONSTRUCTION — an `<img>` paints SVG as a replaced element that cannot read the host `currentColor`, so even the vector easing SVG's baked `hsl(248…)` is as theme-blind as the PNGs; theming is a property of the REFERENCE mechanism (inline `<svg>` component), not the file format — `a-icon-pipeline.md:23-51,184-247,280-305`).
- `a-modes-pertinence` **D8/D11** (KEEP all 4 — each is the ONLY demo of a distinct PUBLIC primitive; MERGE Discrete→Spring (they triple-surface `springLinearStops`); 4 nav → 3; per-mode interactivity: Spring/Sequence/Discrete already MEET D11, Path is the laggard — `a-modes-pertinence.md:17-43,78-263`).
- `a-scene-spring-sequence` **A–F** (the primitive-coverage map + the `springLinearStops`/`springTimingFunction` triple-surface + the no-nav-icon gap + the contract-anim CSS-serializer throw — `a-scene-spring-sequence.md:24-58,177-199,300-316`), `a-scene-path-discrete` **F1–F8** (Path KEEP+ELEVATE inert→drag, Discrete KEEP+FIX the throw/dead-transport, both icon-less, the PATH_D-as-icon DRY proposal, the `springLinearStops` distinct-coverage RECORD — `a-scene-path-discrete.md:12-15,114-141,171-199`), `a-scene-square-easing` (square KILL-candidate / dead `SUPER_KEY` / the `"heyyyy"` placeholder; easing drag-the-dot-on-curve — `a-scene-square-easing.md:78-125,164-168,249-255`), `a-mode-interactivity` **H-MI-1..7** (the interactivity census + the per-mode model: easing promote `EasingCurveCanvas` to stage, motion-path `getPointAtLength`+`ManualTimeline`, square drag+`SpringProgress` else KILL, the `useDragScrub` MEASURE-FIRST BOOK — `a-mode-interactivity.md:50-229`).
- `a-scene-cube-amiga` **A2/A3/A5/A7/A9** (the per-scene quality of the TWO ORIGINAL stage scenes the icon set must also cover — the lane the gap-scorecard §1.1 dropped: A2 amiga `setPixelRatio(window.devicePixelRatio*2)` → up to 16× retina fragments (MEASURE-FIRST cap to `min(dpr,2)`), A3 `tesselateSphere` iterates the 1024² PIXEL grid issuing ~500k `fillRect` of which all but the first 16×16 land OFF the canvas — a genuine bug, not just waste (SHIP-in-H: iterate the 16×16 TILE grid or a 2×2 `DataTexture`), A5 amiga ships an `AnimationGroup` it never `.play()`s and the only interactivity is stock `OrbitControls` on the camera — its pertinence is the weakest in the lane (RECORD→SHIP-if-it-survives: drag the SPHERE via engine impulse+`decay()`, the same idiom the cube already dogfoods, else KILL), A7 cube `idle-bob` is a hand-rolled CSS `@keyframes` (a missed inv-ζ dogfood — BOOK), A9 the matrix-editor reset recovers Euler via `Math.acos(diagonal)` which conflates `scale·cos(angle)` — latently wrong under non-unit scale (BOOK/MEASURE-FIRST) — `a-scene-cube-amiga.md:44-67,76-83,96-116`). The cube engine + OrbitalDrag quaternion core (A8) is ALREADY-SOTA, fenced off (§ ALREADY-SOTA).
- `a-glassmorphism-perf` **G1 (demo-side) / G5** (the demo-side perf SHIPs the H.W2 design-language wave routed "elsewhere" but no wave owned: **G1 demo-side** — stop STACKING blurred surfaces over the perpetually-moving `scene-host` (the rail·stage·rail column separation H.W3 authors makes the panel backdrop sample a STATIC background — the architectural half; the `contain: paint`/de-stack instrumentation is gated HERE, MEASURE-FIRST); **G5** — no `will-change` discipline on the moving/hovered glass + a `content-visibility:auto` opportunity on OFF-SCREEN scene roots (composes with the H.W1 FSM knowing which scene is active) — `a-glassmorphism-perf.md:46-89,234-275`).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §1.1` (scene-icons, modes-pertinence, mode-interactivity rows `:64,66,69`) + **§3 H.W5** (`:147-150`, the headline + the biting proof set) + the §2.x adjudications (Discrete merge, the per-mode interactivity verdict).

## §The state, verified (file:line / live anchors from the lanes)

- **The icon registry is dock-keyed, maps only 4 ids, with an overloaded `<Home>` sentinel.** `ChromeDock.vue:20-30` imports `cube/amiga/square` PNG + `easing` SVG into `const sceneIcons: Record<string, string>` (only those 4 keys); render sites at `:171-172` (trigger, `<Home>` fallback), `:194` (dropdown item — NO fallback, unknown scene shows NOTHING beside its label), `:210-211` (collapsed pill, `<Home>` fallback). `<Home>` (`:180`) is ALSO the real home-scene icon → an overloaded sentinel making spring/sequence/path/discrete visually indistinguishable from Home. The four new scenes lack icons because `scenes.ts:79-122` added them with full descriptors but `ChromeDock.vue:25-30` was NEVER updated (two places, the second forgotten — the literal D8 root cause). (`a-scene-icons.md:47-99,233-243`)
- **`<img :src>` is theme-blind by construction — even the SVG (live + spec).** `ChromeDock.vue:171,194,210` render `<img :src="sceneIcons[id]" class="w-5 h-5 object-contain" />`; an `<img>` paints SVG as a replaced element (a separate document) that cannot read the host `currentColor`/CSS custom props, so `easing-icon-sm.svg:2`'s baked `hsl(248,88%,71%)` paints that hue in BOTH light and dark — as theme-blind as the PNGs. The dock's OWN Lucide glyphs (inline `<svg>`, `:145,172,180`) DO theme — the contrast proves theming is the REFERENCE mechanism, not the file format. Build-measured: all 4 icons inline as base64 into the eager entry chunk (`dist/gh-pages/assets/index-pbywWulI.js`), and `cube-icon-sm.png` DOUBLE-ships (favicon emit `index.html:14` + dock base64). (`a-icon-pipeline.md:32-51,184-209,290-291`)
- **The orphan `-lg` PNGs are 60.6 KB of dead binaries.** `assets/icons/{cube,amiga,square}-icon-lg.png` (256×256, 9432+31261+18935 bytes) imported by NOTHING (`grep import … lg.png` → 0 hits; the only `icon-lg` matches are the unrelated `class="icon-lg"` glyph utility — itself a no-op per H.W4 §1). The "screenshot pipeline" the user remembers is NOT checked in — the PNGs were captured ad-hoc in `74abd2b` (blobs only, no generator); `easing-icon-sm.svg` (`f1d4fe6`) is a hand-drawn vector — two unreconciled, un-reproducible lineages. (`a-scene-icons.md:65-137,189-201`, `a-icon-pipeline.md:54-141`)
- **KEEP all 4 modes — each is the ONLY demo of a distinct public primitive.** Spring = `SpringProgress`/`springTimingFunction`/`springLinearStops` (`spring.ts:491L`, tested); Sequence = `Sequence`/`stagger` (`sequence.ts:628L`, tested); Path = `fromMotionPath` (`animate.ts:32,150`, WAAPI-eligible, zero JS geometry — ALREADY-SOTA on the engine seam); Discrete = `@starting-style`/`allow-discrete` eased by `springLinearStops()`. KILL would orphan a public API from its only live proof. (`a-modes-pertinence.md:29-43`, `a-scene-spring-sequence.md:30-58`, `a-scene-path-discrete.md:12-15,171-174`)
- **`springLinearStops` is TRIPLE-surfaced (the DRY violation that makes Discrete a twin, not a scene).** It is computed in `SpringSidebar.vue:80-96,130`, `SpringTarget.vue`, AND `StartingStyleTarget.vue:91-104` (whose own comment admits "the same `springLinearStops()` path the Spring scene surfaces"); `StartingStyleTarget.vue` shares Spring's solver, four presets, and `linear()` artifact, and lives PHYSICALLY in `demo/spring/` already. As a separate top-level mode it duplicates the artifact a third time, dilutes the nav, and the codebase already treats it as a spring sub-thing. (Distinct-coverage caveat: Discrete and Spring surface the same SOLVER but on DIFFERENT primitives — `@starting-style` vs the parametric playground — so primitive COVERAGE stays distinct; the redundancy is in the solver SURFACING — `a-scene-path-discrete.md:174,180-182`.) (`a-modes-pertinence.md:89-116,188-223`)
- **The D11 interactivity census — Spring/Sequence/Discrete MEET it, Path is the laggard, Square is DEAD.** cube = orbital drag + inertia + pinch + axis keys (the gold standard, `OrbitalDrag.vue`); spring = drag/tap rail `role="slider"` + keys (`SpringTarget.vue:84-124`, SOTA); sequence = master-scrub `role="slider"` + transport (`SequenceTarget.vue:46-63,148-189`, SOTA); discrete = Reveal/Dismiss toggle + presets (`StartingStyleTarget.vue:32-66`, OK by nature); easing = scrub `Slider` ONLY, the draggable `EasingCurveCanvas` (`:12-14,72-92`) is imprisoned in the sidebar (`TimingFunctionPanel.vue:38`), never on stage — WEAK; motion-path = NONE on stage, scrub only via ribbon (`MotionPathTarget.vue` no pointer handlers) — the GAP; square = static engine-driven `<div>heyyyy` (`SquareScene.vue`, no pointer/click anywhere) — DEAD. (`a-mode-interactivity.md:50-71`, `a-modes-pertinence.md:148-185`)
- **The browser already owns path→position; the inverse (pointer→offset-distance) is trivially dogfoodable.** The SVG `<path>` is in the DOM (`MotionPathTarget.vue:20`); `getTotalLength()`/`getPointAtLength()` project the pointer onto the path to a length ratio, fed through a `ManualTimeline` (the engine's caller-driven progress primitive, `timeline.ts`) so the drag uses the SAME pipeline the ribbon scrub does (one progress source, DRY). The PATH_D source is single-sourced (`motionPathGeometry.ts:23`) feeding BOTH the guide and the CSS `offset-path` — a draggable control point re-emits PATH_D updating both in lockstep. (`a-mode-interactivity.md:128-149`, `a-scene-path-discrete.md:136-139`)
- **ALREADY-SOTA, do NOT touch (inv ε):** `easing-icon-sm.svg` is the EXEMPLAR shape idiom (the whole family converges on it; its only wart is the baked hue → `currentColor`); `scripts/lib/demo-driver.mjs` is an exemplary single-sourced driver (its `SCENES` manifest gap — only 6 of 9 scenes, `demo-driver.mjs:40-59` — is the H.W8 `proof:manifest-sourced` concern, not this wave's); the Sequence scene + transport (`SequenceTarget.vue:139-146`, zero per-frame Vue work via `--ball-p`) is the strongest of the four (icon only); the Spring rail interaction + the `fromMotionPath` engine seam are SOTA. (`a-scene-icons.md:329-333`, `a-modes-pertinence.md:119-145`, `a-scene-path-discrete.md:44,199`)

## §Goal

Finish the new modes as themable, icon'd, pertinent, interactive shop-windows — through ONE themable inline-SVG icon family on `SceneDescriptor.icon` (the theming + DRY fix; `<component :is>`, NOT `<img :src>`; the 3 PNGs + 3 orphan `-lg` KILLed in one motion), a pertinence MERGE that folds Discrete into a Spring sub-view (4 nav → 3, `springLinearStops` computed ONCE), and the per-mode interactivity floor that brings the flat scenes toward the cube's direct-manipulation standard (Path drag via `getPointAtLength`+`ManualTimeline`; easing's `EasingCurveCanvas` promoted to the stage; square drag+`SpringProgress` else KILL). Icons are authored ONLY for survivors AFTER the pertinence verdict (designing an icon for a KILLed mode is wasted motion). The reference change makes the "themable via `currentColor`" promise REAL (an `<img>` icon fails by construction); the merge is a FOLD not a deletion (the replaced scene/route/nav entry replaced in one motion, no legacy alias). KISS · DRY · the file IS the source (no checked-in screenshot generator, no binaries in git).

## §Scope (each S = WHAT + WHY)

- **S1 — move the registry onto `SceneDescriptor.icon` as an inline-SVG component family (D8; `a-scene-icons` F1/F2/F4/F5/F6 + `a-icon-pipeline` P5/P8).** WHAT: add `icon?: Component` to `SceneDescriptor` (`scenes.ts:7-14`), populate at declaration (`scenes.ts:54-123`); wire the inline-SVG reference seam (`vite-svg-loader` `?component` query — `import CubeIcon from "…cube.svg?component"` → an inline-`<svg>` SFC; one devDep + one alias-query, idiomatic, no data-URI, no extra request, themable); the dock renders `<component :is="scene.icon" class="size-5 text-muted-foreground" />` with a single `<Home>` fallback ONLY for the explicit home descriptor; DELETE `sceneIcons` + the 4 dock imports + the `<img :src>` sites + the (now-dead) `<Home>` no-icon fallback at `:172,194,211`. WHY: the icon-to-scene binding is data and belongs WITH the scene's other data (the dock should iterate `scene.icon`, never hold a parallel string-keyed map that can silently drift on a rename); inline-`<svg>` is the ONLY reference mechanism that inherits theme color (the `<img>` plumbing would silently negate the "themable" promise even for vector files). (`a-scene-icons.md:165-217`, `a-icon-pipeline.md:217-237`)
- **S2 — author the survivor SVG family in the easing mould; KILL the PNGs (D8; `a-scene-icons` F1/F3 + `a-icon-pipeline` P1/P4/P5).** WHAT: hand-author one `*.svg` per SURVIVOR (cube/amiga/square — if square survives S5 — + easing + spring + sequence + path; NOT discrete, merged into spring) under `assets/icons/`, each `viewBox="0 0 32 32"`, `fill="none"`, `stroke="currentColor"` (NOT a baked hue — fix `easing-icon-sm.svg:2`'s `hsl(248…)` → `currentColor` in the same motion), `stroke-width: 2.5–3`, `stroke-linecap: round`, optional `opacity:0.4` endpoint dots; concepts: spring = a damped-sine settling to a baseline, sequence = staggered ascending bars, path = a scaled `PATH_D` figure-loop with a traveller dot (the most DRY icon possible — one geometry source for icon + guide + traversal); cube/amiga/square become vector GLYPHS of the same concepts (isometric cube outline, checker-sphere, tilted card), not photographs; DELETE the 3 `-sm` PNGs + the 3 orphan `-lg` PNGs in one motion (NO-legacy: the replaced surface replaced once); keep the single emitted PNG favicon explicitly IF a raster favicon is wanted (a named befitting delta — favicons legitimately want raster). WHY: the PNG screenshots and the SVG curve cannot coexist as a "family" (no shared palette/stroke/light-dark behaviour); ONE vector `currentColor` idiom is reproducible (the file IS the source), theme-aware, ~0.4KB each, scalable (kills the `-lg` need), and matches the existing easing icon so the family is coherent. (`a-scene-icons.md:141-162,189-201,263-292`, `a-icon-pipeline.md:217-247`)
- **S3 — MERGE Discrete into a Spring sub-view; de-dup `springLinearStops` to ONE composable (D8/D11; `a-modes-pertinence` §1/§4 + `a-scene-path-discrete` F8 RECORD).** WHAT: fold the Discrete content (`StartingStyleTarget.vue`) in as a second view/tab within the Spring scene ("Spring → Live solver | Discrete transition"); extract `springLinearStops`/`springTimingFunction` to ONE composable consumed by BOTH the rail view and the discrete view (collapse the triple-surface in `SpringSidebar`/`SpringTarget`/`StartingStyleTarget` — the interactive rail IS the hero, the sliders/presets/`linear()` artifact live in their own panel ONCE); replace the standalone `StartingStyleScene.vue` + its `router.ts` route + its nav/dock entry IN ONE MOTION (no legacy alias); the contract-anim throw (the `serializeEasing` on the custom-fn timing function, `StartingStyleScene.vue:24-33` → `format.ts:30-45`) is killed by H.W0 — the demo-side belt is to use the already-emitted `springLinearStops(...)` `linear()` literal as the contract anim's CSS-twin timing function (a registry-free faithful twin that serializes verbatim, `format.ts:16-17`, and is DRY with the artifact). WHY: `@starting-style` is "the spring `linear()` applied to a real discrete transition" — literally the same artifact with a live target; two scenes that share an artifact are ONE scene with two views; the merge removes a nav entry, kills the triple-duplication, and tells a better story (here's the spring curve → here it eases a real CSS transition). 4 nav → 3 (Spring | Sequence | Path), each a non-redundant proof of a distinct primitive. (`a-modes-pertinence.md:78-116,188-222,251-263`, `a-scene-path-discrete.md:94`)
- **S4 — the per-mode interactivity floor: drag the motion-path traveller + promote the easing curve to stage (D11; `a-mode-interactivity` H-MI-2/H-MI-3 + `a-scene-path-discrete` F4).** WHAT: motion-path — make the traveller draggable ALONG its own path: on `pointerdown`, project the pointer onto the SVG `<path>` via `getTotalLength()`/`getPointAtLength()`, find the nearest length, set `offset-distance = len/total`, driven through a `ManualTimeline` (the same progress pipeline the ribbon scrub uses, release→resume autoplay) — the `role="slider"` posture Spring/Sequence already use; (BOOK the editable-path elevation — draggable PATH_D control points re-emitting guide + `offset-path` in lockstep — as an H follow-up). easing — promote `EasingCurveCanvas` (already built, already draggable, already `@update:bezier-points`) to be the easing scene's PRIMARY stage element when the curve is bezier-class (`isBezierEditable`, `useEasingDemo.ts:66-69`): dragging a handle re-shapes the live curve AND the sweep ball riding it (`progress` is already the shared time axis); for non-bezier named curves keep the scrubber, for `steps` the discrete inputs. WHY: Path is the ONLY mode not interactive beyond the bottom scrub (a passive shop-window); the easing scene is ABOUT the shape of a curve yet you cannot touch the curve — both fixes are pure REUSE/dogfood (no new gesture code; `ManualTimeline` the way the cube dogfoods `AnimationGroup`; `EasingCurveCanvas` collapses the sidebar-vs-stage split, DRY). (`a-mode-interactivity.md:104-149`, `a-scene-path-discrete.md:130-141`)
- **S6 — the cube/amiga scene-quality + demo-perf budget the icon set rides over (`a-scene-cube-amiga` A2/A3/A5 + `a-glassmorphism-perf` G1-demo/G5; the dropped band re-homed).** WHAT: the SHIP/MEASURE-FIRST scene-perf + pertinence items for the two ORIGINAL stage scenes (cube/amiga) the icon family must ALSO render, plus the demo-side glassmorphism perf SHIPs H.W2 routed "elsewhere" with no owner. **(A3 — SHIP)** fix `demo/amiga/utils.ts:9,17-24` `tesselateSphere`: iterate the 16×16 TILE grid (`for y<16 for x<16 fillRect(x*64,y*64,64,64)`) or a 2×2 `DataTexture` with `NearestFilter`+`repeat.set(16,16)` — kill the ~500k off-canvas `fillRect` (a genuine bug; the checkerboard render must be pixel-identical, `proof:amiga-tessellate-tilecount` ≤256 calls). **(A2 — MEASURE-FIRST)** cap `AmigaScene.vue:47` `setPixelRatio(Math.min(window.devicePixelRatio, 2))` relying on the already-enabled MSAA — gate `proof:amiga-pixel-cap` (`getPixelRatio() ≤ 2`); land the before/after frame-time number on a dpr≥2 surface before claiming the win. **(A5 — RECORD→SHIP-if-it-survives)** amiga's pertinence: it builds an `AnimationGroup` it never `.play()`s and orbits the CAMERA, not the engine; make the SPHERE the interactive subject (drag → engine impulse + `decay()`, the cube's dogfood idiom) so the "engine drives a non-DOM target" story is real, ELSE KILL the scene (no generic OrbitControls demo) — the KILL/keep call is NAMED here so the icon set (S2) reflects the survivor set deterministically. **(G1-demo — SHIP + MEASURE-FIRST)** the de-stack: H.W3's rail·stage·rail column separation already moves the panel backdrop off the moving scene-host (the architectural half); add `contain: paint` on the scene-host (or the panel column) so a moving transform behind a backdrop does not invalidate the panel's blur every frame — MEASURE-FIRST the dpr=2 win. **(G5 — SHIP + MEASURE-FIRST)** transient `will-change` on the hover/transition ONLY (never resident) + `content-visibility:auto` on INACTIVE scene roots (the FSM names the active scene — H.W1) so off-screen backdrops stop re-blurring. WHY: a whole demo-perf + cube/amiga scene-quality band (the `a-scene-cube-amiga` lane + `a-glassmorphism-perf` G1/G5) had no terminal wave home — these are scene-quality + scene-perf, exactly H.W5's domain (it already touches `AmigaScene` for the icon); A3 is a real bug, A2/G1/G5 are MEASURE-FIRST perf, A5 is the amiga pertinence call that determines the survivor set. (A7 cube idle-bob + A9 matrix-editor decompose are BOOKed in `_SYNTHESIS-deferred-ledger §3.3` — engine/editor-correctness BOOKs, not scene-quality SHIPs.) (`a-scene-cube-amiga.md:44-67,76-83`, `a-glassmorphism-perf.md:74-89,256-275`, `_SYNTHESIS-dock-perf-modes.md:336,395`)
- **S5 — square: KILL-candidate, else drag + `SpringProgress` (D11; `a-mode-interactivity` H-MI-1 + `a-scene-square-easing` 1.1/1.5).** WHAT: square is the weakest of the ORIGINAL set (a non-interactive dead `<div>heyyyy`, doesn't survive a route switch live); its feature (custom transform fn over nested `a.b.c.d` object keyframes) is already proven by the cube's matrix path + the motion-path scalar sweep. RECOMMEND KILL (remove the scene/route/nav in one motion); ELSE it MUST become directly manipulable — drop the box's position onto a `SpringProgress`-per-axis (pointer-drag sets each axis target, the spring chases — the SAME re-seat idiom Spring ships), so the custom `transformFunc` reads live spring values; either way delete the dead `SUPER_KEY` export (`useSquareAnimations.ts:4`) and the `"heyyyy"` placeholder copy. WHY: per the Mandate (NO legacy; a replaced surface replaced in one motion) square earns its keep ONLY if directly manipulable — no third option; the dead export + placeholder are tidy-in-the-same-motion. WHY a decision-in-the-wave: the KILL/rebuild call is named here (RECOMMEND KILL) so the icon set (S2) and `proof:scene-parity` (below) reflect the survivor set deterministically. (`a-mode-interactivity.md:75-101`, `a-scene-square-easing.md:78-125,164-168`)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

Two new gates (Playwright DOM/visual + grep), settle-gated on the H.W1 FSM resting (so the per-mode locks fail on interaction logic, not the route storm):

- **`proof:scene-icons` (the themable-coverage + shape + no-raster lock).** Iterate `allScenes`: every non-`home` `SceneDescriptor` has a defined `icon` (structurally forbids the D8 regression — an icon-less scene cannot ship); every `assets/icons/*.svg` is `fill="none"` + ≥1 `stroke="currentColor"` + `viewBox="0 0 32 32"` with ZERO baked `hsl(`/`rgb(`/`#hex` stroke or fill; `assets/icons/` contains ZERO `.png` (modulo a single explicitly allow-listed favicon); AND the THEMING clause — a mounted scene icon's computed stroke equals the host `currentColor` in BOTH `.dark` and light (an `<img :src>` icon FAILS this by construction — the stage-4 fix). **BITE:** reds TODAY — only 4 of 9 scenes resolve an icon, the registry is 3 PNG + 1 baked-hue SVG, the orphan `-lg` PNGs are live, and the `<img>` render is theme-blind; greens only on the inline-SVG `currentColor` family on the descriptor + the PNG KILL. (`a-scene-icons.md:159-162,238-242,313-323`, `a-icon-pipeline.md:238-247`)
- **`proof:scene-parity` (the pertinence + interactivity lock).** Assert the nav/`scenes.ts`/`router.ts` carry exactly 3 surviving new-mode entries (Spring | Sequence | Path) — `router.ts` has no `starting-style` route AND `scenes.ts` has no `starting-style` descriptor (the Discrete merge landed, in one motion); `springLinearStops` is computed in EXACTLY ONE composable (`grep` ≤1 `springLinearStops(` call-site across the spring surface); each surviving mode exposes ≥1 pointer-interactive affordance — `proof:motionpath-drag` (pointer-drag the traveller to 50% along the path → computed `offset-distance` ≈ 50%) AND `proof:easing-curve-onstage` (on a bezier curve the stage renders `EasingCurveCanvas[editable]` and a handle drag updates `bezierControlPoints`) AND (if square survives) `proof:square-drag` (a `pointerdown`+move on `.demo-box` mutates a target ref and the spring value converges) ELSE square's route/descriptor absent. **BITE:** reds TODAY — Discrete is a standalone scene/route triple-surfacing `springLinearStops`, motion-path/easing/square have NO stage pointer affordance; greens only on the merge + the de-dup + the interactivity floor. (`a-modes-pertinence.md:110-116,144,183-184,245-247`, `a-mode-interactivity.md:122-124,146-148,94`)

- **`proof:scene-perf-budget` (the cube/amiga scene-quality + demo-perf lock — S6).** `proof:amiga-tessellate-tilecount` — spy `CanvasRenderingContext2D.fillRect` (or assert the `DataTexture` path) during an amiga mount ≤ 256 calls AND the checkerboard screenshot is pixel-identical to baseline (the A3 fix is isomorphism-preserving — `a-scene-cube-amiga.md:67`); `proof:amiga-pixel-cap` — `renderer.getPixelRatio() ≤ 2` (the A2 cap; the dpr≥2 frame-time delta is the MEASURE-FIRST number, recorded-with-the-claim, `a-scene-cube-amiga.md:49`); `proof:scene-host-contained` — the moving `scene-host` (or its panel column) resolves `contain: paint` so a backdrop behind it does not invalidate per-frame (G1 demo-side); `proof:offscreen-cv` — an INACTIVE scene root resolves `content-visibility: auto` and no resident `will-change` survives a settled frame (G5); AND (the A5 amiga-pertinence parity) IF amiga survives, `proof:amiga-engine-drives-mesh` — the sphere `position`/`rotation` change while the group plays (engine path live, independent of camera orbit, `a-scene-cube-amiga.md:83`), ELSE the amiga route/descriptor/icon are absent (the survivor-set determinism S2 + `proof:scene-parity` rely on). **BITE:** reds TODAY — `tesselateSphere` issues ~500k off-canvas `fillRect`, `setPixelRatio(dpr*2)` is live (`AmigaScene.vue:47`), the scene-host has no `contain`, off-screen scene roots keep their backdrops alive, and amiga ships an `AnimationGroup` it never plays; greens only on the tile-loop/DataTexture fix + the dpr cap + the `contain`/`content-visibility` discipline + the amiga sphere-drive (or the KILL). (`a-scene-cube-amiga.md:44-67,76-83`, `a-glassmorphism-perf.md:256-275`)

**The §Mandate bar — MUST bite.** Every clause asserts an exact structural/computed fact the lanes captured (4-of-9 icons, the baked-hue SVG, the theme-blind `<img>`, the triple `springLinearStops`, the inert traveller, the imprisoned curve, the 500k off-canvas `fillRect`, the `dpr*2` over-render). The theming clause is the load-bearing one — it bites a length the PNG→SVG swap alone does NOT (an `<img>` SVG still fails it). Icons are gated to survivors only (the chronic-closure discipline: a KILLed mode gets no icon, and `proof:scene-parity` deterministically reflects the survivor set). No clause passes vacuously — the icon coverage gate makes an icon-less scene structurally unshippable (the permanent D8 regression cure).

## §Folds

- `a-scene-icons` **F1** (two unreconciled idioms — S2) + **F2** (registry mis-located → descriptor — S1) + **F3** (orphan `-lg` PNGs — S2, KILL) + **F4** (`<Home>` overloaded sentinel — S1, fallback becomes dead) + **F6** (the D8 root cause — cured by S1's single source). **F5** (string-keyed `Record` can drift) = RECORD (the type-safety rationale for S1's compile-time binding). Pipeline idiom A (hand-authored vector family) = SHIP (the file IS the checked-in source); idiom B (a checked-in screenshot generator) = NOT taken (a second theme-blind idiom + non-trivial determinism work — the `demo-driver.mjs:40-59` manifest gap + the animated-pose determinism — the H.W8 `proof:manifest-sourced` BOOK if photo-real is ever a named delta).
- `a-icon-pipeline` **P5** (codify the 32×32 `fill=none currentColor` contract — S2, `proof:scene-icons` G1) + **P8** (the `<img>` theme-blind construction — S1, `proof:scene-icons` G4 the theming clause) + **P1/P4** (no committed generator / orphan PNGs — S2). **P2/P3** (the manifest gap / screenshot determinism) = BOOK/MEASURE-FIRST (moot under idiom A; the manifest re-sourcing is H.W8). **P6/P7** (base64-into-entry / cube double-ship) = RECORD (resolved by inline-SVG: no asset URL to inline). **P9** (favicon resolve correct) = RECORD ALREADY-SOTA.
- `a-modes-pertinence` (KEEP all 4, MERGE Discrete→Spring, 4→3 nav — S3; the survivor-set + the Path-drag/easing-promote interactivity — S4; the Sequence faint-rail-tint nudge folds into the shared `.progress-rail` idiom, isomorphic) + `a-scene-spring-sequence` (the no-icon gap — S1/S2; the contract-anim throw — H.W0 + the S3 `linear()`-twin belt; the Sequence-bleeds-cube-panel — H.W1/H.W3) + `a-scene-path-discrete` (Path inert→drag — S4; Discrete throw/dead-transport — H.W0 + S3; the PATH_D-as-icon — S2; the distinct-coverage RECORD — S3) + `a-scene-square-easing` (square KILL/rebuild — S5; easing drag-the-dot — S4; the dead `SUPER_KEY`/`"heyyyy"` — S5).
- `a-mode-interactivity` **H-MI-2** (easing curve to stage — S4) + **H-MI-3** (motion-path drag — S4) + **H-MI-1** (square KILL/rebuild — S5). **H-MI-4** (sequence per-row re-time markers) = SHIP (med) but the master scrubber already gives a real handle — ENRICHMENT, folded as a stretch within S4, not a parity floor. **H-MI-5** (spring is the model citizen) = RECORD ALREADY-SOTA. **H-MI-6** (`useDragScrub` extraction) = MEASURE-FIRST → BOOK (extract only if 3+ genuine consumers — don't over-abstract two-line handlers). **H-MI-7** (interactivity moot until scenes hold their route) = the H.W1 DEPENDENCY (gate the D11 locks behind the FSM).
- `a-scene-cube-amiga` **A3** (S6 — the `tesselateSphere` tile-loop bug, SHIP, `proof:amiga-tessellate-tilecount`) + **A2** (S6 — the `dpr*2` retina pixel cap, MEASURE-FIRST, `proof:amiga-pixel-cap`) + **A5** (S6 — the amiga pertinence call: sphere-drive-or-KILL, `proof:amiga-engine-drives-mesh` or absent). **A1/A4/A11** (the scene-machine non-idempotency + the default `?anim=` writeback + the `"......"` engine-throw symptom) = H.W1/H.W0 (the FSM + the bare-text guard own them — fenced off here). **A6** (icon on `SceneDescriptor`) = S1 (the structural enabler, already folded). **A7** (cube `idle-bob` hand-rolled CSS dogfood) + **A9** (matrix-editor `acos` decompose unsound under scale) = BOOK (`_SYNTHESIS-deferred-ledger §3.3` — engine/editor-correctness, not scene-quality SHIPs; A9 carries a measured `proof:matrix-decompose-correct` it ALREADY fails). **A8** (OrbitalDrag quaternion core) + **A10** (shell cross-ref D1/D2/D4 under cube/amiga) = ALREADY-SOTA / cross-ref (own lanes own the fix). (`a-scene-cube-amiga.md:29-145`)
- `a-glassmorphism-perf` **G1-demo-side** (S6 — stop stacking blurred surfaces over the moving scene-host: H.W3's column separation + `contain: paint`, SHIP+MEASURE-FIRST, `proof:scene-host-contained`) + **G5** (S6 — transient `will-change` + off-screen `content-visibility:auto` on inactive scene roots, SHIP+MEASURE-FIRST, `proof:offscreen-cv`). **G2/G6** = H.W2 (cartoon-shadow hover + the glow strategy), **G3** = ALREADY-SOTA (the specular `::before` is SOTA-built), **G4** = the dock glass-ui-HANDOFF (H.W8). (`a-glassmorphism-perf.md:46-89,234-299`)

## §Design decisions (trade-offs RESOLVED)

- **Inline-SVG component, not `<img :src>`; vector-over-raster is INSUFFICIENT alone — RESOLVED.** `a-scene-icons` F1's "themable via `currentColor`" is correct but INCOMPLETE: switching PNG→SVG while keeping `<img :src>` yields a vector that is STILL theme-blind (a replaced element can't read the host `currentColor`). The pipeline truth: theming is a property of the REFERENCE mechanism (inline `<svg>` via `<component :is>`), not the FILE FORMAT. The `proof:scene-icons` theming clause (G4) bites this directly — an `<img>` icon fails by construction. (`a-icon-pipeline.md:42-51,204-209`)
- **The icon lives on the descriptor, not in the dock — RESOLVED.** Adding a scene meant editing `scenes.ts` AND remembering `ChromeDock.vue` (the four new authors did not — the D8 root cause). Folding `icon` onto `SceneDescriptor` makes the binding compile-time + single-source; the coverage gate (every non-home descriptor has an icon) makes an icon-less mode structurally unshippable — the PERMANENT cure for the regression class. (`a-scene-icons.md:165-217`)
- **MERGE Discrete, do NOT KILL — RESOLVED (the §2 adjudication).** Discrete's CONTENT is valuable (the demo's ONLY `@starting-style`/`allow-discrete` proof + the single best copy-paste `linear()` artifact); it is Spring's TWIN, not its own scene (shares the solver, presets, artifact; lives in `demo/spring/`). MERGE as a sub-view (4→3 nav), `springLinearStops` computed ONCE, the standalone scene/route/nav replaced in one motion — a FOLD, not a deletion (nothing dies that has no replacement). Primitive coverage stays distinct (the same solver on DIFFERENT primitives); the redundancy is in the solver surfacing. (`a-modes-pertinence.md:188-222`, `a-scene-path-discrete.md:174,180-182`)
- **Icons designed ONLY for survivors, AFTER the pertinence verdict — RESOLVED.** Designing an icon for a KILLed/MERGEd mode is wasted motion; S3/S5 (the pertinence + KILL calls) precede S2 (the survivor icon set), so the family reflects exactly Spring/Sequence/Path (+ cube/amiga/easing, + square iff it survives) — Discrete needs no icon (merged). (`a-scene-icons.md:233-243`, `a-modes-pertinence.md:236-247`)
- **Per-mode interactivity is REUSE/dogfood, not new gesture code — RESOLVED.** The two cheapest, most idiomatic wins (easing-curve-to-stage) are pure reuse of the already-built draggable `EasingCurveCanvas`; motion-path drag dogfoods `ManualTimeline` (the engine's caller-driven progress primitive) projecting the pointer via `getPointAtLength` — one progress source, DRY. The `useDragScrub` extraction is MEASURE-FIRST (three scenes hand-roll the rail/drag dance — extract only if genuinely the same shape, 3+ consumers). (`a-mode-interactivity.md:104-149,188-204,231-235`)
- **Square: RECOMMEND KILL, named in the wave — RESOLVED.** Square is the weakest original mode (a dead `<div>heyyyy` that doesn't hold its route, its feature already proven by cube + motion-path); per the Mandate it earns its keep only if directly manipulable (drag+`SpringProgress`), no third option. The wave NAMES the KILL recommendation so the survivor set / icon family / `proof:scene-parity` are deterministic; the version/scope owner confirms KILL vs rebuild at impl-open. (`a-mode-interactivity.md:75-101`)
- **DEPENDS on H.W0 + H.W1 — RESOLVED.** The pertinence/interactivity reads are poisoned by the D12 route storm (every deeplink lane observed autonomous scene cycling), so the per-mode `proof:*-drag` locks settle-gate on the H.W1 FSM (fail on interaction logic, not routing churn); the Discrete merge lands on the clean console H.W0 delivers (the `serializeEasing` throw + the `"......"` lerp die first). (`a-modes-pertinence.md:45-72,265-268`, `a-mode-interactivity.md:207-215`)
