# H.W2 — RESTORE THE DESIGN LANGUAGE (Band 2 · cartoon depth + refined specular)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (the headline design-language restoration; a 5-lane consensus, root-caused — the "radial blur on hover everywhere" + the under-deployed cartoon depth) · **Scope:** the 4 panel + editor `<Card>` sites that carry the implicit `surface="glass"` + a manual `.glass-card` plate (`AnimationControlsControls.vue:3`, `RibbonBar.vue:3`, `KeyframesEditor.vue:3`, `KeyframeTimeline.vue:3`) → flip to `surface="cartoon"`, delete the manual `.glass-card` plate + the `transition-shadow duration-normal` class in the SAME edit · a new `demo/@/composables/useSpecularPointer.ts` (the pointer seam for any deliberately-retained `surface="glass"`) · delete the duplicate demo `.scale-on-hover` block from `design-idioms.css:169-188` (glass-ui owns the `@utility`) · a `proof:cartoon-is-panel-depth` + `proof:no-orphan-specular` visual+computed-style gate · plus the **glass-ui-HANDOFF** for the Card specular seam (wire-or-omit + calmer default) and the dock-icon specular, each paired with a born-RED kf gate · NO new CSS authored, NO glass-ui patch in kf · **DAG-deps:** sequenced AFTER H.W0+H.W1 (the live crashes + route storm poison the visual measurement; the hover-screenshot lock needs a stable scene); independent of H.W3's layout grid (the surface swap is per-Card, the grid is structural — they compose cleanly).

## §Provenance (the folded lanes)

- `a-cartoon-shadow` (the deepest, git-archaeology lane — the one-motion fix: flip panel Cards to `surface="cartoon"`; the radial dies at source `CardFooter:37`, cartoon offset-stamp + spring hover-lift becomes the panel depth idiom, the manual `.glass-card` plate deleted; pure glass-ui consumption, net-deletion, zero new CSS — `a-cartoon-shadow.md:1-313`).
- `a-design-language` **A1** (the radial-specular hover artifact ROOT-CAUSED = glass-ui `.glass-specular-track::before`, mouse-tracked radial, .55 white core, `screen` blend, applied by Card+dock defaults — glass-ui-HANDOFF for the calmer default + SHIP-in-H restore cartoon depth) + **A2** (cartoon-depth idiom under-used — only hero text + 1 editor; panels are glass+specular only, the core drift) + **A3** (`.scale-on-hover` duplicates glass-ui's `@utility` — a no-legacy violation, delete the demo copy) + **A7** (the cartoon-depth under-use as the headline gestalt consolidation §7.7) — `a-design-language.md:25-159`.
- `a-glow-artifact` **F1** (`.glass-specular-track` on Cards is an unwired, hover-blooming center-radial = THE D2 artifact; SHIP-in-H demo→cartoon + glass-ui-HANDOFF Card seam) + **F2** (cartoon-shadow under-deployed → demo's gestalt depth idiom) + **F3** (the `useSpecularPointer` composable for retained-glass surfaces) + **F4** (specular rest-intensity softening, glass-ui-HANDOFF) — `a-glow-artifact.md:1-244`.
- `a-glass-ui-consumption` **D2** (demo activation of glass-ui's specular — `surface="cartoon"` on panel/header/timeline Cards) + **D14** (the `--specular-intensity` tune + the glass-ui-HANDOFF for a calmer default / explicit `specular` prop) — `a-glass-ui-consumption.md:63-161`.
- `a-glassmorphism-perf` **G2** (the hover treatment animates `box-shadow` over a blurred card → per-frame paint; the cartoon-shadow idiom animates the CHEAP axis (transform), not the expensive one (shadow geometry + backdrop re-raster); replace `transition-shadow` on the two `.glass-card` panels) — `a-glassmorphism-perf.md:91-131`. Plus **G3** (the specular `::before` is GPU-cheap + SOTA-built — its defect is tuning/blend, NOT perf; keep it, free) — the MEASURE-FIRST honesty that the specular is not a perf problem (`a-glassmorphism-perf.md:135-173`).
- The §2.2 ADJUDICATION (BINDING): **`surface="cartoon"` is the gestalt move** — net-deletion, pure glass-ui consumption (inv-16), no new CSS, the radial dies because the surface map stops emitting the class (not via `!important`/`display:none`); the class/neutralizer approaches are fallbacks ONLY if the Card API forces glass+specular coupling (it does not — `CardFooter:37` gates the class on `surface==="glass"`). D14's refined-specular-where-kept stays: any deliberately-retained `surface="glass"` MUST wire the pointer seam (`useSpecularPointer`, F3) (`_SYNTHESIS-gap-scorecard.md:107-111`).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §1.1` (the "glow/cartoon-shadow design language" GAP row, root-caused 5-lane consensus, `:58`) + `§3 H.W2` (`:132-135`) + `§2.2` (the adjudication) + `§4` (the Card-specular-SEAM glass-ui-HANDOFF, `:174`).

## §The state, verified (file:line / live anchors from the lanes)

- **The radial IS glass-ui's `.glass-specular-track::before` — NOT `design-idioms.css`.** The D2 candidate (`design-idioms.css:263-269`) is the `.progress-dot` conic-ring glow — a deliberate, correct active-playing affordance bound to `--dot-p`, NOT a hover treatment; a red herring, do NOT touch it (`a-cartoon-shadow.md:92-99`; `a-design-language.md:110-111`; `a-glow-artifact.md:63-73`). The actual radial: `glass-specular-track.css:63-68` paints a `::before` `radial-gradient(circle at var(--specular-x,50%) var(--specular-y,50%), hsl(40 30% 100% / 0.55) 0%, … transparent 55%)`, `opacity: var(--specular-intensity, 0.35)` (rest), `:hover → 0.6`, `:active → 0.85`, `mix-blend-mode: screen` (`a-cartoon-shadow.md:104-114`; `a-design-language.md:31-39`).
- **It reaches every panel via the `surface="glass"` default.** `<Card surface="glass">` is the DEFAULT (`CardFooter-C390imy7.js:9` `surface:{default:"glass"}`); the surface→class map bolts the track on (`CardFooter-C390imy7.js:37` `t.surface === "glass" && "glass-specular-track"`). `grep 'surface='` over all demo `*.vue` returns ZERO hits — no demo Card ever passes `surface=`, so all 10+ default to `glass` and inherit the specular (`a-cartoon-shadow.md:120-132`; `a-glass-ui-consumption.md:101-116`).
- **LIVE: 13 specular tracks, 0 pointer-wired → a centered radial bloom.** Live measurement (easing scene): `specularTrackCount: 13`, `anyPointerWrite: false` (NO element has `--mouse-x`/`--mouse-y`), so the `::before` falls to its `var(--mouse-x, 50%)` floor → `radial-gradient(circle, …)` with NO `at <x> <y>` — a static, dead-centered radial blob that merely BRIGHTENS 0.35→0.6 on hover (`a-cartoon-shadow.md:138-150`; `a-glow-artifact.md:34-61`). Exactly D2's "strange circular/radial blur on hover everywhere" — a half-implemented iOS catch-light, not a designed effect. The demo authors ZERO specular writes (`git log -S specular -- demo/` empty; no `mouse-x` grep hit — `a-cartoon-shadow.md:147-150`).
- **The Card NEVER wires the pointer; the dock DOES.** `CardFooter-C390imy7.js` emits `glass-specular-track` with ZERO `--mouse-*`/`pointermove` writes (dist grep); `dock.js` DOES write `--mouse-x`/`--mouse-y` (dist grep: 1 each). So the SAME effect that works on the dock (it rides the cursor) degrades on the Card to a static hover-blooming center-radial. This is a **glass-ui component defect** (Card opts a surface into a pointer-tracked effect it never feeds), not a demo authoring mistake (`a-glow-artifact.md:38-46,57-61`).
- **The double-plate redundancy.** The demo's panels stack TWO surfaces: the `<Card>` glass tier + specular track AND a manually-added `.glass-card` utility (`AnimationControlsControls.vue:3`, `RibbonBar.vue:3`, `KeyframesEditor.vue:3`, `KeyframeTimeline.vue:3`). `.glass-card` is a SEPARATE static-surface utility (`glass.css:175-191`) with its own `--glass-bg-quiet` background + `--shadow-card` plate, explicitly "no hover lift, no transition." Three depth layers, none the cartoon stamp, and the manual `.glass-card` fights the Card's own tier composition (`a-cartoon-shadow.md:153-168`).
- **It is a regression, not a choice.** `git log -S "specular" -- demo/` is EMPTY — the demo never opted into the specular; it arrived with a glass-ui version bump that made `surface="glass"` the Card default and made that surface auto-bolt the track. cartoon-shadow (the C close) and the specular coexist by accident (`a-cartoon-shadow.md:171-179`).
- **glass-ui's OWN token doc names cartoon the brand depth.** `tokens.css:474` "§7 SHADOWS — cartoon offset is the signature"; `tokens.css:510-515` "the cartoon offset is a brand-flavoured decoration … Consumers wanting the cartoon look pass `<Card surface="cartoon">`." The demo is on the wrong default (`a-cartoon-shadow.md:176-179`).
- **glass-ui 3.4.0 ships the FULL cartoon system idiomatically — no glass-ui ask to USE it.** `@utility cartoon-surface` (`cards.css:33-48`): `border-width:2px; box-shadow:var(--shadow-cartoon-md); translate:0;` + `:hover` → `translate: var(--lift-sm) var(--lift-sm); box-shadow:var(--shadow-cartoon-lg)`; transition on `--spring-bouncy`/`--ease-apple`. `<Card surface="cartoon">` (`CardFooter-C390imy7.js:37`) composes `.cartoon-surface` onto the resolved `glass-${tier}` AND OMITS `glass-specular-track`. Tokens are theme-aware, `light-dark()`-driven, dark-parity built in: `--shadow-cartoon-{sm,md,lg}` (`tokens.css:543-554`), `--lift-sm` (`-1px`, `tokens.css:830`), `--spring-bouncy` (`tokens.css:161`), `--ease-apple` (`tokens.css:180`) (`a-cartoon-shadow.md:186-209`).
- **cartoon-shadow survives on exactly ONE site (the C.W2 close).** `CSSCodeEditor.vue:6` carries `.cartoon-surface` directly (commit `179019f`, "the design system made true" — the literal `shadow-[4px_4px_0px_0px_rgba(0,0,0,0.6)]` migrated to the token-backed utility in ONE motion, dark-mode delegated to the token). It is the PROOF the register works; it needs NO change. C.W2 closed the *specific A/B-named off-token site* but did NOT make cartoon-shadow the demo's depth language — INCOMPLETE at the system level, which is why a later glass-ui surface default could silently take over every panel with no contradiction to the C ledger (the deferred-chronic M1 pattern — `a-cartoon-shadow.md:47-87,247-249`).
- **`.scale-on-hover` is a no-legacy VIOLATION.** `design-idioms.css:169-188` defines `.scale-on-hover` with the comment "defined demo-NOWHERE until now" — FALSE. glass-ui ships it as a first-class `@utility scale-on-hover` (`utilities.css:680-690`) with the IDENTICAL recipe, promoted (per its own comment) "from the 13 keyframes.js demo sites." The demo re-authored the very utility glass-ui extracted FROM it; live CSSOM confirms BOTH definitions present — a replaced-surface-living-beside-its-replacement violation (`a-design-language.md:103-105`).
- **MEASURE-FIRST (G3): the specular is GPU-cheap, SOTA-built — its defect is tuning, not perf.** The pointer-storm trial held 121 fps, 0 long frames; the specular explicitly avoids a second `backdrop-filter`, drives intensity via layer opacity (not a per-stop `calc()` Chromium drops), uses typed `@property` for smooth position interp, ships a `prefers-reduced-motion` static pin. There is NO perf defect; the D2/D14 defect is perceptual (`a-glassmorphism-perf.md:135-165`). The hover anti-pattern that DOES bite at dpr=2 is animating `box-shadow` over a blurred card (G2) — the cartoon swap fixes it (cartoon animates `translate`, the cheap axis).

## §Goal

Flip the demo's panel Cards from the implicit `surface="glass"` to `surface="cartoon"` (the §2.2 adjudicated gestalt move) — ONE prop change per panel that simultaneously (a) DROPS `glass-specular-track` at its source (the radial dies with no CSS override, no `!important`, no `display:none` — the surface map simply stops emitting the class, `CardFooter:37`), (b) APPLIES `.cartoon-surface` (the offset-stamp depth + spring hover-lift) as the demo's coherent panel depth/hover register, and (c) lets the Card's own tier own background/blur/border so the redundant manual `.glass-card` plate AND the `transition-shadow duration-normal` class are DELETED in the SAME edit (no double-plate, one source; the hover now animates `translate`, the cheap compositor axis, not `box-shadow` over a backdrop — G2). Net-deletion, pure glass-ui consumption (inv-16), zero new CSS, the panels become "paper-and-glass" again instead of "glass-and-blur." Where `surface="glass"` is DELIBERATELY retained (a surface that genuinely wants the iOS catch-light), wire the pointer seam via a single DRY `useSpecularPointer` composable so the specular becomes the travelling light it was designed to be, never the broken centered bloom. Delete the duplicate demo `.scale-on-hover` (glass-ui owns the `@utility`). And HANDOFF — paired with born-RED kf gates — the glass-ui-owned items: the Card should wire-or-omit its specular seam at the source, and ship a calmer default intensity; the dock-icon specular is the same defect on a surface the demo does not own. This re-balances "paper-and-glass" with ONE structural move and lets the remaining D-items land as clean rung/layout corrections.

## §Scope

- **S1 — flip the panel Cards to `surface="cartoon"`, delete the manual `.glass-card` + `transition-shadow` (the one-motion fix, §2.2).** For each panel Card carrying the manual `.glass-card` plate today (`AnimationControlsControls.vue:3`, `RibbonBar.vue:3`, `KeyframesEditor.vue:3`, `KeyframeTimeline.vue:3`): `- <Card class="… transition-shadow duration-normal glass-card"> → + <Card surface="cartoon" class="…">`. This (a) removes `glass-specular-track` (the radial gone, no override), (b) applies `.cartoon-surface` (offset stamp + spring hover-lift), (c) lets the Card's tier own the plate so `.glass-card` is DELETED, and (d) drops `transition-shadow duration-normal` (`.cartoon-surface` owns its own shadow+translate transition, `cards.css:40-42`) (`a-cartoon-shadow.md:226-241`). Keep `KeyframeTimeline.vue`'s `expanded` branch conditional (it already strips border/shadow — just swap the base surface). **WHY:** the net-deletion gestalt move — the radial dies at source, the depth register is restored, the double-plate collapses, AND the G2 per-frame-paint hover anti-pattern is fixed (cartoon animates the cheap `translate` axis), all in one prop swap. LEAVE `CSSCodeEditor.vue:6` alone (already `.cartoon-surface`, the C close — the proof the register works).

- **S2 — audit the `plain`/bare Cards for the cartoon register.** Verify `MatrixEditor.vue:2` (`<Card>` plain) and `TimingFunctionPanel.vue:17,74` (`<Card plain>`) per-Card: `plain` cards likely already suppress the surface, but the bezier-editor framing (D3, H.W4) may want the cartoon register too (`a-cartoon-shadow.md:243-245`). **WHY:** completeness — the surface decision is per-Card, and the `plain` variant must be confirmed to suppress the specular before assuming it is unaffected. RECORD any `plain` Card that still emits the track as a follow-on; do NOT blanket-apply.

- **S3 — `useSpecularPointer` for deliberately-retained glass surfaces (F3, D14).** A single DRY composable `useSpecularPointer(elRef)` that writes `--mouse-x`/`--mouse-y` as percentages on `pointermove` (glass-ui's documented consumer seam, `glass-specular-track.css:19`), no-ops under `prefers-reduced-motion`. Applied ONLY to surfaces that legitimately keep `surface="glass"` and want the travelling catch-light (interactive scene targets, D11/H.W5) (`a-glow-artifact.md:194-205`; `a-cartoon-shadow.md:250-259`). **WHY:** the spine forbids a half-implemented feature — any retained specular MUST be wired or it stays the broken centered bloom. DRY: one composable across every opt-in surface. If H descopes interactivity entirely, the composable is BOOKed — but D14 reconciles the refined specular WHERE kept, so the seam must exist for the panels that opt in.

- **S4 — delete the duplicate demo `.scale-on-hover` (A3, no-legacy).** DELETE the `.scale-on-hover` block (and its PRM block) from `design-idioms.css:169-188`; consume glass-ui's `@utility scale-on-hover` (`utilities.css:680-690`) directly — the class name is IDENTICAL, so call-sites don't change. Correct the file's false "defined demo-nowhere" framing (`a-design-language.md:103-105`). Optionally keep ONLY the `--scale-hover` token if the demo wants to own the magnitude (glass-ui already defaults it, so even that is optional). **WHY:** a replaced surface living beside its replacement is the no-legacy violation the spine forbids; the demo re-authored the utility glass-ui extracted FROM it. One deletion, zero call-site churn.

- **S5 — the glass-ui-HANDOFFs, each paired with a born-RED kf gate.** Two HANDOFF asks to glass-ui (the dock is being reworked NOW in glass-ui's AW tranche — do NOT patch in kf): (i) the **Card specular SEAM** — Card should wire-or-omit its specular: either wire `--mouse-*` itself (as `dock.js` does) OR omit `.glass-specular-track` until a consumer opts in (`CardFooter:37` adds it on `surface="glass"` with no pointer wire — `_SYNTHESIS-gap-scorecard.md:174`; `a-glow-artifact.md:223-229`); plus a calmer default intensity (rest ≤0.25, radius ≤40% — the 0.55 white core + `screen` blend is too hot as a `resting` default — `a-design-language.md:48`; `a-glow-artifact.md:207-213`). (ii) the **dock-icon specular** — `dock-icon-button` hard-codes `glass-specular-track` (`dock.js:568`); same centered-bloom defect (the dock DOES wire `--mouse-*`, so this is a tuning/intensity ask, ties to D5 dock lag) (`a-cartoon-shadow.md:261-269`; `a-glow-artifact.md:128-134`). **WHY:** these are glass-ui-owned surfaces; inv-16 — consume, do not re-author. Per the chronic-closure discipline (H.W8), each HANDOFF is PAIRED with a born-RED kf gate (`proof:no-orphan-specular` polices the kf-owned panels; the dock + Card-default asks ride a `proof:dock-live`/specular-default born-RED that greens when glass-ui ships) — no bare HANDOFF tag.

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

A `proof:cartoon-is-panel-depth` computed-style gate + a `proof:no-orphan-specular` DOM+visual gate + the no-dup-utility grep + the born-RED HANDOFF gates, wired into the `proof:*` set — each with a STATED bite:

- **`proof:cartoon-is-panel-depth` — ≥4 panel Cards resolve `box-shadow: var(--shadow-cartoon-md)` at rest.** Assert the 4 panel Cards (`AnimationControlsControls`, `RibbonBar`, `KeyframesEditor`, `KeyframeTimeline`) carry `.cartoon-surface` (their resting `box-shadow` resolves from `--shadow-cartoon-md`, growing to `--shadow-cartoon-lg` on `:hover`) (`a-cartoon-shadow.md:278`; `a-glow-artifact.md:190-192`). **BITE:** reds TODAY — only 1 cartoon site (`CSSCodeEditor.vue:6`); the panels are glass+specular (`a-design-language.md:122-123`); greens ONLY on S1 (the `surface="cartoon"` flip). A glass-ui rename of the cartoon family is also caught (the assertion resolves the token).
- **`proof:no-orphan-specular` — ZERO `.glass-specular-track` on panel Cards OR (if retained) a `--mouse-x` writer present; the hover-screenshot lock = offset cartoon stamp, no centered radial.** Assert the built demo DOM has zero `.glass-specular-track` on the panel Cards; OR if any survives, assert a `--mouse-x` write exists on its host. Plus a Playwright hover-bloom screenshot diff on the controls panel locks "no centered radial bloom" — the hover delta is dominated by a shadow/translate change (`a-cartoon-shadow.md:277`; `a-glow-artifact.md:174-178`). **BITE:** reds TODAY — 13 live tracks, 0 pointer-wired, hover blooms a centered radial 0.35→0.6 (`a-cartoon-shadow.md:138-150`); greens ONLY on S1 (the track stops being emitted) + S3 (any retained glass is pointer-wired). Re-introduce `surface="glass"` on a panel without wiring → the gate reds.
- **`proof:no-dup-utility` (grep) — the demo CSS contributes ZERO `.scale-on-hover` rules (only glass-ui's `@utility` resolves).** Plus `grep -rn "glass-card" demo/ | grep "<Card"` = 0 after the swap (CS-3 — the Card surface owns the plate, no manual class). **BITE:** reds TODAY — both `.scale-on-hover` definitions present (demo + glass-ui — `a-design-language.md:105`); the 4 panels carry manual `.glass-card` (`a-cartoon-shadow.md:279`); greens on S4 (the demo copy deleted) + S1 (the manual plate deleted).
- **`proof:specular-handoff` (BORN-RED HANDOFF gate) — the Card-default + dock specular asks.** A born-RED gate that asserts the glass-ui Card no longer emits `.glass-specular-track` unwired (or wires it) AND the default rest intensity ≤0.25 / radius ≤40% — RED until glass-ui ships the calmer default + wire-or-omit seam. **BITE:** the HANDOFF cannot be "closed" by a column migration — the chronic-closure discipline (H.W8) requires the glass-ui-HANDOFF be PAIRED with a born-RED kf gate that greens only when glass-ui publishes (`_SYNTHESIS-gap-scorecard.md:42-43,174`). The dock-icon specular tune rides the same born-RED (folds with D5's `proof:dock-live`).

**The §spine bar — MUST bite.** `proof:cartoon-is-panel-depth` + `proof:no-orphan-specular` RED on the live tree TODAY (1 cartoon site, 13 unwired specular tracks blooming on hover), GREEN on the single `surface="cartoon"` flip + the pointer seam; `proof:no-dup-utility` RED today (both `.scale-on-hover` + the manual `.glass-card` plates present), GREEN on the deletions. No clause passes vacuously: `cartoon-is-panel-depth` asserts the EXACT resting token, `no-orphan-specular` asserts an EXACT count (zero unwired tracks on panels) + a pixel lock, `no-dup-utility` asserts EXACT grep-zero. The born-RED `proof:specular-handoff` is locked so the glass-ui asks cannot be re-papered as a bare HANDOFF tag. The fix is the gestalt move (the radial dies because the surface map stops emitting the class), NOT a CSS `display:none`/`!important` suppression (`a-cartoon-shadow.md:288-289`).

## §Folds

- `a-cartoon-shadow` **CS-1** (S1 + `proof:no-orphan-specular`) + **CS-2** (S1 + `proof:cartoon-is-panel-depth`) + **CS-3** (S1 the double-plate deletion + the `glass-card` grep clause) + **CS-4** (RECORD — `design-idioms.css:263-269` is the benign `.progress-dot` glow, do-NOT-touch; prevents a wrong-target edit) + **CS-5** (the dock-icon specular = glass-ui-HANDOFF, S5) + **CS-6** (the specular kept opt-in + pointer-wired, S3).
- `a-design-language` **A1** (S1 restore cartoon depth + S5 the calmer-default glass-ui-HANDOFF) + **A2/A7** (S1 the cartoon-depth under-use, the headline consolidation) + **A3** (S4 delete the duplicate `.scale-on-hover`) — A10 (the `--glow-*` benign ring) is the same RECORD as CS-4; A11 (rainbow/gold/rail/ball/badge consolidations + z-contract) is ALREADY-SOTA, fenced off; A4/A5/A6/A8 are H.W6/H.W4/H.W3's charge.
- `a-glow-artifact` **F1** (S1 demo→cartoon + S5 the Card-seam glass-ui-HANDOFF, `proof:no-orphan-specular`) + **F2** (S1 the cartoon depth coverage) + **F3** (S3 the `useSpecularPointer` composable) + **F4** (S5 the rest-intensity softening, glass-ui-HANDOFF).
- `a-glass-ui-consumption` **D2** (S1 `surface="cartoon"`) + **D14** (S3 the demo `--specular-intensity` tune on retained-glass + S5 the calmer-default / explicit `specular` prop glass-ui-HANDOFF). D9 (the @mbabb double-trigger), D5 (dock lag), D13 (mobile drawer) are sibling-wave charges (the popover/dock/mobile waves); the consumption-hygiene ALREADY-SOTA (forms/labeled-field/subpaths) is fenced off.
- `a-glassmorphism-perf` **G2** (S1 — the cartoon swap replaces `transition-shadow` over a blurred card with a `translate`-axis hover, fixing the per-frame-paint anti-pattern; `proof:hover-composite` rolls into `proof:no-orphan-specular`'s hover lock) + **G3** (RECORD — the specular `::before` is GPU-cheap + SOTA-built; its defect is tuning/blend, not perf — H.W2 keeps it, free; the MEASURE-FIRST honesty that the radial is a *perceptual* defect, not a perf one). G1/G4/G5/G6 are dock/mobile/perf charges elsewhere (G4 dock = glass-ui-HANDOFF).
- **PRESERVE (no work) — the ALREADY-SOTA:** the C.W2 `CSSCodeEditor` migration (`179019f`) is the exemplary MODEL for S1 — it is generalized, not corrected (`a-cartoon-shadow.md:304-306`); glass-ui's cartoon system is complete + documented (the demo only SELECTS the right surface — the hardest design work is shipped upstream); `design-idioms.css`'s `.progress-dot`/`.progress-rail`/`.progress-ball`/`.status-badge`/`.code-token` consolidations are genuinely good single-sourcing — fenced off from the radial-blur fix (`a-cartoon-shadow.md:307-313`; `a-design-language.md:113-120`).
- **glass-ui-HANDOFF (S5, born-RED-paired):** the Card specular SEAM (wire-or-omit + calmer default ≤0.25 rest / ≤40% radius) + the dock-icon specular tune — each paired with `proof:specular-handoff` born-RED. NO kf patch.

## §Design decisions (trade-offs RESOLVED)

- **`surface="cartoon"`, NOT a `.shadow-cartoon-*` class or a neutralizer — RESOLVED (the §2.2 adjudication, BINDING).** `surface="cartoon"` is the gestalt move: net-deletion, pure glass-ui consumption, no new CSS, and the radial dies because the surface map stops EMITTING the class (not via `!important`/`display:none`). The class/neutralizer approaches (`a-design-language` A1, `a-glow-artifact` F1 fallback, `a-glassmorphism-perf` G2) are fallbacks ONLY if the Card API forces glass+specular coupling — it does NOT (`CardFooter:37` gates the class on `surface==="glass"`). One prop replaces the whole double-plate (`_SYNTHESIS-gap-scorecard.md:107-111`; `a-cartoon-shadow.md:36-45`).
- **The radial is NOT deleted globally — it is demoted to opt-in + wired — RESOLVED.** Per D14 ("the glass is good; reconcile with cartoon depth") and the spine (no workaround), the specular is removed from the *default panel surface* and KEPT where the demo deliberately wants the iOS catch-light — and there it MUST be pointer-wired (S3) or it stays the broken centered bloom. The catch-light is RIGHT when it rides the cursor (the dock proves it); the refinement is to make it track on the surfaces that show it (`a-cartoon-shadow.md:250-259`; `a-glow-artifact.md:90-107`).
- **The `.progress-dot` glow is a RED HERRING — RESOLVED (do-NOT-touch).** The D2 candidate `design-idioms.css:263-269` is the active-playing `.progress-dot` conic-ring affordance bound to `--dot-p`, green, scoped to playing-state — NOT the hover defect. RECORDED so H does not chase the wrong rule and make a wrong-target edit (`a-cartoon-shadow.md:92-99`; `a-design-language.md:110-111`).
- **No glass-ui patch in kf; the dock + Card-default + drawer are HANDOFFs — RESOLVED.** inv-16 — consume, do not re-author. The Card specular seam, the calmer default, and the dock-icon specular are glass-ui-owned (the dock is in glass-ui's active AW tranche); they are TAGGED glass-ui-HANDOFF, each paired with a born-RED kf gate per the chronic-closure discipline. The kf gate only asserts kf-owned surfaces (`proof:no-orphan-specular` on the panels); the glass-ui asks ride `proof:specular-handoff` born-RED (`a-cartoon-shadow.md:261-269,296`).
- **MEASURE-FIRST: the specular is NOT a perf problem — RESOLVED.** The pointer-storm held 121 fps / 0 long frames; the specular is GPU-cheap + SOTA-built (G3). The radial is a *perceptual* defect (a centered unwired bloom), not a perf one — H.W2 does NOT claim a perf win from removing it; the genuine perf anti-pattern (animating `box-shadow` over a blurred card, G2) is fixed as a SIDE-EFFECT of the cartoon swap (cartoon animates the cheap `translate` axis), and that win is measured at dpr=2 before claiming it (`a-glassmorphism-perf.md:135-165,114-131`).
- **The chronic-closure discipline applies (H.W8) — RESOLVED.** D2/D14 is one of the four user-visible chronics that "exited" the deferred ledger by re-classification (cartoon-shadow D2 closed at the ISSUE level — the one CSSCodeEditor site — masquerading as a SYSTEM-level close; the M1 pattern). H closes it with a SYSTEM-property gate (`proof:cartoon-is-panel-depth` over ≥4 panels) AND a HANDOFF-with-born-RED-gate (the Card/dock seam) — the P-invariant repaired to police the PRODUCT, not the column (`_SYNTHESIS-gap-scorecard.md:36-43`; `a-cartoon-shadow.md:79-87`).
