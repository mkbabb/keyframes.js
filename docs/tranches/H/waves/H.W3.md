# H.W3 — the controls-column layout (one-column · rail-width · the rail·stage·rail grid) (Band 2 · SOURCE — the structural transposition)

- **Phase:** IMPL — spec authored in DEV, awaits auth · **Class:** SHIP-in-H (the structural transposition that makes D1/D4/D10 cohesive; pure demo CSS/markup — no engine, no glass-ui patch) · **Scope:** `demo/@/components/custom/animation-controls/AnimationControlsGroup.vue` (the 3-track grid `:5,54-57,63-72` → named `rail·stage·rail`) + `controls/AnimationControlsControls.vue` (the two-track grid + subgrid chain `:4,6,9,124,293` + `col-span-2` `:88,97,125`) + `controls/AnimationControls.vue:4` (the divergent 768px cap) + `components/ControlsPaneWrapper.vue:206` (the `min-width` floor → `width`) + `components/LayerConfigPanel.vue:14-22` (the z-index split-row paradigm) + `controls/TimingFunctionPanel.vue:78,3` (its OWN two-track grid — W3 owns the grep AND the file, WV-W3-HIGH-1b) + `components/ControlsPaneWrapper.vue:188-201` (the desktop `translateX(-110%)` overlay slide, deleted when the `[rail]` track becomes the open/close axis — WV-W3-HIGH-2) + `styles/design-idioms.css:106-111,99` (`--controls-pane-width` → `--rail-width`) + a new `proof:single-column-pack` / `proof:timeline-rail-width` / `proof:demo-shell-grid` gate set · **DAG-deps:** **depends on H.W1** (the scene+playback FSM — `a-timeline-width §4` + `a-modes-pertinence` D12-corruption note: the autonomous route storm makes width/layout measurement flaky; the layout gates settle-gate on the FSM resting); **independent of H.W2** (the cartoon/specular surface work is orthogonal to the grid). MEASURE-FIRST gates step (iii) (the grid-track tightening) on `proof:stage-not-clipped` (the B.W3 "cube half-clipped" invariant).

## §Provenance (the folded lanes)

- `a-controls-sidebar` **D1/F1–F5** (the PRIMARY — the half-finished migration: `grid-cols-[auto_1fr]` two-track grid threaded through nested `subgrid` panel rows, designed for the OLD split-row shape, now a wrong substrate under glass-ui's single-cell `LabeledField`; the gestalt fix = delete the two-track grid + subgrid chain → a single-column stacked-field flow — `a-controls-sidebar.md:20-126,129-189`).
- `a-timeline-width` **D4** (the THREE competing width regimes — token decorative (400), sidebar cap divergent (768), ribbon uncapped (1272); the gestalt fix = make `--controls-pane-width` (→ `--rail-width`) the SINGLE width authority, replace the `min-width` floor with `width`, delete the 768px cap — `a-timeline-width.md:36-145`).
- `a-demo-architecture` **F1/F2/F4** (the LOAD-BEARING frame: the 3-track grid is vestigial — `1fr 1fr` collapses to `0px 0px` when the pane is closed + the `col-start-1 col-end-4` stage span is the toll; collapse to ONE named `[rail] var(--rail-width) [stage] 1fr` × `[top]auto [stage]1fr [bottom]auto` grid; the full-width element is the EXPANDED TIMELINE (`col-span-full row-start-3`), not the ribbon — align it to the `[rail]` track — `a-demo-architecture.md:30-49,54-120,206-265`).
- Synthesis home: `_SYNTHESIS-gap-scorecard.md §1.1` (controls-layout, timeline-width rows `:57,60`) + **§3 H.W3** (`:137-140`, the headline + the biting proof set).

## §The state, verified (file:line / live anchors from the lanes)

- **The sidebar is broken BEYOND the column count — a half-finished migration.** `AnimationControlsControls.vue:4` is `CardContent class="relative grid grid-cols-[auto_1fr] gap-x-3 gap-y-1 px-4 py-3"` — a **label-track | control-track** grid that only yields one logical row per field if each field emits TWO children. glass-ui's `<LabeledInput>`/`<LabeledSelect>` (`@mkbabb/glass-ui ^3.4.0`) each render as ONE self-contained `<div class="labeled-field">` (label stacked OVER control, **no `display:contents`**, glass-ui `dist/styles/utilities.css:62`), so each field occupies exactly ONE cell and auto-flows TWO-per-row. (`a-controls-sidebar.md:78-95`)
- **Live capture (Playwright, :5174, 1440×900) confirms two lopsided columns.** `CardContent` grid resolves `grid-template-columns: "auto 1fr"`; fields alternate `x=76 w=212` (col-1, `auto`) ↔ `x=300 w=466` (col-2, `1fr`); every field `grid-column: auto`, `display: block`. The left-edge `x` set is `{76, 300}` (size 2); the width set is `{212, 466}`. (`a-controls-sidebar.md:45-72,206-214`)
- **The subgrid chain exists ONLY to propagate the two tracks.** `AnimationControlsControls.vue:6` `panel-stack … grid-cols-[subgrid]`, `:9` & `:124` `panel-content grid grid-cols-[subgrid]`, `:293` (scoped CSS) `.panel-row { grid-template-columns: subgrid; }`; the `col-span-2` markers at `:88` (Separator), `:97` (advanced nav row), `:125` (advanced header) exist SOLELY to span the two tracks — all dead weight once the grid is one column. (`a-controls-sidebar.md:97-103`)
- **Two contradictory row shapes live in the grid at once.** `AnimationControlsControls.vue:67-86` (easing) is a hand-built split pair (a `<div>` label + edit pencil THEN `<EasingSelect>` = two cells); `LayerConfigPanel.vue:14-22` (z-index) is a bare `<label>` + bare `<Input>` (two cells) while `blend` (`:3`) and `enabled` (`:36`) are single-cell `<LabeledSelect>`/`<LabeledSwitch>` — why `blend`/`enabled` landed in DIFFERENT columns in the live capture. (`a-controls-sidebar.md:105-116`)
- **The 3-track grid is vestigial.** `AnimationControlsGroup.vue:5` `lg:grid-cols-[var(--controls-pane-width)_1fr_1fr]` (400px | 1fr | 1fr); measured live on `/#/cube` (pane closed): `grid-template-columns: 1353.59px 0px 0px` — the `1fr 1fr` tracks COLLAPSE TO ZERO and the stage spans `lg:col-start-1 lg:col-end-4` (`:54-57`) to stay centred. The pane is a `position:relative; z-controls` overlay (`ControlsPaneWrapper.vue:7`). The grid pretends to be three columns but is "a centred stage with a left rail overlaid on top of it." (`a-demo-architecture.md:30-49,210-216`)
- **Three width regimes, zero agreement (live, 1440×900, `#/cube`, pane open).** Grid track-1 = **1353.59px**; `AnimationControls` root (`controls/AnimationControls.vue:4`) capped **768px** by `lg:max-w-screen-md`; `#controls-ribbon-target` = **1272px** (bound by *nothing*, stretches to `.controls-content`); `--controls-pane-width` token = **400px** (nominal only — not honored). `.controls-content { min-width: var(--controls-pane-width) }` (`ControlsPaneWrapper.vue:206`) is a FLOOR (guarantees ≥400px), never a CAP — it does nothing to stop the stretch to 1272. (`a-timeline-width.md:14-73`)
- **The full-width element is the EXPANDED TIMELINE, not the ribbon.** The PlaybackRibbon teleports to `#controls-ribbon-target` (`AnimationControlsControls.vue:154`) inside `.controls-content` (col-1) — already sidebar-width. The genuinely full-width surface is `#timeline-expanded-target` (`AnimationControlsGroup.vue:63-72`): `col-span-full row-start-3` spans the entire 3-col grid when expanded. (`a-demo-architecture.md:96-120`)
- **The work-area occlusion algebra is the toll of `position:fixed` docks over a centred stage.** `style.css:95-130` is an algebra of `--work-area-vertical-bias-top/bottom` + `--dock-menubar-reserve` whose ONLY job is to keep the two fixed `GlassDock`s from occluding the centred stage; real top/bottom grid tracks DELETE that algebra (MEASURE-FIRST: ~40 lines of token cycle-avoidance commentary). (`a-demo-architecture.md:240-245`)
- **ALREADY-SOTA, do NOT touch (inv ε):** the panel crossfade via `grid-template-rows: 0fr↔1fr` (`AnimationControlsControls.vue:293-302`) is exemplary — it is the panel-crossfade machinery, ORTHOGONAL to the column defect, and MUST survive the transposition (keep `display:grid; grid-template-rows`; remove only `grid-template-columns`). The teleport pipeline is sound (DOM placement is correct; the widths are not). `--visualizer-track-gutter: 3rem` (`design-idioms.css:99`) composes cleanly with a rail-width box. (`a-controls-sidebar.md:242-245`, `a-timeline-width.md:38-44,143-145`)

## §Goal

Collapse the three competing layout authorities into **one named `rail·stage·rail` grid with one `--rail-width` token**, so D1 (one-column controls), D4 (rail-width timeline), and D10 (mobile bottom-sheet, owned by H.W7) become the SAME rail in three positions — the DRY core. The sidebar becomes a single-column stacked-`LabeledField` flow (the two-track grid + subgrid chain + `col-span-2` markers deleted in ONE motion); the expanded timeline aligns to the `[rail]` track; `--rail-width` replaces `--controls-pane-width` + the 768px cap + `col-span-full`. This is the NO-WORKAROUND architectural simplification the spine names ("a replaced surface is replaced in ONE motion / no god-grid") — net-DELETION, fewer lines, one source of truth per axis, no new abstraction (NO `<TimelineWidthProvider>` god-wrapper over a value a single custom property already expresses).

## §Scope (each S = WHAT + WHY)

- **S1 — collapse the sidebar to a single-column stacked-field flow (D1; `a-controls-sidebar` F1/F3/F5).** WHAT: replace `AnimationControlsControls.vue:4` `grid grid-cols-[auto_1fr] gap-x-3 gap-y-1` with a single-column flow (`flex flex-col gap-2`); drop the subgrid chain at `:6` (`col-span-2 grid grid-cols-[subgrid]` → relative stack container), `:9`/`:124` (`grid grid-cols-[subgrid]` → `flex flex-col gap-2 w-full`), and the scoped `.panel-row` `grid-template-columns: subgrid` — **cite the lines distinctly so the wrong one is not deleted (WV-W3-NIT-1):** `:293`=`display:grid` (KEEP — the crossfade), `:294`=`grid-template-columns:subgrid` (DELETE); remove the now-meaningless `col-span-2` markers at `:88,97,125`; normalize the doubled `gap-y` (CardContent `gap-y-1` vs panel-content `gap-y-2`) to one rhythm. WHY: each glass-ui `LabeledField` is a complete self-laying-out unit; the container's only job is to stack units vertically with one rhythm — a 1-D layout, NOT a 2-track grid. (`a-controls-sidebar.md:136-160,184-188`)
- **S2 — normalize the two mixed-paradigm rows onto one field shape (D1; `a-controls-sidebar` F4 · WV-W3-LOW-1/LOW-2: resolve the slot fallback NOW).** WHAT: fold the hand-split easing row (`AnimationControlsControls.vue:67-86`) into one full-width unit — glass-ui 3.4.0 `<LabeledField>` exposes ONLY `default`+`error` slots (no label-action slot, VERIFIED `LabeledField.vue.d.ts`), so TAKE the wrapper fallback NOW (a single `<div class="flex flex-col gap-1">` wrapping the label-row + edit pencil + `<EasingSelect>`) and BOOK the label-action slot as an OPTIONAL glass-ui-HANDOFF (do not block on it); convert `LayerConfigPanel.vue:14-22` z-index from a bare `<label>` + `<Input>` pair to `<LabeledField>` + slotted `<Input>` so blend/z-index/enabled are all one-cell rows — NOTE the raw `<LabeledField>`+slotted `<Input>` needs manual `aria-errormessage`/`controlId` wiring (`LabeledField.vue.d.ts:19-26`). WHY: the grid carries two contradictory row shapes; DRY the panel onto ONE paradigm so no row escapes the single column; the slot fallback is decided now so impl is unblocked. (`a-controls-sidebar.md:161-174`)
- **S3 — promote `--controls-pane-width` → `--rail-width` as the SINGLE width authority (D4; `a-timeline-width` (i)/(ii) + `a-demo-architecture` F4).** WHAT: rename `--controls-pane-width` → `--rail-width` in `design-idioms.css:106-111` (the "COUPLED layout invariant"); change `.controls-content` (`ControlsPaneWrapper.vue:203-210`) from `min-width: var(--rail-width)` (floor) to `width: var(--rail-width)` (keep `box-sizing: border-box` so the shadow-clearance padding stays inside the budget); DELETE the divergent `lg:max-w-screen-md` (768px) cap at `controls/AnimationControls.vue:4`. WHY: the flex box that parents BOTH the sidebar and the ribbon becomes exactly the token width — the ribbon's `w-full` and the visualizer's `100cqw` resolve against it, automatically matching the sidebar; the 768px cap is dead legacy beside its replacement (the spine forbids legacy-beside-replacement). (`a-timeline-width.md:83-97,122-125`)
- **S3b — collapse `TimingFunctionPanel.vue`'s own two-track grid (D3 file, WV-W3-HIGH-1b: W3 owns the grep AND the file).** WHAT: collapse `TimingFunctionPanel.vue:78`'s `grid-cols-[auto_1fr]` + `:3`'s `col-span-2` to the single-column flow. WHY: W3 owns the `proof:demo-shell-grid` grep but H.W4 disclaims `TimingFunctionPanel.vue` — leaving the file UN-OWNED by any §Scope, so the grep would red with no owner (a circular punt). RESOLVE by naming `TimingFunctionPanel.vue` in H.W3 §Scope here: W3 owns the file's grid collapse, H.W4 only sizes/headers the panel (S2 there). (`hd-precepts P-02`, `hd-w4 cross-note`)

- **S4 — collapse the 3-track grid to the named `rail·stage·rail` frame; align the expanded timeline to the `[rail]` track (D4/D10-seed; `a-demo-architecture` F2/F4 + `a-timeline-width` (iii)).** WHAT: replace `AnimationControlsGroup.vue:5` `lg:grid-cols-[var(--controls-pane-width)_1fr_1fr]` with `grid-template-columns: [rail] var(--rail-width) [stage] 1fr` × `grid-template-rows: [top] auto [stage] 1fr [bottom] auto`; the stage gets its OWN `[stage]` track (delete the `lg:col-start-1 lg:col-end-4` span at `:54-57`). **The desktop open/close is an OVERLAY SLIDE, NOT a grid-track animation (WV-W3-HIGH-2):** `ControlsPaneWrapper.vue:147-155` is the MOBILE rows block; desktop open/close is `translateX(-110%)` overlay (`:188-201`) — so "animate the `[rail]` track `400px↔0`" is a NEW mechanism that makes the stage REFLOW. Honestly: the new `[rail]`-track-collapse IS a new idiom (replacing the overlay slide); add the overlay-slide removal (`:188-201`) to S4 scope (legacy-beside-replacement — the `translateX(-110%)` overlay is deleted when the `[rail]` track becomes the open/close axis). The `#timeline-expanded-target` (`:63-72`) moves from `col-span-full row-start-3` to `grid-column: 1` (the `[rail]` track). **`col-end-4` override (WV-W3-HIGH-3):** S4 deletes `col-end-4`, but `a-timeline-width §3(iii)` says KEEP it — NAME the override explicitly: adopt the stronger `[stage]`-track form (delete `col-end-4`), with the lane's conservative `col-end-4`-keep form as the `proof:stage-not-clipped` FALLBACK if the stronger form clips. WHY: the named frame gives every H finding a slot (D1 = `[rail]` one-column, D4 = `[rail][bottom]` rail-width, D10 = the same grid re-parameterized in H.W7) and DELETES the `1fr 1fr` + `col-end-4`/`col-span-full` hacks + the desktop overlay slide. **The `--work-area-*-bias` algebra deletion is DOWNGRADED (WV-W3-MED-1):** both docks remain `position:fixed` (the grid-track docks are deferred F7), so the occlusion algebra STILL has a job — S4 deletes ONLY the `col-end-4`/`col-span-full`/collapse hacks; the ~40-line `--work-area-*-bias` algebra deletion is BOOKED to the dock-relocation work (MEASURE-FIRST there), NOT claimed here. **S4 is the MEASURE-FIRST step** — gated by `proof:stage-not-clipped` (the B.W3 "cube half-clipped" invariant the grid comment guards); S1–S3 alone already make the ribbon match the sidebar, S4 makes the TOKEN literally equal the rendered column. (`a-demo-architecture.md:206-265`, `a-timeline-width.md:98-113,166-169`)

## §Hard gate (the proof:* that BITES — born-RED-today, GREEN-on-fix)

Three new Playwright/grep gates wired into the `proof:*` set (settle-gated on the H.W1 FSM resting — WV-W3-MED-3: make the preconditions EXPLICIT: viewport ≥1024, pane OPEN, route rested ≥500ms; PIN the scene to `#/cube`; re-ASSERT the test viewport AFTER navigation — Playwright resets to 390 on navigate; assert the JS DESKTOP state (no `controls-pane--mobile` class), not just the CSS grid; wait until `getComputedStyle('.controls-layout').gridTemplateColumns.split(' ')[0]` resolves to the `--rail-width` value before asserting; cross-ref H.W1's `proof:no-route-storm` as the flake-defeat so the D12 churn cannot flap the measurement):

- **`proof:single-column-pack` (the column-count + mis-size lock · WV-W3-MED-2: measure the LEAF rows, pin `#/cube`, guard non-vacuity).** Measure the LEAF rows `.controls-content .labeled-field` (NOT `CardContent.children` — that has ONE `panel-stack col-span-2` child → set-size 1 = born-GREEN vacuous; the two columns live at the LEAF `.labeled-field` level through the subgrid chain). PIN `#/cube` (easing has 0 fields → the gate reds VACUOUSLY on a field-less scene). Add a `fieldCount >= 6` NON-VACUITY guard. Assert ALL leaf field rows share ONE left-edge `x` (`new Set(rows.map(r => Math.round(r.getBoundingClientRect().x))).size === 1`) AND every row width is within ±2px (`max(w) - min(w) <= 2`). **BITE:** reds TODAY — the left-edge set is `{76, 300}` (size 2) and the width set is `{212, 466}` (Δ=254); greens ONLY after the two-track grid collapses to a single-column stacked flow. Catches any regression to `auto`/`1fr` or a re-split row. (`hd-live-controls F1`, `a-controls-sidebar.md:206-214`, `a-demo-architecture.md:86-89`)
- **`proof:timeline-rail-width` (the width-binding lock).** At 1440×900, navigate `#/cube`, open the controls pane, settle-gate, then assert `width(#timeline-expanded-target) === width(.controls-content) === width(AnimationControls root) === parseFloat(--rail-width)` (±2px each). **BITE:** reds TODAY — `1272 vs 768 vs 400`, three numbers, zero agreement; greens only when all four resolve to `--rail-width`. A `proof:mobile-ribbon-full-bleed` companion (at 390px the ribbon ≈ controls-pane width) locks that the desktop cap does NOT leak into mobile (today passes 296≈366 — guard so the desktop fix doesn't break the H.W7 full-bleed). (`a-timeline-width.md:151-165`, `a-demo-architecture.md:116-119`)
- **`proof:demo-shell-grid` (the architecture lock — static grep + computed · WV-W3-HIGH-1: narrow the grep PATH SET to avoid out-of-scope reds).** Assert the shell root resolves to exactly the named `[rail] var(--rail-width) [stage] 1fr` × `[top]auto [stage]1fr [bottom]auto` template; grep-gate over the S1–S4 rewrite files PLUS `TimingFunctionPanel.vue` (in scope via S3b) — EXCLUDE `AssetPropertiesPanel.vue` by name (a separate tree, NOT this wave's — its `grid-cols-[auto_1fr]` is legitimate out-of-scope code) — asserting ZERO surviving occurrences of `grid-cols-[auto_1fr]`, `grid-cols-[subgrid]`, `col-span-2`, `col-end-4`, `col-span-full`, `grid-template-columns: subgrid` in the scoped files. **KEEP the `--controls-pane-width` token-deletion clause TREE-WIDE** (the rename to `--rail-width` IS global — land it FIRST so W4/W5 appends never reference the dead token). **BITE:** reds TODAY (every one of those tokens is live); pins the deletion so the obsolete apparatus cannot creep back; the narrowed path set means it does NOT red on `TimingFunctionPanel.vue:78`/`:3` (W4-owned detail panel grid — now collapsed in S3b) or `AssetPropertiesPanel.vue:6` (separate tree). Paired with `proof:stage-not-clipped` (MEASURE-FIRST guard for S4): at 1280 & 1440, pane OPEN and CLOSED, the stage subject's bounding box is fully within the viewport (re-asserts B.W3); the conservative `col-end-4`-keep form is the FALLBACK if the stronger `[stage]`-track form clips (WV-W3-HIGH-3) — the gate that lets S4 ship safely. (`a-demo-architecture.md:260-264`, `a-timeline-width.md:166-169`)

**The §Mandate bar — MUST bite.** Every clause asserts an EXACT live measurement the lanes already captured (the `{76,300}` left edges, the `{212,466}` widths, the `1272/768/400` regimes, the live `--controls-pane-width`/subgrid tokens) — none passes vacuously; each reds on the live tree TODAY and greens only on the transposition. The grep clause is the no-legacy lock (the two-track grid + 768 cap + `--controls-pane-width` are REMOVED, not re-masked).

## §Folds

- `a-controls-sidebar` **F1** (the two-track grid is the wrong substrate — S1) + **F3** (the subgrid/col-span chain is dead weight — S1, delete) + **F4** (the mixed-paradigm easing/z-index rows — S2; the easing label-action slot is a possible small glass-ui-HANDOFF iff no label slot exists) + **F5** (gap-y incoherence — folds into S1). **F2** (glass-ui `LabeledField` single-cell) = RECORD (root cause; glass-ui is CORRECT — NO glass-ui handoff for the field shape itself).
- `a-timeline-width` **(i)+(ii)** (pin the pane box to the token + delete the divergent cap — S3, SHIP-in-H, low-risk one-token) + **(iii)** (the grid-track tightening — S4, MEASURE-FIRST gated by `proof:stage-not-clipped`).
- `a-demo-architecture` **F1** (D1 sidebar two-fields-per-row — S1) + **F2** (D4 full-width = the expanded timeline — S4) + **F4** (the 3-track grid vestigial → named `rail·stage·rail` + one `--rail-width` — S4, THE SPINE). **F5** (D10 mobile single-page bottom-sheet) is the SAME grid re-parameterized — **HANDED to H.W7** (this wave lands the desktop frame `--rail-width` is single-sourced from, so H.W7 only re-parameterizes the track placement). **F3/F6/F8** (the scene+playback FSM, App.vue decomposition, the engine snapshot seam) are **H.W1's** — this wave DEPENDS on the FSM landing first (so the layout sits on a stable behavioral spine and does not re-litigate the home↔cube alias) but writes none of it. **F7** (hero/dock/dots slots) = RECORD / cross-ref — the `[stage]` empty-state slot is reserved here for H.W4's hero, the `[top]`/`[bottom]` dock tracks for the glass-ui-HANDOFF dock.

## §Design decisions (trade-offs RESOLVED)

- **Single-column stack, not a `grid-cols-1` swap — RESOLVED.** D1 is NOT a one-line `grid-cols-1` change; it is the REMOVAL of an obsolete two-track + subgrid + `col-span-2` apparatus that became load-bearing machinery for a layout the components no longer want. A `LabeledField` is already a complete, self-laying-out unit; the container is a 1-D vertical stack (`flex flex-col`), and every escape (`col-span-2`, the split-row easing/z-index) vanishes in the same motion. (`a-controls-sidebar.md:31-39,129-135`)
- **One token, not a wrapper component — RESOLVED.** The coupling already lives in a CSS custom property (`--rail-width`) consumed by the grid track and the pane; adding a width-binding CONTAINER would be a new god-wrapper over a value a single property already expresses. KISS says collapse onto the token ("N files, one token, actually honored"), do NOT invent a `<TimelineWidthProvider>`. (`a-timeline-width.md:115-120`)
- **The expanded timeline aligns to the rail, not `col-span-full` — RESOLVED.** D4 as literally worded ("PlaybackRibbon full-width") is ALREADY satisfied (the ribbon is sidebar-width via the teleport); the genuinely full-width element is `#timeline-expanded-target`. The fix is to give it `grid-column: 1` (the `[rail]` track) so it inherits the rail width — no magic number, a vertical extension of the controls rail. (`a-demo-architecture.md:96-120`)
- **S4 is MEASURE-FIRST, S1–S3 ship the binding alone; the work-area algebra is NOT deleted here — RESOLVED (WV-W3-MED-1).** The overlay/centering interplay (the B.W3 "cube half-clipped" blocker cited in the component's own comment) must not regress. S1–S3 (the one-column stack + the token-as-width + the cap deletion) already make the ribbon match the sidebar and are low-risk; S4 (the grid-track tightening to make the token LITERALLY equal the rendered column, + deleting the `col-end-4`/`col-span-full`/collapse hacks + the desktop overlay slide) ships only behind a green `proof:stage-not-clipped`. **The ~40-line `--work-area-*-bias` algebra deletion is DOWNGRADED out of S4:** both docks remain `position:fixed` (grid-track docks are deferred F7), so the algebra still has a job — it is BOOKED to the dock-relocation work (MEASURE-FIRST there), NOT a claimed line-count win in H.W3. (`a-timeline-width.md:98-113,187-189`, `a-demo-architecture.md:233-245`)
- **DEPENDS on H.W1, INDEPENDENT of H.W2 — RESOLVED.** The autonomous route storm (D12) made width measurement flaky live (`a-timeline-width §4`: every `page.goto` bounced through a restored scene, the grid measured `390px` mid-swap) and would flake a width visual-lock test — so the layout gates settle-gate on the H.W1 FSM resting, and the wave sequences AFTER H.W1. The cartoon-depth/specular work (H.W2) is on a different axis (surface chrome, not grid topology) and does not gate this. (`a-timeline-width.md:131-137`, `a-demo-architecture.md:124-126`)
- **The crossfade is untouched — RESOLVED (ALREADY-SOTA).** The `grid-template-rows: 0fr↔1fr` panel crossfade (`AnimationControlsControls.vue:293-302`) is exemplary and orthogonal to the columns; the transposition keeps `display:grid; grid-template-rows` and removes ONLY `grid-template-columns`. Re-touching it would be the inverse failure (manufacturing work where the post-G state leads). (`a-controls-sidebar.md:152-155,242-245`)
