# Tranche H DEEP harden — lane `hd-chronic-recap`

**Charge.** CHRONIC-CLOSURE + RECAP completeness. (1) The four chronics (cartoon-shadow,
φ-hero, mobile, dock) each exit via a SYSTEM-property gate OR a HANDOFF + born-RED gate —
no re-classification escape. (2) The A→H recap homes EVERY user ask (D1–D14 + recap/
precept/deferred/dock-note + easing-modern-web + specular-refine + scene-corruption +
drawer-spring) terminally. (3) The deferred-ledger fold leaves zero perpetual punt.

**Method.** Read H.md, PROGRESS.md, waves/H.W0–W8.md, `_SYNTHESIS-{deferred-ledger,
prompt-recap,gap-scorecard,dock-perf-modes}.md`, `a-deferred-chronic.md`. Cross-checked
gate names across the four canonical chronic sources. Live-verified the dock-chronic
born-RED anchors on the running demo (`:5173`). gap-scorecard is AUTHORITATIVE where it
disagrees.

**Headline verdict.** The chronic-closure ARCHITECTURE is SOUND and the four chronics are
each genuinely born-RED on the live tree (verified) — the exits are honest, not papered.
The recap is complete: all D1–D14 + the mandate items are terminally homed; the four
"recap-extra" asks my prompt names are all sub-facets of existing D-defects and ARE homed.
The deferred fold leaves no perpetual punt. **BUT** the chronic meta-gate — the single
load-bearing instrument that makes H "the LAST re-paper" — has a self-inconsistency that
will mis-bite at implementation time: its PARSEABLE SUBSTRATE (the PROGRESS.md table) names
a dock gate (`proof:dock-morph-settled`) that NO wave authors as a runner, while the wave
authors a DIFFERENT gate (`proof:dock-live`) the substrate table does not name. One HIGH,
three MED, two LOW. No BLOCKER.

---

## FINDINGS

### H-1 (HIGH) — the chronic meta-gate's substrate names a dock gate no wave authors; resolve-or-red mis-bites

**Location:** `waves/H.W8.md:33` (the meta-gate S3 substrate spec) vs `PROGRESS.md:272`
(the parseable chronic→gate table, the dock row) vs `waves/H.W8.md:46` (the gate W8
actually authors).

**Defect (with evidence).** The W8 meta-gate is specified as a STATIC resolve-or-red parse
of the COMMITTED chronic→gate table in `PROGRESS.md` (canonical source
`_SYNTHESIS-deferred-ledger §1`). Rule (ii): *"every row whose closure is a HANDOFF carries
a born-RED `proof:*` gate name that likewise resolves"*; rule (iii): a row whose gate does
not resolve to an authored runner REDS (this is the M1-paper-close catch — W8.md:45 BITE
(2): *"name a SYSTEM gate that no wave authors → reds on the dangling reference"*).

The dock chronic's gate name is INCONSISTENT across the three sources the gate touches:

| Source | Dock chronic gate name(s) |
|---|---|
| `PROGRESS.md:272` (the table the gate PARSES) | `proof:dock-morph-settled` **only** |
| `_SYNTHESIS-deferred-ledger §1` `:221` (the named CANONICAL source) | `proof:dock-live` **only** |
| `waves/H.W8.md:46` (the gate W8 AUTHORS as a runner) | `proof:dock-live` **only** |
| `waves/H.W8.md:33` (ii) (the meta-gate's prose) | `proof:dock-live`/`proof:dock-morph-settled` (both, slash) |
| `H.md:417` S4 / `H.md:490` §4 | `proof:dock-live`/`proof:dock-morph-settled` (S4); `proof:dock-morph-settled` (§4 table) |

`proof:dock-morph-settled` (≤6% overshoot, ≤200ms) is described in
`_SYNTHESIS-dock-perf-modes.md:109,369` as **glass-ui-owned, kf re-asserts post-bump** — it
is NOT authored as a kf runner in any wave. The gate W8 authors (W8.md:46) is
`proof:dock-live` (popover opens + ≤1-frame settle). So the meta-gate, parsing the
PROGRESS.md dock row, looks up `proof:dock-morph-settled`, finds no authored kf runner →
by its OWN rule (iii) it REDS on a dangling reference forever (or, if implemented to accept
the slash-alias loosely, passes vacuously — the worse failure, since vacuous-pass is
exactly the M3 escape the gate exists to catch). Either way the load-bearing meta-gate does
not bite as specified. The docs paper the gap by writing both names with a slash in PROSE
(W8.md:33, H.md:417), but a static table-parser reads the COMMITTED CELL, which names one.

**Concrete doc edit.** Pick ONE canonical name per the gap-scorecard's authority and make
the THREE artifacts agree:
- Make `PROGRESS.md:272`'s dock row read: *"HANDOFF + born-RED gate — release `53c1b07`;
  **`proof:dock-live`** born-RED (the kf-side WATCH: popover opens + dock settle budget),
  paired with the glass-ui-owned `proof:dock-morph-settled` re-asserted post-bump (H.W8/§4)."*
  i.e. name `proof:dock-live` as the kf-authored gate the meta-gate resolves, and demote
  `proof:dock-morph-settled` to a *glass-ui-owned post-bump re-assert* (not a kf runner the
  meta-gate parses for).
- In `_SYNTHESIS-deferred-ledger §1 :221` and §3.1 :247-249, keep `proof:dock-live` (already
  there) and add the same demotion note so all four sources name `proof:dock-live` as the
  kf-side born-RED runner.
- In `waves/H.W8.md:33` (ii), replace *"the dock row names `proof:dock-live`/
  `proof:dock-morph-settled`"* with *"the dock row names `proof:dock-live` (the kf-authored
  born-RED runner the meta-gate resolves); `proof:dock-morph-settled` is the glass-ui-owned
  post-bump re-assert, NOT a kf runner — the meta-gate does not parse for it."*

---

### H-2 (MED) — CH-4's popover half (D9) escapes the chronic ledger table entirely

**Location:** `PROGRESS.md:272` (chronic table dock row), `a-deferred-chronic §1 CH-4`
(`a-deferred-chronic.md:64`), `_SYNTHESIS-deferred-ledger §1 :221`, vs `H.md:181` /
`H.md:326` S5 (D9 homed in H.W1).

**Defect (with evidence).** CH-4 was authored A→G as a TWO-defect chronic: (a) the dock LAG
(D5-b) and (b) the popover-not-opening (D9). The canonical CH-4 closure gate `proof:dock-live`
asserts BOTH ("popover OPENS on click; dock expand/collapse settles ≤1 frame" —
`a-deferred-chronic.md:115`, `_SYNTHESIS-deferred-ledger.md:221`). In H, the popover half
(D9) was re-root-caused as a kf-side **double-wrapped trigger** (`App.vue:18-21`) and SHIPped
in **H.W1** with its OWN gate `proof:dock-popover-opens` (`finalOpen:true`; `H.md:327`). That
is a correct, honest product fix. BUT the chronic-closure TABLE the meta-gate parses
(`PROGRESS.md:267-272`) reduces the dock chronic to *"dock LAG (D5)"* and closes it via the
HANDOFF gate ONLY — the popover half is no longer represented in the chronic row. CH-4's
own escape mechanism was M3 (column-migration); splitting CH-4 so the popover exits via an
unrelated wave's gate (H.W1) while the chronic table tracks only the lag half is a soft
re-occurrence of the same split-and-lose-the-other-half pattern the chronic-closure
discipline exists to forbid. It is NOT a paper-close (D9 is genuinely gated born-RED in
H.W1, verified live `aria-expanded:false`), but the LEDGER no longer shows that the popover
half of the chronic is discharged — exactly the auditability the meta-gate was built to
guarantee.

**Concrete doc edit.** In the `PROGRESS.md:267-272` chronic table, the dock row's closure
cell should name BOTH halves and BOTH gates so the meta-gate parse covers the whole chronic:
*"**dock (D5 lag + D9 popover)** | M3 | **SYSTEM gate** `proof:dock-popover-opens` (D9, H.W1
kf-side SHIP) + **HANDOFF + born-RED gate** `proof:dock-live` (D5 lag, H.W8 — release
`53c1b07`)."* Mirror in `_SYNTHESIS-deferred-ledger §6` and `a-deferred-chronic §6 DC-5`.

---

### H-3 (MED) — gate-name drift between the canonical chronic sources defeats the meta-gate's "resolves" check on >1 row

**Location:** `_SYNTHESIS-deferred-ledger §1 :217-223` (canonical gate family) vs the
sibling-wave gate names, parsed against `waves/H.W8.md:33` rule (i).

**Defect (with evidence).** The meta-gate parses the chronic table and requires every named
gate to RESOLVE to an authored runner. The canonical `_SYNTHESIS-deferred-ledger §1` gate
table (`:217-223`) names gates that DO NOT MATCH the gates the sibling waves actually author,
beyond the dock case (H-1):

| Chronic | deferred-ledger §1 canonical name | the gate a wave actually authors |
|---|---|---|
| mobile CH-3 | `proof:mobile-composition` (`:221`) + `proof:motion-timing` (`:224`, §3.1 `:249`) | `proof:mobile-single-page` + `proof:drawer-spring` (H.W7; `H.md:405`, `PROGRESS.md:116`) |
| cartoon CH-1 | `proof:cartoon-is-panel-depth` (`:219`) | `proof:cartoon-is-panel-depth` + `proof:no-orphan-specular` (H.W2) — MATCH+1 |
| φ-hero CH-2 | `proof:phi-leaf-zero` (`:220`) | `proof:phi-leaf-zero` (H.W4) — MATCH |
| D12 | `proof:scene-state-machine` (`:222`) | `proof:scene-machine-irrefragable` (H.W1; `H.md:327`) |
| D13 | `proof:motion-timing` (`:224`) | `proof:drawer-spring` (H.W7) |

`proof:mobile-composition`, `proof:motion-timing`, `proof:scene-state-machine` are DANGLING
names — no wave authors a runner under those names; they were the deferred-ledger lane's
PROVISIONAL gate names, superseded by the gap-scorecard/wave names but never reconciled back.
W8.md:33 explicitly names `_SYNTHESIS-deferred-ledger §1 :219-223,247-249` as the meta-gate's
"canonical source." So a literal implementation reading those cells reds on three dangling
names. The PROGRESS.md table (which W8.md ALSO names as the substrate) happens to use the
correct H.W7 names for mobile — so the gate has TWO disagreeing "canonical" substrates.

**Concrete doc edit.** Reconcile `_SYNTHESIS-deferred-ledger §1 :217-223` and §3.1 :247-249
to the wave-authored gate names: `proof:mobile-composition`→`proof:mobile-single-page` +
`proof:drawer-spring`; `proof:motion-timing`→`proof:drawer-spring`;
`proof:scene-state-machine`→`proof:scene-machine-irrefragable`. Add a one-line note in
W8.md:33 that PROGRESS.md is the SINGLE parseable substrate and the deferred-ledger §1 table
is descriptive history (so there is ONE table the gate parses, not two).

---

### H-4 (MED) — the H.W0 born-RED crash anchor did not reproduce on the live fresh Cube load

**Location:** `H.md:311-314` (H.W0 `proof:demo-console-clean`), `PROGRESS.md:109,174`.

**Defect (with evidence).** H.W0's gate asserts *"a fresh `#/cube` load = 0 console errors
(RED: 4× `serializeEasing` today)"*. On live load (`:5173`, kf 4.1.0 + Tranche G) the page
landed on `#/cube` then **autonomously navigated** to `#/easing?anim=Easing+Preview`, and
the console reported **0 errors, 2 warnings** at load — the 4× `serializeEasing` throw did
NOT fire on this fresh load. This is not in my core lane (it is H.W0's anchor, an engine-
regression lane's province), but it is load-bearing for the CHRONIC honesty thesis: the
recap/charter repeatedly assert the crashes "poison every other measurement" and "must die
FIRST" as the prerequisite that gates the whole DAG. If the crash is INTERMITTENT (fires
only when the dock Select is touched / a cube preset readout renders, not on a bare load
that immediately route-storms away to easing), the gate's RED-anchor wording ("EVERY fresh
Cube load") over-states and the gate could pass vacuously on a load that storms past cube.
The route storm (D12) may be MASKING the cube readout that throws.

**Concrete doc edit.** Re-anchor `H.md:311` / `PROGRESS.md:109` to the exact reproduction
trigger, not "every load": e.g. *"4× `serializeEasing` on a Cube load that RESTS on `#/cube`
and renders the KeyframesString readout (the route storm may navigate away before the throw
on an un-pinned load; pin `#/cube?anim=…` to reproduce)."* This keeps the gate falsifiable
and BITING regardless of the D12 storm timing. (Hand to an engine-regression harden lane to
confirm the exact trigger.)

---

### H-5 (LOW) — the recap-extra asks are all homed, but two are homed only IMPLICITLY

**Location:** `_SYNTHESIS-prompt-recap §P10` (the D1–D14 table), §2 (precepts).

**Verification (the charge's positive half — homed, with the gaps).** My prompt names four
"recap-extra" asks beyond D1–D14. Each IS terminally homed:
- **easing-modern-web** — homed: D3 easing-editor → H.W4; the canvas ceiling is explicitly
  a "modern-web Baseline-2023 container query" (`H.md:365` S1, `_SYNTHESIS-prompt-recap` E2
  "modern-web facet folds into the D-defect lanes"). TERMINALLY HOMED.
- **specular-refine** — homed: D14 → H.W2 (`useSpecularPointer` on retained glass +
  glass-ui-HANDOFF calmer default). TERMINALLY HOMED (`prompt-recap §P10 D14`).
- **scene-corruption** — homed: D12 → H.W1 (the FSM keystone). TERMINALLY HOMED.
- **drawer-spring** — homed: D13 → H.W7 (`SpringProgress` sheet). TERMINALLY HOMED.

The LOW: the recap §P10 table is keyed D1–D14 and the four extras are folded INTO D-rows
(D3/D14/D12/D13) without being named as the user's separate phrasings, so a reader scanning
for "easing modern-web" or "scene corruption" as discrete asks finds them only by knowing
the D-mapping. Not a homing gap (each lands in a wave with a biting gate) — a discoverability
nit.

**Concrete doc edit (optional).** Add a one-line "user-phrasing alias" column or footnote to
`_SYNTHESIS-prompt-recap §P10` mapping each extra phrasing to its D-row (easing-modern-web→D3,
specular-refine→D14, scene-corruption→D12, drawer-spring→D13) so the recap is searchable by
the user's own words.

---

### H-6 (LOW) — two "DECIDE" items in the deferred fold risk becoming the next perpetual punt

**Location:** `_SYNTHESIS-deferred-ledger §3.1` (DC-8 scene-swap VT dead-CSS),
`§3.3` (FB-6 `Mod+K` palette); `a-deferred-chronic §4 / §6 DC-8`.

**Defect (with evidence).** The fold's central claim is "zero perpetual punt." Two items are
disposed as **DECIDE** rather than a terminal SHIP/KILL/BOOK:
- **DC-8** (scene-swap VT dead-CSS, "twice-deferred A→B→C") — disposition "KFH — DECIDE (KILL
  the dead CSS or RESTORE via VT)" with gate "grep = 0 after the decision." The decision is
  deferred TO an implementation choice with no owning wave: DC-8 appears in
  `_SYNTHESIS-deferred-ledger §3.1` and §6 but I find NO H.W# that owns the DECIDE. A
  twice-deferred item whose H disposition is "decide later, in some wave" is structurally the
  same phantom-owner re-defer the lane names (`a-deferred-chronic §0`) — it just relocated
  the punt from "next tranche" to "whichever H wave touches scene-swap."
- **FB-6** (`Mod+K` palette) — "BOOK (demo, low) — DECIDE owner (demo-local vs glass-ui
  shell)"; again no owning wave names the decision.

These are LOW (DC-8 is dead CSS, FB-6 is a non-defect BOOK), but the lane's own
P-invariant ("no item re-deferred without the decision") is technically unmet: neither
DECIDE is assigned to a named wave with a gate.

**Concrete doc edit.** Assign DC-8 to a named wave (H.W5 already touches scene/`useSceneSwap`
— fold the DECIDE there with the `grep=0` gate) and FB-6 to H.WZ's BOOK ledger with an
explicit "owner decision recorded at H.WZ" line, so neither is a free-floating "decide
sometime." Then the §3 fold is genuinely punt-free.

---

## What is SOUND (no finding — honest credit)

- **The chronic-closure ARCHITECTURE is the right repair.** The M1/M2/M3 escape taxonomy
  (`a-deferred-chronic §2`) is accurate and the "a chronic exits only via a SYSTEM gate OR a
  HANDOFF + born-RED gate" meta-invariant genuinely closes all three escapes. The meta-gate
  as a STATIC parse of a committed table (W8.md:33) is implementable and idiomatic (it reuses
  the `proof:idioms` clause-1 resolve-or-red + `LISTENER_ALLOWLIST` stale-guard shapes that
  already exist) — no non-existent API. The defects above are NAME-RECONCILIATION, not
  architecture.
- **The four chronics are genuinely born-RED on the live tree (verified, not asserted).**
  `--spring-dock` live = `linear(0, 0.10932 …)` peak **1.16292 (+16.3% overshoot)** — the
  pre-AW.W2 register is real; the `@mbabb` `dock-dropdown-trigger` is `aria-expanded:false`
  (popover does not open); `.glass-specular-track` present on dock panels; the route
  autonomously stormed `#/cube`→`#/easing` mid-session (D12 live). The exits are anchored to
  real defects, not paper. (NOTE: live overshoot is +16.3%, docs say "+18.5%" at
  `H.md:198`/`gap-scorecard §1.1`/`PROGRESS.md:198` — a minor inv-ε precision drift; cross-
  repo/dock-lane's to reconcile, flagged here only because it is the dock-chronic born-RED
  numeric.)
- **The recap is COMPLETE.** All D1–D14 have an owning wave with a biting gate; the phase-1
  UNOWNED trio (D5/D9/D12) is now owned; the mandate items (recap/precept/deferred/devonly/
  dock-note) are each HONORED with anchors; the four recap-extra asks are all homed (H-5).
  No ask is orphaned.
- **The deferred fold is essentially punt-free.** Every A→G carry has a terminal
  (KFH/KILL/OUT/CHRONIC-by-design/sibling-HANDOFF/MEASURE-FIRST/BOOK/RECORD/USER-DOMAIN);
  C-1 is correctly CHRONIC-by-design (the "did the product move?" test is a sound
  discriminator); the only residual punt-risk is the two un-assigned DECIDEs (H-6).
- **The HANDOFF pairing discipline is real.** Each sibling-HANDOFF (dock, Card-specular,
  value.js next-slice, parse-that re-key) is paired with a born-RED kf gate
  (`gap-scorecard §4`, W8.md:46, H.W2.md:52 `proof:specular-handoff`) so a bare column-
  migration cannot masquerade as closure — the M3 repair holds.

## Summary

Chronic-closure architecture: SOUND. Four chronics: genuinely born-RED (live-verified),
no re-classification escape at the ARCHITECTURE level. Recap: COMPLETE (all D1–D14 + mandate
+ 4 extras homed). Deferred fold: punt-free but for two un-assigned DECIDEs (H-6, LOW).
The one HIGH (H-1) is a self-inconsistency in the meta-gate's parseable substrate — it names
a dock gate (`proof:dock-morph-settled`) no wave authors while authoring a different one
(`proof:dock-live`), so the load-bearing "resolve-or-red" meta-gate mis-bites as written;
fix by reconciling the dock gate name across PROGRESS.md / deferred-ledger §1 / W8.md to ONE
kf-authored runner. H-2/H-3 are gate-name drift of the same family (CH-4 popover half left
off the table; deferred-ledger §1 carries three superseded provisional names). No BLOCKER —
all are doc reconciliations, not architecture failures.
