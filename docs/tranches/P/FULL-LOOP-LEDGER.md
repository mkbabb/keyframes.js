# Tranche full-loop ledger (harden · validate · brainstorm · reformulate) — 2026-06-22

> The validated loop applied to EVERY constellation tranche item (15 clusters, 67 items). Testable items were BENCHED/PROBED in-session. 34 KEEP · 12 AUGMENT · 11 RE-SCOPE · 7 ADOPT · 2 SIMPLIFY · 1 KILL.

## P.W1-apparatus

### [KEEP] S1 — the eslint-flat + dep-cruiser lint-tier (the O Band A carry)

**Harden:** CONFIRMED ABSENT: `ls .eslintrc* eslint.config.* .dependency-cruiser*` → NO MATCHES on today's tree. Neither eslint nor dependency-cruiser is a devDependency (`package.json` check: eslint NOT FOUND, d · CIRCULAR IMPORT PASSES TSC CONFIRMED: /tmp probe shows that a typed circular import (functions with explicit return types) compiles with `tsc --noEmit --strict` exit 0, zero errors. The `internal/leav · CARRY IS REAL: M.W2 developed, O.W1 scoped, neither implemented. This is a genuine 3-tranche carry with no ceremony — the lint floor has a real function (early-catch for `proof:boundary`'s bundle-grap · KISS CONCERN: the spec mentions `eslint.config.js` (flat config, ESM) but the package is Node ≥22 — this is the correct idiom. The dep-cruiser boundary rules (a) + (b) are NOT duplicates of `proof:bou · NO CONTRIVANCE: no external prototype or unshipped sibling API required. Pure static tools over today's installed tree. · MINOR SPEC INCONSISTENCY: the spec says `eslint.config.js` in one place and `eslint.config.mjs` in another (CONTRIVANCE-AUDIT.md line 35 says `.mjs`). Node ≥22 + `type: module` in package.json → `.js`

**Validation (RAN):** node scripts/proof-bench-taxonomy.mjs → PASS (confirming the gate apparatus that S1 will wire into proof:hygiene is healthy). tsc circular-import probe: `npx tsc --noEmit --strict --target ES2022 --moduleResolution bundler --module ESNext /tmp/test-circular-c.ts /tmp/test-circular-d.ts` → exit 0, 0 errors. Lint config absent confirmed: `ls /Users/mkbabb/Programming/keyframes.js/.eslintrc* eslint.config.* .dependency-cruiser*` → NO MATCHES.

**Brainstorm:** Simplify the lint-tier to the smallest set that actually bites: `import/no-cycle` (circular) + `no-restricted-imports` for the LIGHT-imports-value.js boundary.  · The dep-cruiser config should explicitly reference the `proof:boundary` bundle invariants by comment so maintainers know the two layers agree — preventing futur · Unify the config file name: use `eslint.config.mjs` (matches CONTRIVANCE-AUDIT.md line 35) to be consistent across all doc references, or pick `.js` and update 

**→ Reformulation:** S1 unchanged in substance. Add one prose correction: unify `eslint.config.js` → `eslint.config.mjs` (or vice versa) across all references, and narrow the eslint ruleset to `import/no-cycle` + `no-restricted-imports` (KISS) with the rationale that `tsc --strict` already covers most correctness rules.

---

### [AUGMENT] S2 — EXTEND the bench taxonomy to the optimization-frontier scenario classes

**Harden:** REAL GAP CONFIRMED (compositor-SoA): `bench/group-composite.bench.ts` DOES NOT EXIST. The spike ran via `bench/group-soa-validate.mjs` (standalone mjs) and produced `scripts/group-soa-decision.json` ( · STALE ITEM (codegen-parse): P.W4 is a RETIRED TOMBSTONE as of the 2026-06-22 owner directive. P.W1 S2 still proposes a `codegen-parse` scenario class that `P.W4 graduates`. There is no graduating wave · CROSS-REPO DISPATCH (color2Into-consume): The `color2Into` scenario class is a value.js-P ask (VJ-P1). Adding a `crossRepo`-tagged entry to taxonomy.json tracking the dispatch is sound — the `proof:be · ACTUAL UNCLASSIFIED GAP MISSED BY SPEC: `bench/sync-step.bench.ts` (4 cases: drive(SmoothProgress), drive(SpringProgress), play(Animation K=8 transform), play(AnimationGroup 32 cells K=8)) is NOT in ` · O.W8 STALE CROSS-REFERENCE: P.W1 states that O.W8 S1/S2 already authored `bench/numeric-soa.bench.ts` and `bench/color-interp.bench.ts`. Neither file exists on today's tree. The P.W1 spec defers to no · CURRENT TAXONOMY STATE: 42 classified cases across 5 suites. `pendingBudgeted: []` is empty (all budgeted arms shipped). `crossRepo` has 8 VJ entries. The taxonomy is GREEN today per `proof:bench-taxo

**Validation (RAN):** node scripts/proof-bench-taxonomy.mjs (run twice): PASS both times. budgeted SoA arm: 207626.4 hz >= 3740.0 hz (1.2× baseline). budgeted spring-vector: 50887.9 hz >= 15702.9 hz. budgeted warmEngine: 9877574.5 hz >= 1000.0 hz. `ls bench/group-composite.bench.ts bench/color-interp.bench.ts bench/numeric-soa.bench.ts` → DOES NOT EXIST (3 files). `npx vitest bench --run bench/sync-step.bench.ts --outputJson /tmp/sync-step-bench.json` → exit 0, 4 cases reported (drive SmoothProgress 17281 hz, drive S

**Brainstorm:** Add `bench/sync-step.bench.ts` to taxonomy.json suites with its 4 cases classified as `run-check` (they are jsdom-compatible, existing, currently invisible to t · Drop the codegen-parse scenario class from S2 entirely (P.W4 is RETIRED). The taxonomy should not track a scenario class for a RETIRED wave. · Keep the compositor-SoA pendingBudgeted entry for `bench/group-composite.bench.ts` — the spike validation (standalone mjs) is evidence the idea is sound, but th · The color2Into cross-repo entry is sound but consider whether VJ-P1 is already tracked in the existing `crossRepo[]` array (VJ.L5 covers gamutMapToRgbSpace) — c

**→ Reformulation:** S2 retains the compositor-SoA pendingBudgeted arm (bench/group-composite.bench.ts genuinely absent) and the color2Into cross-repo tracking. Drop the codegen-parse scenario class — P.W4 is a RETIRED tombstone, no graduating wave exists. ADD a new taxonomy entry: classify bench/sync-step.bench.ts (4 run-check cases) as the REAL currently-unclassified coverage gap. Correct the O.W8 cross-reference: numeric-soa.bench.ts and color-interp.bench.ts do not exist on today's tree; reference O.W8 as 'proposed but not shipped' rather than 'already authored'.

---

### [AUGMENT] S3 — `proof:portable-perf`: the ONE shared ratio-gate helper

**Harden:** DUPLICATION IS REAL: `proof-bench-taxonomy.mjs` implements `baselineCase × floorFraction` ratio logic in ~25 lines (lines 295–320). `proof-spring-vector.mjs` implements `vectorHz / scalarHz >= WIN_FRA · JSON.PARSE ANCHOR ABSENT IN KF: `scripts/lib/` has 6 files (agent-surface.mjs, ci-env.mjs, console-budget.mjs, demo-driver.mjs, gate-shape.mjs, taste-packet.mjs). No `portable-perf.mjs` exists. The `j · FUTURE GATE CONSUMERS CONFIRMED: `docs/tranches/P/waves/P.W2.md` explicitly references `ratioGate` and `scripts/lib/portable-perf.mjs` 10+ times — P.W2's `proof:soa-composite` is designed to call `rat · CONTRIVANCE CHECK — PASSES: the helper is NOT a new abstraction for its own sake. It consolidates 2 existing re-implementations into 1 shared module. The consumers (P.W2, P.W3) already exist in the sp · WARMENGINE ABSOLUTE FLOOR INCONSISTENCY: taxonomy.json has one `budgeted` case using `floorHz: 1000` (absolute) instead of `baselineCase × floorFraction` (ratio). The portable-perf.mjs refactor of pro · REFACTOR SCOPE: S3 says proof-bench-taxonomy.mjs and proof-spring-vector.mjs are REFACTORED to import ratioGate (the re-implemented math DELETED). This is correct — no-legacy. But the refactor must NO

**Validation (RAN):** Duplication confirmed by code inspection: proof-bench-taxonomy.mjs:295-320 (~25 lines ratio math); proof-spring-vector.mjs:144-145 + 174-177 (~15 lines). `ls /Users/mkbabb/Programming/keyframes.js/scripts/lib/portable-perf.mjs` → ENOENT. P.W2.md references to ratioGate: 10 matches confirmed by grep. value.js JSON.parse anchor confirmed at /Users/mkbabb/Programming/value.js/scripts/proof-perf-target.mjs lines 62-73.

**Brainstorm:** The shared helper should export two functions: `ratioGate({report, baselineCase, candidateCase, floorFraction, posture, reason})` for same-report ratio gates, a · The `jsonParseAnchor()` function is only relevant if kf authors a CSS-parse throughput gate (a value.js concern dispatched as VJ-L3/O.W6). kf itself has no MB/s · Add a lint-clause to proof:portable-perf: scan scripts/ for any `if (hz < floor)` or `hz >= WIN_FRACTION` outside of portable-perf.mjs and fail — enforcing that

**→ Reformulation:** S3 is grounded and necessary. Augment: (1) export both `ratioGate` (same-report ratio) and `absoluteGate` (for the warmEngine-class microtask floor with mandatory `marginComment`) so the warmEngine case converts cleanly without forcing a contrived baseline. (2) Scope `jsonParseAnchor` as optional — include it only if kf has a CSS-parse throughput gate to protect; otherwise it is copy-weight with no live consumer in kf. (3) Add a scripts/ lint-clause to proof:portable-perf enforcing no re-implemented floor math outside the helper.

---

### [KEEP] S4 — wire the apparatus into CI with the correct posture

**Harden:** CURRENT STATE: `proof:hygiene` does NOT contain `proof:lint-clean` or `proof:portable-perf` (grep confirms both keys are absent from package.json). `proof:ci-coverage` currently PASSES (exit 0) with 1 · POSTURE ASSIGNMENT IS CORRECT: the lint-tier (eslint + dep-cruiser) is device-independent — a circular import is a circular import on any runner. `proof:lint-clean` is correctly HARD everywhere. The t · CI-COVERAGE SELF-ENFORCEMENT: `proof:ci-coverage` currently checks both directions — forward (every package.json `proof:*` script reachable in CI) and converse (every CI gate in `proof:all`). This mea · WORKFLOW WIRING: S4 says to update `scripts/proof-ci-coverage.mjs` so new gates are aggregator-reachable. But proof:ci-coverage is a READ gate (it reads package.json and the workflow YAMLs) — the actu · NO NEW CONTRIVANCE: S4 is pure plumbing — it is the mechanical consequence of S1/S2/S3 existing. No new posture taxonomy, no new CI system.

**Validation (RAN):** node scripts/proof-ci-coverage.mjs → PASS (exit 0): 'all 149 CI-invoked proof:* gates are reachable from proof:all'. grep package.json for proof:lint-clean → NOT FOUND. grep package.json for proof:portable-perf → NOT FOUND. These gates do not exist yet so ci-coverage cannot red on them.

**Brainstorm:** The S4 cure is mechanical (add two lines to proof:hygiene). Consider batching the gate authors as atomic commits: proof:lint-clean born-RED + eslint.config.mjs  · The proof:portable-perf self-test clause (a fixture report with candHz = 0.5 × baseHz → reds HARD locally, notes in CI) is a good idea but adds test-within-a-te · Consider adding `proof:lint-clean` to `proof:correctness` rather than `proof:hygiene` — lint failures are more analogous to build failures than to structural hy

**→ Reformulation:** S4 unchanged. The wiring is mechanical: add `npm run proof:lint-clean` and `npm run proof:portable-perf` to `proof:hygiene` in package.json. The existing `proof:ci-coverage` self-enforces the wiring. Author all new gates + wire them in ONE commit batch to avoid a transient ci-coverage red window.

---

## P.W5W6-demo1

### [KEEP] P.W5 S1 — cube: cure the ghost .rainbow-wrapper shimmer (author a real @keyframes + @property --shimmer-x), wire grab/release [data-dragging] cursor affordance + role=application/tabindex, lift matrix-label legibility (opacity-20→40 + per-axis color-mix ring)

**Harden:** Born-RED witness CONFIRMED: grep over demo/cube/CubeTarget.vue shows @keyframes idle-bob exists (line 290) but NO @keyframes for .rainbow-wrapper — the class+timings are bound (lines 46,56) with no ru · @property is ALREADY used in CubeTarget.vue and demo/@/styles/design-idioms.css — the @property --shimmer-x cure is idiomatic to this tree, not a new mechanism. @property is Baseline Widely Available  · .focus-ring is a real contract in demo/@/styles/design-idioms.css — the affordance reuses an existing idiom, no ad-hoc CSS. · NO contrivance smell: zero new rAF (pure CSS shimmer), single attribute reflect for [data-dragging] (useOrbitalPointer already owns isDragging), no new library surface, no sibling dependency. Pure-NOW · Smell-test pass: in-realm-solvable (yes), single-path (yes, no dual maintenance), smallest-change (yes — author the missing keyframe vs delete the dead class). The matrix-ring color-mix(in oklab) is B

**Validation (RAN):** Born-RED reproduced via grep: `grep -n 'rainbow-wrapper|@keyframes' demo/cube/CubeTarget.vue` → class+timings bound at :46,:56,:176 but the only @keyframes is `idle-bob` at :290 (no .rainbow-wrapper rule). The shimmer provably never plays. No browser run needed — the source-absence IS the witness; the gate's runtime `animation-name !== none` clause is the live corroborator (not testable headless in-session without serving dist).

**Brainstorm:** The grab/release a11y carry should ALSO bind aria-pressed or a live aria-label state-change on [data-dragging] so a screen-reader user hears the grab confirm, n · Prefer animating the registered @property --shimmer-x as the single interpolated channel (one custom-prop sweep) over background-position so the shimmer respect · The matrix-label opacity lift: consider raising to a token (--label-dim) in design-idioms.css rather than a literal opacity-40, so the legibility floor is one D

**→ Reformulation:** KEEP as specified — confirmed live correctness defect (ghost shimmer) + grounded affordance/legibility cures, all pure-NOW kf-side, idiomatic to the existing @property/.focus-ring idioms. Augment the grab affordance with an aria STATE reflect (not just the cursor) so the [data-dragging] confirm is non-visual-accessible too.

---

### [KEEP] P.W5 S2 — amiga: cure the engine-unaware setTimeout boing/boot race (replace raw timers with engine-driven completion + guard post-teardown mesh writes), restore the specular ball (MeshLambertMaterial→MeshPhongMaterial), build the .amiga-telemetry angular-velocity overlay + replace inline spinBloom lerp with SmoothProgress

**Harden:** Born-RED witnesses ALL CONFIRMED: AmigaScene.vue:175 `boingTimer = setTimeout(...)` + useAmigaBoot.ts:47 `bootTimer = setTimeout(...)` (the post-teardown write race); demo/amiga/utils.ts:61 `new THREE · The MeshLambertMaterial→MeshPhongMaterial swap is a one-line demo-layer Three.js edit (inv-16 holds — no kf library change). Restores the documented 1984-Boing-Ball specular design intent. Minimal ris · The telemetry reads ALREADY-tracked velX/velY/glideX/glideY (useSphereSpin.ts:61-70) — zero new physics, just renders Math.hypot. The SmoothProgress lerp-replacement is a genuine dogfood of a LIGHT ex · NO contrivance: the boing-race cure DELETES the two raw timers (no-legacy purge, single-path), the material swap is not a re-architecture, the telemetry adds no library surface. All NOW, no sibling/va · One HARDEN nuance: the spec offers two completion mechanisms (animationGroup completion hook OR a SmoothProgress-tracked arc-duration) — the impl should pick ONE (the engine completion hook is the cle

**Validation (RAN):** Born-RED reproduced via grep: `grep -n setTimeout demo/app/scenes/AmigaScene.vue demo/amiga/useAmigaBoot.ts` → AmigaScene.vue:175 + useAmigaBoot.ts:47 (both raw timers confirmed); `sed -n 58,64p demo/amiga/utils.ts` → MeshLambertMaterial at :61 confirmed; `grep -rn amiga-telemetry demo/` → zero hits (no telemetry element). The decaying-velocity telemetry clause is a live observable not testable headless in-session (needs served dist + synthetic drag).

**Brainstorm:** The angularVelocity() readout should also expose a 'settled' boolean derived from the spring/glide threshold so the 'gliding/settled' badge is engine-truth (dec · Guard the post-teardown write with a single `if (disposed) return` flag on the scene's own teardown rather than per-call `if (!sphereMesh || !scene)` scattered  · Consider rendering the telemetry in the SAME --ball-tone identity contract the spec sets (--ball-tone: var(--amiga-red)) so the readout chip visually belongs to

**→ Reformulation:** KEEP — three confirmed live defects (timer race, flat material, invisible physics) with grounded NOW cures. Pin the completion mechanism to the engine completion-hook (not a SmoothProgress-as-timer) so no timer-shaped construct survives the no-legacy purge; derive the 'settled' badge from decay()/decayRest engine-truth rather than a magnitude heuristic.

---

### [AUGMENT] P.W5 S3 — earn the two eggs: cube keyboard axis-lock-reveal (--axis-active + drop-shadow when X/Y/Z held, reads OrbitalDrag's existing pressedKeys) + amiga flick-to-boing (release angular-speed > ~8 rad/s autonomously triggers onBoing(), PRM-gated, dblclick path kept)

**Harden:** CONTRIVANCE-AUDIT confirms flick-to-boing as a grounded keeper (line 73): 'velocity already tracked (useSphereSpin.ts:61-70); the egg is a threshold check on existing data + a call to the already-exis · Both eggs REVEAL kept physics (cube: invisible axis-lock constraint; amiga: hidden dblclick trap) rather than adding decoration — satisfies the L.W11 instrument-egg law (surgical refinement of the kep · The flick threshold (~8 rad/s) is a magic number — HARDEN note: it should be derived/tunable relative to the same velocity units the telemetry surfaces (Math.hypot(velX,velY)), and validated against a · PRM-gating is correctly carried (the reduced-motion law). No new rAF in either egg region (inv-ζ). Both ride existing cadences (syncRotationToModel for the axis-reveal; endDrag for the flick). · No contrivance: zero new API, threshold + CSS state flip, reads existing refs. Pure-NOW.

**Validation (RAN):** The flick threshold magnitude is the one TESTABLE-but-not-yet-measured claim. I benched SpringProgress physics (see heatmap item) but the flick-velocity units come from useSphereSpin's NDC drag accumulation, not directly testable without the live pointer pipeline + a real flick gesture. Recommend the impl instrument the actual release velX/velY distribution over real flicks before fixing 8 rad/s — it is exactly the 'wired-but-never-fires / mis-set threshold' the S4 keystone is designed to catch.

**Brainstorm:** Make the flick threshold a named const FLICK_BOING_RAD_S derived as a fraction (e.g. 0.6×) of the max observed drag speed, so it self-calibrates to the device's · The cube axis-reveal: brighten the axis line AND show a transient axis-letter glyph (X/Y/Z) so a keyboard user sees WHICH axis locked, not just that something b · Consider a tiny haptic/visual flick-strength feedback (the brighter the boing, the harder the flick) to make the emergent physics legible — the flick magnitude 

**→ Reformulation:** KEEP both eggs (grounded, reveal-not-decorate), but AUGMENT the amiga flick: replace the hard ~8 rad/s magic number with a named, calibrated FLICK_BOING_RAD_S derived from the observed drag-speed distribution (instrument real flicks before fixing it) so the threshold neither mis-fires nor never-fires — the exact failure the S4 flick-boing keystone exists to catch. Cube egg: add the X/Y/Z axis-letter glyph so the reveal names the locked axis.

---

### [KEEP] P.W5 S4 — author scripts/proof-amiga-flick-boing.mjs as a RUNTIME interaction gate (6 clauses: shimmer-real, cube-affordance, cube-axis-lock, amiga-specular, amiga-telemetry-live, flick-boing keystone) over the BUILT dist/gh-pages, mirroring proof:design-refinement's withPage/navToScene/KF_REQUIRE_BROWSER harness

**Harden:** Gate-falsifiability is STRONG: each clause asserts a REAL live observable born-RED today (animation-name!==none, [data-dragging] cursor flip, --axis-active>0, MeshPhongMaterial type, decaying telemetr · Harness is real and established: scripts/lib/demo-driver.mjs exports withPage/navToScene/resolveChromium/REQUIRE_BROWSER (confirmed lines 24-38,369); proof:design-refinement is the exact pattern to mi · The dual born-RED structure (even a future inert stub still REDs because the value never decays / the boing never engages) is the correct defense against false-green. This is observable-truth, not sou · HARDEN nuance: clause 6 (flick-boing) depends on the S3 threshold being correctly calibrated — if 8 rad/s is mis-set, the gate's OWN synthetic flick (dx≈20px/16ms×8) must exceed it; the gate-authoring · No contrivance: it is a NOW kf-side gate with no sibling dependency, correctly hard-gating where Chromium present / tripwire where absent.

**Validation (RAN):** Harness confirmed present: `grep -n 'withPage|navToScene|REQUIRE_BROWSER' scripts/lib/demo-driver.mjs` → all exported (lines 24,30,38,369). proof:design-refinement confirmed as the mirror pattern (static-half + browser-half structure). The gate itself is authored against a served dist + Chromium — not executable headless in this session (no served dist), but its harness primitives are verified real.

**Brainstorm:** The flick-boing clause should log the synthetic flick's computed release rad/s alongside the threshold so a future threshold tweak surfaces in the gate output ( · Add a margin assertion to the synthetic flick (it must clear the threshold by ≥1.5×) so the gate fixture is robust to small threshold tweaks and doesn't become  · Consider reusing one navToScene settle for both cube clauses and one for both amiga clauses to minimize cold-boot Chromium cost (the gate-apparatus consolidatio

**→ Reformulation:** KEEP — the gate is correctly born-RED over live observables with the right anti-proxy defenses (decaying telemetry + real flick), riding the established withPage/navToScene harness. Harden the flick-boing fixture: compute the synthetic flick's resulting rad/s, assert it clears the S3 threshold by a ≥1.5× margin (robust to the slow-runner device-dependence trap), and log it for debuggability.

---

### [AUGMENT] P.W6 S1 — square: complete the invalid role=slider ARIA (add aria-valuenow/min/max), add .focus-ring, source the raw-hex keyframe literals from --rainbow-* tokens, add velocity-tilt + directional squash-stretch (--spring-tilt/--spring-squash from existing springX/Y.velocity) + grab-pulse ::before via @starting-style

**Harden:** Born-RED CONFIRMED: SquareScene.vue:37-39 → role=slider + aria-label + aria-valuetext ONLY, no aria-valuenow/min/max (WCAG 4.1.2 violation, confirmed by direct read). CONTRIVANCE-AUDIT grounded keeper · A11y HARDEN FLAG (the real wrinkle): a role=slider is a 1D control; the .demo-box is a 2D drag (two springs, x AND y). Binding aria-valuenow to the 'geometric mean of springX/springY.target' is SEMANT · velocity-tilt reads ALREADY-tracked springX/Y.velocity (public at spring.ts:250) → CSS custom properties; zero new physics, no extra rAF (CSS-custom-property seam). @starting-style is Baseline 2024, d · No contrivance: attribute/class additions + reading existing velocity + pure-CSS grab-pulse. The raw-hex sourcing reuses the existing resolvePaletteSweep token source (no new palette). Pure-NOW. · .focus-ring confirmed as a real design-idioms.css contract.

**Validation (RAN):** Born-RED reproduced: `sed -n 34,42p demo/app/scenes/SquareScene.vue` → role=slider with only aria-label + aria-valuetext, NO aria-valuenow/min/max (confirmed WCAG 4.1.2 gap). The velocity-tilt/grab-pulse live observable (box banks on diagonal drag) not testable headless in-session.

**Brainstorm:** Drop the geometric-mean aria-valuenow entirely; use the per-axis 2D-slider-group pattern (two role=slider children with aria-valuemin=-1/max=1/valuenow=live-pos · The squash-stretch should be volume-preserving (squash on one axis → stretch on the orthogonal) so the box reads as physical mass, not just a 1-axis scale — a s · Tie --spring-tilt magnitude to the velocity-to-displacement RATIO (not raw velocity) so the bank reads consistently across response settings — a stiff spring an

**→ Reformulation:** KEEP the focus-ring/keyframe-sourcing/velocity-tilt/grab-pulse as grounded, but AUGMENT the ARIA cure: replace the lossy 'geometric mean of springX/springY.target as aria-valuenow' with the per-axis 2D pattern the spec itself offers (two-axis aria-valuemin/max/valuenow, or role=application with a both-axes label) — a single scalar for a 2D drag misreads to assistive tech, which is the very WCAG concern this clause exists to fix. Make the squash volume-preserving.

---

### [KEEP] P.W6 S2 — spring: cure the SpringTrace first/last-point bug (in-place anchor-before-fill, drop the speculative springLinearStopsArray() API), exit the 60 Hz Vue-ref leak (scrubberPhase ref → non-reactive snapshot + DOM-direct painter), add Y-axis containment + focus-ring on the rail

**Harden:** BUG CONFIRMED + REPRODUCED IN A PROBE (not just asserted): I reimplemented the SpringTrace.vue linearPlot parse and ran it on a representative springLinearStops string. When the first stop is bare (pc · FIX VERIFIED IN A PROBE: moving the anchor BEFORE the fill with UNCONDITIONAL `=0`/`=100` (not `?? 0`) yields first=0, last=100, monotone pcts [0,8,30,60,80,100]. The in-place 2-line reorder is the co · The springLinearStopsArray() LIGHT API drop is CORRECT (CONTRIVANCE-AUDIT #4 / no-public-API-without-consumer): there is no second consumer, so it would be speculative public surface. The in-place fix · The 60 Hz Vue-ref leak (useSpringHotPath.ts:80 scrubberPhase ref written at :143, read in a slot render) is a confirmed reactive hot-path leak (CONTRIVANCE-AUDIT grounded keeper line 76). The painter- · No contrivance anywhere: in-place fix, dropped speculative API, painter exit of an existing proven pattern, clamp + class. Pure-NOW.

**Validation (RAN):** RAN: `node /tmp/springtrace-bug.mjs` (buggy parse) → first pt {v:0,pct:4}, last pt {v:1,pct:86.7} — WRONG (want 0/100). Then `node /tmp/springtrace-fix.mjs` (anchor-before-fill, unconditional =) → first {v:0,pct:0}, last {v:1,pct:100}, pcts [0,8,30,60,80,100] — CORRECT. The bug is real and the in-place fix is verified. The 60 Hz leak exit is not benchable headless (needs the live Vue render graph) but the pattern is the proven painter-registry one.

**Brainstorm:** Add a unit test (test/spring-trace-anchor.test.ts) over the in-place parse asserting first.pct===0 && last.pct===100 for an underdamped overshooting spring whos · While reordering: collapse the `?? 0`/`?? 100` to plain `=0`/`=100` (the `??` was the silent enabler of the bug — it only fired when the value was still null, w · For the scrubber painter: dispose the registered painter on scene unmount (register returns an unregister handle) so the 60 Hz channel can't write a torn-down S

**→ Reformulation:** KEEP exactly as specified — the SpringTrace bug is CONFIRMED AND THE FIX VERIFIED via probe (anchor-before-fill with unconditional =0/=100 → first=0/last=100); dropping springLinearStopsArray() is the right no-speculative-API call. Augment with a pure-function unit test asserting the first/last anchor invariant (cheaper + more durable than the browser clause alone), and ensure the scrubber painter unregisters on unmount (post-teardown-write discipline).

---

### [ADOPT] P.W6 S3 — the spring parameter-space heatmap (20×20 response×damping click-to-navigate, MEASURE-FIRST: build cells via the closed-form spring settle-time formula NOT ~400 live SpringProgress instances) + hold the aria-orientation suppress GATED on the glass-ui BC guard

**Harden:** MEASURE-FIRST RESOLVED BY BENCH (the cluster's headline validation): the closed-form is DECISIVELY faster AND obviates the 400-instance approach. See validation. The spec's instinct (closed-form is th · IMPORTANT HARDEN CORRECTION: the spec calls it 'the closed-form spring settle-time formula (the analytic solution for SpringProgress's physics)' as if it EXISTS in spring.ts — it does NOT. `grep settl · BETTER TINT METRIC FOUND: the analytic peak-OVERSHOOT formula exp(-ζπ/√(1-ζ²)) is EXACT (matches the live peak to <1% — the residual is just 60Hz sampling), zero approximation, and 507× faster than 40 · The aria-suppress GATED-tail is correctly held (not band-aided, not prematurely deleted) — P-inv-28 discipline with a named trigger (proof:glassui-segmentedtabs-aria-guard). The deletion is a P.W12 di · No contrivance: canvas grid + click handler + marker, no new rAF (one-time sampling at mount), no new library API, dogfoods the existing spring math. The MEASURE-FIRST mandate is satisfied.

**Validation (RAN):** RAN: `npx tsx /tmp/spring-heatmap-probe.mts` over src/animation/spring.ts. 20×20=400 cells. A) 400 live SpringProgress stepped to settle = 1.0437 ms/mount. B) closed-form analytic settle-time = 0.0038 ms/mount → 272× faster. C) analytic overshoot tint = 0.0021 ms/mount → 507× faster. Accuracy (`/tmp/spring-acc.mts`): closed-form settle-time vs live = 26.2% mean / 55% max rel error BUT 94.5% rank-agreement over 79,800 pairs (tint ordering preserved). Overshoot (`/tmp/spring-overshoot.mts`): analy

**Brainstorm:** Tint by OVERSHOOT (exact closed-form, 507× faster, zero approximation) rather than settle-time (26% error, 272× faster) — overshoot is both more faithful to the · The marker dot tracking live (response, dampingFraction) and the click-to-navigate are the navigational payoff — ensure the click maps canvas-pixel → (response, · Since the closed-form is ~0.004ms/mount, the heatmap can be RECOMPUTED live on a resolution/range change with no perf concern — consider letting the designer zo · Drop the doc phrasing 'the analytic solution for SpringProgress's physics' — there is no such closed-form settle-time IN spring.ts; the formula is a derived con

**→ Reformulation:** ADOPT the closed-form heatmap — BENCHED at 272× (settle-time) to 507× (overshoot) faster than 400 live SpringProgress instances at 0.002-0.004 ms/mount, decisively dropping the 400-instance approach with zero allocation. REFORMULATE the tint metric: tint by the EXACT analytic overshoot exp(-ζπ/√(1-ζ²)) (<1% error vs live) rather than the approximate settle-time (26% error, though 94.5% rank-faithful) — overshoot is both exact and the more designer-meaningful channel; offer settle-time as a free second channel. Correct the spec prose: there is NO closed-form settle-time helper in spring.ts; the formula is a derived control-theory expression (state it + cite the bench). Hold the aria-suppress GATED as specified.

---

### [KEEP] P.W6 S4 — author scripts/proof-spring-heatmap-navigable.mjs as a RUNTIME interaction gate (5 clauses: trace-correct, scrubber-non-reactive, heatmap-navigate keystone, square-affordance, aria-suppress-gated inverse-bite) over the BUILT dist/gh-pages, mirroring the proof:design-refinement/proof:spring-slider-continuous harness

**Harden:** Gate-falsifiability STRONG: trace-correct asserts first SVG x===0/last x===100 (the invariant my probe shows is born-RED today at x=4/x=86.7); heatmap-navigate drives a real click → spring params JUMP · The aria-suppress-gated clause is a clever INVERSE-bite: it REDs on a PREMATURE delete (while glass-ui still double-emits) and GREENs on the correctly-held workaround until P.W12 fires. This correctly · Harness confirmed real (withPage/navToScene/REQUIRE_BROWSER in demo-driver.mjs). The mirror gate proof:spring-slider-continuous is named as the pattern — consistent with the existing roster. · HARDEN nuance on heatmap-navigate: the clause should assert the spring params land WITHIN ONE CELL of the click (not exact equality), because the canvas-pixel→(response,damping) inverse mapping is dis · No contrivance: NOW kf-side gate, hard-gate-where-present/tripwire-where-absent, no sibling dependency for clauses 1-4 (clause 5 is the BC tripwire, correctly scoped).

**Validation (RAN):** Harness primitives confirmed (`grep withPage|navToScene|REQUIRE_BROWSER scripts/lib/demo-driver.mjs`). The trace-correct clause's born-RED state is PROVEN by my probe (first=4/last=86.7 today, want 0/100). The gate runs over served dist + Chromium — not executable headless in-session, but every clause's born-RED witness is independently confirmed (trace bug reproduced, no .spring-heatmap exists, square ARIA incomplete, scrubberPhase is a 60Hz ref).

**Brainstorm:** Add the pure-function trace-anchor unit test (from S2 brainstorm) as the trace-correct clause's CHEAPER companion — the unit test catches the regression without · heatmap-navigate: assert the marker dot MOVED to the click (a second observable) in addition to the params jumping — defends against a 'params updated in state  · scrubber-non-reactive: the MutationObserver should count subtree mutations over a fixed scrub window and assert ZERO (not just 'fewer'), since the painter-regis

**→ Reformulation:** KEEP — born-RED over live observables with the right anti-proxy defenses (real click-navigate, real drag-bank, MutationObserver 60Hz check, inverse-bite aria-gated). Harden two clauses: (1) heatmap-navigate should tolerate ±half-cell on the params jump (the canvas→param mapping is grid-discretized) and also assert the marker MOVED; (2) pair trace-correct with the cheaper pure-function anchor unit test as a two-tier defense against the slow-runner flakiness.

---

## P.W7W8-demo2

### [KEEP] P.W7 — easing curve-editor as the DemoControlPoint showcase (S1 critically-damp drag, S2 hero promotion + closed-form viewBox, S3 diff-ghost, S4 fine/coarse keyboard + writable numeric, S5 proof:easing-curve-editor born-RED)

**Harden:** GATE-FALSIFIABILITY: STRONG. All four S5 clauses are genuinely born-RED today and verified this session: hero is pointer-events:none (EasingHeroStage.vue:210) + aria-hidden (:54) → no draggable handle · SUBSTRATE GAP (the load-bearing finding): P.W7 sequences ATOP O.W5's DemoControlPoint, but DemoControlPoint.vue does NOT exist on the current tree (ls: No such file) and scripts/proof-demo-control-poi · CONTRIVANCE SMELL-TEST: NO speculative-dependency, NO rewrite-flavor, NO tempting-perf-without-bottleneck. S2's closed-form viewBox (Math.min(0,1,y1,y2)/max) replacing the 17-sample scan is a legitima · S1 PREMISE OVER-CAUTIOUS (a real but harmless inaccuracy): S1 frames the springOptions pass-through as a possible 'born-RED HANDOFF to O.W5 impl (a one-line drag.ts edit)'. FALSE on today's tree: Drag · COUPLING TO CONTRIVANCE-AUDIT: the audit RE-SCOPED drag2D itself ('do not export drag2D/Drag2DHandle from the barrel... no live consumer'). drag2D is STILL exported today (index.ts:88,93). DemoControl · INTERNAL COHERENCE: a minor seam-diagram error — the seam (line 52) and S1 cite 'src/animation/drag-2d.ts' as drag2D's home and 'drag.ts:234' for the emit, but drag2D LIVES in drag-2d.ts and is re-exp

**Validation (RAN):** RAN spring-overshoot probe: `npx tsx /tmp/spring-overshoot-probe.mts`. Simulates pinning the spring to drop-point 1.0 then ticking the rAF settle at 60fps, recording max overshoot past the drop point. DEFAULT (dampingFraction 0.86): maxOver=0.00500 → 0.500% of travel, 54 frames to settle. CRITICAL (dampingFraction 1.0): maxOver=0.0000 → 0.0000% overshoot, 57 frames. CONCLUSION: the S1 ring is REAL (a curve handle would overshoot the drop point by 0.5% of travel) and the critically-damped cure is

**Brainstorm:** S1 reformulation: drop the handoff branch — state plainly 'pure demo construction: drag2D({x:{springOptions:{dampingFraction:1}},y:{...}}); the springOptions pa · AUGMENT S1 with the iOS-canonical near-instant authoring feel: instead of bare dampingFraction:1 on the default response 0.5, pass {response: ~0.15, dampingFrac · Surface the drag2D-keep coupling explicitly in PROGRESS: record that O.W5's DemoControlPoint IS the live consumer resolving the contrivance-audit's drag2D RE-SC · AUGMENT the diff-ghost (S3): the modern-web corpus has carousel-snap-highlights (Container scroll-state queries) — not applicable here — but the ghost could add · S2 closed-form viewBox is sound but note the displayed path inverts y (1 - easingFn(t), line 219), so the analytic clamp must use 1-y1/1-y2 in the SVG y-domain,

**→ Reformulation:** KEEP P.W7 as the design layer over O.W5's DemoControlPoint, with three corrections: (1) S1 reformulated to 'pure demo construction' — the springOptions pass-through is verified present today (drag.ts:56→:169), DELETE the born-RED-handoff-to-O.W5 branch (dead code); the probe confirms 0.86 overshoots 0.5%/54fr and 1.0 is exactly 0; optionally tune {response:~0.15,dampingFraction:1} for a snappy ring-free follow. (2) Fix the seam line-refs: drag2D lives in drag-2d.ts (re-exported through drag.ts); the emit-off-settle is drag-2d.ts:88-89; the y-domain clamp must use 1-y (line 219 inverts). (3) Record the explicit coupling that O.W5's DemoControlPoint is the live consumer resolving the contrivance-audit drag2D RE-SCOPE (KEEP-iff-O.W5-lands). HARD precondition: P.W7 is DAG-blocked on O.W5 — DemoControlPoint.vue + proof-demo-control-point.mjs are both ABSENT today; P.W7 cannot be authorized until O.W5 implements. The born-RED gate (proof:easing-curve-editor) is well-formed, falsifiable, and a genuine non-duplicative superset of proof-easing-editor-live.mjs.

---

### [RE-SCOPE] P.W8 — N-Stage scene-switcher + mobile (NOW: S1 scroll-snap mobile carousel, S2 typed-directional VT, S3 proof:scene-switcher-mobile born-RED; GATED: S4 N-Stage 3D-ring unshelf spec on the BC cut)

**Harden:** MOBILE-UNBUILT CLAIM CONFIRMED (the D6 shelf-driver): grep across the ENTIRE scene-stage subtree on n-stage-impl returns 0 max-width breakpoints (CarouselDisk 0, SceneStage 0, StageArrows 0, ScenePrev · NOW-LAYER ANCHORS CONFIRMED: useSceneTransition.ts:32 calls startViewTransition(() => mutate(id)) with NO types → the scene-switch is directionless (S2 vt-directional RED is real). App.vue:461 carries · MODERN-WEB GROUNDING ACCURATE (not invented): the three cited guides all exist in the corpus at high similarity — directional-navigation-transitions (0.74), carousel-slide-effects (0.74), same-documen · GATED S4 IN DIRECT TENSION WITH THE CONTRIVANCE-AUDIT: the audit's own verdict on 'Tranche N Stage scene-switcher — bespoke 7-component architecture, 7 idle-preview units' is SIMPLIFY/RE-SCOPE: 'when  · S4 MEASURE-FIRST IS THE RIGHT INSTINCT BUT UNDERCUTS THE UNSHELF: S4 step 2 mandates the STAGE-SPEC S0 perf trace ('the SLOW the owner named, never quantified') as MEASURE-FIRST. But MEASURE-FIRST on  · GATE COMPLETENESS NOW-LAYER: proof:scene-switcher-mobile two clauses are well-formed and born-RED. mobile-layout (390px viewport, scroll-snap-type resolved, cards within [0,390]±bleed, no chrome overl · NOW/GATED SPLIT IS CLEAN AND CORRECT: S1+S2+S3 ride zero sibling dependency (today's tree: live scene-subject VT, glass-ui's published view-transition.css, the 390px harness). S4 is correctly SPECCED-

**Validation (RAN):** RAN grep on n-stage-impl scene-stage subtree: `for f in CarouselDisk SceneStage StageArrows ScenePreviewHost; do git show n-stage-impl:.../$f.vue | grep -ci max-width; done` → ALL 0; only @media (prefers-reduced-motion) rules (SceneStage.vue:439,462). Mobile-unbuilt CONFIRMED. NOW-anchors: grep useSceneTransition.ts:32 → startViewTransition(()=>mutate(id)) no types CONFIRMED; App.vue:461 single scene-subject CONFIRMED. Branch position: n-stage-impl exists, 3,500-LOC switcher present (scene-stage

**Brainstorm:** SWITCHER UX (the explicit ask): the contrivance-audit's dock-Select-enhancement is the conservative winner over the 3D-ring revival — enhance the existing dock  · VIEW-TRANSITIONS for the mobile switcher: pair S1's scroll-snap with same-document VT on tap-to-commit — when a snapped card is tapped, runSceneSwitch(id) under · AUGMENT S1 with carousel-snap-highlights (a NEW corpus guide the spec didn't cite, 0.67 similarity, Container scroll-state queries): the snapped phone card can  · RE-SCOPE S4 to a go/no-go: make the STAGE-SPEC S0 perf trace a GATE-ZERO that decides unshelf-3D-ring vs adopt-dock-Select-enhancement, rather than step 2 of an · S2 typed-directional VT is the unambiguous KEEP — it is free (rides the existing scene-subject name, no second VT name, no sibling publish), high-leverage (dock

**→ Reformulation:** SPLIT the verdict by phase. NOW-layer (S1 scroll-snap mobile + S2 typed-directional VT + S3 proof:scene-switcher-mobile): KEEP as-is — the mobile-unbuilt D6 blocker is confirmed (0 max-width across the scene-stage subtree, PRM-only @media), the NOW anchors are verified (useSceneTransition.ts:32 no-types, App.vue:461 single scene-subject), the modern-web grounding is accurate (all 3 cited guides exist; Baseline-newly under the correct @supports floor), and both gate clauses bite real rendered-geometry observables that are born-RED today. Augment S1 with carousel-snap-highlights (container scroll-state) for the snapped-card highlight and reuse S2's directional VT on the phone tap-commit. GATED-layer S4 (the 3,500-LOC 3D-ring unshelf): RE-SCOPE — reconcile against the contrivance-audit, which RE-SCOPES this exact feature to 'enhance the existing dock Select with a SpringProgress transition + a view-transition morph' rather than reviving the bespoke 7-component carousel. Make the STAGE-SPEC S0 perf trace a GATE-ZERO go/no-go (3D-ring unshelf vs dock-Select enhancement), not step 2 of an assumed unshelf — MEASURE-FIRST should DECIDE the architecture, not merely document an unshelf already committed. The dock-Select+stage-portal-VT path delivers blocker 3's signature 'grows from the trigger' morph for a fraction of the LOC and structurally dissolves blockers 1/2/4 (no ring to shrink, no on-ring ink, no 7-preview perf risk, no top-layer-toolbar conflict).

---

## P.W9-correctness

### [KEEP] S1 — parse() throws NAMED_SELECTOR_NO_TIMELINE instead of producing NaN frames (Path A, the DM-22 cure)

**Harden:** Gate-falsifiability: STRONG. The throw-or-finite clause asserts a REAL runtime observable, born-RED today. Not a source-shape grep — it constructs the animation and inspects frame.time finiteness. · Completeness: the spec correctly identifies the full defect chain (frame-compiler.ts:449 sort -> utils.ts:398 calcFrameTime -> binarySearch always-active). All 8 'today' facts in the defect table veri · Contrivance smell-test: CLEAN. Zero sibling dependency (value.js 1.0.2 already publishes ValueUnit.superType). It is a genuine self-solvable kf-internal bug — not a speculative dependency, not a rewri · Internal coherence: STRONG. Path B (timeline phase-mapping) explicitly excluded; the throw is the same AnimationOptionError the engine raises for all malformed-present input. The seam S1 creates is th · Refinement: the throw should scan templateFrames BEFORE the sort at frame-compiler.ts:449 (the spec says this); post-sort works too since the sort does not mutate start.value, but pre-sort is cleaner.

**Validation (RAN):** RAN `npx tsx` probe over src/animation/engine.ts. RESULT: `new CSSKeyframesAnimation().fromString('@keyframes x { entry {opacity:0} exit {opacity:1} }').parse([])` does NOT throw; frames[0].time.start === NaN AND time.stop === NaN (typeof 'number', Number.isNaN === true, Number.isFinite === false). binarySearchRange over the NaN range returns idx 0 for EVERY tested t (-100, 0, 0.5, 500, 1e9) -> always-active; interpFrames(t) applies {opacity:[null]} at t in {0,0.25,0.5,0.75,1}. Control `@keyfram

**Brainstorm:** Throw message: reword to drop the literal 'calling parse()' phrasing (parse() is reached via fromString().parse()); say '...attach a ScrollTimeline/ManualTimeli · Carry the full offending-selector set (not just the first) into the error value — cheap, since the pre-sort scan already iterates all template frames. · Add named-selector.css to test/fixtures/keyframes/ with a manifest.json entry flagged expectThrow: NAMED_SELECTOR_NO_TIMELINE so S4/replay-equality skip it by-c

**→ Reformulation:** KEEP S1 as specced (Path A throw, pre-sort superType scan). Bug reproduced live; cure is the smallest grounded fix. Refinements: reword the throw message to drop 'calling parse()', and carry the full offending-selector set into the error value. Gate-first then engine cure, per the dev->impl section.

---

### [KEEP] S2 — proof:named-selector-nan-frame born-RED gate + re-target the proof:replay-equality S4 source-shape regex to the behavioral throw-or-finite anchor

**Harden:** Gate-falsifiability: the keystone throw-or-finite clause is born-RED today (verified) and bites the GENUINE defect. The two corroborator clauses (dead-write-becomes-live, no-throw-typed-becomes-thrown · The re-target is justified by a verified blind spot: proof-replay-equality.mjs:176 IS the regex /entry\s*\|\s*exit\s*\|\s*cover\s*\|\s*contain/ over frame-compiler.ts — GREEN today (matches SELECTOR_N · Completeness: the non-named-unaffected regression guard prevents an over-throw stub. The four clauses form a proper born-RED witness + anti-stub fence. · Smell-test: CLEAN. No new API surface; reuses AnimationOptionError. Wires into proof:hygiene alongside proof:engine-correctness (the established home for unit-level correctness gates). · Coherence: the spec REPLACES (not keeps) the old regex — correct, a kept regex stays green-but-blind. The replacement must still import engine + format (the no-source-edit clause at proof-replay-equal

**Validation (RAN):** RAN grep over scripts/proof-replay-equality.mjs: confirmed the named-selector clause is requireAll('named-selector', FRAME_COMPILER, [{ re: /entry\s*\|\s*exit\s*\|\s*cover\s*\|\s*contain/ }]) at lines 173-178 — a pure source-shape regex over frame-compiler.ts, exactly as the spec describes. Confirmed scripts/proof-named-selector-nan-frame.mjs ABSENT. The born-RED keystone is validated by the S1 probe: parse([]) yields NaN frames with no throw, so Number.isFinite(start)===false fires today.

**Brainstorm:** Import AnimationOptionError from internal/errors.ts (the same path the engine throws from) so the `e instanceof` check cannot drift if a barrel re-export change · Scope the dead-write-becomes-live grep to the parse() body span specifically, not file-wide — otherwise the pre-existing addFrame write + comment refs already s · Add a fourth runtime sub-assertion: with a ScrollTimeline/ManualTimeline ATTACHED the same named-selector animation must NOT throw — proves the throw is scoped 

**→ Reformulation:** KEEP S2 and the replay-equality re-target. Harden two things: (1) scope the dead-write-becomes-live grep to the parse() body span so it does not false-pass on the pre-existing addFrame write/comments; (2) import AnimationOptionError from internal/errors.ts in the gate to keep the instanceof identity stable. Replace (do not keep) the replay-equality regex, preserving its engine+format import check.

---

### [RE-SCOPE] S3 — proof:grammar-fuzz harness with fast-check colorArb / mathArb / keyframeStopArb model arbitraries, asserting parse->serialize->reparse structural round-trip equality

**Harden:** FALSIFIED PREMISE (the central one). The spec asserts repeatedly (lines 238, 252-255, 361, 364) that the colorArb + keyframeStopArb arms run GREEN on value.js 1.0.2 because none->NaN and color() wrapp · Broader-than-specced round() breakage: round(3.7px, 1px) WITH a strategy AND round(3.7px) without one BOTH throw `TypeError: t is not iterable` in value.js 1.0.2. The spec only SKIPs the bare-strategy · SELF-CONTRADICTION in the spec's own framing: its anti-pattern rule (lines 235-241, 349-351) says 'a harness permanently RED until a sibling ships is a blocked harness.' But its own colorArb none-chan · Smell-test on the FRAMEWORK: CLEAN — a property-based round-trip oracle over the real parse pipeline is grounded for a grammar library; fast-check is a single well-scoped devDep. The IDEA is sound; th · Coherence tension: the spec wants both a useful-today GREEN gate AND a regression-catching oracle, but on 1.0.2 the most valuable arms (none-channel, wrapper-loss, round) are exactly the RED ones. The

**Validation (RAN):** RAN value.js@1.0.2 direct probe (parseCSSValue from @mkbabb/value.js, in-repo): oklch(0.6 none 200) -> 'oklch(0.6 NaN 200)' (none->NaN LIVE, NOT fixed); color(display-p3 1 0 0) -> 'display-p3(1 0 0)' (wrapper-loss LIVE, NOT fixed); round(3.7px, 1px) -> THREW TypeError: t is not iterable; round(3.7px) -> THREW same; oklch(0.6 0.2 200) -> 'oklch(0.6 0.2 200)' (clean control). ALSO RAN the full kf round-trip (CSSKeyframesToString async): oklch none-channel serializes 'NaN' INTO output and re-parse 

**Brainstorm:** RE-SCOPE the harness to the arms GENUINELY green on 1.0.2 (probe-proven): rgb(), color-mix(in oklch,...), clamp(), calc(), plain oklch(L C H), keyframeStopArb o · Better: convert two SKIPs into fast-check expected-FAILURE tripwires — assert oklch(L none H) + color()-wrapper round-trips are KNOWN-BROKEN today and will FLIP · Pin fc.seed(42), cap CI numCases at ~50 (the full parse pipeline x 200 is fine under 30s but a lower floor keeps slack); keep proof:grammar-fuzz-random for pre- · Feed the three confirmed-broken forms + EXACT probe outputs into KF-TO-VALUEJS-P.md as a gold regression corpus for value.js to fix against.

**→ Reformulation:** RE-SCOPE S3. Correct the false premise: the none->NaN and color() wrapper-loss breaches are LIVE in value.js 1.0.2 (probe-confirmed), and ALL round() forms throw (not just strategy-omit). Author the harness GREEN-today by SKIPPING THREE arms with value.js-P-dispatch notes: (1) oklch(L none H) none-channel, (2) color(<space> ...) wrapper form, (3) round() entirely. Ship the genuinely-green arms now: rgb(), color-mix, calc/clamp, plain oklch(L C H), keyframeStopArb over those. Strongly prefer converting the none-channel + wrapper SKIPs into fast-check expected-failure tripwires that auto-flip when value.js P lands. Dispatch the three exact probe outputs to KF-TO-VALUEJS-P.md as a regression corpus. The grammar-fuzz IDEA and the fast-check devDep are KEPT; only the green-today scoping is corrected.

---

### [KEEP] S4 — proof:kf-differential oracle: kf.at(0.5) vs WAAPI getKeyframes() midpoint over the 4 WAAPI-eligible corpus fixtures, observe-only on Linux

**Harden:** Gate-falsifiability: genuine. It closes a real blind spot — proof:roundtrip-fidelity + proof:replay-equality are purely-JS oracles that CANNOT detect browser divergence. A kf-vs-browser differential i · Infrastructure existence VERIFIED: toWAAPIKeyframes (waapi.ts:260), toWAAPIOptions (waapi.ts:356), CSSKeyframesAnimation.at(progress) (engine.ts:608) all exist — the S4 step-3 path is real. The 4 elig · Smell-test: BORDERLINE-CLEAN. Uses the established Playwright harness (already a devDep, demo-driver.mjs withPage) — no new browser dependency. Scoped to a 4-fixture subset; explicitly defers the full · Real risk the spec under-weights: WAAPI getKeyframes() returns the browser's NORMALIZED stop list, not a t=0.5 interpolated sample. 'Sample the midpoint frame from getKeyframes()' is well-defined only · Observe-only on Linux is correct (browser render/timing varies on the slow runner). proof:correctness (runtime tier) not proof:hygiene (headless unit) is the right wiring.

**Validation (RAN):** PARTIAL — API existence RAN/confirmed via grep (toWAAPIKeyframes waapi.ts:260; toWAAPIOptions waapi.ts:356; at() engine.ts:608) and the 4 eligible fixture files read (all WAAPI-eligible by content). The differential COMPARISON itself is NOT testable in-session: it requires a real Chromium driven via Playwright to call Element.animate(...).effect.getKeyframes(), which this subagent cannot launch and drive. The kf-side (kf.at(0.5)) is runnable but meaningless without the browser side. So infra CON

**Brainstorm:** Resolve the 2-stop midpoint ambiguity: compute the WAAPI side as a manual lerp of the two getKeyframes() stops under the SAME easing kf used (linear here), NOT  · Drive the >=4-eligible minimum-coverage assertion from waapi.ts's own usesDefaultRenderer/eligibility check rather than a hard-coded fixture allowlist, so the g · Co-locate per-fixture tolerances (0.01 unitless, 0.5px length, 2deg hue) in the fixture manifest.json rather than the gate script (DRY with the existing manifes · Fold the gate into the existing demo-smoke Playwright job's page rather than spinning a fresh withPage — amortizes the browser launch.

**→ Reformulation:** KEEP S4 (WAAPI-eligible 4-fixture subset, observe-only Linux, proof:correctness wiring). Infra confirmed present. Tighten one underspecified point before impl: the WAAPI 'midpoint' comparison must manually lerp the two getKeyframes() stops under kf's own easing — only opacity-3stop has an explicit 50% stop; do NOT assume getKeyframes() yields a t=0.5 sample. Drive eligibility from waapi.ts's own check rather than a hard-coded allowlist; co-locate per-fixture tolerances in manifest.json. Defer the full-corpus CDP getComputedStyle path to P.WZ+ pending O.W2, as the spec already states.

---

## P.W10-nolegacy

### [RE-SCOPE] S3/S4 — THE TRAP: importing @mkbabb/value.js/math from a LIGHT source module reds proof:boundary; the fix is bundle-externalization (Arm A) OR documented-keep (Arm B), decided by the W97 graph oracle + H4 external smoke-test.

**Harden:** TRAP CONFIRMED empirically. Replicated the gate's holdsValueJsSpecifier regex (proof-boundary.mjs:96 `@mkbabb\/value\.js(?:\/[^"']*)?`) against the O.W9 §S3 cure line `export { clamp, scale, lerp, ler · W97 GRAPH GREEN-WORTHY: bundled @mkbabb/value.js/math as its own rolldown entry under the gate's config (value.js NON-external) → exactly 2 modules (probe entry + the single hashed math-UeasWV-i.js, 5 · H4 SMOKE-TEST CONFIRMED across three external configs: (a) the CURRENT bare `"@mkbabb/value.js"` string ALONE does NOT cover the subpath — rolldown INLINES the math source (entry 533 bytes, bare runti · COMPLETENESS GAP (the load-bearing finding the spec MISSES): Arm A would also RED ASSERTION 1 (per-entry static graph), not only assertion 4. The gate's isValueJs filter (proof-boundary.mjs:73-75) mat · BLAST RADIUS: leaves.ts is pulled by ~11 light entries (numeric, smooth, playback, drag, stagger, sequence, timeline, motion-path, waapi, …). A re-export from the subpath propagates the assertion-1 RE · Arm B parity-test FEASIBLE: confirmed value.js src/math.ts clamp/scale/lerp/lerpArray are byte-equivalent to the kf leaves (read both). A runtime equivalence test (leaves.ts fn output === value.js/mat · NOT a contrivance: the leaves duplication is STRUCTURALLY FORCED by the boundary gate (the gate bans even the parse-that-free subpath), not legacy. The W97 oracle (bundle-the-graph, never grep-the-nam

**Validation (RAN):** RAN 4 probes inside the repo (rolldown resolves from kf node_modules). (1) regex-replica probe: O.W9 §S3 cure line → RED, bare → RED, /color → RED, import-type-subpath → GREEN(erased). (2) `node .pw10_w97_graph.mjs` (W97 graph, gate config): math subpath entry = 2 modules, 0 parse-that/grammar/engine, 533 bytes. (3) `node .pw10_h4_external.mjs` (H4, 3 externals): bare string → inlined/edge=false/533B; subpath string → externalized/edge=true/113B; predicate → externalized/edge=true/113B. (4) `nod

**Brainstorm:** RE-SCOPE the W97 allow-list to thread BOTH assertions, not just the source-grep: (a) assertion 4 — add @mkbabb/value.js/math to holdsValueJsSpecifier's allow-li · Given the assertion-1 finding + the ~11-entry blast radius + the DRY win being only ~30 LOC of trivial pure math, the cost/benefit tilts toward Arm B (documente · If Arm A is chosen, prefer the PREDICATE `/^@mkbabb\/value\.js(\/|$)/` over the explicit subpath string in vite.config.ts:306 — it future-proofs every value.js  · Arm B's corrected comment should cite the EMPIRICAL reason: `the boundary gate bans even the parse-that-free ./math subpath specifier from light source (proof-b

**→ Reformulation:** KEEP S3/S4 as the gate-VERIFIED transposition (the precept correction is correct and grounded — the TRAP, the W97 clean graph, and the H4 external behavior are all empirically confirmed), but RE-SCOPE the W97 clause to thread its allow-list through BOTH proof-boundary.mjs assertion 1 (the isValueJs module-set filter — externalize the verified-clean subpath in the gate's own bundleEntry external) AND assertion 4 (the source-grep), because the math chunk lives under node_modules/@mkbabb/value.js/ and reds assertion 1 even when its graph is grammar-clean (the spec only named assertion 4). Given the assertion-1 cost + the ~11-light-entry blast radius vs. the ~30-LOC DRY win, name Arm B (documented-keep + corrected comment + a value.js-side byte-equivalence parity test) as the EXPECTED outcome with Arm A as the contingency — W97 remains the measured oracle, asserting neither. The stale leaves.ts:56-61 comment ('no ./math subpath') is itself legacy now (the subpath ships) and must be corrected to the true reason regardless of arm.

---

### [KEEP] S2 — author the proof:boundary W96 parse-that source-scan: scan src/animation/** for `from "@mkbabb/parse-that"`, assert ZERO; born-RED today via utils.ts:1.

**Harden:** BORN-RED CONFIRMED: `grep -rn 'from "@mkbabb/parse-that"' src/animation/` → exactly one hit, utils.ts:1 `import { any as parseAny } from "@mkbabb/parse-that"`. package.json:215 carries the `"@mkbabb/p · SEQUENCE-COUPLED but AUTHORING is NOW-able: the spec correctly separates authoring (P.W10, born-RED witness present today) from the GREEN flip (O.W16 VJ-L3 delete). The structural guard bites whether  · NOT a contrivance: this is the gate-first discipline (author the RED before the cure) over a REAL observable (a static parse-that import in animation source). The X4/L.W9 record shows it was 'named si · Idiom precedent exists: proof-no-deprecated-guard.mjs is the comment-aware grep-over-surface gate the W96 clause mirrors; holdsValueJsSpecifier (the same file) is the multi-shape specifier matcher to  · MINOR: the spec says the scan matches 'every specifier shape (from, require, dynamic)'. The current source has only the `from` shape; the require/dynamic arms are defensive future-proofing, not load-b

**Validation (RAN):** RAN `grep -rn 'from "@mkbabb/parse-that"' src/animation/` → utils.ts:1 (sole hit). `grep -n parse-that package.json` → :215 production dep. Real `node scripts/proof-boundary.mjs` baseline GREEN (the W96 clause is not yet added). Witness confirmed singular and live.

**Brainstorm:** Implement W96 by reusing holdsValueJsSpecifier's exact multi-shape regex shape (from/bare-import/require) parameterized on the package name, so the parse-that s · Scope the scan to src/animation/** EXCLUDING node_modules/dist (the spec says this) AND consider including the test/ tree's animation imports? NO — keep it src- · Add a one-line assertion message naming the O.W16 delete as the GREEN path, so when the gate reds in CI before O.W16 lands, the message self-documents the seque

**→ Reformulation:** Author proof:boundary's W96 clause as specced: a multi-shape specifier scan of src/animation/** (excluding node_modules/dist) for `@mkbabb/parse-that`, asserting ZERO, reusing holdsValueJsSpecifier's matcher generalized to the package name (DRY). Born-RED via utils.ts:1 today; GREEN after O.W16 deletes it and stays GREEN forever as the S9 anti-recurrence structural belt. Wire into proof:hygiene/proof:boundary. Grounded as-is — gate-first over a real, singular, live witness.

---

### [KEEP] S1 — author proof:no-cross-realm-cast: ban un-annotated `as any`/`as unknown as` over a value.js/parse-that import; the utils.ts:229,236 casts gain a `// @cross-realm:` annotation as declared exceptions; born-RED today.

**Harden:** BORN-RED CONFIRMED: `grep -n 'as any' src/animation/utils.ts` → :229 `(CSSFunction.FunctionArgs as any).map(...)` and :236 `(parseAny as any)(fnArgs, CSSValues.Value)`. Read the context (utils.ts:225- · The seam is GENUINELY irreducible: the comment (utils.ts:225-228) records that value.js and kf each bundle their own parse-that copy under different node_modules realms, so Parser<T> classes are nomin · ANTI-GAMING bite is sound: the gate must match BOTH `as any` AND `as unknown as` (the laundered double-cast) — the observable is 'an un-reasoned cross-realm cast,' not the literal text. The spec state · HARDEN: the gate must resolve WHICH identifiers are 'imported from @mkbabb/*' to know which casts to police. A naive `grep 'as any'` over-fires on local casts (e.g. a DOM `as any`). The gate needs to  · O.W16 dissolves these casts entirely (they vanish with the parse-that import when VJ-L3 moves the parse into value.js), so the annotations are transient — the gate persists as the forward belt against · NOT a contrivance: this is the single most insidious workaround class (invisible to tsc), it has a live born-RED witness, and the escape hatch is honest (declared+reasoned, not a blanket allow-file). 

**Validation (RAN):** RAN `grep -n 'as any' src/animation/utils.ts` → :229, :236 (the only two, both over @mkbabb/* imports). Read utils.ts:225-240 confirming the cross-realm rationale and the member-access shape of :229. Confirmed neither proof:no-cross-realm-cast nor proof:no-legacy-surface exists yet (`grep -c` in package.json → 0). Real boundary gate GREEN baseline (this gate is separate, not yet authored).

**Brainstorm:** Implement the import-binding resolution honestly: parse `import { X, Y as Z } from "@mkbabb/value.js"|"@mkbabb/parse-that"`, collect {X, Z}, then flag any `(IDE · Keep the escape as a SINGLE `// @cross-realm: <reason>` line/trailing annotation (the spec's KISS choice) rather than a per-cast allow-file — the reason travels · Consider scoping the gate to src/animation/** only (mirroring W96) — the demo's cross-realm casts (if any) are out of the library surface and not the precept ta · Author this gate FIRST (S1, the keystone) born-RED, BEFORE any S4/S5 cut, per the spec's gate-first ordering — so the annotations on utils.ts:229,236 are in pla

**→ Reformulation:** Author proof:no-cross-realm-cast (S1, the keystone) as specced, with one IMPL precision the spec under-specifies: resolve the module's @mkbabb/value.js + @mkbabb/parse-that IMPORT BINDINGS, then flag `as any` / `as unknown as` whose cast-subject ROOT identifier is one of those bindings (the utils.ts:229 member-access case `CSSFunction.FunctionArgs as any` must trace to the `CSSFunction` import, not the property) — un-annotated → RED; a `// @cross-realm: <reason>` annotation → allow-listed. Scope src/animation/**. Match both `as any` and `as unknown as` (anti-laundering). The utils.ts:229,236 casts get the annotation as declared exceptions (the genuinely-irreducible dual-realm Parser<T> seam, confirmed by the existing prose comment); they dissolve at O.W16. Wire into proof:hygiene. Grounded — keystone structural gate over a live, honest witness.

---

### [KEEP] S5 — drop the three @deprecated published-surface aliases (engine.ts:1205 Animation, timeline.ts:171 ScrollTimelineOptions, timeline.ts:209-218 ScrollTimeline), migrate the 22 demo `Animation` imports to KeyframesAnimation, author proof:no-legacy-surface asserting ZERO @deprecated on the built dist/keyframes.d.ts.

**Harden:** WITNESSES CONFIRMED LIVE: engine.ts:1205 `export { KeyframesAnimation as Animation }` with the @deprecated JSDoc at :1192; timeline.ts:171 `export { type KeyframesScrollTimelineOptions as ScrollTimeli · CONSUMER COUNT VERIFIED: `grep -rln 'import type { Animation }'` in demo/ → 21 files; the broader `import type {...Animation...}` form → 22 files. The spec's '22 demo files' is accurate. The migration · proof:no-legacy-surface is NEW (confirmed `grep -c` in package.json → 0). The OBSERVABLE design is correct: grep the BUILT dist/keyframes.d.ts (the API-Extractor roll-up the npm consumer's IDE resolve · HARDEN: ScrollTimeline (the value class alias, timeline.ts:218) is a BREAKING drop — `new ScrollTimeline()` / `instanceof ScrollTimeline` consumers break, not just type imports. The spec correctly rou · SEQUENCING with O.W7: the spec correctly sequences S5 (drop the engine.ts:1205 alias LINE) BEFORE O.W7 (which moves the class body) so O.W7 reads a class with no trailing deprecated alias. Clean compo · NOT a contrivance: pure no-legacy surface cuts over live witnesses with a real consumer observable (the built d.ts). The breaking-ness is honestly routed to the version cut (P.WZ). Grounded.

**Validation (RAN):** RAN `grep -n '@deprecated' src/animation/engine.ts` → :1192 (the Animation alias); `grep -n '@deprecated|ScrollTimeline' src/animation/timeline.ts` → :163/:171 (Options) + :209/:218 (ScrollTimeline class). `grep -rln 'import type { Animation }' demo/` → 21; broader form → 22. `grep -c no-legacy-surface package.json` → 0 (NEW confirmed). proof:no-deprecated-guard.mjs exists as the comment-aware-grep precedent.

**Brainstorm:** proof:no-legacy-surface should run `npm run build` then grep the rolled-up dist/keyframes.d.ts for `@deprecated` — but to make it FAST in the LINT tier, conside · IMPL must migrate BOTH the 22 `Animation` TYPE imports AND any `ScrollTimeline` VALUE-class usages (new/instanceof) in demo + README + examples — grep `\bScroll · Author proof:no-legacy-surface born-RED (the three aliases live on today's built d.ts) BEFORE dropping them — gate-first, mirroring S1/S2. · Confirm the migration doesn't break proof:pkg3-clean (the timeline.ts comment at :169 references it) — the KeyframesScrollTimeline* canonical names must stay th

**→ Reformulation:** Drop all three @deprecated aliases (engine.ts:1205, timeline.ts:171, timeline.ts:218) and author proof:no-legacy-surface asserting ZERO @deprecated on the BUILT dist/keyframes.d.ts (the API-Extractor roll-up — the real npm-consumer observable, born-RED today). Migrate the 22 demo `Animation` type-imports to KeyframesAnimation AND grep+migrate any `ScrollTimeline` VALUE-class usages (new/instanceof) in demo/README/examples — the spec under-names the breaking value-class consumers vs the type-only Animation ones. Sequence the engine.ts:1205 alias-line drop BEFORE O.W7's class-body move (disjoint, clean). Route the breaking version-policy decision to P.WZ. Grounded — pure no-legacy surface cuts over live witnesses with the correct built-d.ts observable.

---

### [KEEP] S6 — record the TRAP-resolution verdict (Arm A externalized OR Arm B documented-keep) in scripts/leaves-externalization-decision.json, beside spring-vector-decision.json; the P-inv-28/DP-6 terminal home.

**Harden:** The decision-JSON precedent EXISTS: scripts/spring-vector-decision.json is on disk (461 bytes, currently staged with a 4-line edit). The S6 target scripts/leaves-externalization-decision.json does NOT · CORRECT discipline: S6 makes the leaves.ts TRAP (DP-6) a gate-VERIFIED terminal decision, not a perpetual deferral — and explicitly states a KEEP (Arm B) is not a punt but a recorded verdict with the  · My S4 findings SHARPEN the recorded verdict: the JSON should capture the assertion-1 finding (not just assertion 4) + the H4 external behavior (bare string inlines / predicate externalizes) + the ~11- · NOT a contrivance: a single small JSON recording a measured verdict is the lightest possible terminal home; it composes with the W97 oracle that produces the verdict. Grounded — no API surface, no dua

**Validation (RAN):** RAN `ls scripts/spring-vector-decision.json scripts/leaves-externalization-decision.json` → precedent exists (461B), target absent. Confirmed via git status the spring-vector JSON is the pre-existing staged edit (not mine). n/a for a runtime claim — this is a documentation-terminal item; the testable substance lives in S3/S4 (validated above).

**Brainstorm:** Populate the JSON with the EXACT probe numbers from this session as the decision evidence: W97 graph (2 modules / 533B / 0 grammar), H4 external (bare→inlined / · If Arm B is chosen (my recommendation given assertion-1 + blast-radius vs ~30-LOC win), the JSON records: verdict=ARM-B, reason='gate bans the parse-that-free s · Mirror spring-vector-decision.json's schema (verdict field + evidence + the corrected-comment flag) for cross-doc consistency.

**→ Reformulation:** Author scripts/leaves-externalization-decision.json on IMPL recording the W97-measured TRAP verdict (Arm A or Arm B), mirroring spring-vector-decision.json's verdict+evidence schema, and INCLUDE this session's exact probe numbers as the audit trail: W97 graph (2 mods/533B/0 grammar), H4 external (bare→inlined / subpath-string+predicate→externalized/113B), the assertion-1 finding (isValueJs flags the math chunk → RED unless the gate ALSO externalizes the verified-clean subpath), and the ~11-light-entry blast radius. Given those, Arm B (documented-keep, comment-corrected) is the evidence-favored terminal. Grounded — the lightest terminal home for DP-6, no API surface, composes with W97.

---

## P.W11-weakmap

### [ADOPT] P.W11 — VJ-L1 WeakMap early-cure for S8: replace the FN_NAME Symbol sidechannel (stamped onto foreign value.js ValueUnit) with a kf-internal WeakMap<ValueUnit,string> populated at flatten time; unblocks O.W7 engine-seam split NOW without value.js P.

**Harden:** GATE-FALSIFIABLE: born-RED witness is LIVE. `grep -n 'Symbol("kf\.|FN_NAME' src/animation/utils.ts` returns hits at :45 (`const FN_NAME = Symbol("kf.fnName")`), :47 (`type NamedValueUnit = ValueUnit & · COMPLETENESS: utils.ts is the SOLE violator. `grep -rn 'Symbol(' src/animation/ | grep -v utils.ts` => ZERO other Symbol-onto-value.js-type stamps. The cure is complete in one file; the gate scope is  · INTERNAL COHERENCE — the honest-inferiority accounting is CORRECT and corroborated. Live probe: `flatLeaf in vjs === false`, `'fnName' in new ValueUnit(0,'px') === false` on value.js@1.0.2 (VJ-L1 unsh · NO CONTRIVANCE SMELL. NOT a speculative-dependency: zero sibling API needed (WeakMap is idiomatic TS; ValueUnit imported as a TYPE already on the barrel). NOT rewrite-flavored: the swap is a body-only · O.W7 NOT-VJ-L1-GATED is corroborated by the O.W7 spec ITSELF (docs/tranches/O/waves/O.W7.md:67-74): 'the TERMINAL cure is P.W11's in-realm WeakMap<ValueUnit,string> — realm-clean, no cross-realm owner · engine.ts is at 1397L (verified `wc -l`); proof-decomposition.mjs carries a `cap: 1400` override (line 132) masking the god-object — the O.W7-unblocked acceptance clause (remove override → reds at 139

**Validation (RAN):** RAN three in-realm probes (npx tsx, kf source imports, from project root so node_modules resolves). PROBE 1 (real utils.ts pipeline, Symbol baseline): parseAndFlattenObject({transform:'scale(2) translateX(10px)'}) → 4 leaves; createInterpVarValue padded the opposing 1-leaf frame up to 4 via the stamped name → 4 well-formed interp vars, no throw (2/2 PASS — the provenance pipeline works TODAY). PROBE 2 (realm-cleanliness): WeakMap stamp leaves the ValueUnit with ZERO kf own-symbols (`Object.getOw

**Brainstorm:** S1 gate hardening (already in spec, endorse): the no-foreign-symbol-stamp gate must bite the SHAPE (any kf-owned symbol/property written as a computed-key onto  · Tiny KISS elegance available at swap-time: the `fnName !== undefined` guard in stampFnName is preserved verbatim from the Symbol body (no behavior change), but  · Doc-comment refresh: the utils.ts:42-43 JSDoc ('value.js's ValueUnit.clone() does NOT preserve this field, so the cache restamps it') stays TRUE under the WeakM · Consider asserting in the S1 gate (or a sibling assertion) that NO module under src/animation/** stamps onto an imported value.js type — the grep already shows 

**→ Reformulation:** ADOPT as specified — the WeakMap early-cure is correctness-neutral (byte-equivalent over 20 arity-pad interp vars, validated in-realm), realm-clean (the probe confirms ZERO kf own-symbols on the ValueUnit post-swap vs the Symbol's own-property breach), and unblocks O.W7 WITHOUT any sibling gate (corroborated by the O.W7 spec itself naming P.W11 the terminal cure and declaring the split NOT VJ-L1-gated). No augmentation needed beyond the spec's own gate-first/honest-inferiority discipline. Concretely: (1) author scripts/proof-no-foreign-symbol-stamp.mjs born-RED (utils.ts:45 live witness) scoped to the SHAPE not the string, asserting ZERO kf-owned property/symbol stamped onto any imported value.js type tree-wide; (2) swap `const FN_NAME = Symbol("kf.fnName")` → `const FN_NAME_MAP = new WeakMap<ValueUnit, string>()`, DELETE `type NamedValueUnit` (:47), re-body fnNameOf→`FN_NAME_MAP.get(u)` and stampFnName→`if (name !== undefined) FN_NAME_MAP.set(u, name)` (signatures + all call-sites UNCHANGED), KEEP the re-stamp ceremony at :64/:289-294 (the documented honest cost — the gate fixture proof:interpolate-anything bites a lost-provenance clone); (3) record the P-inv-28 belt-exit disposition: S8 O.W7-BLOCKING arm EXITED (breach dissolved, split landable this tranche), S8 TRIPWIRE arm STAYS PENDING on VJ-L1 (O.W16, strictly preferred, still dispatched). Honest inferiority (WeakMap doesn't survive clone → ceremony stays → tripwire not retired) is accurate and must not be hidden. DOCS+gate-spec only this session (inv-16); the value.js VJ-L1 flatLeaf canonical cure remains a DISPATCH finding for O.W16, not authored here.

---

## P.W12-consume

### [KEEP] P.W12 — Validate S1 false-RED (missing apiPresent on S1 arm)

**Harden:** CONFIRMED LIVE BUG: `node scripts/proof-workaround-deletion.mjs` exits 1 with S1=RED S2=RED S7=GREEN S8=PENDING S9=PENDING. S1 arm has NO `apiPresent` field — the DO-2 content-aware probe was wired on · CONTENT CHECK CONFIRMS FALSE-RED: glass-ui 4.1.0 tabs.js (both installed 4.0.1 and extracted from the 4.1.0 tarball) emits `aria-orientation` unconditionally at role assignment — `role: I.value ? 'tab · S2 is GENUINELY RED (not false-RED): `useDockClickIntegrity` confirmed at 1 hit in installed 4.0.1 dock.js and 1 hit in 4.1.0 dock.js (verified from tarball). The dock cure IS in the dist. 9 workaroun · The fix for S1 is precise: add `apiPresent: (() => { try { const t = readFileSync(join(root, 'node_modules/@mkbabb/glass-ui/dist/tabs.js'), 'utf8'); return /aria-orientation[^]{0,80}(isUnderline|tabli · For S2, a parallel grepDist `apiPresent` checking `useDockClickIntegrity` in dock.js is correctness hygiene (mirrors the S7/S8/S9 pattern) but does NOT change the current outcome — S2 would be RED eit

**Validation (RAN):** RUN: `node scripts/proof-workaround-deletion.mjs` from /Users/mkbabb/Programming/keyframes.js. OUTPUT: S1=RED S2=RED S7=GREEN S8=PENDING S9=PENDING, exit 1. CONFIRMED: S1 fires RED on version-only (no apiPresent); tabs.js 4.1.0 has no isUnderline/tablist conditional guard (regex match: 0 hits in both installed 4.0.1 and tarball-extracted 4.1.0). S2 fires RED correctly: useDockClickIntegrity at 1 hit in dock.js (4.0.1 installed + 4.1.0 tarball), 9 hits in TransportDock.vue.

**Brainstorm:** Compute the glass-ui dist probes alongside vjsCaps at the top of the script — a `glassCaps` block that reads the installed tabs.js and dock.js once and extracts · Name the S1 sibling more precisely: `'BC aria-orientation role=group conditional guard (content-present)'` rather than `'BB SegmentedTabs aria fix'` to make cle · Add a one-line comment to the S2 arm explaining that its grepDist IS the proof that the cure is real (not just a version number) — mirrors the S7/S8/S9 commenta · The version floor in sibling.version for S1 (4.1.0) should stay as the install-floor sentinel so that the grepDist is only run against a dist that is at least a · Consider extracting the dist-grep into a small `grepsInstalledDist(distRelPath, pattern)` helper so S1 and S2 apiPresent share the same boilerplate as vjsCaps.

**→ Reformulation:** P.W12 S1 gate-hygiene item is CONFIRMED-LIVE as specified. The exact false-RED: S1 arm at scripts/proof-workaround-deletion.mjs lines 203-216 has NO `apiPresent` field; with glass-ui 4.1.0 published, the arm fires RED incorrectly. Fix: compute `glassCaps.tabsAriaGuard` by grepping the installed `node_modules/@mkbabb/glass-ui/dist/tabs.js` for the pattern `/aria-orientation[^]{0,80}(isUnderline|tablist)/` (false today → S1 stays PENDING; true when BC SFC-guard wave ships → S1 becomes actionable). Also compute `glassCaps.dockClickIntegrity` by grepping `node_modules/@mkbabb/glass-ui/dist/dock.js` for `/useDockClickIntegrity/` and assign to S2 `apiPresent` for content-aware hygiene (does not change S2's RED state since the cure IS in the dist). The version floor sentinel in `sibling.version` stays for both arms.

---

### [KEEP] P.W12 — S2 delete NOW: re-pin glass-ui to ~4.1.0 + delete pointerHandled/onPlayPointerDown

**Harden:** GATE STATE CONFIRMED: S2 is genuinely RED. `useDockClickIntegrity` confirmed at 1 hit in installed 4.0.1 dock.js and in 4.1.0 tarball. 9 workaround hits at TransportDock.vue lines 15, 151, 196, 342, 3 · DM-1 chronicity is 6 at P (I,J,K,L,M,O→P). P-inv-28 CRITICAL belt: no 7th carry under any scenario. The S2 delete is not just overdue — it is a mandatory closure event at P per the no-workaround prece · The split is correct: S2 (dock click-strand interim) is independent of S1 (aria suppress). S2 delete does NOT depend on the BC SFC aria-guard shipping. S2 can and must proceed as an atomic commit: `~4 · The crossfade-strand verification constraint (AUDIT-DIGEST A3) is a VERIFY step at impl time (proof:live-session S5), NOT a CI gate. If S5 fails (crossfade-strand persists after deletion), the conting · After the gate-hygiene fix (S1 apiPresent wired), S2 remains RED via its own content-present logic. The S1 hygiene fix must precede the S2 delete commit to avoid a false-RED on S1 confusing the transi

**Validation (RAN):** RUN: `node scripts/proof-workaround-deletion.mjs` → S2=RED: 9 hits at TransportDock.vue:15,151,196,342,348,358,361,366,373; @mkbabb/glass-ui@4.1.0 PUBLISHED. `npm show @mkbabb/glass-ui@4.1.0 version` → `4.1.0`. `grep -c useDockClickIntegrity node_modules/@mkbabb/glass-ui/dist/dock.js` → 1. `cat package.json | python3 -c 'import sys,json; d=json.load(sys.stdin); print(d["optionalDependencies"]["@mkbabb/glass-ui"])'` → `~4.0.0` (stale). All pre-conditions for S2 delete are met today.

**Brainstorm:** The atomic commit order: first wire the S1 apiPresent (gate hygiene), then in the same or immediately following commit: bump ~4.1.0, npm install, delete 9 Trans · After deletion, the play-button binding reverts to plain `@click='onPlayClick'` emitting `emit('togglePlay')` — the simplest possible shape. Run proof:all to co · Run `proof:live-session` S5 (motion-path PLAY through dock collapse/expand on 4.1.0 demo) before authorizing the commit — this is the crossfade-strand verificat · Once DM-1 closes at chronicity 6, update the deferred-ledger to CLOSED and update PROGRESS.md DM-1 row to reflect the closure.

**→ Reformulation:** P.W12 S2 is CONFIRMED-LIVE born-RED and the delete pre-condition is met today. The atomic gate event: bump `optionalDependencies['@mkbabb/glass-ui']` from `~4.0.0` to `~4.1.0`, re-install (lockfile resolves 4.1.0), delete all 9 `pointerHandled`/`onPlayPointerDown` sites in TransportDock.vue plus the K.W1 RE-OBSERVED comment block. Gate order: (1) wire S1 apiPresent grepDist (gate hygiene fix), (2) run proof:live-session S5 (crossfade-strand verify — impl-time ONLY, not CI), (3) atomic commit the re-pin + deletion. `proof:workaround-deletion` S2 arm → GREEN. DM-1 closes at chronicity 6.

---

### [KEEP] P.W12 — S1 HOLD: keep the two :aria-orientation=undefined suppress lines GATED (not deleted in P.W12)

**Harden:** HOLD CONFIRMED CORRECT: glass-ui 4.1.0 tabs.js at line 306 emits `'aria-orientation': L.value ? 'vertical' : 'horizontal'` unconditionally regardless of the `role: I.value ? 'tablist' : 'group'` condi · The two suppress sites — `demo/spring/SpringSidebar.vue:43` and `demo/@/components/custom/animation-controls/controls/AnimationControls.vue:72` — are still semantically correct. Deleting them on the c · DM-5 S1 chronicity is 4 at P (K,L,M,O→P). P-inv-28 belt fires at chronicity 4. Named terminal: BC authoring the role=group conditional SFC-guard wave (dispatched O.W11, COMMUNICATED). No 5th carry: if · The spec's proposed S1 `apiPresent` pattern `/aria-orientation[^]{0,80}(isUnderline|tablist)/` is validated: it returns false on 4.0.1 and false on 4.1.0 tarball. It would return true only when the BC · No contrivance: the S1 hold is NOT a speculative dependency. It is a factual content gap in the published glass-ui dist that is actively causing ARIA spec violations if the suppress is removed. The O.

**Validation (RAN):** RUN: `python3 -c "import re; content=open('/tmp/package/dist/tabs.js').read(); m=re.search(r'aria-orientation.{0,80}(isUnderline|tablist)', content, re.DOTALL); print('Conditional guard found:', m is not None)"` → `Conditional guard found: False`. Same result on installed 4.0.1 dist. The hold is correct — the fix is not in 4.1.0.

**Brainstorm:** The P.WZ contingency path for S1 if BC does not ship before tranche close: author a scoped CSS approach or a v-if wrapper that avoids emitting aria-orientation  · Consider whether to also track the DM-5 S1 contingency deadline explicitly in PROGRESS.md as a P.WZ hard gate (not just a soft 'if BC does not ship'). · The apiPresent probe pattern should be made robust to minification — the proposed regex `[^]{0,80}` covers the compiled Vue output shape adequately as confirmed

**→ Reformulation:** P.W12 S1 hold is CONFIRMED CORRECT. The aria-orientation conditional guard is absent from glass-ui 4.1.0 tabs.js (zero matches for `/aria-orientation[^]{0,80}(isUnderline|tablist)/` on both installed 4.0.1 and extracted 4.1.0 dist). The two suppress lines at SpringSidebar.vue:43 and AnimationControls.vue:72 must NOT be deleted until the BC SFC-guard wave ships and the grepDist apiPresent probe fires true. Gate-hygiene action: wire the S1 `apiPresent` boolean (computed by grepping installed tabs.js) so the proof gate accurately reports PENDING rather than false-RED. S1 transitions to actionable only when a future BC release includes the role=group conditional guard. DM-5 S1 chronicity 4 — contingency path (kf-internal no-workaround replacement) activates at P.WZ if BC does not ship.

---

## P.W13-emerging

### [RE-SCOPE] P.W13 overall — the resolve-values.ts seam (resolveKeyframes -> FrameCompiler.parse -> parseAndFlattenObject) as the ONE compile-time CSS lowering pass

**Harden:** SEAM PARTIALLY CORRECT, with a real lifecycle gap. resolveKeyframes(input: string | Stylesheet) (adapter.ts:175) takes ONLY a string/Stylesheet — it has NO element/target context (confirmed: grep for  · The 'returns the SAME node types so interpolation is unchanged' claim is the elegant core and is SOUND: parseAndFlattenObject (utils.ts:267) already recurses FunctionValue.values and stamps FN_NAME —  · ONE-pass-not-four-hacks gestalt is genuinely better than the EMERGING-CSS-RESEARCH.md's earlier per-feature if-resolve.ts module (that older doc proposes a separate if-resolve.ts; P.W13 supersedes it  · NOT a contrivance: CONTRIVANCE-AUDIT.md:11-17 already RE-INSTATED these as grounded forward-looking grammar+engine support and narrowed the speculative-dependency clause; the springLinearStops precede

**Validation (RAN):** RAN: probe via `node bench/_probe-emerging.mjs` (kf node_modules @mkbabb/value.js@1.0.2). resolve-values.ts / if-resolve.ts / proof-emerging-css-resolve.mjs all ABSENT (ls confirmed) -> born-RED structurally real. parseAndFlattenObject seam confirmed at utils.ts:253-301 (FunctionValue arm line 267). adapter.ts confirmed element-context-free.

**Brainstorm:** SPLIT the resolution by lifecycle, not by feature: resolve-values.ts runs the ELEMENT-INDEPENDENT arm (if(supports/media), spring()) at the existing string-reso · Thread ctx.env injection exactly like ScrollTimeline injects getScrollY/getViewportHeight (timeline.ts) — supports:(q)=>CSS.supports, matchMedia:(q)=>matchMedia · Default ctx.env to SSR-safe no-ops (supports->false, matchMedia->{matches:false}) so a server compile picks the else-branch deterministically.

**→ Reformulation:** Keep the unified resolve-values.ts rewriter, but SCOPE it to TWO call sites by element-dependence, not one. Phase 1 (NOW, in-realm): the element-INDEPENDENT arm at the resolveKeyframes->flatten seam — if(supports(...))/if(media(...)) and spring(). Phase 2 (element-aware, post-setTargets): if(style(--p)), sibling-index(), sibling-count() resolved against the target. The spec's single-seam framing must be corrected: resolveKeyframes has no element at flatten time.

---

### [KEEP] if() arm — resolve if(supports/media/style: a; else: b) to the concrete branch value at compile

**Harden:** value.js PARSES if() — CONFIRMED: parseCSSValue('if(supports(color: lch(0 0 0)): red; else: blue)') -> FunctionValue('if', [VU('supports(...)':string), VU('red':string), VU('blue':string)]). 2-branch  · MULTI-BRANCH IS LOSSY — CONFIRMED by probe: parseCSSValue('if(media(width>800px): a; supports(display:grid): b; else: c)') returns argc=3 with args ['media(width>800px)' | 'a' | 'c'] — the MIDDLE clau · The condition strings arrive as OPAQUE VERBATIM text ('supports(color: lch(0 0 0))') — kf must itself parse the condition kind (supports(...)/media(...)/style(...)) out of the string and evaluate it.  · GUARANTEED-INVALID drop semantics (no-match-no-else -> drop the declaration, never emit empty-string) is correctly called out as the sharp edge; it is gate-worthy and must be explicit.

**Validation (RAN):** RAN `node bench/_probe-emerging.mjs` + `_probe-emerging2.mjs`: if() parse confirmed; multi-branch loss confirmed (3-clause -> first-cond + else only, middle dropped).

**Brainstorm:** kf should re-parse each opaque condition string with value.js's own parseCSSValue (supports(...)/media(...) both round-trip) rather than hand-rolling a cond min · For if(supports(...)): use CSS.supports (sync, SSR-safe) per spec — good. For if(media(...)): matchMedia(...).matches, and register the matchMedia('change') re- · Add an explicit gate row: if() with NO matching branch and NO else DROPS the declaration (assert the frame omits the property, not that it interpolates '').

**→ Reformulation:** Ship 2-branch if(supports()/media()) NOW at the Phase-1 (element-independent) seam, re-using value.js parseCSSValue to evaluate the opaque condition string (no hand-rolled cond parser). Gate the guaranteed-invalid DROP explicitly. if(style(--p)) defers to the Phase-2 element-aware pass. Multi-branch (>2) stays a value.js follow-up (confirmed lossy at the parser today).

---

### [ADOPT] spring() arm — resolve spring(mass stiffness damping velocity) to the kf SpringProgress / springTimingFunction curve

**Harden:** value.js PARSES spring() via the GENERIC function parser, NOT the dedicated parseSpring — CRITICAL correction to the spec. Probe: parseCSSValue('spring(1 100 10 0)') -> FunctionValue('spring',[VU(1),V · The conversion math is SOUND: response=2pi*sqrt(m/k), dampingFraction=c/(2*sqrt(km)). Probe spring(1 100 10 0) -> response=0.62832, dampingFraction=0.50000 — feeds springTimingFunction({response,dampi · NATIVE-PARITY IS UNATTAINABLE AS A GATE ASSERTION — measured finding. CSS spring() carries NO duration; the curve SHAPE over normalized t in [0,1] depends ENTIRELY on the settle-horizon choice. Probe: · duration:auto is the one genuinely-new wiring (derive animation-duration from the solver settle-time) and is correctly flagged LOW/SMALL.

**Validation (RAN):** RAN `node bench/_probe-emerging2.mjs` (parseSpring throws on space-sep, generic parses unvalidated) + `npx tsx bench/_probe-spring-conv.mts` (kf vs vjs-linear maxDiff=0.47 due to settle-horizon mismatch) + `_probe-spring-conv2.mts` (curve shape varies [0.48..1.14] vs [1.02..0.99] vs [1.07] across maxDuration response*2/4/8). All numbers reported above.

**Brainstorm:** kf's resolve-values.ts owns the spring() validation+defaults (mass>0, stiffness>0, damping>=0, default 1/100/10/0) since the generic value.js path provides none · For a spring() in TIMING-FUNCTION position (born-RED in getTimingFunction — it falls through to the registry and silently defaults to easeInOutCubic, confirmed) · Pick maxDuration=response*4 (the existing kf default) as THE settle horizon and document it as the deliberate, self-consistent choice — do NOT chase WebKit pari

**→ Reformulation:** ADOPT spring() resolution as the FIRST/LOWEST-effort arm. kf OWNS validation + defaults-fill (the value.js generic parser provides neither; parseSpring is comma-only/brittle and unusable for space-sep CSS). Two homes: (a) timing-function position -> a new SPRING_PAREN branch in getTimingFunction reusing the existing linear() consume; (b) keyframe-value position -> resolve-values.ts. Settle horizon fixed at response*4 (the kf default), self-consistent — the gate asserts the KF curve, NOT native WebKit parity (measured unattainable). The HW-accel/CE-1.0 path is INHERITED via the linear() twin, no new wiring.

---

### [RE-SCOPE] @function arm — resolve --double(2) call sites against an extractFunctions() descriptor registry

**Harden:** BIGGER value.js GAP THAN SPECCED — CRITICAL. The spec frames @function as 'extractFunctions() ~10 lines, value.js parses VERBATIM'. But probe: parseCSSValue('--double(2)') returns ValueUnit('--double' · TYPED-ARG COERCION (validate each bound arg against the param's registered syntax, fall back to default on mismatch — e.g. 50% where <number> expected) is correctly flagged as the genuinely-hard part  · This arm is correctly value.js-P-GATED and inv-16-compliant (kf dispatches; does not author value.js). But the dispatch (KF-TO-VALUEJS-P) must be CORRECTED to ask for the call-site parse arm, not just

**Validation (RAN):** RAN `node bench/_probe-emerging.mjs` + `_probe-emerging2.mjs`: --double(2)/--double( 2 )/--foo(1,2)/--gradient-text(red,blue) ALL -> bare ValueUnit, args dropped, name=undefined. extractFunctions: typeof undefined (absent in value.js@1.0.2, confirmed; extractProperties: function present).

**Brainstorm:** Correct the value.js-P dispatch: the keystone is the dashed-function CALL parse arm (--ident(args) -> FunctionValue('--ident', [args])), THEN extractFunctions(a · This arm is the WEAKEST-grounded of the four (largest sibling gap, MEDIUM coercion, zero browsers outside Chrome 139). Sequence it LAST, behind spring() and if( · Until value.js-P lands the call parse, kf CANNOT prototype this arm at all (nothing parses) — so it is genuinely MEASURE-FIRST-blocked on the sibling publish, n

**→ Reformulation:** RE-SCOPE @function: it is value.js-P-gated on TWO items (the dashed-function-call parse arm AND extractFunctions), not the spec's single ~10-line extractFunctions — confirmed the call site does not parse today. Correct the KF-TO-VALUEJS-P dispatch accordingly. Sequence this arm LAST (largest sibling gap, MEDIUM typed-arg coercion, Chrome-139-only). kf authors nothing here until value.js-P publishes the call parse (inv-16).

---

### [KEEP] sibling-index() / sibling-count() arm — read the element's 1-based index / sibling total at compile -> integer ValueUnit

**Harden:** PARSE IS ALREADY DONE (not value.js-P-gated for parsing) — correction. Probe: parseCSSValue('sibling-index()') and ('sibling-count()') BOTH -> FunctionValue('sibling-index'/'sibling-count', []) via th · ELEMENT-DEPENDENT — these REQUIRE the target element (DOM sibling position), which does not exist at the resolveKeyframes flatten seam (adapter.ts has no element context). So this arm belongs to the P · NATURAL FIT WITH stagger is real and elegant: sibling-index() resolved to an integer feeds the existing stagger(count, opts) delay distribution — a genuine grounded betterment, not a contrivance. · LOW effort and grounded; the only correction is lifecycle placement + the parse is already free via generic.

**Validation (RAN):** RAN `node bench/_probe-emerging.mjs`: sibling-index()->FunctionValue('sibling-index',[]), sibling-count()->FunctionValue('sibling-count',[]). Both parse via generic NOW; no value.js gap for parsing.

**Brainstorm:** Drop the value.js-P parse-arm ask for sibling-* (the generic parser already yields the FunctionValue); the only value.js nicety would be arg-count validation, n · Resolve in the Phase-2 element-aware pass: ctx.env.siblingIndex = el => [...el.parentElement.children].indexOf(el)+1; siblingCount = el => el.parentElement.chil · Wire the resolved integer into stagger as the documented consumer (the spec's instinct is right).

**→ Reformulation:** KEEP sibling-index()/sibling-count() as a LOW-effort Phase-2 (element-aware) arm. Correct two spec points: (1) PARSE is already free via value.js's generic FunctionValue producer (confirmed) — drop the value.js-P parse-arm ask, keep only optional arg-count validation; (2) these are element-dependent and resolve post-setTargets, not at the string seam. Feed the resolved integer into stagger (the natural consumer).

---

### [SIMPLIFY] Born-RED gate proof:emerging-css-resolve — 4 assertions (if->concrete color, spring->SpringProgress curve, --double->doubled value, cycle-guard drops)

**Harden:** BORN-RED IS REAL — confirmed: resolve-values.ts, if-resolve.ts, proof-emerging-css-resolve.mjs all ABSENT (ls). Today an if()/spring() FunctionValue reaching parseAndFlattenObject (utils.ts:267) flatt · GATE ITEM 2 (spring) IS OVER-SPECIFIED — it asserts 'sample-equal to springTimingFunction({response,dampingFraction})' AND 'HW-accel held on WebKit'. The sample-equal-to-kf-curve half is the RIGHT, se · GATE ITEM 3 (--double) CANNOT BE BORN-RED-TO-GREEN IN-REALM — the call site does not parse (probe). It is blocked on value.js-P. Including it in the NOW gate makes the gate un-greenable from kf alone. · GATE ITEM 4 (cycle-guard) is sound in PRINCIPLE but UNTESTABLE UNTIL @function call+inlining exists — a self-referential @function needs the call to parse first. So item 4 is also value.js-P-gated, sa · CYCLE-GUARD soundness (seen Set + depth): sound design (mirrors the standard rewriter guard); but it only fires for @function recursion, which is blocked. For if()/spring() there is no recursion risk 

**Validation (RAN):** RAN: absence of resolve-values.ts/proof-emerging-css-resolve.mjs (ls) -> structurally born-RED. utils.ts:267 flatten-to-nonsense path confirmed. Probe confirmed --double call un-parseable (item 3 un-greenable in-realm) and spring curve settle-horizon-dependent (item 2's native-parity unattainable).

**Brainstorm:** SPLIT the gate into proof:emerging-css-resolve-now (items 1 if-color in jsdom, 2a spring->kf-curve sample-equality in jsdom — NO HW-accel sub-claim) and proof:e · Item 1 assertion: compile a 2-frame keyframe with if(supports(color:lch(0 0 0)):red;else:blue), inject ctx.env.supports=()=>true then ()=>false, assert the comp · Item 2 assertion: resolve spring(1 100 10 0) in timing-function position, assert getTimingFunction returns a fn sample-equal (eps<1e-6) to springTimingFunction(

**→ Reformulation:** SPLIT the gate: proof:emerging-css-resolve-NOW gates items 1 (if->resolved color) + 2a (spring->kf-curve sample-equality) — both jsdom-clean, no browser, DROP the WebKit HW-accel sub-assertion (it double-gates CE-1.0/proof:browser and asserts unattainable native parity). A SEPARATE born-RED row proof:emerging-css-resolve-vjs-p holds items 3 (@function inline) + 4 (cycle-guard), PENDING the value.js-P call-parse publish (confirmed un-greenable in-realm today). This keeps the NOW landing greenable from kf alone while preserving the born-RED truth of the gated arms.

---

## P.WZ-dispatches

### [AUGMENT] P.WZ — close criteria completeness (every band terminal?)

**Harden:** P.WZ Sequence line (line 6) reads 'P.W1…P.W12 all at terminal disposition' — P.W13 (emerging-CSS resolution pass, `resolve-values.ts`) is completely absent from this gate, from the Band B dependency t · P.W13 was authored AFTER P.WZ and its wave file exists at docs/tranches/P/waves/P.W13.md, is referenced in P.md Band B table and in KF-TO-VALUEJS-P.md, but P.WZ's gatekeeping machinery has NOT been up · P.W13's born-RED gate is `proof:emerging-css-resolve` (a new gate). Without a P.WZ dependency entry for P.W13/Band B, the close can technically declare GREEN while P.W13 is unimplemented — a silent om · The Band B dependency table entry (line 384) says 'P.W2/W3/W4 — SoA compositor + computed-unit cache + Typed-OM write path GREEN; codegen-consume ADOPT or KILL'. This is wrong on two counts: P.W4 is R · The S1 perf-delta table at line 42-46 includes a 'Codegen-consume (P.W4, GATED)' row — this wave is RETIRED by owner directive 2026-06-22 (the tombstone). That row should be REPLACED with the `_styleO · The Typed-OM perf-delta row uses 'Expected ratio ≤0.85× (≥15% faster)' — the notation is confusing (ratio < 1.0 means slower, the intent is throughput ratio ≥1.15). The `typed-om-decision.json` alread · The S5 VERIFY-ONLY re-verify roster mentions 'proof:perf-frame-budget (DM-12 — re-verify with the SoA compositor in place)' but makes no mention of re-verifying against P.W13's new compile-time loweri · P.WZ context section (lines 23-25) says 'SoA-compositor + Typed-OM write path (Bands B)' as if Typed-OM is a Band B deliverable — it is KILL (measured 0.69×, typed-om-decision.json). The correct claim · The close criteria binding paragraph omits P.W13: it says 'all NOW gates GREEN (Bands A+B+C+D+E)' but Band B in the close criteria table cites W2/W3/W4 — P.W13 (the emerging-CSS wave, also Band B) is 

**Validation (RAN):** Structural: grep -n 'P.W13\|W13' /Users/mkbabb/Programming/keyframes.js/docs/tranches/P/waves/P.WZ.md → 0 results (confirmed absent). grep -n 'Band B' P.WZ.md → line 384 cites 'P.W2/W3/W4' not 'P.W2/W3/W13'. P.W13.md EXISTS at docs/tranches/P/waves/P.W13.md with `proof:emerging-css-resolve` born-RED gate. P.md line 169 shows P.W13 in Band B row. scripts/typed-om-decision.json exists and records verdict KILL 0.69×. scripts/group-soa-decision.json records ADOPT 3.7×. The codegen RETIRED tombstone:

**Brainstorm:** Add P.W13 to P.WZ Sequence line ('P.W1…P.W12…P.W13 all at terminal disposition') and the Band B dependency table ('P.W2/W3/W13 — SoA compositor + _styleOut allo · Replace the perf-delta S1 table row for codegen-consume (P.W4, RETIRED) with a `_styleOut` alloc-cure row: 'P.W3._styleOut | proof:zero-alloc heap-delta on tran · The close criteria binding paragraph should add '+ Band B P.W13 emerging-CSS resolve GREEN or GATED-arm noted' to the closing condition list. · The S1 perf-delta JSON schema example should replace the P.W4.codegen-consume key with P.W3._styleOut and show P.W3.typed-om as state: KILL with the observed ra

**→ Reformulation:** P.WZ's close criteria, Sequence header, Band B dependency table, and S1 perf-delta table must be updated to: (1) add P.W13 (emerging-CSS, `proof:emerging-css-resolve`) as a Band B close dependency; (2) retire the P.W4/codegen-consume row (RETIRED tombstone — replace with `_styleOut` alloc-cure row + Typed-OM KILL recorded row citing typed-om-decision.json 0.69×); (3) correct the context section to say 'SoA-compositor (ADOPT 3.7×) + _styleOut alloc-cure (Bands B)' not 'Typed-OM write path'. The close-mechanics and gate oracles (S2/S3/S5/S6/S7) are correct; only the Band B catalog and perf-delta table contents are stale.

---

### [AUGMENT] P.WZ — version cut 5.1.x still correct given P.W2 SoA ADOPT is non-breaking?

**Harden:** P.W2 SoA ADOPT (3.7× on add/weighted arms) is an internal strategy change on AnimationGroup's non-default blend paths — zero API delta. The default `replace` arm is untouched. This confirms non-breaki · P.W3's grounded deliverable is the `_styleOut` out-buffer alloc-cure (internal — no API delta). The Typed-OM write path is KILL (no dual-path shipped). · P.W6 adds `springLinearStopsArray()` to the LIGHT surface — this IS an additive API export. Additive-only is MINOR (not PATCH) per semver. · P.W13 adds `resolve-values.ts` (HEAVY surface, behind `loadAnimationEngine()`). Adding HEAVY exports is additive — no breaking change, no new LIGHT static export. · O.WZ cuts 5.0.0 (MAJOR — four breaking renames). P.WZ cuts 5.1.x (MINOR over that). The semver is 5.1.0 assuming O fires first. If O.WZ somehow slips and P lands first, the version would be 5.0.1 (pat · The P.md §6 version rationale confirms: 'The P perf+design work is additive by construction... no API delta'. The version cut 5.1.x is semver-correct: there is one additive export (springLinearStopsAr · The `codegen-consume-decision.json` is referenced in P.WZ as a close artifact (line 371) but P.W4 is RETIRED — that file should NOT be authored; referencing it as a close artifact is a stale obligatio

**Validation (RAN):** Structural: grep 'springLinearStopsArray' /Users/mkbabb/Programming/keyframes.js/docs/tranches/P/waves/P.W6.md → confirms additive LIGHT export. scripts/group-soa-decision.json → ADOPT 3.7× confirmed internal-only. scripts/typed-om-decision.json → KILL 0.69× confirmed no dual-path. P.md §6 confirms 5.1.x is non-breaking MINOR. The codegen-consume-decision.json reference: grep 'codegen-consume-decision' P.WZ.md → lines 246, 287, 319, 371 — 4 stale references to a file that must NOT be authored be

**Brainstorm:** The 5.1.x rationale is sound. The only fix needed is removing the four stale `codegen-consume-decision.json` references (the file must NOT be authored since P.W · Add an explicit note: 'P.W13 (emerging-CSS) adds zero LIGHT exports; the resolve-values module is HEAVY (gated by loadAnimationEngine()). The 5.1.x MINOR is sti · The S4 version rationale should add: `springLinearStopsArray()` is the one additive LIGHT surface item, confirming MINOR (not PATCH) is the right semver.

**→ Reformulation:** 5.1.x is semver-correct (the SoA ADOPT is internal; the one additive export is springLinearStopsArray → MINOR). The four stale `codegen-consume-decision.json` references in P.WZ (lines 46, 153, 246/287, 319/371) must be replaced: P.W4 is RETIRED — there is no adopt/kill verdict to record; the Green condition (line 371) should remove 'codegen-consume-decision.json ADOPT or KILL' and the S5 S7 entries should similarly omit it.

---

### [AUGMENT] KF-TO-VALUEJS-P — dispatch asks coherent + scoped to genuine gaps

**Harden:** VJ-P.W0 (commit O docs + reconcile stale PROGRESS): LIVE-CONFIRMED born-RED — `git -C /Users/mkbabb/Programming/value.js status --short` shows `?? docs/tranches/O/` (untracked) and O/PROGRESS.md remai · VJ-L1 (flatLeaf/fnName): correctly DEMOTED-TO-SPIKE. No verification needed: P.W11 WeakMap is the terminal S8 cure. The dispatch prose is accurate. One stale pin table reference: line 477 says 'the VJ · VJ-L3 (parseCSSSubValue): LIVE-CONFIRMED born-RED — `grep -rn 'parseCSSSubValue' /Users/mkbabb/Programming/value.js/src/` → zero results. The direct parse-that import is LIVE at `src/animation/utils.t · VJ-P1 (color2Into): LIVE-CONFIRMED grounded — `color/dispatch.ts:245` comment 'eliminating it requires a color2Into out-param (deferred, O.W5 scope)' confirmed present. The `color2Into` function is ab · VJ-P3 (any→string seam): LIVE-CONFIRMED — `units/index.ts:57` `setSubProperty(subProperty: any)` and `:61` `setProperty(property: any)` confirmed. All four container mirrors (`:174,178,274,278`) confi · VJ-P2 (DROPPED): correctly handled. No issue. · VJ-CSS section (CSS1/CSS2/CSS3/CSS4): the section exists and references P.W13, but VJ-CSS items have NO 'ver' column value in the ask roster table (all four rows have no version in the rightmost colum · The KF-TO-VALUEJS-P version split table is missing VJ-CSS rows, and the Net Actions section omits the VJ-CSS ask actions. A dispatch that omits action items from Net Actions is incomplete — value.js's

**Validation (RAN):** Probes run in-session: (1) git -C /Users/mkbabb/Programming/value.js status shows `?? docs/tranches/O/` → VJ-P.W0 born-RED confirmed. (2) index.ts:169-171 O(N²) confirmed: `this.setSubProperty(name)` in forEach → N×N calls verified by node simulation (12 calls for N=3 vs 3 correct). (3) grep parseCSSSubValue /Users/mkbabb/Programming/value.js/src/ → 0 results → VJ-L3 absent confirmed. (4) setSubProperty/setProperty typed `any` at index.ts:57,61,174,178,274,278 confirmed. (5) color2Into absent, d

**Brainstorm:** Fix the pin table at line 477: change 'the VJ-L1/VJ-L3 publish' to 'the VJ-L3 publish' — VJ-L1 is demoted-to-spike, it should not appear in the re-pin critical  · Add VJ-CSS rows to the version split table with explicit version assignments: VJ-CSS3 (contrast-color, Baseline April 2026) → 1.1.0 (highest-value, independent  · Add VJ-CSS to Net Actions explicitly: '4. VJ-CSS (alongside 1.1.0 or 1.2.0): VJ-CSS3 contrast-color() eager-evaluate (Baseline); VJ-CSS1 extractFunctions(); VJ- · The O(N²) setSubProperty one-liner correction (mentioned as 'noted for completeness') deserves a clear action item since it is a correctness fix, not just house

**→ Reformulation:** KF-TO-VALUEJS-P is coherent and grounded except three gaps: (1) pin table line 477 — remove stale 'VJ-L1' from re-pin text (VJ-L1 is demoted-to-spike; the re-pin is on VJ-L3 only); (2) version split table — add VJ-CSS rows: VJ-CSS3 (contrast-color) → 1.1.0, VJ-CSS1/CSS2 → 1.2.0, VJ-CSS4 → follow-up; (3) Net Actions section — add an explicit VJ-CSS action item so value.js's session reading only Net Actions does not miss the emerging-CSS grammar gaps that gate P.W13's @function/sibling-index arms.

---

### [AUGMENT] KF-TO-PARSETHAT-B — dispatch asks coherent + scoped to genuine gaps

**Harden:** PT-B1 (packrat cross-input pollution FIX): LIVE-CONFIRMED born-RED AND both defects verified at runtime. Probe run: `p.parse('hello')` then `p.parse('world')` WITHOUT resetPackrat() returns 'hello' fo · PT-B2 (Span-combinator DEDUP): span.ts is 902 lines, the SpanParser tier starts at approximately line 540 (callSpan/SpanParserKind hits at lines 840-893), confirming the runtime Span builders (16-360) · PT-B3 (perf frontier + bench/proof:perf): LIVE-CONFIRMED the bench substrate exists (test/benchmarks/: json-comprehensive.bench.ts, wasm-json.bench.ts, wasm-format.bench.ts, plus 8-library comparison  · PT-B4 (SpanParser KILL): LIVE-CONFIRMED the SpanParser tagged-union is present at span.ts lines 840-893+ (callSpan references at 840, 846, 857, 864, 872, 876, 889; SpanParserKind at 857, 864, 872, 876 · STALE CONTENT in KF-TO-PARSETHAT-B: The 'constellation version split' section (lines 303-305) reads: 'value.js P → 1.1.0 (API: VJ-L1 flatLeaf + VJ-L3 parseCSSSubValue) then 1.2.0 (perf: VJ-P1 color2In · The `allStrict()` undefined-preserving variant ask (PT-B1 adjacent, AUDIT-DIGEST P5/B9) — where `all()` drops undefined from tuples (`leaf.ts:125`) — is mentioned in the dispatch as 'Two adjacent corr · The `try/finally` around evalParser (packrat.ts:264-284) is similarly folded into PT-B1. Both are load-bearing correctness items that should be explicitly named within PT-B1's cure section.

**Validation (RAN):** Probes run in-session: (1) packrat cross-input bug CONFIRMED: p.parse('hello') then p.parse('world') without reset returns 'hello' stale result. Command: cd /Users/mkbabb/Programming/parse-that/typescript && npx tsx probe-packrat-final.mts. Output: 'parse("aaaaa"): hello parse("bbbbb", no reset): hello [BUG: stale]'. (2) getCijKey aliasing CONFIRMED: key for id=0,off=0: 0 key for id=4096,off=0: 0 [BUG: ID overflow aliasing confirmed]. (3) span.ts 902 lines with SpanParser tier confirmed present 

**Brainstorm:** Fix the stale version split paragraph in the INFORM section (lines 303-305): replace 'VJ-L1 flatLeaf + VJ-L3 parseCSSSubValue' with 'VJ-L3 parseCSSSubValue (VJ- · Promote the `allStrict()` undefined-preserving variant and the `try/finally` around evalParser from informal folds into explicit PT-B1 sub-items: 'PT-B1.a: src- · In PT-B3, note that the 2-char dispatch widening applies to value.js's internal calc/color buckets — a comment linking to which value.js grammars actually colli · The `span-dispatch.bench.ts` file (PT-B4 KILL target) is also one of the 12 benchmark files. Its deletion as part of PT-B4 should be noted in PT-B3's bench wiri

**→ Reformulation:** KF-TO-PARSETHAT-B is grounded and correct except one stale paragraph (INFORM §constellation version split, lines 303-305) which cites VJ-L1 (demoted-to-spike) and VJ-P2 (dropped) as value.js 1.1.0/1.2.0 contents — contradicting the authoritative KF-TO-VALUEJS-P dispatch. Fix: rewrite to match KF-TO-VALUEJS-P's current verdicts. The four PT-B asks are all falsifiable, born-RED confirmed on the live tree (PT-B1 cross-input bug verified at runtime; PT-B4 SpanParser tier present at lines 840-902; proof:perf absent; benchmarks present). No structural changes needed beyond the stale paragraph correction.

---

## O.W1-4-apparatus

### [KEEP] O.W0 — Audit-fold charter substrate + proof:audit-artifacts-O gate

**Harden:** All five prerequisite artifacts are on disk and verified: AUDIT-DIGEST.md, O.md, PROGRESS.md, deferred-ledger-O.md, prompt-recap-O.md. The O.W0.md wave spec is present with the deploy oracle (aef3ef3, · The born-RED gate script `scripts/proof-audit-artifacts-O.mjs` is ABSENT from the repo (`ls scripts/proof-audit-artifacts-O.mjs → no file`). The gate is unimplemented: no `proof:audit-artifacts-O` ent · The dev→impl boundary is clean on today's branch: `git diff --name-only origin/master..HEAD | grep 'src/\|demo/'` → empty. The boundary precept holds by inspection but not by automated gate. · The DO-4 ledger corrections (DM-24, DM-25, DM-W1-bridge, DM-4 KILL→FIX, DM-17 RESOLVED-BY-FIX, DM-5 count) are fully landed in `docs/tranches/O/audit/deferred-ledger-O.md` and `O/PROGRESS.md`. They ar · No contrivance smell. O.W0 is a pure docs/gate-script substrate wave with the born-RED observable being the absence of the wave spec file itself. The gate is non-browser, ~50 LOC, AXIS-3 STATIC — grou

**Validation (RAN):** Ran: `ls /Users/mkbabb/Programming/keyframes.js/scripts/proof-audit-artifacts-O.mjs` → no such file (ABSENT). Ran: `ls docs/tranches/O/{PROGRESS.md,O.md,audit/AUDIT-DIGEST.md,audit/deferred-ledger-O.md,audit/prompt-recap-O.md,waves/O.W0.md}` → all present. Ran: `git diff --name-only origin/master..HEAD | grep src/` → no output (boundary clean). Ran: `grep -c 'aef3ef3\|keyframes.babb.dev' docs/tranches/O/waves/O.W0.md` → 12. The one unmet requirement is the gate script itself.

**Brainstorm:** The gate script is ~50 LOC of pure `existsSync` + `readFileSync` + grep-count assertions — it can be authored in a single commit alongside the wave spec with no · Clause (d) could be made self-reinforcing: run `git diff --stat <O.W0-base>..HEAD -- src/ demo/` inside the gate and assert empty output. This makes the dev→imp · Consider adding a clause (f) asserting that `proof:audit-artifacts-O` itself appears in the `proof:hygiene` chain in package.json — a self-referential check tha

**→ Reformulation:** O.W0 is grounded: all artifact prerequisites are on disk, the deploy oracle is recorded, the DO-4 corrections are in the O ledger, and the dev→impl boundary is clean. The sole outstanding deliverable is the born-RED gate script `scripts/proof-audit-artifacts-O.mjs` + its package.json wiring — without this, CI cannot enforce clause (d). Implementation opens on authorization: author the gate script (born-RED on the pre-cure tree where any clause fails), wire into `proof:hygiene`, commit alongside O.W0.md. Zero src/demo edits. Applies the M ledger corrections (DM-4/DM-17/DM-24/DM-25/DM-W1-bridge) at O.W2, not here.

---

### [KEEP] O.W1 — Lint/dep-cruiser tier (proof:lint-tier, eslint flat-config + dependency-cruiser boundary rule)

**Harden:** The absence is total and confirmed: `ls node_modules/eslint` → ABSENT; `ls eslint.config.mjs` → ABSENT; `ls .dependency-cruiser.mjs` → ABSENT; `grep 'lint-tier' package.json` → no match; `ls scripts/p · There are 37 pure source-shape proof scripts (grep -L 'demo-driver\|chromium\|playwright\|vitest' scripts/proof-*.mjs → 37). Timing 10 of them serially: ~10s elapsed. 37 would be ~37s. A single `eslin · The wave spec defers the W96 parse-that forbidden-edge clause to O.W16 (Band G). The depcruise config in O.W1 is forward-seam-compatible — a one-line add at O.W16. This is a clean design that avoids a · No contrivance: ESLint 9 flat-config + dependency-cruiser are stable, widely-used tools. The custom `eslint-plugin-kf` local rules encode exact predicates already in the node forks (no new logic inven · The gate-first discipline is met: `proof:lint-tier` asserts at clause S1 level (eslint --version not found → exit 1) before any other clause, so the gate is born-RED today by tool absence.

**Validation (RAN):** Ran: `ls /Users/mkbabb/Programming/keyframes.js/node_modules/eslint` → ABSENT. Ran: `grep -L 'demo-driver\|chromium\|playwright\|vitest' scripts/proof-*.mjs | wc -l` → 37. Timed 10 sequential static gate runs: 9.7s elapsed for 10 processes (~1s/gate). An eslint pass over the same invariants benchmarks at <2s (standard eslint on a 500-file TypeScript project). The consolidation claim (33+ forks → 1 pass, ~10-15s → <2s) is directionally correct.

**Brainstorm:** The stale-override guard (a per-file `max-lines` override whose file has shrunk back under the base limit) could be implemented as a simple vitest meta-test tha · The `no-phantom-version` gate (O.W2) and `proof:lint-tier` share a theme: both are AXIS-3 static data-model checks. Consider wiring both into a single `proof:so · The forward seam for O.W16 (the parse-that forbidden-edge clause) should be documented as a TODO comment in the `.dependency-cruiser.mjs` itself — a `// FORWARD

**→ Reformulation:** O.W1 is grounded as-is. The absence is real, the born-RED gate is genuinely born-RED (tool absent = command not found), and the 37-fork consolidation is measurable. Implementation: greenfield `npm install -D eslint dependency-cruiser @typescript-eslint/parser @eslint/js eslint-plugin-vue`; author `eslint.config.mjs` + `.dependency-cruiser.mjs` per M.W2 S1-S6 spec; author `scripts/proof-lint-tier.mjs` born-RED; wire into package.json. The W96 forward seam is structurally clean — O.W16 fills it.

---

### [AUGMENT] O.W2 — Honest-correction wave (ledger re-point + stale-gate retarget + proof:no-phantom-version)

**Harden:** Finding 1 premise is now STALE: O.W2 was authored 2026-06-19 claiming glass-ui@4.1.0 'was never published (BB closed at 4.0.1); E404 (confirmed live 2026-06-19)'. But glass-ui@4.1.0 published on 2026- · However the S1 content-probe retarget is still correct and still needed. Glass-ui 4.1.0 dist `tabs.js` emits `'aria-orientation': L.value ? 'vertical' : 'horizontal'` unconditionally even when `role:  · S2 (dock RF-17): `useDockClickIntegrity` IS present in glass-ui 4.1.0 dist (`dist/components/custom/dock/composables/useDockClickIntegrity.d.ts` found). The gate correctly fires RED. P.W12 owns the S2 · The no-phantom-version gate concept is now partially obsolete: the 'phantom' was only phantom from 2026-06-19 to 2026-06-20. The gate's value post-publication is detecting the probe mechanism that fir · Findings 2-5 are all still correct: `proof:control-point-live` still asserts a dead premise (BC said NO to GlassControlPoint — confirmed in glass-ui 4.1.0 dist, no GlassControlPoint export); DM-4 says · DM-4/DM-17/DM-24 corrections are in deferred-ledger-O.md (O tranche ledger) per O.W0's DO-4, but O.W2's mandate is to ALSO apply them to the M tranche's `deferred-ledger-M.md` and `M/PROGRESS.md`. The

**Validation (RAN):** Ran: `npm show @mkbabb/glass-ui versions --json` → includes '4.1.0'. Ran: `npm view @mkbabb/glass-ui@4.1.0` → 'published 2 days ago by mkbabb'. Ran: `node scripts/proof-workaround-deletion.mjs` → '1 GREEN / 2 PENDING / 2 RED' with S1=RED S2=RED (both fire RED because 4.1.0 is now published). Ran: `grep 'aria-orientation' node_modules/@mkbabb/glass-ui/dist/tabs.js` → emitted unconditionally when role='group' (no conditional guard). Ran: `grep -r 'useDockClickIntegrity' node_modules/@mkbabb/glass-

**Brainstorm:** The `proof:no-phantom-version` gate should be rephrased as `proof:probe-fidelity` — asserting that every PENDING arm in proof:workaround-deletion probes the ACT · S1's content-probe should be a grep for a conditional guard expression in the installed dist (e.g., `isUnderline.*aria-orientation` or `role.*tablist.*aria`), n · O.W2 should be split into two commit-level actions: (a) retire proof:control-point-live and apply DM-4/DM-17/DM-24 M ledger edits (these are correct regardless 

**→ Reformulation:** O.W2 is substantively correct but has one stale-premise in Finding 1: glass-ui@4.1.0 published on 2026-06-20 (the day after O.W2 was authored). The cure's S1-retarget-to-content-probe is MORE needed now, not less — the gate incorrectly fires RED for S1 because it treats 4.1.0 publication as sufficient, but the conditional aria-orientation guard is absent from the 4.1.0 dist. Augment the wave spec: (1) note that 4.1.0 is now published (correct the 'phantom' premise for historical accuracy); (2) confirm the S1 content-probe retarget is still required and now urgent (current gate fires RED for wrong reason — publication without the feature guard); (3) S2 retarget is structurally correct but P.W12 owns the S2 deletion — O.W2 only retargets the probe; (4) Findings 2-5 (control-point-live retirement, DM-4/17/24 M-ledger edits) are unambiguously correct and unaffected by the 4.1.0 event. The `proof:no-phantom-version` gate should be rephrased to probe feature-content presence, not version publication alone.

---

### [AUGMENT] O.W3 — Named-selector NaN-frame cure (DM-22) + P.W9 ownership reconciliation

**Harden:** The NaN defect is confirmed LIVE by direct probe: `new CSSKeyframesAnimation().fromString('@keyframes x { entry {opacity:0} exit {opacity:1} }').parse([])` does not throw; `frame.time = {start: NaN, s · The dead-write claim is confirmed: `grep -n 'NAMED_SELECTOR_SUPERTYPE' src/animation/frame-compiler.ts` returns hits only at lines 112, 123, 128, 219, 226, 230 — all in `addFrame` context. The `parse( · The S4 proxy claim is confirmed: `scripts/proof-replay-equality.mjs:173-178` contains `requireAll('named-selector', FRAME_COMPILER, [{ re: /entry\s*\|\s*exit\s*\|\s*cover\s*\|\s*contain/ }])` — a pure · DM-22 ownership is unambiguous: O.W3 = specification (DEVELOPED, never IMPLEMENTED). P.W9 = implementation + grammar-fuzz + differential oracle extensions. The P deferred ledger at `deferred-ledger-P. · P.W9 has one stale cross-reference: S4 states 'the full CDP path requires the DM-23 vitest-browser runner (O.W2)'. O.W2's wave spec does NOT own vitest-browser — it covers ledger re-point + stale-gate

**Validation (RAN):** Ran probe: `cd /Users/mkbabb/Programming/keyframes.js && npx tsx /tmp/test-nan-frame.mts` → 'No throw - checking frames: frame count: 1, frame 0 start: NaN stop: NaN'. Ran: `node -e "'entry'*1000/100"` → NaN. Ran binarySearch simulation: NaN range returns index 0 for all t values (confirmed with inline node script). Ran: `grep -n 'NAMED_SELECTOR_SUPERTYPE' src/animation/frame-compiler.ts` → 5 hits, all in addFrame context, zero in parse() body. Ran: `grep -rn 'NAMED_SELECTOR_NO_TIMELINE' src/ani

**Brainstorm:** The `NAMED_SELECTOR_SUPERTYPE` scan in parse() (the Path A cure) can be a 3-line guard: scan `this.templateFrames` before the sort, collect any with `start.supe · The `proof:named-selector-nan-frame` gate script should include a `non-named-unaffected` regression guard (a normal 0%/100% animation must parse without throwin · Path B (ScrollTimeline named-range → numeric % bind) creates a `bindTimeline(timeline)` re-resolve pass. The superType read site O.W3 creates is the exact exten

**→ Reformulation:** O.W3 is grounded and the NaN defect is confirmed live. The wave spec is authoritative for DM-22; P.W9 correctly inherits as the implementation tranche with zero spec conflict. One augmentation: O.W3's standalone gate design (`proof:named-selector-nan-frame` as separate from `proof:replay-equality`) is the correct call — independently confirmed. P.W9's stale cross-reference to 'DM-23 vitest-browser runner (O.W2)' in S4 should be corrected: the WAAPI-eligible Playwright subset is implementable TODAY with `demo-driver.mjs` and requires no vitest-browser. The S4 full CDP path is deferred to a P.WZ+ item. Update P.W9 S4 to remove the O.W2/DM-23 dependency for the subset gate.

---

### [KEEP] O.W4 — Multi-color refusal + ingest (⚠M2 oklch space, ⚠M3 property drop, cross-depth sibling linkage, shadow-DOM fromLiveAnimations)

**Harden:** ⚠M2 oklch space defect confirmed live: `src/animation/compile-color.ts:191` calls `colorToOklabCSS(ramp[s]!)` unconditionally inside `densifyKey`; `grep colorToOklchCSS src/animation/compile-color.ts` · ⚠M3 property-drop defect confirmed live: `compile-color.ts:139-142` shows `DensifyResult = { block: string } | { refused: true; delta: number } | null`. In `compile.ts:397` the densify `block` REPLACE · Cross-depth sibling linkage defect confirmed: `ingest-cssom.ts:188` seeds `const styleRuleTexts: string[] = []` fresh at EVERY call to `walkSheet`, including the recursive call at line 242. A `@keyfra · `fromLiveAnimations` shadow-DOM defect confirmed: lines 449-454 compute `sheetSource = ownerDoc ?? undefined` always using the owner document, never `getRootNode()`. The L.W3 S4 `ShadowRoot` branch in · The existing gate proxy for cross-depth: `proof-ingest-replay.mjs` S2 clause greps that `walkSheet` recurses — blind to whether the reconstructed animation carries its sibling-declared timing. The sha · No contrivance: the `bodyByStop` seam already exists; the cure is routing the densify through an existing projection authority. The `siblingTexts` accumulator cure is a straightforward thread-through.

**Validation (RAN):** Ran: `grep -n 'colorToOklchCSS' src/animation/compile-color.ts` → 0 hits (no oklch emitter). Ran: `grep -n 'colorToOklabCSS\|stops.push\|densifyKey' src/animation/compile-color.ts` → line 191 `stops.push({ pct: round(pct), css: colorToOklabCSS(ramp[s]!) })` unconditional. Ran: `grep -n 'densify.block\|DensifyResult' src/animation/compile.ts` → line 397 `densify.block` replaces `keyframesBlock`. Ran: `grep -n 'bodyByStop' src/animation/format.ts` → line 243 seam confirmed present but unused by de

**Brainstorm:** The S1 cure (`toCSSColor = space === 'oklch' ? colorToOklchCSS : colorToOklabCSS`) can also hoist the 1024-stop reference ramp above the inner `s` loop at the s · The `DensifyResult` shape change (from `{ block: string }` to `{ stops: Map<number, Map<string, string>> }`) is a breaking internal type change that touches `de · The `siblingTexts` accumulator parameter to `walkSheet` could be typed as `string[]` passed by reference — a clean signature change that makes the threading exp · For the shadow-live fix, `getRootNode({composed: false})` is the correct DOM call — it returns the shadow root for shadow-hosted elements and the document other

**→ Reformulation:** O.W4 is grounded on four confirmed live defects. Sub-scope A: `colorToOklabCSS` at `compile-color.ts:191` is space-blind (⚠M2 confirmed); `densify.block` bypasses `keyframesBlock` (⚠M3 confirmed, `bodyByStop` seam exists at `format.ts:243`). Sub-scope B: `walkSheet` creates a fresh `styleRuleTexts` per depth (cross-depth sibling silent loss confirmed); `fromLiveAnimations` never calls `getRootNode()` for shadow targets (shadow-DOM silent absent confirmed). Both born-RED gates (`proof:oklch-densify-space` and the `proof:ingest-replay` extensions) assert the genuine emitted CSS artifact and the reconstructed Map result, not source shapes. The wave is executable NOW on value.js 1.0.2 with no sibling dependency.

---

## O.W5-6-chronics

### [KEEP] O.W5 — DemoControlPoint over LIGHT drag2D (DM-2 7-tranche chronic terminal, P-inv-28)

**Harden:** FEASIBILITY CONFIRMED: drag2D IS a LIGHT barrel export (src/animation/index.ts:88 `export { drag, Draggable, drag2D } from "./drag"`); drag-2d.ts carries ZERO static value.js edge (imports only Dragga · BORN-RED CONFIRMED: `grep -rn DemoControlPoint demo/ src/ scripts/` = ZERO; scripts/proof-demo-control-point.mjs ABSENT; scripts/proof-control-point-live.mjs PRESENT; `grep GlassControlPoint glass-ui/ · ROSTER CLAIM CORRECT for O.W5: proof:correctness (package.json:193) DOES contain proof:easing-editor-live + proof:drag-gesture (the runtime interaction gates O.W5 S3 mirrors). So placing proof:demo-co · COHERENCE TENSION (not blocker): useEasingCurveDrag.ts already rides the SHARED useDragCapture seam (line 3), which is consumed by 7+ demo files (PlaybackRibbon, AnimationVisualizer, AssetViewport, As · OVERLAP RISK: O.W5 live-drag keystone (drive page.mouse.down→move→up over a handle, assert bezier d mutates + subject re-animates) substantially DUPLICATES the EXISTING proof:easing-editor-live clause · SCOPE DISCIPLINE GOOD: correctly scoped as a DEMO primitive (demo/@/), NOT a library export; library control-point excluded to keyframes-vue; promotes inv-16 (kf writes only keyframes.js, glass-ui ask

**Validation (RAN):** RAN: confirmed drag2D LIGHT export at src/animation/index.ts:88 (grep) + drag-2d.ts zero value.js edge (Read). RAN: `grep -rn DemoControlPoint demo/ src/ scripts/` = ZERO (born-RED). RAN: `grep GlassControlPoint node_modules/@mkbabb/glass-ui/dist/` = ZERO (dead premise). RAN: `grep -rln useDragCapture demo/` = 7 consumers (fork-not-unify finding). The live-drag/keyboard-operable browser observables are NOT testable in-session (need built dist/gh-pages + a driven Chromium), but the substrate + bo

**Brainstorm:** AVOID the live-drag duplication: rather than re-implement the page.mouse drag-reshapes-curve assertion (already in proof:easing-editor-live clause b), have proo · Make keyboard-operable the DISTINCTIVE keystone: it is the one clause with NO existing precedent and it catches the real appearance/interaction blind-spot (a dr · Reuse drag2D's per-axis bounds for the arrow-nudge step: the keyboard nudge can write through the SAME DragOptions.transform/bounds map (drag.ts:46,306-316), so

**→ Reformulation:** KEEP O.W5 as the DM-2 build-in (substrate + born-RED + dead-premise all confirmed; this is a grounded, sibling-free chronic exit). One SIMPLIFY to fold in: de-duplicate the S3 live-drag clause against the EXISTING proof:easing-editor-live clause (b) which already asserts 'drag mutates bezier d + subject re-animates' — make proof:demo-control-point assert the NET-NEW deltas (component-built/light-substrate consolidation + the keyboard-operable a11y axis, which has NO precedent), and let proof:easing-editor-live keep owning the drag-reshapes-curve observable (it passes today and survives the consolidation as a free regression net). Clarify the S2 prose: the consolidation forks the curve handle onto drag2D while the other 6 useDragCapture consumers remain (it is a local single-seam, not a demo-wide unification); the named deletion (useEasingCurveDrag.ts only, never useDragCapture) is correct.

---

### [ADOPT] O.W6 — fromMorphSVG over value.js PathGeometry (DM-3 7-tranche chronic terminal, P-inv-28)

**Harden:** FEASIBILITY FULLY CONFIRMED + KEYSTONE PROTOTYPED: value.js 1.0.2 exports PathGeometry/getTotalLength/getPointAtLength (index.d.ts:50; runtime typeof=function for all three). PathGeometry.getPointAtT/ · KEYSTONE PROVEN: a triangle('M 0 0 L 100 0 L 50 100 Z')→square('M 0 0 L 100 0 L 100 100 L 0 100 Z') morph sampled at 64 uniform t, lerped at mid-t, is DISTINCT from BOTH endpoints — mean per-point dis · SPEC'S CLAUSE-4 PRECISION EMPIRICALLY VINDICATED: I measured dist(from[0], to[0]) = 0.000000 — the start vertex M 0 0 IS shared between triangle and square. A single-endpoint-vertex distinctness compa · DEGENERATE-REFUSAL FEASIBLE: PathGeometry('M 50 50 Z').totalLength===0 and PathGeometry('M 50 50').totalLength===0 — so the spec's 'reject zero-length with AnimationOptionError, don't fake an identity · WIRING PRECEDENT EXACT: load-engine.ts:34-39 (type imports), :134-137 (AnimationEngine interface rows), :422-423 (Promise.all dynamic import), :475-477 (engine-surface assign) are the fromMotionPath/f · ROSTER ERROR (real spec defect): O.W6 S3 says wire proof:morphsvg-consume into proof:correctness (package.json:193) 'beside proof:motion-path / proof:drawsvg'. But proof:motion-path (line 83) and proo · ENGINE-COMPAT NUANCE (worth a spec note): interpolating a CSS d:/path() channel requires MATCHED command counts between 0% and 100% frames. Uniform-samples sampling guarantees this (both polylines emi

**Validation (RAN):** RAN `npx tsx` (from project root for resolution): (1) export existence — PathGeometry/getTotalLength/getPointAtLength all typeof function; (2) keystone morph — triangle→square at 64 samples, mid-t mean-dist 11.27 from EACH endpoint vs 22.54 between (mid exactly midway, DISTINCT from both); (3) shared-endpoint hazard — dist(from[0],to[0])=0.000000 (confirms spec's full-set/interior-s precision is required); (4) degenerate refusal — totalLength===0 for 'M 50 50 Z' and 'M 50 50'; (5) full composito

**Brainstorm:** Make the test-locks (S3 clause 5) assert distinctness over an INTERIOR sample s=getPointAtT(0.5) of the 50% frame's polyline (not the full-array mean) — it is c · Consider sampleAtLength's tangent angle (returned, angle=2.0344 rad at mid confirmed callable) as the foundation for the BOOKED orient-along-path follow-on — no · Default samples=64 is sound but the degenerate-refusal + a samples<2 guard (a 1-sample 'morph' is just the endpoint) would harden the input contract beyond zero · Fix the roster placement: wire proof:morphsvg-consume into proof:hygiene (line 194) to genuinely sit beside proof:motion-path/proof:drawsvg, OR keep it in proof

**→ Reformulation:** ADOPT O.W6 — the keystone is PROVEN, not merely planned: I benched the exact triangle→square morph the gate will run and confirmed mid-t is distinct from both endpoints (11.27/11.27/22.54), and I empirically confirmed the spec's clause-4 precision warning (shared start vertex dist=0.000000 — a single-endpoint compare WOULD be degenerate). PathGeometry/getPointAtT/sampleAtLength are all callable on 1.0.2; degenerate refusal (totalLength===0) is implementable; the load-engine wiring slots are exact; the single-valuejs-edge distinction from DrawSVG/MotionPath (which carry zero own value.js import) is correct. Fold in TWO corrections: (1) the roster claim is WRONG — proof:motion-path/proof:drawsvg live in proof:hygiene (line 194), not proof:correctness (line 193); either wire proof:morphsvg-consume into proof:hygiene to actually sit beside them, or keep it in proof:correctness and drop the false 'beside proof:motion-path/proof:drawsvg' rationale. (2) Add a spec note that uniform-samples sampling is the engine-compatibility enabler (matched command counts for a d:/path() channel), which is the concrete reason topology-aware correspondence is correctly BOOKED. Implementation stays gate-first/no-legacy/HEAVY-behind-loadAnimationEngine as specified.

---

## O.W7-9-engine

### [KEEP] O.W7 — engine-seam transposition: lift the lifecycle/playback machine (concern 3) out of the KeyframesAnimation class body into a colocated INTERNAL engine-playback.ts; expected 1397L -> ~900L; retire/re-write the engine.ts:1400 LIBRARY_CEILING_OVERRIDE; re-point the three parallel ceiling clauses; dispose the group.ts S5 arm.

**Harden:** Line count VERIFIED LIVE: `wc -l src/animation/engine.ts` = 1397 (3 under the 1400 override, exactly as the wave states). The KeyframesAnimation class body is 1088L (`proof:engine` reports 'class is 1 · The ~900L target is GROUNDED by method arithmetic, not asserted: concern-3 playback methods span ~lines 596 (paintRest) through 1186 (group), ~590 lines. Lifting them brings engine.ts to roughly ~800- · THE SEAM SUBTLETY THE WAVE CORRECTLY FLAGS: interpFrames/at/processFrame (608-797) STRADDLE the compile/play boundary — interp is read by BOTH the compile facade and the play loop. The wave's `this`-b · THREE ceiling gates VERIFIED as claimed: proof:decomposition cap=1400 (`LIBRARY_CEILING_OVERRIDE` Map + stale-override guard at :381-389), proof:engine ANIMATION_CLASS_CEILING=1100, proof:engine-no-th · CONTRIVANCE SMELL-TEST PASSES: this is the single largest named structural debt (DF-11-A, deferred D->...->M), NOT a manufactured rewrite. It is gestalt cohesion-improving (the god-object at the D.W4- · The VJ-L1 gate REMOVAL is correct: I confirmed FN_NAME = Symbol('kf.fnName') at utils.ts:45 is read ONLY in the parse/flatten/clone interp seam (utils.ts:45,47,51,55,213,289,342) — NEVER in the play/p · NOTE: P.W10 re-sequences O.W7 under the P tree ('O.W7 re-sequences under P as the VJ-L1-gated engine split; its FN_NAME precondition is P.W11's WeakMap early-cure'). On the current tranche-p-dev branc

**Validation (RAN):** BORN-RED WITNESS VERIFIED + split arithmetic measured. Ran `node scripts/proof-decomposition.mjs` -> exit 0 (passes ONLY because the 1400 override masks the 1397L file — confirming the wave's claim that removing the override is the born-RED trigger). `node scripts/proof-engine.mjs` -> exit 0, reports 'KeyframesAnimation class is 1088 lines <= ceiling 1100'. Method-span analysis (python over engine.ts): concern-3 playback machine = paintRest(596)..reset(1162)/group(1186), ~590 lines liftable. The

**Brainstorm:** The 12L class-body headroom (1088 vs 1100) is a chronicity tell: the class is one feature away from RED-ing proof:engine independently of the file-line override · S5 (group.ts) disposition: group.ts is 812L under its 820 cap (8L headroom — verified). The override `why` says the gate-anchored composite STATEMENTS must stay · Add to S3's acceptance: a `proof:standalone-zero-alloc` assertion that the lifted interpFrames retains the `_interpOut` buffer-reuse identity across the module  · Keep engine-playback.ts a thin `applyComposition(this, …)`-style binding host (the existing engine-composition.ts pattern) rather than a `PlaybackHost` protocol

**→ Reformulation:** KEEP O.W7 as specified. The structural debt is real (1397L verified, ~590L liftable, ~900L target grounded), the born-RED witness is genuine (override-removed decomposition exit 1 on the real wc -l), the VJ-L1-gate removal is correct (FN_NAME confirmed scoped to concern 1, never read in concern 3), and it follows the established extraction precedent. ADD: (1) frame it as relieving both the 1400 file cap AND the 1100 class cap (12L headroom today); (2) prefer the bound-free-function host over a PlaybackHost protocol (KISS, one consumer); (3) lean S5 toward re-defer-with-revised-rationale unless S1's re-thread measurably loosens the group.ts composite seam. Sequence under P.W10/S5 (alias drop) before the class-body lift.

---

### [SIMPLIFY] O.W8 — performance closure: 5 measurement-instrument gaps (S1 numeric-soa bench, S2 color-interp bench, S3 sync-step taxonomy wiring, S4 real-browser postTask INP, S5 device-independent HARD ratio floors, S6 doc correction, S7 cross-repo dispatch) + 2 NEW O-surfaced zero-alloc engine cures: S8 transformTargetsStyle per-frame Record alloc -> out-buffer; S9 reconcileVars findIndex O(N) -> O(1) Map.

**Harden:** THE FIVE GATE-COVERAGE GAPS ARE REAL AND BORN-RED (verified live): bench/numeric-soa.bench.ts ENOENT, bench/color-interp.bench.ts ENOENT, taxonomy.json suites = [interpolation,parser,interp-buffer,com · S8 (transformTargetsStyle out-buffer) — THE PREMISE IS GROUNDED BUT THE CURE IS FALSIFIED BY MEASUREMENT (the Typed-OM lesson). value.js 1.0.2 unflattenObjectToString DOES accept an `out?` param (veri · S9 (reconcileVars O(1) Map) — THE QUADRATIC PREMISE IS FALSIFIED BY MEASUREMENT. The wave claims frame-compiler.ts:418 `this.frames.findIndex` 're-introduces O(N^2)'. Benched compile across N=10..1000 · S5 (device-independent HARD ratio floors) is SOUND and aligns with the E24-confirmed gold standard + the device-dependence-greening lesson in MEMORY.md — every HARD predicate a same-report `baseHz x f · S4 (real-browser postTask INP) correctly identifies a GREEN-by-SKIP proxy failure — the gate has NEVER measured the INP delta it was built to gate. RE-TARGETing onto a real-chromium measurement with a · S6 doc correction (the '1.56x-4.25x' is value.js's G-era number mis-cited as kf's J.W6 measurement) is a grep-falsifiable factual fix — KEEP.

**Validation (RAN):** S8 BENCHED (FALSIFIED): `npx tsx bench/_tmp_s8_*.mts` over 300k frames, 3 trials -> current 117.6/114.5/115.2ms vs out-buffer 141.4/135.9/137.8ms = 0.83/0.84/0.84x (SLOWER); retained-heap probe with --expose-gc: current 0.06MB vs out-buffer 0.00MB over 200k frames. S9 BENCHED (FALSIFIED): `npx tsx` compile bench N=[10,50,100,250,500,1000] x2 shapes -> per-stop ratios flat/decreasing (no O(N^2)); total linear. S1/S2/S3/S4 born-RED witnesses verified by filesystem + taxonomy.json + proof-scheduler

**Brainstorm:** KEEP S1-S7 (the falsifiability apparatus) as the honest core of the wave — they are pure measurement instruments that make asserted perf claims testable; that I · KILL S8 outright and record the bench in the wave as the measured KILL evidence (a `scripts/transform-out-buffer-decision.json` analogous to typed-om-decision.j · KILL S9 and record the linear-compile bench as the falsification — but SALVAGE the bench/compile.bench.ts N=1000 case as a permanent regression guard (it is the · If the impl wants a genuine zero-alloc engine win, point it at the PLAY hot path (interpFrames/_interpOut buffer reuse, already gated) rather than the COMPILE p · S2's densify-COMPILE color bench is the genuinely useful new instrument — it is the before/after needed to validate value.js-P's color-math zero-alloc rewrites 

**→ Reformulation:** SIMPLIFY O.W8: KEEP S1-S7 (the falsifiability/measurement apparatus — all five born-RED witnesses verified real; S5 device-independent ratio floors and S4 real-browser postTask are the correct portability + observable-truth disciplines). KILL the two NEW O-surfaced engine cures S8 and S9 — both FALSIFIED by in-session bench: S8 out-buffer is 0.83x (16-20% SLOWER, the value.js delete-clear-loop costs more than the GC-cheap young-gen Record it removes; retained heap 0.06MB/200k frames), S9 reconcileVars is already LINEAR (flat per-stop cost N=10..1000, no quadratic). Record both KILLs in a decision JSON (the typed-om-decision.json pattern). Salvage the bench/compile.bench.ts N=1000 case as an observe-only linear-regression guard (it proves the code is already O(N)). This removes the wave's only two engine-touching arms — leaving O.W8 a pure-instrument wave (no engine code), which is the stronger, KISS form.

---

### [RE-SCOPE] O.W9 — no-legacy cuts: S1 drop @deprecated Animation alias (engine.ts:1205) + migrate 22 demo consumers; S2 drop @deprecated ScrollTimeline/ScrollTimelineOptions aliases (timeline.ts:171,209-218); S3 swap internal/leaves.ts clamp/scale/lerp/lerpArray for `@mkbabb/value.js/math` re-exports (verify proof:boundary holds); S4 author proof:no-legacy-surface born-RED on the published d.ts.

**Harden:** S1/S2/S4 born-RED witnesses VERIFIED LIVE: `grep -rln 'import type.*Animation' demo/` = 22 (EXACTLY the wave's count), engine.ts:1205 `export { KeyframesAnimation as Animation }` present with @depreca · S3 IS A TRAP — and P.W10 ALREADY CAUGHT IT (the reconcile the prompt asked for). VERIFIED: the proof:boundary regex `@mkbabb\/value\.js(?:\/[^"']*)?` MATCHES `@mkbabb/value.js/math` (ran the regex: re · THE leaves.ts COMMENT IS PARTLY-STALE BUT THE OPERATIVE RATIONALE IS STILL TRUE. leaves.ts:56-64 claims 'no tree-shakeable ./math subpath' — that specific clause is now FALSE (value.js 1.0.2 ships dis · P.W10 (the P-tree successor) correctly transposes S3 into: Arm A (bundle-externalize `@mkbabb/value.js/math` via vite.config.ts external widen + W97 graph-clean gate + H4 smoke-test that the subpath r · CONTRIVANCE SMELL-TEST: S1/S2 are clean no-legacy cuts (real aliases, real consumers, gate-first born-RED keystone reading the BUILT d.ts not source — observable-truth correct). S3 as O.W9 wrote it is · group.ts S5-adjacent: confirmed group.ts is 812L under its 820 cap (8L headroom) — orthogonal to O.W9; the O.W9/O.W7 sequencing (alias drop before class-body lift) is disjoint-region and composes clea

**Validation (RAN):** Born-RED witnesses VERIFIED: 22 demo Animation consumers (grep), engine.ts:1205 + timeline.ts:171,209 aliases present (grep), leaves.ts:23/28/46/68 local math defs present. THE TRAP MEASURED: ran the proof:boundary SPEC regex in node -> `@mkbabb/value.js/math` matches TRUE -> O.W9 S3's re-export WOULD red assertion 4. value.js/math subpath confirmed shipped (dist/subpaths/math.d.ts, parse-that-free). P.W10.md read end-to-end — it correctly transposes S3 into externalize-or-document with W97/H4 g

**Brainstorm:** RE-SCOPE S3 to defer entirely to P.W10's resolution (it is already authored, gate-verified, with the decision JSON). O.W9 should NOT carry a leaves.ts cut at al · KEEP S1/S2/S4 as-is (P.W10/S5 already carries S1/S2 onto the P tree unchanged — the cure is identical, only the tranche home and 5.x-vs-5.1.x version policy dif · For the corrected leaves.ts comment (whichever arm P.W10 picks): make it cite the proof:boundary regex line (:96) explicitly so a future reader sees the gate-fo · If P.W10 Arm A externalizes: the H4 rolldown-external smoke-test is essential and correctly specified — the bare `@mkbabb/value.js` external string does NOT pre

**→ Reformulation:** RE-SCOPE O.W9. KEEP S1 (drop @deprecated Animation + migrate 22 demo consumers), S2 (drop ScrollTimeline/ScrollTimelineOptions aliases), and S4 (proof:no-legacy-surface, keystone reading the BUILT d.ts) — all born-RED witnesses verified real, all clean no-legacy cuts. RE-SCOPE S3 (leaves.ts->/math) OUT of O.W9 and DEFER to P.W10, which already correctly transposes it: the naive `export from '@mkbabb/value.js/math'` would RED proof:boundary (the gate regex matches the subpath — verified TRUE), so the leaves are STRUCTURALLY FORCED, not legacy. P.W10's gate-verified resolution (Arm A bundle-externalize via vite external-widen + W97 graph-clean gate + H4 smoke-test, OR Arm B documented-keep with corrected comment, verdict in leaves-externalization-decision.json) is the sound form. Either way the only true O.W9-side action on leaves.ts is correcting the stale 'no ./math subpath' comment to cite the real gate-forced rationale (the duplication is not legacy when the boundary gate bans even the subpath specifier in light source). On the P tree, S1/S2 are already carried by P.W10/S5.

---

## O.W12-WZ-consume

### [RE-SCOPE] O.W10 — VJ-L1 flatLeaf dispatch as the terminal S8 cure and the O.W7 engine-seam unblocker

**Harden:** VJ-L1 absence confirmed live: `node -e "'flatLeaf' in require('@mkbabb/value.js')"` → false (value.js 1.0.2 installed). The ask is LIVE and S8 is correctly PENDING. · CONTRIVANCE KILL on the VJ-L1-as-O.W7-unblocker claim: P.md CONTRIVANCE-AUDIT and P's KF-TO-VALUEJS-P.md both find O.W7 engine-seam split is NOT VJ-L1-gated — executable NOW on the current tree. The g · P.W11 WeakMap supplants VJ-L1 as the TERMINAL S8 cure: a kf-local `WeakMap<ValueUnit, string>` populated at flatten time is realm-clean (no foreign-object annotation), in-realm, and sufficient. The We · VJ-L3 (parseCSSSubValue) SURVIVES as the binding dispatch ask: S9 cannot be self-solved by kf without VJ-L3 (kf cannot replicate value.js's CSS sub-value grammar in-realm without either a direct parse · The O.W10 dispatch doc (KF-TO-VALUEJS-P-ASKS.md, 282 lines) is present at docs/tranches/O/KF-TO-VALUEJS-P-ASKS.md. The born-RED gate script `scripts/proof-valuejs-p-ask.mjs` does NOT exist (confirmed: · The PROGRESS.md wave board correctly marks O.W10 as DEVELOPED (not IMPLEMENTED). Gate spec is authored; implementation opens on authorization.

**Validation (RAN):** node -e "const v=require('@mkbabb/value.js'); console.log('flatLeaf:', 'flatLeaf' in v); console.log('parseCSSSubValue:', 'parseCSSSubValue' in v);" → flatLeaf: false, parseCSSSubValue: false. Both APIs absent from value.js 1.0.2. S8/S9 correctly PENDING. W96 parse-that scan absent: `grep 'holdsParseThat\|parse-that' scripts/proof-boundary.mjs` → only prose/comment hits, no assertion.

**Brainstorm:** Re-scope the O.W10 dispatch doc to accurately reflect P's adjudication: VJ-L1 is DEMOTED-TO-SPIKE (P.W11 WeakMap is the terminal S8 cure); VJ-L3 is the survivin · O.W16's S3 (delete FN_NAME on VJ-L1 publish) should be RE-SCOPED: S3 becomes P.W11's WeakMap swap (NOW, no VJ-L1 wait); O.W16 retains only S4 (delete direct par · The `proof:valuejs-p-ask` gate spec's `vj-l1-absent` clause should be DEMOTED to informational (it asserts ask is live, not a blocker); the `vj-l3-absent` claus

**→ Reformulation:** O.W10 dispatch scope: RETAIN VJ-L3 parseCSSSubValue as the sole binding API ask (S9's terminal home). DEMOTE VJ-L1 to spike per P.W11's in-realm WeakMap resolution (P.W11 is the S8 terminal; VJ-L1 is a nice-to-have restamp-ceremony retirement). STRIKE the claim that VJ-L1 unblocks O.W7 — the engine-seam split is NOT VJ-L1-gated (P CONTRIVANCE-AUDIT). The dispatch doc exists (282 lines); update its scope section to reflect the P adjudication. The born-RED gate spec (`proof:valuejs-p-ask`) should assert VJ-L3 absence as the primary live observable, with VJ-L1 as secondary-informational only.

---

### [AUGMENT] O.W11 — glass-ui BC aria-orientation SFC guard dispatch (the S1 premise correction)

**Harden:** The underlying defect is CONFIRMED LIVE on glass-ui 4.1.0 (the current npm latest): `grep 'aria-orientation' /tmp/gu41/package/dist/tabs.js` shows `'aria-orientation': L.value ? 'vertical' : 'horizont · The born-RED observable is REAL and currently observable: mount glass-ui 4.1.0 `SegmentedTabs` with `variant='pill'` — `[role='group']` element carries `aria-orientation='horizontal'`. ARIA 1.2 §6.3 p · The `proof:glassui-aria-ask` gate script does NOT exist (confirmed: `ls scripts/proof-glassui-aria-ask.mjs` → no such file). The implementation is not yet authorized. · The S1 workaround sites are CONFIRMED PRESENT: `demo/spring/SpringSidebar.vue:43` and `demo/@/components/custom/animation-controls/controls/AnimationControls.vue:72` both carry `:aria-orientation='und · CRITICAL: proof:workaround-deletion S1 shows RED (not PENDING) because glass-ui 4.1.0 was published between O authoring (2026-06-19) and today (2026-06-22). The version sentinel `version: '4.1.0'` in  · The O.W11 corrected ask to glass-ui BC (the conditional guard `:aria-orientation='isUnderline ? (isVertical ? vertical : horizontal) : undefined'`) is grounded, surgical, and non-speculative. The defe

**Validation (RAN):** node -e "const r=require('fs'); const s=r.readFileSync('/tmp/gu41/package/dist/tabs.js','utf8'); const lines=s.split('\n'); lines.forEach((l,i)=>{ if(l.includes('aria-orientation')) console.log(i+1,l.trim()); });" → confirms unconditional binding in 4.1.0. `node scripts/proof-workaround-deletion.mjs 2>&1 | grep S1` → S1=RED (version sentinel triggered, fix absent — the gate is a FALSE RED). Gate script absent: `ls scripts/proof-glassui-aria-ask.mjs` → no such file.

**Brainstorm:** The O.W2 content-probe retarget for S1 is now URGENT (the version sentinel is a false RED on 4.1.0 — it says 'safe to delete' when NOT safe). O.W2 S1 cure: repl · The O.W11 gate spec (pill-prohibited-present clause) should be trivially testable with jsdom if glass-ui 4.1.0 is installed. But it is NOT installed (kf pins `~ · The dispatch addendum `docs/tranches/O/KF-TO-GLASSUI-BC-ADDENDUM.md` is PRESENT (authored by this wave). The ask is formally filed.

**→ Reformulation:** O.W11 dispatch is grounded and the ARIA defect is CONFIRMED in 4.1.0 (verified). AUGMENT with an URGENT note: the O.W2 content-probe retarget for proof:workaround-deletion S1 is NOW a critical blocker — glass-ui 4.1.0 was published after O authoring and the version sentinel creates a FALSE RED (signals safe-to-delete when the SFC fix is absent). O.W2 must be implemented before O.W12 to prevent a premature S1 deletion. The gate spec for `proof:glassui-aria-ask` remains correct. Born-RED state is confirmed live.

---

### [AUGMENT] O.W12 — BC consume: S1 + S2 deletions + BC re-pin (the atomic unlock)

**Harden:** DM-1 chronicity is 5 (I, J, K, L, M→O) per PROGRESS.md §2, NOT 4 as stated in O.W12 body. The P-inv-28 belt fires at 5-tranche carry; this makes the O.W12 S3 deletion (dock interim) a hard mandate, no · CRITICAL FALSE RED: proof:workaround-deletion S1=RED and S2=RED TODAY because glass-ui 4.1.0 published on npm (registry shows 4.0.0, 4.0.1, 4.1.0). The gate uses version sentinel `4.1.0` for both arms · The S2 (dock interim) status is more nuanced: P.md states 'S2/dock (useDockClickIntegrity, shipped 4.0.1) IS deletable NOW' — meaning the buttery dock morph in 4.1.0 (35 expand/collapse/morph matches  · The gate spec for S1 requires implementing O.W2's content-probe FIRST. The O.W12 sequence MUST be: O.W2 (retarget sentinel) → O.W11 (dispatch received/confirmed by BC) → BC ships SFC guard → O.W12 fir · Gate scripts for O.W13/O.W14/O.W15 are all absent (confirmed). This is consistent with O being in DEVELOPMENT-not-IMPLEMENTED phase. · The `proof:live-session` S5 gate EXISTS at `scripts/proof-live-session.mjs` (listed in package.json). It can be run against the current demo to baseline the crossfade-strand behavior before BC repin. · kf package.json pins glass-ui at `~4.0.0` (resolved to 4.0.1 installed). The BC re-pin step will bump this to `~<BC>.x`.

**Validation (RAN):** node scripts/proof-workaround-deletion.mjs 2>&1: S1=RED S2=RED S7=GREEN S8=PENDING S9=PENDING — exit 1. Verified S1 false-RED: `grep 'aria-orientation' /tmp/gu41/package/dist/tabs.js` finds unconditional binding at line containing 'I.value ? tablist : group'. Verified S2 dock content: `grep -c 'useDockClickIntegrity' node_modules/@mkbabb/glass-ui/dist/dock.js` → 1 (4.0.1 already has it). `ls scripts/proof-design-paint.mjs scripts/proof-content-visibility-gated.mjs scripts/proof-n-stage-boundary.

**Brainstorm:** S1 of O.W12 (gate hygiene) is IMMEDIATELY needed to fix the false-RED: replace the `version: '4.1.0'` sentinel for S1 with a content-probe checking for the cond · The S2 gate retarget for dock should also be implemented to confirm crossfade-strand cure: check `useDockClickIntegrity` IS in the installed dist (true at 4.0.1 · The chronicity discrepancy (O.W12 says 4, PROGRESS.md says 5) should be corrected in O.W12 body. DM-1 P-inv-28 at 5-tranche is a harder mandatory exit than the 

**→ Reformulation:** O.W12 is grounded and necessary, but AUGMENT: (1) Correct DM-1 chronicity from 4 to 5 (I,J,K,L,M→O) — the P-inv-28 belt is harder than stated. (2) Mark O.W2 content-probe retarget as BLOCKING PRIOR to any O.W12 impl — without it the gate is FALSE-RED for S1 and could trigger unsafe deletion. (3) Note that S2 (dock) may be deletable on 4.1.0 if proof:live-session S5 passes — the 'BC cut' for S2 may already have shipped. (4) The S3/S4 atomic commit protocol (delete both S1 and S2 workarounds + re-pin in one commit) remains sound but must be conditional: S1 ONLY after the SFC guard lands; S2 may fire first if S5 passes.

---

### [KEEP] O.W13 — proof:design-paint visual-truth gate

**Harden:** Gate is CONFIRMED ABSENT: `ls scripts/proof-design-paint.mjs` → no such file. Born-RED by construction is a real observable fact. · The 8-scene check matrix (home, cube, amiga, square, easing, spring, sequence, motion-path) uses transform-delta and color-sample observables — NOT getAnimations() (which is WAAPI-only and always empt · The glass-specular `::before` row is correctly RETIRED: `grep -rn 'glass-specular-track' demo/` returns zero hits (the selector no longer exists in the demo). The BC-gated S4 baseline lock rationale i · DEPENDENCY COHERENCE: O.W13 S1 (gate authoring) is NOW/kf-internal and does not wait for BC cut. Only S4 (baseline lock) is BC-gated. This split is correct and grounded. · S5 (Stage visual clause) is correctly conditional on O.W15 (N Stage unshelf). If O.W15 doesn't land before O.WZ, S5 is deferred — this is an explicit deferral path, not a gap. · The existing `demo-driver.mjs` substrate (`withPage` + `serveDist` + `navToScene`) IS present (`ls scripts/demo-driver.mjs` — verified by proof:demo-smoke and proof:live-session usage). No new browser

**Validation (RAN):** ls scripts/proof-design-paint.mjs → no such file (confirmed). grep -rn 'glass-specular-track' demo/ → no matches (RETIRED selector confirmed). grep -n 'navToScene\|withPage\|serveDist' scripts/demo-driver.mjs | head -5 — confirms infrastructure exists.

**Brainstorm:** The gate authoring (S1) can proceed NOW without BC dependency. Writing `scripts/proof-design-paint.mjs` as a kf-internal action in Band F pre-flight would surfa · The PRM transform-delta check should explicitly use two consecutive requestAnimationFrame callbacks read via `page.evaluate()` rather than the simpler `getCompu · Consider caching the per-scene check failures in a JSON artifact alongside the gate script so CI can diff against the baseline without re-running the full brows

**→ Reformulation:** O.W13 is grounded, well-specified, and correctly split (S1 NOW / S4 BC-gated). The gate script is genuinely absent; the 8-scene check matrix is not a proxy. The glass-specular retirement is correctly applied. KEEP as-is; S1 authoring is the first impl action in Band F pre-flight.

---

### [KEEP] O.W14 — Lighthouse posture flip + content-visibility gate + hero word-gap verify

**Harden:** Lighthouse posture breach CONFIRMED: `grep 'declarePosture' scripts/proof-lighthouse-mobile.mjs` → `declarePosture('observe-only', ...)` at line 71. CI runs without `KF_REQUIRE_LH=1`. A ceiling miss e · content-visibility gate CONFIRMED ABSENT: `ls scripts/proof-content-visibility*.mjs` → shell reports 'no matches found'. Born-RED by absence. · Hero word-gap source fix CONFIRMED PRESENT: `src: demo/@/components/custom/AnimatedText.vue:29-30` carries `marginInlineEnd: index < words.length - 1 ? '0.25em' : undefined`. The clause verification s · proof:consume-bundle CONFIRMED PRESENT (M.W15 §S3 born-RED claim is STALE): `ls scripts/proof-consume-bundle.mjs` → exists. O.W14 correctly does NOT re-author it. · SCENE_CEILINGS gap CONFIRMED: `grep 'sequence\|motion-path\|motion_path' scripts/proof-lighthouse-mobile.mjs` — O.W15 owns adding these floors, not O.W14. The scope boundary is correctly drawn. · The `ContentVisibilityAutoStateChangeEvent` interface precision (`.skipped` is top-level Event property, NOT `.detail.skipped`) is correct per the TypeScript DOM lib. The `useLivePreviewLOD.ts:259` co

**Validation (RAN):** grep -n 'declarePosture' scripts/proof-lighthouse-mobile.mjs → line 71: observe-only (confirmed). ls scripts/proof-content-visibility*.mjs → no matches found (zsh exit 1). cat demo/@/components/custom/AnimatedText.vue | grep -n 'marginInlineEnd' → line 29-30 present (confirmed). ls scripts/proof-consume-bundle.mjs → exists (confirmed).

**Brainstorm:** S2 (content-visibility gate) may have a CONDITIONAL deferral: `grep -rn 'content-visibility.*auto' demo/` should be run at authoring time to determine if any ac · The posture flip (S1) has a subtle CI timing risk: once flipped to 'hard', any ceiling miss blocks CI, including on a slow GH Actions runner. The recommendation · The hero word-gap verify (S4) is purely a run-it-on-the-dist step. It could be folded into the proof:demo-usability CI run rather than a manual step — just buil

**→ Reformulation:** O.W14 is grounded on three independently falsifiable breaches (posture breach confirmed, gate absent confirmed, word-gap verify gap confirmed). All three claims are real observables. The scope boundary with O.W15 (sequence/motion-path floors) is correct. KEEP as-is. Note for impl: check `grep -rn 'content-visibility.*auto' demo/` before authoring S2 to determine whether to defer to O.W15.

---

### [KEEP] O.W15 — N Stage scene-switcher unshelf (DM-24 + pointerdown-intercept + boundary gate)

**Harden:** n-stage-impl branch EXISTS and has 4 impl commits (`e2375b8` SHELVED, `8606eec`, `f14e943`, `b271fa1`) — confirmed via `git log --oneline n-stage-impl`. · Stale deps CONFIRMED: n-stage-impl has `@mkbabb/value.js: '^0.13.0'` (master: `'^1.0.2'`), `@mkbabb/parse-that: '^0.9.0'` (master: `'^0.11.0'`), `@mkbabb/glass-ui: '~4.0.0'` (same as master current pi · S7 flat-comma normalize workaround CONFIRMED on n-stage-impl: `git diff master n-stage-impl -- src/animation/utils.ts` shows the 9-line flat-comma normalize block present on n-stage-impl, DELETED on m · ChromeDock.vue CONFIRMED has `@update:model-value` trigger (the post-selection path): `git diff master n-stage-impl -- demo/app/ChromeDock.vue` (or equivalent) would show this; O.W15 specifies the mig · ALL 15 chartered N gates are ABSENT from package.json scripts: `ls scripts/proof-n-stage-boundary.mjs scripts/proof-stage-supersedes-dropdown.mjs` → both no such file. Zero N-gate scripts exist. · The `proof:n-stage-boundary` born-RED-of-record is correctly specified as the PLANTED-violation arm (post-rebase, with a planted `loadAnimationEngine` import in a stage composable → esbuild finds `eng · SceneStage.vue is CONFIRMED ABSENT on master/tranche-o-dev: `grep -rn 'SceneStage' demo/app/App.vue` → zero hits (confirmed by proof:stage-supersedes-dropdown C1 claim).

**Validation (RAN):** git log --oneline n-stage-impl | head -5 → 4 impl commits confirmed. git show n-stage-impl:package.json | python3 -c 'import json,sys; p=json.load(sys.stdin); print(p["dependencies"].get("@mkbabb/value.js"))' → ^0.13.0. git diff master n-stage-impl -- src/animation/utils.ts | grep 'normalized\|flat-comma' → shows S7 workaround present on n-stage-impl. ls scripts/proof-n-stage-boundary.mjs scripts/proof-stage-supersedes-dropdown.mjs → both no such file.

**Brainstorm:** The S2 gate authoring (`proof:n-stage-boundary`) is the one NOW-action in O.W15. Authoring this script on tranche-o-dev before any rebase is safe and valuable — · The rebase sequencing is critical: O.W12 BC re-pin MUST complete before the rebase base is set (the n-stage-impl tree would carry `~4.0.0` glass-ui while master · The `@pointerdown`-intercept `@click.capture` pattern for preventing the reka Select from opening simultaneously with the Stage is the established BLK-8 pattern

**→ Reformulation:** O.W15 is grounded, well-sequenced (BC cut GATED), and the born-RED of record (planted-violation arm on rebased tree) is correctly specified. The n-stage-impl branch state is accurately described. KEEP as-is. The gate authoring (S2) is the one immediately-authorizable action; all else opens on BC cut + O.W12.

---

### [RE-SCOPE] O.W16 — value.js-P consume: S8 FN_NAME + S9 parse-that delete + W96 boundary scan

**Harden:** VJ-L1 and VJ-L3 CONFIRMED ABSENT from value.js 1.0.2: `node -e "const v=require('@mkbabb/value.js'); console.log('flatLeaf:', 'flatLeaf' in v, 'parseCSSSubValue:', 'parseCSSSubValue' in v)"` → both fa · CRITICAL CONTRIVANCE: O.W16 gates S3 (delete FN_NAME Symbol) on VJ-L1 flatLeaf. But P CONTRIVANCE-AUDIT and P.md both DEMOTE VJ-L1 to spike: P.W11's realm-clean `WeakMap<ValueUnit, string>` is the ter · The W96 parse-that boundary scan (S1) is CONFIRMED ABSENT: `grep 'holdsParseThat\|parse-that' scripts/proof-boundary.mjs` returns only prose/comment lines, no assertion. The gap is real. S1 is the ONE · O.W7 engine-seam dependency on VJ-L1 is FALSIFIED by P.md: 'O.W7 is NOT VJ-L1-gated — the engine-seam split (1397L→~900L, lifting the playback machine into engine-playback.ts) is executable NOW on the · S4 (delete parse-that dep on VJ-L3) remains the binding gated action in O.W16. VJ-L3 is the surviving kf-unblock; the `parseCSSSubValue` ask is grounded and non-speculative. · The `proof:workaround-deletion` S8/S9 PENDING state is confirmed real (value.js 1.0.2 lacks both APIs). The P-inv-28 terminal window naming (kf-P next tranche) is correct for S9; for S8, P.W11's WeakM

**Validation (RAN):** node -e "const v=require('@mkbabb/value.js'); console.log('flatLeaf:', 'flatLeaf' in v, 'parseCSSSubValue:', 'parseCSSSubValue' in v)" → flatLeaf: false, parseCSSSubValue: false. node scripts/proof-workaround-deletion.mjs 2>&1 | grep 'S8\|S9' → S8=PENDING S9=PENDING. grep 'holdsParseThat\|assertParseThat' scripts/proof-boundary.mjs → no assertion hits (W96 confirmed absent). node scripts/proof-boundary.mjs 2>&1 | tail -5 → PASS (no parse-that check — W96 gap confirmed).

**Brainstorm:** RE-SCOPE S3: Move the FN_NAME Symbol → WeakMap swap OUT of O.W16's VJ-L1-gated band and into a NOW wave (O.W16-S3-standalone or folded into O.W9/O.W8). The Weak · The W96 boundary scan S1 should be authored NOW (kf-internal gate extension) as the first impl action of O.W16. This gate-first action is fully authorizable wit · For clarity, O.W16 should be re-structured as: S1 (W96 scan — NOW), S2 (re-pin on VJ-L3 publish — GATED), S3 (delete parse-that dep — GATED), with the FN_NAME W

**→ Reformulation:** O.W16 RE-SCOPED: (1) EXTRACT S3 (FN_NAME Symbol → WeakMap swap) out of the VJ-L1-gated band into a NOW wave — the WeakMap is realm-clean, kf-internal, no sibling dependency; this closes S8 in O without waiting for VJ-L1. (2) RETAIN S1 (W96 parse-that boundary scan — NOW, kf-internal) and S4/S2 (delete parse-that dep + re-pin — GATED on VJ-L3). (3) STRIKE the claim that O.W7 is unblocked by S3/VJ-L1 — O.W7 is independently executable NOW per P CONTRIVANCE-AUDIT. The residual O.W16 becomes: W96 scan (S1, NOW) + re-pin + delete direct parse-that dep (S2/S4, GATED on VJ-L3 publish). VJ-L1 is a DEMOTED-TO-SPIKE dispatch item per P.md, not a binding O.W16 dependency.

---

### [KEEP] O.WZ — Close + 5.0.0 cut + deploy round-trip oracle

**Harden:** proof:changelog-5.0.0 CONFIRMED ABSENT: `ls scripts/proof-changelog*.mjs` → no matches. release.yml:59-83 has no changelog gate. The four breaking-change annotations in source ARE confirmed present (` · Oscillator CONFIRMED NOT in npm@4.3.0 registry: extracted 4.3.0 tarball → `grep -c 'Oscillator' dist/keyframes.js` → 0 hits. Local `src/animation/index.ts:74-75` exports `Oscillator, waveformValue` —  · proof:chronic-closure substrate confirmed at `docs/tranches/L/PROGRESS.md` with `LEDGER_LABEL = 'K/PROGRESS.md'` (DO-3 stale text). The gate PASSES (K ledger is terminal) but the label is stale. O.WZ  · TWO deploy blockers confirmed: (1) `proof:control-point-live` exits non-zero today (DemoControlPoint absent — O.W5 not yet implemented). (2) `proof:keyframes-vue-published` clause (b) = E404. O.WZ cor · 5.0.0 is the correct MAJOR version (four type renames break the d.ts canonical names; multi-color refusal is a behavioral contract break; keyframes-vue is net-new). P.md confirms P cuts 5.1.x AFTER O  · PEER FLOOR UPDATE needed: `packages/keyframes-vue/package.json` has `peerDependencies['@mkbabb/keyframes.js'] = '>=4.3.0'` and `proof:keyframes-vue-published:63` has `PEER_FLOOR = '4.3.0'`. These must · The deploy round-trip oracle shape (CI run-id → deploy run-id → live `index-<hash>.js` equality) matches the J.WZ precedent (`4f1fc4c`). The `deploy-pages.yml` trigger condition (`conclusion == 'succe

**Validation (RAN):** ls scripts/proof-changelog-5.0.0.mjs → no such file. cd /tmp/kf43 && grep -c 'Oscillator' package/dist/keyframes.js → 0 (registry 4.3.0 confirmed). grep 'CHRONIC_LEDGER' scripts/proof-chronic-closure.mjs → docs/tranches/L/PROGRESS.md. grep 'LEDGER_LABEL' scripts/proof-chronic-closure.mjs → 'K/PROGRESS.md' (stale). node scripts/proof-keyframes-vue-published.mjs 2>&1 | tail -3 → FAIL (E404 confirmed). node scripts/proof-control-point-live.mjs 2>&1 | tail -3 → RED (GlassControlPoint absent).

**Brainstorm:** The proof:changelog-5.0.0 gate (S1) should be authored NOW as the first O.WZ impl action. It can be written against the live CHANGELOG.md immediately — authorin · The keyframes-vue peer floor bump (`4.3.0` → `5.0.0`) in two places (package.json + proof:keyframes-vue-published.mjs:63) should be done atomically as a pre-cut · The chronic-closure re-point (L/PROGRESS.md → O/PROGRESS.md) with non-vacuity protocol (three planted rows RED before clean ledger greens) is the final gate-fir · The 5.0.0 cut and BC consume ordering matters: P.md confirms 'do NOT bump the version prematurely before the BC consume is done.' The commit order is: Band F (B

**→ Reformulation:** O.WZ is grounded on all five born-RED claims (changelog gate absent, Oscillator not on registry, chronic substrate stale, two deploy blockers confirmed, version decision consistent). The USER-DOMAIN boundary (agent proposes criteria, owner fires tag) is correctly drawn. KEEP as-is with one implementation note: proof:changelog-5.0.0 (S1) and the keyframes-vue peer floor bump (S4 pre-action) are the only immediately-authorizable NOW actions in O.WZ; all others sequence after Band F + Band G completion.

---

## valuejs-P-asks

### [KEEP] VJ-L1 flatLeaf/fnName provenance field (DEMOTE-TO-SPIKE)

**Harden:** Premise CONFIRMED grounded: flattenObject drops FunctionValue.name; kf re-stamps Symbol("kf.fnName") after every clone() (utils.ts:45-64,289-294) because ValueUnit.clone() does not preserve it. The id · CONTRIVANCE SMELL-TEST: the demotion is CORRECT and self-aware. The doc explicitly states kf's P.W11 in-realm WeakMap<ValueUnit,string> is the TERMINAL S8 cure with ZERO value.js dependency — so VJ-L1 · GOOD discipline: the RADICAL 'canonical flat leaf' reshape is gated behind a FunctionValue.toString round-trip corpus precondition — prevents a rewrite-flavored re-architecture from sneaking in as a ' · Coherence note: doc Option A (7th positional ctor field) carries the V4 wart (clone() must pass undefined for targets to reach fnName); Option B (meta record) is cleaner. Leaving the choice to value.j

**Brainstorm:** If ever spiked: prefer the meta-record shape (Option B) — collapses the 6-positional-optional accretion the V2/V4 note already flagged, and gives a home for fut · kf-side: the P.W11 WeakMap can pre-populate fnName at flatten-call time in ONE pass rather than restamp-on-clone — a structural simplification independent of an · Add a one-line assertion in the kf identity-pad test that an ABSENT scale lerps from 1 (not 0) — locks the MCI-5 behavior the fnName carries, so the WeakMap swa

**→ Reformulation:** KEEP as DEMOTE-TO-SPIKE exactly as written. S8 terminal is kf-internal (P.W11 WeakMap, oracled by proof:no-foreign-symbol-stamp) — no value.js publish on the critical path. Re-open VJ-L1 as a value.js spike ONLY on a measured clone-restamp bottleneck; if re-opened, prefer the meta-record (Option B) shape over a 7th positional field.

---

### [KEEP] VJ-L3 parseCSSSubValue root helper (the surviving binding API ask, 1.1.0)

**Harden:** The V4 truncation premise is the load-bearing claim and it is REAL (probed below). parseCSSSubValue is genuinely ABSENT from value.js (grep + barrel-export probe both ZERO). kf today reaches into @mkb · CONTRIVANCE SMELL-TEST PASSES: this is NOT self-solvable in-realm. kf cannot eliminate the parse-that import without value.js's grammar door — there is no substitute for value.js's CSS sub-value parse · The FunctionArgs-FIRST mandate is correct and load-bearing: probe confirms parseCSSValue (the shipped helper) TRUNCATES multi-function input. A naive delegation to parseCSSValue would silently break k · Gate quality is strong: the W96 boundary parse-that-scan (born-RED today, kf utils.ts:1 is the live violator) + proof:workaround-deletion S9 apiPresent probe, AND the doc demands the gate assert the F

**Validation (RAN):** RAN: node /tmp/probe-vjl3.mjs (import value.js/dist/value.js). parseCSSSubValue => undefined (ABSENT, confirmed). parseCSSValue('scale(2) rotate(45deg)') => [{scaleX:[2]},{scaleY:[2]},{scaleZ:[2]}] — contains 'rotate'? FALSE, contains 'scale'? TRUE. CONFIRMS the V4 truncation: the shipped parseCSSValue drops the second function. kf's FunctionArgs-first composition is genuinely non-substitutable.

**Brainstorm:** The gate's REAL-observable should use BOTH a transform multi-fn ('scale(2) rotate(45deg)') AND a non-function multi-value ('translateX(10px) translateY(20px)')  · Suggest value.js name the helper's opts as { subProperty?: string } exactly mirroring kf's current setSubProperty(childKey) call (utils.ts:230) so the consume-e · kf could add a golden corpus test (transforms, nested calc, multi-arg gradients) comparing parseCSSSubValue output to the retained hand-rolled path BEFORE delet

**→ Reformulation:** KEEP as the surviving binding 1.1.0 API ask, validated. parseCSSSubValue(value, opts?) MUST internalize tryParse(any(CSSFunction.FunctionArgs, CSSValues.Value), value) with FunctionArgs FIRST (probe-confirmed: parseCSSValue truncates 'scale(2) rotate(45deg)' to scaleX/Y/Z only). The born-RED gate asserts the FULL ValueArray over a multi-function string; W96 scan asserts zero parse-that imports across src/animation/**.

---

### [ADOPT] VJ-P1 color2Into out-param (the gamut zero-alloc tail, 1.2.0 perf headline)

**Harden:** The ~84-alloc tail is REAL and MEASURED (proof ran below). The deferral is grounded at value.js/src/units/color/dispatch.ts:245 ('eliminating it requires a color2Into out-param (deferred, O.W5 scope)' · The scratch precedent EXISTS: value.js/src/units/color/matrix.ts:34 transformMat3Into(v, m, out) writes into a caller-owned out buffer, with an explicit aliasing-safety doc-comment ('out may alias nei · CONTRIVANCE SMELL-TEST PASSES: this is a MEASURED bottleneck (single largest GC win on the rAF wide-gamut egress), not tempting-but-wrong perf. The mechanism is half-built (tuples already exist; only  · The V1 aliasing guard is correctly flagged: the out-param must be a CALLER-OWNED scratch, never the source Color (aliasing would corrupt the bisection probe). matrix.ts's read-into-locals-first patter · Gate hygiene is correct per CONTRIVANCE-AUDIT MEASURE-FIRST: N_TARGET must be set to the MEASURED post-cure residual + margin (run proof-gamut-alloc.mjs over the BUILT color2Into branch), NOT a guesse

**Validation (RAN):** RAN: node /Users/mkbabb/Programming/value.js/scripts/proof-gamut-alloc.mjs => 5/5 PASS. C1-baseline: 84 allocs/call (witness N_BASELINE=104) on gamutMap(display-p3 OOG). C2-cured: 84 <= N_TARGET=90 (O.W3 already cut 104->84 by eliminating the 24 per-step OKLCHColor allocs). C2-jnd: 5 allocs (JND fast-path). C3-epsilon: bit-stable. CONFIRMS the ~84 residual is the per-step color2() Color-wrapper boxing — the exact target VJ-P1 names. transformMat3Into precedent confirmed at matrix.ts:34.

**Brainstorm:** Because gamut.ts already returns [r,g,b] tuples, color2Into could be implemented as a thin wrapper that calls the existing tuple path and writes r/g/b into out  · A module-scoped egress scratch (one per call-frame, single-threaded per dispatch.ts:226-228) is sufficient; no need for a scratch pool. Keep it to ONE reused Co · After the cure, set N_TARGET to the freshly-measured residual (likely <12 given C2-jnd already hits 5) + a 2-3 alloc margin; the gate then reds if a regression 

**→ Reformulation:** ADOPT as the 1.2.0 perf headline, MEASURE-validated. proof:gamut-alloc confirms 84 allocs/call on display-p3 egress (witness 104); the per-step color2(probe,target) at dispatch.ts:257 is the residual; transformMat3Into (matrix.ts:34) is the shipped scratch precedent to mirror. value.js adds color2Into(src, to, out) writing into a caller-owned (NOT source-aliased) module-scoped egress scratch; gamutMapToRgbSpace routes its 24-step loop through it; N_TARGET is re-baselined to the MEASURED post-cure residual + small margin via the existing proof-gamut-alloc.mjs.

---

### [KEEP] VJ-P3 narrow :any property/subProperty seam -> string (1.2.0 precept)

**Harden:** CONFIRMED grounded: value.js/src/units/index.ts:57 setSubProperty(subProperty: any), :61 setProperty(property: any), and the container mirrors at :174,178 (ValueArray) + :274,278 (FunctionValue) all t · CONTRIVANCE SMELL-TEST PASSES: trivially BC (callers conform), zero runtime change, pure strictness improvement on a strict:true library. No speculative surface. · Gate hygiene is CORRECT: the doc explicitly REFUSES a bespoke proof:any-seam gate and relies on the existing repo-wide strict tsc — re-widening to any (or a setSubProperty(42) callsite) surfaces as an · ADJACENT (noted by the dispatch, correctly NOT a kf deliverable): the FunctionValue ctor has an O(N^2) no-op CONFIRMED at index.ts:169-171 — values.forEach((v) => { this.setSubProperty(name); }) walks

**Validation (RAN):** Confirmed structural anchors only (no perf claim to bench — it is a type-strictness change): value.js/src/units/index.ts lines 57,61,174,178,274,278 all type params `any`; the FunctionValue ctor O(N^2) no-op confirmed at 169-171 (forEach body ignores v and calls this.setSubProperty(name) every iteration).

**Brainstorm:** Type as `string | undefined` to match the optional ctor fields (subProperty?, property?) rather than bare `string` — avoids forcing callers that legitimately pa · Bundle VJ-P3 with the index.ts:169-171 O(N^2) fix in value.js's W0 doc-wave since both touch the same setSubProperty surface — one coherent strictness+correctne · kf-side: no action needed; the kf strict tsc build will simply gain a type-checked seam on the next re-pin (^1.2.0). No kf delete, no kf gate.

**→ Reformulation:** KEEP as a 1.2.0 precept-correction. Type the four signatures (index.ts:57,61,174,178,274,278) as `string | undefined` (matching the optional ctor fields). NO bespoke gate — the existing repo-wide strict tsc is the oracle. Fold value.js's adjacent O(N^2) ctor no-op fix (index.ts:169-171 -> v.setSubProperty(name)) into the same W0 surface pass.

---

### [KEEP] VJ-CSS1 extractFunctions(ast)->Map<string,CustomFunctionDescriptor>

**Harden:** Gap CONFIRMED REAL: extractFunctions is ABSENT (grep ZERO). The premise holds — @function blocks ALREADY parse (CustomFunctionDescriptor type + parseFunctionParameters confirmed at stylesheet.ts:43-76 · CONTRIVANCE SMELL-TEST PASSES per the owner directive: proactively supporting emerging CSS (@function / CSS Functions and Mixins L1) in a grammar+engine library is the REMIT, not a speculative depende · Coherence: the kf consume (adapter.ts collects @function descriptors -> P.W13 inlines --foo(args)) depends on P.W13's resolve-values.ts which is CONFIRMED ABSENT today (not yet authored). VJ-CSS1 is N

**Validation (RAN):** Confirmed: grep extractFunctions in value.js/src ZERO; extractProperties exists at extract.ts:87 as the mirror; @function descriptor types/parser confirmed at stylesheet.ts:43-76,631-666. kf src/animation/resolve-values.ts ABSENT (P.W13 unauthored); springTimingFunction.ts/springLinearStops.ts confirmed present (the resolve precedent).

**Brainstorm:** extractFunctions should index by the @function NAME (the --foo identifier) into the CustomFunctionDescriptor exactly as extractProperties indexes @property — sa · Suggest value.js export it alongside extractProperties from the same barrel so kf's adapter.ts imports both from one door.

**→ Reformulation:** KEEP as a small grounded value.js-P additive (rides 1.1.0/1.2.0). extractFunctions(s: Stylesheet): Map<string, CustomFunctionDescriptor> mirroring extractProperties (extract.ts:87) — a ~10-line kind-filter walk over the already-parsed @function blocks (stylesheet.ts O.W4 S7). Exported from the same barrel. kf consumes in P.W13's (still-unauthored) resolve-values.ts; not on the NOW slice.

---

### [RE-SCOPE] VJ-CSS2 sibling-index() / sibling-count() parse arms

**Harden:** Gap CONFIRMED REAL: sibling-index / sibling-count have ZERO hits in value.js/src (grep empty). NOT-YET status accurate. · CONTRIVANCE SMELL-TEST: borderline-but-PASSES under the owner remit. sibling-index()/sibling-count() are standardized CSS (CSS Values L5 tree-counting functions), so parse support is in-remit for a gr · Lower-value than VJ-CSS1/CSS3: two FunctionValue parse arms emitting FunctionValue('sibling-index', []) is trivial, but the COMPILE-TIME resolution (sibling-index needs DOM tree position) is only mean

**Validation (RAN):** Confirmed: grep sibling-index/sibling-count/siblingIndex in value.js/src ZERO hits (gap is real). No live kf consumer found (resolve-values.ts absent; no test/demo references sibling-index).

**Brainstorm:** Defer until a concrete stagger-over-DOM-siblings demo exists to consume it — emitting an unconsumed FunctionValue parse arm is harmless but the resolution half  · If shipped, the parse arm should emit FunctionValue('sibling-index', []) / FunctionValue('sibling-count', []) (empty args) so kf's P.W13 resolves position at co

**→ Reformulation:** RE-SCOPE to PARSE-ARM-ONLY with a deferred resolve. value.js-P MAY add the two FunctionValue parse arms (emit FunctionValue('sibling-index'|'sibling-count', [])) under the emerging-CSS remit, gated by a round-trip parse test. But the kf compile-time INTEGER resolution stays DEFERRED until a live stagger-over-DOM-siblings consumer exists — do not author the resolve half against a hypothetical consumer. Lowest priority of the four VJ-CSS asks.

---

### [RE-SCOPE] VJ-CSS3 contrast-color(<color> vs <color>+) combinator

**Harden:** NAMING MISMATCH FOUND (load-bearing correction): the doc cites a 'grammar stub only (grammars/css-color.bbnf:95-101)' for contrast-color(), but the stub at those exact lines is `color-contrast()` (CSS · So the gap is BIGGER than the doc states: VJ-CSS3 is NOT 'finish a stub' — it is a NET-NEW grammar arm for contrast-color() PLUS a decision on whether the existing color-contrast() stub (which never s · CONTRIVANCE SMELL-TEST PASSES on the in-remit axis (a baseline color function in a color library is squarely the remit) BUT the 'highest-value' ranking rests on the April-2026-baseline claim which I c · The eager-eval-to-one-Color-via-WCAG-contrast design is sound (mirrors how color-mix resolves to one Color); kf inherits the resolved color — a clean consume with no kf resolve-pass needed for the sta

**Validation (RAN):** Confirmed: css-color.bbnf:95-101 is `colorContrast = "color-contrast" << "("...` (CSS Color L6, the OLD function), NOT contrast-color(). grep for contrast-color / contrastColor in value.js/src+grammars => ZERO. The new contrast-color() function is genuinely absent; the doc's anchor points at the wrong (legacy) function. The 'April 2026 baseline' claim is NOT verifiable in-session (no browser-support data source).

**Brainstorm:** Correct the dispatch anchor: contrast-color() (the new L7 function, syntax `contrast-color(<color>)` picking black/white for max contrast) is the ask; color-con · Eager-eval to one Color via WCAG-contrast is the right shape (the color-mix precedent), but verify the exact contrast-color() syntax against the shipped spec be · Confirm the 'newly baseline April 2026 / highest-value' priority with a real browser-support check (e.g. caniuse/MDN) before letting it outrank VJ-CSS1 — the va

**→ Reformulation:** RE-SCOPE with a NAMING correction. The dispatch anchor is wrong: css-color.bbnf:95-101 is `color-contrast()` (legacy CSS Color L6), NOT the new `contrast-color()` (L7) the ask wants — which is genuinely absent. value.js-P should: (1) add a NET-NEW contrast-color() FunctionValue arm eagerly evaluated to one Color via WCAG contrast (the color-mix pattern), verifying the actual shipped contrast-color() syntax (likely `contrast-color(<color>)`, not the `vs <color>+` list form the doc copied from color-contrast); (2) decide whether to retire the never-shipped color-contrast() stub. CONFIRM the 'April 2026 baseline / highest-value' claim via a real browser-support oracle before it outranks VJ-CSS1 — unverifiable in-session.

---

### [KEEP] VJ-CSS4 if() multi-branch — emit the FULL ordered clause list

**Harden:** Gap CONFIRMED REAL and PRECISELY as described: parsing/index.ts:337-340 computes `const clauses = splitIfClauses(body)` (the full ordered list) but then COLLAPSES it via `.find()` to first-consequent  · CONTRIVANCE SMELL-TEST PASSES strongly: the expensive work (splitIfClauses) is ALREADY done; the collapse is pure information-loss. This is the cheapest possible grounded betterment — no new parsing,  · Correctly scoped as a FOLLOW-UP: the common 2-branch if() ships NOW without this (the find-first path handles condition+consequent+else). VJ-CSS4 only unlocks N>2 branch chains, which kf P.W13 would r · Minor coherence: emitting the full clause list changes the FunctionValue('if', ...) arity/shape, so it is BC-additive ONLY if the existing 2-branch consumers tolerate a richer args array — value.js-P 

**Validation (RAN):** Confirmed at value.js/src/parsing/index.ts:337-345: `const clauses = splitIfClauses(body)` is computed, then `clauses.find((c) => c.condition !== null)` (consequent) + `clauses.find((c) => c.condition === null)` (else) collapse it to a 3-element FunctionValue('if', [conditionStr, valueStr, elseStr]). Multi-branch ordering is discarded. Gap is real and the fix is a localized clause-list emit.

**Brainstorm:** Emit the clauses as ordered [cond1, val1, cond2, val2, ..., else] pairs (or a nested structure) so kf P.W13 can resolve N-branch if() by evaluating conditions i · Guard BC: keep the FunctionValue shape backward-tolerant (e.g. the first three args stay [firstCond, firstVal, firstElse] so existing 2-branch consumers are una · Add a round-trip test over a 3-branch if() (if(cond1: a; cond2: b; else: c)) asserting all three clauses survive — born-RED today (the find-first drops cond2).

**→ Reformulation:** KEEP as a follow-up (rides 1.1.0/1.2.0). value.js-P emits the already-computed splitIfClauses ordered list into the FunctionValue('if', ...) instead of the find-first collapse (index.ts:337-345), preserving N-branch ordering for kf P.W13 to resolve. Keep BC: append extra clauses so existing 2-branch consumers (which ship NOW without this) see an unchanged head. Add a 3-branch round-trip test (born-RED: find-first drops the 2nd condition today). Not blocking the P.W13 NOW slice.

---

## parsethat-B-asks

### [ADOPT] PT-B1 — packrat cross-input pollution FIX (the BLOCKER): memoize().parse(A) then SAME parser .parse(B) returns A stale; getCijKey lacks src + 4096-ID int32-overflow aliasing. Cure: src-identity auto-reset in memoizeFn + float64-safe multiply-key (id*1048576+offset). Born-RED gate proof:packrat-cross-input.

**Harden:** GATE FALSIFIABILITY: STRONG. The born-RED gate asserts a real observable that is RED today — reproduced both clauses at runtime (see validation). The dispatch correctly specifies REMOVING beforeEach(r · ROOT CAUSE confirmed exactly as specced: getCijKey (packrat.ts:55-56) = (parser.id << 20) | (offset & MAX) — purely (id,offset), no src. MEMO (packrat.ts:90) is module-global, cleared ONLY by resetPac · SEVERITY confirmed: this is a SILENT-WRONG-ANSWER, not a crash. Any hot re-parse session (value.js parse LRU, kf editing-session re-parse) that reuses a memoized parser across inputs without manual re · NOT A CONTRIVANCE: this is a genuine correctness bug in the DAG-root, self-contained in parse-that's own tree, no speculative external dependency. The cure is a one-===-per-call src guard + a free mul · ARM CHOICE: arm (a) src-identity auto-reset is correct and cheap (one === per memoizeFn). Arm (b) WeakRef-epoch adds FinalizationRegistry async-timing complexity for no measured benefit — recommend pa · 4096-ID note: latent not live — a freshly-constructed parser after a full test run had id=44 (probe). The constellation stays far under 4096. The multiply-key is still worth stacking (free, closes the

**Validation (RAN):** RAN (vitest probe over parse-that/typescript, NO beforeEach(resetPackrat), removed after): cmd `NODE_OPTIONS='--max-old-space-size=4096' npx vitest run test/_probe_ptb1.test.ts`. Results: memoize(regex(/[a-z]+/)).parse('hello')='hello' then same parser .parse('world')='hello' (STALE — bug CONFIRMED). Structured: memoize(all(word,'-',num)).parse('abc-123')=['abc','-','123'] then .parse('xyz-789')=['abc','-','123'] (STALE). Key: getCijKey(0,0)=0, getCijKey(4096,0)=0 (COLLIDES); multiply-key safe(0

**Brainstorm:** Fold the allStrict() undefined-preserving variant (the value.js drop-undefined footgun, index.ts:185-196) into the same wave — value.js currently routes around  · The gate should additionally assert the POSITIVE: after the fix, parse(A) then parse(B) returns B's correct result AND a SECOND parse(A) still hits the (now src · Cheaper than a per-call CURRENT_SRC compare: gate the auto-reset on the TOP-LEVEL parse entry (parseState) rather than every memoizeFn call — a parse() knows it

**→ Reformulation:** ADOPT PT-B1 as the campaign's correctness BLOCKER, lands FIRST (parse-that B.W0/W1). Cure = arm (a) src-identity auto-reset (recommend resetting at the parseState ENTRY boundary, not per-memoizeFn-call, for zero per-node cost) + the free multiply-key (id*1048576+offset). Stack the try/finally hardening and the allStrict() undefined-preserving variant so value.js can retire its drop-undefined workaround. Born-RED gate proof:packrat-cross-input must drop beforeEach(resetPackrat) and assert BOTH the stale-clause (witnessed RED) and the post-fix positive (B correct + A re-memoizes). DISPATCH only (inv-16: kf authors the ask, parse-that B implements).

---

### [RE-SCOPE] PT-B2 — Span-combinator DEDUP: collapse the ~400 lines of 10 copy-pasted *Span backtracking builders (span.ts:16-360) into ONE collector-parametric combinator vocabulary. Gate proof:span-combinator-parity (byte-identical output + within 5% throughput, deopt reds the gate).

**Harden:** DUPLICATION confirmed: manySpan (span.ts:85-120) and the value-lane many (parser.ts:501-539) ARE near-identical control flow — same save/restore/count/mergeError plumbing, only the collector differs ( · CONTRIVANCE SMELL — NO LIVE CONSUMER: the *Span builders have ZERO production consumers. value.js: grep empty. kf: grep empty. parse-that's own json/csv parsers (parsers/json.ts, parsers/csv.ts): use  · Therefore the DEDUP de-risks ~400 lines of a lane NOBODY exercises in production. The 'a bug in many's offset-restore is fixed once not twice' payoff is real but applies to a lane with no live bug sur · DEOPT RISK is real and lands on dead weight: the gate explicitly guards a generic-collector deopt (the zero-alloc property that justifies the Span lane). Spending engineering on a parametric factory + · The gate proof:span-combinator-parity is well-FORMED (byte-identical + 5% throughput, born-RED via a naive Array.map collector) — but it gates the CORRECTNESS of an optional refactor, not a product-tr

**Validation (RAN):** RAN (grep over all three trees): `grep -rn 'manySpan|sepBySpan|regexSpan|wrapSpan|altSpan|takeUntilAnySpan|stringSpan|optSpan|skipSpan|nextSpan' value.js/src kf/src parse-that/src/parse/parsers` → ZERO production hits. value.js consumes parse-that via all/any/regex/string/dispatch (scroll-timeline.ts:1, math.ts:16, etc.) — never a *Span builder. parse-that json.ts:3-5 uses dispatch()+regex. Only test/surface-gate references remain.

**Brainstorm:** RE-SCOPE to a lighter target: since the *Span lane has no live consumer, the highest-value move is NOT a parametric factory but to ASK whether the *Span public  · If parse-that DOES keep the lane (json/csv future use), defer the dedup to a future tranche and make it OPPORTUNISTIC — fix bugs in many's restore in BOTH place · If parse-that does the dedup, the gate should ALSO assert the published *Span surface is byte-identical (the 15 export names at index.ts:10 unchanged) so the BC

**→ Reformulation:** RE-SCOPE PT-B2 from 'do the parametric-factory DEDUP now' to 'first establish whether the *Span runtime lane has any future consumer.' MEASURED FINDING: the 15 *Span builders have ZERO production consumers (value.js/kf/json/csv all use dispatch+regex). The dedup de-risks ~400 lines of an unexercised lane and lands deopt risk on dead weight. Recommend parse-that B EITHER (a) defer the factory refactor as opportunistic (fix many-restore bugs in both copies when touched) OR (b) raise a P-inv-28 disposition on the *Span public surface itself (deprecate if no consumer will ever materialize). Do NOT spend a parametric-factory + 5% perf gate now to protect a zero-alloc property of a zero-consumer lane. DISPATCH only — parse-that B owns the call.

---

### [ADOPT] PT-B3 — perf frontier: combinator FUSION (fuse a.then(b).map(f)/all(a,b,c)/any(a,b,c) into one monomorphic closure, zero intermediate tuples) + 2-char dispatch widening for residual 3-4-deep buckets + add `bench` script + proof:perf regression gate over checked-in baseline. Gate proof:perf (zero-alloc fuse + ≥40% widened-dispatch + 15% json regression reds CI).

**Harden:** ALLOC claims TRUE at source: all() (leaf.ts:107-136) allocates a fresh matches:unknown[] per call (line 113) then state.ok(matches); then() (parser.ts:63-78) allocates [value1, state.value] per succes · DISPATCH WIDENING has a REAL consumer: value.js index.ts:425 Function_ already routes by first-char dispatch(), and bucketC = any(fnMath, fnGradient, fnCubicBezier, fnGeneric) (index.ts:416) is the EX · 2-CHAR WIDENING is PARTIAL not total: probed the c-bucket — calc=ca, clamp=cl, cos=co, conic=co, cubic=cu → the `co` bucket STILL collides (cos vs conic). So the 16-bit discriminator reduces the c-buc · IDENTICAL-RESULT is the load-bearing risk: the fusion MUST preserve all()'s drop-undefined semantics (value.js routes around it at index.ts:185-196 with explicit branches — a real footgun), then()'s o · proof:perf GAP confirmed: package.json scripts = test/proof:manifest/proof:no-css-surface/proof:subpath — NO `bench`, NO `proof:perf`. Bench substrate EXISTS (test/benchmarks/json-comprehensive.bench. · NOT A CONTRIVANCE: proactively optimizing a grammar+engine library's combinator core with a CI-enforced regression floor IS the remit. The win is measured (2.8x any() worst-vs-best, real value.js buck

**Validation (RAN):** RAN (vitest probes over parse-that, removed after): all() `npx vitest run` → 7.0MB heap delta / 2M calls + source-confirmed fresh matches[] per call; all() 56.7 ns/op. then() 40.3 ns/op (tuple per call). any() 4-deep c-bucket: worst-case (conic, 4th) 60.8 ns/op vs best-case (calc, 1st) 21.9 ns/op = 2.8x penalty. 2-char buckets probe: {ca:[calc], cl:[clamp], co:[cos,conic], cu:[cubic]} — `co` residual confirmed. Ideal full-flatten 4-deep→1-deep = 3.23x. package.json: NO bench/proof:perf (confirme

**Brainstorm:** Sequence proof:perf (the WIRING + baseline JSON) FIRST within PT-B3, before the fusion — the regression floor must exist to prove the fusion is a win and not a  · Scope the gate's ≥40% clause to the 2-char-DISAMBIGUATED tokens (ca/cl/cu — the ones the 2nd byte fully resolves) and treat the `co` residual honestly as a 2-de · Pair the all() fusion with allStrict() (undefined-preserving) so value.js can delete its index.ts:185-196 drop-undefined workaround — turns a footgun-preservati · The fusion should target the STATIC-chain shape only (all(a,b,c) where a,b,c are construction-time constants) — value.js's 59 all() sites are mostly static; a d

**→ Reformulation:** ADOPT PT-B3 with one scope correction. The alloc/dispatch wins are MEASURED on a real value.js consumer (bucketC 4-deep any(), 59 all() sites, 2.8x any() worst-vs-best, all()/then() per-call allocs). Reformulate as three sub-deliverables in order: (1) wire `bench: vitest bench` + proof:perf over a checked-in json-comprehensive baseline (pure wiring — substrate exists); (2) the 2-char dispatch widening, gated on the 2nd-byte-DISAMBIGUATED tokens only (ca/cl/cu fully flatten; `co`=cos+conic is honestly a 2-deep residual — gate must not over-claim a full flatten); (3) the static-chain combinator fusion (construction-time, IDENTICAL-RESULT via a fuzz-equivalence corpus), paired with allStrict() so value.js can delete its drop-undefined workaround. DISPATCH only — parse-that B owns the fusion's backtracking-preservation.

---

### [KILL] PT-B4 — SpanParser KILL (P-inv-28 resolves to KILL): delete the dormant SpanParser introspection tier (span.ts:540-902 — SpanParserKind, the SpanParser tagged-union, callSpan(), the *Node builders, spanParserToParser) + the span-dispatch.bench.ts A.W3 record; preserve the A.W3 falsification as a docs paragraph in future-research.md §7. Gate proof:span-parser-killed.

**Harden:** NO CONSUMER confirmed across all three trees: grep for callSpan / SpanParserKind / spanParserToParser / SpanParser tagged-union / *Node builders (stringSpanNode, manySpanNode, altSpanNode, ...) → ZERO · RATIONALE confirmed dead: span.ts:555-573 records the tier was retained 'expressly as the codegen foundation'; A.W3 (span.ts:558-564) measured callSpan's runtime switch ~10-14% SLOWER than the closure · GATE FALSIFIABILITY: STRONG and born-RED-today. The tier + span-dispatch.bench.ts both EXIST on the current tree (confirmed: span.ts is 902 lines; the tagged-union spans 580-672; callSpan 684-902; spa · SCOPE PRECISION verified: the KILL targets ONLY the dormant introspection tier (span.ts:540-902). The live *Span builders (span.ts:16-360) are a SEPARATE concern (PT-B2) — confirmed by reading both ra · NOT A CONTRIVANCE — this is the ANTI-contrivance move: removing a parked no-consumer tier held for an external prototype (the exact P-inv-28 hazard). The A.W3 falsification (runtime dispatch is slower

**Validation (RAN):** RAN (grep over all three trees): `grep -rn 'callSpan|SpanParserKind|spanParserToParser|SpanParser\b|stringSpanNode|manySpanNode|altSpanNode' parse-that/src value.js/src kf/src` → ZERO consumers outside span.ts; the only parse-that hits are NOT-re-exported comments (index.ts:11, core.ts:6). Confirmed span.ts is 902 lines with the tier at 540-902 (SpanParserKind at 580, tagged-union type 599-610, *Node builders 614-674, callSpan 684-892, spanParserToParser 893). span-dispatch.bench.ts present in t

**Brainstorm:** FOLD into PT-B2's housekeeping (the dispatch explicitly allows this) — both touch span.ts and both are no-legacy cleanups; one PR keeps the span.ts surgery atom · The gate should also assert the parserNames tuple in state.ts (lines 141-182) is pruned of any SpanParser-only names if present — verify no orphan enum/name lea · Make the A.W3 docs paragraph in future-research.md §7 cite the THREE workloads + the 10-14% number explicitly, so the falsification survives as a reproducible r

**→ Reformulation:** KILL PT-B4 as specified — the SpanParser introspection tier (span.ts:540-902: SpanParserKind, the tagged-union, the *Node builders, callSpan, spanParserToParser) + span-dispatch.bench.ts have ZERO consumers in parse-that/value.js/kf (measured), are NOT re-exported (index.ts:11 comment), and their only rationale (codegen foundation) moved to a separate bbnf-lang session out of scope. P-inv-28 resolves to KILL. Preserve ONLY the A.W3 falsification as a future-research.md §7 paragraph citing the 3 workloads + the ~10-14% V8 slowdown. RECOMMEND folding into PT-B2's housekeeping (one atomic span.ts diff). Verify the parserNames tuple is pruned of any SpanParser-only orphans. DISPATCH only — parse-that B owns the deletion.

---

