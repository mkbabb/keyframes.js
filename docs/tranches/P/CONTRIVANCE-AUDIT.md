# Tranche P — the CONTRIVANCE AUDIT ledger + the smell-test

> A conservative, fastidious audit (9 lanes, owner-requested 2026-06-22 after the codegen-spine retraction) of the recent value.js + keyframes.js tranche specs. It hunts items in the codegen-spine vein and reformulates each toward the smallest grounded change. DOCS-ONLY.

## OWNER CORRECTIONS (2026-06-22) — applied OVER the lane verdicts below

Two owner directives override specific lane verdicts. The raw lane findings are preserved below as evidence; these corrections take precedence.

1. **VALIDATE, don't abrogate — and the SoA verdict FLIPPED to ADOPT.** The "demote-to-spike" items are to be PROTOTYPED + MEASURED, not dropped on paper. The headline (SoA compositor) was tested: `bench/group-soa-validate.mjs` → `scripts/group-soa-decision.json` measured the SoA fold on `transformFramesGrouped`'s ACTUAL leaf shape and returned **ADOPT** — weighted **3.66×** / add **3.69×**, **bit-identical** output. The audit was right that the *justification* was contrived (the 3.86× was transplanted); the *idea* is sound on the real path. P.W2 is now ADOPT-scoped to the add/weighted arms (default `replace` untouched). Every other `MEASURE-FIRST`/`DEMOTE-TO-SPIKE` item gets the same treatment: run the bench, let the number decide — `KILL` only on a measured loss.

2. **Novel CSS features BELONG in our grammar — `if()` / `@function` / `spring()` are GROUNDED, not speculative.** The audit (lane `valuejs-tranches`) flagged value.js's O.W4/O.W5 support for emerging CSS (`if()`, `@function`, `sibling-index()`, `spring()`) as a SPECULATIVE DEPENDENCY because no browser ships them. **That smell-test clause does NOT apply to a grammar+engine library whose REMIT is to lead the platform.** value.js is the canonical CSS grammar; supporting standardized + emerging CSS is its purpose, and the keyframes engine can RESOLVE/ANIMATE these (`if()` → branch resolution; `@function` → lowering/inlining; `spring()` → kf already owns spring easing via `SpringProgress`/`springTimingFunction`) **today, before browsers catch up.** Those O.W4/O.W5 items are RE-INSTATED as grounded forward-looking grammar+engine support (dispatched to value.js to support proactively; kf engine to resolve). The "SPECULATIVE DEPENDENCY" smell is narrowed accordingly (see the refined clause in the smell-test below).

## The contrivance skeleton

A speculative-payoff perf/arch claim, justified by a TRANSPLANTED or ASSERTED number, attached to a NON-DEFAULT or NOT-YET-REAL path, carrying NEW abstraction or DUAL maintenance, chartered BEFORE the bottleneck was measured born-RED on the wave’s OWN target path on the SAME report.

**Refined SPECULATIVE-DEPENDENCY clause (per owner correction #2):** the smell is depending on an external PROTOTYPE (bbnf-lang) or a not-yet-shipped SIBLING API for the consumer's OWN fix (e.g. VJ-L1 to unblock O.W7 when kf can self-solve). It is NOT proactively supporting standardized/emerging CSS in a grammar+engine library — leading the platform is the library's purpose, not a contrivance.

## The reformulation ledger

| item | location | severity | disposition | reformulation |
|---|---|---|---|---|
| The SoA Float64Array compositor rewrite of the three blend arms — chartered on the 3.86x NumericAnimation/Spri | P.W2 S1-S2 (docs/tranches/P/waves/P.W2.md:56-86); cure  | CLEAR | **DEMOTE-TO-SPIKE** | DEMOTE-TO-SPIKE. Author bench/group-composite.bench.ts (P.W2 S3) FIRST as a born-RED measurement of the CURRENT boxed add/weighted blend vs a prototype SoA fold. Charter the engine |
| The framing that 'the AnimationGroup compositor is the multi-animation hot path and it is a megamorphic boxed  | P.W2.md:8,14-16,58 (the headline framing); cross-check  | CLEAR | **RE-SCOPE** | RE-SCOPE. If the spike (above) shows add/weighted is a real workload bottleneck, scope the SoA fold to those two arms ONLY and state plainly that the default replace path is alread |
| The Typed-OM (StylePropertyMap.set) write path with a permanent setProperty feature-detect fallback. | P.W3 S1 (docs/tranches/P/waves/P.W3.md:48-51, 88-110);  | MODERATE | **DEMOTE-TO-SPIKE** | DEMOTE-TO-SPIKE. The write-cost-ratio Playwright gate (S5) is the right instrument — run it FIRST as a measurement over the multi-property case. Ship the Typed-OM path ONLY on a KI |
| The Playhead value-object (the K2 driver-protocol formalization) with delegating accessors over _playhead. | P.W3 S4 (docs/tranches/P/waves/P.W3.md:6,76-84,112-116) | CLEAR | **DROP** | DROP. The grounded goal (shrink engine.ts, decouple the playback machine) is O.W7's file-split, which has not even landed. Let O.W7 do the lift; do not deepen it into a value-objec |
| The computed-unit composite-numeric-key cache refinement (targetId * EPOCH_SCALE + epoch, one integer compare  | P.W2 S4 (docs/tranches/P/waves/P.W2.md:26-28,105-116) | MODERATE | **MEASURE-FIRST** | MEASURE-FIRST, then likely DROP. Keep ONLY the kf-side observe-only bench arm (the calc() leaf steady-window already exists in bench/interp-buffer.bench.ts). Do not graduate it to  |
| The alloc-count heap-delta arm asserting zero per-frame Float64Array alloc as a co-keystone of proof:soa-compo | P.W2 S3 (docs/tranches/P/waves/P.W2.md:97,131) | BORDERLINE | **SIMPLIFY** | SIMPLIFY. Fold the zero-per-frame-alloc check into the existing proof:zero-alloc / proof:standalone-zero-alloc heap-delta harness (package.json:61,63) rather than authoring a bespo |
| spring parameter-space heatmap — ~400 SpringProgress instances at mount | P.W6/S3 — demo/spring/SpringHeatmap.vue (new), P.W6.md: | MODERATE | **MEASURE-FIRST** | MEASURE-FIRST: build a 20×20 canvas grid (400 cells still, but using the raw closed-form spring settle-time formula rather than 400 live SpringProgress instances) and bench it agai |
| springLinearStopsArray() new LIGHT API addition | P.W6/S2 — src/animation/springLinearStops.ts + src/anim | MODERATE | **SIMPLIFY** | SIMPLIFY: apply the in-place anchor-before-fill fix directly in SpringTrace.vue (move the anchor lines before the null-pct fill loop). Drop the springLinearStopsArray() API additio |
| grammar-fuzz harness (P.W9 S3) with planted round()-omit regression gated on value.js P | P.W9/S3 — test/grammar-fuzz.test.ts (new), scripts/proo | MODERATE | **RE-SCOPE** | RE-SCOPE: author the grammar-fuzz harness with the colorArb and keyframeStopArb arms that are fully functional on today's value.js 1.0.2 tree. Gate the round() arm behind a SKIP an |
| unify-all-demo-drag-handles-onto-drag2D refactor (recorded in P.W5/P.W7 as a BOOK) | P.W5.md:172, P.W7/S5 — docs/tranches/P/PROGRESS.md (fut | BORDERLINE | **SIMPLIFY** | SIMPLIFY: record as a BOOK note without the named-gate obligation. Drop the 'proof:drag-primitive-unified' aspirational gate name. If a future wave independently needs the consolid |
| Houdini Paint-Worklet `--lit` relight transposition (documented as progressive-enhancement EXCLUSION) | P.W5.md:29-30,170 — cube Houdini paint-worklet transpos | CLEAR | **KEEP** | KEEP as excluded — the spec correctly documents it as a progressive-enhancement EXCLUSION with no Baseline floor, never required. |
| O.W7 gates the engine-seam split on VJ-L1 flatLeaf | Band D / O.W7.md:4-5,59-70; O.md:97,113 | MODERATE | **SIMPLIFY** | Remove the VJ-L1 gate from O.W7. The engine-seam split (1397L→~900L, lifting concern 3 into engine-playback.ts) is executable NOW on the current tree — the same phase as O.W8/W9. T |
| O.W1 custom eslint-plugin-kf encoding 33+ grep gates as eslint rules | Band A / O.W1.md:83-87 (S4); M.W2 §S4 (referenced spec) | BORDERLINE | **SIMPLIFY** | Split O.W1 into two concrete halves: (a) GROUNDED NOW — install eslint + dep-cruiser, author eslint.config.mjs with max-lines per-file overrides (S2) and import/no-restricted-paths |
| VJ-P2 — typed Float64 channel view (optional packed _ch: Float64Array, lazily materialized, to 'kill string-ke | Tranche P / 1.2.0 perf trio — docs/tranches/P/KF-TO-VAL | CLEAR | **DROP** | DROP the new Color._ch view ask. If a residual read is suspected, the in-realm move is NOT a public Color shape addition — it is widening the existing interpolate.ts buildColorChan |
| VJ-P1 color2Into N_TARGET tightening 90 → 15 (gamut allocs/call ≤ 15, 'born-RED on today's ~84') | Tranche P / VJ-P1 — docs/tranches/P/KF-TO-VALUEJS-P.md: | BORDERLINE | **MEASURE-FIRST** | KEEP color2Into; but set N_TARGET to the MEASURED post-cure residual + a small margin, captured by running the existing proof-gamut-alloc.mjs instrumentation on the color2Into bran |
| VJ-L1 framed as the O.W7 engine-split UNBLOCKER (and as the chronicity-4 belt exit) requiring a new value.js c | Tranche P inherit — docs/tranches/P/KF-TO-VALUEJS-P.md: | MODERATE | **RE-SCOPE** | RE-SCOPE VJ-L1 in the dispatch to its TRUE residual payoff: 'retire the clone-restamp ceremony' (clone() preserves fnName), explicitly NOT the O.W7 unblock (P.W11's in-realm WeakMa |
| VJ-P3 — narrow setSubProperty/setProperty :any → string (the strict-mode precept correction) with a dedicated  | Tranche P / VJ-P3 — docs/tranches/P/KF-TO-VALUEJS-P.md: | BORDERLINE | **SIMPLIFY** | KEEP the :any→string narrowing (it is grounded and trivial). SIMPLIFY the verification: rely on the existing repo-wide tsc strict build to catch regressions rather than standing up |
| VJ-P.W0 adjacent one-liner folded into the dispatch: fix the O(N²) setSubProperty no-op in the FunctionValue c | docs/tranches/P/KF-TO-VALUEJS-P.md:82-87 | BORDERLINE | **RE-SCOPE** | Drop it from the binding ASK roster; leave it as a one-line INFORM note (value.js's own ledger), which is how the doc already half-frames it. Do not give it a gate or a wave slot i |
| O.W4 comprehensive grammar: EXPERIMENTAL clauses (if(), sibling-index()/sibling-count(), contrast-color(), @fu | O.W4 / /Users/mkbabb/Programming/value.js/docs/tranches | MODERATE | **RE-SCOPE** | Split O.W4 into O.W4-core (S1–S4, S8: the confirmed grounded items—recursive at-rules, CSS nesting, color-mix/light-dark round-trips, @property extensions) and O.W4-experimental (S |
| O.W5.S3 spring() moderate-supersede parser addition | O.W5 / /Users/mkbabb/Programming/value.js/docs/tranches | MODERATE | **DROP** | Drop S3 from O.W5. The round-trip idempotence harness (S4) does not require spring() in its corpus—remove that corpus entry. If a consumer (beyond kf-internal) requires value.js to |
| O.W5.D6 colorSpace annotation — per-value interpolation space hint on Color ValueUnit nodes | O.W5 / /Users/mkbabb/Programming/value.js/docs/tranches | BORDERLINE | **DROP** | Drop the colorSpace annotation from O.W5. If kf's interpolation engine later needs per-value space hints, that can be a co-authored ask (kf files it as a VJ-ask, value.js responds) |
| O.W5.D6 @function lowering utility: lowerCustomFunction(stylesheet, name, args) | O.W5 / /Users/mkbabb/Programming/value.js/docs/tranches | MODERATE | **DROP** | Drop lowerCustomFunction from O.W5 scope. If @function ships in a stable browser baseline and kf or another consumer needs resolution, add lowerCustomFunction at that point. The ro |
| O.W6.S2 Harvest the monolithic byte-loop scanner technique from parse-that's CSS parser + O.W6.S3 SpanParser d | O.W6 / /Users/mkbabb/Programming/value.js/docs/tranches | MODERATE | **DEMOTE-TO-SPIKE** | Demote O.W6.S2 and O.W6.S3 to DEMOTE-TO-SPIKE. Keep only O.W6.S1 (the born-RED bench that establishes baseline MB/s) and O.W6.S5 (the proof:perf-target gate). Require: the S1 bench |
| O.W6.S4 gamutMapToRgbSpace bisection numeric unrolling (directOklchToRgb inlining into the bisection loop body | O.W6 / /Users/mkbabb/Programming/value.js/docs/tranches | BORDERLINE | **MEASURE-FIRST** | Keep S4 as a documented conditional (its own conditional framing is correct) but remove it from the S-clause list and from the gate target. Record it as: 'If the O.W3 bench shows g |
| SpringProgress.setTargets(Float64Array) vector-sugar API on the public LIGHT barrel | L.W7 S2-constraint / M (spring-vector-decision.json shi | MODERATE | **DEMOTE-TO-SPIKE** | DEMOTE-TO-SPIKE: retain the bench + decision JSON as evidence, but remove setTargets/tickVector/values from the public API surface until a concrete consumer (demo or downstream) ex |
| drag2D single-call sugar (drag-2d.ts, Drag2DHandle) — no live consumer | L.W5 S4 / src/animation/drag-2d.ts:59 (drag2D) + src/an | MODERATE | **RE-SCOPE** | RE-SCOPE: keep the two-Draggable composition pattern in the documentation/README as a code snippet, but do not export drag2D/Drag2DHandle from the barrel. If a real consumer arises |
| Tranche N Stage scene-switcher — 9-wave spec, bespoke 7-component architecture, 7 idle-preview units, per-scen | Tranche N (docs/tranches/N/N.md, STAGE-SPEC.md, waves/N | CLEAR | **SIMPLIFY** | RE-SCOPE: when BC dock cut lands and DM-24 fires, build a simpler scene-switcher: enhance the existing dock Select with a SpringProgress transition and a view-transition morph usin |
| L.W7 S4 / M.W12 S4: scheduler.postTask priority bands 'background' adoption for warmEngine — gated on a real-b | L.W7 S4 / M.W12 S4 (docs/tranches/M/waves/M.W12.md:278- | BORDERLINE | **MEASURE-FIRST** | MEASURE-FIRST: before adding the postTask branch or authoring the real-browser INP arm, measure whether warmEngine's dynamic import actually causes INP issues in production (LoAF t |
| The contrivance SKELETON itself (the durable pattern the 5 instances share) | docs/tranches/P/waves/P.W2.md, P.W3.md; docs/tranches/P | CLEAR | **KEEP** | Adopt a CONSERVATIVE 7-question pre-charter smell-test that every future wave spec answers IN ITS HEADER before chartering: (1) MEASURED-BOTTLENECK? Is there a born-RED bench showi |
| The transplanted-ratio failure mode (the 3.86x reused across paths) — the smell-test's load-bearing question | scripts/spring-vector-decision.json:5-7 (vectorHz/scala | CLEAR | **KEEP** | The smell-test gate's HARD clause: a perf wave's born-RED bench baseline path MUST be the wave's own target function (asserted by name in the wave header), and the decision-JSON $c |
| Born-RED `proof:wave-charter` gate (the durable enforcing artifact) | NEW — scripts/proof-wave-charter.mjs; wired into proof: | MODERATE | **KEEP** | Author `proof:wave-charter` as a docs-and-presence gate (KISS — no engine surface). It bites three real regressions: (1) a [radical] wave chartered with no smell-test answers; (2)  |
| Wave-spec template clause: the MEASURE-FIRST-AT-CHARTER mandate for perf waves | Template clause for docs/tranches/*/waves/*.md headers; | MODERATE | **KEEP** | Add a template clause to the wave-spec format: every PERF/[radical] wave header carries a `MEASURE-FIRST:` line stating 'the born-RED bench on <target-function> on the SAME report  |
| The BC-preserving-by-construction tell (zero-external-delta indirection — the Playhead pattern) | P.W3.md:78-85, 140 (the S4 Playhead delegating-accessor | MODERATE | **KEEP** | Smell-test question 7 (EXTERNAL-BEHAVIOR-DELTA?): a BC-preserving-by-construction wave with no measured payoff and an unlanded-dependency is DROP/DEFER until the dependency lands A |
| The dual-path-for-partial-availability tell (the Typed-OM pattern) | P.W3.md:48-55, 122 (StylePropertyMap LIMITED AVAILABILI | MODERATE | **KEEP** | Smell-test question 4 (SINGLE-PATH?): a cure that adds a PERMANENT second path (the old path can never be deleted) for a partial-availability or partial-case win is DEMOTE-TO-SPIKE |
| VJ-L1 flatLeaf fnName field on value.js ValueUnit | KF-TO-VALUEJS-P.md:33,97-179; utils.ts:45-57,64,289-294 | MODERATE | **DEMOTE-TO-SPIKE** | Keep the WeakMap terminal; re-open VJ-L1 only on a measured bottleneck. |
| VJ-L1 framed as terminal after P.W11 | KF-TO-VALUEJS-P.md:372-419; P.W11.md:135 | BORDERLINE | **RE-SCOPE** | Re-scope S8 close against proof:no-foreign-symbol-stamp. |
| SoA composite Float64Array buffer for AnimationGroup (the contiguous-typed-fold rewrite of the 3-arm blend) | docs/tranches/P/waves/P.W2.md S1/S2 (lines 56-87); P.md | CLEAR | **DEMOTE-TO-SPIKE** | Keep P.W2 EXACTLY as written structurally — it is already gate-first and MEASURE-FIRST: proof:soa-composite is born-RED, the throughput arm records ADOPT(>=1.2x)/KILL(<1.2x) in soa |
| Typed-OM (Houdini StylePropertyMap) batched write path with permanent setProperty fallback | docs/tranches/P/waves/P.W3.md S1/S5 (lines 34-55, 88-11 | CLEAR | **DEMOTE-TO-SPIKE** | Split the wave at the seam the spec already draws: S2 (the _styleOut out-buffer alloc-cure) is GROUNDED and lands UNCONDITIONALLY on both paths — ship it now (see grounded keepers) |
| Playhead value-object transposition (BC-preserving delegating-accessor extraction over the unlanded O.W7) | docs/tranches/P/waves/P.W3.md S4 (lines 78-84, 113-116) | CLEAR | **DROP** | DROP from the perf campaign — it carries no perf claim and no perf gate (its gates are structural: zero-import-of-KeyframesAnimation + manual-clock-drive). If the driver-protocol f |
| computed-unit composite-numeric-key cache (fold the C1 (target, epoch) two-compare fast path into one targetId | docs/tranches/P/waves/P.W2.md S4 (lines 26-28, 105-116) | CLEAR | **MEASURE-FIRST** | MEASURE-FIRST kf-side ONLY: keep the bench/interp-buffer.bench.ts 'calc() leaf 600-frame steady window (C1 endpoint memo)' arm as an observe-only baseline, but do NOT dispatch the  |
| 2-char (length+second-byte) dispatch widening of parse-that's first-char dispatch() LUT | docs/tranches/P/KF-TO-PARSETHAT-B.md PT-B3 part 2 (line | BORDERLINE | **MEASURE-FIRST** | Keep it folded under PT-B3's proof:perf regression gate (which IS the grounded keeper — the missing CI bench floor), but gate the widening on the json-comprehensive baseline NOT on |

## The grounded keepers (pass the smell-test)

- **The _styleOut module-scope out-buffer alloc-cure (the O.W8 S8 floor) passed as the `out` a** — Real, measured allocation cure of a proven pattern. utils.ts:417 calls unflattenObjectToString(vars) with NO out arg — a fresh Record<string,string> per apply f
- **The byte-exact proof:blend regression oracle riding the existing scripts/proof-blend.mjs +** — A dedup, not new surface — it RIDES the already-shipped blend oracle (package.json:84). It correctly captures the un-clamped add (group.ts:300-301), the spring-
- **The MEASURE-FIRST / ADOPT-or-KILL decision-JSON discipline (soa-composite-decision.json, t** — The correct conservative pattern and the antidote to the contrivances in this very lane: it forbids unproven engine code from shipping (P-inv-28). spring-vector
- **P.W5 S1: ghost .rainbow-wrapper shimmer cure (no @keyframes rule for the class)** — Confirmed live bug: CubeTarget.vue:54-60 binds the class and writes animation-delay/duration but no @keyframes rule for .rainbow-wrapper exists in the file. The
- **P.W5 S2: amiga boing/boot raw setTimeout race cure** — Confirmed live bug: AmigaScene.vue:175-183 boingTimer = setTimeout(..., 4200) writes sphereMesh.position.set() on a potentially torn-down mesh after unmount. Th
- **P.W5 S2: MeshLambertMaterial → MeshPhongMaterial swap** — Verified at demo/amiga/utils.ts:61. A one-line material-constructor swap restoring the specular pop. No architectural change, minimal risk, correct design inten
- **P.W5 S3: flick-to-boing via release angular-speed threshold in useSphereSpin.ts:endDrag** — The velocity is already tracked (useSphereSpin.ts:61-70 velX/velY). The egg is a threshold check on existing data + a call to the already-existing onBoing(). Ze
- **P.W6 S1: invalid role=slider ARIA (missing aria-valuenow/min/max) fix** — Confirmed at SquareScene.vue:37-39 — role=slider with no aria-valuenow is a WCAG 4.1.2 violation. Fix is attribute additions, not structural change.
- **P.W6 S2: SpringTrace first/last-point anchor-before-fill fix** — Confirmed at SpringTrace.vue:77-79 — anchor lines appear after the fill loop, assigning wrong x-coordinates to the first/last SVG points. The in-place fix is 2 
- **P.W6 S2: 60 Hz Vue-ref leak exit (scrubberPhase ref at useSpringHotPath.ts:80,143)** — Confirmed at useSpringHotPath.ts:80 — scrubberPhase = ref(0) written at 60 Hz and read in a slot render triggers 60x/s component re-renders. The DOM-direct pain
- **P.W9 S1: parse() throws NAMED_SELECTOR_NO_TIMELINE instead of producing NaN frames** — Confirmed live: frame-compiler.ts:449 sorts templateFrames by a.start.value - b.start.value where start.value is the string 'entry', producing NaN. The binarySe
- **P.W9 S4: kf-vs-WAAPI differential oracle (WAAPI-eligible subset only)** — The differential oracle covers a genuinely blind spot in the existing purely-JS oracles (which cannot detect browser divergence). The WAAPI-eligible subset (4 c
- **P.W10 S1: proof:no-cross-realm-cast structural gate (annotated escape hatch for utils.ts:2** — The as any casts over value.js/parse-that types at utils.ts:229,236 are a real workaround class invisible to tsc. The annotation-based escape hatch is KISS — on
- **P.W10 S2: proof:boundary W96 parse-that source-scan** — utils.ts:1 directly imports @mkbabb/parse-that — a confirmed production dependency breach. The scan (assert zero from '@mkbabb/parse-that' in src/animation/**) 
- **P.W10 S3/S4: leaves.ts externalization TRAP resolution via W97 decision oracle** — The self-contradiction is real and verified: proof-boundary.mjs:96 bans the @mkbabb/value.js/math subpath specifier (the regex matches it explicitly). O.W9's as
- **P.W10 S5: drop three @deprecated aliases (Animation, ScrollTimelineOptions, ScrollTimeline** — Confirmed at engine.ts:1205 and timeline.ts:171,209-218. These are published-surface legacy with no rationale for keeping after the 5.x rename. The 22-consumer 
- **P.W11 S2: Symbol(kf.fnName) → WeakMap<ValueUnit, string> swap** — Confirmed live: utils.ts:45-64 stamps a private Symbol onto published value.js ValueUnit instances it does not own. The WeakMap keeps the same fnNameOf/stampFnN
- **P.W12 S1: fix proof:workaround-deletion S1 false-RED (missing apiPresent on the S1 arm)** — Confirmed live bug: the S1 arm has version:'4.1.0' but no apiPresent field. glass-ui 4.1.0 is published but SegmentedTabs.vue:406 still emits :aria-orientation 
- **P.W12 S2/S3: re-pin to ~4.1.0 + delete pointerHandled/onPlayPointerDown dock interim (DM-1** — useDockClickIntegrity is confirmed present in the installed glass-ui 4.0.1 dist (dock.js:534). The 9 workaround hits in TransportDock.vue are live. DM-1 at chro
- **O.W5 DemoControlPoint build-in (DM-2 terminal)** — Glass-ui BC answered ASK#5=NO (confirmed in KF-INBOUND.md). The 7-tranche P-inv-28 chronic has a PUBLISHED substrate (kf's own LIGHT drag2D at index.ts:88) that
- **O.W6 fromMorphSVG build-in (DM-3 terminal)** — PathGeometry (getTotalLength/getPointAtT) is confirmed present at value.js 1.0.2 (dist/transform/path.d.ts:36-54). The compositor is the third member of the exi
- **O.W3 named-selector NaN-frame cure (DM-22)** — The exact defect chain is verified live: NAMED_SELECTOR_SUPERTYPE written at addFrame (frame-compiler.ts:228-231), never read in parse() (dead write), sort at :
- **O.W4 multi-color refusal + ingest deepening** — Both bugs are verified live: (1) compile-color.ts:191 calls colorToOklabCSS unconditionally regardless of space parameter (grep colorToOklchCSS → zero hits conf
- **O.W7 engine-seam transposition itself (the 1397L→~900L split)** — The split is a genuine P-inv-28 structural debt (the BORN-RED HANDOFF in proof-decomposition.mjs:151-157 is confirmed verbatim). Engine.ts is confirmed at 1397L
- **O.W8 S8: transformTargetsStyle per-frame Record allocation** — The alloc is confirmed live: utils.ts:417 calls unflattenObjectToString(vars) without an out buffer on every rAF frame. The value.js API is confirmed to accept 
- **O.W8 S9: reconcileVars residual O(N) findIndex** — The findIndex at frame-compiler.ts:418 (scanning this.frames for a matching (startIx,endIx) pair) is confirmed inside the reconcileVars loop, which is called fr
- **O.W9 no-legacy alias drops (Animation, ScrollTimeline, ScrollTimelineOptions)** — All three @deprecated aliases are confirmed on the published dist/keyframes.d.ts. The ./math subpath is confirmed present at value.js 1.0.2 (dist/subpaths/math.
- **O.W10 VJ-L1 flatLeaf and VJ-L3 parseCSSSubValue dispatch (as sibling asks)** — S8 (FN_NAME Symbol sidechannel) is a real constellation-spine breach — confirmed at utils.ts:45-57 with 7 stamp/read sites. S9 (direct parse-that production dep
- **O.W11 glass-ui aria-orientation correction dispatch** — The ARIA defect is correctly identified: SegmentedTabs.vue:406 emits aria-orientation unconditionally including on role=group (the pill variant, the default), w
- **O.W2 stale-gate retarget (phantom 4.1.0 version sentinels)** — The 4.1.0 version sentinel in proof-workaround-deletion.mjs:216,228 is confirmed phantom — npm shows only ['4.0.0','4.0.1']; 4.1.0 returns E404. The content-pro
- **O.W16 W96 boundary scan for parse-that (S1, NOW-executable)** — The gap is confirmed: proof-boundary.mjs:93-107's holdsValueJsSpecifier matches only @mkbabb/value.js, not @mkbabb/parse-that. The utils.ts:1 direct parse-that 
- **O.WZ proof:changelog-5.0.0 authoring** — No proof:changelog-* gate exists (confirmed: ls scripts/proof-changelog* → no matches). release.yml:59-83 has no changelog completeness gate. The four @deprecat
- **O.W8 S4: postTask retarget to real-browser INP measurement** — proof:scheduler-posttask currently GREEN-by-SKIP in jsdom — confirmed at proof-scheduler-posttask.mjs:31. The gate has NEVER measured the INP delta it was built
- **VJ-L1 the additive fnName provenance carrier (conservative form: a 7th optional ctor field** — The conservative additive field is genuinely minimal (~10 LoC, BC-additive) and cures a real foreign-realm annotation: kf today stamps Symbol('kf.fnName') onto 
- **VJ-L3 parseCSSSubValue root helper — internalize tryParse(any(CSSFunction.FunctionArgs, CS** — Real cross-realm breach: kf imports @mkbabb/parse-that directly (utils.ts:1) and carries it as a production dep (package.json:215) SOLELY for the any combinator
- **VJ-P1 color2Into out-param mechanism (write r/g/b or l/c/h into a caller-owned scratch Col** — Textbook grounded alloc cure: it MIRRORS the already-proven matrix.transformMat3Into out-param (value.js matrix.ts:34-42, O.W3 zero-alloc), the gamut math is AL
- **VJ-P.W0 record-hygiene: git add the untracked value.js docs/tranches/O/ + reconcile the st** — A real, zero-risk record-truth fix, not an API ask — the dispatch packet cites those O docs as its supersede anchor, so an uncommitted tree under the cross-repo
- **VJ-P3 :any → string param narrowing on setSubProperty/setProperty (the change itself, minu** — A trivially BC strictness improvement on a strict:true library: every caller already passes a string (kf's parseAndFlattenObject sets property+subProperty on ev
- **O.W0 — two P0 crash fixes (linear-gradient crash + CSS Nesting ParseError) + linear() stop** — All three are confirmed live failures on 0.13.0 with exact source locations and reproducible test cases. Real crashes shipping to consumers. No speculation.
- **O.W1/O.W2 — subpath split (./color, ./parsing, ./math, ./easing, ./transform, ./units, ./q** — The 145 KB monolith is a confirmed, measured structural problem (all size and graph-trace claims verified live). The subpath split is the keyframes.js LIGHT/HEA
- **O.W3 — gamutMapToRgbSpace scalar bisection rewrite + transformMat3Into out-param + JND ear** — The 52+ Color allocations per gamutMap call are confirmed in source (dispatch.ts:223–257). The cure pattern (scalar scratch, out-param, early-exit) is a proven-
- **O.W4.A — recursive at-rule bodies (@layer/@media/@container/@scope/@starting-style)** — The @layer opaque body is a confirmed live gap: extractKeyframes silently loses @keyframes nested inside @layer. This is a real kf consumer failure path (CSSKey
- **O.W4.B — CSS Nesting AST (full typed children, building on O.W0.S2 crash fix)** — CSS Nesting is baseline in all modern browsers. The crash fix (O.W0.S2) is grounded; promoting nested rules to typed children: StylesheetItem[] is the natural f
- **O.W5.S1 — FunctionValue.toString() linear() stop-spacing fix (also O.W0.S3)** — Confirmed live serializer bug. parseCSSValue('linear(0, 0.5 50%, 1)').toString() === 'linear(0, 0.5, 50%, 1)' is a real round-trip corruption affecting any cons
- **O.W5.S4 — semantic-idempotence fuzz harness (deterministic fixed corpus, vitest)** — The idempotence invariant (inv-O-2) is a real structural correctness requirement for a parser/serializer pair. The fixed-corpus deterministic harness is KISS (n
- **O.W5.S2 — serializers for all O.W4/O.W4b new StylesheetItem variants** — The serializers are a direct structural prerequisite for the idempotence harness. Without serialize arms for the new typed variants, the harness fails with Type
- **O.W6.S1 and O.W6.S5 — CSS-parse throughput bench + proof:perf-target gate (MEASURE-FIRST)** — The bench authorship and born-RED gate are correctly structured: establish baseline before claiming improvement. No CSS-parse throughput bench exists today; add
- **N.W7 library asks — linear()/steps() parsers, toAnimationString, LRU memoize bound, pretti** — Each is a concrete consumer-filed bug or measured problem: prettier in dist is 54% of 560.7 KB unpacked (measured); the LRU unbounded growth is a real memory bu
- **L — api/src legacy-excision (sessionToken, status field, WithId transposition, DI seam)** — All items are confirmed live technical debt with exact grep counts and file locations. The status 4-state → (visibility,tier) transposition is load-bearing (one
- **K.W2 — acyclic cross-repo color topology + OKLab dedup + tsconfig.lib/demo split** — The byte-exact OKLab coefficient duplication in glass-ui is a real correctness/maintenance risk. The 75 TS7016 cascading errors are a confirmed live build failu
- **I — CRUD-CONTRACT v2.0.0 conformance waves (visibility split, soft-delete, SOTA envelopes)** — 53 DEFERRED cells from the cross-repo CONFORMANCE-MATRIX with exact fourier-D.W5 audit evidence. Each wave addresses a confirmed live API contract violation. No
- **Draggable bounds + rubber-band + snap (L.W5 S1+S2, drag.ts:70-85)** — Real bug/gap: drag.ts:233-240 sets spring.target unconditionally with no bounds enforcement. useSpringPaneDrag.ts manually implements clamping (lines 57-63), co
- **Sequence.on() transport-events (L.W5 S3, sequence.ts:249, sequence-events.ts)** — Fills a genuine functional gap: Sequence had no crossing-callback channel despite the SequenceScene demo needing timeline callbacks. The implementation mirrors 
- **engine-seam transposition (M.W13, engine.ts → ~900L + engine-playback.ts)** — Legitimate cohesion cure: engine.ts is 1397L carrying four distinct concerns per the D.W4 audit. The override cap (1400) has been a named HANDOFF since D, disch
- **proof:bench-taxonomy + bench/numeric-soa.bench.ts (M.W12 S1+S3, budgeted-floor enforcement** — Honest measurement gap closure: the G-era 1.56×-4.25× number was value.js's own bench, not a kf-side measurement. The L.W7.md mis-attribution is a documented fa
- **workaround-deletion gates (M.W9 S7+S8+S9, proof:workaround-deletion)** — All three are genuine inv-L-acyclic-purity violations: (S7) a consumer-side normalization of a published sibling serializer bug, (S8) a private Symbol stamped o
- **proof:replay-equality + compile-surface totality waves (M.W5+M.W6, L.W1+L.W2)** — Fixes four genuine observable bugs: @property not emitted by compileToCSS; named-selector parse path producing NaN frame-times making every frame always-active;
- **Oscillator primitive (src/animation/oscillator.ts, L.W9 S5)** — The Oscillator is a genuine periodic-phase-clock primitive with a grounded consumer signal (glass-ui BC books W-TUNABLE-ANIM/W-VIZ-CHOREOGRAPHY/W-MOTION-ONE-CLO
- **gate-apparatus consolidation (M.W1-W4, the parallel report-all runner + inv-M-one-runner)** — The O(N²) serial 141-clause && chain with 72 cold-boot Chromium launches was a measured architectural bottleneck (3-hour iterate-to-green loop, documented in M.
- **fromMorphSVG build-in over published PathGeometry (M.W14 S1, morph-svg.ts)** — The L-ledger premise ('value.js PathGeometry absent at 0.13.0') was a factual error (viol-M5). PathGeometry, getTotalLength, getPointAtLength were present in th
- **inv-M-observable-truth discipline (the proxy-detection reform across M waves)** — This is a meta-architectural correction of the exact failure mode that produced I's catastrophe (97 green gates on a broken product) and K's false cold-entry ce
- **The decision-JSON / P-invariant-28 MEASURE-FIRST discipline (spring-vector-decision.json's** — scripts/spring-vector-decision.json is the canonical, already-shipped terminal-home pattern: winFraction threshold, measured ratio, ADOPT/KILL verdict, recorded
- **The born-RED bench requirement already present in the waves (P.W2 S3, P.W3 S5)** — Both waves already mandate a born-RED, same-report, device-independent ratio bench routed through P.W1's ratioGate (P.W2 S3 throughput; P.W3 S5 write-cost-ratio
- **The kf _styleOut out-buffer alloc-cure (P.W3 S2 FLOOR)** — A proven-pattern alloc cure: hoist a module-scope reuse Record and pass it as the `out` arg to value.js's unflattenObjectToString (the sink already accepts it —
- **The proof:blend byte-exact regression oracle (P.W2 S2)** — An already-shipped regression gate (package.json:84, proof-blend.mjs + test/blend.test.ts over the group/cube corpus) that the SoA transposition must ride — it 
- **The grounded-bug-fix / dedup counter-class (packrat cross-input pollution FIX; Span-combin** — The CONSTITUTION §4/§5 sibling payloads include real correctness bugs (packrat memo key has no src component, packrat.ts:55 — a stale-result-across-inputs BLOCK
- **VJ-L3 parseCSSSubValue (FunctionArgs-first in-realm door)** — Real, not self-solvable: parse-that import (utils.ts:1), two as-any casts (utils.ts:229,236), prod dep (package.json:215); parseCSSValue truncates multi-functio
- **LIGHT/HEAVY boundary at index.ts + loadAnimationEngine** — Clean, CI-gated by proof:boundary; types erased via verbatimModuleSyntax; only cosmetic prose density (index.ts:143-202).
- **P.W11 WeakMap in-realm cure for S8** — Replaces a Symbol on a foreign value.js class (utils.ts:45-57) with a kf-local WeakMap; unblocks O.W7 with zero sibling dependency; makes VJ-L1 unnecessary.
- **value.js color2Into out-param (VJ-P1) — gamut bisection writes channels into a caller-owne** — Real measured 84-alloc tail: dispatch.ts:245 explicitly DEFERS the cure ('eliminating it requires a color2Into out-param, deferred, O.W5 scope'); the per-step c
- **kf _styleOut out-buffer alloc-cure (P.W3 S2 / O.W8 S8)** — Verified at utils.ts:415 (read): transformTargetsStyle calls unflattenObjectToString(vars) with NO out arg, minting a fresh Record<string,string> every rAF appl
- **packrat cross-input pollution fix (PT-B1)** — A real correctness BUG, verified at parse-that packrat.ts:55: getCijKey = (parser.id << 20) | (offset & MAX) — purely (id, offset), NO src component. The module
- **combinator fusion (PT-B3 part 1) — fuse all(a,b,c)/then chains into one monomorphic closur** — Real per-call allocs, verified: all() at leaf.ts:112 mints `const matches: unknown[] = []` every call; then() allocates [v1, state.value] per call. A static N-d
- **Span-combinator dedup (PT-B2) — collapse ~400 copy-pasted *Span backtracking lines into on** — A real deduplication, not a perf gamble: span.ts:16-360 carries 10 *Span builders (manySpan/sepBySpan/...) whose control flow is byte-identical to the value-lan
- **MEASURE-FIRST portable decision-JSON discipline (P.W1 ratioGate + *-decision.json + declar** — This IS the elegant conservative-perf spine and it is real and shipped-pattern: spring-vector-decision.json is the durable ADOPT/KILL verdict (ratio/winFraction
- **value.js VJ-P2 typed Float64 channel view + VJ-P3 :any->string seam narrowing** — VJ-P2 is an additive INTERNAL fast-path (lazily-materialized _ch Float64Array read by lerpColorValue) that preserves the public [key:string]:any AoS shape (colo

## The pre-charter smell-test (every future wave answers in its header)

1. **MEASURED-BOTTLENECK?** A born-RED bench shows the CURRENT code is the bottleneck on THIS wave’s actual target path (not a sibling’s, not a synthetic micro-bench).
2. **DEFAULT-PATH?** The breach exists on the DEFAULT/most-common path, not only a non-default arm.
3. **IN-REALM-SOLVABLE?** The consumer can solve it in its own realm without a not-yet-shipped sibling API or external prototype.
4. **SINGLE-PATH?** The cure REPLACES the old path — no permanent dual maintenance for a partial-availability/marginal win.
5. **SMALLEST-CHANGE?** It is the smallest edit that fixes the named defect, not a rewrite of a working gestalt for an asserted payoff.
6. **SAME-REPORT-RATIO?** The perf claim is a same-report device-independent ratio (numerator + denominator in ONE pass), not an absolute floor or a transplanted multiple.
7. **EXTERNAL-BEHAVIOR-DELTA?** If BC-preserving by construction (delegating accessors), there is some OBSERVABLE payoff — not pure indirection.

ANY “no” on 1/3/4/6 → DEMOTE-TO-SPIKE or MEASURE-FIRST before charter. The durable enforcer: a born-RED **proof:wave-charter** docs-and-presence gate — every perf wave names its target function + points to a born-RED bench whose baseline measures THAT function; the bench baselineCase must name-match the wave’s declared target (catching the transplanted-ratio trap).
