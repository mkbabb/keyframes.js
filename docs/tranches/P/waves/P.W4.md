# P.W4 — RETIRED (tombstone slot)

**Status: RETIRED — out of campaign scope.**

The codegen-consume (kf inherits value.js's generated CSS parser + the
per-keyframe-shape interp-plan codegen) has been **removed from the Tranche P
campaign entirely**. Codegen is BBNF-lang's job and belongs in a completely
separate session — it is out of this campaign's scope. kf consumes value.js's
CSS parser as-is; no generated parser, no `./codegen` subpath, no
SpanParser→TS emitter, no "grammar-as-source-of-truth" generated parser is part
of this campaign.

This slot is kept as a **stable tombstone** so that the P.W5–P.WZ wave
numbering is unchanged. No P wave is renumbered.
