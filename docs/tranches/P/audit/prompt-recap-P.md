# Tranche P — prompt-recap-P · THE TOTAL PROMPT RECAP (A→O chain-trusted · new P session intake)

**Lane:** `L3-prompt-recap` (P audit, DOCS-ONLY). **Read-only on source/tests/gates/CI/demo** —
this workflow wrote exactly ONE file: this doc. **Tree at authoring:** branch `tranche-o-dev` tip
`44e7fae` (the O ratification commit — "RATIFIED (owner)"; see O.md ratification note); master
tip `aef3ef3` (M consume-close: value.js 1.0.2 re-pin); kf library: **4.3.0** on npm. **Method
(inv ε):** every coverage verdict is VERIFIED against the tree, the O charter + wave docs, the
32-lane P AUDIT-DIGEST.md, the CONSTELLATION-OPTIMIZATION-CAMPAIGN.md (the P constitution), and
the O prompt-recap chain. No row chain-trusted without its anchor named.

The A→O set is **chain-trusted** to `prompt-recap-O.md` (zero-drops, inv ε, authored 2026-06-19
on `tranche-o-dev`), which itself chain-trusts `prompt-recap-M.md` → `prompt-recap-L.md` →
`prompt-recap-K.md` → `prompt-recap-H/E/D/C`. This doc EXTENDS that spine with:

1. The **A→O chain-trust** — verified held, zero re-litigation.
2. The **new top-level session intake (2026-06-20, postdates O ratification 44e7fae)** — the
   optimization+triumvirate challenge + the frontend-design-fleet ask, the ONLY open drop found by
   the 32-lane P audit (AUDIT-DIGEST.md F3 BLOCKER: "NEW ASK NOT CAPTURED").
3. The **standing 7-clause mandate** as the immutable spine — cited, not re-litigated.
4. The **P-tranche forward path** — every open frontier from the P audit named with a P wave home
   or a USER-DOMAIN / sibling-DISPATCH terminal.

The recap holds to **inv-O-observable-truth** (gates test REAL observables, never proxies) and
**inv ε** (cite observed oracles, no overclaim). Every row carries one terminal verdict:
**ADDRESSED** / **HANDOFF** / **USER-DOMAIN** / **DISPATCH** / **FOLD-INTO-P** — each cited by a
born-RED gate, a named wave, a named commit, or a named sibling tripwire.

**Phase note (inv-16):** P is DEVELOPMENT-PHASE-ONLY. This lane authors only this doc. Every
"FOLD-INTO-P" verdict below names the P wave that WILL author the cure on explicit implementation
authorization. inv-16 holds throughout (P writes only keyframes.js; every cross-repo need is a
dispatch, never a foreign-tree edit).

---

## 0. The reckoning in one paragraph

A→O executed the constellation campaign (parse-that A 0.11.0 → value.js O 1.0.2 → kf M consume +
gates + deploy) and ratified Tranche O on 2026-06-20 (`44e7fae`). O is the **close-out tranche**
for M's unimplemented work — chartered with born-RED gates, sibling dispatches (VJ-L1/L3 +
glass-ui BC aria correction), and the 5.0.0 major cut — but O implementation is NOT yet
authorized. The 32-lane P audit (AUDIT-DIGEST.md, ~3.8M tokens, 2026-06-20, 297 findings, 172
novel ideas) found one genuine prompt-recap gap: the owner's **new directive to aggressively
optimize the triumvirate + brainstorm novel architectural approaches + improve the demos via a
frontend-design fleet** was issued on 2026-06-20 (postdating O ratification `44e7fae`) and has
no prompt-recap row anywhere in the tree (F3 BLOCKER). That gap is closed here. The P tranche
is the answer to that directive: ONE kf-hosted optimization tranche + TWO sibling dispatch
packets (value.js Tranche P, parse-that Tranche B), sequenced DAG-ordered, inv-16 clean.

---

## 1. THE STANDING SPINE — the immutable 7-clause mandate (cite, do not re-litigate)

**Source:** `J/J.md:111-119` (verbatim, carried A→O). **Re-issued for Tranche P** as the
P-intensified challenge: "aggressively optimize the triumvirate; brainstorm novel architectural
approaches that challenge each library; improve demos' usability/clarity/correctness via a
frontend-design fleet" (owner, 2026-06-20, AUDIT-DIGEST.md F3 / CONSTELLATION-OPTIMIZATION-
CAMPAIGN.md §1).

The 7 clauses, as carried into P:

| Clause | P terminal | Evidence |
|---|---|---|
| DEEPLY audit (32 agents in parallel) | **ADDRESSED — THIS SESSION** — 32-lane P re-audit on disk (AUDIT-DIGEST.md, ~3.8M tokens, 2026-06-20, 297 findings, 172 novel ideas, 29 radical) anchored to `aef3ef3` | `docs/tranches/P/audit/AUDIT-DIGEST.md`; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §1` |
| NO quick solutions / NO workarounds — idiomatic gestalt | **ADDRESSED + NAMED RESIDUALS** — P mandate intensified to "NO quick solutions, NO workarounds; idiomatic + gestalt; architectural transpositions for elegance/simplicity/PERFORMANCE above all are necessary + desirable; falsify-first, not passive" (CAMPAIGN §1); each P wave carries a born-RED gate; the S8/S9/leaves.ts residuals name their P wave or sibling home | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §1`; P.W2 (SoA compositor) / P.W3 (Typed-OM) / P.W9 (NaN cure) / P.W10 (leaves TRAP) — P.W4 codegen RETIRED |
| Architectural transpositions for elegance/simplicity/performance | **ADDRESSED (chartered, gate-first)** — P.W2 SoA-compositor; P.W3 Typed-OM write path; P.W9 NaN-frame-cure; P.W10 leaves-externalization-TRAP (bundle-externalization, not delete); P.W11 VJ-L1-WeakMap early-cure; P.WZ close. (P.W4 codegen-consume RETIRED 2026-06-22 — codegen = BBNF-lang separate session.) | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §5`; `AUDIT-DIGEST.md K1/K2/K3` |
| NO legacy beside its replacement | **ADDRESSED (chartered)** — P.W10 leaves.ts-externalization-TRAP (the O.W9 claim was a TRAP — `proof:boundary` bans the subpath from LIGHT source; correct transposition is bundle-externalization); deprecated aliases exiting at O.W9/O.WZ (5.0.0); no new legacy introduced by P | `AUDIT-DIGEST.md F4 HIGH`; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §5` |
| Fold ALL chronics to terminal or KILL (P-invariant-28) | **ADDRESSED (chartered) — FOLD-INTO-P** — DM-2 (born E · O.W5 BUILD-IN, authorized separately) / DM-3 (born C · O.W6 BUILD-IN, authorized separately) / S8/S9 at chronicity-4 P-inv-28 MANDATORY BELT → P.W11 WeakMap early-cure (kf-internal) + O.W16 VJ-L1/L3 consume (the clean sibling exit); P.W12 is S1/S2 ONLY (glass-ui aria/dock); every P chronic has a terminal home in the deferred-ledger-P | `docs/tranches/P/audit/deferred-ledger-P.md` (chartered); `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §6` |
| Recap ALL prompts (zero drops) | **ADDRESSED — THIS DOC** — `prompt-recap-O.md` zero-drops chain-trusted (A→O); this lane extends it to the new P session intake (the F3 BLOCKER gap, the only open drop) | this document |
| Dev only (no source) | **HONORED** — P is DEVELOPMENT-PHASE-ONLY; inv-16 holds; this lane wrote only this file; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §7` dev→impl boundary | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §7`; inv-16 |

**P implication:** the standing mandate carries forward unchanged and is INTENSIFIED into an
active challenge — not passive documentation but falsify-first adversarial re-examination of
each library's architectural assumptions. The 7 clauses are the immutable spine; they are not
re-litigated per session.

---

## 2. THE A→O LINEAGE — chain-trusted to prompt-recap-O.md's zero-drops close

The A→O per-tranche lineage (U1–U8 edicts, cold-path P0 seeds, K/L/M/O rows, the constellation
campaign execution, the four post-M-charter IMPL-OPEN events, the D1–D11 locked decisions, the
demo-gate cascade, and the O re-audit frontiers) are ALL dispositioned to terminal verdicts in
`prompt-recap-O.md` (§1–§12, TOTALS: zero drops) — itself chain-trusting `prompt-recap-M.md`.
This lane does NOT re-litigate them. It verifies the close held and carries forward ONLY the
rows the P re-audit re-opens, extends, or corrects.

**The P audit's re-opens / corrections on the O close record:**

| O close state | P re-open / correction | P home |
|---|---|---|
| O is RATIFIED-but-unimplemented (O.W1…O.WZ = authorized separately, not yet executed) | P sequences ATOP O — O's chronic terminals DM-2/DM-3 + engine-seam O.W7 are P's foundation; P does NOT re-implement O waves | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §6`; P waves start AFTER O implementation (or with O as GATED predecessor) |
| VJ-L1 (flatLeaf) + VJ-L3 (parseCSSSubValue) absent from value.js 1.0.2 (S8/S9 PENDING) | P.W11 VJ-L1-WeakMap-early-cure is the kf-internal unblock arm; the O.W16 VJ-L1/L3 consume is the clean sibling exit; P.W4 codegen-consume RETIRED | `AUDIT-DIGEST.md K2 HIGH·workaround`; owner directive 2026-06-22 |
| O.W7 engine-seam VJ-L1-gated (1397L un-split) | P.W2 SoA-compositor + P.W3 Typed-OM write path are NOW-phase; P.W11 unblocks O.W7 kf-side via WeakMap early-cure; P.W4 codegen-consume RETIRED | `AUDIT-DIGEST.md K2`; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §3 DAG` |
| value.js O tranche docs UNCOMMITTED (`git status: ?? docs/tranches/O/`) | P dispatches to value.js Tranche P whose VJ-P.W0 COMMITS + reconciles the stale PROGRESS.md as its FIRST action (NOTE: VJ-P.W0 = value.js Tranche P's first wave; there is NO kf P.W0 — the kf roster starts at P.W1) | `AUDIT-DIGEST.md F5 HIGH·gap`; `KF-TO-VALUEJS-P.md §VJ-P.W0` |
| glass-ui 4.1.0 published but `SegmentedTabs.vue:406` STILL emits `aria-orientation` unconditionally — kf S1 deletion NOT safe | O.W12 S1 delete correctly remains GATED on BC cut + guard; P.W12 absorbs the BC glass-ui consume (S2 delete NOW, S1 GATED) | `AUDIT-DIGEST.md K5 HIGH·gap`; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §1` |
| proof:workaround-deletion S1 FALSE RED on `glass-ui@4.1.0` version probe (DO-2 O.W2 unexecuted) | P inherits the stale gate; P.W12 retargets S1 tripwire from version probe to content-present guard grep | `AUDIT-DIGEST.md K5`; P.W12 |
| No bench/ premise FALSE for both siblings — value.js has 9 bench files + proof-perf-target.mjs; parse-that has `typescript/test/benchmarks/` | P corrects the tranche brief: perf frontier has a measurement substrate; no "create benchmarks" task needed at either sibling | `AUDIT-DIGEST.md F5 Recs`; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §1` |
| SpanParser tagged-union retained as "codegen foundation" with ZERO consumer (P-inv-28 deferral) | **RETIRED (2026-06-22).** Codegen spine moved to BBNF-lang separate session. SpanParser has NO in-realm consumer → P-inv-28 resolution is KILL recommendation dispatched to parse-that B (fold into PT-B2 housekeeping or PT-B4). P.W4 kf-side codegen-consume is RETIRED tombstone. | `AUDIT-DIGEST.md P2 HIGH·deferred` (historical); owner directive 2026-06-22 |

---

## 3. THE NEW SESSION INTAKE (2026-06-20 — the F3 BLOCKER, the only open drop)

> **Source:** Owner directive, 2026-06-20, postdates O ratification commit `44e7fae`. Captured as
> the **F3 BLOCKER** in AUDIT-DIGEST.md: "the current workflow's NEW ask — CHALLENGE the
> triumvirate with AGGRESSIVE OPTIMIZATION + NOVEL architectural approaches above all (perf +
> arch), plus a frontend-design fleet for demo usability/clarity/correctness — postdates the O
> ratification commit (44e7fae 2026-06-20) and has no prompt-recap row anywhere in the tree."

This is the ONLY open drop found by the 32-lane P audit. Every prior prompt A→O is dispositioned
in `prompt-recap-O.md` (chain-trusted zero-drops). This section closes the F3 gap.

### 3a — "aggressively optimize the triumvirate (value.js · parse-that · keyframes)"

The core new mandate: not incremental tuning but **aggressive + novel architectural approaches
that challenge each library**. Falsify-first.

| Sub-directive | Terminal | Evidence / oracle |
|---|---|---|
| Aggressively optimize value.js | **ADDRESSED (chartered + dispatched)** — `KF-TO-VALUEJS-P.md` dispatches to value.js Tranche P: VJ-P1 `color2Into`/`mixColorsInto` out-params (~84→<12 allocs/call, gamut completion), VJ-P2 typed Float64 channel view, V2 `flatLeaf`-first provenance model, V3-grammar-correct color() / `none` / `round()` correctness cures | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §5`; `AUDIT-DIGEST.md V1/V2/V3/V5` |
| Aggressively optimize parse-that | **ADDRESSED (chartered + dispatched)** — `KF-TO-PARSETHAT-B.md` dispatches to parse-that Tranche B: **PT-B1** packrat cross-input pollution FIX (BLOCKER correctness bug — memoize() returns stale across inputs); **PT-B2** Span-combinator DEDUP (collapse ~400 lines of copy-pasted Span backtracking plumbing into ONE collector-parametric vocabulary); **PT-B3** perf frontier (combinator fusion + 2-char dispatch widening + bench gate); **PT-B4** SpanParser KILL recommendation (no in-realm consumer after codegen retraction). Codegen emitter (formerly PT-B1/P1-N1) RETIRED — BBNF-lang separate session. | `AUDIT-DIGEST.md P1/P2/P3/P5`; owner directive 2026-06-22 |
| Aggressively optimize keyframes.js | **ADDRESSED (chartered, NOW-phase)** — P.W1 bench-coverage + portable-perf-gate infra; P.W2 SoA-compositor Float64Array for AnimationGroup; P.W3 Typed-OM write path; P.W9 NaN-frame-cure + grammar-fuzz + differential-oracle. (P.W4 codegen-consume RETIRED — codegen = BBNF-lang separate session.) | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §5`; `AUDIT-DIGEST.md K1/K2/K3` |
| Novel architectural approaches (falsify-first) | **ADDRESSED (chartered, born-RED-gated)** — the A.W3 falsification record is locked (SpanParser runtime-switch SLOWER on V8; codegen RETIRED to BBNF-lang); `Playhead` value-object transposition (K2); SoA composite buffer (K1); Typed-OM write path (K3); the leaves-TRAP bundle-externalization (F4); each gate is a PORTABLE RATIO bench, not an absolute number | `AUDIT-DIGEST.md F4/K1/K2/K3`; `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §5` |
| Measure-first / record-as-built / no stale premises | **ADDRESSED** — stale premises corrected up-front: (1) bench/ dirs EXIST at both siblings; (2) glass-ui 4.1.0 published BUT S1-delete is NOT safe (aria guard absent). (bbnf-lang stale-premise retracted: codegen is BBNF-lang's separate session; not a "wiring" premise for this campaign.) | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §1`; `AUDIT-DIGEST.md F5 Recs` |

### 3b — "brainstorm novel architectural approaches that challenge each library"

The directive to CHALLENGE — not to accept the current architecture as fixed.

| Library | Novel challenge | P home | Born-RED gate |
|---|---|---|---|
| **keyframes** | `Playhead` value-object — transpose the god-object at the TRUE seam (the driver protocol: Sequence/Group/ingest poke 4 raw fields today); `KeyframesAnimation` becomes a thin compile-facade | **P.W2 / P.W4** | `proof:playhead-decoupled`: `engine-playback.ts` has ZERO import of `KeyframesAnimation` |
| **keyframes** | SoA composite buffer for AnimationGroup — single contiguous `Float64Array` accumulator; blend becomes a branch-free/dispatch-free typed fold | **P.W2** | `proof:soa-composite` ratio arm: SoA-engine-hz / per-channel-engine-hz ≥ 1.5 at K=8 |
| **keyframes** | Typed-OM write path — batched `StylePropertyMap` mutation eliminating per-frame string-serialize + browser CSS re-parse | **P.W3** | `proof:typed-om-eligible`: animation with pure-transform applies via `StylePropertyMap.set()` |
| **keyframes** | leaves.ts-externalization-TRAP — the no-legacy cut as written REDs `proof:boundary`; the correct transposition is bundle-externalization (teach `proof:boundary` a `math-subpath-clean` clause) | **P.W10** | `proof:boundary` new `math-subpath-clean` clause: bundle `@mkbabb/value.js/math` as a lib entry, assert zero grammar modules in its static graph |
| **value.js** | `flatLeaf`-first value model — make the provenance-carrying leaf the canonical parsed shape so `flattenObject` is a projection, not a tree-dissolve that drops `FunctionValue.name`; dissolves the entire kf S8 sidechannel class intrinsically | **DISPATCH → value.js P** | `proof:flatleaf-roundtrip` born-RED: parse nested-function CSS corpus + assert re-serialized output is byte-identical to input |
| **value.js** | typed Float64 channel view for Color — kill the string-keyed megamorphic `[key:string]:any` reads without removing the public dynamic shape | **DISPATCH → value.js P** | born-RED bench: Float64-view interp ≥ 1.2x string-keyed interp over 600-frame P3 gradient |
| **parse-that** | ~~BBNF→specialized-monomorphic-TS codegen~~ — **RETIRED (2026-06-22).** Codegen = BBNF-lang separate session. parse-that B headline becomes: **PT-B1 packrat-fix** (correctness BLOCKER) + **PT-B2 Span-dedup** (no-legacy/gestalt DEDUP) + **PT-B3 perf-frontier** (combinator fusion + dispatch widening + bench gate) + **PT-B4 SpanParser KILL** (no in-realm consumer). | **DISPATCH → parse-that B** | PT-B1: born-RED correctness test; PT-B2: line-count ratio; PT-B3: portable bench ratio; PT-B4: deletion gate |
| **parse-that** | packrat cross-input pollution FIX (BLOCKER) — `memoize()` returns stale result across different inputs; WeakRef-epoch / auto-reset cure | **DISPATCH → parse-that B** | born-RED test: `memoize(parser).parse('hello')` then apply to `ParserState('CAPS123')` WITHOUT `resetPackrat()` — must return CORRECT result post-fix |

### 3c — "improve the demos' usability/clarity/correctness via a frontend-design fleet"

The frontend-design-fleet ask — the 29 demo novel ideas across 7 scenes, constituting Band C
(demo-fleet) of kf-P.

| Scene | Headline fleet ask | P wave | Born-RED gate |
|---|---|---|---|
| **easing** | DemoControlPoint as the DemoControlPoint showcase (the easing bezier handles ARE the O.W5 build-in; put handles on the HERO stage, not only the sidebar) | **P.W7** | proof:easing-hero-handles: ≥2 draggable handles on the hero stage element |
| **spring** | parameter-space heatmap (2D response×dampingFraction click-to-navigate canvas) — transforms abstract sliders into a navigable physics landscape | **P.W6** | proof:spring-heatmap-mount: `.spring-heatmap` exists; clicking a cell updates demo.response |
| **amiga** | flick-to-boing (earn the easter-egg via a deliberate-flick gesture, not double-click) — emergent physics interaction | **P.W5** | proof:amiga-boing-from-spin: pointer sequence dx=20px/16ms × 8 → boing triggered |
| **cube** | progressive disclosure + physics HUD (quaternion/angular-velocity chip) + ghost `.rainbow-wrapper` shimmer fix | **P.W5** | proof:cube-physics-hud + proof:cube-shimmer-live |
| **square** | velocity-tilt + directional squash-stretch via CSS custom property seam; phase-trace ghost (ring buffer SVG) | **P.W6** | proof:design-paint: velocity tilt observable during diagonal drag |
| **N-Stage switcher** | mobile is ENTIRELY UNBUILT (BLOCKER) — the shelf-driver; scroll-snap CSS carousel on mobile eliminates the mobile gap | **P.W8** | proof:n-stage-boundary: ZERO `loadAnimationEngine()` imports in stage modules; `@media (max-width)` rule exists |
| **parse-that dispatch() LUT heatmap** | turn the perf differentiator into a teaching tool — a visual of the dispatch LUT firing pattern | **P.W7** | proof:parse-lab-scene: scene mounts, calls parseCSSValue, asserts re-serialized output matches input |

**The F3 BLOCKER is now CLOSED.** The new session intake is fully captured above in §3a/3b/3c.

---

## 4. THE P TRANCHE TOPOLOGY — the F5 decision (cited, not re-litigated)

**Source:** `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §2` (the F5 decision locked in the
constitution). Recap: ONE kf-hosted optimization tranche + TWO dispatch packets.

| Topology decision | Status | Evidence |
|---|---|---|
| ONE kf-hosted tranche (kf-P) + TWO dispatch packets (value.js P + parse-that B) — NOT three independently-authored per-repo tranches | **LOCKED** — the proven inv-16 fence pattern; a single session drives all; siblings schedule the dispatched asks into their own trees (exactly as O did for VJ-L1/L3 + BC aria) | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §2` |
| DAG: parse-that B → value.js P → kf-P | **LOCKED** — the API + correctness + perf-floor coupling: packrat fix + Span-dedup + perf-frontier (parse-that B) → VJ-L1/L3 + color perf (value.js P) → kf consume. NOT a codegen spine (RETIRED). | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §3 DAG`; owner directive 2026-06-22 |
| kf-P version: **5.1.x** (perf is non-breaking; rides after O's 5.0.0 cut) | **LOCKED** — O.WZ cuts 5.0.0; P cuts 5.1.x | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §3` |
| value.js P version split: **1.1.0** (API: VJ-L1 + VJ-L3) then **1.2.0** (perf: color2Into + VJ-P2 typed Float64 channel view + `:any` seam narrowing — codegen-consume REMOVED from 1.2.0 scope) | **LOCKED (re-scoped 2026-06-22)** | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §3`; owner directive 2026-06-22 |
| parse-that B version: **0.12.0** — packrat cross-input bug fix (PT-B1) + Span-dedup (PT-B2) + perf-frontier (PT-B3) + SpanParser KILL recommendation (PT-B4) | **LOCKED (re-scoped 2026-06-22; codegen subpath RETIRED)** | `CONSTELLATION-OPTIMIZATION-CAMPAIGN.md §3`; owner directive 2026-06-22 |
| Value.js O docs uncommitted → value.js Tranche P's VJ-P.W0 commits + reconciles PROGRESS.md (NOTE: VJ-P.W0 = value.js Tranche P's first wave; there is NO kf P.W0) | **DISPATCH** — value.js Tranche P's FIRST action | `AUDIT-DIGEST.md F5 HIGH·gap`; `KF-TO-VALUEJS-P.md` |

---

## 5. THE P BAND/WAVE MAP — every open frontier named with a P wave home

| Band | Wave(s) | Phase | Headline |
|---|---|---|---|
| **A — Apparatus** | P.W1 (lint + bench-coverage + portable-perf-gate infra) | NOW | the fast-iterate floor: eslint/dep-cruiser tier already absent from tree; no portable bench baseline; the foundation for all perf claims |
| **B — Engine perf** | P.W2 (SoA-compositor + computed-unit-cache), P.W3 (Typed-OM write path), ~~P.W4 (RETIRED tombstone — codegen-consume)~~ | NOW W2/W3 | the two in-realm engine transpositions: Float64Array compositor; batched DOM write. Codegen is BBNF-lang's separate session — P.W4 is a RETIRED tombstone. |
| **C — Demo fleet** | P.W5 (cube + amiga), P.W6 (square + spring), P.W7 (easing-curve-editor + DemoControlPoint showcase), P.W8 (N-Stage switcher + mobile) | NOW | the frontend-design fleet; N-Stage mobile THE shelf-driver |
| **D — Correctness** | P.W9 (NaN-frame-cure + grammar-fuzz + differential-oracle) | NOW | DM-22 NaN-frame chronic + fast-check fuzz harness + Playwright CDP differential |
| **E — No-legacy** | P.W10 (leaves.ts-externalization-TRAP + deprecated-aliases + cross-realm-seam-gate) | NOW W10 · GATED(siblings) seam | the TRAP correctly named: bundle-externalization, not delete; `proof:boundary` `math-subpath-clean` clause |
| **F — Unblock** | P.W11 (VJ-L1-WeakMap-early-cure → unblock O.W7) | NOW | the P-inv-28 chronicity-4 belt early exit for S8/S9 (kf-internal arm); WeakMap<ValueUnit,string> at flatten time (realm-CLEAN; does NOT survive clone — VJ-L1 strictly preferred; O.W16 is the clean sibling exit) |
| **G — Consume** | P.W12 (glass-ui 4.1.0 S2-delete-NOW + S1-GATED-on-guard) | NOW S2 · GATED(glass-ui BC) S1 | S2/dock (`useDockClickIntegrity`, ships 4.0.1) deletable NOW; S1/aria GATED on BC aria SFC guard |
| **Z — Close** | P.WZ (close + 5.1.x cut) | NOW-author · USER-DOMAIN publish | the kf-P close + the 5.1.x non-breaking cut |

**Phase axis:** NOW (kf-internal, zero sibling dependency) / DISPATCH (cross-repo ask) / GATED
(fires on named sibling publish).

---

## 6. THE DISPATCH PACKETS — inv-16 clean

Both dispatch packets are AUTHORED in the kf tree and DISPATCHED to the siblings; kf never writes
in the sibling trees.

| Packet | Status | Key asks |
|---|---|---|
| `KF-TO-VALUEJS-P.md` (the value.js Tranche P dispatch) | **DISPATCH** — authored in `docs/tranches/P/KF-TO-VALUEJS-P.md` | VJ-L1 (`fnName?: string` on ValueUnit, BC-additive); VJ-L3 (`parseCSSSubValue`, BC-additive); VJ-P1 `color2Into`/`mixColorsInto` out-params (gamut completion); VJ-P2 typed Float64 channel view; V3 grammar-correct color() / `none` / `round()` cures; V5 BBNF grammar limbo resolution; VJ-P.W0 commit uncommitted O docs |
| `KF-TO-PARSETHAT-B.md` (the parse-that Tranche B dispatch) | **DISPATCH** — authored in `docs/tranches/P/KF-TO-PARSETHAT-B.md` | **PT-B1** packrat cross-input pollution FIX (BLOCKER — memoize() stale across inputs + 4096-ID float64-key fix); **PT-B2** Span-combinator DEDUP (one collector-parametric vocabulary, ~400 copy-paste lines collapsed); **PT-B3** perf frontier (combinator fusion + 2-char dispatch widening + bench/proof:perf gate); **PT-B4** SpanParser KILL recommendation (no in-realm consumer after codegen retraction). ~~P1-N1 BBNF→TS codegen emitter RETIRED.~~ |

---

## 7. THE DEFERRED FOLD + THE CHRONIC TERMINAL (P-invariant-28)

| Chronic (chronicity at P) | P terminal | Oracle / tripwire |
|---|---|---|
| **DM-2 GlassControlPoint** (born E · 7 carries through M · O.W5 BUILD-IN authorized separately) | **O.W5 BUILD-IN** (O implementation lane, not P) — P inherits the BUILD-IN as foundation, does NOT re-carry | `docs/tranches/O/O.md §4`; O implementation authorization |
| **DM-3 MorphSVG** (born C · 7 carries through M · O.W6 BUILD-IN authorized separately) | **O.W6 BUILD-IN** (O implementation lane, not P) | `docs/tranches/O/O.md §4`; O implementation authorization |
| **DM-5 S8/S9 workarounds** (chronicity 3 entering O; chronicity 4 = P-inv-28 MANDATORY BELT at P) | **P.W11 WeakMap early-cure (kf-internal) + O.W16 VJ-L1/L3 consume (the clean sibling exit)** — the P-inv-28 belt fires HERE; kf-side WeakMap is the early-cure arm; O.W16 VJ-L1/L3 is the clean solution; P.W12 is S1/S2 ONLY (glass-ui aria/dock — NOT an S8/S9 exit); P.WZ closes S8/S9 on O.W16 consume | `AUDIT-DIGEST.md K5/F4`; `proof:workaround-deletion` S8/S9 PENDING; tripwire: `"fnName" in new ValueUnit(0,"px") === false` today |
| **DM-1 RF-17 dock crossfade** (5-tranche + O.W12 BC-gated) | **O.W12 CONSUME** (BC-gated; P.W12 absorbs if BC fires during P) — S2 deletable NOW (O.W12 / P.W12); S1 GATED on BC SFC guard | `proof:workaround-deletion` S2; P.W12 |
| **S1 aria-orientation suppress** (BC-gated) | **P.W12 GATED on BC SFC guard** — S1 delete fires when BC ships `SegmentedTabs.vue:406` conditional emit; FALSE RED on `glass-ui@4.1.0` version probe (stale) retargeted to content-present grep in P.W12 | `AUDIT-DIGEST.md K5 HIGH·gap`; P.W12 |
| **DM-16 no-legacy renames** (5.0.0 cut, O.WZ) | **O.WZ** (O implementation lane; P rides the 5.0.0 foundation) | `docs/tranches/O/O.md §6` |
| **leaves.ts-externalization-TRAP** | **P.W10** — the correct transposition is bundle-externalization (teach `proof:boundary` a `math-subpath-clean` clause), NOT a delete that REDs `proof:boundary` | `AUDIT-DIGEST.md F4 HIGH·risk`; P.W10 |
| **SpanParser tagged-union P-inv-28 deferral** (retained with ZERO consumer) | **DISPATCH → parse-that B** — KILL recommendation (no in-realm consumer after codegen retraction; P-inv-28: dispatch RECOMMENDS deleting SpanParser tier span.ts:540-902 + the span-dispatch bench, keeping the A.W3 falsification as a docs paragraph — fold into PT-B2 housekeeping or PT-B4). P.W4 kf-side codegen-consume is RETIRED. | `AUDIT-DIGEST.md P2 HIGH·deferred`; owner directive 2026-06-22 |
| **value.js O docs uncommitted** | **DISPATCH → value.js VJ-P.W0** — first action of value.js Tranche P (VJ-P.W0, NOT kf P.W0) commits + reconciles the PROGRESS.md stale "DEVELOPMENT — charter only" record | `AUDIT-DIGEST.md F5 HIGH·gap` |

---

## 8. THE CODEGEN SPINE — RETIRED (2026-06-22, owner directive)

**The codegen spine is RETIRED from this campaign.** Owner directive 2026-06-22: "No codegen or
contrivance like that in parse-that. That's BBNF-lang's job and to be done in a completely separate
session." The spine (BBNF→TS emitter, generated CSS-value parser, P.W4 kf-side consume) is out of
scope. bbnf-lang is NOT referenced as a dependency, substrate, fallback, or design-reference for
this campaign.

| Element | Status | Note |
|---|---|---|
| ~~bbnf-lang codegen spine~~ | **RETIRED** — out of campaign scope; BBNF-lang's separate session | Not a dependency, substrate, fallback, or design-reference here |
| A.W3 falsification record: SpanParser runtime-switch ~10-14% SLOWER on V8 | **LOCKED historical record** — kept in AUDIT-DIGEST.md as evidence; NOT re-litigated; informs the KILL recommendation for SpanParser | `AUDIT-DIGEST.md P4` |
| ~~Born-RED gate for codegen~~ (`proof:codegen-consume`, `codegen-consume-decision.json`) | **NOT AUTHORED** — RETIRED | Owner directive 2026-06-22 |
| ~~P.W4 codegen-consume~~ | **RETIRED TOMBSTONE** — P.W5..P.WZ numbering stable | This slot is kept as a tombstone |

The campaign's perf payload is the **IN-REALM optimizations**: value.js color zero-alloc + flatLeaf
(dispatch to value.js P); kf SoA compositor + Playhead + Typed-OM (P.W2/W3); parse-that correctness
(PT-B1 packrat fix) + Span-dedup (PT-B2) + perf-frontier (PT-B3) (dispatch to parse-that B).

---

## 9. THE CROSS-REPO ASKS + DISPATCH SEQUENCING

| Dispatch | Status at P-open | Key asks / consume gate |
|---|---|---|
| `KF-TO-VALUEJS-P.md` (AUTHORED in kf-P) | **DISPATCH** — value.js P VJ-L1/L3 unblock S8/S9 (O.W16 exit); P.W11 WeakMap is the kf-internal arm | VJ-L1 `fnName?: string`; VJ-L3 `parseCSSSubValue`; VJ-P1 `color2Into`/`mixColorsInto` out-params; VJ-P2 Float64 channel view; grammar-correct cures; VJ-P.W0 record-as-built |
| `KF-TO-PARSETHAT-B.md` (AUTHORED in kf-P) | **DISPATCH** — packrat bug fix (PT-B1) is the correctness BLOCKER; Span-dedup (PT-B2) + perf-frontier (PT-B3) + SpanParser KILL recommendation (PT-B4) | PT-B1 packrat FIX; PT-B2 Span-dedup; PT-B3 perf-frontier; PT-B4 SpanParser KILL. ~~BBNF codegen emitter RETIRED.~~ |
| `KF-TO-GLASSUI-BC.md` (FILED at M, `74b7305`; O.W11 corrected aria ask dispatched) | **HANDOFF** — P.W12 fires on BC cut publish; S2 deletable NOW; S1 GATED on SFC guard | S1 aria SFC guard; S2 dock confirm; N-Stage unshelf (P.W8) |

**The acyclic DAG (phase-ordered):**
```
P.W1 apparatus ─► P.W2 SoA-compositor ─► P.W3 Typed-OM ──────────────────────────────────┐
                                          [P.W4 RETIRED tombstone — codegen out of scope]  │
P.W5 cube+amiga ──► P.W6 sq+spring ──► P.W7 easing+DCP ──► P.W8 N-Stage+mobile ──────────┤
                                                                                           │
P.W9 NaN-frame+fuzz ───────────────────────────────────────────────────────────────────────┤
P.W10 leaves-TRAP ─────────────────────────────────────────────────────────────────────────┤
P.W11 VJ-WeakMap (unblocks O.W7) ──► O.W7 engine-seam (VJ-L1) ────────────────────────────┤
                                                                                           ▼
P.W12 S2-delete-NOW + S1-GATED(BC) ──────────────────────────────────────────► P.WZ close (5.1.x)
          │
          └─ parse-that B (PT-B1/B2/B3/B4) → value.js P 1.1.0 (O.W16 VJ-L1/L3) → S8/S9 exits (O.W16)
```

---

## 10. THE USER-DOMAIN FINALE — named, not asserted done

| Finale motion | Terminal shape | Honest state |
|---|---|---|
| O implementation authorization (O.W1…O.WZ) | **USER-DOMAIN** — P sequences ATOP O; O impl fires first on owner's explicit go | O.md §8 dev→impl boundary |
| Version cut **5.0.0** (O.WZ) | **USER-DOMAIN** — the tree carries 4.3.0; the cut is Mike Babb's | O.WZ `proof:changelog-5.0.0` |
| Version cut **5.1.x** (P.WZ) | **USER-DOMAIN** — P is non-breaking (perf + demo); the cut fires after P bands close | P.WZ |
| npm publishes (kf 5.1.x + value.js 1.1.0/1.2.0 + parse-that 0.12.0) | **USER-DOMAIN** — `npm publish` is the user's hand; each sibling drives to green CI first | per-repo proof:all |
| glass-ui BC cut publish (the tripwire for S1 delete + N-Stage unshelf) | **USER-DOMAIN / sibling-gated** — BC EXECUTION ~60%; CUT at tier 27 pending; the BC cut version is USER-DOMAIN | `AUDIT-DIGEST.md K5` |
| value.js P (VJ-L1/VJ-L3) publish (the tripwire for S8/S9 clean exit + O.W7 engine-seam) | **USER-DOMAIN / sibling-gated** — value.js 1.0.2 is current; P is the dispatched follow-on | `proof:workaround-deletion` S8/S9 tripwires |
| parse-that B (packrat bug fix + Span-dedup + perf-frontier + SpanParser KILL) publish | **USER-DOMAIN / sibling-gated** — parse-that 0.11.0 is current; B is the dispatched follow-on; P.W4 codegen-consume is RETIRED | `KF-TO-PARSETHAT-B.md` dispatch |
| L.W11 TASTE verdict | **USER-DOMAIN-PENDING** — closes only on Mike Babb's "meets the bar" | `docs/frontend-design/taste-packets/l-w11/` |
| `proof:all` GREEN on the consolidated P apparatus | **HANDOFF (P.WZ close-impl)** — closes when all bands A→G green, consume gates fired | P.WZ |

---

## 11. CONFIRMATION: NO PROMPT DROPPED

| Check | Result |
|---|---|
| A→O lineage (chain-trusted to prompt-recap-O.md) | ALL terminal — zero drops (verified); prompt-recap-O.md §12 TOTALS confirm |
| O-session rows (§1–§11 of prompt-recap-O.md) | ALL terminal — zero drops (chain-trusted) |
| F3 BLOCKER — new session intake (optimization+triumvirate+frontend-design-fleet, 2026-06-20, postdates 44e7fae) | **ABSORBED into §3 above** — the only open drop from the 32-lane P audit, NOW CLOSED |
| P audit re-opens / corrections on O close | ALL named with P wave homes (§2 above) |
| P topology / DAG / version split | LOCKED in constitution (§4 above); cited, not re-litigated |
| Dispatch packets (value.js P + parse-that B) | AUTHORED in kf tree (§6 above) |
| Chronic / deferred terminal P-inv-28 | ALL named with P terminals (§7 above) |
| Codegen spine | RETIRED (§8 above, 2026-06-22 owner directive) — A.W3 falsification record stays in AUDIT-DIGEST.md; campaign NON-adoption recorded here; P.W4 tombstone stable |
| USER-DOMAIN finale | ALL named (§10 above) — NONE asserted done |

**Zero drops. Every distinct owner request A→O (chain-trusted to prompt-recap-O.md) reaches a
terminal verdict in `prompt-recap-O.md`. The ONE open drop found by the 32-lane P audit — the new
2026-06-20 session intake (optimization+triumvirate challenge + frontend-design-fleet ask,
postdating O ratification `44e7fae`) — is ABSORBED HERE in §3. The standing 7-clause mandate
(J/J.md:111-119) is cited as the immutable spine; it is not re-litigated. The P tranche topology,
DAG, and version split are locked in the constitution and cited here. inv-16 holds throughout: P
writes only keyframes.js; every cross-repo need is a dispatch. The dev→impl boundary is honored:
this lane wrote zero source, zero tests, zero gate scripts.**

---

## TOTALS

| Category | Count | Status |
|---|---|---|
| Standing-spine MANDATE clauses (§1) | 7 | 7 — ADDRESSED or HONORED |
| A→O lineage (chain-trusted to prompt-recap-O.md) | A→O all | ALL terminal — zero drops; 8 rows RE-OPENED/CORRECTED → named P waves or dispatch homes (§2) |
| NEW session intake — F3 BLOCKER (§3) | 3 sub-directives | ALL ADDRESSED: optimization (§3a) · novel approaches (§3b) · frontend-design fleet (§3c) |
| P topology / DAG / version split (§4) | 5 decisions | 5 LOCKED (cited from constitution) |
| P band/wave map (§5) | 8 bands, 13 waves + WZ | ALL chartered with born-RED gates; phase (NOW/DISPATCH/GATED) assigned per wave |
| Dispatch packets (§6) | 2 | 2 AUTHORED in kf tree (KF-TO-VALUEJS-P.md + KF-TO-PARSETHAT-B.md) |
| Chronic / deferred P-inv-28 terminals (§7) | 9 items | ALL named terminals: O.W5/W6 BUILD-IN (O lane) · P.W11 WeakMap cure (S8/S9 kf-arm) + O.W16 VJ-L1/L3 (S8/S9 clean exit) · P.W12 S1/S2 ONLY (glass-ui aria/dock) · O.WZ close · P.W10 TRAP · parse-that B dispatch · value.js VJ-P.W0 |
| Codegen spine (§8) | — | RETIRED (2026-06-22) — A.W3 falsification record kept in AUDIT-DIGEST.md; P.W4 RETIRED tombstone; campaign NON-adoption recorded in all four P lane docs |
| Cross-repo dispatches + sequencing (§9) | 3 dispatches + DAG | ALL named; DAG acyclic and phase-ordered |
| USER-DOMAIN finale (§10) | 9 | USER-DOMAIN (O impl auth, 5.0.0, 5.1.x, publishes, BC/VJ-P/PT-B tripwires, TASTE) · HANDOFF (proof:all, P.WZ) — NONE asserted done |

**Zero drops. Every request A→O (chain-trusted) and the ONE new P session intake (the F3 BLOCKER,
optimization+triumvirate+frontend-design-fleet, 2026-06-20) reaches a TERMINAL verdict: ADDRESSED
/ HANDOFF / USER-DOMAIN / DISPATCH / FOLD-INTO-P — each cited by a born-RED gate over the REAL
observable, a named wave, a named commit, or a named sibling tripwire. Owner directive 2026-06-22
RETIRES the codegen spine from the campaign; that retraction is recorded consistently across all
four P lane docs (P.md §2/§4/§7, PROGRESS.md P.W4, deferred-ledger-P.md §3/DP-2, this doc §8).
The standing 7-clause mandate is the immutable spine, cited not re-litigated. inv-O-observable-truth
+ inv ε + inv-16 hold throughout.**
