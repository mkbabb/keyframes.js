# Lane 28 — the total prompt recap (A → T-dev): ask ledger · leak analysis · the T recap-gate shape

> **Surface:** the owner's standing demand, re-issued verbatim as the last operative clause of the
> T directive — *"Recap ALL of our prompts and requests hitherto and ensure they've been addressed"*
> (`ORIGINAL-PROMPT.md:110`). Sources: `docs/tranches/S/OWNER-ASKS.md` (4 mid-S rows incl. the
> verdict), `docs/tranches/S/audit/prompt-recap.md` (the S DEV-phase recap substrate),
> `S.md §6` (the recap plan), `waves/S.Z.md` (the S.Z1 gate spec), and the T directive itself.
> **Method (T5):** every claim checked against the tree, not the PROGRESS prose — `ls`/`grep` for the
> recap artifacts, `git log` for what the S.Z band actually ran, the VERDICT catalogue read
> shot-grounded. This lane owns the **meta-recursion**: the ONE band S built specifically to prevent
> "letter-addressed while spirit-leaks" is the band the drive never ran, and the recap that *does*
> exist self-certifies the exact leak the owner then rejected on sight.

## The one-sentence verdict

**The anti-leak instrument never fired and the recap that stands in its place leaks by construction:**
S.Z1's `proof:prompt-recap-s` and its fixed parse target `docs/tranches/S/PROMPT-RECAP.md` **do not
exist on the tree** (the entire S.Z close band is `PENDING-IMPL`, zero commits — pre-empted by the
very rejection it was meant to prevent), so the only recap that ran is a DEV-phase *proposal*
(`audit/prompt-recap.md`) whose "spirit column — the point" grades **every** hard ask ADDRESSED by
citing a **band mapping** (→ S.G, → S.D, → S.E) rather than a verified owner-observable output — and
the owner rejected the output of exactly those bands wholesale; the recap column engineered to catch
the leak *is itself* the leak, and the owner-review-in-the-loop precept the S DEV phase had already
learned (the S.E shelf) leaked again one phase later at full scale.

---

## 1. THE CONSOLIDATED ASK LEDGER — A → T-dev (letter · spirit · T disposition)

The A→Q lineage is exhaustively rowed in `R/audit/retro-prompt-recap.md §2` and re-consolidated in
`S/audit/prompt-recap.md §3`; lane 27 owns the 74-row chronic/deferral fold. **This lane's unique
cut is the standing-precept + S-kickoff-element ledger carried THROUGH the S impl drive to its
landed reality and the owner's live verdict** — the three columns the owner's demand actually wants:
did the *letter* land, did the *spirit* land (owner-observable), and what is T's disposition.

**Legend.** Letter = did the mechanism/wave land on the tree. Spirit = is it addressed against the
owner's observable bar (the VERDICT is the oracle). T disp = where it goes.

### 1a. The immutable standing mandate (the 7-clause block, first captured `J/J.md:111-119`)

| Standing precept | Letter (S impl) | Spirit (owner verdict) | T disposition |
|---|---|---|---|
| **NO quick solutions / NO workarounds — idiomatic, gestalt** | Bands S.B/S.C/S.D ran | **LEAKED** — `useToolbarKeyboard` hand-roll (S.C3b), `KfPillTabs` hand-roll blessed (S.D2), the `gatedSliderDown` bespoke touch-gate (lane 27 F3) all survived; VERDICT #18 "why aren't these just glass-ui components?" | **RE-ISSUED verbatim** `ORIGINAL-PROMPT:67,102` → T (the hand-roll census is a T wave) |
| **architectural transposition for elegance/simplicity/perf** | S.B carve landed (SOUND, lane 24) | **PARTIAL** — library carve sound; VERDICT #19 "performance on every single page is god awful" refutes the perf half | RE-ISSUED `:102` → T (perf-from-ground-up, lane 11) |
| **NO legacy code** | Band S.C (animate.ts, shadcn, SPRING_SMOOTH deleted — SOUND, lane 25) | **RE-ISSUED anyway** — owner does not trust totality; `:59,104` re-demand the full sweep | RE-ISSUED `:59,104` → T (lane 21 owns the residual sweep) |
| **delineate + fold chronic/deferred items** | §4 fold table authored (74 rows) | **LEAKED** — the fold's *re-verification* (S.Z1 S9) never ran; lane 27 finds 52/74 rows unverified, 8 rows false-as-written | RE-ISSUED **TWICE** `:106,108` → T (lane 27's ledger re-run) |
| **Recap ALL prompts — ensure addressed** | **NOT LANDED** — `PROMPT-RECAP.md` absent, `proof:prompt-recap-s` absent (F1) | **LEAKED WHOLESALE** — the recap never ran; the substrate self-certifies (F2) | RE-ISSUED `:110` → **T (THIS lane) — the recap-gate rebuild (F-gate)** |
| **This is NOT an implementation phase** | S was DEV-only at plan; then the owner authorized an IMPL drive (OWNER-ASKS row 3) | **RE-ISSUED `:112`** — the phrase that opens every tranche, re-issued because the "development" tranches keep producing rejected implementations | RE-ISSUED `:112` → T (T is dev-only; the recursion is the finding, F5) |

### 1b. The S-kickoff twelve elements — landed reality + owner verdict

| # | S kickoff element | Band | Letter | Spirit (owner verdict) | T disposition |
|---|---|---|---|---|---|
| 1 | deeper library sub-zoning | S.B | ✓ landed | **SOUND** (below owner sight-line; lane 24) | none — genuinely terminal |
| 2 | demo/app + playground restructure | S.D | ✓ partitioned | **LEAKED** — VERDICT #26 "half baked", "wtf is app/chrome", "90% junk"; organize-not-prune (lane 25 F5) | → T (lanes 13/14/15) |
| 3 | SOTA re-research parse-that + animation | S.F/S.H | ✓ landed | **SOUND** (below sight-line; lane 26) | none (F/H) — but hero per-CHAR reversal → T |
| 4 | bbnf-lang HARD-EXCLUDED | charter §1 | ✓ held | held | none |
| 5 | glass-ui consume-published-only | S.E6→HANDOFF | ✓ tilde-pinned 4.0.1 | correctly open | HANDOFF (BG/BH) |
| 6 | resurrect scene-switcher | S.E | built+converged 100/100 | **SHELVED by owner** — "looks awful" (the S.E lesson, F3) | RECORD (terminal); the *lesson* → T |
| 7 | NO legacy (re-issued) | S.C | ✓ landed | RE-ISSUED (see 1a) | → T |
| 8 | fold ALL chronic+open | §4 | ✓ authored | **LEAKED** — re-verify never ran (lane 27) | → T |
| 9 | **full prompt recap** | S.Z1 | **✗ NEVER RAN** (F1) | **LEAKED** | → **T (THIS lane)** |
| 10 | **mandatory-Fable per page + design routing** | S.G | Fable ran at DEV (10 lanes) + on the SHELVED S.E proto | **LEAKED** — the *shipped* S.G surfaces never got a per-page Fable+owner pass; born-RED oracles proxied for "design done"; VERDICT #16/#24 rejects the output (F3) | RE-ISSUED `:114` → T |
| 11 | 5-step convergence loop | method | ✓ 11×100% | **the convergence certified a rejected demo** (lane 26) | → T (owner-in-loop, F3) |
| 12 | batches of three | method | ✓ | ✓ | carried |

### 1c. The NEW T-verdict asks (first-issued in the live review — the recap must adopt them)

These are not in any prior recap; the T ledger must originate them (VERDICT rows in parentheses):
single-option dock/animation elision (#17); per-CHAR hero uplift, contradicting F.W16's word-split
(#3); **restore the square keyframes/controls/timeline panel — reversing S.G2's honest-collapse**
(#12/#25); cursor-light "done right" (#22); prune motion-path/morph/compose (#23); the itemized
structural asks — `demo/@/styles`, `demo/app/chrome`, `demo/app` junk, `demo/scenes` composition
(#26). Each is owned by a sibling T lane; this ledger's job is to assert **none is dropped**.

---

## 2. FINDINGS

### F1 — The recap's own gate and parse target do not exist; the "ensure addressed" ask is the one ask S structurally scheduled to run LAST and never reached

**Defect.** The owner has demanded a full prompt recap *every tranche since I*. S answered it with a
whole close band and a rewritten born-RED gate. Neither shipped:

```
$ ls docs/tranches/S/PROMPT-RECAP.md            → No such file or directory
$ grep -n "prompt-recap" package.json           → (empty — no proof:prompt-recap-s script)
$ ls scripts/proof-prompt-recap*.mjs            → no matches
```

`PROGRESS.md:77-79` shows **S.Z1 / S.Z2 / S.Z3 all `PENDING-IMPL`**; `git log --oneline --all | grep
S\.Z[123]` returns **zero wave-closure commits** (lane 27 F1 confirms the whole Z band never
executed). `PROGRESS.md:261-262` states it plainly: *"the Z close is BLOCKED pending Tranche T
dispositioning (proof:prompt-recap-s requires OWNER-ASKS row 4 dispositioned)."*

**Root cause.** The recap gate's dependency is **`all bands`** (`S.Z.md:57`, `:110`) and it is coupled
to the S.Z3 from-clean roster run (clause ii binds every ADDRESSED oracle to Z3's exit map). By
construction the recap can only run *after everything else closes* — so a drive pre-empted before
close (as this one was, by the owner's live-review verdict) leaves the recap **never materialized**.
The instrument built to prove "we addressed the asks" is the instrument most guaranteed to be missing
exactly when the asks were *not* addressed. This is the recap analogue of lane 27's Z-void: the
verification lives structurally downstream of the failure it verifies.

**Evidence.** The commands above; `PROGRESS.md:77-79,261-262`; `S.Z.md:57,110,181-187` (the born-RED
witness plan that never fired); lane 27 F1.

**T-wave-shaped recommendation.** See T-1 — the recap ledger must be born at drive ENTRY and updated
per-wave, never gated only at close.

---

### F2 — The recap that DOES exist self-certifies: its "spirit column — the point" grades every hard ask ADDRESSED by citing a BAND, not an owner-observable output

**Defect.** `S.md §6` principle 5 is emphatic: *"The spirit column is the point. A recap that lists
letter-compliance while the same mechanism leaks (r1's headline) fails S's own bar."* Yet in the
substrate that implements it (`audit/prompt-recap.md`), **every** spirit-disposition on a hard ask
resolves to a *band mapping* — the wave that is *supposed* to address it — never to a verified result:

- `prompt-recap.md:82` — `idiomatic → **BAND S.B**`
- `:84` — `gestalt → **BAND S.D** + **S.B**`
- `:86` — `design (routed via Fable) → **BAND S.G**`
- `:89` — `NO legacy → **BAND S.C**`
- `:114` — the NO-workaround precept → "spirit-gap → **BAND S.B + S.C**"

The spirit column asks "what would falsify *addressed*" and answers "a band exists that will address
it." That is **precisely the letter-compliance the principle was written to forbid** — the leak
merely moved up one level of abstraction, from "the ask" to "the band that owns the ask." The proof:
the owner rejected the *output* of S.G (the rejection epicentre — lane 26), S.D ("half baked" —
VERDICT #26), and S.E (shelved). The three bands the recap cites as its spirit-oracles are the three
bands that produced the rejected demo. **The anti-leak column certified the leak.**

**Root cause.** A band mapping is a *plan* artifact; an owner verdict is an *output* artifact. The
recap bound its spirit column to the plan (which the drive controls and can always satisfy) instead
of the output (which only the owner can grade). Nothing in `proof:prompt-recap-s`'s clause set
(`S.Z.md:166-177`) requires the ADDRESSED oracle to be an *owner-observed* artifact — clause (ii)
accepts "a real gate or live-observation artifact" green in the Z3 roster, and every S.G gate was
green (lane 26: "green was the defect"). A band that ships a green born-RED oracle satisfies the recap
even when the owner rejects what the oracle greened.

**Evidence.** `S.md:951-954` (the principle); `prompt-recap.md:82,84,86,89,114`; `S.Z.md:166-177`
(clause ii accepts a green gate as the spirit oracle); lane 26's "green was the defect" divergence
taxonomy.

**T-wave-shaped recommendation.** See T-1/T-2 — the spirit column must cite an **owner-observed token**
(a shot + APPROVED/REJECTED verdict), and a green gate may NOT stand as a spirit oracle for a
design/appearance ask.

---

### F3 — The owner-review-in-the-loop precept leaked in the SAME tranche it was learned; design precept #10 was applied only to the shelved prototype, never to the shipped surfaces

**Defect.** The S DEV phase produced the lesson, recorded in MEMORY verbatim: *"S.E scene-stage:
built+converged 100/100 at pass 3, then owner live-review ruled it SHELVED ('looks awful') — critic
consensus ≠ owner verdict, put the owner review inside the design loop."* Then the S IMPL drive ran
**30 waves + 10 fan-outs entirely on instrument convergence** (born-RED oracles + 11×100% critic
percentages) with **zero running-demo owner checkpoints**. The mandatory-Fable design routing
(kickoff #10) fired at DEV time (`PROGRESS.md:337` "10 Fable design lanes") and on the *shelved* S.E
prototype (`:350` "SceneStage design loop converged 100/100 over a 3-round Fable loop") — but the
S.G waves that actually **shipped** the demo the owner rejected discharged **per-item born-RED
oracles** (`PROGRESS.md:70`, S.G2) with no per-page Fable pass and no owner review before "CLOSED."

The lesson was re-surfaced into the T resume anchor **only after the rejection**: `PROGRESS.md:282-283`
— *"owner-review-in-the-loop BEFORE folding design into waves — the S.E lesson."* It is written down
as a thing to do *next time*, one phase after it should have governed *this* time.

**Root cause.** The precept "put owner review inside the design loop" was captured as a *narrative
lesson* (MEMORY, user-domain, non-gated — `S.Z.md:152` explicitly lists MEMORY as observe-tier) rather
than as a *wave-closure gate*. A design wave (S.G1/G2/G3) could reach CLOSED on critic-convergence +
green oracle alone; nothing structurally required an owner (or owner-proxy live-review) token. So the
most recently-taught, most expensively-learned precept in the entire tranche had **no enforcement
surface** and leaked at the first opportunity, at full scale.

**Evidence.** MEMORY "Tranche S DEVELOPED" (the S.E lesson); `PROGRESS.md:70` (S.G2 closes on named
oracles), `:282-283` (the lesson re-surfaced post-rejection), `:337,350` (Fable ran at DEV / on the
shelved proto); VERDICT #16/#24 (the shipped design rejected); lane 26 L2 (the critic-rubric locus).

**T-wave-shaped recommendation.** See T-2 — an owner-review token is a **wave-closure precondition**
for every design/appearance wave, not a close-band afterthought and not a MEMORY note.

---

### F4 — The N-Stage 4×-rejection was the owner's taste-bar SIGNATURE; the recap logged each rejection as a per-feature ruling and never generalized it into a standing precept

**Defect.** The oldest first-class ask (the scene-switcher) was rejected by the owner **four times**
with escalating language, all captured in the recap corpus (`prompt-recap.md:135-139`): *"completely
awful"*, *"still awful"*, *"still awful … totally and unmeasurably wrong"*, *"shelf this idea …
Going nowhere."* The recap dispositioned this as `ADDRESSED-with-ruling (SHELVED)` — a clean
per-feature terminal. But the *shape* of those four rejections — **the owner rejects on taste what the
process passes on convergence** — is exactly the shape of the whole-demo rejection that followed. The
recap had the four data points and read them as a closed feature story, missing that they were a
**standing, un-instrumented precept about the owner's bar itself**.

**Root cause.** The recap's taxonomy has no class for "a rejection pattern that generalizes." Every
row is dispositioned as a discrete ask → a discrete wave/KILL/HANDOFF. A recurring *correction shape*
(the same critic-consensus-vs-owner-verdict gap, hit repeatedly) has no home and gets filed as N
independent rulings, so its recurrence is invisible in the ledger even though r1 literally counted
the instances.

**Evidence.** `prompt-recap.md:135-139` (the four verbatim rejections + the SHELVED disposition); the
VERDICT (the same rejection applied whole); MEMORY "put the owner review inside the design loop"
(the lesson the pattern implied but the recap did not encode).

**T-wave-shaped recommendation.** See T-3 — the T recap adds a **recurring-correction-shape** row
class that promotes a rejection pattern into a standing precept with its own gate once it recurs ≥2×.

---

### F5 — The re-issuance census: the 7-clause mandate is re-issued VERBATIM into T (its ~9th consecutive tranche); six specific S-band precepts are re-issued because the owner does not trust their spirit landed — the re-issuance IS the spirit-status

**Defect.** `ORIGINAL-PROMPT.md:100-112` is a word-for-word re-issue of the `J/J.md:111-119` standing
mandate — carried through K, L, M, O, P, Q, R, re-issued in the S kickoff (`prompt-recap.md:49`), and
now re-issued again into T. **The re-issuance is the falsification**: if a precept were spirit-addressed
to the owner's bar, the owner would not need to say it again. The T directive re-issues, on top of the
standing block, six specific precepts each S band claimed ADDRESSED:

| Re-issued precept | T line | S band that "addressed" it | Why re-issued (spirit-status) |
|---|---|---|---|
| legacy/workaround/fallback sweep | `:59,104` | S.C (landed, SOUND per lane 25) | owner does not trust totality |
| NO god modules >500L | `:65` | S.B5 (≤460L tripwire; 483L/492L survivors, lane 24 F5) | over the plan's own tripwire |
| glass-ui components when befitting | `:98,55` | S.G/S.D | KfPillTabs + useToolbarKeyboard hand-rolls survived (VERDICT #18) |
| Fable for ALL design | `:114` | S.G (kickoff #10) | shipped design rejected (F3) |
| fold ALL deferred (×2) | `:106,108` | §4 fold table | re-verify never ran (lane 27) |
| recap ALL prompts | `:110` | S.Z1 | never ran (F1) |
| **"This is NOT an implementation phase"** | `:112` | every tranche's opener | the "dev" tranches keep shipping rejected impls — the deepest recursion |

**Root cause.** No recap in the chain has ever *measured re-issuance*. Each tranche's recap grades the
current asks and moves on; the fact that the owner is repeating himself — the single strongest signal
that the spirit did not land — is nowhere in the ledger's columns. The spirit column was invented in S
precisely to catch this, and F2 shows it self-certified instead.

**Evidence.** `ORIGINAL-PROMPT.md:59,65,98,100-112,114`; `J/J.md:111-119` via `prompt-recap.md:47-59`;
lane 24 F5 (the 483L/492L survivors); lane 25 F2 (the hand-roll); lane 27 (the un-run fold).

**T-wave-shaped recommendation.** See T-3 — the T recap-gate greps the current directive against the
prior tranche's directive and **auto-REDs the ADDRESSED claim of any verbatim-re-issued precept** until
a new owner observation clears it.

---

### F6 — OWNER-ASKS row 4 is a circular deadlock: the recap gate that blocks the S close depends on dispositioning the verdict that opened T — the T recap must break the loop, not inherit it

**Defect.** `OWNER-ASKS.md:12` (row 4, the verdict) is `IN EXECUTION`, and its own disposition text
says the S close is *"BLOCKED pending this dispositioning."* `proof:prompt-recap-s`'s S6 clause
(`S.Z.md:148-151`, `:176`) REDs unless `OWNER-ASKS.md` is fully-dispositioned. So: the S recap gate
can never green because its final undispositioned ask **is the tranche (T) that supersedes the recap
gate itself**. S cannot close its recap until T dispositions row 4; T *is* the disposition of row 4.
A recap that inherits this coupling can never terminate.

**Root cause.** `OWNER-ASKS.md` was designed as an *append-and-block* laundering guard (`S.md §6`
principle 4) — correct for mid-tranche asks, but it has no exit for an ask that is itself a
*tranche-scope pivot*. A verdict that says "stop and open a new tranche" is not dispositionable by a
wave in the tranche it stops; it can only be dispositioned by the successor.

**Evidence.** `OWNER-ASKS.md:12`; `S.Z.md:148-151,176`; `PROGRESS.md:261-262`.

**T-wave-shaped recommendation.** See T-1 — the T ledger dispositions the entire S residue (S.A3 +
S.Z1/Z2/Z3 + drag-gesture + the fold carries + OWNER-ASKS row 4) as **explicit CARRY-INTO-T rows with
their own T-gate**, closing S's recap by *transfer* (a named successor home) rather than by an
in-S re-run that can never happen. Row 4's disposition becomes "→ Tranche T, dispositioned by this
ledger," which is terminal.

---

## 3. THE T RECAP-GATE SHAPE (the deliverable)

Every prior recap gate measured **letter** (row-shape) and bolted on a **spirit column** that
self-certified via band-mappings (F2). The T recap-gate must bind spirit to an **owner-observable
artifact** and must be **born at entry, not deferred to close** (F1/F6). Concretely:

1. **Born-at-entry, updated-per-wave.** `docs/tranches/T/PROMPT-RECAP.md` materializes at drive ENTRY
   (skeleton with all rows), and every wave that touches an ask updates its row at close. A drive
   pre-empted at any point still has a real, current recap — the F1 failure mode is structurally
   impossible.
2. **The spirit column cites an OWNER-OBSERVED token, not a band.** Each ask's spirit-status ∈
   {`OWNER-APPROVED shot:NN`, `OWNER-REJECTED shot:NN → T-wave`, `PENDING-OWNER`}. A band mapping is a
   *letter* artifact and lives in the letter column only. **A green gate may NOT stand as a spirit
   oracle for any design/appearance/interaction ask** (the direct cure for F2 and lane 26's "green
   was the defect").
3. **The re-issuance census clause.** The gate greps the current owner directive against the prior
   tranche's directive corpus; **any verbatim-re-issued precept AUTO-REDs its `ADDRESSED` claim** until
   a new owner observation clears it (F5). The owner repeating himself is a machine-detectable
   falsification.
4. **The recurring-correction-shape class.** A rejection pattern that recurs ≥2× (the N-Stage 4×
   signature; now the whole-demo) is promoted from N discrete rulings to **one standing precept with
   its own gate** (F4).
5. **Owner-review is a WAVE-closure precondition, not a close-band gate.** Every design/appearance
   wave carries an owner (or owner-proxy live-review, the pass-3 chrome-devtools-mcp pattern) token as
   a born-RED closure oracle — critic-convergence + green born-RED oracle is **necessary but not
   sufficient** to close a design wave (F3). This is the S.E lesson made structural.
6. **The S-residue transfer.** The ledger dispositions S.A3 + S.Z1/Z2/Z3 + drag-gesture + lane 27's
   fold carries + OWNER-ASKS row 4 as explicit CARRY-INTO-T rows, closing S's recap by named transfer
   (F6).

**Non-vacuity (both ways).** (a) A spirit-status of `ADDRESSED` citing a green gate for a design ask
→ REDs (the F2 plant). (b) A verbatim-re-issued precept marked `ADDRESSED` with no post-re-issuance
owner token → REDs (the F5 plant). (c) A design wave marked CLOSED with no owner-review token → REDs
(the F3 plant). (d) An OWNER-ASKS append with no disposition → REDs (inherited S6 clause). (e) The
ledger absent at drive entry → REDs (the F1 plant).

---

## 4. The T fold table (recap-band residue)

| # | Item | S disposition | Truth (this lane) | T disposition |
|---|---|---|---|---|
| R1 | `PROMPT-RECAP.md` + `proof:prompt-recap-s` | S.Z1 born-RED | **never materialized** (F1) | **T-1** — born-at-entry rebuild |
| R2 | the spirit column | "the point" (`S.md §6`) | self-certifies via band-mappings (F2) | **T-1/T-2** — owner-token binding |
| R3 | owner-review-in-loop | MEMORY lesson (observe-tier) | leaked same-tranche (F3) | **T-2** — wave-closure gate |
| R4 | N-Stage 4× rejection | per-feature SHELVED ruling | ungeneralized taste signature (F4) | **T-3** — correction-shape class |
| R5 | re-issuance of the 7-clause mandate | not measured by any recap | ~9th verbatim re-issue (F5) | **T-3** — re-issuance census |
| R6 | OWNER-ASKS row 4 | IN EXECUTION / blocks S close | circular deadlock (F6) | **T-1** — CARRY-INTO-T transfer |
| R7 | S.Z2/Z3 (template + FINAL) | PENDING-IMPL | never ran (lane 27 F1) | carried with T's own close discipline |
| R8 | the NEW T-verdict asks (§1c) | not in any recap | first-issued in the live review | **T-1** — the ledger originates them |

---

## T recommendations

1. **Rebuild the recap as a born-at-entry, owner-token-bound ledger (`docs/tranches/T/PROMPT-RECAP.md`)
   that transfers the S residue by name.** Scope: materialize the ledger at drive ENTRY with all rows
   (the standing mandate, the S-kickoff elements carried through their landed reality, the NEW
   T-verdict asks §1c, and the S-residue carries R1/R6/R7); the spirit column cites an owner-observed
   token (`OWNER-APPROVED shot:NN` / `OWNER-REJECTED → wave` / `PENDING-OWNER`), never a band; each
   wave updates its row at close; disposition OWNER-ASKS row 4 as `→ Tranche T (this ledger)` to break
   the F6 deadlock. Falsifiable gate: `proof:prompt-recap-t` REDs if the ledger is absent at entry, if
   any `ADDRESSED` row cites a *band* (not an owner token) as its spirit oracle, if any
   design/appearance ask cites a green gate as its spirit oracle, or if OWNER-ASKS is not
   fully-dispositioned; plant one row of each shape and confirm each REDs independently. Size: **M**.

2. **Make owner-review a wave-closure precondition for every design/appearance wave (the S.E lesson,
   structural).** Scope: every design/appearance/interaction wave carries an owner-or-owner-proxy
   live-review token (the pass-3 chrome-devtools-mcp + live-shot pattern) as a born-RED closure oracle;
   critic-convergence + green born-RED oracle is explicitly declared *necessary but not sufficient* to
   reach CLOSED; the token is recorded in PROGRESS with its shot. Falsifiable gate: a design wave
   marked CLOSED without a paired owner-review token REDs (`proof:owner-review-gate`); plant a
   token-less CLOSED design wave and confirm RED. Size: **S**.

3. **Add the re-issuance census + the recurring-correction-shape class to the recap-gate.** Scope: the
   gate greps the current owner directive corpus against the prior tranche's directive and auto-REDs
   the `ADDRESSED` claim of any verbatim-re-issued precept until a post-re-issuance owner token clears
   it (F5); the ledger gains a row class for rejection patterns recurring ≥2× that promotes them to a
   standing precept with a named gate (the N-Stage 4× → whole-demo signature, F4). Falsifiable gate:
   `proof:prompt-recap-t` REDs on a precept present verbatim in both directives yet marked `ADDRESSED`
   with no newer owner token; plant a re-issued+ADDRESSED precept and confirm RED. Size: **S**.
