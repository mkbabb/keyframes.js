# T.D11 — TASTE-VERDICT (the home deck voice — the serif-italic ramp)

> The born-OWNER deck-voice wave (OD-4). The deck voice was the contested
> design fork between two audit lanes (09: Jakarta body 500 non-italic · 01:
> Instrument Serif true italic 400 — T.D.md charter-conflict note 2), resolved
> the S.E way: surfaced UPSTREAM to the owner, who approved the P-HERO
> composition CARRYING the serif-italic deck. This packet records the
> production landing + the one rung deviation, for the mid-drive re-verify.

## The owner token (the OD-4 ruling this wave's green encodes)

**Owner (mkbabb), 2026-07-05: "APPROVED"** — the P-HERO live review (the deck
rendered serif-italic inside the approved composition; OWNER-DECISIONS.md OD-4:
**APPROVED**; blessed reference branch `worktree-wf_1e744f4d-2bb-1`).

## Packet — the landed deck (this drive, production)

Captures: `shots-TD-hero/home-{light,dark}-{1440x900,390x844}.png` (the deck
under the poster in all four).

- **The voice:** both deck lines (`.hero-deck` "from the list ☰ below, then
  press Play." + `.hero-hint` "or drag M. cubert") resolve Instrument Serif
  TRUE italic 400 — the `ital@1` face already loaded, zero new payload; the
  bold-italic system-sans register ("AI-slop subtitle" under a 177px serif) is
  dead. No computed weight above 400 anywhere on the start screen.
- **The lockstep:** the `italic` utility classes stacked on
  `text-heading`/`text-subheading` died with the swap (no manual weight/style
  stacking on a semantic utility survives — proof:hero-deck-voice clause (c)).
- **The italic axis:** T-TY9 (drop the `:ital@0;1` axis) resolves KEEP — the
  deck USES the italic face (the conditional deferral in T.D.md discharges on
  this branch).
- **The oracle:** `proof:hero-deck-voice` (OWNER — authored post-token per
  T.M2, GREEN on the landed tree). The font-tuple census carries the new
  `hero-deck`/`hero-hint` roles (font-roles.json, T.D1's manifest extended, not
  fought).

## Deviations from the P-HERO reference (named explicitly)

1. **The hint stepped UP one rung (heading → title).** P-HERO's ramp was mega
   roman → title italic → heading italic muted; the landed T.D2 serif floor
   (an INSTRUMENT gate post-dating the prototype: Instrument Serif never below
   the display floor — 28px desktop / 24px mobile, proof:font-census clause b)
   makes the 25.9px heading rung serif-ILLEGAL. Landed ramp: mega roman →
   title italic (foreground 0.85) → title italic MUTED — ink strength carries
   the deck→hint step instead of size. One family, two styles, honest rungs.
2. **The phone clamp floors rose to 1.5rem (24px)** (P-HERO: 1.25rem) — the
   same serif floor at the mobile viewport.

## Verdict

**Owner (mkbabb), 2026-07-05: "APPROVED"** (the OD-4 token, carried from the
live P-HERO review; the landed deck keeps the approved voice — the rung
deviation is forced by the owner-approved OD-6/T.D2 honest-serif floor and is
named above for the re-verify.)

Disposition: APPROVED
Reference: OD-4 (OWNER-DECISIONS.md) · P-HERO branch `worktree-wf_1e744f4d-2bb-1` ·
landed captures `shots-TD-hero/` · oracles proof:hero-deck-voice (OWNER) +
proof:font-census roles hero-deck/hero-hint (T.D1 manifest).
