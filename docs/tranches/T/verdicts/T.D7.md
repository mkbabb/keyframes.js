# T.D7 — TASTE-VERDICT (the ONE oklch violet accent authority — the applied theme)

> The born-OWNER theme wave (OD-6). The direction + ramp were reviewed LIVE by the
> owner on the P-THEME prototype (:5183 vs the :5180 baseline) and APPROVED; this
> packet records the PRODUCTION application of that blessed reference (T.D7 + the
> composite look of T.D2/T.D3/T.D4 riding it) with the applied-state captures and
> every deviation from the prototype named.

## The owner token (the OD-6 ruling this wave's green encodes)

**Owner (mkbabb), 2026-07-05: "Good."** — verbatim, on the live P-THEME review
(OWNER-DECISIONS.md OD-6: **APPROVED**; blessed reference branch
`worktree-wf_1e744f4d-2bb-3` @ cc3e64d, capture packet
`audit/prototypes/p-theme/`).

## Packet — the applied theme (this drive, production)

Captures (BUILT dist, 1440×900, light + dark): `shots-TD-theme/{home,cube,spring,easing}-{light,dark}-1440x900.png`

- **The red-kill (RULED #16), applied at the token root:** `--color-progress` /
  `--primary` / `--ppmycota-primary` ride ONE violet authority
  (`--accent-kf: light-dark(oklch(0.56 0.17 295), oklch(0.74 0.13 305))` + the
  strong/subtle/foreground companions — the P-THEME ramp verbatim);
  `--color-slider-track` neutralized (border-derived, not a red twin);
  `--accent-red` returns to DESTRUCTIVE-ONLY with its hue pinned
  (proof:crayon-preserved). The KeyframeCard delete-X moved off the raw Tailwind
  `red-500` onto the destructive register (`data-destructive`-marked).
- **Honest display weight (RULED #24):** `font-synthesis: none` at :root; the
  `text-display-*` rungs resolve Instrument Serif's TRUE 400 at neutral tracking
  (the BG-6 demo-side `@layer` override until glass-ui ships
  `--font-display-weight`); the `650` magic weight and the seq-row serif-600 are
  dead.
- **Jakarta body (RULED):** the I.W6 system-stack reclaim is DELETED — glass-ui's
  bundled Plus Jakarta Sans flows to every body/controls/dropdown/dock surface;
  the K.W2 `.dock-label` serif force is deleted (the serif chrome flip is dead).
- **Mono demoted to data (RULED):** control labels, tabs/pills, buttons, section
  headers, gesture prose, tooltips, legends, status badges → the Jakarta label
  register; mono survives ONLY as contract-bound data (tabular numerals, code,
  identifier chips) — desktop mono leaves 95 → 62, ceiling 70 declared.
- **The oracles:** `proof:accent-census` (OWNER authority — the blessed hue
  window [280°,330°] both themes + the rendered red-census) and the T.D1
  role-bound tuple census (`proof:font-census` + `font-roles.json`) are GREEN on
  the applied tree; `proof:crayon-preserved` keeps the crayons + the pinned
  destructive red hue-exact.

## Deviations from the P-THEME reference (named explicitly)

1. **The hero ink is still `--primary`-violet** (the legacy `.depth-text` stamp
   recolors the h1 — visible in `home-*.png`). The honest-ink correction
   (`--foreground`, de-glow, per-char rebirth) is **T.D10/T.D9's** RULED work in
   the hero batch (⑤) riding the P-HERO reference — NOT silently absorbed here.
2. **The home deck voice is untouched** (still the italic sub-header): the deck
   is **OD-4/T.D11's** call (P-HERO carries a serif deck; P-THEME carried a
   Jakarta deck) — resolved by the hero batch, not pre-empted here.
3. **The typing-card egg survives** (its red dot/caret now ride the violet
   authority): its EXCISION is **T.D12** (RULED removal, batch ⑤).
4. **`.seq-row-name`** (sequence channel numbers) inherits the mono data voice
   rather than P-THEME-era serif-600 (an honest-weight consequence — the serif
   ships no 600; numerals are data).
5. **glass-ui-owned mono voices consumed as-shipped:** the SelectTrigger
   `fira-code` value default (CSS-keyword values) + the MetricBadge telemetry
   label — allowlisted in the mono contract as consumed contracts, never
   demo-authored chrome.

## Verdict

**Owner (mkbabb), 2026-07-05: "Good."** (the OD-6 token, carried from the live
P-THEME review; the applied state grafts that blessed reference production-grade,
deviations 1–3 owned by the named later waves.)

Disposition: APPROVED
Reference: OD-6 (OWNER-DECISIONS.md) · P-THEME branch `worktree-wf_1e744f4d-2bb-3` ·
applied captures `shots-TD-theme/` · oracles proof:accent-census (OWNER) +
proof:font-census (T.D1 tuple) + proof:crayon-preserved (keeper lock).


## OWNER VERDICT — FILLED 2026-07-06

> **Token (verbatim):** “Ratify all with your best judgment. We shall adopt the glass-ui drawer, but ensure that we identify any gaps in that implementation and forward any and all glass-ui suggestions to that working agent's tranche execution (with the exhortation to research, plan, and fold into our running BG/BH wave set--no prefunctory implementation)”
>
> **Disposition:** RE-CONFIRMED under the blanket ratification (the applied theme matches the OD-6 blessing).
