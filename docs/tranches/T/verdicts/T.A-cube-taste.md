# T.A-cube-taste — TASTE-VERDICT

> The ONE cube-restage taste packet (T.A lane 02). It bundles the two aesthetic
> dispositions the T.A cube waves carry — everything else in T.A1…T.A5 is
> objective correctness with its own born-RED oracle (proof:cube-silhouette,
> proof:cube-settle's ≤1-sign-change clause, proof:cube-relight-writes). Per T.M2
> (the S.E lesson: critic consensus ≠ owner verdict) these two taste values carry
> NO standalone born-RED oracle keyed on the exact number/choice — they ride this
> capture sign-off. Verdict PENDING-OWNER.

## Packet

The before/after review packet (cured-cube captures + the two taste deltas):

- **Cured cube captures.** `shots-02-cube/02-cube-cured-filter-off.png` — the die
  with the `--spin-energy` bloom deleted: all six faces render in 3D (was one
  face + 15px slivers, verdict #1). Geometry honest (`.cube` = side×side, was
  0 × 450px). Stage stripped: no rx/ry/rz attitude readout, no face axis tags, no
  gesture legend, no rainbow-wrapper corpse (rulings #5/#8).

### Delta 1 — the axis-lock line REST opacity (T.A2)

- **Was:** `opacity: calc(0.75 + --axis-active·0.25)` — the three CubeAxisLines sit
  at the loud 0.75 register at rest, competing with the die.
- **Now:** `opacity: calc(0.45 + --axis-active·0.55)` — the resting grid is quiet
  (0.45) so only the LOCKED axis speaks (still lifts to FULL 1.0 opacity + its
  colored drop-shadow bloom while X/Y/Z is held). The taste call is the 0.45 rest
  value; the lock-contrast behavior is unchanged.

### Delta 2 — the settle-motion easing (T.A3)

- **Was:** the graph intro ran `easeInBounce` — a bounce-IN that jitters backwards
  at the start and rings through several overshoot sign-changes before rest.
- **Now:** `ease-out-back` (~650ms) — the scene's ONE settle language, matching the
  roll egg's landing: a single gentle overshoot into the opening attitude
  rotate3d(-1,1,0,30deg). The ≤1-overshoot-sign-change property is OBJECTIVE
  (proof:cube-settle); the CHOICE of `ease-out-back` over other single-overshoot
  settles is the taste call recorded here.

## Verdict

**Owner (___), ____-__-__: "___"**

Disposition: PENDING-OWNER
Reference: shots-02-cube/02-cube-cured-filter-off.png ; CubeAxisLines.vue:51 (0.45) ; useCubeDemo.ts changeGraphPerspectiveAnim (ease-out-back, 650ms)


## OWNER VERDICT — FILLED 2026-07-06

> **Token (verbatim):** “Ratify all with your best judgment. We shall adopt the glass-ui drawer, but ensure that we identify any gaps in that implementation and forward any and all glass-ui suggestions to that working agent's tranche execution (with the exhortation to research, plan, and fold into our running BG/BH wave set--no prefunctory implementation)”
>
> **Disposition:** RATIFIED AS LANDED — axis-line rest opacity 0.45 + the ease-out-back settle (one settle language; only the locked axis speaks).
