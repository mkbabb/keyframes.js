# T.A11 — TASTE-VERDICT (amiga transient-ω decay badge — existence + placement)

## Packet
- **Before:** a permanently-parked, hand-rolled `.amiga-telemetry` readout showing
  "ω 0.00 rad/s" FOREVER at rest (a re-implementation of glass-ui's
  MetricBadge/AnimatedDigit), occluded by the mobile sheet at 390.
- **After (this drive):** the parked readout is DELETED (correctness, RULED —
  landed). The decay() dogfood is witnessed through a NON-DOM sampling hook on the
  gesture layer (`window.__kfAmigaProbe.omega()`), so proof:amiga-decay-visible
  clause (1) is re-armed (RE-ARMED, green: peak 4.24 → tail 0.15 rad/s) WITHOUT any
  on-stage readout. Per T.M4 default (no un-manifested chrome), nothing replaces it.
- **Deltas claimed:** the dishonest always-present readout is gone; the decay coast
  is still witnessed (non-DOM); no bespoke on-stage element re-introduced.

## Verdict
**Owner (___), ____-__-__: "___"**

Disposition: PENDING
Reference: lane 03 rec 6 (T-AM6). The EXISTENCE of a transient glass-ui MetricBadge
(value ω, top-right, mounted only while |ω| > 0.05) is a T.M2 owner disposition — no
VERDICT line touches the ω readout, so the existence question is genuinely
owner-open (S.G lesson: no bespoke on-stage readout rebuilt without owner sign-off).
Default per T.M4 = removal, no replacement. No born-RED existence oracle is authored
ahead of the token; this packet records the option for the owner review.
