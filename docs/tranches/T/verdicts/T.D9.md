# T.D9 — TASTE-VERDICT (the hero φ-band seat + the two-focal home composition)

> The born-OWNER hero wave (OD-4). The whole home COMPOSITION — the φ-band seat,
> the two-focal balance (hero over die), the ink-on-graph-paper direction — was
> reviewed LIVE by the owner on the P-HERO prototype (:5181 vs the :5180
> baseline) and APPROVED; this packet records the PRODUCTION landing of that
> blessed reference (T.D9 + T.D10 riding it) with the landed-state captures and
> every deviation from the prototype named. The landed capture set below rides
> the packet for the owner's mid-drive re-verify at review (the born-OWNER
> capture discipline — a re-look is invited, the token already binds).

## The owner token (the OD-4 ruling this wave's green encodes)

**Owner (mkbabb), 2026-07-05: "APPROVED"** — the P-HERO live review, no
objection to the composition; the aurora subtlety amendment (OD-2) is the only
rider (OWNER-DECISIONS.md OD-4: **APPROVED**; blessed reference branch
`worktree-wf_1e744f4d-2bb-1` @ 88bde55, capture packet
`audit/prototypes/` P-HERO).

## Packet — the landed hero (this drive, production)

Captures (BUILT dist, light + dark × 1440×900 + 390×844):
`shots-TD-hero/home-{light,dark}-{1440x900,390x844}.png`

- **The φ-band seat (VERDICT #3 "lower on the page, more towards the centre"):**
  the start-screen block anchors at `--work-area-top-offset + 0.45 ×
  --work-area-height` (0.52 below lg), left at the page gutter — measured
  h1.top 398px @1440×900 / 412px @375×812 (the rejected header hero parked at
  y≈21). NO raw vh/px seat (the K.W3 M4/C5 ban holds; proof:hero-two-focal
  clause (d)).
- **Two-focal, overlap WELCOME:** hero∩die = 68,986px² desktop / 17,590px²
  mobile — the ink PRINTS over the die deterministically (the start-screen
  overlay stepped z-content → z-controls; the mobile word-behind-the-face
  defect is structurally dead). Hero∩dock = 0 on both viewports.
- **The stage is clean:** zero typing-card / kf-source-egg / gallery-door
  furniture on home (T.D12 executed; proof:hero-two-focal clause (c) asserts
  the absence; the owner-sanctioned manifest `stage-manifests/home.json`
  re-derived against this landed set).
- **The oracles:** `proof:hero-two-focal` (OWNER — authored post-token per
  T.M2, GREEN on the landed tree) is the machine-witnessed MIGRATION successor
  of the three retired FROZEN locks `proof:hero-rung`/`-balance`/`-cls`
  (gate-bands.mjs DISCHARGE); `proof:appearance-suffusion` clause (c) re-spec'd
  from hero∩cube==0 (the overturned lock) to the dock-clearance + φ-floor +
  print-over contract; `proof:demo-usability` clause 2 re-armed to the per-char
  contract (T.D10).

## Deviations from the P-HERO reference (named explicitly)

1. **The mobile die seat is unchanged** (CubeScene's `--start-hero-band` 34dvh
   recede is T.A's surface, not this wave's): at 390×844 the hero overlaps the
   die's TOP quadrant rather than P-HERO's die-upper/hero-under split. The ink
   prints over it (z-controls) and OD-4 rules overlap welcome; if the owner
   wants the P-HERO die-raised mobile balance, that is a CubeScene seat move
   (T.A) — flagged, not silently absorbed.
2. **The aurora is DEFAULT-mounted** (P-HERO gated it behind `?light=1` as a
   review lever): the OD-2 ruling blessed the light fork AS the default; the
   toggle died with the prototype (proof:cursor-light-subtle clause (c)).
3. **Deck/hint rungs** — see the T.D11 packet (the serif-floor deviation).

## Verdict

**Owner (mkbabb), 2026-07-05: "APPROVED"** (the OD-4 token, carried from the
live P-HERO review; the landed state grafts that blessed reference
production-grade, deviations 1–3 named — deviation 1 owned by T.A if the owner
asks at re-verify.)

Disposition: APPROVED
Reference: OD-4 (OWNER-DECISIONS.md) · P-HERO branch `worktree-wf_1e744f4d-2bb-1` ·
landed captures `shots-TD-hero/` · oracles proof:hero-two-focal (OWNER) +
proof:demo-usability clause 2 (per-char) + proof:appearance-suffusion clause (c)
(re-spec'd) + proof:stage-inventory home manifest (re-derived).
