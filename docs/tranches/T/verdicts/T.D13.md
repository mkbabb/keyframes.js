# T.D13 — TASTE-VERDICT (the cursor light — Aurora-on-hero, MORE SUBTLE)

> The born-OWNER cursor-light disposition (OD-2). The owner ruled option A
> ("Aurora on hero") and AMENDED it same-day after the live P-HERO review:
> "I like the aurora, but more subtle" — verbatim. The prototype's
> opacityCeiling 0.15 is therefore the CEILING, not the target; this packet
> records the production landing at the subtler bound, encoded in the oracle,
> for the mid-drive re-verify.

## The owner token (the OD-2 ruling this wave's green encodes)

**Owner (mkbabb), 2026-07-05: "Aurora on hero" → "I like the aurora, but more
subtle"** — verbatim (OWNER-DECISIONS.md OD-2: **RULED AURORA-ON-HERO, AMENDED
MORE SUBTLE**; the `?light=1` P-HERO fork blessed as DEFAULT with the subtlety
amendment; blessed reference branch `worktree-wf_1e744f4d-2bb-1`).

## Packet — the landed light (this drive, production)

Captures: `shots-TD-hero/home-{light,dark}-{1440x900,390x844}.png` (the wash is
the faint violet tint IN the graph paper — compare against the P-HERO
`audit/prototypes/` captures at 0.15 for the presence delta).

- **The subtlety amendment, encoded:** `HERO_AURORA_OPACITY_CEILING = 0.1`
  (HeroAurora.vue) — one-third below the prototype's 0.15 wall;
  `proof:cursor-light-subtle` (OWNER) asserts the literal, the binding, AND
  the strict < 0.15 wall, so raising it back needs a new owner token. Every
  other field atom (the OD-6-axis violet seed, energy 0.18, PAPER_WASH_GROUND,
  cursor-as-light ON) is the P-HERO blessed value verbatim — ONLY the
  amendment's named lever moved.
- **DEFAULT-mounted, home-only:** the `?light=1` review lever died with the
  prototype; App.vue mounts HeroAurora in the EditorShell #backdrop slot
  (under the graph-paper ink lines — the wash tints the paper) gated on
  `isHome`; navigating to a scene tears the layer down (browser-asserted).
- **DONE RIGHT (VERDICT #22):** glass-ui's PUBLIC `@mkbabb/glass-ui/aurora`
  door — rAF-coalescing `setCursor`, PRM-safe `renderMode="auto"`, DPR budget,
  whole-viewport (no clipped-rect partiality), zero DOM geometry reads in the
  pointer path (the T-CL-3 read-after-write class impossible by construction).
  The standing recurrence guard `proof:no-hand-rolled-cursor-tracker` (T.D14,
  INSTRUMENT, hygiene-banded) keeps a THIRD hand-rolled tracker impossible.

## Deviations from the P-HERO reference (named explicitly)

1. **opacityCeiling 0.15 → 0.1** — the amendment itself (the ONLY field
   change). If the owner wants it subtler still at re-verify, the bound is one
   constant + one oracle literal.
2. **Default-on** (P-HERO: behind `?light=1`) — per the OD-2 blessing of the
   light fork as the default.

## Verdict

**Owner (mkbabb), 2026-07-05: "I like the aurora, but more subtle"** (the OD-2
token + amendment; the landed state executes the amendment at 0.1 — strictly
under the prototype ceiling — with the bound machine-encoded.)

Disposition: APPROVED
Reference: OD-2 (OWNER-DECISIONS.md) · P-HERO branch `worktree-wf_1e744f4d-2bb-1`
(`?light=1` fork) · landed captures `shots-TD-hero/` · oracles
proof:cursor-light-subtle (OWNER) + proof:no-hand-rolled-cursor-tracker
(INSTRUMENT, T.D14).
