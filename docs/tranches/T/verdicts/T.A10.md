# T.A10 — TASTE-VERDICT (amiga grid-room composition)

## Packet
- **Before:** the amiga stage was a foreign gray Lambert box behind a CRT-pastiche
  overlay (scanlines/grille/vignette/flash) — the "gray-purple slab" the owner
  rejected, with a per-frame compositing cost and a gesture-legend / boot-flash
  atmosphere stack on the DOM.
- **After (this drive):** the gray box + CRT overlay + boot flash + gesture legend
  are DELETED (RULED, born-RED — proof:amiga-subject-is-pivot still green; T.A10
  DOM census: the on-stage DOM is ONE full-bleed WebGL canvas). The room is now a
  **paper-grid floor + back-wall** drawn in-canvas over the theme backdrop
  (renderer alpha:true), with a **soft radial contact-shadow blob** that tracks the
  ball's x and scales/fades with bounce height (sells the arc's altitude). The
  boing arc renders at authored scale (py peaks ±3.998 world units, T.A9).
- **Deltas claimed:** foreign slab → the demo's own paper-grid substrate; the
  atmosphere compositing cost removed (feeds T.A12 render-on-demand + T.G); a fake
  contact-shadow anchors the ball to the floor.

## Verdict
**Owner (___), ____-__-__: "___"**

Disposition: PENDING
Reference: lane 03 rec 5 (T-AM5); grid-room composition is a T.M2 capture sign-off
surface (grid density/tint, floor↔back-wall balance, contact-shadow softness). The
color TOKENS ride T.D. The RULED REMOVALS + the in-canvas draw are landed; the
COMPOSITION awaits the owner token before its born-RED capture oracle is authored.


## OWNER VERDICT — FILLED 2026-07-06

> **Token (verbatim):** “Ratify all with your best judgment. We shall adopt the glass-ui drawer, but ensure that we identify any gaps in that implementation and forward any and all glass-ui suggestions to that working agent's tranche execution (with the exhortation to research, plan, and fold into our running BG/BH wave set--no prefunctory implementation)”
>
> **Disposition:** RATIFIED AS LANDED — the paper-grid room + contact-shadow composition approved (the demo's own substrate over the foreign gray box).
