# OWNER-ASKS — mid-T owner asks (the S.Z1 mechanism carried forward)

> Every owner ask arriving during T lands here verbatim with a disposition;
> `proof:prompt-recap-t` asserts full dispositioning at close.

| # | Date | Ask (verbatim) | Disposition |
|---|---|---|---|
| 1 | 2026-07-05 | **THE GRAND COLOCATION EDICT**: "Ecoute-moi, and ensure this is to be planned: Further, our frontend structure--and this is a grand edict for ALL file directories--needs to be wildly re-structured: components should be COLOCATED with their sub-components, composables, skeletons, constants, etc (and this should be done recursively for nested components). Composables that are truly module-level or global-level—and other dirs of that nature—can be found within a composables/ dir therein, but otherwise they're to be COLOCATED--same for styles, etc. Long running dirs must and always be broken into common modules and encapsulated thereof. Similar treatment and enforcement should be applied to all backend files, too—though abstracted and made befitting for those languages and implementations. The library AND the demo should have a similar treatment. The demo should be AGGRESSIVELY analyzed for deprecated, legacy, superfluous code, sub-optimal encapsulation, glass-ui usage, etc." | IN EXECUTION (dev-phase fold, 2026-07-05) — T.F ELEVATED from "demo re-taxonomy" to THE GRAND COLOCATION EDICT band: (a) the recursive-colocation RULE (colocate sub-components/composables/skeletons/constants/styles with their component, recursively; `composables/`-style shared dirs reserved for the truly module/global-level) encoded as a standing ENFORCEMENT gate, not a one-time move; (b) long-running dirs always broken into encapsulated common modules; (c) the LIBRARY half — same principle abstracted befittingly for TS library idiom (per-zone internal cohesion, long-file encapsulation, colocated per-zone structure; the S zone-partition DECISIONS stand — see T.md §4 reconciliation); (d) the AGGRESSIVE demo purge wave (deprecated/legacy/superfluous/dead-export/`any`-ceiling/duplicated-throttle + glass-ui-usage census — folds lane 21 recs 4/5/6). Charter §1/§4 amended same-day; waves/T.F.md amended by the edict-fold; PROMPT-RECAP row added. |
| 2 | 2026-07-05 | **THE PROTOTYPE VERDICTS + RATIFICATION**: "I like the aurora, but more subtle. Square is much better, but the controls need work. And that curve preview in the top left needs to be improved dramatically. Good. Ratify. Do not begin dev yet." | ADDRESSED — all seven OD rows now carry final tokens (OWNER-DECISIONS.md): OD-1 PRUNE FINAL (fuse case not taken), OD-2 Aurora-on-hero AMENDED more-subtle, OD-3 KEEP ppmycota, OD-4 APPROVED, OD-5 APPROVED-DIRECTION + two named reworks (controls composition; the top-left curve preview improved dramatically — both bound as born-OWNER re-review riders on T.B4/T.B6/T.D), OD-6 APPROVED. **The T corpus is RATIFIED.** **Implementation is explicitly NOT authorized** ("Do not begin dev yet") — the drive opens only on the owner's next word; prototypes stay served for reference. |
| 3 | 2026-07-05 | **THE IMPL AUTHORIZATION (compaction-armed)**: "Prepare for compaction: thereupon we shall begin execution. Update your task list, and any other appurtenant items." | ADDRESSED — the hold from row 2 ("Do not begin dev yet") is LIFTED effective post-compaction: the T impl drive begins immediately on session resume. Anchors written same-day: task #299 flipped to AUTHORIZED with the full drive recipe; the "State of play — the T impl-drive entry anchor" appended to PROGRESS.md; memory updated; the guard cron re-armed with T-drive wording (the 2 stale S-drive crons deleted). Governing docs: T.md §2 (DAG) + §5 (orchestration), OWNER-DECISIONS (all 7 tokens final — OD-2's more-subtle amendment and OD-5's two born-OWNER riders BIND the drive), the S drive lessons as standing prompt clauses. |
