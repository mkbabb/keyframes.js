# Tranche T — THE OWNER REVIEW PACKET (mid-drive / close)

> **What this is.** The single surface the owner reviews to (a) fill the ONE open
> ruling (OD-7), (b) glance-confirm the RULED-and-applied appearance waves, (c) make
> the handful of genuinely-owner-open taste calls the lanes surfaced (no VERDICT line
> settles them), and (d) bless the T.M3 owner-golden reference frames. It exists
> because the S impl demo passed **85/85** deploy-gating gates + **11×100%** critic
> convergence and the owner **rejected it on sight** — the cure (T.M) is that taste
> dispositions now carry a committed owner token, and this packet is where the owner
> supplies them.
>
> **The doctrine (T.M2, the S.E shelf lesson made structural).** *Critic consensus ≠
> owner verdict.* A green gate may NOT stand as the spirit oracle for any
> design/appearance/interaction ask. Every row below wants a **committed owner
> token** (an owner name + a verbatim quote), recorded in the row's `docs/tranches/T/
> verdicts/<id>.md` verdict slot and — for the OD forks — in `OWNER-DECISIONS.md`.

---

## 0 · How to serve each surface

| Surface | How to serve | What it shows |
|---|---|---|
| **The LANDED tree** (all RULED-and-applied waves) | `npm run build && npm run gh-pages`, then serve `dist/gh-pages/` (or `npm run dev` for HMR) — the demo the deploy publishes | the cube/amiga/square/easing/spring/home scenes as they render TODAY on `tranche-t-impl` |
| **The prototype worktrees** (the not-yet-applied design forks) | kept worktrees served on `:5180` for live review (T.md §3; capture packets commit under `audit/prototypes/`) — **P-HERO** (OD-4), **P-THEME** (OD-6), **P-GALLERY** (OD-7 easing gallery) | the design a fork proposes, before it lands |
| **The verdict capture packets** (committed shots) | `docs/tranches/T/verdicts/shots-TD-hero/`, `shots-TD-theme/`, and the per-wave `verdicts/<id>.md` packet sections | the before/after the wave claims |
| **The owner-golden candidates** (T.M3) | `node scripts/proof-owner-golden.mjs --capture-candidates` (after `npm run gh-pages`) → `docs/tranches/T/goldens/candidates/` | the 12 PRM-frozen reference frames to bless |

---

## 1 · The OD register — ONE ruling remains open

Six of the seven owner-decision forks were **RULED 2026-07-05** (P-HERO / P-THEME /
panel / square / cursor / prune live reviews); their gated design waves are now
authored + landed. **OD-7 is the single PENDING-OWNER ruling.** (Full evidence in
`OWNER-DECISIONS.md`; the summary table there is the source of truth for the ruling
state — see the drift note §5 below.)

| OD | The fork | State | The token it wants |
|---|---|---|---|
| **OD-7** | **Easing specimen-drawer gallery DESIGN** — the drawer geometry, the tile treatment, the one-shared-clock comparative sweep across the named-curve tiles | ⬜ **PENDING-OWNER** | a live sign-off on **P-GALLERY** (`:5180`). Until it lands, `proof:easing-gallery` (T.E6's born-RED oracle) may NOT be authored (T.M2 enforcement); the easing scene ships as the RULED "the balls ARE the scene" (#14) with the furniture removed |
| OD-1 | morph/motion-path PRUNE vs FUSE | ✅ RULED: **PRUNE (FINAL)** | — (T.E3 executed; T.E2 DEAD) |
| OD-2 | cursor light REMOVE vs Aurora-on-hero | ✅ RULED: **AURORA-ON-HERO, MORE SUBTLE** | — (T.D13 landed; ceiling 0.1) |
| OD-3 | ppmycota brand toggle KEEP vs CUT | ✅ RULED: **KEEP** | — (intentional brand mark) |
| OD-4 | hero direction (ink-on-graph-paper φ-seat + per-char) | ✅ RULED: **APPROVED** | — (P-HERO blessed; rides §3 re-verify) |
| OD-5 | panel chrome (two floating GlassPanels, no pane) | ✅ RULED: **APPROVED-DIRECTION + 2 reworks** | **see §2 (R1/R2 still want re-review)** |
| OD-6 | theme (Jakarta + honest serif + one violet ramp) | ✅ RULED: **APPROVED** ("Good.") | — (P-THEME blessed; rides §3 re-verify) |

---

## 2 · PENDING-OWNER appearance rows — the calls this packet wants

From `verdicts/APPEARANCE-WAVES.json`. Each wave here may NOT close (`PROGRESS.md`)
without a FILLED verdict artifact (`proof:owner-verdict-recorded`, T.M1).

| Wave | Surface | The decision it wants | Where to look |
|---|---|---|---|
| **T.E6** (OD-7) | easing gallery IS the easing scene | the gallery **composition** (drawer geometry / tile treatment / shared-clock sweep) — the one open OD | **P-GALLERY** worktree `:5180`; `verdicts/` (packet pending live review) |
| **T.B-panel** (OD-5) | two-floating-panel composition | **R1** — the controls composition reworked ("the controls need work"); **R2** — the top-left easing-curve preview improved **dramatically** ("needs to be improved dramatically"). Direction APPROVED; these two riders are born-OWNER re-reviews on T.B4/T.B6/T.D | `verdicts/T.B-panel.md`; the landed square scene + the curve canvas |
| **T.A-cube-taste** | cube restage taste | axis-line **rest opacity 0.45** + the **ease-out-back settle** choice — genuinely owner-open (NO VERDICT line names the number/easing) | `verdicts/T.A-cube-taste.md`; landed cube scene |
| **T.A10** | amiga grid-room composition | grid density/tint, floor↔back-wall balance, contact-shadow softness (the RULED removals — #8 legend, CRT, boot-flash — are already executed) | `verdicts/T.A10.md`; landed amiga scene |
| **T.A11** | amiga transient-ω decay badge | whether a transient on-stage ω badge is wanted **AT ALL** + placement (default per T.M4 = removal, no un-manifested chrome) | `verdicts/T.A11.md`; landed amiga scene |
| **T.H3** | mobile-sheet Drawer geometry | **HOLD** the bespoke sheet behind BG-11 (recommended — preserves the owner-verified occlusion cure) vs **ADOPT** glass-ui full-height Drawer now (deletes ~350L, accepts the menubar overlap). Owner-open (no VERDICT rules the mobile-sheet geometry) | `verdicts/T.H3.md`; landed demo @ 375×667 |

---

## 3 · RULED-and-APPLIED verdict packets — glance-confirm (they ride the owner's mid-drive re-verify)

These waves are RULED + landed + their verdict slots FILLED against the blessed
prototype references; the owner glances the applied captures to confirm the applied
tree matches the blessed prototype (and reviews the **named deviations** each packet
declares — a deviation is a place the applied tree honestly departs from the
prototype, surfaced rather than hidden).

| Wave | What landed | Named deviation to review | Packet |
|---|---|---|---|
| **T.D7** (OD-6) | the applied theme — ONE oklch violet accent authority + Jakarta + honest-weight serif; red-kill (~168 refs → destructive-only) | hero ink / deck / egg → deferred to the hero batch | `verdicts/T.D7.md` + `shots-TD-theme/` |
| **T.D9** (OD-4) | hero φ-band two-focal seat (hero over cube) | named **P-HERO deviations**; the machine-witnessed MIGRATION successor of the retired hero-rung/-balance/-cls locks | `verdicts/T.D9.md` + `shots-TD-hero/` |
| **T.D11** (OD-4) | hero deck-voice serif-italic ramp | ONE named rung deviation (hint heading → title, forced by the T.D2 serif floor) | `verdicts/T.D11.md` |
| **T.D13** (OD-2) | cursor-light Aurora-on-hero, home-only, ceiling 0.1 (strictly under the P-HERO 0.15 wall) | the subtlety amendment encoded as the ≤0.1 ceiling | `verdicts/T.D13.md` |
| **T.A-cube-taste** | (see §2 — its verdict is still genuinely open) | — | `verdicts/T.A-cube-taste.md` |

### The named mobile-seat deviation (hero)

The hero's mobile seat is a **named deviation** in the T.D hero packet — the φ-band
two-focal seat is a desktop composition; the mobile layout departs (the hero cannot
sit "a bit on top of the cube" at 375px without occluding the stage). Reviewed as a
declared deviation, not a hidden compromise — the owner confirms the mobile seat is
acceptable or re-directs it.

---

## 4 · Batch-⑧ glances (landed; confirm the taste read)

- **Easing / spring triad-reversal** — the ⑧ fan-out reversed the control-surface
  triad ordering on the easing + spring scenes (the DFA-derived triad now COMPUTES
  from "does the selected channel paint," so a painting group is never denied its
  triad — the #25 asymmetry cure). Glance the two scenes to confirm the ordering
  reads right.
- **Cube idle-bob REMOVAL** — the cube's non-compositable `preserve-3d` idle-bob
  (1.00 recalc/frame at rest) was removed at T.G3: the die now **rests still** and
  comes alive on play/hover (true rest 0.00/frame, measured). Confirm the resting
  cube reads as intentional stillness, not a dead scene.

---

## 5 · The T.M3 owner-golden blessing (the appearance authority action)

`proof:owner-golden` (T.M3) replaces `proof:visual-lock`'s self-captured baseline +
full-subject mask with an **owner-blessed** reference render — the appearance
authority the S roster never had. It is **born-RED + born-OWNER**: green is
UNREACHABLE until the owner blesses. The action:

1. `npm run gh-pages` (build the demo the deploy publishes).
2. `node scripts/proof-owner-golden.mjs --capture-candidates` — writes the **12**
   PRM-frozen candidate frames (home/cube/amiga/square/easing/spring × light/dark) to
   `docs/tranches/T/goldens/candidates/` as PENDING-OWNER.
3. Review the candidates (the subject stays IN — the mask visual-lock painted flat is
   FORBIDDEN; a masked/blank/one-face frame reds the subject-energy floor).
4. **Bless** by committing `docs/tranches/T/goldens/BLESSED.json` (schema in
   `goldens/README.md`) naming each approved candidate's `sha256` + copying it into
   `goldens/golden/`. On the blessing, `proof:visual-lock` is retired or demoted to a
   pure no-drift corroborator (T.M3 lockstep — the demote-vs-retire call executes
   WITH the blessing).

> **Note — OWNER-DECISIONS.md drift to reconcile.** The register's *header* still
> reads "Seven rulings PENDING-OWNER; every ruling slot is EMPTY," but the register
> *table* records OD-1..OD-6 as RULED (2026-07-05) and the per-OD *sections* below
> the table still show `PENDING-OWNER — token: ___`. The **table is the current
> truth** (six ruled, OD-7 open). The header + section slots want a mechanical
> reconcile to the ruled state (owner-domain edit; surfaced here so it is visible,
> not silently carried).

---

## 6 · The born-RED backlog + the external-blocked set (what waits on whom)

### T_BORNRED_BACKLOG (9 gates — declared, machine-bound; failing ⊆ backlog exactly)

| Gate | DischargedBy |
|---|---|
| `proof:owner-golden` | **the owner golden-blessing at review** (this packet, §5) |
| `proof:stage-inventory` | T.A/T.B/T.C/T.D/T.E furniture prune + the KF_REQUIRE_BROWSER rendered-set reconciliation (a later wave) |
| `proof:subject-legible` | T.C dock icon de-blur (a glass-ui born-RED handoff — **GU-1**) |
| `proof:blur-not-resampled` | glass-ui `blur-source="static"` frozen-backdrop publish (**BG-5**) + the kf re-pin |
| `proof:roster-ceiling` | T.M7 retirements + the FROZEN discharge fold (converges as bands delete keys) |
| `proof:visual-lock` | T.A3 landed (cube-pose-flap cause removed); terminal re-baseline rides T.M3 `owner-golden` |
| `proof:no-collision-rename` | value.js renames `PropertyDescriptor` → collision-free (**KF-7**) + kf re-points |
| `proof:dock-rest-crisp` | glass-ui **GU-1** publish + re-pin (T.C6) — resting docks CRISP |
| `proof:dock-morph-continuity` | glass-ui **GU-2** publish + re-pin (T.C6) — real laid-out endpoint geometry |

### External-blocked (neither kf-fixable; owner-domain / upstream publish)

| Item | Owner | What it unblocks | Letter |
|---|---|---|---|
| **KF-7** | value.js | `proof:no-collision-rename` — rename exported `PropertyDescriptor` → `CSSPropertyDescriptor` (the `PropertyDescriptor_2` collision-rename in the published d.ts) | `KF-TO-VALUEJS-T.md` |
| **GU-1** | glass-ui | `proof:dock-rest-crisp` + `proof:subject-legible` — gate the dock reveal-blur on `[data-morphing]`; a resting dock is CRISP | `KF-TO-GLASSUI-BG.md` |
| **GU-2** | glass-ui | `proof:dock-morph-continuity` — measure laid-out geometry; no `max-content` jump-cut | `KF-TO-GLASSUI-BG.md` |
| **BG-5** | glass-ui | `proof:blur-not-resampled` — a static-backdrop blur mode so the glass does not re-rasterize the live stage every frame (VERDICT #19) | `KF-TO-GLASSUI-BG.md` |
| **BG-11** | glass-ui | the **T.H3** ADOPT branch — a detented live-behind Drawer needs a bottom-reserve token + max-detent cap (HOLD the bespoke sheet until it ships) | `KF-TO-GLASSUI-BG.md` |

---

## 7 · The close gate (what "reviewed" unblocks)

`proof:owner-verdict-recorded` (T.M1) blocks every appearance wave's close until its
verdict artifact is FILLED; `proof:owner-review-gate` (T.M2) blocks authoring a
design wave's born-RED oracle until its OD row carries a token; `proof:owner-golden`
(T.M3) is the appearance authority born-RED until §5's blessing. This packet is the
surface that fills them. **T.Z's close requires every OD slot filled** (or explicitly
deferred with a reason) — today that is **OD-7** plus the §2 owner-open taste calls
(cube taste, amiga grid-room + ω-badge, panel R1/R2, Drawer geometry) and the §5
golden blessing.
