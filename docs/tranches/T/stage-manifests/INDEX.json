{
  "_doc": "T.M4 (proof:stage-inventory) — the per-scene owner-sanctioned on-stage element manifest index. The INVERSE of the DFA: instead of 'the control set equals the table', assert 'the on-stage element set equals the owner-sanctioned inventory', so extra chrome NOT in the manifest REDs. This is the negative-space gate that catches the entire never-gated Class D ('no gate can assert this element should not exist'). Each scene's `manifest` slot is either an owner-sanctioned inventory file (docs/tranches/T/stage-manifests/<scene>.json) or the string 'EMPTY-RED' (born-RED — the band that rebuilds the scene fills it WITH owner sign-off, T.M4 lockstep). A scene whose manifest is EMPTY-RED, or whose retire-target chrome is un-manifested, REDs. Blessed prototype scenes (P-HERO/P-PANEL/P-THEME + the VERDICT removals) may carry a derivable skeleton NOW; T.A/T.B/T.C/T.D rebuild the rest.",
  "scenes": [
    { "scene": "home", "manifest": "home.json", "note": "T.D9/T.D13 LANDED (batch ⑤) — re-derived against the landed hero (per-char mark, serif deck, default aurora at the 0.1 amended ceiling); forbidden set browser-asserted by proof:hero-two-focal clause (c)" },
    { "scene": "square", "manifest": "square.json", "note": "T.M batch ⑧ — the gesture-legend (#8) removal executed; SKELETON sanctioned set derived from the landed DOM. #11 caption-block DOM prune + owner sign-off ride T.B/OD-5 (forbidden in the manifest; browser-reconciliation reds until pruned)" },
    { "scene": "cube", "manifest": "cube.json", "note": "T.A restage COMPLETE — #5 rx/ry/rz readout + #8 gesture legend removed; owner-sanctioned inventory filled (rulings #5/#8). (An unresolved ②′-merge conflict marker sat across the cube/amiga rows until batch ⑤ — cured as the UNION: both sides' filled manifests exist on disk.)" },
    { "scene": "amiga", "manifest": "amiga.json", "note": "T.A7–A12 rebuild (impl drive): #8 gesture legend + CRT overlay + boot flash + parked telemetry REMOVED; WebGL-only stage. Grid-room composition + transient-ω-badge option PENDING-OWNER (T.M2)" },
    { "scene": "easing", "manifest": "easing.json", "note": "T.E TERMINAL batch — RE-DERIVED from the LANDED OD-7 gallery (T.E6, APPROVED 2026-07-06: the specimen drawer IS the scene; hero/ghost/smear/readout/view-select forbidden; #13/#15 stay forbidden). proof:easing-gallery AUTHORED (OWNER authority) is the browser oracle; the manifest is FILLED + owner-sanctioned (T.M2 satisfied for easing)" },
    { "scene": "spring", "manifest": "spring.json", "note": "T.M batch ⑧ — the gesture-legend (#8) removal executed; SKELETON sanctioned set derived from the landed T.B7-facility DOM. #17 dock-label dedup rides T.C (an app-shell dock concern, not on-stage)" },
    { "scene": "sequence", "manifest": "sequence.json", "note": "T.M batch ⑧ — the gesture-tell (#8) removal executed; SKELETON sanctioned set derived from the landed T.B-facility DOM" }
  ]
}
