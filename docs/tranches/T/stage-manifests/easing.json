{
    "_doc": "T.M4 owner-sanctioned on-stage inventory for the easing scene — RE-DERIVED at the easing TERMINAL batch (T.E6, OD-7 APPROVED 2026-07-06: P-GALLERY is the blessed reference; proof:easing-gallery is the OWNER-authority browser oracle over this composition). THE SPECIMEN DRAWER IS THE SCENE: the singular hero (EasingHeroStage + ghost/smear), the f(t)= readout, and the Singular/Group/All view-mode machinery are DELETED — every named curve is a specimen tile racing under ONE shared departure clock. The `sanctioned` set is the landed OD-7 composition; any rendered on-stage element NOT in this set REDs proof:stage-inventory. The T.E7 ruled removals (#13 telemetry, #15 gallery door) stay explicitly forbidden.",
    "scene": "easing",
    "sanctioned": [
        "easing-gallery-card (the standard glass Card plate hosting the whole drawer — `<Card>` .easing-target.easing-gallery; the I5 STAGE-CARD register; --ball-tone = --color-progress, the ONE OD-6 violet consumer)",
        "specimen-name (the PROMOTED selected-curve name at the Instrument-Serif display rung — h2.specimen-name; 140ms out-in crossfade on selection, PRM-suppressed)",
        "specimen-literal (the COMPLETE re-parseable literal beside the name — Fira Code `data-register=code` + the CopyButton affordance; never truncated — the F7 kill; proof:easing-gallery clause (4))",
        "family-filter (the QUIET single-select ToggleGroup — All · Standard · Sine · … · Back · Bounce · Steps — inside an x-axis FadingScroll; replaced the former unlabeled view-mode Select; proof:easing-gallery clause (5))",
        "specimen-drawer (the y-axis FadingScroll auto-fill grid of 33 ToggleChip specimen tiles — role=group; the pressed tile IS the selected curve; proof:easing-gallery clause (1))",
        "specimen-tile stage (per tile: the static sparkline portrait path + the 1px hairline rail with origin/terminus ticks + the 14px OD-6-violet `data-curve` ball racing x = fn(phase)·maxX on the ONE shared registerDotPainter clock — IO-gated, transform-only writes, PRM rests every ball at its terminus; proof:easing-gallery clauses (2)/(3)/(6))",
        "specimen-tile name (the curve identifier under the stage — `data-register=code` mono caption, as-cased, never truncated at the 150px tile floor)"
    ],
    "forbidden": [
        "easing-hero-stage (EasingHeroStage — the singular one-ball hero; DELETED at T.E6 per OD-7: the buried balls-preview was PROMOTED to be the scene)",
        "easing-ghost/trace-smear (useEasingGhost + useEasingTraceSmear — the hero's ghost rail + the S5 drag-smear egg surface; died WITH the hero at T.E6; the design-refinement S5 arm was re-cut in gate-bands)",
        "easing-readout (the f(t)= AnimatedDigit readout — a hero-era companion; the comparative race IS the pedagogy now)",
        "view-select (the Singular/Group/All view-mode Select — superseded by the family-filter ToggleGroup at T.E6)",
        "curve-physics-telemetry (#13 — the EasingCurvePhysics telemetry block incl. its 'name this curve' dblclick egg; ruled OUT, DELETED at T.E7)",
        "gallery-door-button (#15 — the full-width red-glyph button promoting the easter-egg gallery tour into primary chrome; ruled OUT, DELETED at T.E7 — the T.E6/OD-7 scene IS the gallery)",
        "hand-rolled curve canvas ON STAGE (EasingCurveCanvas/DemoControlPoint — instrument/easing/ DELETED at T.E8; bezier/steps AUTHORING is the glass-ui EasingPicker in the Curve facet BODY (EasingSidebar), a controls-panel concern that never mounts on stage)"
    ],
    "authority": "OD-7 (OWNER, APPROVED 2026-07-06 — P-GALLERY blessed reference wf_558e7859-5ca-3) + proof:easing-gallery (the browser oracle: census / analytic fn(φ)·maxX placement / transform-only trace / header promotion / family filter / coherent freeze)",
    "status": "FILLED + OWNER-SANCTIONED (OD-7). RENDERED-set reconciliation is the browser discharge (KF_REQUIRE_BROWSER, the T_BORNRED_BACKLOG row)."
}
