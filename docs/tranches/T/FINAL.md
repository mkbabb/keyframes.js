# Tranche T — FINAL (the close record)

> **Status: DRAFT — assembled at batch ⑪ close-prep (2026-07-06). Close NOT yet fired.**
> This is the T.Z close template (the S.Z3 substance transferred — the FINAL + version + deploy
> record S never ran; PROMPT-RECAP §4 carries S.Z3 → T.Z by name). It is authored WITH the drive
> so the close is a reconciliation, not a re-derivation. The terminal actions it gates (the M3
> golden blessing under the delegated judgment, the version cut, the deploy-of-record firing) land
> at T.Z under the owner-anchored close per OWNER-ASKS row 4.
>
> Cross-links: the plan is `T.md`; the owner asks are `OWNER-ASKS.md`; the ruled register is
> `OWNER-DECISIONS.md`; the ask-ledger with owner tokens is `PROMPT-RECAP.md` (§3 = the planned
> verdict→home map this file lands; §4 = the S-residue closure); the board is `PROGRESS.md`; the
> glass-ui forwarding is `KF-TO-GLASSUI-BG.md`; the value.js letter is `KF-TO-VALUEJS-T.md`.

---

## 1 — The verdict-item → cure map (all 28, with landed wave + gate)

The itemized owner catalogue is `audit/owner-review/VERDICT.md` (28 rows). Each row's LANDED cure,
its executing wave, and the gate that holds it. Status legend: **LANDED** (in-tree + gate green or
retired) · **⑪-INFLIGHT** (executing in the terminal batch's parallel lanes) · **EXTERNAL-BLOCKED**
(cure is glass-ui/value.js-owned; a kf-side born-RED tripwire rides the re-pin).

| # | Owner verdict (short) | Cure | Wave | Gate / witness | Status |
|---|---|---|---|---|---|
| 1 | cube "does not render fully" | `--spin-energy` bloom delete; the die renders | T.A1 | `proof:subject-full` (cube leg, batch ②′) + `cube-silhouette`/`cube-settle` | LANDED |
| 2 | "@KEYFRAMES·LIVE typing card — remove this crap" | `useHeroSourceEgg.ts` deleted | T.D12 | `proof:design-refinement` RETIRED (⑧); absence at `hero-two-focal` (c) | LANDED |
| 3 | hero — per-CHAR uplift, lower/centred, overlap-OK | φ-seat + per-char two-tier uplift over the P-HERO reference | T.D9/D10/D11 · **OD-4** | `proof:hero-two-focal` + `hero-deck-voice` (born-OWNER); `hero-rung/balance/cls` → DISCHARGE migration | LANDED |
| 4 | docks "blurry, broken, janky" | dock recut; play-first rail; resting de-blur | T.C1/T.C | `proof:transport-play-first-render` (⑤); `subject-legible` + `dock-rest-crisp`/`dock-morph-continuity` | LANDED (kf) + EXTERNAL-BLOCKED (glass-ui GU-1/GU-2) |
| 5 | cube rx/ry/rz readout "remove" | readout stripped | T.A | `proof:stage-inventory` (T.M4 negative-space) | LANDED |
| 6 | dock cluster — ghost tooltip, home divider, play-first | rail-core recut; play button first | T.C1 | `proof:transport-play-first-render` DISCHARGED (⑤) | LANDED |
| 7 | "remove the surrounding pane" | two floating GlassPanels; pane removed | T.B · **OD-5** | `proof:scene-facility` DISCHARGED (⑥′) | LANDED |
| 8 | gesture legends "remove all elements like this" | `GestureLegend.vue` + all tells deleted | T.E/T.B | `proof:gesture-manifest` RETIRED (⑧) → `stage-inventory` (T.M4) | LANDED |
| 9 | amiga "broken mess, doesn't interleave" | rotation/NaN cure; rides the AnimationGroup | T.A | `proof:live-session` (amiga leg) | LANDED |
| 10 | dock single-option / divider / order | context for #12/#13/#17 | T.C · T.B2 | `proof:no-single-option-select` (widened) | LANDED |
| 11 | square caption "superfluous nonsense" | caption removed | T.A/T.E | `proof:stage-inventory` (T.M4) | LANDED |
| 12 | square "restore the controls/keyframes panel" (reverses S.G2 DFA=[]) | `square-honest` v2 inversion; square in the triad | T.A13/T.B3 | `proof:scene-control-dfa` (square-in-triad EXPECT) + `square-honest` v2 → demo-correctness | LANDED |
| 13 | easing curve-physics telemetry "remove all" | curve-physics + telemetry deleted | T.E7 | `proof:design-refinement` RETIRED (⑧) | LANDED |
| 14 | easing "just have the balls previewed here" | specimen-drawer gallery IS the scene (P-GALLERY) | T.E6 · **OD-7** | `proof:easing-gallery` (born-OWNER, OD-7 APPROVED) | **⑪-INFLIGHT** (lane 1) |
| 15 | Gallery button "remove" | gallery-door deleted | T.E7 | `proof:easter-egg` RETIRED (⑧) | LANDED |
| 16 | easing "re-design w/ glass-ui; latent red theme" | red-kill + ONE violet ramp; easing redesign | T.D7/D · **OD-6** + T.E6/E8 | `proof:easing-sidebar-*` RETIRED (RETIREMENT_LEDGER, lane 1); T.D7 accent-census | **⑪-INFLIGHT** (lane 1 easing; T.D theme LANDED) |
| 17 | spring dock "∿ Spring │ ∿ Spring" single-option elision | `surfacesFor` derived-triad; dock-label elision | T.B2 | `proof:dfa-derived` (⑧) + `no-single-option-select` | LANDED |
| 18 | spring presets + KfPillTabs "why not glass-ui" | facility triad restore; KfPillTabs colocated | T.B + T.H | facility gates; KfPillTabs → glass-ui pill ask (BG) | LANDED (colocate) + EXTERNAL (glass-ui pill) |
| 19 | "performance god awful — from the ground up" | true-rest + transform-not-left + Monaco LCP defer; blur de-layer (kf half) | **T.G** (G1–G10) | `proof:perf-counters` BLOCKING (⑧) + `scene-rests` + `no-layout-animation`; `single-raf-clock` RECORDED-subsumed (⑪) | LANDED (kf) + EXTERNAL-BLOCKED (BG-5 backdrop compositor cost) |
| 20 | motion-path "barely works" | scene PRUNED (library factory survives) | T.E3 · **OD-1** | `proof:motion-path-editable`/`-copy` KILLED (DISCHARGE, witness) | LANDED |
| 21 | morph "does not work" | scene PRUNED (library MorphSVG survives) | T.E3 · **OD-1** | `proof:morph-scene` RETIRED (RETIREMENT_LEDGER) | LANDED |
| 22 | cursor light "done right or removed" | Aurora-on-hero, MORE SUBTLE; bespoke wash dead | T.D13 · **OD-2** | `proof:cursor-light-subtle` + `cursor-light-no-sync-layout` | LANDED |
| 23 | compose "remove this crap" | scene DELETED in totality | T.E1 · **OD-1** | `proof:compose-scene` RETIRED (RETIREMENT_LEDGER) | LANDED |
| 24 | fonts/sizes sitewide "not right" | Jakarta body, Instrument Serif display, mono→data | T.D3/D5 · **OD-6** | the font-tuple gate + `demo-typography` | LANDED |
| 25 | panel facility "forgot entirely" | `SceneFacility` returns for every sub-animation | T.B/T.B2 | `proof:scene-facility` + `dfa-derived` | LANDED |
| 26 | demo/ "re-structure from first-principles" (THE GRAND COLOCATION EDICT) | instrument/ fold, styles split, colocation keystone, purge terminal | **T.F** (23 waves) | `proof:colocation` (KEYSTONE, ⑩) + `style-file-ceiling` + `no-dead-export` + `any-ceiling` | LANDED |
| 27 | glass-ui "leverage latest; delineate gaps" | the ask letter + gaps census; Drawer ADOPT | **T.H** | `KF-TO-GLASSUI-BG.md` §FORWARDING; `demo/glass-ui-gaps.ts`; BG-5/GU-1/GU-2/BG-11 | **⑪-INFLIGHT** (T.H3 Drawer, lane 2) + EXTERNAL-BLOCKED (BG/BH) |
| 28 | codebase refactor litany (verbatim re-issue) | library carves; legacy excision; DI; no god modules | §1 standing mandate + **T.F/T.S** | `any-ceiling`(102) + `no-dead-export`(28) + `decomposition` + `boundary`; `prompt-recap-t` re-issuance census | LANDED |

**Coverage:** 22 LANDED · 3 ⑪-INFLIGHT (#14/#16 easing terminal lane 1; #27 T.H3 Drawer lane 2) ·
the EXTERNAL-BLOCKED residue (#4/#18/#19/#27 glass-ui BG-5/GU-1/GU-2/BG-11) rides the re-pin as
born-RED tripwires. No verdict item is unaccounted.

---

## 2 — The OD register (final state)

All seven rulings FILLED (`OWNER-DECISIONS.md`, register CLOSED). Final tokens:

| OD | Ruling | Landed |
|---|---|---|
| OD-1 | **PRUNE (FINAL)** — morph + motion-path + compose deleted; library factories survive | T.E1/T.E3 (LANDED) |
| OD-2 | **AURORA-ON-HERO, MORE SUBTLE** — the `?light=1` fork default at a subtler bound than the 0.15 ceiling | T.D13 (LANDED) |
| OD-3 | **KEEP ppmycota** — intentional brand mark; the CUT gate is dead | (KEEP — no wave) |
| OD-4 | **APPROVED** — the P-HERO composition blessed; the aurora-subtlety rider is OD-2 | T.D9/D10/D11 (LANDED) |
| OD-5 | **APPROVED-DIRECTION + 2 riders** — R1 controls composition (satisfied-as-landed); R2 top-left curve preview improved dramatically (→ easing terminal) | T.B (R1 LANDED; R2 ⑪ lane 1) |
| OD-6 | **APPROVED** — Jakarta + honest-weight serif + ONE violet ramp; red → destructive-only | T.D7/D (LANDED) |
| OD-7 | **APPROVED** — P-GALLERY = the blessed reference; the specimen-drawer gallery IS the easing scene | T.E6 (⑪ lane 1) |

T.M2 mechanism satisfied: every born-OWNER oracle carries its OD token before authoring
(`proof:owner-verdict-recorded`); the register is machine-bound, not prose.

---

## 3 — The backlog + external-blocked residue (discharge owners)

The T born-RED backlog (`scripts/gate-bands.mjs T_BORNRED_BACKLOG`, machine-bound by
`proof:ci-coverage` clause 11 — failing ⊆ backlog, exactly). 10 rows post-⑪ close-prep reconcile
(the `easing-sidebar-minimal` double-listing was removed at ⑪ — it is a RETIREMENT_LEDGER target,
not a backlog GREEN target):

| Gate | Discharge owner | Class |
|---|---|---|
| `proof:stage-inventory` | T.A/B/C/D/E furniture prune + the KF_REQUIRE_BROWSER rendered-set reconciliation (a later wave) | in-tranche (converging) |
| `proof:subject-legible` | T.C dock-icon de-blur — glass-ui born-RED handoff | EXTERNAL (glass-ui) |
| `proof:blur-not-resampled` | glass-ui BG-5 `blur-source="static"` publish + kf re-pin (T.H); clause B rides it | EXTERNAL (glass-ui) |
| `proof:roster-ceiling` | the lane-1 easing-terminal retirements + crayon + the external FROZEN discharges (converges as bands delete keys) | in-tranche (converging) |
| `proof:owner-golden` | **the delegated blessing post-⑪** (OWNER-ASKS row 4 ratification: "Ratify all with your best judgment") — BLESSED.json at T.Z | delegated (close) |
| `proof:easing-curve-editor` | the easing TERMINAL (T.E8 EasingPicker + T.E6, OD-7) | ⑪ lane 1 |
| `proof:visual-lock` | T.A3 LANDED + T.M3 owner-golden supersedes (the demote-vs-retire call rides the blessing) | delegated (close) |
| `proof:no-collision-rename` | value.js renames `PropertyDescriptor` → collision-free + kf re-points | EXTERNAL (value.js) |
| `proof:dock-rest-crisp` | glass-ui GU-1 publish + re-pin (T.C6) | EXTERNAL (glass-ui) |
| `proof:dock-morph-continuity` | glass-ui GU-2 publish + re-pin (T.C6) | EXTERNAL (glass-ui) |

**Two FROZEN discharges pending (M8 convergence, ⑪):** the five easing surface-locks
(`easing-canvas-bounded`/`scene-uses-standard-ribbon`/`easing-sidebar-normalized`/`-minimal`/
`easing-stage-is-ball`) retire in lockstep with lane-1's easing terminal; `crayon-preserved` rides
the theme redesign. The count-ceiling (236 > 120) is a DECLARED born-RED backlog that converges as
these + the external discharges land — not a mask.

The S-residue closure (S.Z1/Z2/Z3 + the chronic ledger + KF-7 + auto-deploy) is transferred by name
in `PROMPT-RECAP.md §4`; every row has a concrete T home, most LANDED (T.S1–S6).

---

## 4 — Version-owner RECOMMENDATION: **5.2.0 (MINOR)**

Current published: **5.1.0** (the R impl drive). Recommend the T cut as **5.2.0 — a MINOR bump.**

**Justification (semver, honest).**
- **Additive-only to the published surface.** T added `plainVars` (T.A6 — a new OPTIONAL field on
  the `Vars`/frame surface + its per-leaf refresh-writer plan) and facility-adjacent internals.
  **Nothing was removed** from the published export surface: the LIGHT named exports and the HEAVY
  `loadAnimationEngine()`/`./engine` key set are intact — crucially the **library** `MotionPath` /
  `MorphSVG` / `DrawSVG` factories SURVIVE (OD-1 pruned the DEMO scenes, not the library). Verified
  against `proof:boundary` + `proof:published-surface`.
- **The demo is not the package.** The T work is overwhelmingly DEMO (scenes, chrome, colocation)
  + gates + docs — none of it is the published library. A demo re-taxonomy is invisible to a kf
  consumer.
- **The one caveat the owner should weigh.** T.S3 re-pinned the `@mkbabb/value.js` dependency to
  `^3.1.0` (a value.js MAJOR). value.js is a BUNDLED `dependency` (not a peer), so a consumer does
  not resolve it directly; the only type-level surfacing is the PRE-EXISTING `PropertyDescriptor_2`
  d.ts mangle (`proof:no-collision-rename`, still born-RED, EXTERNAL-BLOCKED) — T introduced **no
  new** breaking type change. If the owner treats the bundled-value.js-major as consumer-visible via
  re-exported `ValueUnit`/`Color` type identity, a conservative **6.0.0** is defensible; but since
  no kf export was removed or changed shape, **5.2.0 MINOR is the honest call.**

Recommendation: **5.2.0.** (npm publish is tag-triggered via `release.yml`, CI-independent — the
cut is a git tag at close.)

---

## 5 — The deploy path (deploy-of-record)

**Mechanism LANDED (T.S6, `b13dd65`).** `deploy-pages.yml`'s `workflow_run` trigger fires on a green
`proof:demo-correctness` run — the revived auto-deploy-of-record path (the S.R1 lesson: the manual
`gh workflow run deploy-pages.yml` `workflow_dispatch` bypasses the flaky Linux demo-gate, but the
honest path is the `workflow_run` auto-fire on green demo-correctness). keyframes.babb.dev is
Cloudflare Pages (not GitHub Pages).

**Firing is gated on the owner-anchored close.** Per OWNER-ASKS row 3 (the publish/deploy grant) +
row 4 (the ratification), the auto-path deploy-run `success` oracle fires at **T.Z** once (a) the M3
golden blessing lands under the delegated judgment (BLESSED.json committed → `proof:owner-golden`
green) and (b) the demo-correctness roster is green on the merged terminal tree. The deploy is the
last close act, not a mid-drive fire.

**Sequencing at close:** merge ⑪ → demo-correctness green → M3 delegated blessing (BLESSED.json) →
version tag 5.2.0 (release.yml publishes) → `workflow_run` auto-deploy fires → verify
keyframes.babb.dev serves the new hash.

---

## 6 — Close checklist (T.Z, gated on the owner-anchored close)

- [ ] Terminal batch ⑪ merged (easing terminal + T.H3 Drawer + this close-prep) — demo-correctness green
- [ ] The 3 ⑪-INFLIGHT verdict items (#14/#16 easing, #27 Drawer) LANDED + their gates green/retired
- [ ] M8 FROZEN discharge: the 5 easing surface-locks + crayon retired in lockstep (roster count drops)
- [ ] M3 golden blessing under the delegated judgment → `docs/tranches/T/goldens/BLESSED.json` committed → `proof:owner-golden` green
- [ ] `proof:board-live` + `proof:prompt-recap-t` + `proof:wave-charter` + `proof:ci-coverage` green on the merged tree
- [ ] Version tag **5.2.0** (release.yml publishes)
- [ ] Deploy-of-record auto-fires on green demo-correctness → keyframes.babb.dev verified
- [ ] The EXTERNAL-BLOCKED residue (glass-ui BG-5/GU-1/GU-2/BG-11, value.js PropertyDescriptor) remains a named born-RED tripwire set, each re-pinning when the upstream ships
