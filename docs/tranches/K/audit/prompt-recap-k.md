# Tranche K — prompt-recap-k · THE TOTAL PROMPT RECAP (A→J + this session)

**Lane:** `prompt-recap-k` (K audit, DOCS-ONLY). **Read-only on source/tests/gates/CI** — this
lane wrote exactly ONE file: this doc. **Tree at audit:** branch `tranche-j-dev == master @
`4f1fc4c` (Tranche J CLOSED 2026-06-11: kf `4.2.0` published, `release.yml` first-ever run
`27378331075`, the auto-deploy round-trip OBSERVED TWICE). **Method (inv ε):** every coverage
status is verified against the TREE TODAY (`package.json`, source `file:line`, `npm view`, the K
screenshot corpus the orchestrator triage already captured), NOT chain-trusted over the J FINAL.

**Substrate read in full:** `docs/tranches/J/audit/recap-{AB,CD,EF,GH,I-session}.md` (the A→I
lineage, 65 rows, 55 ADDRESSED / 10 PARTIAL at J-open — all resolved by J per FINAL §10);
`docs/tranches/J/FINAL.md §10` (the J prompt-recap terminal); `docs/tranches/J/J.md §MANDATE`
(the verbatim development + design directives); `docs/tranches/J/waves/J.W7c-impl.md` (the
mid-tranche live-audit U1–U8 register). This doc EXTENDS that substrate through 2026-06-11 23:2x.

The recap is held to inv ε: each row reaches a TERMINAL verdict — **ADDRESSED** (cited closed) /
**PARTIAL** (what remains, with the residual) / **UNADDRESSED** — and names its **K home**.

---

## 0. The reckoning in one paragraph

A→J is a chain of FALSE CLOSES corrected by the next tranche's live audit. The canonical lesson
(I-born, J-hardened): **a green source-shape gate is not a working product**; the H catastrophe
(97 gates green / 9 user-visible breakages) is the proof. J closed honestly *at the boundaries it
gated* (publish, auto-deploy, the W4 axes battery, `proof:live-session` B1 choreographed play) —
but **J's gates never drove the COLD hero CTA**, and the 2026-06-11 23:2x live audit (U-K1..K20)
re-opened exactly that unexercised axis: from the HERO start screen the rainbow-play does NOT
smoothly bind+animate the subject (it freezes while the playhead advances). This is the SAME
gate-blindspot class (an unexercised interaction axis) that B→I kept re-discovering, re-emerging at
the **cold-start / hero-CTA** axis that no `proof:*` gate actuates. K's charter is: drive the cold
hero path, root every U-K seed, total the layout/typography/currency debt, and close the
single-option-dropdown + design-language asks that J's W7c partially addressed but did not finish.

---

## 1. THE STANDING SPINE — re-asserted VERBATIM every tranche A→J, RE-ENTERING K

The user's standing development mandate + the standing design directive are NOT per-tranche asks —
they are the durable project spine. First NAMED as a §Mandate block in `E/E.md:26-53` (deriving
from `B/B.md:15`), carried verbatim into every tranche, and re-entered J's charter at
`J/J.md:111-130`. **They RE-ENTER K verbatim.**

### 1a — the development mandate (verbatim, `J/J.md:111-119`)

> *DEEPLY audit (32 agents in parallel) our original plan and waves thereof, alongside all changes
> made herein. Devise a path forward… recapitulate our original prompts, plans, and precepts: NO
> quick solutions, NO workarounds: idiomatic, gestalt approaches. This is a development product,
> architectural transpositions in the sake of elegance, simplicity, and performance above all are
> both necessary and desirable. NO legacy code. Delineate any chronically deferred items and fold
> them… Delineate any deferred items and fold them… Recap ALL of our prompts and requests hitherto
> and ensure they've been addressed. This is NOT an implementation phase. Tranche development only.*

| Clause | Status entering K | K home |
|---|---|---|
| DEEPLY audit (parallel agents) | **RE-ASSERTED** — K's audit fleet IS this clause; this lane is the recap leg | the K audit fleet |
| NO quick solutions / NO workarounds — idiomatic gestalt | **RE-ASSERTED VERBATIM** (binding charter precept A→J) | every K wave |
| Architectural transpositions for ELEGANCE/SIMPLICITY/PERF | **RE-ASSERTED** — invited, not optional | K impl waves (layout/typography/dock) |
| NO legacy beside its replacement | **RE-ASSERTED** (the ι sweep) | every K wave |
| Fold ALL chronic + deferred to terminal home or KILL (P-inv-28) | **RE-ASSERTED** — J's ledger was TERMINAL; K inherits the OUT/sibling-HANDOFF band + the U-K register | K deferred-ledger lane |
| Recap ALL prompts + ensure addressed | **THIS LANE** — the no-drops ledger, now subsuming A→J + this session | this doc |
| NOT an implementation phase / dev only | **HONORED** — K is DEVELOPMENT (docs); impl awaits explicit authorization on green CI | the K dev/impl boundary |

### 1b — the design directive (verbatim, `J/J.md:123-130`) · THE RECURRING DESIGN-AUDIT DIRECTIVE

> *Run a frontend design audit of our ui, easing ui — all UI panes. How might we better structure
> and suffuse proper design hierarchy of elements? Check for any obvious visual incongruences. Look
> for areas wherein we might better suffuse our design language of glass, grid, math, large and
> audacious typography, with colorful audacious pops, like those found in our icons (how might we
> increase this, too? within a sense of proportion), and our animation targets. What glass-ui
> idioms might we adopt — what glass-ui items, if totally befitting, might we smoothen, refine,
> hone, and abstract out — or just generally refine within an extant component — within glass-ui.
> Look for gaps.*

| Status entering K | Evidence | K home |
|---|---|---|
| **RE-ASSERTED VERBATIM — and SHARPENED by U-K1..K20** | J ran 14 design lanes (`J/audit/design/*.md`) + 48 screenshots; the J.W7a/W7c suffusion landed `--ball-tone`, the φ-dock, the FourierField, the spring/sequence/motion-path redesigns. The 23:2x live audit says the result still has WRONG fonts globally (U-K6/K8/K10), awful tabs (U-K12), inadequate spring UI (U-K11/K16), bad hierarchy (U-K18), and unresolved layout (U-K7). **The directive is the standing design spine AND its current dissatisfaction is itemized in U-K1..K20.** | the K design lanes + the K layout/typography wave |

**inv-16 binds every design fold:** kf consumes glass-ui PUBLISHED; a glass-ui refinement is a
HANDOFF to the AX session or a consume-edge of a published version — **never a kf-side patch**
(`J/J.md:154-156`). U-K6's "fix at the ROOT" (the dock display voice) is therefore a glass-ui
HANDOFF if it is a glass-ui primitive, or a demo token wire if the demo owns the surface — K must
partition this exactly as J partitioned the FourierField freeze (consumer-side `freeze` prop, NOT a
patch — `EditorStartScreen.vue:101-114`).

---

## 2. THE A→J PER-TRANCHE LINEAGE — coverage TODAY (the no-drops ledger)

Each tranche's prompt set, resolved against the J recap substrate + the tree. The full per-prompt
tables live in `J/audit/recap-{AB,CD,EF,GH,I-session}.md`; this is the terminal roll-up.

| Tranche | Prompt set (recap source) | Status TODAY | The residual (K home if any) |
|---|---|---|---|
| **A** | execute A in full · 3.0.0 release · export RAFPlayback · `--provenance` changesets · gate release on green CI (`recap-AB.md §1.2`) | **ALL ADDRESSED** | none — inv α/β still bite (VERIFY-ONLY) |
| **B** | deps-latest · 6-agent audit · gestalt/no-workaround · fold chronic+deferred · recap all · dev-only · e2e lighthouse · remove loading screen · 6 design agents · perfected CI · no-occlusion Playwright (`recap-AB.md §2.2`) | **ALL ADDRESSED** | none — inv γ/δ still bite |
| **C** | make B's close honest (7 gates) · φ-ladder · dogfood (inv ζ) · capture harness · π · engine residuals · recap (`recap-CD.md §Prompt-C`) | **ALL ADDRESSED** | none — inv ε/ζ born here, carry to K |
| **D** | decompose 5 oversized · design localized · brittleness · engine transposed · dock leveraged + mobile · terminate every deferral · recap (`recap-CD.md §Prompt-D`) | **PARTIAL→RESOLVED** | `AnimationMenuBar→TransportDock` rename (CD-1, D FINAL OVERCLAIMED closed) — **EXECUTED J.W2** (`TransportDock.vue` exists; `AnimationMenuBar.vue` gone). CLOSED. |
| **E** | encapsulation r2 · vueuse listener gestalt · styling r2 · perf+modern-web · deep-SOTA (engine bugs, zero-alloc, FrameCompiler SoA, platform-adopt, orchestration tier, demo elevate) · recap (`recap-EF.md §1-3`) | **ADDRESSED** (W7-S1/S2/S3 SoA withholds RECORD-only) | the typed-time-index / slot-map / incremental-updateSegments withholds remain un-measured (EF-2, RECORD) — K MEASURE-FIRST if re-litigated |
| **F** | fix benches · proof:all→CI · engine perf (delete-loop, sync-step) · serializer symmetry · adapter metadata · Sequence transport · MotionPath · text-wrap · undo/redo · a11y · rail/ball (`recap-EF.md §4-5`) | **ADDRESSED**; the value.js Band V is CHRONIC-by-design | EF-4 `composition`-honoring (WAAPI composite + rAF accumulate) BOOK; EF-5 value.js charter v2 sibling-HANDOFF; EF-8 MorphSVG sampler (VJ-F1) — all ride the next value.js re-pin |
| **G** | the re-pin spine (consume published siblings, ZERO kf edit) · DrawSVG/.finished · blend-leaf · rAF-leak · Playwright demo SHIPs (`recap-GH.md §G`) | **ALL ADDRESSED** | sibling HAND-OFFs (value.js E1/E2 `parseLinearStops` shim, VJ-F1/F2/F4/F6, MCI-5 `it.fails` witness, PT-2 packrat) STILL OPEN — ride next re-pin; **K must re-check `npm view @mkbabb/value.js` at open** (G-1/G-2) |
| **H** | demo-quality/design-language/FSM · four chronics · gate-regime blindspot (`recap-GH.md §H`) | **ADDRESSED via I's correction** — H was the catastrophe; I re-closed all four chronics with runtime gates; 5 H proxy gates RETIRED, `proof:no-route-storm` PHANTOM killed | the gate-blindspot is a STANDING risk; **U-K2/K3 prove it re-emerged at the cold-hero axis** → K |
| **I** | A1 standing mandate · B1–B9+K live breakages · orchestration directive · publish/deploy authorization · totality (`recap-I-session.md §a`) | **ADDRESSED** — 10/10 correctness gate keys present; publish+deploy EXECUTED; the YAML-invalid `ci.yml` caught post-close | the CI-on-Linux flake → DISCHARGED at J.W0 (per MEMORY); the version bump → CUT at J (`4.2.0`) |
| **J** | the §MANDATE (verbatim dev + design) · fold all deferrals · recap all · the mid-tranche live-audit U1–U8 · publish/deploy/glass-ui currency (`J/FINAL.md §10`) | **ADDRESSED** — `4.2.0` published, auto-deploy OBSERVED twice, all 5 boundaries oracle-discharged, U1–U8 BUILT (J.W7c) | **the COLD hero CTA was never gated** (J's own admitted unexercised axis) → re-opened by U-K2/K3; glass-ui currency `~3.11.2` now STALE vs `3.13.0` → U-K14 |

**No A→J prompt is dropped.** Every PARTIAL above has a cited resolution or a named K home. The
J FINAL §10 already reconciled the 10 lineage PARTIAL rows + every PROGRESS §"Open deferrals" fold
row to terminal verdicts; this lane confirms that reconciliation holds against the tree and carries
only the OUT/sibling-HANDOFF band + the brand-new U-K register into K.

---

## 3. THE J-SESSION IMPLEMENTATION EDICTS — the mid-tranche live-audit U1–U8

Beyond the J charter, the user issued a BINDING live-audit register mid-implementation (2026-06-11,
driving the running product). J dispositioned it as **J.W7c** (`J/waves/J.W7c-impl.md`); all eight
BUILT with named deltas + `file:line`. Verified against the tree:

| U | The edict | J.W7c disposition (cite) | Status TODAY | K residual |
|---|---|---|---|---|
| **U1** | dock proportions · golden-proportion asymmetry · no hardcoded offsets | BUILT — `--phi:1.618` single constant; `--dock-bottom-anchor` φ-derived (`style.css:128,223-229`; `TransportDock.vue:8`) | **PARTIAL** — φ-anchor landed, but **U-K7 says layout STILL needs WILD refinement (no hardcoded dock offsets, grid/subgrid, pathological screens)** → the U1 work is a down-payment, not the finish | K layout wave (U-K7) |
| **U2** | TransportDock SHRUNKEN state · collapsed pill = name + rainbow play | BUILT — `:always-expanded="false"`; `#collapsed` slot (`TransportDock.vue:23,152-181`) | **CONTRADICTED by U-K1** — the bottom dock now collapses, but U-K1 says "dock not shrunken by DEFAULT" (the TOP dock `ChromeDock` uses `:start-collapsed="true"` `ChromeDock.vue:178`; the user wants the shrunk default to hold/be correct) → re-verify which dock | K dock wave (U-K1) |
| **U3** | easing glass card · real dropdown chrome · kill the option-span override | BUILT — view-mode `<SelectTrigger size="sm">`; option span `font-mono normal-case` (`EasingTarget.vue:64-70`; `EasingSelect.vue:68,78`) | **ADDRESSED** (verify in K live drive) | — |
| **U4** | conditional selects · a lone-option dropdown must NOT render | BUILT — `<Select v-if="animationNames.length > 1">` (`TransportDock.vue:39`), `v-else` static label (`:93-95`) | **SUSPECT in the COLD-PATH P0** — see §4. The `v-if` gates the dock select on count; the freeze symptom may be downstream. **U-K16 says single-option dropdowns STILL render somewhere — the rule is NOT total** | K cold-path root + K single-option lane (U-K16) |
| **U5** | spring UI · redesigned from first principles · keyframes variant | BUILT — `SpringSidebar.vue` (367-line delta): SegmentedTabs view fork, `.labeled-field-grid`, in-cell preset balls, `linear()|@keyframes` artifact fork (`SpringSidebar.vue:21-126,217-231`) | **CONTRADICTED by U-K11/K15/K16** — U-K11 "spring UI still inadequate — no proper keyframes editor"; U-K15 "spring slider literally steps"; U-K16 "vizs need real OPTIONS" → the J.W7c spring redesign did NOT satisfy the user | K spring-redesign wave (U-K11/K15/K16) |
| **U6** | sequence · "entirely broken" · REDESIGN-or-REMOVE | BUILT — REDESIGN (kept; `SequenceTarget.vue` 445-line delta; the `--ball-p` shadow bug fixed `:484-491`) | **PARTIAL** — U-K5 "none of the animations work properly (/square)" + the cold-path freeze suggest the sequence subjects may still not drive in the cold path | K cold-path root |
| **U7** | motion-path · "just broken" · redesigned idiomatically | BUILT — stage sizes from its own slot (`MotionPathTarget.vue:291-296`), padding `p-6→p-4` (`:71`) | **ADDRESSED** (verify in K live drive) | — |
| **U8** | the 03.21.14 pane · "awful, redesign idiomatically" | BUILT — folded into U5 (the spring sidebar; `SpringSidebar.vue:1-22`) | **CONTRADICTED by U-K13** — U-K13 "two panes look awful (spring-adjacent)" + U-K8 "top dock expanded fonts wrong" suggest the U8 fold did not resolve the panes the user means | K design/typography wave (U-K13) |

**The J.W7c register is NOT a clean close.** Six of the eight U-rows are CONTRADICTED or PARTIAL
under the 23:2x re-audit — the design refinement was BUILT but did not converge to the user's bar.
This is itself the recurring pattern: a wave lands the named deltas (source-shape true) but the
felt product still fails the human (U-K1..K20). K must treat U-K1..K20 as the re-opened,
authoritative version of U1–U8 plus net-new asks.

---

## 4. THE GROUND-TRUTH P0 — the COLD hero-CTA path (verified + extended)

**The orchestrator triage (verify+extend):** the CHOREOGRAPHED play path works
(`proof:live-session` B1: cube 101 distinct transforms after openControlsPanel+select+play) but the
COLD path is broken — from the HERO start screen, clicking the rainbow play does NOT smoothly
transition to the cube animating; **subjects freeze while the playhead/slider advances**; the amiga
"floats and flashes"; the gates never drive the hero CTA (an unexercised-axis blindspot). Suspect:
the J.W7c U4 conditional-select deletion killed an auto-binding side-effect.

**VERIFIED in source (this lane):**
- The hero is `EditorStartScreen.vue` (shown when no scene playing); the rainbow play lives in
  `TransportDock.vue` (`#collapsed` mirror `:156-181` + expanded `:120-135`) and routes
  `emit("togglePlay")` (`TransportDock.vue:316-319`).
- `togglePlay → toggleAnimationGroup()` (`useAnimationGroupPlayback.ts:56-81`). **The group-level
  auto-bind STILL EXISTS:** lines `70-72` — `if (!storedControls.selectedAnimation) {
  selectedAnimation = allNames[0] }` before `animationGroup.play()`. So at the GROUP level the play
  CTA self-heals a missing selection. The U4 `v-if` (`TransportDock.vue:39`) only gates whether the
  dock SELECT renders — it does not remove this group auto-bind.
- **THEREFORE the suspect is NUANCED, not confirmed-as-stated:** the freeze-while-playhead-advances
  symptom means the GROUP plays (advancing `effectiveT`/the slider) but the per-subject animation
  is not VISUALLY bound on the cold mount. The likely seam is the managed-rAF / `markRaw` reactive
  bridge (`useAnimationSync` gated `useRafFn`) or the `onMounted` auto-play race
  (`AnimationControlsGroup.vue:219-223` plays only `if (autoPlay && animations.length > 0)` — and
  on a COLD hero the group may have ZERO animations at mount, so `onMounted` no-ops and the later
  group arrival via `watch(() => animationGroup, …, {flush:'post'})` `:211-216` only `syncPlayState`s
  if `group.started` — it does NOT (re)bind the subject). **This is a P0 root-cause lane's job; this
  recap lane CITES the seed + the suspect file:line and assigns the K home.**

| Seed | Symptom | Suspect seam (file:line) | K home |
|---|---|---|---|
| **U-K2** | hero rainbow-play → no smooth immediate transition to cube animating | the cold-mount bind race: `AnimationControlsGroup.vue:211-223` (the `onMounted` autoPlay no-ops on an empty cold group; the later `watch` only `syncPlayState`s) + `useAnimationGroupPlayback.ts:70-75` | **K cold-path root-cause wave (P0)** |
| **U-K3** | rainbow play "broken" while slider progresses (subjects freeze, playhead advances) | the GROUP advances `effectiveT` but the subject DOM write / `useAnimationSync` (`markRaw` rAF bridge) does not paint the cold-bound subject | **K cold-path root-cause wave (P0)** |
| **U-K4** | amiga floats + flashes constantly | the amiga subject (Three.js, `AmigaScene`) — the I.W3 `proof:amiga-subject-is-pivot` gate covers the choreographed path, NOT the cold path; "floats+flashes" is a cold-mount / visibility-pause race | **K cold-path root-cause wave (P0)** |
| **U-K5** | none of the animations work properly (/square) | the custom-transform-fn scene; same cold-path bind class | **K cold-path root-cause wave (P0)** |

**K MUST add an actuating gate that drives the COLD hero CTA** (navigate HOME → click rainbow play
WITHOUT openControlsPanel+select → assert the subject produces N distinct transforms). This is the
born-RED witness the cold-path fix needs, and the gate that closes the blindspot for good. The
existing `proof:live-session` B1 must be EXTENDED with a cold-start leg, not relied upon (it
pre-selects, masking the defect).

---

## 5. THE 2026-06-11 23:2x LIVE AUDIT — U-K1..K20 (each verified + rooted + K home)

The binding live-audit register. Each row is verified against the tree by THIS lane; the deep
rooting belongs to the owning K lane (cited). **No row is dropped.**

| # | The edict (verbatim intent) | Verification (file:line / observed) | Verdict | Owning K lane / wave |
|---|---|---|---|---|
| **U-K1** | dock not shrunken by default | TOP dock `ChromeDock` `:start-collapsed="true" :collapse-delay="2500"` (`ChromeDock.vue:178`); bottom `TransportDock` `:always-expanded="false"` (`TransportDock.vue:23`) — BOTH default-collapsed | **PARTIAL** (J U2 made them collapse; user wants default-expanded OR correct shrink) | K dock wave |
| **U-K2** | hero rainbow-play → no smooth transition to cube animating | §4 — the cold-mount bind race | **UNADDRESSED (P0)** | K cold-path root (P0) |
| **U-K3** | rainbow play broken while slider progresses | §4 — group advances, subject frozen | **UNADDRESSED (P0)** | K cold-path root (P0) |
| **U-K4** | amiga floats + flashes constantly | `AmigaScene`/Three.js cold-mount race; `proof:amiga-subject-is-pivot` covers choreographed only | **UNADDRESSED (P0)** | K cold-path root (P0) |
| **U-K5** | none of the animations work properly (/square) | cold-path bind class | **UNADDRESSED (P0)** | K cold-path root (P0) |
| **U-K6** | fonts wrong globally — bottom dock should carry the DISPLAY voice (Instrument Serif) — fix at the ROOT | the dock chrome is glass-ui (`GlassDock`); the display voice is `--font-display` (Instrument Serif, C.W2). If the dock font is a glass-ui primitive default → **glass-ui HANDOFF (inv-16, fix at ROOT)**; if demo-owned → a `--font-display` token wire | **UNADDRESSED** | K typography wave + glass-ui HANDOFF |
| **U-K7** | dock/stage/controls layout needs WILD refinement desktop+mobile — modern grid/subgrid, NO hardcoded dock offsets, pathological screens (docks + controls cluster past a max) per modern-web-guidance | U1 landed `--phi` φ-anchors (`style.css:128,206-229`) but the full grid/subgrid + max-cluster is NOT done; K screenshots `cube-3440x1440-ultrawide.png`, `cube-5120x2880-5k.png` exist for the pathological-screen probe | **PARTIAL** (U1 down-payment) | K layout wave (modern-web-guidance MANDATORY) |
| **U-K8** | top dock expanded fonts wrong | the `ChromeDock` expanded items' type ladder; same root as U-K6 | **UNADDRESSED** | K typography wave |
| **U-K9** | a wrapped line that should be one line | a specific overflow (likely a dock label / readout); identify in K live drive | **UNADDRESSED** | K layout/typography wave |
| **U-K10** | fonts inconsistent globally | the global type ladder coherence (`design-idioms.css` φ-ladder + glass-ui `text-*`); same family as U-K6/K8 | **UNADDRESSED** | K typography wave |
| **U-K11** | spring UI still inadequate — no proper keyframes editor | J.W7c U5 added a `linear()|@keyframes` ARTIFACT fork (`SpringSidebar.vue:110-126`) but NOT a real keyframes EDITOR (the Monaco/timeline editor the user means) | **PARTIAL** (J U5 insufficient) | K spring-redesign wave |
| **U-K12** | the top tabs look awful — pills if tabs at all, likely dock-dropdown items instead | the scene-switcher tabs in `ChromeDock`; the user wants pills or dock-dropdown items, not tabs | **UNADDRESSED** | K dock/nav wave (glass-ui SegmentedTabs/dropdown) |
| **U-K13** | two panes "look awful" (spring-adjacent panels) | the spring sidebar + an adjacent readout pane (`SpringSidebar.vue` / `SpringTarget.vue` / `StartingStyleTarget.vue`); J U8 folded U8→U5 but did not satisfy | **PARTIAL** (J U8 insufficient) | K spring/design wave |
| **U-K14** | upgrade to LATEST glass-ui (sliders etc.) | `npm view @mkbabb/glass-ui version` → **`3.13.0`**; kf pins `~3.11.2` (`package.json:182`) — a 2-minor STALE gap; the re-cut slider (U-K15) likely lives in 3.12/3.13 | **UNADDRESSED** (currency gap CONFIRMED) | K currency wave (re-pin `~3.13.0`, verify slider) |
| **U-K15** | the spring animation slider literally STEPS (not smooth) | a stepped slider — likely the glass-ui slider primitive pre-3.13; U-K14's re-pin may carry the smooth slider; verify the spring sliders bind a continuous (not discrete-step) model | **UNADDRESSED** | K currency wave + spring wave |
| **U-K16** | vizs need real OPTIONS (keyframes etc.); single-option dropdowns STILL render — the no-single-option rule must be TOTAL | J U4 gated the DOCK select (`TransportDock.vue:39`) but `<Select>`/`<LabeledSelect>` survive in `easing/EasingTarget.vue:60`, `easing/EasingSidebar.vue:63` — the rule is NOT total; "real options" overlaps U-K11 (spring needs keyframes editor) | **PARTIAL** (J U4 not total) | K single-option-totality lane + K spring wave |
| **U-K17** | a pane clipped on the LEFT + should be DRAGGABLE; the GREEN disliked — prefer the main-controls RED with DASHED outline for the FINAL state | the rest/final-state ball is `--ball-tone` parameterized, scene-green for sequence (`design-idioms.css:434-455`); the user wants the FINAL state to be RED + dashed (the main-controls style); a left-clipped pane needs a drag affordance | **UNADDRESSED** | K design wave (`--ball-tone` final-state seam) + K layout (clip+drag) |
| **U-K18** | better hierarchy with LESS useless information (two readout panes) | two readout/metric panes carry redundant info; the design directive's "design hierarchy" clause | **UNADDRESSED** | K design/hierarchy wave |
| **U-K19** | a demo where DRAGGING resizes the container instead of dragging | a drag seam binds the wrong gesture (resize vs drag) — candidate: `square/` or `orbital-drag/` (`OrbitalDrag.vue`, `useOrbitalPointer.ts`); identify exact pane in K live drive | **UNADDRESSED** | K drag-seam wave |
| **U-K20** | REMOVE the FourierField from the hero background; grid lines slightly less opaque | FourierField IS in the hero (`EditorStartScreen.vue:78-86,96`, glass-ui `/fourier-field`); J.W7a/W7c ADDED it (D18) — K REMOVES it; the grid-line opacity is a `--grid-*` token in `design-idioms.css`/`style.css`; K screenshots `fourier-removed-bottomleft.png`/`fourier-removed-sim.png` already simulate the removal | **UNADDRESSED** (a J-ADDED surface the user now rejects) | K hero wave (remove FourierField, dial grid opacity) |

**Net of the 20 rows:** 5 UNADDRESSED-P0 (cold path U-K2..K5), 9 UNADDRESSED design/layout/typography
(U-K6,K8,K9,K10,K12,K17,K18,K19,K20), 1 UNADDRESSED currency (U-K14, with U-K15 dependent), 5
PARTIAL (U-K1,K7,K11,K13,K16 — J built something but did not converge). **Zero ADDRESSED** — the
entire 23:2x register is live work for K. This is the strongest single signal that J's W7c design
band, though BUILT, did not meet the bar.

---

## 6. THE LAYOUT / MODERN-WEB DIRECTIVE (U-K7, called out separately per the orchestrator)

> *dock/stage/controls layout needs WILD refinement desktop+mobile — modern grid/subgrid, NO
> hardcoded dock offsets, pathologically large screens handled (docks + controls cluster past a
> max) per modern-web-guidance.*

**This is a first-class K wave, not a sub-bullet.** It binds the `modern-web-guidance` skill as
MANDATORY (the skill is in the K toolset; it must run FIRST for the layout work). The current state:
J U1 derived dock anchors from `--phi` (`style.css:128,206-229`) — a partial answer that still uses
viewport-unit offsets, not a true grid/subgrid composition. The pathological-screen requirement
(docks + controls cluster past a max width rather than stretching edge-to-edge on a 5K/ultrawide) is
UNBUILT; the K corpus already has `cube-5120x2880-5k.png` and `cube-3440x1440-ultrawide.png` as the
born-RED witness screens. **K home:** the K layout wave, modern-web-guidance-gated, with a
pathological-screen actuating probe (resize to 5120px, assert the dock+controls cluster does not
exceed a max and centers).

---

## 7. THE FourierField REMOVAL (U-K20, called out separately)

> *REMOVE the FourierField from the hero background; grid lines slightly less opaque.*

The FourierField was ADDED by J.W7a S4 (D18) as the hero math-motif suffusion
(`EditorStartScreen.vue:65-86`, `J.W7a` design-adopt A4). The user now rejects it. K REMOVES it —
a clean deletion of the `.fourier-vacancy` block + the two glass-ui imports (`:79-86,96-97`) + the
scoped style (`:178-205`) + the `prefersReducedMotion`/`freeze` plumbing (`:101-118`) that exists
ONLY for the field. **inv-16 note:** this is a pure demo deletion (the FourierField is a consumed
glass-ui primitive; removing the consumer touches no glass-ui). The grid-opacity dial is a
`--grid-*` / background-grid token in `style.css`/`design-idioms.css` (K to locate the exact token).
**K home:** the K hero wave.

---

## 8. THE glass-ui CURRENCY ASKS (U-K14/K15, the recurring re-pin)

The dep re-pin is a recurring ask every tranche (G was the re-pin spine; I bumped to `~3.9.0`; J to
`~3.11.2`). **TODAY:** `npm view @mkbabb/glass-ui version` → **`3.13.0`**; kf pins `~3.11.2`
(`package.json:182`) — STALE by 2 minors. value.js `^0.11.2`, parse-that `^0.9.0`
(`package.json:178-179`). **K home:** the K currency wave — re-pin glass-ui to `~3.13.0` (verify
the tilde semantics per H-5: a naive caret could pull a specular regression; the tilde is
intentional), re-check `npm view @mkbabb/value.js` for the E1/E2 `parseLinearStops` retirement
(G-1/G-2 sibling-HANDOFFs still OPEN), and VERIFY the U-K15 stepped-slider is cured by the newer
slider primitive. **inv-16 holds:** consume PUBLISHED; never patch.

---

## 9. THE RECURRING DESIGN-AUDIT DIRECTIVE (verbatim) — re-stated for K's binding

Reproduced verbatim in §1b above (`J/J.md:123-130`). It is the standing design spine A→J and the
PARENT of U-K1..K20 (every U-K row is an instance of "check for visual incongruences / suffuse
glass·grid·math·typography·colorful-pops / what glass-ui idioms / look for gaps"). **K re-runs the
full frontend design audit** over all UI panes (the 14-lane J design-audit shape:
`J/audit/design/{pane-*,cross-*,glassui-adopt,glassui-abstract}.md`), now anchored to the 20 named
dissatisfactions rather than a cold scan. The glass-ui REFINE/ABSTRACT half (U-K6 root fix, U-K12
pills, U-K15 slider, U-K17 dashed-final-state) partitions into: a demo-owned token wire, OR a
glass-ui consume-edge of `3.13.0`, OR a born-RED glass-ui AX HANDOFF — exactly the J partition
(`glassui-AX-handoff.md` RF-16/RF-17 are the J precedent).

---

## §FOLD — finding → severity → the seam → suggested K wave-class

| Finding | Severity | The seam | Suggested K wave-class |
|---|---|---|---|
| **PR-K1** Cold hero-CTA path broken: rainbow-play freezes subjects while the playhead/slider advances (U-K2/K3/K4/K5); the choreographed `proof:live-session` B1 masks it (it pre-selects) | **P0** | `AnimationControlsGroup.vue:211-223` (onMounted autoPlay no-ops on empty cold group; the `watch` only `syncPlayState`s) + `useAnimationGroupPlayback.ts:56-81` (group auto-bind is group-level, not per-subject) + `useAnimationSync` markRaw rAF bridge | **K cold-path root-cause wave** (born-RED cold-start gate FIRST: HOME → click play → assert N distinct subject transforms; then root the bind race) |
| **PR-K2** The gate-blindspot re-emerged at the COLD-START / hero-CTA axis — no `proof:*` gate drives the hero CTA (J's own admitted unexercised axis) | **P0** | `proof:live-session` covers PLAY-after-select, never the cold hero CTA; the axis is structurally unexercised | **K gate-regime wave** (extend `proof:live-session` with a cold-start leg; close the axis per the I-born gate-ORACLE precept) |
| **PR-K3** glass-ui currency STALE: pins `~3.11.2`, registry latest `3.13.0` (U-K14); the U-K15 stepped-slider likely fixed in the newer primitive | **P1** | `package.json:182`; `npm view @mkbabb/glass-ui` → `3.13.0` | **K currency wave** (re-pin `~3.13.0`, re-check value.js E1/E2, verify slider; inv-16 consume-only) |
| **PR-K4** Single-option-dropdown rule NOT total (U-K16): J U4 gated only the dock select; `<Select>`/`<LabeledSelect>` survive in easing | **P1** | `TransportDock.vue:39` (gated) vs `easing/EasingTarget.vue:60`, `easing/EasingSidebar.vue:63` (un-gated) | **K single-option-totality lane** (the no-single-option rule applied by construction across ALL panes) |
| **PR-K5** Layout needs WILD refinement: modern grid/subgrid, no hardcoded dock offsets, pathological-screen clustering (U-K7) — U1's φ-anchors are a down-payment | **P1** | `style.css:128,206-229` (φ-anchors, still viewport-unit offsets, no subgrid composition, no max-cluster) | **K layout wave** (modern-web-guidance MANDATORY; pathological-screen actuating probe at 5120px) |
| **PR-K6** Fonts wrong/inconsistent globally; the bottom dock should carry the Instrument-Serif display voice — fix at the ROOT (U-K6/K8/K10) | **P1** | the dock chrome is glass-ui `GlassDock`; `--font-display` (Instrument Serif, C.W2) is not threaded to the dock; root = glass-ui primitive default OR a demo token wire | **K typography wave** (+ glass-ui HANDOFF if the dock font is a primitive default; inv-16) |
| **PR-K7** Spring UI still inadequate: no proper keyframes EDITOR (U-K11), stepped slider (U-K15), needs real OPTIONS (U-K16), two adjacent panes "look awful" (U-K13) — J.W7c U5/U8 BUILT but did not converge | **P1** | `SpringSidebar.vue` (artifact fork, not an editor; the panes); the slider primitive (currency-dependent) | **K spring-redesign wave** (real keyframes editor; consume the 3.13 slider; hierarchy pass) |
| **PR-K8** REMOVE the FourierField from the hero (U-K20); a J-ADDED surface (W7a S4/D18) the user now rejects; grid lines slightly less opaque | **P1** | `EditorStartScreen.vue:78-86,96-97,101-118,178-205`; the grid-opacity `--grid-*` token in `style.css`/`design-idioms.css` | **K hero wave** (delete the field + its plumbing; dial grid opacity) |
| **PR-K9** Top tabs "look awful" — pills or dock-dropdown items, not tabs (U-K12) | **P1** | the scene-switcher in `ChromeDock`; glass-ui SegmentedTabs/dropdown available | **K dock/nav wave** (idiomatic glass-ui pills/dropdown) |
| **PR-K10** Dock default-collapsed contested (U-K1): top `ChromeDock :start-collapsed=true`, bottom `TransportDock :always-expanded=false` — the J U2 collapse may not be what the user wants | **P1** | `ChromeDock.vue:178`; `TransportDock.vue:23` | **K dock wave** (resolve the default-state intent with the user; re-baseline) |
| **PR-K11** The GREEN final-state disliked — prefer the main-controls RED + dashed outline; a pane clipped on the left + should be draggable (U-K17) | **P1** | `--ball-tone` final-state seam (`design-idioms.css:434-455`); the left-clipped pane + missing drag affordance (identify in K drive) | **K design wave** (red+dashed final state via `--ball-tone`) + **K layout** (un-clip + drag) |
| **PR-K12** Hierarchy: less useless info, two redundant readout panes (U-K18) | **P2** | the two metric/readout panes (identify; `AnimationVisualizer`/`MetricBadge` family) | **K design/hierarchy wave** |
| **PR-K13** A drag demo where dragging RESIZES the container instead of dragging (U-K19) | **P2** | a drag seam binds resize not drag — candidate `square/` or `orbital-drag/` (`OrbitalDrag.vue`, `useOrbitalPointer.ts`) | **K drag-seam wave** (identify the exact pane in live drive; correct the gesture) |
| **PR-K14** A wrapped line that should be one line (U-K9) | **P2** | a specific overflow (dock label / readout); identify in K live drive | **K layout/typography wave** |
| **PR-K15** Inherited sibling-HANDOFF band still OPEN: value.js E1/E2 `parseLinearStops` shim (`utils.ts`), VJ-F1/F2/F4/F6, MCI-5 `it.fails` witness, PT-2 packrat; `composition`-honoring (EF-4) BOOK | **P2** | `src/animation/utils.ts:106-130` shim; `test/interpolate-anything.test.ts` witness | **K deferred-ledger lane** (re-check `npm view @mkbabb/value.js` at open; fold-if-landed in the SAME motion; else re-affirm OPEN with born-RED kf gate) |
| **PR-K16** The J.W7c U-register (U1–U8) did not converge — 6 of 8 rows CONTRADICTED/PARTIAL under the 23:2x re-audit | **P2 (meta)** | the build-vs-felt gap: named deltas landed (source-shape true), the human bar unmet | **K design fleet** (treat U-K1..K20 as the authoritative re-statement of U1–U8; gate the FELT result, not the delta) |

**§FOLD totals:** 2 P0 (the cold-path defect + its gate-blindspot), 9 P1, 4 P2, 1 P2-meta. The two
P0s are the same defect from two angles (the broken cold path + the unexercised gate axis) — K's
first wave must be the cold-path root-cause + its born-RED cold-start gate, because every other U-K
design refinement is observed THROUGH that broken cold path.

---

## inv ε / inv-16 compliance

This lane wrote ONLY `docs/tranches/K/audit/prompt-recap-k.md`. ZERO source/test/gate/CI/demo
edits. Every status cites a tree anchor verified TODAY: `package.json:178-182` (kf `4.2.0`; glass-ui
`~3.11.2`; value.js `^0.11.2`; parse-that `^0.9.0`); `npm view @mkbabb/glass-ui version` → `3.13.0`;
`TransportDock.vue:23,39,316-319`; `useAnimationGroupPlayback.ts:56-81`;
`AnimationControlsGroup.vue:211-223`; `EditorStartScreen.vue:78-118,178-205`;
`ChromeDock.vue:178`; `SpringSidebar.vue:21-126`; `easing/EasingTarget.vue:60`,
`easing/EasingSidebar.vue:63`; `design-idioms.css:434-455`; the J recap substrate
(`recap-{AB,CD,EF,GH,I-session}.md`) + `J/FINAL.md §10` + `J/waves/J.W7c-impl.md`; and the K
screenshot corpus already captured by the orchestrator triage. The J claims were re-verified against
the running tree, NOT chain-trusted over the J FINAL. No prompt A→J or U-K1..K20 is dropped; each
has a terminal verdict + a named K home.
