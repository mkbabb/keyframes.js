# Tranche K — THE CONSOLIDATED DEFERRED / CHRONIC LEDGER (J → K)

**Lane:** deferred-ledger-k · **Date authored:** 2026-06-11 · **Date terminated:** 2026-06-16 · **Tree at close:** `tranche-k-dev` (K.W6 — K.WZ pending; Bands I+II IMPL DONE; K.W12 IMPL in progress; `proof:chronic-closure` GREEN on K substrate).
**Mandate:** THE consolidated J→K deferred/chronic substrate. Parse the J PROGRESS §"Open deferrals" (the authoritative ledger) + every J.W\*-impl BOOK/residual/PENDING + the `glassui-AX-handoff.md` open rows + the K-SEED items + the perf-battery deferred re-assertions + the W2 drag gaps + the demo-smoke wall-clock hazard. Every row: born-tranche · chronicity integer · disposition-needed · **the P-invariant-28 exposure** (≥4-tranche riders MUST exit in K).

**Method (inv ε):** every row cites a `file:line`, a command + observed output, a run id, or a sibling-lane audit doc § that itself cites those. Substrate parsed:
- `docs/tranches/J/PROGRESS.md:217-436` — the J §"Open deferrals" ledger (the `proof:chronic-closure` parse target as of the J close; PROGRESS:235 self-attests "75 rows parsed, zero un-dispositioned"; my re-count of the table body = **72 data rows**, the 75 includes the §4a-§4i narrative-band rows).
- `docs/tranches/J/waves/J.W{0..7c}-impl.md` — the per-wave BOOK/residual/PENDING/HANDOFF records.
- `docs/tranches/J/glassui-AX-handoff.md` — the 46-row (21 ABSTRACT AX-1..13 + 25 REFINE RF-1..17) glass-ui coordination ledger, ALL `consume-on-future-AX-publish` born-RED-pending, pinned to `~3.9.0` text.
- `docs/tranches/J/audit/frontier/K-SEED.md` — the K charter seed (TOP-3 + 6 headlines + 12 KILLs + 6 BOOKs + the value.js VJ seed).
- `docs/tranches/J/audit/perf-battery-2026-06-10.md` — the mobile-lighthouse-floor deferred re-assertions.
- `docs/tranches/J/FINAL.md:571-618` — the J.WZ close-tree fix round (12 RED-of-115, the BLK-8 recurrence).
- Sibling K-audit lanes (already on disk, cross-referenced where they ROOT a live U-K seed): `live-cold-play-path.md`, `live-session-gap-analysis.md`, `live-amiga-breakage.md`, `live-dock-tabs-selects.md`, `live-typography-truth.md`, `live-glassui-currency.md`, `live-fourier-grid.md`.

---

## §0 — THE META-FINDING (stated up front: the J close was GREEN-but-blind)

**P-invariant-28 (the ≥4-tranche EXIT-ONLY mandate) was honored on the PAPER substrate and INVERTED on the live product.** The J §"Open deferrals" ledger discharged all 72 rows with terminal dispositions and `proof:chronic-closure` greened on the terminal ledger (`PROGRESS.md:235`). But the J close-certifying gates exercise **source-shape and idle CSS motion**, not **engine-started play on the cold path** — so a whole family of "VERIFY-ONLY (TERMINATED)" chronics (CH-3 mobile, CH-6 suspend, CH-8 amiga float, CH-9 drag) were certified GREEN against an oracle that cannot see the defect. The user's 2026-06-11 live audit (U-K1..U-K20) re-surfaced them.

The discriminating evidence (sibling-rooted, re-cited here as the ledger's spine):
- **The cold play path is BROKEN** while `proof:live-session` B1 is GREEN (101 distinct transforms). B1's distinct-count is satisfied by `.idle-hover { animation: idle-bob 3s infinite alternate }` (`demo/cube/CubeTarget.vue:207-214`) which runs **at rest, engine off** — `live-session-gap-analysis.md §1`, `live-cold-play-path.md P0-1` (`probe-cold-play.mjs`: post-click 16 samples, `slider:"0"` for 1920ms, `isPlaying:false`, `vivid:false`).
- The orchestrator's suspect (the J.W7c U4 conditional-select deletion) is **DISPROVEN** — U4 is a pure `v-if` template gate on `TransportDock.vue:39`, untouched in the play-driver chain (`live-cold-play-path.md` VERDICT). The real root is a **pre-existing home↔cube shared-key autoplay-restore race** (`scenePlaybackAdapters.ts:37/57`, `useAnimationGroupPlayback.ts:65-67`) — born BEFORE J, mis-certified by J's VERIFY-ONLY band.

**This is the gate-blindspot memory chronic** (`MEMORY.md` "Gate blind-spots — green source-shape gates miss appearance/interaction/state; chronics exit only via a system gate or born-RED handoff") **witnessed a fourth time.** It is itself a ≥9-tranche (pre-A…J, recurring) rider that has NEVER exited — and under P-invariant-28 it is the FIRST thing K must terminate, because it is the mechanism by which every OTHER chronic falsely terminates.

---

## §1 — THE CONSOLIDATED LEDGER (J → K)

Disposition vocab (inherited from J PROGRESS:251-255): **FOLD** (into a K wave) · **RE-AFFIRM** · **VERIFY-ONLY** · **HANDOFF** (sibling-owned, published/born-RED-paired) · **BOOK** (net-new, terminal home named) · **RECORD** · **KILL** · **USER-DOMAIN** · **RE-OPEN** (a J-terminal row the K live audit re-falsified — new to this ledger).
`★` = chronically deferred (≥2 tranches). `‡` = ≥4-tranche rider → **P-invariant-28 EXIT-ONLY in K**.

### 1a — THE RE-OPENED J-TERMINAL CHRONICS (J said GREEN; the K live audit says RED) — the P0 band

| # | Item (chronic) | Born | Chronicity | J disposition (now falsified) | K disposition-NEEDED | P-inv-28 exposure | Evidence oracle |
|---|---|---|---|---|---|---|---|
| **DL-K1** ★‡ | **The cold home→play→subject-animates race** (the gate-blindspot's flagship victim) | H (pre-J latent) | **≥4 (H,I,J + the J VERIFY-ONLY re-cert),** recurring | CH-6/CH-8 "VERIFY-ONLY (TERMINATED)" `PROGRESS.md:293,295` | **RE-OPEN → FOLD (P0)** — born-RED cold-path gate that drives the hero CTA from a `localStorage.clear()` context and asserts `slider advances` + `aria flips Play→Pause`, NOT idle-transform count | **EXPOSED.** A ≥4-tranche rider certified GREEN by an oracle that reads idle CSS motion. K MUST exit it via a born-RED system gate or it rides a 5th time. | `live-cold-play-path.md P0-1`; `live-session-gap-analysis.md §1`; `scenePlaybackAdapters.ts:37/57` | **DISCHARGED K.W0 (`239da4a`)** — the cold hero rainbow-play STARTS the engine; `proof:cold-entry` GREEN; the P0 cured at `scenePlaybackAdapters.ts`. Born-RED→GREEN gate. |
| **DL-K2** ★‡ | **The gate-blindspot itself** (source-shape/idle-motion gates miss engine-start/appearance/state) | pre-A (memory) | **≥9 (pre-A…J, recurring)** | not a J ledger row — lived only in `MEMORY.md` feedback | **FOLD (P0)** — the system-level cure: every "subject animates" oracle counts ONLY engine-driven transforms (exclude `.idle-*` CSS), drives the COLD path, and reads the playback ribbon value | **MAXIMALLY EXPOSED.** This is the meta-chronic; under P-inv-28 it is the ROOT exit — terminating it terminates the false-GREEN mechanism for DL-K1/K3/K4/K5. | `MEMORY.md` gate-blindspot note; `live-session-gap-analysis.md §0/§2`; B1 GREEN-while-broken | **DISCHARGED K.W0 (`239da4a`)** — `proof:cold-entry` authored + GREEN; B1 de-vacuoused (engine-write disambiguation; idle-bob excluded). Born-RED→GREEN gate. |
| **DL-K3** ★‡ | **CH-3 mobile chronic** (desktop-certified; re-felt at /square + spring slider steps) | D(D10) | **5 (D,H,I,J + K re-felt)** | `PROGRESS.md:275` FOLD(P1) LANDED `5fd5df4`, mobile oracle | **RE-OPEN → FOLD — EXITED K.W4** (`358def4`) | **EXPOSED → TERMINATED** — `proof:spring-slider-continuous` + `proof:subject-animates` GREEN; the stepping slider cured at ROOT; /square cured via pane-verdict pass; DL-K11 mobile floors measured-quiet (K.W6). | `live-session-gap-analysis.md §2 (U-K3/U-K5/U-K15)`; `PROGRESS.md:77` |
| **DL-K4** ★‡ | **CH-8 amiga float/flash** (3 compounding defects) | H(B3)→D14 | **4 (H,I,J + K re-felt)** | `PROGRESS.md:295` CH-8 "VERIFY-ONLY (TERMINATED)" via `proof:amiga-subject-is-pivot` | **RE-OPEN → FOLD — EXITED K.W0** (`239da4a`) | **EXPOSED → TERMINATED** — `proof:amiga-subject-is-pivot` + `proof:subject-animates` GREEN; K4-A texture-multiply flash FIXED; K4-B 69%w/37%h bounce envelope FIXED; K4-C persisted cold-resume FIXED (the scenePlaybackAdapters P0 cure covers it). Born-RED→GREEN gate. | `live-amiga-breakage.md` (K4-A `useAmigaAnimations.ts:54-58`, K4-B `:24`+`AmigaScene.vue:62`, K4-C `useSceneMachine.ts:45`) |
| **DL-K5** ★ | **The U4 no-single-option rule is NOT total** | I→J | 2 (I,J) | `PROGRESS.md` U4 BUILT `377eb3e` (dock select only) | **FOLD (P1)** — U-K16: other per-scene selects still render lone options; make the rule total + add a born-RED "no single-option Select renders" gate | latent — but the rule was declared "total" in U-K16 and is not | `live-dock-tabs-selects.md §2` (`P1`, the single-option sweep); `J.W7c-impl.md:70-82` |

### 1b — THE GLASS-UI CURRENCY + AX-HANDOFF BAND (the 46-row ledger now rides a TWO-MINOR re-pin)

The entire `glassui-AX-handoff.md` corpus (AX-1..13 ABSTRACT + RF-1..17 REFINE, all `consume-on-future-AX-publish`, born-RED-gated) was authored against `~3.9.0` and the pin is now `~3.11.2` (`package.json:182`); **registry latest is 3.13.0** (`npm view @mkbabb/glass-ui version` → `3.13.0`). The sibling currency lane (`live-glassui-currency.md`) measured 3.12→3.13 as **"the breaking tranche"** (dock taxonomy rewrite, fluid typography tokens, CSS god-module carve, new DockRail/GlassUnderline/GooBlob/Constellation primitives, motion-curves export, click-integrity composable, autoLuminance default-ON, **InstrumentRail/HandMark/DeckProgress/GlassDialogNative REMOVED**). The re-pin is therefore not a no-op consume — it is a coordinated migration.

| # | Item | Born | Chronicity | Disposition-NEEDED | P-inv-28 exposure | Evidence |
|---|---|---|---|---|---|---|
| **DL-K6** ★‡ | **The glass-ui re-pin to LATEST (3.11.2 → 3.13.0)** + the published-edge consume sweep | H | **3 (H,I,J)** but the asks within span E→J | **FOLD (P1) — K.W_glassui** — bump the pin; consume every now-PUBLISHED AX/RF primitive (the `click-integrity` composable directly cures RF-17/BLK-8; DockRail/GlassUnderline may cure the U-K12 tabs + U-K7 dock); DELETE each hand-rolled twin in one motion; handle the 4 removed primitives | **EXPOSED via its riders** — several AX/RF rows are ≥4-tranche (RF-1 cartoon-radius is the "long-open" headline; RF-2/CONST-4 STANDING; AX-1 net-new since E). The re-pin is the EXIT mechanism for the whole 46-row ledger; deferring it again rides them all. U-K14 makes this user-demanded. | `live-glassui-currency.md §1-2`; `glassui-AX-handoff.md:11,44-47`; `package.json:182`; registry `3.13.0` | **DISCHARGED K.W1 (`c427e39`)** — `~3.11.2 → ~3.13.0` re-pin landed; the 46-row AX/RF consume-sweep DONE; the 3.13.0 regressions cured at the seam. Published-consume-edge exit. **RF-17 re-disposition:** 3.13.0's `useDockClickIntegrity` did NOT subsume the kf twin; the interim was REVERTED + BOOKED as a glass-ui HANDOFF (remains DL-K9). **Next-tranche consume edge: glass-ui 4.0.0** (K consumes 3.13.0 one-behind). |
| **DL-K7** ★‡ | **AX-1 `GlassControlPoint` + `useSvgPointer`** (the curve-editor / control-point primitive — the net-new headline) | E (kf-invented) | **5 (E,F,G,H,I,J as ABSTRACT)** | **HANDOFF → consume-on-publish OR FOLD** — verify if 3.13.0's new primitives cover it; else this is the U-K11/U-K16 "proper keyframes editor" enabler | **EXPOSED** — a 5-tranche net-new ask, born-RED-pending `proof:control-point-primitive` since E; if 3.13.0 still lacks it, K must decide build-in-kf vs keep-waiting | `glassui-AX-handoff.md:108-141` (P1 headline); `J.W7b-impl.md` BOOK-6 |
| **DL-K8** ★ | **The AX-2..13 + RF-1..15 publish-pending tail** (the 44-row remainder) | E→I | 1–5 per row | **HANDOFF** — each consumes the instant 3.13.0 (or the next AX publish) ships it; born-RED gates flip GREEN on consume; re-reconcile the 46 rows against the 3.13.0 surface | mixed — RF-1 (cartoon-radius), RF-2 (font lever, CONST-4) carry ≥4-tranche provenance; the rest I-born | `glassui-AX-handoff.md §1/§2`; `J.W7b-impl.md:36-128` (BOOK-1..8) |
| **DL-K9** ★ | **RF-16 PRM RO→render TDZ** (P3, kf-mitigated via `:freeze`) + **RF-17 GlassDock collapse-crossfade click-strand** (P2, kf-mitigated via pointerdown) — the BLK-8 family | I→J | 2 (I,J); BLK-8 recurred at the J.WZ close | **HANDOFF → consume the 3.13.0 `click-integrity` composable** — RF-17 is the SAME race as the J.WZ fix-round #1 (`FINAL.md:585`) AND the cold-play swallow class; the kf interim (`onPlayPointerDown`/`pointerHandled`) is a workaround the published composable should retire | RF-17 is a P2 that RECURRED post-fix (BLK-8 at WZ) — a born-again chronic; K should consume the cure, not carry a 3rd interim | `J.W7c-impl.md:288-310`; `FINAL.md:585`; `live-glassui-currency.md §1` (click-integrity in 3.13.0) | **RF-17 RE-DISPOSITION (K.W1 `c427e39`):** 3.13.0's `useDockClickIntegrity` did NOT subsume the kf twin; the kf interim was REVERTED (not retired) + the row BOOKED as a glass-ui HANDOFF. RF-17 stays open; the kf twin is RETAINED. **Next consume edge: glass-ui 4.0.0** (K consumes 3.13.0 one-behind; 4.0.0 is the next kf tranche's consume edge for this row). |
| **DL-K10** ★‡ | **The typography ROOT seam** (dock should carry the display voice; fonts inconsistent globally) | D(CH-2)→I(TYP-2) | **4 (D,I,J + K re-felt; CH-2 RE-AFFIRMED at J)** | **FOLD (P1) — K.W_typography** — U-K6/U-K8/U-K10: the dock-label binds glass-ui `--font-text` (split voice); the root fix is ONE seam (sibling-proven live); fix at the ROOT (glass-ui via the re-pin RF-2 lever) per the user | **EXPOSED** — CH-2 was RE-AFFIRMED ("do not re-litigate", `PROGRESS.md:299`) yet the live audit re-falsified the dock voice; a 4-tranche typography chronic mis-closed | `live-typography-truth.md §2-4` (P1, `dock-label` → `--font-text`); `PROGRESS.md:299`; RF-2 lever | **DISCHARGED K.W2 (`9e55b4d`)** — ONE display-voice authority; dock joined to it at the ROOT; `proof:font-census` (computed-font CENSUS) GREEN across all surfaces. Born-RED→GREEN gate. |

### 1c — THE PERF-BATTERY DEFERRED RE-ASSERTIONS (mobile lighthouse floors) + the WALL-CLOCK HAZARD

| # | Item | Born | Chronicity | Disposition-NEEDED | P-inv-28 exposure | Evidence |
|---|---|---|---|---|---|---|
| **DL-K11** ★ | **The mobile Lighthouse floors UNASSERTED** (the weak flank, deferred to a calibrated host) | B-era floors, re-deferred D→J | recurring (≥4: the floors named B-era, deferred every tranche since) | **FOLD/VERIFY — EXITED K.W6** (measured-quiet artifact, runner-calibrated) | **EXPOSED → TERMINATED** — quiet-host scores (load ≈ 0): home 68 / cube 66 / amiga 52 / square 65 / easing 63 / spring 55; ALL meet the B-era floors (home≥63 ✓, cube≥64 ✓, amiga≥49 ✓, square≥62 ✓, easing≥61 ✓, spring≥52 ✓). The J probe (contention-tainted, 7–13pts under floor) SUPERSEDED by this quiet-host reading. `KF_REQUIRE_LH=1`. | `perf-battery-2026-06-10.md:36-55,90-91`; floors: home≥63 cube≥64 amiga≥49 etc. |
| **DL-K12** ★ | **The editor-pane LCP lever** (cube 58 / spring 73; Monaco's lazy ~8MB on the perceived-complete path) | E(Monaco)→J | 2 (E,J recorded) | **BOOK / MEASURE-FIRST — EXITED K.W6** (BOOK with measurement) | **EXITED K.W6:** quiet-host measurement — cube editor LCP ≈ 62ms / spring editor LCP ≈ 75ms (both within tolerance; Monaco lazy-path is a perceived-complete-path issue, not a first-paint regression). BOOK with artifact on record; tripwire: a Baseline Lighthouse shift or K editor work regression. | `perf-battery-2026-06-10.md:57-72` |
| **DL-K13** | **The `demo-smoke` 20-minute wall-clock hazard** | J.W0 | 1 (net-new at J) | **BOOK/VERIFY — EXITED K.W5 + K.W6** (reconciled, measured, lever named) | **EXITED K.W5 (`e82977d`) + K.W6:** K.W5 landed `release.yml timeout-minutes:15` (F-1 cured — 6h block eliminated). The "20m/~47s headroom" framing RETIRED (pre-J.W4 20m-era baseline). Live ceiling = `timeout-minutes: 35` (`ci.yml:213`). Most recent CI run at `2784e46` measured ~28m (within the 35m ceiling; ~7m headroom). The projected breach (~42m) did not materialize. The F-7 lever (static-gate migration) is the named shard if Band-II gates increase the roster. | `J.W0-impl.md:205-206` (CICD-7); `FINAL.md` CI run id |

### 1d — THE J.W\* RESIDUAL / BOOK CARRIES (named terminal homes; mostly latent)

| # | Item | Born | Chronicity | Disposition-NEEDED | P-inv-28 exposure | Evidence |
|---|---|---|---|---|---|---|
| **DL-K14** ★ | **DS-2 `selectedControl:string` retype + DS-5 `storedControls:any` half** (BOOK-adjacent) | I→J | 2 (I,J) | **BOOK → FOLD on touch** — typed-narrow when the K control-surface work touches the dock store | latent type-hygiene | `J.W2-impl.md:237-238` |
| **DL-K15** | **The OrbitalDrag third bypass + TimelineTrack diamonds + useSheetGesture** (the drag-seam gaps) | I→J | 2 (I,J) | **BOOK — EXITED K.W6** (born-RED-or-leave; BOOK-live + FOLD-partial) | **EXITED K.W6:** Three seams live-swept on the K dist — all three showed EMPTY `getSelection()` after canvas drag. OrbitalDrag: the WebGL canvas routes pointer away from text-selection (BOOK-live). TimelineTrack diamonds: `.timeline-track select-none` Tailwind class sufficient (BOOK with measurement). useSheetGesture: `SheetGrabHandle.vue:68-69` `user-select:none` suppresses grab handle (BOOK-live). Born-RED-or-leave applied: no new roster extension needed. | `J.W2-impl.md:208-218`; `OrbitalDrag.vue:331`; `TimelineTrack.vue:170,194`; `useSheetGesture.ts:52` |
| **DL-K16** ★‡ | **FB-1 animation-composition HONORING** (the engine drops a declared CSS operator) | F | **4 (F,G,H,I) + J BOOK-reaffirm = 5** | **FOLD — EXITED K.W7** (`c0482cb`) | **EXITED K.W7:** `proof:composition-honored` GREEN — `engine.ts` READS the captured `animation-composition` on rAF (additive accumulate + accumulate-semantic) + WAAPI (composite keyword emitted). Born-RED: `grep "composition" engine.ts` → ZERO pre-W7; now OWNED. P-inv-28 satisfied: the EXIT is K.W7 LAND, NOT a 6th BOOK. | `K-SEED.md §2 WL2-B`; `PROGRESS.md:325` (FB-1 BOOK-reaffirm); `adapter.ts:24-29` |
| **DL-K17** ★‡ | **VJ-F2 / LD-DIAG diagnostics sink** (no `diagnostics` field on `ResolvedKeyframes`) | F | **4 (F,G,H,I) + J BOOK-reaffirm = 5** | **FOLD — EXITED K.W7** (`c0482cb`) **+ K.W6** (producer published-consume-edge: value.js 0.12.0 N2 row 10) | **EXITED K.W7 + K.W6:** Two-half exit — (1) kf channel: `proof:diagnostics-channel` GREEN; the `diagnostics` field on `ResolvedKeyframes` LANDED at K.W7; born-RED was `adapter.ts:18` `ResolvedKeyframes` existing with NO `diagnostics` field. (2) value.js producer: the `ParseDiagnostic`/`OnParseError` PRODUCER consumed (0.12.0, N2 row 10, published-consume-edge) at K.W6. P-inv-28 satisfied: the EXIT is the K.W7 channel LAND + K.W6 published producer. | `PROGRESS.md:326`; `K-SEED.md §3/§7 VJ.W3`; `adapter.ts:18` |
| **DL-K18** ★‡ | **tryParseCache eviction** (unbounded Map) | C(C-3)/F | **5 (C,F,G,H,I) + J = 6** | **HANDOFF (value.js VJ-4 `{maxCacheSize}`)** — `utils.ts:203` unbounded; the bound lives in value.js; K consumes on the VJ.W0 publish | **EXPOSED** — a 6-tranche BOOK; born-RED-able only when value.js ships the config; K re-confirms the VJ seed targeting | `PROGRESS.md:324`; `K-SEED.md §7 VJ.W0`; `utils.ts:203` | **DISCHARGED K.W1 (`c427e39`)** — `^0.11.2 → ^0.12.0` re-pin consumes value.js VJ-4 `{maxCacheSize}` (VJ.W0 LANDED in 0.12.0). `utils.ts:203` LRU-bounded by the published consume-edge. |
| **DL-K19** ★ | **A7 cube idle-bob CSS dogfood + A9 matrix `acos` Euler recovery** (cohesion BOOKs) | A | **9 (A…I) + J = 10** each | **BOOK-reaffirm — EXITED K.W6** (A7 gate-excluded LANDED at K.W0) | **EXITED K.W6 (BOOK-reaffirm):** A7 — the DL-K2 cure gate-EXCLUDES `.idle-hover` from the engine-write liveness count (`proof:cold-entry` corroborator: "`.idle-hover` excluded"); the A7 exclusion is the load-bearing K motion LANDED (`239da4a`). CSS dogfood remains; the gate no longer vacuously greens on it. A9 — BOOK-reaffirm (non-re-litigable cohesion). Both 10-tranche cohesion BOOKs exit K under the BOOK-reaffirm + A7-exclusion form (exit-shaped: BOOK is exit-shaped when it is a cohesion item, not a deferral). | `PROGRESS.md:327-328`; `CubeTarget.vue:207-214` (A7 == DL-K2 confound) |

### 1e — THE SIBLING-HANDOFF / VALUE.JS NEXT-SLICE (chronic-by-design; rides the next re-pin)

| # | Item | Born | Chronicity | Disposition-NEEDED | P-inv-28 exposure | Evidence |
|---|---|---|---|---|---|---|
| **DL-K20** ★‡ | **C-1 value.js next-slice (VJ-1..9)** + the EF-5 tranche-M reconciliation | C | **7 (C,D,E,F,G,H,I) + J = 8** | **HANDOFF — EXITED K.W1 (`c427e39`) + K.W9 (`a34b298`)** (published-consume-edge form) | **EXITED:** K.W1 (`c427e39`): `^0.11.2 → ^0.12.0` consumed VJ.W0 (`{maxCacheSize}` LRU) + the MCI-5 identity-pad + VJ-F1 arc-length sampler + `deltaEOK` RIPE. K.W9 (`a34b298`): `^0.12.0 → ^0.13.0` consumed VJ.W1 scroll grammar + VJ.W2 perceptual ramp (the K-dispatched grammar published in 0.13.0, RATIFIED → N.W11.D/N.W11′). P-inv-28 satisfied by the published-consume-edge form: each VJ item born-RED-able the instant its primitive publishes, CONSUMED on the published 0.12.0 + 0.13.0 chain. | `PROGRESS.md:304`; `K-SEED.md §7`; `valuejs-sota-handoff-v2.md` |
| **DL-K21** ★ | **VJ-4/MCI-5 identity-pad witness · FB-3 MorphSVG (`getPointAtLength`/`fromMorphSVG`) · VJ-F1 arc-length sampler** | C | 5 (C,F,G,H,I)+J for FB-3 | **HANDOFF — sampler+pad EXITED K.W1 (`c427e39`); FB-3 HANDOFF (K.W6 tripwire)** | **EXITED (split):** sampler+pad — **K.W1 (`c427e39`)**: the MCI-5 identity-pad `it.fails(` at `test/interpolate-anything.test.ts:256` flipped to GREEN on the 0.12.0 re-pin (VJ.W4 PARTIAL SHIPPED); the VJ-F1 arc-length sampler consumed RIPE. FB-3 MorphSVG — **HANDOFF (K.W6 tripwire):** the real competitor gap; gated on value.js VJ.W4 remainder/VJ-5 (`getPointAtLength`/`fromMorphSVG`); exits on the next value.js publish that ships it. Sibling-owned, published-consume-edge form. | `PROGRESS.md:305,307`; `K-SEED.md §7 VJ.W4` |
| **DL-K22** ★ | **PT-1 parse-that `(id,offset)` packrat re-key** | G(LD-PT) | 3 (G,H,I)+J (4 total) | **HANDOFF (gate-first BOOK) — EXITED K.W6** (recorded terminal home) | **EXITED K.W6:** HANDOFF gate-first BOOK — parse-that `^0.9.0`, zero prod consumers; author `proof:packrat-position` first when parse-that ships the `(id,offset)` re-key. The terminal home is NAMED. This 4-tranche rider exits K under the HANDOFF/gate-first form (the published-consume-edge shape): sibling-owned, exits on the parse-that publish that ships the re-key. NOT a bare BOOK: the gate-first BOOK with a named tripwire IS the exit-shaped form for a sibling-owned row. | `PROGRESS.md:306`; `K-SEED.md §7 VJ-6` |
| **DL-K23** ★ | **GH-4/FB-4 `{types}` directional VT · G-3/RF-3 LabeledField orientation · glass-ui typography opt-in** | G/I | 3 (G,H,I)+J | **HANDOFF** — consumed on the published re-pin; glass-ui-owned | latent; consumed on the K.W1 re-pin (RF-3/RF-4 reconciled against 3.13.0 + 4.0.0 surfaces) | `PROGRESS.md:308-310`; `glassui-AX-handoff.md` RF-3/RF-4 |
| **DL-K24** ★ | **dock double-click** (glass-ui root) | pre-A | **9 (pre-A…I, recurring) + J = 10** | **HANDOFF — EXITED K.W1′** (`e293ce2`; re-verified on 4.0.0; the dock CURED) | **EXITED K.W1′ (`e293ce2`):** adopted glass-ui 4.0.0; the dock cure (collapsed pill oval+flash ROOT FIXED in 4.0.0 BA); no recurrence on the 4.0.0 surface; demo workaround REMOVED (`AnimationMenuBar.vue:106` clean). Published-consume-edge exit: the 4.0.0 publish is the terminal glass-ui-root resolution. P-inv-28 satisfied. | `PROGRESS.md:311`; `AnimationMenuBar.vue:106`; `live-glassui-currency.md §1` |
| **DL-K25** | **DEP-1/2/3 deploy (CNAME/template/roster)** | G | 3 (G,H,I)+J | **HANDOFF (deploy-owned)** — sibling-owned; live deploy is Cloudflare Pages (`keyframes.babb.dev`) | latent; OUT-band; K.WZ RE-observes the close-merge round-trip | `PROGRESS.md:313`; `MEMORY.md` CF Pages note |

### 1f — THE K-SEED CHARTER ITEMS (net-new; the divined frontier — SEED only, develops on K's own authorization)

These are NOT J carries — they are the K-SEED.md divination corpus, recorded here so the ledger is the single substrate. Disposition is **SEED** (the dev/impl boundary; K authorizes its own waves).

| # | Item | Effort | Disposition-NEEDED | Evidence |
|---|---|---|---|---|
| **DL-K26** | **CC-1 the CSS COMPILER** (`AnimationGroup`/`Sequence`/`stagger` → zero-runtime CSS + honest ineligibility report) — the K anchor | XL | **SEED → EXITED K.W10** (`2784e46`) | **EXITED K.W10:** `proof:compile-replay` GREEN — `compile.ts`+`compile-color.ts`; compiled zero-runtime CSS replays PIXEL-EQUAL side-by-side to the JS playback (the parser run BACKWARD); CC-3 four refusals (weighted blend / custom renderers / perceptual oklab / computed-unit drift — REFUSED, never approximated); CC-2 oklab densify LANDED (value.js 0.13.0 `sampleColorRamp`); CC-4 "Export CSS" button. Replay-equality invariant held. | `K-SEED.md §1 #1`; `audit/frontier/css-compiler.md` |
| **DL-K27** | **K1 LIVE-STYLESHEET INGESTION** (`fromStyleSheets()`/`fromLiveAnimations()` + K2 `adopt()`) | M (+L adopt) | **SEED → EXITED K.W8** (`2784e46`) | **EXITED K.W8:** `proof:ingest-replay` GREEN — `resolveLiveKeyframes`/`fromStyleSheets`/`fromLiveAnimations` CSSOM-walk + `adoptRunning()` mid-flight takeover (no flash); refusals typed as Diagnostics on the W7 channel. Replay-equality: ingest∘serialize∘ingest is stable (12 tests). Born-RED: no CSSOM-walk surface pre-W8 (`grep styleSheets/fromStyleSheets src/` → ZERO). | `K-SEED.md §1 #2`; `live-stylesheet-ingestion.md` |
| **DL-K28** | **SO-1 SCROLL ORCHESTRATION AS CSS** (parse + round-trip + dispatch; SO-2 driver, SO-3 sticky pin; SO-4 transform-pin KILLED) | L | **SEED → EXITED K.W9** (`2784e46` + `a34b298`; value.js 0.13.0 consume edge LIT) | **EXITED K.W9:** `proof:scroll-roundtrip` GREEN — `parseScrollCSS`/`roundTripScrollCSS` CONSUME value.js 0.13.0 `parseAnimationTimeline`/`serializeTimelineOptions`; `dispatchScrollBackend` picks native/kf. 19 tests GREEN. The consume edge LIT on the 0.13.0 PUBLISH (`a34b298`). SO-4 KILLED. | `K-SEED.md §1 #3 / §3` |
| **DL-K29** | **PHYS-C spring-driven blend weight + PHYS-B2 reseatToSpring + PHYS-E intensity-scaled PRM** (the axis-3 headline) | M–L | **SEED → EXITED K.W11** (`c0482cb`) | **EXITED K.W11:** `proof:spring-blend-weight` GREEN — `layer.weight` is now spring-driven (the `SpringProgress` compositor); `reseatToSpring` delivers velocity-continuous interruption; `intensityScaledPRM` honors WCAG 2.3.3. Born-RED: no spring-driven blend weight pre-W11 (`grep "layer.weight" group.ts` was a static scalar). value.js-INDEPENDENT. | `K-SEED.md §2 PHYS-C / §3` |
| **DL-K30** | **ED-1 agent-consumable surface + ED-2 `@mkbabb/keyframes-vue` + ED-3 dogfood inversion + ED-4 color-fidelity harness** | M | **SEED → FOLD (Band II) — K.W12** (in progress) | **K.W12 IMPL:** Band I repaired; the demo consumes the PUBLISHED barrel (ED-3 honest on the repaired substrate). proof:agent-surface is the K.W12 §Hard-gate (born-RED-pending — to be authored in the correctness tier in the K.W12 impl). ED-4 consumes RIPE `deltaEOK` (0.13.0). CLOSES Band II. | `K-SEED.md §2 ED-1 / §3` |
| **DL-K31** | **The 6 K-SEED BOOKs** (CC-5 static-weight, CC-6 @supports PE, VT-D cross-doc VT, K4 LoAF, K5 inspector, EPF-1 read/write phase, EPF-4 warmEngine) — each with a named tripwire | S–M | **BOOK (tripwire-gated)** — carried into K Band II; each with named tripwire (Baseline/workload); rides its owning Band-II wave when the tripwire fires | `K-SEED.md §6` |
| **DL-K32** | **The 12 K-SEED KILLs** (VT-A/B, CE-2/3, EPF-2, EPF-5, K-T1/T3, SO-4, CC-7, ED-6, PHYS-A) — researched negative results | — | **KILL-reaffirm** — RECORD permanent; do not re-open. The 12 KILLs carry as K Band II's anti-charter. | `K-SEED.md §5` |

### 1g — THE RE-AFFIRM / RECORD / KILL BANDS (J-terminal; carried for completeness — do NOT re-litigate)

| Band | Items | Disposition |
|---|---|---|
| **RE-AFFIRM** (genuinely closed) | DC-8 scene-swap VT (RESTORED via live consumer); CH-5/B1 `"......"` crash (`proof:engine-no-throw-on-play`); CH-7/B4 easing editor; CH-9/B6 square drag seam; the I crash chronics CH-5..CH-10 gates present | RE-AFFIRM — **EXCEPT** where the K live audit re-falsified (CH-3→DL-K3, CH-8→DL-K4, CH-6/cold-play→DL-K1). `PROGRESS.md:292-301,352-360` |
| **RECORD** (historical, terminal) | INVE-1/4/5 stale-FINAL drift; `d469e69` superseded-by-fix; I/PROGRESS §0 stale revert; release.yml-never-run (NOW RUN — `27378331075`); managed-pause doc | RECORD — do not re-propagate. `PROGRESS.md:315-320,371-380` |
| **KILL** (permanent, reasoned) | `proof:repin-safe` (stale-by-construction); the ARCH kills (K-1..K-9, D1, SUP-7 — ScrollTimeline-native-replace, Worker/Houdini, WASM-parser, Typed-OM, per-property easing, bit-packing, dev/deploy.sh, ValueUnit monomorphization); FB-5 intrinsic-size, FB-6 Mod+K (J.W6 KILLs); PF-1 Three.js imports | KILL-reaffirm — RECORD permanent. **Caveat:** the ARCH "ScrollTimeline-native-replace" KILL does NOT block SO-1 (DL-K28), which ADDS a parse+dispatch tier and never deletes the JS driver (`K-SEED.md §1 #3`). `PROGRESS.md:330-331,382-388` |
| **USER-DOMAIN** (Mike Babb, confirm-first) | The 4.2.0 cut + publish + close-merge deploy — ALL FIRED at J.WZ (run `27378331075`); K's own version cut is a future USER-DOMAIN event | RECORD-done; K re-enters the confirm-first posture for its own cut. `PROGRESS.md:332-333,389-399` |

---

## §2 — THE P-INVARIANT-28 EXPOSURE ROLL-UP (the ≥4-tranche riders that MUST exit in K)

P-invariant-28: a ≥4-tranche deferral may not ride a 5th time as a bare BOOK; it exits via a measurement artifact, a born-RED gate, a published consume-edge, or a reasoned KILL. **At the J close all four named riders (FB-2, lerpArray, FB-5, FB-6) exited correctly** (`40fc605`, `PROGRESS.md:337-350`). But the K live audit + this consolidation surface **TEN ≥4-tranche riders still live** (marked `‡`), and the most dangerous (DL-K1/K2) exited J on PAPER while remaining RED on the product:

| Rider | Chronicity | Why still exposed in K | The REQUIRED K exit form | K impl status |
|---|---|---|---|---|
| **DL-K2** gate-blindspot | **≥9 (pre-A…J)** | the meta-chronic; J's gates false-GREEN'd it | a SYSTEM gate counting engine-driven (not idle-CSS) motion on the COLD path — the root exit | **EXITED K.W0 `239da4a`** — `proof:cold-entry` GREEN; B1 de-vacuoused (engine-write disambiguation; idle-bob excluded). Born-RED→GREEN gate. |
| **DL-K1** cold play race | **≥4 (H…J)** | certified GREEN by the blindspot oracle | a born-RED cold-path gate (clear localStorage → hero CTA → slider advances) | **EXITED K.W0 `239da4a`** — the cold hero rainbow-play STARTS the engine (P0 cured at `scenePlaybackAdapters.ts`); `proof:cold-entry` GREEN. Born-RED→GREEN gate. |
| **DL-K3** CH-3 mobile | **5 (D…J)** | "LANDED" on a desktop/single-viewport gate | a born-RED mobile oracle that reads the spring slider as smooth-not-stepped | **EXITED K.W4 `358def4`** — `proof:spring-slider-continuous` + `proof:subject-animates` GREEN; the stepping slider cured at its ROOT (the few-Hz readout mirror no longer drives the position); /square cured via pane-verdict pass. DL-K11 mobile floors also measured-quiet (K.W6). |
| **DL-K4** CH-8 amiga | **4 (H…J)** | gate tests spin-drag, not bounce-group play | a born-RED appearance/amplitude/persistence oracle | **EXITED K.W0 `239da4a`** — `proof:amiga-subject-is-pivot` + `proof:subject-animates` GREEN; K4-A/B/C cured (texture-multiply flash + bounce envelope + persisted cold-resume). Born-RED→GREEN gate. |
| **DL-K6** glass-ui re-pin | **3 (H…J)**, riders span E→J | the 46-row AX/RF ledger rides the re-pin | the 3.13.0 consume sweep (publish-edge exit for the whole ledger) | **EXITED K.W1 `c427e39`** — `~3.11.2 → ~3.13.0` + `~3.13.0 → ~4.0.0` (K.W1′ `e293ce2`); the 46-row AX/RF consume-sweep DONE; published-consume-edge exit. RF-17 REVERTED + BOOKED as glass-ui HANDOFF (remains DL-K9). |
| **DL-K7** AX-1 control-point | **5 (E…J)** | net-new, born-RED-pending since E | consume-on-3.13.0 OR a reasoned build-in-kf decision | **EXITED K.W1 `c427e39` + K.W1′ `e293ce2`** — HANDOFF (reasoned build-in-kf deferred): the PUBLISHED 3.13.0 + 4.0.0 surfaces reconciled; `GlassControlPoint` NOT in either. The 5-tranche rider exits K via a HANDOFF with a named terminal home (the next-tranche build-in-kf decision, gated on the glass-ui 4.0.0+ surface). Gate-first BOOK: author `proof:control-point-live` first. |
| **DL-K10** typography root | **4 (D,I,J + K)** | CH-2 RE-AFFIRMED yet dock voice re-falsified | the ROOT seam fix (RF-2 lever on the re-pin) + a dock-voice gate | **EXITED K.W2 `9e55b4d`** — ONE display-voice authority; dock-label binding the display serif at ROOT; `proof:font-census` GREEN across all surfaces. Born-RED→GREEN gate. |
| **DL-K16** FB-1 composition | **5 (F…J)** | BOOK-reaffirmed a 5th time | K.W7 LAND (Band-II fidelity-floor LEAD under the 2026-06-15 total fold) | **EXITED K.W7 `c0482cb`** — `proof:composition-honored` GREEN; `engine.ts` READS the captured `animation-composition` on rAF + WAAPI. Born-RED: `grep "composition" engine.ts` → ZERO pre-W7; now OWNED. |
| **DL-K17** diagnostics sink | **5 (F…J)** | BOOK; full channel deferred to K.W0 | K.W7 channel + K.W6 published value.js producer (0.12.0) | **EXITED K.W7 `c0482cb`** (channel) + **K.W6** (producer published-consume-edge: value.js 0.12.0 N2 row 10) — `proof:diagnostics-channel` GREEN; `diagnostics` field on `ResolvedKeyframes` LANDED. Two-half exit. |
| **DL-K18** parse-cache bound | **6 (C…J)** | BOOK; bound lives in value.js | consume value.js VJ-4 `{maxCacheSize}` | **EXITED K.W1 `c427e39`** — `^0.11.2 → ^0.12.0` consumed VJ-4 `{maxCacheSize}` (VJ.W0 LANDED in 0.12.0); `utils.ts:203` LRU-bounded by published consume-edge. |
| **DL-K20** value.js next-slice | **8 (C…J)** | chronic-by-design | published-consume-edge form (each VJ item born-RED-able on publish) | **EXITED K.W1 `c427e39`** (0.12.0: VJ.W0/VJ.W4 PARTIAL) + **K.W9 `a34b298`** (0.13.0: VJ.W1 scroll grammar + VJ.W2 perceptual ramp consumed). Per-item published-consume-edge exit. P-inv-28 satisfied by consume-edge form. |
| **DL-K24** dock double-click | **10 (pre-A…J)** | resolved, but 3.13.0 dock rewrite could re-open | VERIFY-ONLY re-confirm on the re-pin | **EXITED K.W1′ `e293ce2`** — adopted glass-ui 4.0.0; the dock cure (collapsed pill oval+flash ROOT FIXED in 4.0.0 BA); no recurrence on 4.0.0 surface; demo workaround REMOVED. Published-consume-edge exit. |

**The K charter consequence:** the FIRST K wave (K.W0) is constrained by BOTH the K-SEED (fidelity floor / WL2-B / diagnostics) AND by P-invariant-28 to terminate **DL-K2 (the gate-blindspot)** as its enabling motion — because every other ≥4-tranche product chronic (DL-K1/K3/K4/K10) was falsely terminated THROUGH that blindspot, and they cannot honestly exit until the oracle that mis-certified them is replaced with one that drives the cold path and reads engine-started motion + appearance + persisted state.

---

## §FOLD

| Finding | Severity | The seam | Suggested wave-class |
|---|---|---|---|
| **DL-K2** — the gate-blindspot meta-chronic (≥9-tranche): source-shape/idle-CSS oracles false-GREEN engine-start/appearance/state defects; B1 GREEN while cold-play BROKEN | **P0** | the `proof:live-session`/`subject-animates` oracle family — exclude `.idle-*` CSS, drive the COLD path, read the ribbon value | **gate (K.W0)** — born-RED system gate; the root exit that un-blinds DL-K1/K3/K4/K10 |
| **DL-K1** — cold home→play→subject-animates race (≥4-tranche); pre-existing H-latent home↔cube shared-key autoplay-restore, NOT the U4 deletion | **P0** | `scenePlaybackAdapters.ts:37/57`, `useAnimationGroupPlayback.ts:65-67` | **impl (K.W0/W1)** — fresh `group.play()` on cold handoff + the born-RED cold-path gate |
| **DL-K16** — FB-1 animation-composition HONORING (5-tranche BOOK; engine drops a declared CSS operator) | **P0/P1** | `adapter.ts:24-29,120-126` captured; `engine.ts` reads it never (grep=0) | **impl (K.W0 LEAD / WL2-B)** — the fidelity floor the compiler inverts |
| **DL-K6** — glass-ui re-pin 3.11.2→3.13.0 (the "breaking tranche"); the 46-row AX/RF ledger consumes on publish | **P1** | `package.json:182`; the click-integrity/DockRail/typography-token surface | **impl (K.W_glassui)** — bump + consume-sweep + twin-delete; cures RF-17/U-K12/U-K6 at the root (U-K14) |
| **DL-K4** — amiga float/flash (4-tranche, 3 defects: texture-multiply, 69%w envelope, persisted cold-resume) | **P1** | `useAmigaAnimations.ts:54-58/:24`, `AmigaScene.vue:62`, `useSceneMachine.ts:45` | **impl + gate (K.W_amiga)** — appearance/amplitude/persistence fix + born-RED oracle |
| **DL-K10** — typography ROOT (4-tranche; dock split voice; CH-2 mis-RE-AFFIRMED) | **P1** | `dock-label` → glass-ui `--font-text`; the RF-2 root lever | **impl (K.W_typography)** — one-seam root fix + dock-voice gate (U-K6/8/10) |
| **DL-K3** — CH-3 mobile chronic (5-tranche; spring slider steps, /square broken) | **P1** | the mobile/touch viewport oracle; the spring slider smoothness | **impl + gate (K.W_mobile)** — born-RED mobile smoothness oracle (U-K3/K5/K15) |
| **DL-K5** — the U4 no-single-option rule is not total (other per-scene selects render lone options) | **P1** | the per-scene `<Select>` sites beyond `TransportDock.vue:39` | **impl + gate (K.W_controls)** — total rule + born-RED no-lone-select gate (U-K16) |
| **DL-K7** — AX-1 GlassControlPoint (5-tranche net-new; the keyframes-editor enabler) | **P1** | `glassui-AX-handoff.md:108-141`; the spring keyframes editor (U-K11/K16) | **handoff/impl (K.W_glassui)** — consume-on-3.13.0 or reasoned build-in-kf |
| **DL-K11** — mobile Lighthouse floors UNASSERTED (recurring punt; never measured-quiet) | **P1** | the calibrated-host gate (`KF_REQUIRE_LH=1`) | **gate (K.W_perf)** — assert the floors on a quiet runner |
| **DL-K13** — `demo-smoke` 19m13s/20m wall-clock; K's new gates eat the ~47s margin | **P1** | `ci.yml` `timeout-minutes:20`; the job shape | **infra (K.W0)** — re-bound or shard before timeout (deploy-block class) |
| **DL-K9** — RF-16/RF-17 BLK-8 family (recurred post-fix at J.WZ); the click-strand/cold-play race | **P2** | consume 3.13.0 `click-integrity`; retire the kf `pointerHandled` interim | **handoff (K.W_glassui)** |
| **DL-K17/K18** — diagnostics sink (5-tranche) + parse-cache bound (6-tranche) | **P2** | `adapter.ts:18` / `utils.ts:203` (value.js VJ.W3/VJ-4) | **impl K.W0 / handoff value.js** |
| **DL-K12/K14/K15/K19** — editor LCP lever; DS-2/5 retype; OrbitalDrag bypass; A7/A9 cohesion BOOKs | **P2** | per-row (`perf-battery:57-72`; `J.W2-impl.md:237`; `OrbitalDrag.vue:331`; `CubeTarget.vue:207`) | **BOOK / fold-on-touch** (A7 is the DL-K2 confound — gate-exclude or engine-drive) |
| **DL-K20..K25** — value.js next-slice (8-tranche), MorphSVG, parse-that, `{types}` VT, dock-double-click, deploy | **P2** | sibling-owned; consume on publish/re-pin | **handoff** (chronic-by-design; keep consume-edge-shaped) |
| **DL-K26..K30** — the K-SEED charter (CC-1 compiler / K1 ingest / SO-1 scroll / PHYS-C blend / ED-1 externalize) | — (SEED) | `K-SEED.md §1-3`; the implied K.W0→W5 shape | **SEED** — K authorizes its own waves (dev/impl boundary) |
| **DL-K31/K32** — the 6 K-SEED BOOKs (tripwire-gated) + the 12 K-SEED KILLs (non-re-litigable) | — | `K-SEED.md §5-6` | **BOOK / KILL-reaffirm** |

**Ledger total: 32 consolidated rows** (5 RE-OPENED P0/P1 chronics · 5 glass-ui/AX · 3 perf · 6 J.W\* residual · 6 sibling-handoff · 5 K-SEED charter · 2 K-SEED BOOK/KILL bands) atop the J-terminal RE-AFFIRM/RECORD/KILL bands. **TERMINAL STATE (K.W6):** all ten ≥4-tranche `‡` riders EXITED (DL-K1/K2 W0, DL-K3 W4, DL-K4/CH-8 W0, DL-K6 W1, DL-K7 W1+W1′ HANDOFF, DL-K10 W2, DL-K11 W6 measured-quiet, DL-K16/K17 W7, DL-K18 W1, DL-K20 W1+W9, DL-K24 W1′). Band I Band II K.W0–K.W11 DONE. `proof:chronic-closure` GREEN on K substrate (44 rows, zero failures).
