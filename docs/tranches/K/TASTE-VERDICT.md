# K — the TASTE-boundary verdict (the user-domain close of the design band)

**The TASTE boundary (the K-born invariant):** gates carry CORRECTNESS; they do NOT
carry the design VERDICT. The design band of every appearance wave (K.W2 fonts, K.W3
layout, K.W4 panes + the K.W1′ dock) closes ONLY on the USER's verdict on the
review packet — a named USER-DOMAIN step, scheduled BEFORE the K close. An agent's
"designer-eye PASS" is corroboration, never the verdict (the J taste-tension that
produced agent-PASS-vs-user-"awful" is the precise failure this boundary closes).

## The packet presented (2026-06-16)

The before/after review packet under `audit/screenshots-k/`:
- **The dock cure** (`dock-4.0.0-{collapsed,expanded}-cube.png`): the collapsed pill a
  perfect circle (aspect 1.000, was the clipping oval), the flash gone, the expanded
  bar clean — glass-ui 4.0.0 adopted (K.W1′).
- **K.W4 the panes** (`k-w4-spring-{before,after}-desktop.png`,
  `k-w4-cube-ribbon-after-desktop.png`, the mobile twins): the spring made a real
  keyframes EDITOR; the stepping slider cured (211 distinct positions, was ~13); the
  motion-color unified RED-DASHED; the tabs pilled; the readout re-tiered; the
  FourierField removed. Plus the user's 2026-06-16 live findings: the rail ~25% wider
  (F1), one subtle wrapping border (F2), red-accent hover (F3), the thicker scrubber
  (F4), scrub-while-idle (F5), the icon-fit collapsed dock (F6).

## The verdict

**User (Mike Babb), 2026-06-16: "Meets the bar — close Band I."**

The design band of K.W1′/K.W2/K.W3/K.W4 CLOSES on this verdict. Band I (the
product-truth + design-totality repair) is COMPLETE: K.W0 (cold-entry P0), K.W1 +
K.W1′ (consume edge → 4.0.0, the dock cured), K.W2 (the typographic root), K.W3 (the
layout transposition), K.W4 (the pane verdicts + the live findings) — all on a
witnessed born-RED→green oracle, all committed/pushed, the TASTE verdict given.

## The directive carried into Band II

**User, 2026-06-16: "Drive it in totality"** — the round-trip frontier in full,
INCLUDING driving value.js 0.13.0 (the K-dispatched scroll + perceptual-ramp grammar,
N.W11.D / N.W11′) so the scroll/compile waves unblock. Full constellation execution:
value.js 0.13.0 published → kf Band II W7–W12 → K.WZ close.
