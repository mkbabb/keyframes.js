# K.W2 — THE TYPOGRAPHIC ROOT (∥ K.W3 · the design language made TOTAL at its voice-token root: ONE display authority, the dock joined to it AT THE ROOT, suffusion proven by a computed-font CENSUS not per-site classes)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** DESIGN-ROOT-in-K (the font half
  of the "beautiful at its roots" mandate; a 4-tranche typography chronic that mis-CLOSED —
  `DL-K10 ★‡`, CH-2 was RE-AFFIRMED "do not re-litigate" at J yet the live audit RE-FALSIFIED the
  dock voice — so this wave is BOTH a design-root and a P-invariant-28 termination). · **Scope
  (the demo's TWO style files + a re-verify of the glass-ui typography contract on 3.13.0, inv-16
  consume-published; NO source/test/gate/CI source written in DEV):** the voice-token authority in
  `demo/@/styles/style.css` (collapse `--font-serif` ≡ `--font-display` to ONE display token;
  close the `.font-display`→`--font-stack-display` trap; delete/repair the `style.css:41-44`
  comment that lies about the build) + ONE demo-side `@layer` rule binding the dock band
  (`.dock-label`) to the single display authority + the transport/tab register decision against
  that ONE token (`playback-button.css:22`, `tab-trigger.css:28`) + the U-K9 one-line-wrap seam
  (BOUNDED with K.W3 — voice is correct, the WRAP is layout) + the U-K10 census-driven suffusion
  sweep (the gate IS the census, NOT a hand-list of sites). · **DAG-deps:** **FOLLOWS K.W1 (the
  glass-ui re-pin to 3.13.0) — BINDING.** K.W1 lands the 3.13.0 surface; K.W2's dock-binding and
  the `.dock-label`→`--font-text` root all rest on the glass-ui typography contract, which 3.13.0
  REWRITES (the dock taxonomy + fluid-type tokens — `live-glassui-currency.md §1/§3.2`). The
  audit's `dock-label{font-family:var(--font-text)}` root was read on **3.11.2**
  (`node_modules/@mkbabb/glass-ui/dist/styles/typography.css:282-283`); K.W2 RE-VERIFIES it on
  3.13.0 before binding. **∥ K.W3 (the layout transposition) — file-adjacent in `demo/@/styles`
  but a BINDING disjoint-loci split:** K.W2 owns the VOICE tokens (`--font-*` families), K.W3 owns
  the GRID/anchoring tier (`--rail-width`, the work-area/dock-anchor chain). The U-K9 wrap and the
  `--graph-*` opacity tokens straddle the seam — partitioned below (§Hand-off). **W4 consumes** the
  single display authority (the pane re-cuts ride the new voice). **W5 owns the gate
  INFRASTRUCTURE seam** but the font-census gate LANDS WITH this wave (it certifies THIS surface —
  `K.md §wave-map`: "the remaining gate-truth legs land as the surfaces they certify land").

## §Provenance (the folded root causes + the chronic that mis-closed)

- `styling-typography-k.md` — THE decisive input, the SYSTEM/authority view, wave-ready. §0: the
  one-line answer ("There is NOT one font-voice authority. There are THREE voices but FOUR family
  tokens — and the display voice has TWO redundant authorities"); §1: the verified token chain
  (`style.css:40` `--font-serif` ≡ `:53` `--font-display`, both Instrument Serif; the `:113-117`
  `:root` body reclaim wins last); §2: the dock-renders-sans root mechanism (glass-ui
  `typography.css:283` `dock-label{font-family:var(--font-text)}`, no demo override, no glass-ui
  family knob); §3: the dual-authority SYSTEM defect (the 2 `--font-serif` sites
  `playback-button.css:22` + `tab-trigger.css:28` reach display via the OTHER alias, so a
  `--font-display`-only edit leaves them split SILENTLY); §4: the `style.css:41-44` comment FALSE
  on three counts (text-title/heading/subheading/dock-label bind `--font-text` NOT `--font-display`;
  glass-ui's default is Plus Jakarta NOT Fraunces; the census proves `text-heading` → SANS); §5:
  the `.font-display` utility → `--font-stack-display` (native sans), opposite its name, latent;
  §6: token hygiene CLEAN (no dead demo-owned tokens; the 4 documented deletions are notes not
  duplicates); §7: the U-K20 `--graph-*` opacity token home (BOUNDED to K.W4/hero — recorded, not
  this wave's call); §9: the five-step root-fix seam (collapse → bind dock → decide transport/tab →
  close the trap → repair the comment), ALL in `style.css`.
- `live-typography-truth.md` — the per-site LIVE census (the prior lane this wave's §0 VERIFIES and
  EXTENDS). §1: the voice histogram by land over 19 scene/state passes
  (`CONTROLS-PANE DISPLAY 30 / MONO 694 / SANS 33`; `DOCK DISPLAY 31 / SANS 21` — **the dock split
  is the defect**; `HERO DISPLAY 12` — coherent); §2: the dock split table (`.dock-scene-title`
  DISPLAY ✅ vs the 6 `.dock-label` sites SANS ✗); §3: U-K10 Play (serif via `.btn-playback`) vs
  Reverse (sans via `text-body`) in the SAME `grid-cols-2` row; §4: the ROOT fix proven LIVE
  (`root-fix-probe.mjs` injected ONE `@layer` rule against the unmodified dist, re-censused, all 6
  dock sites flipped DISPLAY, controls-pane mono + body prose UNTOUCHED — screenshot
  `cube-dock-AFTER-root-fix.png`); §5: U-K9 the home subtitle wrap at 390px (voice CORRECT, the
  WRAP is the defect — layout lane); §6: what is ALREADY correct (the φ-`text-display-*` rungs DO
  resolve Instrument Serif; mono data voice coherent; body reclaim works); §7: the 3.13.0 re-pin
  caveat (re-verify the `dock-label`/`text-heading` bindings on upgrade).
- `gate-estate-k.md §6` — THE born-RED witness root. `proof:demo-fonts` asserts only the NEGATIVE
  (no "Plus Jakarta" on body/dock/chrome) + Instrument Serif PRESENT on display elements + no font
  error — and **a dock rendering native sans instead of Instrument Serif PASSES the gate** (`§6`:
  "it never asserts `.dock-label` carries the DISPLAY voice"). The named gap: "A future K gate
  clause should assert `getComputedStyle(dock-label).fontFamily.includes('Instrument Serif')`."
  THIS is the born-RED shape — the existing gate is GREEN today on the split-voice dock; the census
  gate reds on it.
- `deferred-ledger-k.md DL-K10 ★‡` — the chronic that mis-CLOSED. The typography ROOT seam is a
  **4-tranche chronic (D[CH-2] → I[TYP-2] → J + K re-felt)**; CH-2 was RE-AFFIRMED ("do not
  re-litigate", `PROGRESS.md:299`) at J — yet the live audit RE-FALSIFIED the dock voice
  (`live-typography-truth.md §2-4`: `dock-label` → `--font-text`). Tagged **EXPOSED — a 4-tranche
  typography chronic mis-closed.** Under P-invariant-28 it must exit via a born-RED gate + the
  ROOT-seam fix, NOT another bare RE-AFFIRM. This wave is its terminal home.
- `live-glassui-currency.md §1/§3.2/§ U-K6 row` — the 3.13.0 contract context (the W1→W2 binding):
  3.12→3.13 is "the breaking tranche" (dock taxonomy rewrite + fluid-type `clamp()` tokens); AND
  (the row at `:423`) "glass-ui 3.13.0 font stack: `--font-stack-display: var(--font-stack-text)` =
  Plus Jakarta Sans. **Instrument Serif is NOT in the glass-ui token system.**" → the dock display
  voice MUST be a kf-side consumer override (the §S2 `@layer` rule); it is NOT addressed by the
  re-pin alone. This is WHY the wave exists DOWNSTREAM of W1 but is its OWN seam.
- `prompt-recap-k.md PR-K6/PR-K14` + the U-K register — the user verbatim: U-K6 "fonts wrong
  globally — bottom dock should carry the DISPLAY voice (Instrument Serif) — fix at the ROOT";
  U-K8 "top dock expanded fonts wrong"; U-K9 "a wrapped line that should be one line"; U-K10 "fonts
  inconsistent globally". All four UNADDRESSED at the J close.

## §The state, verified (file:line / live census / command+output)

Verified against the tree @ `tranche-k-dev` (forked off `4f1fc4c`) and the installed glass-ui
(`3.11.2` today — K.W1 re-pins to `3.13.0` BEFORE this wave; the contract re-verify is §S0).

- **Two display tokens, ONE face (the SYSTEM root):** `demo/@/styles/style.css:40`
  `--font-serif: "Instrument Serif", "Instrument Serif Fallback", Georgia, serif`; `style.css:53`
  `--font-display: "Instrument Serif", "Instrument Serif Fallback", Georgia, serif` — **identical
  value**. Live probe (`k-typo-system-probe.mjs`, 1440×900, built dist): `font-serif ===
  font-display ? true` (`styling-typography-k.md §0`). No rule exists for "which is the display
  voice."
- **The body reclaim (correct, do NOT touch):** `style.css:113-117` `:root` overrides
  `--font-stack-text`/`--font-text`/`--font-sans` to native sans, winning glass-ui's "Plus Jakarta
  Sans" by last-wins (verified in dist `index-BFS8FWWM.css`). `--font-mono: "Fira Code"` single,
  coherent (`style.css:54`). The display half is the defect, NOT the body/mono halves.
- **The dock renders SANS (the live defect):** glass-ui `typography.css:282-283` —
  `@utility dock-label { font-family: var(--font-text); … }` (RE-READ on the installed copy:
  CONFIRMED). The demo dock-label sites (grep, non-dist): `ChromeDock.vue:206,211,232,238`
  (top-dock controls-tab + scene-select triggers/groups) + `TransportDock.vue:50,65,94,147,153`
  (the bottom transport animation labels + "Timeline"). The demo overrides exactly ONE dock site
  to display: `ChromeDock.vue:299` `.dock-scene-title { font-family: var(--font-display) }` (the
  collapsed scene pill). Live census (`font-census-raw.json`, cube@desktop-closed): the pill "Cube"
  is `DISPLAY(Instrument Serif) 25.888px w600`; "Rotations" is `SANS(native) 20.352px w400` — **two
  voices in one band** (screenshot `cube-dock-closed.png`). Histogram: `DOCK DISPLAY 31 / SANS 21`.
- **The dual-authority surfaces (the SILENT-split hazard):** `playback-button.css:22`
  `font-family: var(--font-serif)` (the Play/Pause button) and `tab-trigger.css:28`
  `font-family: var(--font-serif)` (the top tabs) — both reach display via `--font-serif`, NOT
  `--font-display`. Live (`font-census-raw.json`): `DISPLAY "Play" (.btn-playback)` vs
  `SANS "Reverse" (text-body btn-interactive)` in the SAME `grid-cols-2` ribbon row
  (`PlaybackRibbon.vue:42` "Reverse" = `text-body`). **A `--font-display`-only dock edit would
  leave these on the OTHER alias — the split persists silently** (`styling-typography-k.md §3`).
- **The `.font-display` trap (latent):** glass-ui `components.css`
  `.font-display{font-family:var(--font-stack-display)}`; `--font-stack-display: var(--font-stack-text)`
  (glass-ui `tokens.css:52`, `theme.css:166` `--font-display: var(--font-stack-display)`) — the
  demo does NOT override `--font-stack-display`, so a bare `class="font-display"` resolves NATIVE
  SANS, the opposite of its name. The demo uses no bare `.font-display` today (only `text-display-*`
  + `var(--font-display)`) → LATENT (`styling-typography-k.md §5`).
- **The lying comment:** `style.css:41-44` (inside the `--font-serif` `@theme` block, re-read on
  the tree): asserts "text-display, **text-title, text-heading, text-subheading and dock-label all
  resolve var(--font-display)** (which would otherwise fall to glass-ui's default **Fraunces**…)."
  FALSE on three counts vs `typography.css` (RE-READ): `text-title:251`/`text-heading:259`/
  `text-subheading:267`/`dock-label:283` ALL bind `var(--font-text)`; glass-ui's display default is
  Plus Jakarta (`tokens.css:52`), not Fraunces; the census proves `text-heading` → SANS. The
  comment has MASKED U-K6/U-K8 since J (`styling-typography-k.md §4`, `gate-estate-k.md §6`).
- **The U-K9 wrap (voice CORRECT, layout defect):** `EditorStartScreen.vue:54-56`
  `<h2 class="start-screen-prose text-heading w-full italic">{{ subtitle }} <List class="inline"/>
  {{ subtitleSuffix }}</h2>` with defaults `subtitle="from the list"`,
  `subtitleSuffix="below, then press Play."` (`:131-133` — RE-READ). At 390px the inline `<List>`
  glyph + suffix overflow the 342px line → 2 lines (`wrap-probe.mjs`:
  `{"lines":2,"h":62,"w":342,"cls":"start-screen-prose text-heading w-full italic"}`). `text-heading`
  is the SANS body voice — **CORRECT for prose; the WRAP is the defect, not the family**
  (`live-typography-truth.md §5`).
- **The existing gate is a NEGATIVE (the born-RED root):** `scripts/proof-demo-fonts.mjs` asserts
  (a) no "Plus Jakarta" on body/dock/chrome, (b) Instrument Serif PRESENT on display elements,
  (c) no font `error`. It PASSES today on the split-voice dock because it never asserts the dock
  `.dock-label` carries the display voice (`gate-estate-k.md §6`). **This green-on-a-defect is the
  born-RED witness shape** (§Hard gate).
- **3.13.0 changes the contract the audit rests on (the W1→W2 re-verify):** the `dock-label` +
  `text-heading` family bindings above were read on **3.11.2**; 3.13.0 is "the breaking tranche"
  (dock taxonomy rewrite + fluid-type `clamp()` tokens — `live-glassui-currency.md §1/§3.2`).
  3.13.0 STILL ships `--font-stack-display: var(--font-stack-text)` (= Plus Jakarta) and STILL has
  no Instrument Serif in its token system (`live-glassui-currency.md` U-K6 row `:423`) → the
  dock-display override is STILL a kf-side seam. **S0 RE-VERIFIES the exact `dock-label`/φ-ladder
  family bindings on the 3.13.0 copy before §S2 binds them** — a fact this wave must confirm, not
  assume (the audit's source-of-truth was 3.11.2).

## §Goal

Make the design language TRUE at its font ROOT: there is exactly **ONE display token, ONE mono
token, ONE body token** — the "ONE font-voice authority" the lane was asked to find and could not —
and **every visible text element, the dock included, resolves to one of the three voices by
construction**, proven by a computed-font CENSUS, not by per-site font classes. Six moves, each at
the ROOT altitude the mandate demands (the font truth dies at ONE token authority, **NOT**
per-component font classes — `K.md §MANDATE`):

1. **Re-verify the 3.13.0 contract (S0):** RE-READ the `dock-label` + φ-ladder family bindings on
   the post-W1 3.13.0 glass-ui copy; confirm the `.dock-label`→`--font-text` root + the
   `--font-stack-display`→Plus-Jakarta trap survive the breaking tranche before binding them.
2. **Collapse the display voice to ONE token (S1):** `--font-serif: var(--font-display)` in
   `style.css` (the alias route) OR repoint the 2 `--font-serif` sites to `--font-display` + delete
   the `:40` reclaim — either way the display voice is single-sourced.
3. **Bind the dock band to it AT THE ROOT (S2 — U-K6/U-K8):** ONE demo-side `@layer` rule
   `.dock-label { font-family: var(--font-display) }` (proven live, `root-fix-probe.mjs`) flips all
   ~11 dock-label sites to the display voice, controls-pane mono + body prose UNTOUCHED — zero
   call-site churn.
4. **Decide the transport/tab register (S3 — U-K10):** Play/Reverse and the tabs MATCH against the
   ONE display token (today Play=serif, Reverse=sans in one row); the dual-authority silent-split
   becomes impossible.
5. **Close the trap + repair the lie (S4):** `--font-stack-display: var(--font-display)` in the
   `:root` reclaim (the `.font-display` utility, the φ-ladder, and the dock all agree); delete/
   repair the false `style.css:41-44` comment so it matches the build.
6. **The census-driven suffusion (S5 — U-K10):** the gate IS the census — every visible text leaf
   across all 8 scenes + docks + panes resolves one of {display, mono, body}, and the dock resolves
   DISPLAY — NO per-site font classes added; suffusion is proven, not patched.

## §Scope

- **S0 — re-verify the glass-ui typography contract on 3.13.0 (VERIFY-ONLY; the W1→W2 binding).**
  After K.W1 re-pins to `3.13.0`, RE-READ `node_modules/@mkbabb/glass-ui/dist/styles/typography.css`
  for the `dock-label` family bind (the audit's root: `:282-283`
  `dock-label{font-family:var(--font-text)}` on 3.11.2) and the φ-ladder family assignments
  (`text-display-*`→`var(--font-display)`; `text-title`/`-heading`/`-subheading`/`dock-label`→
  `var(--font-text)`), and the `.font-display` utility (`components.css`
  `.font-display{font-family:var(--font-stack-display)}`) + `--font-stack-display` home. **If the
  3.13.0 dock-taxonomy rewrite renamed/restructured `.dock-label` or moved the family bind**, the
  S2 selector + the S5 census probe adapt to the 3.13.0 class names — the PRINCIPLE (the dock band
  binds the single display token) is invariant; the SELECTOR is contract-dependent. The currency
  lane already confirms the two facts S2/S4 rest on SURVIVE 3.13.0: `--font-stack-display =
  var(--font-stack-text)` (= Plus Jakarta) and no Instrument Serif in glass-ui's tokens
  (`live-glassui-currency.md §1`, U-K6 row), so the dock-display override is STILL kf-side. **WHY:**
  the audit's source-of-truth was 3.11.2; this wave runs AFTER the re-pin; binding a 3.11.2
  selector against a 3.13.0 surface is the no-workaround trap (`§No-workaround` — NO 3.11.2-contract
  binding). VERIFY-ONLY: S0 reads, it does not edit glass-ui (inv-16; any glass-ui-root change is a
  HANDOFF — `glassui-handoff-k.md`).

- **S1 — collapse the display voice to ONE token (the SYSTEM root; `styling-typography-k.md §3`).**
  Locus: `demo/@/styles/style.css` (the `@theme` block + the 2 consumer sites). Two equivalent
  routes, the IMPL picks the cleaner against the no-legacy rule:
  - **Alias route:** `--font-serif: var(--font-display)` (in `@theme` or the `:root` reclaim) — the
    2 existing `--font-serif` sites keep working, now sourced from the ONE authority. Minimal churn.
  - **Repoint route:** change `playback-button.css:22` + `tab-trigger.css:28` from `var(--font-serif)`
    to `var(--font-display)` and DELETE the `style.css:40` `--font-serif` reclaim. Removes the
    redundant token entirely (the net-deletion preference).
  Either way: after S1 there is exactly ONE display token (`--font-display`); the `--font-serif`
  redundancy is GONE (deleted or reduced to a pure alias of the authority). **WHY (no-legacy):** two
  tokens carrying the identical face with no rule for which is "the display voice" is the
  redundancy that lets a future edit split the band silently (`styling-typography-k.md §3` — the
  exact mechanism). The cure is ONE authority, not a comment explaining the two.

- **S2 — bind the dock band to the single display token AT THE ROOT (U-K6/U-K8; the demo-side
  `@layer` seam, NOT per-site classes).** Locus: ONE rule in `demo/@/styles/style.css`, in an
  `@layer` ordered AFTER glass-ui's utilities so it wins the cascade:
  ```css
  @layer demo-typography {        /* after glass-ui; the dock band joins the display voice at the root */
      .dock-label { font-family: var(--font-display); }
  }
  ```
  This flips ALL dock-label sites (the ~11 enumerated in §State: `ChromeDock.vue:206,211,232,238`
  + `TransportDock.vue:50,65,94,147,153`, plus `.dock-scene-title` already display) to the single
  display authority. Proven LIVE on the unmodified dist (`root-fix-probe.mjs`, `live-typography-truth.md
  §4`): BEFORE → top pill DISPLAY, bottom labels SANS; AFTER the one rule → ALL dock sites DISPLAY,
  the controls-pane mono labels + body prose UNTOUCHED (the rule matches only `.dock-label`).
  Screenshot `cube-dock-AFTER-root-fix.png`. **NO per-site font classes** (the mandate's named
  forbidding): not a `.dock-display-label` utility sprinkled on each `<span>`, not 6 component
  edits — ONE selector, one place. **WHY:** the dock band SHOULD be the display voice (U-K6) and
  `.dock-scene-title` already proves the pattern; the ROOT is the `.dock-label` family bind, so the
  ROOT fix is one selector at the family bind. **The selector adapts to S0's 3.13.0 contract** — if
  3.13.0 renamed the dock label class, the rule targets the new name; the principle is invariant.

- **S3 — decide the transport/tab register against the ONE token (U-K10; fold into the dock pass).**
  Locus: `playback-button.css:22`, `tab-trigger.css:28`, and the sibling `text-body` controls
  (`PlaybackRibbon.vue:42` "Reverse"). The DECISION (a design call the IMPL makes, recorded with
  the §Hard-gate census): the transport band carries ONE voice — Play/Pause AND Reverse MATCH, the
  tabs MATCH (today Play=serif via `.btn-playback`, Reverse=sans via `text-body`, in the SAME
  `grid-cols-2` row — `live-typography-truth.md §3`). After S1 collapses the display authority, the
  decision is "both display" OR "both control/body" — but they MUST be the SAME voice, sourced from
  the ONE token. Note `live-typography-truth.md §3` flags a serif verb on a transport button is
  itself questionable; whichever way the decision goes, the dual-AUTHORITY incoherence (Play
  reaching display via `--font-serif`, the dock via `--font-display`) is dead after S1. **WHY:** two
  faces in one transport row is the visible U-K10 defect; the SYSTEM cause is the dual authority
  (S1 kills it); S3 is the design decision that the now-single token makes coherent.

- **S4 — close the `.font-display` trap + repair the lying comment (`styling-typography-k.md §4/§5`).**
  Locus: `demo/@/styles/style.css` (the `:root` reclaim + the `@theme` comment). Two one-liners:
  - `--font-stack-display: var(--font-display)` in the `:root` reclaim → the `.font-display` utility
    (which reads `--font-stack-display`), the φ-ladder, and the dock all resolve the SAME display
    face; the latent trap (a bare `class="font-display"` rendering native sans) is closed for free.
  - DELETE/REPAIR the `style.css:41-44` comment — it FALSELY claims text-title/heading/subheading/
    dock-label resolve `--font-display` and names Fraunces (glass-ui's default is Plus Jakarta). The
    repaired comment states the TRUE build: only `text-display-*` (+ the S2 dock-label rule) resolve
    display; the others bind `--font-text`. **WHY:** the comment encoded a belief the build does not
    honor — it has actively MASKED U-K6/U-K8 since J (a reader trusting it thinks the dock is already
    display — `gate-estate-k.md §6`). A comment that lies about the build is a no-legacy violation;
    repair it beside the edit that makes the assertion finally true (S2).

- **S5 — the census-driven suffusion (U-K10; the gate IS the census, NOT a site list).** Locus: the
  computed-font census harness (the audit's `font-census.mjs`/`reduce.mjs` family, re-purposed into
  the W5-owned gate infrastructure — the SCRIPT is W5's seam, the PROPERTY this wave certifies).
  After S1–S4, the census over all 8 scenes + docks + panes (desktop 1440×900 + mobile 390×844)
  asserts: **every visible text leaf resolves to exactly one of the THREE voices** {display =
  Instrument Serif, mono = Fira Code, body = native sans} — no orphan family, no Plus Jakarta, no
  glass-ui-default leak — **AND the dock band (`.dock-label` ∪ `.dock-scene-title`) resolves
  DISPLAY**. The histogram (the audit baseline: `DOCK DISPLAY 31 / SANS 21`) becomes `DOCK DISPLAY
  N / SANS 0` for the dock band. **NO per-site font classes** — suffusion is achieved by the S1–S4
  token authority + the ONE S2 rule, and PROVEN by the census; if the census finds a stray voice,
  the cure is at the TOKEN root (a missing alias, a missed `@layer` order), NOT a per-site patch.
  **WHY (the mandate's verbatim):** U-K10 "fonts inconsistent globally" is a TOTALITY claim; a
  hand-list of "these 6 sites are fixed" cannot bound it. The census is the only oracle that bounds
  "every visible text element resolves one of the three voices" — the suffusion is total or the
  census reds.

> **The U-K9 wrap (BOUNDED with K.W3, recorded here):** the home subtitle wraps to 2 lines at 390px
> (`EditorStartScreen.vue:54-56`). The voice is `text-heading` = CORRECT body sans
> (`live-typography-truth.md §5`); the defect is the WRAP. K.W2 does NOT change the family. The wrap
> cure (a tighter clamp on the subtitle size at phone width, `text-wrap: balance`/budget on the
> `<h2>`, or the inline `<List>` glyph reflow) is a LAYOUT seam — K.W3 (the layout-refine lane) owns
> it (`§Hand-off`). Recorded here because the typography census surfaced it; the WAVE that fixes it
> is W3. (Symmetrically, the U-K20 `--graph-*` opacity tune is the HERO/W4 lane's seam, not this
> wave — `styling-typography-k.md §7`.)

## §Hard gate (the proof:* that BITES — born-RED on TODAY's split-voice dock · the computed-font CENSUS gate)

**The oracle (per the gate-ORACLE precept):** a computed-font CENSUS over the BUILT dist —
`getComputedStyle(el).fontFamily` for every visible text leaf, through the same surface the human
reads, across all 8 scenes + docks + panes, desktop AND mobile — asserting the design's font
PROPERTY by construction, not a proxy. The existing `proof:demo-fonts` is a NEGATIVE-only oracle
(no Plus Jakarta) that GREENS on today's split-voice dock (`gate-estate-k.md §6`); this gate adds
the POSITIVE clauses that make the dock voice and the global three-voice totality finally
falsifiable.

- **clause (a) — the dock band resolves the DISPLAY voice (U-K6/U-K8; CORRECTNESS).** For every
  dock-label site (the ~11 enumerated: `ChromeDock.vue:206,211,232,238` + `TransportDock.vue:50,65,
  94,147,153`) and `.dock-scene-title`, on the BUILT dist, both desktop and mobile:
  `getComputedStyle(el).fontFamily` resolves to the display face — `.includes("Instrument Serif")`
  (the exact assertion `gate-estate-k.md §6` names). **BORN-RED WITNESS:** on TODAY's tree the
  bottom dock "Rotations"/"Timeline"/transport labels census `SANS(native)` (`font-census-raw.json`,
  cube@desktop-closed: "Rotations" = `SANS(native) 20.352px w400`); the histogram is `DOCK DISPLAY
  31 / SANS 21`. The clause REDS on those 21 SANS dock leaves. **BITE:** reds on the pre-cure tree
  (glass-ui `dock-label{font-family:var(--font-text)}` falls through to native sans); greens on S2
  (the `@layer` rule binds `.dock-label` to `--font-display`) + S1 (the display token is single-
  sourced so the bind is unambiguous). **NO escape:** the clause asserts the COMPUTED family per
  dock leaf — it cannot be satisfied by adding `--font-text` to the display stack (that would red
  the body census, clause (c)) or by a per-site class (the census reads the computed result, not
  the class; and §No-workaround forbids per-site classes).
- **clause (b) — ONE display token authority (the SYSTEM root; CORRECTNESS).** After S1, there is
  exactly ONE display-voice token. The assert (computed, not source-grep — the oracle reads the
  built CSS custom properties): `getComputedStyle(documentElement)` resolves `--font-display` and,
  IF `--font-serif` survives as an alias, `--font-serif === --font-display` (the alias is provably
  the SAME authority, not a parallel one); the 2 former `--font-serif` consumer sites
  (`.btn-playback`, `.tab-trigger-base`) census the SAME display family the dock does. **BORN-RED
  WITNESS:** today `font-serif === font-display → true` but they are TWO independent `@theme`
  declarations (`style.css:40` ≠ `:53`) — a `--font-display`-only edit would split them; the clause
  asserts they are ONE authority (alias-or-deleted), which is FALSE on today's two-declaration tree
  the instant the dock binds only `--font-display`. **BITE:** reds if `.btn-playback`/`.tab-trigger`
  census a different display source than the dock (the silent-split the audit names,
  `styling-typography-k.md §3`); greens on S1's collapse. *(The source-shape of "one declaration"
  is corroborated by the §S1 edit; the COMPUTED clause is that the consumer sites resolve the same
  authority as the dock — falsifiable through the surface.)*
- **clause (c) — the THREE-voice totality (U-K10; CORRECTNESS).** The census over all 8 scenes +
  docks + panes (desktop 1440×900 + mobile 390×844, the 19-pass matrix the audit ran): EVERY visible
  text leaf's computed `fontFamily` matches exactly ONE of the three voice fingerprints — display
  (`Instrument Serif`), mono (`Fira Code`), body (native sans, the reclaimed stack) — with ZERO
  leaves on "Plus Jakarta", glass-ui's Fraunces default, or any orphan family. **BORN-RED WITNESS:**
  the histogram today carries `DOCK SANS 21` IN THE DOCK BAND (a body voice where the design intends
  display) — the totality "the dock resolves display" is FALSE, so the combined clause (the dock
  band ∈ display) reds; additionally the `.font-display` trap means a leaf COULD resolve native sans
  under its own name (latent — clause is born-RED-able by adding a `class="font-display"` probe leaf
  if needed). **BITE:** reds wherever a leaf resolves a 4th family or the dock resolves body; greens
  when S1–S4 make every leaf resolve one of the three and the dock resolves display. **The census
  IS the suffusion oracle:** a per-site fix-list cannot satisfy it — it reads EVERY leaf.
- **clause (d) — the `.font-display` trap is closed (HYGIENE — labeled).** `getComputedStyle` of an
  element carrying bare `.font-display` resolves the display face (`--font-stack-display` now aliases
  `--font-display` per S4), NOT native sans. **BITE:** reds on the pre-S4 tree (the utility resolves
  `--font-stack-display` = native sans, opposite its name); greens on the S4 one-liner. *(Labeled
  HYGIENE: the demo uses no bare `.font-display` today (latent), so this corroborates the totality
  (c) but does not by itself certify a visible defect; it forecloses a future trap.)*
- **clause (e) — the comment matches the build (HYGIENE — labeled, no born-RED-able runtime).** The
  `style.css:41-44` comment, after S4, states the TRUE build (only `text-display-*` + the dock-label
  rule resolve display; the rest bind `--font-text`; no "Fraunces"). *(Labeled HYGIENE: a comment is
  not runtime-falsifiable; it is verified by reading the repaired comment against the §State facts —
  a doc-hygiene corroborator of the §S4 edit, never a substitute for clauses (a)-(c). Recorded so a
  future reader is not re-masked, `gate-estate-k.md §6`.)*

**The §spine bar — MUST bite.** Clauses (a)-(c) are the CORRECTNESS oracle: they read the COMPUTED
font family through the human's surface (the rendered dock label, the transport button, every text
leaf) over the BUILT dist, desktop AND mobile. Each asserts an EXACT property (every dock leaf
includes "Instrument Serif"; the consumer sites resolve the SAME authority as the dock; every leaf
∈ the three voices with the dock ∈ display). Revert S2 → (a) reds (the dock falls to native sans);
revert S1 → (b) reds (the consumer sites split from the dock's authority) AND (a) flakes (the bind
is ambiguous across two aliases); revert any of S1–S4 → (c) reds (the totality breaks — a stray
voice or a body-voice dock). **The born-RED witness is CONCRETE:** the audit histogram `DOCK
DISPLAY 31 / SANS 21` (`live-typography-truth.md §1`) + the named SANS dock leaves in
`font-census-raw.json` — the census is RED on that observed tree BEFORE the cure, GREEN after.
**Two-tier taxonomy:** the wave's GREEN depends on the CORRECTNESS clauses (a)-(c); clauses (d)
(the trap) and (e) (the comment) are HYGIENE corroborators that may NEVER substitute for a red
correctness clause. **P6 posture (declared):** the computed font family of a DOM element is
DEVICE-INDEPENDENT (the font-FAMILY resolution is CSS-cascade-deterministic, not a rasterized pixel
diff — the same `dock-label{font-family:var(--font-display)}` resolves identically on macOS and the
Linux runner; the metric-matched "Instrument Serif Fallback" is a CLS device-dependence handled by
the W5 font-LOAD wait, not this clause) — clauses (a)-(d) HARD-gate on Linux. **The W1→W2
contract:** the census probe's dock SELECTOR is bound to S0's 3.13.0 re-verify, not the 3.11.2
class names the audit read.

## §No-workaround prohibitions (BINDING — the mandate's named forbiddings for this wave)

- **NO per-component / per-site font classes.** The font truth dies at ONE token authority, NOT
  per-component font classes (`K.md §MANDATE`, verbatim). The cure is S1 (the single display token)
  + S2 (ONE `@layer` rule at the `.dock-label` family bind) — NOT a `.dock-display` utility sprinkled
  on each dock `<span>`, NOT 11 component edits, NOT a `font-family:` on each call site. The S5
  census reads the COMPUTED result across every leaf precisely so a hand-list of sites cannot satisfy
  it (`live-typography-truth.md §4`: "ONE demo-side rule flips all 6 sites… the fix at the ROOT, one
  token seam, not per-site patches").
- **NO second display token / NO retaining the redundancy.** S1 collapses `--font-serif` ≡
  `--font-display` to ONE authority (alias or deletion). Keeping both tokens "but documenting which
  is display" is the redundancy that lets a future edit split the band silently
  (`styling-typography-k.md §3`); a comment is not an authority. After S1 there is ONE display token,
  full stop.
- **NO binding the 3.11.2 contract against the 3.13.0 surface.** S0 RE-VERIFIES the `dock-label`
  family bind + the φ-ladder + the `--font-stack-display` home on the post-W1 3.13.0 copy before S2
  binds the selector. The audit's source-of-truth was 3.11.2; 3.13.0 is "the breaking tranche" (dock
  taxonomy rewrite — `live-glassui-currency.md §1`). Binding a stale selector is the workaround that
  would silently no-op against a renamed class.
- **NO glass-ui-root edit (inv-16).** The dock display voice is a kf-side CONSUMER override (the S2
  `@layer` rule) — Instrument Serif is NOT in glass-ui's token system and never will be by this wave
  (`live-glassui-currency.md` U-K6 row). K.W2 does NOT patch glass-ui's `dock-label{font-family:
  var(--font-text)}` in the demo's `node_modules`; any change to glass-ui's typography contract
  itself is a HANDOFF (`glassui-handoff-k.md`), not a demo patch. The demo OVERRIDES the consumed
  contract at the `@layer` it owns.
- **NO touching the body/mono halves (they are ALREADY correct).** The body reclaim
  (`--font-stack-text` native sans, `style.css:113-117`) and the mono voice (`--font-mono` = Fira
  Code) are single, coherent, and verified (`styling-typography-k.md §8`, `live-typography-truth.md
  §6`). S5 must not "fix" them; the defect is the DISPLAY family token redundancy + the dock split,
  not the body/mono halves. A census clause that reds a correct body/mono leaf is a gate bug.
- **NO changing the U-K9 family.** The home subtitle is `text-heading` = the CORRECT body voice; the
  defect is the WRAP (layout). K.W2 does NOT change the subtitle to display/nowrap-by-font — the wrap
  cure is K.W3's clamp/text-wrap seam (`§Hand-off`). Re-voicing the subtitle to "fix the wrap" is the
  workaround that breaks the prose voice.
- **NO raising/relaxing the census to pass.** If the S5 census finds a stray voice, the cure is at
  the TOKEN root (a missing alias, a missed `@layer` cascade order, a 3.13.0 selector mismatch from
  S0), NEVER excluding the offending leaf from the census or widening a voice fingerprint to
  swallow it. The census is the totality oracle; narrowing it is the U-K10 vacuity.

## §Folds (every K.md-assigned fold for this wave, with its evidence citation)

- **DL-K10 ★‡ — the typography ROOT seam, a 4-tranche chronic that MIS-CLOSED** (D[CH-2] → I[TYP-2]
  → J + K re-felt; CH-2 RE-AFFIRMED "do not re-litigate" yet the live audit RE-FALSIFIED the dock
  voice) — S1 (the single authority) + S2 (the dock bind) + the §Hard-gate census (the born-RED exit
  P-invariant-28 demands of a mis-closed chronic; NOT another bare RE-AFFIRM). `deferred-ledger-k.md
  DL-K10`, `PROGRESS.md:299`, `live-typography-truth.md §2-4`.
- **U-K6 — fonts wrong globally; the bottom dock should carry the DISPLAY voice (Instrument Serif),
  fix at the ROOT** — S2 (the ONE `@layer` `.dock-label` rule) + S1 (the single token it binds).
  `styling-typography-k.md §2/§3`, `live-typography-truth.md §2`, `prompt-recap-k.md PR-K6`.
- **U-K8 — top-dock expanded fonts wrong** — S2 (the top-dock controls-tab + scene-select triggers
  `ChromeDock.vue:206,232` carry `.dock-label` → flipped to display by the same one rule).
  `styling-typography-k.md §2`, `live-typography-truth.md §2`.
- **U-K9 — a wrapped line that should be one line** — BOUNDED to K.W3 (the voice is correct; the
  WRAP is layout); RECORDED here because the typography census surfaced it. `live-typography-truth.md
  §5`, `prompt-recap-k.md PR-K14`. (§Hand-off.)
- **U-K10 — fonts inconsistent globally** — S1 (kill the `--font-serif` ≡ `--font-display`
  redundancy) + S3 (the transport/tab register against the ONE token) + S5 (the census-driven
  suffusion that PROVES the totality). `styling-typography-k.md §0/§3`, `live-typography-truth.md
  §3`, `prompt-recap-k.md PR-K6` (U-K10 family).
- **The `proof:demo-fonts` negative-only gap** — the §Hard-gate POSITIVE clauses (a)-(d) extend the
  existing negative oracle; the dock-display assertion `gate-estate-k.md §6` named is clause (a).
  `gate-estate-k.md §6`. (The gate INFRASTRUCTURE seam — where the census script lands and how it
  rides `demo-smoke` — is W5's; the PROPERTY this wave certifies lands WITH this wave.)
- **The `style.css:41-44` lying comment + the `.font-display` trap** — S4 (close the trap, repair
  the comment), clauses (d)/(e). `styling-typography-k.md §4/§5`, `live-typography-truth.md §2`.
- **The 3.13.0 contract re-verify** — S0 (the W1→W2 binding); the audit's 3.11.2 source-of-truth
  re-read on the re-pinned surface before S2 binds. `live-glassui-currency.md §1/§3.2`, U-K6 row;
  `live-typography-truth.md §7`.
- **Token hygiene + the U-K20 grid-opacity / dead-style sweep** — RECORD (no-op confirmation): no
  dead demo-owned tokens, the 4 documented deletions are notes not duplicates, the size ladder is
  single-sourced; the U-K20 `--graph-*` opacity tune is the HERO/W4 lane's seam (re-checks the
  `§Hard-gate clause-g` legibility floor), NOT this wave. `styling-typography-k.md §6/§7`.

## §Hand-off / cross-wave boundaries (BINDING)

- **← K.W1 (the re-pin, BINDING UPSTREAM):** K.W1 lands glass-ui `3.13.0`; K.W2's S0 RE-VERIFIES the
  `dock-label`/φ-ladder/`--font-stack-display` contract on that surface before S2 binds the selector.
  K.W2 does NOT run before W1 lands locally (`K.md §wave-map`: "the re-pin must precede the design
  waves so W2/W3/W4 build on 3.13.0, not against it"). The two facts S2/S4 rest on (no Instrument
  Serif in glass-ui tokens; `--font-stack-display` = Plus Jakarta) SURVIVE 3.13.0
  (`live-glassui-currency.md` U-K6 row) — the dock-display override stays kf-side.
- **∥ K.W3 (the layout transposition, BINDING DISJOINT-LOCI):** K.W2 owns the VOICE tokens
  (`--font-*` families) + the dock-band font bind + the transport/tab voice decision; K.W3 owns the
  GRID/anchoring tier (`--rail-width`, the work-area / dock-anchor / slack→offset chain,
  `layout-grid-k.md`). File-adjacent in `demo/@/styles` but the spec boundary is BINDING. **The two
  straddling seams, partitioned:** (1) **U-K9 wrap → K.W3** — the family is correct (K.W2's domain
  confirms it), the WRAP cure (clamp/text-wrap/`<List>` reflow) is layout (`live-typography-truth.md
  §5`). (2) **the `--graph-*` opacity (U-K20) → K.W4/hero** — recorded by the typo lane
  (`styling-typography-k.md §7`) but it is the hero/visual-refine seam, not a voice token. If the
  IMPL finds a font-size clamp at phone width (the W3 wrap cure) wants a voice-token co-edit, the
  VOICE half is K.W2's and the SIZE/wrap half is K.W3's — named in each wave.
- **→ K.W4 (the panes, BINDING DOWNSTREAM):** the pane re-cuts (U-K11 spring editor, U-K12 tabs→
  pills, U-K13/K18 readout hierarchy) consume the SINGLE display authority + the suffused voices
  K.W2 lands. The U-K12 tab register (today `.tab-trigger-base` reaches display via `--font-serif`
  — `styling-typography-k.md §3`) is decided as a SHAPE in W4 (pills/dock-dropdown), but the VOICE
  it carries is K.W2's single token; W4 must not re-introduce a second display authority.
- **→ K.W5 (the gate infrastructure, BINDING):** the computed-font census SCRIPT (the
  `font-census.mjs`/`reduce.mjs` family industrialized) + its wiring into `demo-smoke` + the
  `proof:demo-fonts` POSITIVE-clause extension + the TASTE review-packet capture harness are W5's
  seam. K.W2 AUTHORS the census PROPERTY (the §Hard-gate clauses); W5 INDUSTRIALIZES the gate around
  it and owns the `demo-smoke` wall-clock disposition (`DL-K13` — every added gate erodes the live
  35m ceiling's headroom against the post-J.W4 roster; W5/W6 re-measure, re-bound or shard). K.W2's clauses are device-INDEPENDENT (font-family resolution) —
  they HARD-gate; the font-LOAD wait (the CLS metric-fallback device-dependence) is W5's harness
  primitive, not this wave's clause.
- **→ K.WZ (the close + the TASTE boundary, BINDING):** the font band is an APPEARANCE band — its
  design VERDICT is NOT carried by clauses (a)-(c) (those carry CORRECTNESS: the dock resolves
  display, the totality holds). The TASTE packet for the font band (per-pane before/after
  screenshots, desktop+mobile — the dock split→unified, the Play/Reverse match, the global voice
  coherence) is generated at W4's close (the packet harness rides W5's capture lib) and presented to
  the user BEFORE the version cut; the font band's design closes ONLY on the user's verdict on that
  packet (the TASTE boundary, `K.md §invariants`). An agent's "the dock looks coherent now" is
  corroboration, never the verdict. The TASTE packet rows for the font band: **the dock voice
  (split→display), the transport register (Play/Reverse match), the global three-voice coherence.**
- **OUT / sibling (do NOT touch):** glass-ui's `dock-label{font-family:var(--font-text)}` contract
  itself → HANDOFF (`glassui-handoff-k.md`; inv-16 — the demo OVERRIDES, never patches
  node_modules); the body/mono voices (ALREADY correct — §No-workaround); the U-K20 grid opacity →
  K.W4/hero; the cold-path P0 / B1 vacuity → K.W0 (a frozen subject is an ENGINE defect, not a voice
  defect — `design-synthesis-k.md §intro`).

## §Design decisions (trade-offs RESOLVED)

- **The cure is ONE display token + ONE `@layer` rule, NOT per-site classes — RESOLVED.** The mandate
  forbids per-component font classes (`K.md §MANDATE`); the audit proved LIVE that one `@layer` rule
  flips all dock sites with zero call-site churn and leaves the mono/body voices untouched
  (`root-fix-probe.mjs`, `live-typography-truth.md §4`). The display redundancy is collapsed at the
  token (S1), the dock joined at the family bind (S2) — the ROOT, not the surface.
- **Collapse `--font-serif` to `--font-display`, not the reverse — RESOLVED.** `--font-display` is the
  φ-ladder's own name (glass-ui's `text-display-*` rungs already bind it); making it THE authority and
  aliasing/deleting `--font-serif` keeps the demo's display token aligned with the consumed contract
  (`styling-typography-k.md §3`). The IMPL picks alias-vs-delete by the no-legacy rule (delete if no
  consumer needs the name; alias if minimal churn wins) — either yields ONE authority.
- **The census is the suffusion oracle, NOT a fix-list — RESOLVED.** U-K10 "fonts inconsistent
  globally" is a TOTALITY claim; only a computed-font census over every leaf can bound "every visible
  text element resolves one of the three voices, the dock resolves display." A per-site list is
  structurally unable to certify totality and is exactly the vacuity the engine-write-disambiguation
  invariant's sibling logic forbids (assert the PROPERTY, not a proxy count). The §Hard-gate census
  IS the U-K10 oracle.
- **W2 runs AFTER W1, binds against 3.13.0 — RESOLVED.** The design waves build ON the re-pinned
  surface, not against it (`K.md §wave-map`); the audit's 3.11.2 contract is re-verified on 3.13.0
  (S0) before the selector binds. The two facts the dock-display override rests on survive the
  breaking tranche, so the WAVE's principle is invariant; only the selector is contract-bound.
- **The font band's design closes at the TASTE boundary, NOT on a gate — RESOLVED.** Clauses (a)-(c)
  carry CORRECTNESS (the dock resolves display; the totality holds) — they are necessary and
  device-independent. They do NOT carry the design VERDICT (is the unified serif dock BEAUTIFUL?).
  That is the user's, on the W4-close TASTE packet, before the version cut (`K.md §invariants` — the
  TASTE boundary). A green census is the floor; the user's verdict is the band's close.
- **The transport/tab voice is a design decision made coherent BY the single token — RESOLVED.** Today
  Play=serif / Reverse=sans is the visible U-K10 defect; the SYSTEM cause is the dual authority. S1
  kills the dual authority; S3 is then a clean "both display OR both body" decision against the ONE
  token, recorded with the census. The serif-verb-on-a-transport-button question
  (`live-typography-truth.md §3`) is the IMPL's design call within S3 — but whichever way, the two
  buttons MATCH, sourced from one authority.
