# K.W1 — THE CONSUME EDGE (the glass-ui re-pin · `~3.11.2 → ~3.13.0` · the W7b parity-clause exits re-examined on the breaking tranche · the 46-row AX/RF ledger's EXIT mechanism · inv-16 held throughout)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** SHIP-in-K (P1; the
  infrastructure/currency wave — a `package.json` pin + the `proof:deps-current` floor + the
  consume-edge re-verification battery + the visual-lock re-pin protocol + the hand-rolled-twin
  net-deletions the published primitives make redundant). kf pins `"@mkbabb/glass-ui": "~3.11.2"`
  (`package.json:182`) — the tilde resolves `>=3.11.2 <3.12.0`, so BOTH 3.12.0 and 3.13.0 sit
  OUTSIDE the window (`live-glassui-currency.md §0`, `glassui-handoff-k.md §1`). The published
  latest is **3.13.0** (`npm view @mkbabb/glass-ui version` → `3.13.0`, published
  2026-06-12T02:11:45Z — `glassui-handoff-k.md §1`). The 3.12.0→3.13.0 hop is "the breaking
  tranche" (dock-taxonomy rewrite, fluid-typography tokens, CSS god-module carve, 4 removed
  primitives, `useDockClickIntegrity`, `autoLuminance` default-ON — `live-glassui-currency.md §1`);
  every one of its breaking seams is verified DISJOINT from kf's consume surface (§State-verified).
  U-K14 (*"upgrade to LATEST glass-ui (sliders etc.)"*) makes the re-pin user-demanded; the re-pin
  is the EXIT mechanism for the whole 46-row AX/RF handoff ledger (`deferred-ledger-k.md` DL-K6 —
  "deferring it again rides them all"). · **Scope (the consume edge — `package.json` + ONE gate
  floor + the demo consume seams the published primitives make redundant; inv-16 UNFENCED on the
  consume half, glass-ui itself NEVER patched):** the pin advance (`package.json:182`) + the
  `proof:deps-current` floor advance (`scripts/proof-deps-current.mjs:80`) + the RF-17 twin
  net-deletion (`TransportDock.vue:306-341` — the `useDockClickIntegrity` upstream cure makes the
  kf pointerdown workaround redundant) + the `autoLuminance` disposition on the two kf docks
  (`TransportDock.vue`, `ChromeDock.vue`) + the visual-lock baseline RE-CAPTURE at the re-pin (the
  H.W8 precedent) + the K3-internal diagnostics VERIFY-LANDED disposition. (The press-scale/RF-16/
  F-1 hazard STATUS is RECORDED here per the K.W1 cluster's "press-scale/RF-16 hazards status"
  charge — but RF-16's durable cure is a BOOKED P3 AX ask, NOT a K.W1 edit, and the F-1 release.yml
  `timeout-minutes` IMPLEMENTATION is K.W5-owned per `K.md §clusters`; K.W1 RECORDS the status and
  hands the YAML edit to K.W5, it does not write it.) · **DAG-deps:** **RUNS
  IMMEDIATELY AFTER K.W0 LANDS LOCALLY** (`K.md §WAVE MAP`, `PATH-FORWARD.md §5`): the re-pin MUST
  precede the design waves so W2 (fonts), W3 (layout), and W4 (panes) build ON 3.13.0 — the fluid
  typography tokens, the `startCollapsed` lever, the `motion-curves`/`SPRING_PRESETS` registry, the
  `DockRail`/`GlassUnderline` primitives, and the SegmentedTabs liquid indicator are all surfaces
  those waves consume; a re-pin AFTER them would force a rebuild against a moved foundation. K.W1
  consumes K.W0's `proof:cold-entry` + de-vacuoused B1 as its OWN green-baseline (the re-pin must
  not regress the just-cured cold path). The longest serial path runs THROUGH this wave: W0 → **W1**
  → (W2 ∥ W3) → W4 → WZ.

## §Provenance (the folded root causes + the consumed audit lanes)

- `audit/live-glassui-currency.md` — THE decisive input, wave-ready: the version ladder (§1 —
  3.11.2→3.12.0 internal/hygiene, 3.12.0→3.13.0 the breaking tranche); the breaking seams each
  proven DISJOINT from kf's consume surface (§2 — `variant-rail`/`instrument-strip` removed but kf
  passes no `variant=`; `instrument-rail.css` dropped but kf has zero hits; handmark/deck-progress/
  dialog-native exports gone, zero hits; PAPER_WASH_GROUND gone, zero hits; `autoLuminance`
  default-ON but no crash, ≤4Hz IO-gated); the new features mapped to user findings (§3 —
  `useDockClickIntegrity` §3.1, fluid typography §3.2, slider `touch-hit-area` §3.3, SegmentedTabs
  liquid indicator + `segmented-tabs.css` §3.4, `GlassUnderline` §3.5, `motion-curves`/
  `SPRING_PRESETS` §3.6, `DockRail` §3.7, `useGlassBackdropLuminance` §3.8, Constellation §3.9); the
  CSS-architecture changes vs kf's gates (§4 — the tokens carve is cascade-isomorphic §4.1, the
  adaptive `color-mix` is a no-op at 0% strength §4.3, the dock `padding-block` now PINNED §4.4); the
  re-pin mechanics (§5 — the floor advance §5.1, all 18 subpath exports resolve §5.2, the peer range
  unchanged §5.3, the press-scale hazard path §5.4); the findings NOT cured by re-pin alone (§6 —
  U-K1/K2/K3/K6/K15/K20); the risk-surface summary (§7) and the §FOLD table.
- `audit/glassui-handoff-k.md` — the consume-vs-handoff split + the J-ledger discharge state: the
  observed version timeline (§1); 3.12.0's single landed item (§2 — the PRM `data-allow-motion`
  carve); 3.13.0's J-ledger discharges (§3 — **RF-17 RESOLVED** via `useDockClickIntegrity`
  §3·RF-17; the slider standard-variant redesign §3·slider; FourierField `intensity` §3·FourierField;
  **RF-7 RESOLVED** via `startCollapsed` §3·DockRail; the fluid clamp scale §3·typography; the
  `LabeledField hideLabel` §3; the ExpandableContainer BREAKING change with ZERO kf impact §3; the
  4 removed components with ZERO kf impact §3); the full J-ledger state after 3.12/3.13 (§4 — 25
  REFINE rows, 21 ABSTRACT rows, the ADOPT residue); the K-audit new handoff candidates K-HO-1..8
  (§5); the U-K14 consume-vs-handoff split + the pin-strategy recommendation (§6); the RF-16/RF-17
  hazard-status table (§7); the §8 §FOLD.
- `audit/wave-J.W7bc.md §1.2` — the W7b set-(i) plan-vs-delivery: 3 consumed, **4 parity-clause
  exits** (S1a/ADOPT-7 SegmentedTabs spring pill; S1c/ADOPT-4 ScrubberTimeline base; S1e/ADOPT-1+2
  MetricBadge/AnimatedDigit; S1f StatusDot/Badge→BOOK-5) — the exact rows K.W1 re-examines on 3.13.0;
  §1.3 the pin-state discrepancy (the W7b worktree ran on 3.9.0, the merged tree on 3.11.2 — the
  documentation slip K.W1 must not repeat: the consume-edge claims are gated on the SAME pin they
  are claimed against).
- `audit/ci-cd-k.md §F-1` — release.yml has NO `timeout-minutes` (GHA default 360m = 6 hours); a
  stuck `npm publish` blocks silently (`grep -n "timeout-minutes" .github/workflows/release.yml` →
  no output; verified). The CI hardening whose IMPLEMENTATION is K.W5-owned (`K.md §clusters` line
  135 names "release.yml `timeout-minutes` (F-1, `ci-cd-k.md`)" in the **K.W5** gate-truth row, NOT
  the K.W1 row); K.W1 RECORDS the status per its "press-scale/RF-16 hazards status" charge (line 131)
  and hands the edit to K.W5.
- `audit/deferred-ledger-k.md` DL-K6 (the re-pin is the EXIT mechanism for the 46-row AX/RF ledger),
  DL-K17 (the VJ-F2/LD-DIAG diagnostics-sink fold) + `audit/k-seed-reconciliation.md §R5` (the
  K3-internal companion ~20-LoC OR confirm-already-landed).
- `audit/precepts-k.md` P13 (inv-16 — "consume published siblings, don't fork"; HELD: glass-ui
  `~3.11.2` lock resolves `3.11.2` from `registry.npmjs.org`, zero `file:` siblings).

## §The state, verified (file:line / on-disk tarball anchors / command+output)

- **The pin (the born-RED root):** `package.json:182` (section `optionalDependencies`):
  `"@mkbabb/glass-ui": "~3.11.2"`. Installed on-disk:
  `node -e "console.log(require('./node_modules/@mkbabb/glass-ui/package.json').version)"` → **`3.11.2`**
  (verified this session). The tilde `~3.11.2` resolves `>=3.11.2 <3.12.0`; 3.12.0 and 3.13.0 are
  blocked (`live-glassui-currency.md §0`, `glassui-handoff-k.md §1`).
- **The gate floor (the second born-RED root):** `scripts/proof-deps-current.mjs:80` hard-codes
  `"@mkbabb/glass-ui": "3.11.2"` (verified: the comment at `:71` reads "J.W7b: ADVANCED to 3.11.2 —
  the currency re-pin (~3.9.0 → ~3.11.2)"; `:79` reads "dropping below 3.11.2 re-introduces the
  broken slider + the small-option font"). The floor is a FLOOR, not a ceiling — but the K.W1 hard
  gate asserts the floor is ADVANCED to `3.13.0` so the gate AFFIRMS the new currency (a stale floor
  passing `3.13.0` is not a witness that the re-pin happened).
- **The breaking seams are DISJOINT from kf's consume surface (verified by tarball diff +
  `grep` over the kf tree, `live-glassui-currency.md §2`, `glassui-handoff-k.md §3`):**
  | 3.13.0 breaking change | kf consume exposure | Verdict |
  |---|---|---|
  | `variant="rail"`/`"instrument-strip"` removed; DockProps unified | `grep -rn 'variant="rail"' demo/` → 0; kf `<GlassDock>` passes no `variant=` | **NO IMPACT** |
  | `instrument-rail.css` dropped from `index.css` | `grep -rn "instrument-rail\|InstrumentRail" demo/` → 0 | **NO IMPACT** |
  | handmark/deck-progress/dialog-native exports removed | `grep -rn "HandMark\|DeckProgress\|GlassDialogNative" demo/` → 0 | **NO IMPACT** |
  | `PAPER_WASH_GROUND` removed from aurora API | `grep -rn "PAPER_WASH_GROUND" .` → 0 | **NO IMPACT** |
  | `ExpandableContainer` API gutted (`viewTransitionName`/`anchorEl`/`surfaceEl`/`expand`/`collapse` removed) | `grep -rn "ExpandableContainer" demo/` → 0 | **NO IMPACT** (`glassui-handoff-k.md §3`) |
- **All 18 subpath imports resolve in 3.13.0** (`live-glassui-currency.md §5.2` — Python cross-check
  of `package.json#exports` vs `grep` of `from "@mkbabb/glass-ui...`): `animated-digit`, `color`,
  `controls`, `dark`, `dock`, `forms`, `fourier-field`, `glass-panel`, `header-ribbon`,
  `icon-tooltip`, `keyboard`, `labeled-field`, `metric-badge`, `motion-core`, `status-dot`, `tabs`,
  `toggle-chip` — all present.
- **The peer range is unchanged** (`live-glassui-currency.md §5.3`): 3.13.0 `peerDependencies`
  declare `@mkbabb/keyframes.js: "^2.2.0 || ^3.0.0 || ^4.0.0"` — kf at `4.2.0` satisfies `^4.0.0`. No
  peer-cycle issue. (The 3.13.0 `devDependencies` bump of its OWN kf ref to `^4.1.0` is glass-ui's
  internal dev dep, not kf's peer requirement.)
- **The RF-17 upstream cure ships + the kf twin is live:** `useDockClickIntegrity.d.ts` ships in
  3.13.0 (`dist/components/custom/dock/composables/`); `grep -rn "clickIntegrity" /tmp/gu-313/…/dist/`
  → `dock.js` (the composable is wired) — `glassui-handoff-k.md §3·RF-17`. The kf workaround is
  CONFIRMED live in the tree: `grep -rn "pointerHandled\|onPlayPointerDown\|onCollapsedPlayClick"
  demo/` → `TransportDock.vue:129,174,175,306,312,322,325,330,337,341` (verified this session). The
  `useDockClickIntegrity` identity-guard is the upstream implementation of exactly RF-17's intent
  (capture press-time element identity, swallow the trailing click if identity changed during the
  collapse crossfade) — the kf twin becomes redundant.
- **`autoLuminance` default-ON** (`live-glassui-currency.md §2.5`): `useDockShellProps.d.ts:130-138`
  in 3.13.0 — `autoLuminance?: boolean`, ON unless explicitly `false`; wires `useGlassBackdropLuminance`
  at ≤4Hz (IntersectionObserver+PRM gated). kf's two docks (`TransportDock.vue`, `ChromeDock.vue`)
  pass no `:auto-luminance` → both would run the observer after re-pin. No crash risk; a subtle dock
  tint over the FourierField light canvas is possible (the `color-mix` is a no-op at 0% strength,
  §4.3).
- **The dock `padding-block` is now PINNED** (`live-glassui-currency.md §4.4`): 3.13.0
  `dock/morph.css:134-138` makes block padding INVARIANT across collapse/expand (the AY.W-DOCK-CHROME
  §1 layout-isolation fix) — the dock height no longer shrinks ~4px on collapse. This may make the
  J.W7c `--menubar-measured-h`/`--dock-top-band-reserve-stable` jitter workaround REDUNDANT — verify,
  do not assume.
- **The CSS tokens carve is cascade-isomorphic** (`live-glassui-currency.md §4.1`): `tokens.css`
  2372-line monolith (3.11.2) → 34-line `@import` root over 9 partials (3.13.0) — same compiled CSS,
  same `:root` context, same cascade order. kf's source-shape gates (`proof:no-brittle-selector`,
  `proof:styling-idioms`) inspect the kf source, not glass-ui's dist; the runtime gates
  (`proof:glass-and-cartoon`) measure the live rendered tree — the file-split is invisible to both.
- **The fluid typography tokens** (`live-glassui-currency.md §3.2`): the 3.13.0 `typography.css`
  replaces fixed-rem `--type-caption/small/body/prose` with `clamp()` whose FLOOR is byte-identical
  to the 3.11.2 fixed value (no narrow/mobile reflow; wide displays grow ~1-2 steps). The display/
  heading/title rungs stay fixed-rem. This is a K.W2 (fonts) input, NOT a K.W1 deliverable — K.W1's
  job is to verify the four-token `:root` override (`style.css:100-117`, the RF-2 workaround that
  feeds `--font-stack-text` into the clamp rungs) still applies cleanly; the font REDESIGN is K.W2.
- **release.yml has no timeout** (`ci-cd-k.md §F-1`): `grep -n "timeout-minutes"
  .github/workflows/release.yml` → no output (verified) — GHA default 360m. (`ci.yml:51` gates=10m;
  `ci.yml:213` demo-smoke=35m; `deploy-pages.yml:48` deploy=20m — release.yml is the lone job with no
  ceiling.)
- **The K3-internal structured codes LANDED in J.W1** (verified this session):
  `src/animation/internal/errors.ts:35` — `export type AnimationOptionErrorCode = "EMPTY_PARSE" |
  "UNKNOWN_TIMING_FN"`. The comment at `:27` reads "NOT a diagnostics channel (the full
  `Diagnostic[]` surface stays a K.W0 item)"; `adapter.ts` carries NO `diagnostics` field on
  `ResolvedKeyframes`. So the ~20-LoC companion the reconciliation lane (`k-seed-reconciliation.md
  §R5`) names is NOT a K.W1 implementation item — the structured codes are present; the FULL channel
  is the **K.W7 fidelity-floor** `ResolvedKeyframes.diagnostics` field (Band II), consuming the
  value.js 0.12.0 `ParseDiagnostic`/`OnParseError` producer that SHIPPED (DL-K17 exits via the K.W7
  channel + the K.W6 published-consume-edge — NOT a deferral to an L that will not come;
  `K.W7.md §Folds`, `PROGRESS.md` DL-K17 row).
- **visual-lock is self-captured-baseline, NON-AUTHORITATIVE for appearance correctness**
  (`scripts/proof-visual-lock.mjs:11-18,69-71`): the baseline is `--update-baseline` golden-captured
  from whatever the tree renders; "GREEN means the appearance matches the captured baseline" — never
  "the appearance is CORRECT". The H.W8 precedent (`:69-71`) RE-CAPTURED the baseline at the glass-ui
  `~3.5.1` re-pin (`docs/tranches/H/audit/harden/impl-w8-repin.md`). K.W1 follows that precedent: the
  baseline is RE-CAPTURED on 3.13.0, the re-capture is the disposition (NOT a diff that reds — the
  appearance legitimately changes under the new currency).

## §Goal

Make the consume edge tell the truth: **kf consumes the CURRENT published glass-ui (3.13.0), every
W7b consume-edge claim is RE-VERIFIED on the new pin (not on a stale pin a documentation slip
claimed against), the 4 parity-clause exits are RE-EXAMINED for whether 3.13.0 dissolves their
non-isomorphism, the RF-17 upstream cure RETIRES the kf hand-rolled twin (net-deletion), and the
visual-lock baseline is dispositioned per the re-pin protocol** — and the re-pin is the EXIT
mechanism that unblocks the design waves (W2/W3/W4 build on 3.13.0). Six moves, each at the
consume-edge altitude (NO glass-ui patch — inv-16; NO twin kept beside its published replacement —
net-deletion; NO consume-edge claim gated against a pin it was not measured on — the W7b §1.3 slip
not repeated):

1. **The pin advance (S1):** `package.json:182` `~3.11.2 → ~3.13.0` + `npm install` → lockfile
   resolves 3.13.0 from `registry.npmjs.org`. (A `^3.13.0` caret is also semver-valid —
   `glassui-handoff-k.md §6` recommends the caret since the specular-regression era that made the
   tilde load-bearing is long past; the IMPL chooses tilde-vs-caret confirm-first, the tilde being
   the conservative default that the gate floor mirrors.)
2. **The floor advance (S2):** `scripts/proof-deps-current.mjs:80` `3.11.2 → 3.13.0` — ATOMIC with
   S1 (a re-pin without the floor advance leaves the gate certifying a currency it no longer
   enforces; a floor advance without the re-pin reds immediately).
3. **The consume-edge re-verification battery (S3):** every glass-ui-touching gate re-run GREEN on
   3.13.0 (the §Hard-gate battery) + the W7b consume-edge claims (the 3 consumed S1 edges) confirmed
   intact + the 4 parity-clause exits RE-EXAMINED.
4. **The RF-17 twin net-deletion (S4):** `TransportDock.vue:306-341` — the `onPlayPointerDown`/
   `pointerHandled`/`onCollapsedPlayClick` workaround DELETED, the play controls reverted to a single
   `@click="actuatePlay"`; `useDockClickIntegrity` carries it upstream. Gated by `proof:dock-popover-opens`
   + `proof:live-session` S5 staying GREEN after the deletion.
5. **The `autoLuminance` disposition (S5):** the two kf docks' `autoLuminance` behavior over the
   FourierField light canvas is OBSERVED on the built dist; the disposition (accept the subtle tint,
   pass `:background-canvas`, or `:auto-luminance="false"`) is recorded with its evidence. (The
   FourierField hero REMOVAL is U-K20 = K.W4 scope, not K.W1 — this leg only dispositions the
   luminance interaction on whatever the hero renders at K.W1 time.)
6. **The visual-lock re-pin protocol (S6):** the baseline is RE-CAPTURED on 3.13.0 (the H.W8
   precedent) — the appearance legitimately changes under the new currency; the re-capture is the
   disposition, with a before/after note for the K.WZ TASTE packet input.

(The press-scale/RF-16/F-1 hazard STATUS is RECORDED — per the K.W1 cluster's "press-scale/RF-16
hazards status" charge — but each is a HAND-OFF, not a K.W1 edit: RF-16's durable cure is a BOOKED
P3 AX ask, the press-scale click-miss is subsumed by S4's RF-17 upstream cure, and the F-1
release.yml `timeout-minutes` IMPLEMENTATION is K.W5-owned per `K.md §clusters` line 135 — K.W1
records the status and hands the YAML edit to K.W5. See §Folds.)

## §Scope

- **S1 — the pin advance (`package.json:182`; inv-16 consume-only).** `"@mkbabb/glass-ui":
  "~3.11.2"` → `"~3.13.0"` + `npm install`; the lockfile resolves `3.13.0` from
  `registry.npmjs.org` (NO `file:`/`link:`/`git:` spec — `proof:deps-current` clause forbids it,
  `:25-29`). The pin stays in `optionalDependencies` (glass-ui is the demo's, not the library's,
  dependency — the `gates` CI job is glass-ui-free). **Tilde-vs-caret is a confirm-first IMPL
  choice:** `~3.13.0` (the conservative default, mirroring the gate floor) resolves `3.13.x`;
  `^3.13.0` resolves `<4.0.0` and is the `glassui-handoff-k.md §6` recommendation (the specular-
  regression era that made the H tilde load-bearing — `MEMORY.md §constellation re-pin`, glass-ui
  3.6/3.7 regressed specular, ship point 3.8.0 — is well past at 3.13.0). The §Hard gate asserts the
  CURRENCY (resolves 3.13.0), not the range syntax. **WHY (the EXIT mechanism):** the re-pin
  discharges the whole 46-row AX/RF handoff ledger's consume side AND unblocks the design waves
  (`deferred-ledger-k.md` DL-K6) — deferring it a fourth time rides every booked AX/RF row again.

- **S2 — the `proof:deps-current` floor advance (`scripts/proof-deps-current.mjs:80`; ATOMIC with
  S1).** `"@mkbabb/glass-ui": "3.11.2"` → `"3.13.0"`. The floor is the gate-of-the-gate: it asserts
  the consumed currency is at-or-above the K-certified baseline. Advancing it in the SAME wave that
  advances the pin is the §No-workaround discipline — a stale floor that passes `3.13.0` is not a
  witness that the re-pin happened (it would silently pass on a tree that NEVER re-pinned). The
  floor-comment at `:71,79` (which still narrates the J.W7b `~3.9.0 → ~3.11.2` advance) is updated to
  narrate the K.W1 `3.11.2 → 3.13.0` advance + the slider/click-integrity rationale. **WHY:** the
  floor is the durable record of "kf consumes the current glass-ui" — it must move WITH the pin or
  the gate certifies a fiction.

- **S3 — the consume-edge re-verification battery + the W7b exits re-examined (the §Hard gate's
  body; inv-16 — kf re-VERIFIES, never patches glass-ui).** Two halves:

  **(a) The gate battery re-runs GREEN on 3.13.0.** Every glass-ui-touching gate (the runtime
  computed-style gates that read the live rendered tree + the source-shape gates that the carve does
  not touch) re-runs on the re-pinned tree:
  | Gate | What it reads | 3.13.0 risk (`live-glassui-currency.md §4/§7`) |
  |---|---|---|
  | `proof:glass-and-cartoon` | computed background-color α on glass/cartoon surfaces | LOW — the adaptive `color-mix(…, 0%)` is a no-op at rest; the α ceiling is permissive |
  | `proof:dock-zorder` | `.glass-dock` z-order + hit-test + keep-open mutex | LOW — still keys on `.glass-dock` (unchanged), not `variant-dock` |
  | `proof:dock-popover-opens` | the @mbabb dock popover OPENS (BLK-8) | LOW — gates S4's twin deletion (must stay GREEN AFTER the workaround is removed) |
  | `proof:specular-absent-at-rest` | `--specular-intensity` at rest | LOW — defaults unchanged; `glass-specular-track` still emitted |
  | `proof:mobile-single-page` | the mobile stack→overlay; the stage-band-reserve | BENEFICIAL — the pinned `padding-block` (§4.4) may REMOVE the `--menubar-measured-h` jitter; verify |
  | `proof:styling-idioms` / `proof:no-brittle-selector` | the kf SOURCE shape | NONE — inspect kf source, not glass-ui dist; the tokens carve is invisible |
  | `proof:demo-fonts` | the demo's font identity vs glass-ui's | LOW — the four-token `:root` override (`style.css:100-117`) feeds the new clamp rungs; verify it applies cleanly |
  | `proof:deps-current` | the pin floor | the S2 floor (the AFFIRMATIVE currency assert) |
  And the full `proof:all` roster runs GREEN end-to-end (the re-pin regresses NOTHING — including
  K.W0's just-landed `proof:cold-entry` + de-vacuoused B1).

  **(b) The W7b consume-edge claims re-examined on 3.13.0.** The 3 CONSUMED edges
  (`wave-J.W7bc.md §1.2`) stay intact (their twin-deletions hold: `grep -rn "preset-active" demo/`
  = 0, `grep -rn "kf-editor" demo/` = 0, `grep -n "@keyframes gold-shimmer-slide" design-idioms.css`
  = 0). The **4 parity-clause exits RE-EXAMINED** — for each, the question is whether 3.13.0
  dissolves the non-isomorphism that justified the exit on 3.9.0:
  | Exit | The 3.9.0 exit reason | The 3.13.0 re-examination | Disposition |
  |---|---|---|---|
  | **S1a / ADOPT-7** (SegmentedTabs spring pill) | appearance-bearing (animated indicator vs instant color); twin survives `SpringScene.vue:8-26,211-236` | 3.13.0 ships `segmented-tabs.css` (un-scoped classes) + the liquid `--stretch` indicator (`DEFAULT_INDICATOR_MAX_STRETCH=1.08`, `live-glassui-currency.md §3.4`) — the animation is RICHER, but the swap remains appearance-bearing | **EXIT STANDS at K.W1**; the consume is K.W4's pane redesign (U-K12 tabs→pills), where the appearance change is the wave's WORK, not a parity violation |
  | **S1c / ADOPT-4** (ScrubberTimeline base) | structurally non-isomorphic (PlaybackRibbon is ms-domain Slider; ScrubberTimeline is 0–1 normalized w/ own chrome) | 3.13.0 does not change ScrubberTimeline's normalization domain — still non-isomorphic with the ms-domain ribbon | **EXIT STANDS**; not a K.W1 consume |
  | **S1e / ADOPT-1+2** (MetricBadge/AnimatedDigit) | the swap IS the W7a appearance delta (moved to W7a lane) | 3.13.0 does not change this — it was always an appearance delta, not a parity swap | **EXIT STANDS** (correctly re-homed to the appearance lane) |
  | **S1f** (StatusDot/Badge → BOOK-5) | tone/auto-contrast NOT in 3.9.0 published palette | `status-dot.d.ts` unchanged in 3.13.0 (`glassui-handoff-k.md §4` RF-6 STILL MISSING) — the tone slot still absent | **EXIT STANDS / BOOK-5 reaffirmed**; rides a future AX publish |
  **The re-examination outcome is recorded, not assumed:** all four exits are EXPECTED to stand on
  the §State-verified evidence, but the IMPL RE-CHECKS each against the on-disk 3.13.0 tarball before
  closing the row (P-invariant-28 — a parity exit re-examined is a real disposition, not a carried
  assumption). **WHY:** the W7b §1.3 slip (a consume-edge claim gated on a different pin than it was
  claimed against) is the failure mode this leg exists to not repeat — every K.W1 consume claim is
  measured against the pin K.W1 actually lands.

- **S4 — the RF-17 twin net-deletion (`TransportDock.vue:306-341`; net-deletion rule).** The
  J.W7c-born pointerdown workaround for the dock collapse-crossfade click-strand (RF-17) — the
  `onPlayPointerDown` handler (`:322-330`), the `pointerHandled` guard (`:312,337`), the
  `onCollapsedPlayClick` (`:341`), and the `@pointerdown`/`@click.stop` template bindings
  (`:129,174,175`) — is DELETED; the play controls revert to a single `@click="actuatePlay"`.
  `useDockClickIntegrity` (shipped in 3.13.0, wired in `dock.js`) carries the identity-guard
  upstream. **Born-RED-of-the-deletion (the regression guard):** the deletion lands ONLY if
  `proof:dock-popover-opens` (BLK-8) AND `proof:live-session` S5 (the motion-path PLAY/INTERACT leg)
  stay GREEN AFTER it — i.e. the upstream guard genuinely subsumes the kf twin (a deletion that reds
  either gate is reverted, the twin kept, and the residual booked). **WHY (net-deletion):** keeping a
  hand-rolled workaround beside its published replacement is the legacy-beside-replacement the mandate
  forbids; the two guards are non-exclusive (`live-glassui-currency.md §5.4` — the upstream guard on
  the dock ROOT, the kf guard on the TRIGGER) so the deletion is safe, but it MUST be VERIFIED, not
  assumed.

- **S5 — the `autoLuminance` disposition (the two kf docks; OBSERVE + decide).** After re-pin, both
  kf docks (`TransportDock.vue`, `ChromeDock.vue`) run `useGlassBackdropLuminance` at ≤4Hz by default
  (`live-glassui-currency.md §2.5`). The IMPL OBSERVES the dock appearance over whatever the hero
  renders at K.W1 time (the FourierField canvas is a LIGHT backdrop → the observer may set
  `--glass-backdrop: dark` → a subtle dock tint). The disposition is one of: (i) ACCEPT the tint (the
  adaptive-glass effect is a visual improvement — record it as a TASTE-packet input for K.WZ); (ii)
  pass `:background-canvas` so the observer samples the canvas accurately; (iii)
  `:auto-luminance="false"` to opt out. The choice is recorded with its observed evidence (a
  before/after dock screenshot over the hero). **NO crash risk** (§7) — this is an APPEARANCE
  disposition, not a correctness fix. **Boundary:** the FourierField hero REMOVAL is U-K20 / K.W4
  (`live-fourier-grid.md`) — S5 dispositions only the luminance INTERACTION on the K.W1-time hero,
  never the hero's content.

- **S6 — the visual-lock re-pin protocol (`proof:visual-lock`; the H.W8 precedent).** visual-lock is
  self-captured-baseline and NON-AUTHORITATIVE for appearance correctness
  (`proof-visual-lock.mjs:11-18`); its GREEN means "the appearance matches the committed baseline",
  never "the appearance is correct". A re-pin legitimately CHANGES the appearance (fluid typography,
  the dock padding-block pin, the liquid SegmentedTabs indicator) → a naive diff would red on a
  CORRECT change. Per the H.W8 precedent (`proof-visual-lock.mjs:69-71`,
  `docs/tranches/H/audit/harden/impl-w8-repin.md` — the baseline was RE-CAPTURED at the glass-ui
  `~3.5.1` re-pin), K.W1 RE-CAPTURES the baseline on the 3.13.0 tree via `--update-baseline`. The
  re-capture is the disposition; the before/after PNGs are recorded as a K.WZ TASTE-packet input
  (the appearance delta is named, not silently absorbed). **WHY:** visual-lock is a SAME-TREE
  regression catch (does the appearance match what was last captured), not an appearance ORACLE — the
  re-pin is exactly the moment its baseline must be refreshed, and pixel-exactness across an
  intentional currency change is a category error.

- **The press-scale / RF-16 / F-1 hazard STATUS (RECORD + HAND-OFF; NO K.W1 edit).** The K.W1
  cluster (`K.md §clusters` line 131) charges K.W1 with the "press-scale/RF-16 hazards status" — a
  STATUS recording, not a fix. Each is dispositioned and handed to its owner:
  - **press-scale (the `--scale-press-dock .96` reflow click-miss):** SUBSUMED by S4's RF-17 upstream
    cure (`useDockClickIntegrity` guards the press-scale/reflow click-miss internally,
    `live-glassui-currency.md §5.4`) — no separate K.W1 edit; the disposition rides S4.
  - **RF-16 (the PRM ResizeObserver→render TDZ crash):** NO upstream fix in 3.12.0/3.13.0
    (`glassui-handoff-k.md §7`); the consumer `:freeze` guard at `EditorStartScreen.vue:84` STAYS
    (removing it re-introduces the crash). The durable cure is a BOOKED P3 AX ask — K.W1 RECORDS the
    non-discharge, it does NOT edit. (See §Folds.)
  - **F-1 (release.yml has no `timeout-minutes`):** the IMPLEMENTATION (`timeout-minutes: 15` on the
    `publish` job) is **K.W5-owned** per `K.md §clusters` line 135 (the gate-truth wave's "release.yml
    `timeout-minutes` (F-1, `ci-cd-k.md`)" charge). K.W1 RECORDS the status (`grep -n "timeout-minutes"
    .github/workflows/release.yml` → no output, verified; GHA default 360m; `ci-cd-k.md §F-1`) and
    HANDS the YAML edit to K.W5 — it does not write it. **WHY the charter splits this:** the F-1
    timeout is a CI-gate hardening, file-adjacent to K.W5's other release.yml/CI legs (the F-2
    wall-clock, the `proof:ci-wallclock` gate); the charter keeps all release.yml/CI hardening in the
    ONE gate-truth wave rather than scattering a lone YAML line into the consume-edge wave. (PATH-FORWARD
    §5 lists F-1 in BOTH the K.W1 and K.W5 rows — an executive-summary inconsistency; the BINDING
    charter `K.md §clusters` is authoritative and places the F-1 IMPLEMENTATION in K.W5.)

## §Hard gate (the proof:* that BITES — born-RED on the pre-re-pin tree, GREEN-on-re-pin · the consume-edge battery)

**The consume-edge oracle:** the consume edge is GREEN iff kf consumes the CURRENT published
glass-ui (3.13.0), the floor AFFIRMS it, the full gate battery passes on the new pin, the RF-17 twin
is gone with its regression guards green, and the visual-lock baseline is dispositioned per the
re-pin protocol. Composed of the clauses below; clauses (a)-(c) are the CORRECTNESS body, (d)-(e)
are HYGIENE/observe-only corroborators (two-tier taxonomy).

- **clause (a) — `proof:deps-current` AFFIRMS the 3.13.0 currency (CORRECTNESS).** The floor at
  `scripts/proof-deps-current.mjs:80` reads `"3.13.0"` AND the resolved on-disk glass-ui is `3.13.0`
  (lockfile resolves `registry.npmjs.org` 3.13.0, NO `file:`/`link:`/`git:` spec). **BORN-RED
  WITNESS:** on the pre-re-pin tree the pin is `~3.11.2` and the on-disk version is `3.11.2`
  (verified this session); if the floor is advanced to `3.13.0` WITHOUT the pin advance, the gate
  reds (the resolved `3.11.2` is below the floor) — and if the pin is advanced WITHOUT the floor, the
  gate passes vacuously (a stale floor does not witness the re-pin), which is why S2 ADVANCES the
  floor as the affirmative assert. **BITE:** reds on the stale pin against the advanced floor; greens
  only when BOTH the pin and the floor read 3.13.0. **NO workaround:** the floor moves WITH the pin
  (atomic), never a stale floor passing the new version.

- **clause (b) — the full `proof:all` battery is GREEN on the 3.13.0 pin, with the
  glass-ui-touching gates explicitly re-verified (CORRECTNESS).** `npm run proof:all` runs GREEN
  end-to-end on the re-pinned tree; the §Scope S3(a) table's gates (`proof:glass-and-cartoon`,
  `proof:dock-zorder`, `proof:dock-popover-opens`, `proof:specular-absent-at-rest`,
  `proof:mobile-single-page`, `proof:styling-idioms`, `proof:no-brittle-selector`,
  `proof:demo-fonts`) are each confirmed GREEN, AND K.W0's `proof:cold-entry` + de-vacuoused B1 stay
  GREEN (the re-pin regresses nothing the lead wave cured). **BORN-RED WITNESS:** the re-pin is an
  unverified change — the battery has NEVER run on the 3.13.0 tree (the pin has been tilde-stuck at
  `~3.11.2` since J.W7b); the clause exists to red on any gate the new currency breaks (e.g. if the
  adaptive `color-mix` exceeded the α ceiling, or the dock z-order keyed on the removed `variant-dock`
  class — both verified NOT the case in §State-verified, so the clause is EXPECTED green, but it is
  RUN, not assumed). **BITE:** reds if any gate regresses on 3.13.0. **NO escape:** a regressed gate
  is fixed at the kf consume seam or the re-pin is reverted — NEVER silenced with `IN_CI`/
  `continue-on-error`, NEVER papered by patching glass-ui (inv-16).

- **clause (c) — the RF-17 twin is DELETED and its regression guards stay GREEN (CORRECTNESS;
  net-deletion).** After S4 deletes `TransportDock.vue:306-341` (the pointerdown workaround),
  `grep -rn "pointerHandled\|onPlayPointerDown\|onCollapsedPlayClick" demo/` → 0, AND
  `proof:dock-popover-opens` (BLK-8) + `proof:live-session` S5 (the motion-path PLAY/INTERACT leg)
  are GREEN. **BORN-RED WITNESS:** the twin is CONFIRMED live today
  (`TransportDock.vue:129,174,175,306,312,322,325,330,337,341` — verified this session); the clause
  reds if (i) the twin is NOT deleted (the legacy-beside-replacement the net-deletion rule forbids)
  OR (ii) the deletion reds the dock-click gates (the upstream guard does NOT subsume the kf twin
  after all — the deletion is reverted, the twin kept, the residual booked). **BITE:** reds on the
  un-deleted twin OR on a deletion that breaks the dock click integrity. **NO workaround:** the
  deletion is VERIFIED green, never assumed safe; a residual that genuinely needs the kf guard is
  BOOKED with its born-RED witness, not silently retained.

- **clause (d) — the visual-lock baseline is RE-CAPTURED on 3.13.0 (HYGIENE — labeled,
  re-pin protocol).** `proof:visual-lock` runs GREEN on the re-pinned tree against a baseline
  RE-CAPTURED on 3.13.0 (`--update-baseline`); the before/after PNGs are committed under
  `scripts/baselines/visual-lock/` with a re-pin note. *(Labeled HYGIENE: visual-lock is
  NON-AUTHORITATIVE for appearance correctness — `proof-visual-lock.mjs:11`; its GREEN means
  "matches the captured baseline", never "the appearance is correct". The re-capture is the
  disposition per the H.W8 precedent; the APPEARANCE verdict on the changed panes is the K.WZ TASTE
  boundary, not this gate.)* **BITE:** would red if the re-pin produced an appearance change AND the
  baseline were NOT re-captured (a category error this protocol exists to prevent).

- **clause (e) — the `autoLuminance` disposition + the press-scale/RF-16/F-1 hazard status are
  RECORDED (HYGIENE — labeled, observe-only/record).** The `autoLuminance` disposition (S5) is
  recorded with its observed before/after dock screenshot; the press-scale/RF-16/F-1 hazard status
  is recorded with each hazard's owner (press-scale→subsumed by S4; RF-16→BOOKED P3 AX; F-1→handed to
  K.W5). *(Labeled HYGIENE: the `autoLuminance` choice is an appearance disposition whose verdict is
  the K.WZ TASTE boundary; the hazard status is a RECORD + HAND-OFF, not a K.W1 implementation —
  K.W1's GREEN does NOT depend on the F-1 YAML edit (that is K.W5's gate), only on the status being
  recorded.)*

**The §spine bar — MUST bite.** Clauses (a)-(c) are the consume-edge correctness oracle: each
asserts an EXACT property (the floor AND the on-disk version read 3.13.0; the full battery is green
on the new pin; the RF-17 twin is gone with its dock-click gates green). Revert S1/S2 → (a) reds
(the stale pin against the advanced floor). Revert the re-pin under any gate → (b) reds (the battery
was never run green on 3.13.0). Revert S4 → (c) reds (the twin survives beside its published
replacement). **The born-RED witnesses are CONCRETE:** the on-disk `3.11.2` (verified) below an
advanced floor; the live `TransportDock.vue` workaround line numbers (verified). **Two-tier
taxonomy:** the wave's GREEN depends on the RUNTIME/correctness clauses (a)-(c); clauses (d)
(visual-lock re-capture) and (e) (the `autoLuminance` record + the hazard-status RECORD/HAND-OFF)
are HYGIENE/observe-only corroborators — they support the consume edge but may NEVER substitute for a
red correctness clause. **P6 posture (declared):** clauses (a)-(c) are device-INDEPENDENT (the pin
resolution, the source-shape gates, the DOM-membership dock-click gates) — they hard-gate; clause
(b)'s runtime computed-style gates that ride glass-ui (`proof:glass-and-cartoon`, `proof:mobile-single-page`)
inherit their existing per-gate P6 posture (the K.W5/J.W3 single `IN_CI` seam) — the re-pin does not
change a gate's tier, only its pin. clause (d) (visual-lock) is the device-DEPENDENT pixel gate
already HARD-locally / observe-only-in-CI by its own protocol (`proof-visual-lock.mjs:281-287`).

## §No-workaround prohibitions (BINDING — the mandate's named forbiddings for this wave)

- **NO glass-ui patch (inv-16).** kf consumes the PUBLISHED 3.13.0 — every finding cured by the
  re-pin is cured by CONSUMING the upstream surface, never by editing `node_modules/@mkbabb/glass-ui`
  or forking a glass-ui file into the demo (`precepts-k.md` P13). A glass-ui-owned gap that 3.13.0
  does NOT close (RF-1 cartoon-radius, RF-2 typography opt-out, RF-3/4 LabeledField orientation, the
  21 ABSTRACT rows — `glassui-handoff-k.md §4`) stays BOOKED in the handoff ledger, NOT patched in kf.
- **NO twin kept beside its published replacement (net-deletion).** The RF-17 pointerdown workaround
  (S4) is DELETED in the same wave that consumes `useDockClickIntegrity` — keeping the hand-rolled
  twin "just in case" is the legacy-beside-replacement the mandate forbids. The ONLY legitimate
  retention is a VERIFIED residual (the deletion reds a dock-click gate) — and that is BOOKED with
  its born-RED witness, never a silent keep.
- **NO consume-edge claim gated against a pin it was not measured on (the W7b §1.3 slip).** Every
  K.W1 consume-edge re-verification (S3) is run against the SAME pin K.W1 lands (3.13.0) — the W7b
  documentation error (the impl record claimed authority "3.9.0" while the merged tree gated on
  3.11.2, `wave-J.W7bc.md §1.3`) is the failure mode this prohibition closes. The §Hard gate clause
  (b) runs the battery ON the re-pinned tree, not on a memory of a prior pin.
- **NO floor lag (the gate certifying a fiction).** The `proof:deps-current` floor (S2) advances
  ATOMICALLY with the pin (S1) — a stale floor passing `3.13.0` does not witness the re-pin; a floor
  advanced without the pin reds. The floor is the durable currency record and moves WITH the pin.
- **NO pixel-exact visual-lock across the intentional currency change (the category error).** The
  re-pin LEGITIMATELY changes the appearance (fluid typography, the dock padding-block pin, the
  liquid indicator); demanding a pixel-exact match against the OLD baseline is a category error.
  visual-lock's baseline is RE-CAPTURED per the H.W8 re-pin protocol (S6); the appearance VERDICT on
  the changed panes is the K.WZ TASTE boundary, NOT a visual-lock diff.
- **NO scope creep into the design waves.** K.W1 RE-PINS and re-verifies the consume edge; it does
  NOT REDESIGN. The fluid-typography CONSUME is K.W2 (fonts); the `startCollapsed`/`DockRail`/anchor
  CONSUME is K.W3 (layout); the `motion-curves`/`SPRING_PRESETS` CONSUME + the SegmentedTabs
  pills/U-K12 + the slider-stepping cure is K.W4 (panes); the FourierField hero REMOVAL (U-K20) is
  K.W4. K.W1 lands the PIN those waves build on, verifies the edge is green, and stops. (S5
  dispositions only the `autoLuminance` INTERACTION on the K.W1-time hero, never the hero content.)
- **NO `IN_CI`/`continue-on-error` to pass the battery.** A gate that reds on 3.13.0 is fixed at the
  kf consume seam or the re-pin is reverted — never silenced. (The re-pin is EXPECTED green on the
  §State-verified disjointness evidence; if it surprises, the surprise is investigated, not papered.)

## §Folds (every K.md-assigned fold, with its evidence citation)

- **DL-K6 (U-K14) — the glass-ui re-pin `3.11.2 → 3.13.0` + the published-edge consume sweep** — S1
  (the pin) + S2 (the floor) + S3 (the battery + the W7b exits) + S4 (the RF-17 twin net-deletion).
  The re-pin is the EXIT mechanism for the whole 46-row AX/RF handoff ledger's consume side
  (`deferred-ledger-k.md` DL-K6, `live-glassui-currency.md §1-2`, `package.json:182`). The §Hard gate
  clause (a)/(b) is its born-RED witness.
- **U-K15 (the spring slider steps) — re-pin VERIFY only; the cure is K.W4.** The 3.13.0 slider
  standard-variant (invisible-thumb) MAY dissolve the visual stepping percept
  (`glassui-handoff-k.md §3·slider`, `live-glassui-currency.md §3.3`), but the desktop stepping is a
  `:step="0.01"` data issue (`SpringSidebar.vue:48,58` — 110 stops over [0.1,1.2]), and the ROOT cure
  (the few-Hz readout mirror must not drive the slider) is K.W4 (`PATH-FORWARD.md §6` K.W4). K.W1
  VERIFIES whether the re-pin alone moves the percept and records the observation; it does NOT own
  the stepping cure. `deferred-ledger-k.md` DL-K3, `prompt-recap-k.md §PR-K3`.
- **RF-17 (the dock collapse-crossfade click-strand) — RESOLVED upstream in 3.13.0; the kf twin
  net-deleted** — S4. `glassui-handoff-k.md §3·RF-17, §7`, `live-glassui-currency.md §3.1, §5.4`.
  The §Hard gate clause (c).
- **RF-16 (the PRM ResizeObserver→render TDZ crash) — NO upstream fix in 3.12.0 or 3.13.0; the kf
  consumer `:freeze` guard remains the active mitigation (P3).** Verified: no TDZ fix in the bundle
  diffs (`glassui-handoff-k.md §7`, `live-glassui-currency.md` does not list it among the §3 cures).
  The consumer guard at `EditorStartScreen.vue:84` stays; the durable upstream cure is a BOOKED AX
  ask (downgraded P1→P3 at J.W7c r2). K.W1 RECORDS that the re-pin did NOT discharge RF-16 — it is
  NOT a K.W1 deletion (removing the `:freeze` guard would re-introduce the crash). `precepts-k.md`
  (RF-16 booked, not patched).
- **The press-scale hazard (the `--scale-press-dock .96` reflow click-miss) — subsumed by RF-17's
  upstream cure.** The J.FINAL P0 lesson (`live-glassui-currency.md §5.4`): `useDockClickIntegrity`
  now guards the press-scale/reflow click-miss internally; the kf `App.vue` click workaround (if any
  survives) becomes redundant and is dispositioned with S4 (verify `proof:dock-popover-opens` green
  after any removal). RECORD: the two guards are non-exclusive (upstream on the dock root, kf on the
  trigger), so the deletion is safe but VERIFIED.
- **F-1 (release.yml has no `timeout-minutes`; a stuck `npm publish` blocks 6h) — STATUS RECORDED,
  IMPLEMENTATION HANDED to K.W5.** K.W1 RECORDS the hazard status (`grep -n "timeout-minutes"
  .github/workflows/release.yml` → no output, verified; GHA default 360m; `ci-cd-k.md §F-1`) per the
  K.W1 cluster's "press-scale/RF-16 hazards status" charge, but the YAML edit (`timeout-minutes: 15`
  on the `publish` job) is **K.W5-owned** per `K.md §clusters` line 135 (the gate-truth wave's
  "release.yml `timeout-minutes` (F-1)" leg). The charter keeps all release.yml/CI hardening (F-1
  with the F-2 wall-clock + the `proof:ci-wallclock` gate) in the ONE gate-truth wave. (PATH-FORWARD
  §5 lists F-1 in both K.W1 and K.W5 — an executive-summary inconsistency; `K.md §clusters` is
  BINDING and places the IMPLEMENTATION in K.W5.) F-2 demo-smoke wall-clock + `proof:ci-wallclock` →
  K.W5; F-4 release.yml thinner-gate-set → OUT, separate `ci-cd-k.md §F-4` row.
- **DL-K17 / the K3-internal diagnostics fold — the codes LANDED at J.W1; the FULL channel is K.W7
  (Band II).** The structured codes `EMPTY_PARSE`/`UNKNOWN_TIMING_FN` LANDED in J.W1 (`errors.ts:35`
  — verified this session); the FULL `Diagnostic[]` channel on `ResolvedKeyframes` (`adapter.ts` has
  no `diagnostics` field today) is authored at the **K.W7 fidelity floor** (Band II), where it CONSUMES
  the value.js 0.12.0 `ParseDiagnostic`/`OnParseError` producer that SHIPPED (N2 row 10). So the
  `~20-LoC companion` the reconciliation lane (`k-seed-reconciliation.md §R5`) named is NOT a K.W1
  item — the codes are present at J.W1, and the FULL channel exits as the K.W7 channel + the K.W6
  published-consume-edge (NOT a deferral to an L that will not come — `K.W7.md §S4/§Folds`,
  `PROGRESS.md` DL-K17 row). K.W1 RECORDS this disposition; it writes no engine code. (The K.W1
  frontier-adjacent fold is VERIFY-LANDED for the codes; the channel itself is a Band-II wave.)

## §Hand-off / cross-wave boundaries (BINDING)

- **← K.W0 (the lead, BINDING):** K.W1 RUNS ONLY AFTER K.W0 LANDS LOCALLY (`K.md §WAVE MAP`,
  `PATH-FORWARD.md §5`). K.W1 consumes K.W0's `proof:cold-entry` + de-vacuoused B1 as its OWN
  green-baseline — the §Hard gate clause (b) asserts the re-pin regresses NEITHER. The re-pin must
  NOT disturb the just-cured cold path (the adapter resume-made-total, the engine-write
  disambiguation). If K.W0's cold-path fix touched a glass-ui consume seam (the dock play control,
  the `TransportDock` actuate path), K.W1's S4 twin-deletion verifies it stays green AFTER the
  pointerdown workaround is removed.
- **→ K.W2 (fonts, BINDING):** K.W1 lands the 3.13.0 pin whose FLUID TYPOGRAPHY tokens
  (`typography.css` clamp rungs, `live-glassui-currency.md §3.2`) K.W2 consumes; K.W1 VERIFIES the
  four-token `:root` override (`style.css:100-117`) applies cleanly on the new clamp base (the
  `proof:demo-fonts` re-run), but the ONE-voice-authority REDESIGN (kill the `--font-serif`≡
  `--font-display` redundancy, the dock joining the display voice at the ROOT) is K.W2's work, NOT
  K.W1's. Disjoint loci: K.W1 at the pin; K.W2 at the voice-token authority.
- **→ K.W3 (layout, BINDING):** K.W1 lands the pin whose `startCollapsed` lever (RF-7 resolved,
  `glassui-handoff-k.md §3·DockRail`) + `DockRail` primitive K.W3 may consume for the dock
  default-detent + anchoring redesign (U-K1/U-K7); K.W1 does NOT thread `startCollapsed` through
  `App.vue`/`ChromeDock.vue` (that is K.W3's grid/anchoring work). The dock `padding-block` pin
  (§4.4) that K.W1 VERIFIES against `proof:mobile-single-page` is consumed by K.W3's layout tier.
- **→ K.W4 (panes, BINDING):** K.W1 lands the pin whose `motion-curves`/`SPRING_PRESETS` registry
  (the spring picker, U-K11/U-K16), `segmented-tabs.css`/liquid indicator (U-K12 tabs→pills),
  `GlassUnderline`, slider invisible-thumb (U-K15 percept), and Constellation (U-K20 hero
  replacement) K.W4 consumes — and the 4 W7b parity-clause exits that K.W1 RE-EXAMINES-and-stands
  (S1a SegmentedTabs especially) are the EDGES K.W4 then consumes as part of its pane REDESIGN (where
  the appearance change is the wave's work, not a parity violation). K.W1 verifies the edge is green;
  K.W4 spends it.
- **→ K.W5 (the gate-truth wave, BINDING):** the F-1 release.yml `timeout-minutes` IMPLEMENTATION,
  the F-2 demo-smoke wall-clock recalibration, AND the `proof:ci-wallclock` hygiene gate
  (`ci-cd-k.md §F-1/§F-2`) are ALL K.W5 (the charter's gate-truth wave keeps every release.yml/CI
  hardening in one wave, `K.md §clusters` line 135) — K.W1 RECORDS the F-1 hazard status and HANDS
  the YAML edit to K.W5, it does NOT write release.yml. The per-gate P6 posture / the single `IN_CI`
  seam that K.W1's battery (clause (b)) relies on is the K.W5/J.W3-owned helper — K.W1 consumes the
  posture, does not author the seam.
- **→ K.WZ (the close, BINDING):** the visual-lock before/after PNGs (S6) + the `autoLuminance`
  before/after dock screenshot (S5) are recorded as TASTE-packet INPUTS — the appearance VERDICT on
  the re-pin's changed panes is the K.WZ user-domain review-packet step (the TASTE boundary), NEVER a
  K.W1 agent verdict. The K.WZ version cut publishes THROUGH release.yml (whose F-1 `timeout-minutes`
  is hardened by K.W5, NOT K.W1).
- **OUT / sibling (do NOT touch):** the 21 ABSTRACT (AX-1..13 + folded) + the 18 remaining REFINE
  rows that 3.13.0 does NOT ship → BOOKED in `glassui-handoff-k.md §4`, ride future AX publishes,
  NEVER patched in kf (inv-16); the F-4 release.yml thinner-gate-set → its own `ci-cd-k.md §F-4` row
  (not consume-edge); the version cut + npm publish → K.WZ (USER-DOMAIN, confirm-first, Mike Babb);
  the value.js VJ.W3 full-diagnostics channel → K.W7 (Band II) (DL-K17); the tilde-vs-caret pin-range choice →
  confirm-first IMPL decision (the gate asserts currency, not syntax).
- **DL-K23 (`{types}` directional VT · RF-3/RF-4 LabeledField orientation · glass-ui typography
  opt-in) — HANDOFF, consumed on the published re-pin (K.W1 owning).** RF-3/RF-4 are the
  LabeledField orientation rows that MAY land in the published 3.13.0; the S3 consume-edge battery
  RE-RECONCILES them against the 3.13.0 surface (the ones it ships consume now; the ones it does not
  fall into the §4 BOOKED REFINE band above). The `{types}` directional view-transition folds ONLY
  IF K elects scene interactivity (it does not in K — RECORD only). Sibling-owned,
  published-consume-edge, NEVER a future version (`deferred-ledger-k.md` DL-K23, `glassui-handoff-k.md`
  RF-3/RF-4). This is the OWNING-wave home; K.W6's §1.8 tail band is the AUDIT roll-up, not a second
  disposition.
- **DL-K21 (VJ-4/MCI-5 identity-pad witness · FB-3 MorphSVG · VJ-F1 arc-length sampler) — HANDOFF,
  consume on the value.js publish (K.W1 owning, re-pin re-confirm).** Gated on value.js VJ.W4 (the
  `getPointAtLength`/`fromMorphSVG` competitor gap); the consume signal is `it.fails(` at
  `test/interpolate-anything.test.ts:256` flipping GREEN the instant value.js ships the primitive.
  K.W1's re-pin re-confirms the VJ targeting; it writes no engine code (the producer is value.js).
  Sibling-owned, gated on a published primitive (`deferred-ledger-k.md` DL-K21, `K-SEED.md §7 VJ.W4`).
  K.W6's §1.8 tail band is the AUDIT roll-up of the consume-edge form, not a second disposition.

## §Design decisions (trade-offs RESOLVED)

- **The re-pin RUNS AFTER W0, BEFORE the design waves — RESOLVED.** The audit lanes
  (`live-glassui-currency.md §FOLD`, `glassui-handoff-k.md §8`) tagged the re-pin "K.W0/K.W-REPIN"
  before the charter's final wave ordering; the BINDING charter (`K.md §clusters/§WAVE MAP`,
  `PATH-FORWARD.md §5`) assigns it **K.W1** — AFTER the P0 cold-path (W0 LEADS, the product decays
  daily), BEFORE the design waves (W2/W3/W4 must build ON 3.13.0). The audit's wave-class
  suggestions are superseded by the charter ordering; this spec follows the charter.
- **The pin advances to the CURRENT published (3.13.0), tilde-vs-caret is confirm-first — RESOLVED.**
  The §Hard gate asserts the CURRENCY (the floor + on-disk both read 3.13.0), not the range syntax.
  `~3.13.0` (the conservative default mirroring the gate floor) and `^3.13.0` (the
  `glassui-handoff-k.md §6` recommendation — the specular-regression era that made the H tilde
  load-bearing is long past at 3.13.0) are both semver-valid; the IMPL chooses confirm-first.
- **The 4 W7b parity-clause exits are RE-EXAMINED, not carried — RESOLVED.** Each exit's 3.9.0
  reason is re-checked against the on-disk 3.13.0 tarball (S3(b)): S1a (SegmentedTabs — still
  appearance-bearing, the consume is K.W4's redesign), S1c (ScrubberTimeline — still non-isomorphic),
  S1e (MetricBadge — was always the W7a appearance delta), S1f (StatusDot — tone slot still absent,
  BOOK-5 reaffirmed). All four EXPECTED to stand on the evidence, but RE-CHECKED before closing the
  row (P-invariant-28 — a re-examined exit is a real disposition, not a carried assumption).
- **The RF-17 twin is DELETED, verified, not assumed — RESOLVED.** `useDockClickIntegrity` carries
  the click-strand guard upstream (`glassui-handoff-k.md §3·RF-17`); the kf twin is net-deleted, BUT
  the deletion lands ONLY if `proof:dock-popover-opens` + `proof:live-session` S5 stay green after it
  (the upstream guard genuinely subsumes the kf twin). A residual that needs the kf guard is BOOKED,
  never silently retained.
- **The visual-lock baseline is RE-CAPTURED, not diffed-against-the-old — RESOLVED.** visual-lock is
  NON-AUTHORITATIVE for appearance correctness (`proof-visual-lock.mjs:11`); a re-pin legitimately
  changes the appearance; the H.W8 precedent RE-CAPTURED at the `~3.5.1` re-pin. K.W1 re-captures on
  3.13.0; the appearance VERDICT is the K.WZ TASTE boundary.
- **RF-16 stays the consumer `:freeze` mitigation; the re-pin does NOT discharge it — RESOLVED.** No
  upstream TDZ fix in 3.12.0/3.13.0 (`glassui-handoff-k.md §7`); removing the `:freeze` guard would
  re-introduce the crash. K.W1 RECORDS the non-discharge; the durable cure is a BOOKED P3 AX ask.
- **K.W1 RE-PINS, it does NOT REDESIGN — RESOLVED.** The fluid-typography/`DockRail`/`motion-curves`/
  SegmentedTabs/slider/Constellation surfaces the re-pin makes AVAILABLE are CONSUMED by W2/W3/W4;
  K.W1 lands the foundation those waves build on, verifies the edge is green (the battery, the W7b
  exits, the twin deletion, the visual-lock re-capture), and stops. The scope discipline is what lets
  the design waves build on 3.13.0 instead of against it.
- **The K3-internal fold discharges as VERIFY-LANDED — RESOLVED.** The structured codes landed in
  J.W1 (`errors.ts:35`); the full diagnostics channel is OUT (L / VJ.W3); the `~20-LoC companion` is
  NOT owed. K.W1 writes no engine code — it records the disposition (the only frontier-adjacent fold
  into K, discharged as already-landed, not as work).
