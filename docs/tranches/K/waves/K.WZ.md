# K.WZ — THE CLOSE (the boundary FINAL held to inv ε · the prompt-recap TOTAL · the chronic-closure substrate transition J→K · **the TASTE review-packet verdict ON RECORD before the cut** · the version cut + publish via release.yml + the close-merge round-trip RE-observed · L-SEED final · the K ledger terminal)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** BOOK (the close docs —
  `docs/tranches/K/FINAL.md`) + **USER-DOMAIN** (the TASTE review-packet VERDICT + the version cut +
  npm publish + the close-merge deploy — version owner **Mike Babb** `mike@babb.dev`, confirm-first)
  + **RECORD** (the L-SEED final commit; the K-born invariant verdicts). · **Scope (docs + the
  substrate-transition wiring + the USER-DOMAIN verdict-and-release; NO new behaviour source):**
  `docs/tranches/K/FINAL.md` (NEW — the K product-truth + design-totality close report held to inv
  ε) + the prompt-recap TOTAL (`prompt-recap-k.md` extended through the close: every MANDATE row,
  every U-K1..K20 row, every A→J lineage PARTIAL, every DL-K fold row dispositioned) + the
  **TASTE-boundary review-packet protocol DISCHARGED** (the W4-close packets PRESENTED to the user,
  the user's per-pane VERDICT recorded BEFORE the version cut — the design band CANNOT close without
  it) + the chronic-closure SUBSTRATE TRANSITION (the meta-gate's parse target moves
  `J/PROGRESS.md §"Open deferrals"` → `K/PROGRESS.md §"Open deferrals"` in ONE motion, the gate
  re-run proving non-vacuity on the new substrate) + the `changeset version` cut (≥patch for the
  P0; the design band may justify a minor — content-honest) + the npm publish via `release.yml`
  (USER-DOMAIN, confirm-first) + the close-merge auto-deploy RE-observation (the K.W0/J.W0 round-trip
  oracle re-witnessed on the K close merge itself) + the AFTER review-packet capture (the W3-lib
  capture harness vs the K-open screenshot corpus under `audit/screenshots-k/`) + `L-SEED.md`
  committed (the frontier re-seeded UNCHANGED) + the K ledger terminal. **ZERO `src/**` / `test/**`
  / `.github/**` / `demo/**` BEHAVIOUR edit** — the only non-doc motions are the chronic-closure
  parse-target re-point (a one-line `proof-chronic-closure.mjs` path constant, itself gated by the
  non-vacuity re-run) and the USER-DOMAIN `changeset version`/`npm publish` the wave AUTHORS the
  trigger for and the owner FIRES. · **DAG: LAST in K** — the close runs after every SHIP/DECIDE wave
  (K.W0–K.W6) lands + green CI; it consumes `proof:all == CI` (K.W5/K.W6), the OBSERVED green-CI →
  auto-deploy round-trip primitive (K.W0's `proof:cold-entry` lands WITH the deterministic deploy
  chain; the J.W0 round-trip oracle carries forward), the `proof:cold-entry` GREEN (K.W0), the
  `proof:published-surface` GREEN on the cut tree (K.W1 re-pin + K.W5 publish-edge), and the W4-close
  TASTE packets (K.W4/K.W5). The close's own arbiter is the RUNNING product across every boundary K
  taught it to cross — the COLD axis and the TASTE boundary included — never a prior FINAL, never an
  agent's "designer-eye PASS".

**Title.** *The product-truth + design-totality close. K extended the gate-ORACLE precept to the
COLD axis (the product is TRUE from the first gesture — the hero rainbow-play starts the engine) and
bounded it honestly at the TASTE boundary (where gates cannot carry the design verdict, the protocol
packaged it and the USER signed it BEFORE the cut), made the design language total at its ROOTS (one
font authority, one anchoring grid, the current glass-ui), terminated every ≥4-tranche rider the
gate-blindspot had falsely closed, and deferred the entire library frontier WHOLESALE to L. The
close writes the K FINAL held to inv ε (each boundary claim cites its OBSERVED oracle — the cold-path
engine-write, the deploy round-trip, the published surface, AND the user's recorded packet verdict),
recaps EVERY prompt A→J + U-K1..K20 to a terminal verdict, TRANSITIONS the chronic-closure parse
substrate from J's board to K's in one non-vacuous motion, RE-observes the auto-deploy round-trip on
the close merge itself, books the USER-DOMAIN content-honest cut + publish, captures the AFTER
review-packet corpus, and commits `L-SEED.md` (the frontier re-seeded UNCHANGED).*

This is the §MANDATE's **P-invariant close** at the product-truth + design scale: every carry exits
with a terminal disposition, no perpetual punt survives a fifth tranche (the ten ≥4-tranche `‡`
riders the K audit surfaced all EXIT in K — `deferred-ledger-k.md §2`), and inv ε is mechanical — the
FINAL may not overclaim because the close's own evidence is the boundary battery GREEN
(`proof:all == CI` GREEN where `proof:all` IS the CI roster; `proof:cold-entry` GREEN reading the
ENGINE's own write channel, NOT the idle bob; the close-merge auto-deploy OBSERVED; the tarball passes
`proof:published-surface`; AND the user's per-pane packet verdict ON RECORD). **The catastrophe K
exists to end is the SAME overclaim shape displaced TWO axes in:** J closed honestly at every boundary
it gated — but its oracle family had two unnamed blind axes, the COLD entry and the TASTE verdict, and
the user crossed both hours after the J close (`K.md §opening`, `live-cold-play-path.md §P0-1`,
`live-spring-sequence-mp-verdict.md`). The K FINAL is held to the arbiter K installed: the running
product at the FIRST gesture, OBSERVED, and the design VERDICT signed by the only oracle that can
judge it — the user.

## §Provenance (the close mandate + the two K-born blind axes K exists to certify)

- **`K.md §finding-cluster → wave ledger · K.WZ`** — the close's owned items, verbatim: *"FINAL.md
  held to inv ε; the prompt-recap (`prompt-recap-k.md` extended through the close); the
  chronic-closure substrate transition J→K; the TASTE review packets presented + the user verdict
  recorded; the version cut (patch/minor per content — the P0 fix is at minimum a patch; the design
  band may justify minor) + publish + close-merge round-trip RE-observed; `L-SEED.md` committed (the
  frontier re-seeded UNCHANGED); the K ledger terminal."* The §Hard gate the charter names: *"proof:all
  green where proof:all==CI; the close merge auto-deploys; the packet verdict ON RECORD."*
- **`K.md §invariant set` — the three K-born invariants the close must DISCHARGE WITH WITNESSES.**
  The COLD-axis invariant (*"a certified surface is exercised from its COLD/DEFAULT entry … the cold
  entry gates FIRST"*); the engine-write disambiguation rule (*"a liveness oracle must distinguish
  ENGINE-DRIVEN writes from decorative CSS animation; the B1 vacuity class is forbidden"*); the TASTE
  boundary (*"gates carry CORRECTNESS; they do not carry the design VERDICT … the wave's design band
  closes ONLY on the user's verdict on that packet — a named USER-DOMAIN step, scheduled BEFORE the
  tranche close, never after; an agent's 'designer-eye PASS' is corroboration, never the verdict"*).
  The K FINAL CITES each as DISCHARGED with its observed oracle: the COLD-axis by `proof:cold-entry`
  GREEN reading the isolated ENGINE write channel (the aria-gated OrbitalDrag-wrapper transform +
  the `--ball-p` advance, `.idle-hover` excluded — `K.W0.md §Provenance`); the disambiguation rule by
  the de-vacuoused B1 (the oracle no longer greens on `.idle-hover`); the TASTE boundary by the recorded user packet
  verdict — never a re-assertion.
- **`PATH-FORWARD.md §1-3` / the two unnamed blind axes** — the J close was HONEST at every boundary
  it certified (the 10 plan-vs-delivery lanes spot-verify 10/10 TRUE; the deploy/publish/docs
  boundaries stay discharged and are NOT re-litigated — `K.md §Honest already-done`) but had two
  unnamed blind axes the user crossed hours later: the COLD axis (`live-session-gap-analysis.md §0`:
  `goto #/` ∧ clickPlay ∧ ¬seedControlsOpen → 0 gates) and the TASTE boundary
  (`live-spring-sequence-mp-verdict.md`, `wave-J.W7bc.md`: agent "designer-eye PASS" vs the user's
  "still sucks"/"awful"). The K FINAL records each as CERTIFIED with its observed oracle, never
  CARRIED-silent.
- **`live-cold-play-path.md §P0-1` / `live-session-gap-analysis.md §1` / `demo-scenes-k.md`** — the
  triple-rooted P0 the close certifies CURED: the hero rainbow-play → `#/cube` resume NO-OP at
  `scenePlaybackAdapters.ts:76-79` (resume on a never-started group; the FSM plays, subjects freeze)
  AND the VACUOUS certifying oracle (B1 greens on 41/101 `.idle-hover` CSS-bob transforms with the
  engine OFF — `k-verify-gate-blindspot.mjs`). The close CITES K.W0's `proof:cold-entry` born-RED
  witness + the de-vacuoused B1 as the cures — the COLD-axis invariant made true, OBSERVED.
- **`deferred-ledger-k.md §2` — the P-invariant-28 EXIT roll-up the close confirms TERMINAL.** Ten
  ≥4-tranche `‡` riders remain live entering K and MUST exit (headed by DL-K2, the ≥9-tranche
  gate-blindspot meta-chronic — the ROOT exit that un-blinds DL-K1/K3/K4/K10). The close ledger
  confirms each reached a terminal disposition (FOLD-landed / RE-OPEN-then-FOLD / HANDOFF-with-
  born-RED-gate-or-published-consume-edge / SEED / BOOK-tripwired / KILL-reaffirmed) — ZERO bare
  BOOKs on a fifth ride.
- **`prompt-recap-k.md §0-9 + §FOLD`** — the recap substrate the close extends to TOTAL: A→J no-drops
  ledger (every PARTIAL has a cited resolution or a K home), the J.W7c U1–U8 register (6 of 8 rows
  CONTRADICTED/PARTIAL under the 23:2x re-audit), the cold-path P0 (PR-K1/PR-K2), and the full
  U-K1..K20 register (each verified + rooted + K home; net: 5 P0, 9 P1, 4 P2, 1 P2-meta). The close
  dispositions every row to a terminal verdict held to inv ε.
- **`design-synthesis-k.md §1 + §5` — the TASTE packet content the close presents.** The per-pane
  hierarchy verdicts (HERO COHERENT — the reference; CUBE MID; SPRING FAIL — flattened/green-saturated;
  pathological-width 2560px FAIL — no max-cluster; mobile 390px ACCEPTABLE-structure but inherits the
  green/serif defects) + the TASTE-ANCHOR LEDGER (the four anchors the user named — the rainbow play
  PRESERVE, the icon pops, the red-dashed final state, the audacious serif — each verified). The
  AFTER packet is the W4-close before/after pairing the user verdicts AGAINST.
- **`ci-cd-k.md §F-1/F-2` / `packaging-k.md §6.4` — the publish-boundary facts the close consumes.**
  `release.yml` ran its first-ever time at J (`27378331075`) but omits `proof:published-surface` +
  `proof:readme-runs` (F-4/PKG-11 — K.W1/K.W5 backfill); the `demo-smoke` 35m budget vs the projected
  ~42m post-K-gate-additions (F-2 — K.W5/K.W6 measure-first); the glass-ui `~3.11.2 → 3.13.0` re-pin
  with the `proof:deps-current` floor advanced atomically (PKG-10 — K.W1). The publish fires ONLY on
  the boundary gates GREEN on the cut tree.
- **`L-SEED.md` (head)** — the frontier divination re-filed UNCHANGED from `K-SEED.md` as the
  Tranche-L charter seed, with the reconciling preface (`k-seed-reconciliation.md §4`, Shape A). The
  close COMMITS it as the K terminal deliverable — the wholesale deferral on the record, NOT a punt
  (the value.js grammar resolves in the interval; nothing in the frontier decays by waiting; the P0
  decayed daily, which is why K took the repair band first).
- **`completeness-critic.md §0 + §FOLD`** — the fleet-completeness verdict the close inherits: 32/32
  lanes substantive, 19/20 U-K rooted, U-K19 closed by the critic (the resize-on-drag site is
  `AssetViewport.vue`, playground-ONLY — NOT a deployed-SPA defect; dispositioned RECORD, not folded
  as a live-site repair). The close honors that disposition (U-K19 = RECORD/playground-scope, the
  CC-1 decision-gate verdict recorded).

## §The state, verified (file:line / command / live anchors — re-checked first-hand on `tranche-k-dev`)

The load-bearing facts for the close, each re-checked against the tree at `4f1fc4c`-base
(`tranche-k-dev` forked off the J close tip; the DEV-phase docs are the only on-disk K artifacts):

1. **`docs/tranches/K/FINAL.md` does NOT yet exist** — it is the close report this wave authors
   (after the SHIP/DECIDE waves land). The K tranche dir carries `K.md` + `PATH-FORWARD.md` +
   `L-SEED.md` + `audit/` (32 lanes + the completeness critic + probes + `screenshots-k/`) on disk;
   `waves/` is EMPTY at DEV-spec authoring (`ls docs/tranches/K/waves/` → this file is the first).
   The J tranche's `FINAL.md` is the shape exemplar (numbered §§, `file:line` + gate evidence per
   wave, the catastrophe-it-ended framing, the chronic ledger re-verified live); the K FINAL extends
   it from the FIVE boundaries to the COLD axis + the TASTE boundary.

2. **`docs/tranches/K/PROGRESS.md` does NOT yet exist — the substrate-transition prerequisite is a
   DEV-phase deliverable, and the transition is the close's named motion.** Verified:
   `ls docs/tranches/K/PROGRESS.md` → absent. The chronic-closure parse target is STILL
   `J/PROGRESS.md`: `scripts/proof-chronic-closure.mjs:109` → `const CHRONIC_LEDGER =
   path.join(REPO, "docs/tranches/J/PROGRESS.md")` (verified); the in-script comment (`:56-57`)
   records the J-close transition pattern (*"at the J close the parse target MOVES to
   J/PROGRESS.md §'Open deferrals' in the SAME motion the J ledger becomes authoritative"*). **The K
   ledger (`K/PROGRESS.md §"Open deferrals"`, with the machine-readable Chronicity-integer column the
   gate parses) is authored as a K DEV-phase deliverable** (`K.md §DEV/IMPL boundary` names
   `PROGRESS.md` — *"the board + the K open-deferrals ledger as the next chronic-closure
   substrate"*); through K's dev + impl phases it carries the SUBSTRATE-TRANSITION NOTE (the J ledger
   remains the authoritative parse target until THIS close, exactly the J.WZ pattern —
   `J.WZ.md §state-verified 2`). **This wave EXECUTES the transition + proves non-vacuity (S3).** The
   precept lane binds it: P-SUBSTRATE (`precepts-k.md §3 T6`) — *"the substrate grooming and the K
   chronic registration happen in ONE motion, never in sequence."*

3. **The version is `4.2.0`, published; `release.yml` has run exactly once (the J cut).**
   `package.json:3` `"version": "4.2.0"`; `npm view @mkbabb/keyframes.js version` → `4.2.0` (verified);
   `release.yml` ran its first-ever time at J (run `27378331075`; `release.yml` is tag-triggered on
   `v*.*.*`, INDEPENDENT of the `ci` workflow — `ci-cd-k.md §1.1`). The K cut is content-honest: the
   P0 cold-path fix is at MINIMUM a patch; the glass-ui re-pin (K.W1) is a demo-only dependency change
   (no public API delta — `packaging-k.md §7.1`); the design band is demo-surface, not library
   surface. The cut TYPE is decided at the close against the landed content (S4) — `4.2.0 → 4.2.1`
   (patch) if the K library surface is unchanged, OR `4.2.0 → 4.3.0` (minor) only if a K wave adds a
   genuinely-useful public export (e.g. a canonical keyframe-editor composable — `packaging-k.md §7.1`
   names the candidate, but K.W4's spring editor is demo-internal unless explicitly exported). **The
   default is the honest patch** (the P0 fix + demo design totality, no library API addition); the
   minor fires only if a public export landed. The CUT + PUBLISH are this wave's USER-DOMAIN motion.

4. **The close-merge auto-deploy round-trip is the J.W0 oracle, RE-observed (the deterministic chain
   is K.W0/K.W5's precondition).** `deploy-pages.yml` auto-fires iff `workflow_run.conclusion ==
   'success' && head_branch == 'master' && event == 'push'` (`ci-cd-k.md §1.1`); the J.W0 §Hard gate
   clause (c) requires the observed deploy run's `event_name` read `workflow_run` with
   `workflow_run.event == 'push'` — the AUTO arm, NOT a `workflow_dispatch` substitute. The CLOSE MERGE
   is itself a real master push: this wave RE-witnesses the round-trip on the close merge's own CI run.
   The precondition is that `demo-smoke` runs to completion within the validated wall-clock — K.W0's
   `proof:cold-entry` + K.W5/K.W6's gate additions must NOT push `demo-smoke` past its ceiling
   (`ci-cd-k.md §F-2`: the 35m budget vs the projected ~42m; DL-K13 — re-bound or shard before the
   timeout, the deploy-block class J.W0 fought).

5. **The W3-lib capture harness is the TASTE review-packet generator + the AFTER-corpus engine.** The
   K-open screenshot corpus lives under `audit/screenshots-k/` (the pathological-width, fourier-removed,
   per-scene shots the audit fleet captured — `prompt-recap-k.md §inv-ε`, `completeness-critic.md §1`).
   The TASTE review-packet generator rides the W3-lib capture harness (`K.md §K.W5` — *"the TASTE
   review-packet protocol instrumented (the packet generator rides the W3-lib capture harness)"*;
   `PATH-FORWARD.md §5 K.W5`). The AFTER packet is the W4-close before/after pairing (per-pane,
   desktop+mobile, the named deltas) the user verdicts AGAINST — captured BEFORE the version cut.

6. **The `vite build --watch` build/dist footgun is a LIVE workspace hazard the close must guard
   against.** `live-spring-sequence-mp-verdict.md §0` + `completeness-critic.md §5` recorded a
   background `vite build --watch --mode production` (PID 31288) that EMPTIES `dist/gh-pages` mid-probe
   (observed 3× — `dist/gh-pages/index.html` → `No such file or directory` after a serve). Any close-
   phase browser capture (the AFTER packet, the round-trip served-bytes verify) MUST rebuild-right-
   before-serve or the watch process must be killed. This is the recorded MEMORY build/dist footgun;
   the close records it as a fleet-setup hazard, not a defect (DL-K-adjacent INFRA).

## §Goal

Close Tranche K with a FINAL that tells the truth at the COLD axis and the TASTE boundary — proven,
not asserted — and a terminal ledger with ZERO un-dispositioned rows. The boundary FINAL is held to
inv ε: the product-truth claim (the hero rainbow-play STARTS the engine) cites `proof:cold-entry`
GREEN reading the isolated ENGINE write channel (the aria-gated wrapper transform + the `--ball-p` advance, NOT the idle bob); the design-band claim cites the
USER'S recorded per-pane packet verdict; the deploy/publish claims cite their OBSERVED round-trips.
**The design band CANNOT close until the TASTE review packets are PRESENTED to the user and the
verdict is ON RECORD — a named USER-DOMAIN step scheduled BEFORE the version cut, never after** (the
TASTE-boundary invariant; an agent's "designer-eye PASS" is corroboration, never the verdict). The
prompt-recap dispositions EVERY MANDATE row, EVERY U-K1..K20 row, EVERY A→J lineage PARTIAL, and EVERY
DL-K fold row to a terminal verdict. The chronic-closure substrate TRANSITIONS from J's board to K's
in ONE motion with the gate re-run proving it BITES on the new substrate (no vacuous-green hand-off).
The content-honest cut is CUT and PUBLISHED (USER-DOMAIN, confirm-first), the close-merge auto-deploy
round-trip is RE-observed (the J.W0/K.W0 oracle on the close itself), the AFTER review-packet corpus
is captured against the K-open shots, and `L-SEED.md` is COMMITTED (the frontier re-seeded UNCHANGED,
the wholesale deferral on the record). Seven moves, each at the gestalt altitude the mandate demands;
nothing rides a fifth tranche; no boundary is certified by hand, by paperwork, by one viewport, or by
an agent's taste.

## §Scope

- **S1 — `docs/tranches/K/FINAL.md` (the product-truth + design-totality close report; inv ε
  mechanical).** The authoritative K close. Records what LANDED across K.W0–K.W6 with `file:line` +
  gate evidence, structured by the boundaries K crossed (matching and extending the J FINAL's
  numbered-§ shape from the five boundaries to the COLD axis + the TASTE boundary):
  - **The COLD-axis boundary (K.W0).** The P0 died at the ADAPTER seam — the resume made TOTAL
    (`scenePlaybackAdapters.ts` resume-no-op cured: autoplay-intent + freshly-bound group ⇒
    `group.play()`, NOT a demo-side `play()` sprinkle — `K.md §MANDATE`); the B1 vacuity died by
    engine-write DISAMBIGUATION (the oracle reads `interpFrames`/group-composite inline-style mutation
    or `anim.t`-correlated subject deltas, NOT bare `getComputedStyle` churn — NOT a raised
    distinct-count); the hero→scene handoff smoothed; U-K1 dock default detent; U-K4 amiga
    float/flash (the three seams — `live-amiga-breakage.md`); per-scene cold-mount defects. **inv-ε
    evidence — the OBSERVED oracle, not the assertion:** `proof:cold-entry` GREEN on the fixed tree
    — fresh `localStorage.clear()` context, NO seed, `goto #/`, click the hero rainbow play as the
    FIRST gesture, assert the dock aria flips Play→Pause AND `--ball-p`/the slider advances from 0
    AND the ISOLATED `.cube` engine write traverses ≥3 distinct transforms (the element-isolation IS
    the disambiguation). Born-RED on today's tree: `k-verify-gate-blindspot.mjs` greens B1=101 while
    the play button reads "Play animation" — the gate exists to red on exactly that. **This is the
    COLD-axis invariant + the engine-write disambiguation rule made true, recorded with their observed
    witnesses** (`K.W0 §Hard gate`, `live-session-gap-analysis.md §1`, `K.md §invariant set`).
  - **The TASTE boundary (K.W2 ∥ K.W3 → K.W4, verdict at THIS close).** The design language made total
    at its ROOTS: ONE font-voice authority (display/mono/body — the `--font-serif`≡`--font-display`
    redundancy KILLED, the dock joining the display voice at the ROOT, NOT per-component font classes
    — `styling-typography-k.md`, `live-typography-truth.md`); the dock anchoring a DERIVED grid (NO
    hardcoded offsets; pathological widths AND heights CLUSTER into a bounded container —
    `layout-grid-k.md`); the panes the user indicted re-cut on the current glass-ui with the taste
    anchors the user loves (the rainbow play, the icon pops, the red-dashed final state — the green
    progress palette retired; the STEPPING slider cured at its root, the few-Hz readout mirror no
    longer driving it — `live-spring-sequence-mp-verdict.md §2`, `design-synthesis-k.md §3`); the
    FourierField removed from the hero (`live-fourier-grid.md`). **inv-ε evidence — the design VERDICT
    is the USER's, ON RECORD:** the W4-close review packets (per-pane before/after, desktop+mobile, the
    named deltas) PRESENTED, the user's per-pane verdict RECORDED before the cut (S2/clause (g)). The
    per-finding RUNTIME asserts (the positive font-voice resolve, the cluster + no-hardcoded-offset
    assert, the no-single-option + continuous-slider + red-dashed asserts — `PATH-FORWARD.md §6`)
    CORROBORATE; they never substitute for the verdict. **This is the TASTE-boundary invariant made
    true** — the boundary the J close crossed silently (agent PASS vs user "awful") now has a NAME, a
    protocol, and the user's signature.
  - **The DEPLOY boundary (K.W0 carries J.W0 forward).** The deploy chain stays deterministic on
    Linux (the `navToScene` per-expected primitive J.W0 landed; `proof:cold-entry` joins it as a
    correctness-tier browser gate); the close-merge auto-deploy round-trip RE-observed (S6). **inv-ε
    evidence:** the green-CI → `deploy-pages.yml` auto-fire → live-bytes round-trip OBSERVED on the K
    close merge (the CI run id, the deploy run's `event_name == 'workflow_run'` /
    `workflow_run.event == 'push'`, the served-build hash). NOT re-litigated as a J boundary — the J
    deploy boundary stays discharged (`K.md §Honest already-done`); K re-observes it OF THE K CLOSE.
  - **The PUBLISH boundary (K.W1 re-pin + K.W5 publish-edge).** `proof:published-surface` GREEN on the
    cut tree; the glass-ui re-pin `~3.11.2 → 3.13.0` with `proof:deps-current` floor advanced
    atomically (`packaging-k.md §6.3`, `ci-cd-k.md §F-6`); `release.yml` backfilled with
    `proof:published-surface` + `proof:readme-runs` (F-4/PKG-11 — the publish gate made == the CI
    publish-boundary oracle); the README Quick Start fixed (PKG-4 — the `loadAnimationEngine()`
    preamble, the dead links, the `package.json` typo, the lockfile `4.1.0→4.2.x` drift —
    `packaging-k.md §4-5`). **inv-ε evidence:** the gate witnessed born-RED on the pre-fix tree
    (the `release.yml` 4-step subset reds against the publish-boundary; the floor reds against 3.13.0
    while tilde-stuck at `~3.11.2`); GREEN only when the tarball, the exports, the README, and the
    floor agree on the cut tree. The content-honest cut + published (S4).
  - **The DOCS boundary (K.W5).** The CLAUDE.md files held to the tree (re-verified, not re-written
    unless drift — the J docs boundary stays discharged); the executable-README runner is the runtime
    DOCS oracle (`proof:readme-runs` GREEN on the cut dist). **inv-ε evidence:** each structural claim
    verifiable against the tree; the executable snippets the runtime oracle.
  - **The TERMINATIONS layer (K.W6 — P-invariant-28).** Every ≥4-tranche `‡` rider EXITED via a
    born-RED gate, a measurement artifact, a published consume-edge, or a reasoned KILL — the close
    ledger carries ZERO rows tagged MEASURE-FIRST without a measurement (`deferred-ledger-k.md §2`).
    The ROOT exit is DL-K2 (the ≥9-tranche gate-blindspot meta-chronic) — terminated by
    `proof:cold-entry`'s engine-write disambiguation, which un-blinds DL-K1/K3/K4/K10; the
    mobile-Lighthouse floors re-asserted on a calibrated quiet host (DL-K11 — never measured-quiet
    before); the W2-noted drag-seam gaps + the dev-mode parity chronicle dispositioned (DL-K15/K24,
    CC-4); U-K19 RECORD (playground-only, NOT a deployed-SPA defect — `completeness-critic.md §4`).
  - **inv ε is MECHANICAL at the close (the §Design-decisions binding):** the K FINAL's evidence is
    the boundary battery GREEN on the fixed tree — `proof:all == CI` GREEN, `proof:cold-entry` GREEN
    reading the engine's own write channel, the close-merge auto-deploy OBSERVED,
    `proof:published-surface` GREEN, AND the user's recorded packet verdict. NO chain-of-trust over a
    prior FINAL; the catastrophe (J's own honest gates blind to two axes) proves that is worthless.
    The close CANNOT overclaim because its oracle is the running product at the FIRST gesture +
    the user's signature on the design — and where K could not make a boundary fully true (the
    library frontier deferred WHOLESALE to L; an open glass-ui consume-edge), it says so with the
    carry's named mechanism, never a silent gap.
  - **The single-voice discipline (the J §G supersession-coherence pattern to AVOID):** the K FINAL
    is the SOLE authoritative narrative. Where it supersedes a frozen DEV-phase disposition in a K
    wave spec or `K/PROGRESS.md`, it says so EXPLICITLY (a pointer to the FINAL's §), never leaving
    two live narratives. The exact two-narrative drift `wave-I.WZ-postclose.md §G` caught in I's
    close (and that J forbade) is forbidden here: one tranche, one authoritative close voice.

- **S2 — the prompt-recap reconciliation TOTAL (every MANDATE row + every U-K1..K20 + every A→J
  PARTIAL + every DL-K fold row — terminal).** The close reconciles FOUR strata to a terminal
  disposition, none dropped (`prompt-recap-k.md` extended through the close):
  - **Every charter MANDATE row** (`K.md §MANDATE` — the verbatim development directive + the design
    directive + the precept spine + the K-specific named forbiddings): the deep parallel audit RAN
    (32 lanes + the completeness critic); NO quick solutions / NO workarounds is HELD (the P0 died at
    the ADAPTER seam, NOT a demo-side `play()` sprinkle; the B1 vacuity died by DISAMBIGUATION, NOT a
    raised distinct-count; the font truth died at ONE token authority, NOT per-component font classes;
    the dock layout died at the GRID/anchoring system, NOT retuned magic offsets — the four K-specific
    forbiddings, `K.md §MANDATE`); NO legacy beside its replacement is HELD (the hand-rolled glass-ui
    twins died WITH the 3.13.0 consume; the redundant display token KILLED; the green palette retired);
    architectural transpositions for ELEGANCE/SIMPLICITY/PERFORMANCE landed at the right seams (the
    adapter resume, the one font authority, the derived anchoring grid, the engine-write oracle —
    measure-first where perf-claimed); MEASURE-FIRST is HELD (the mobile-Lighthouse floors re-asserted
    on a quiet host, not deferred again; the demo-smoke wall-clock re-measured before K gates shipped);
    inv-16 is HELD (glass-ui/value.js/parse-that consumed PUBLISHED; every glass-ui item a consume-edge
    or a born-RED handoff, never a kf-side patch; the engine in scope per the permanent T2 rule);
    isomorphic styling is HELD except the named K design deltas (deliberate, enumerated, user-verdicted).
  - **Every U-K1..K20 row** — each verified + rooted + the K wave that landed it, to a terminal verdict
    (`prompt-recap-k.md §5`): U-K2/K3/K4/K5 (the cold-path P0 + amiga + square) FOLD-landed K.W0;
    U-K14/K15 (glass-ui currency + stepped slider) K.W1/K.W4; U-K6/K8/K9/K10 (fonts) K.W2; U-K7
    (layout) K.W3; U-K11/K12/K13/K16/K17/K18/K20 (the panes) K.W4 with the USER PACKET VERDICT;
    U-K19 RECORD (playground-only, NOT deployed — `completeness-critic.md §4/CC-1`). **The 23:2x
    register that was ZERO-ADDRESSED at K-open is the close's recap spine** — the strongest signal
    J's W7c design band did not meet the bar; the close confirms each row reached the user's verdict,
    not an agent's.
  - **Every A→J lineage PARTIAL** (`prompt-recap-k.md §2`, the J FINAL §10 reconciliation re-verified
    against the K tree) — each confirmed terminal or carried as an OUT/sibling-HANDOFF (the value.js
    next-slice VJ-1..9, the parse-that packrat, the MorphSVG sampler — ride the next re-pin,
    consume-edge-shaped; the dock double-click resolved-and-re-verified on the 3.13.0 dock rewrite).
  - **Every DL-K fold row** (`deferred-ledger-k.md §1` — 32 consolidated rows) — each carries its
    closure cell citing the gate/evidence: the RE-OPENED P0 band (DL-K1..K5) FOLD-landed; the glass-ui
    band (DL-K6..K10) consumed-on-3.13.0; the perf band (DL-K11..K13) measured; the J.W* residual
    (DL-K14..K19) folded-on-touch or BOOK; the sibling-handoff band (DL-K20..K25) consume-edge-shaped;
    the K-SEED charter (DL-K26..K30) DEFERRED-TO-L via `L-SEED.md`; the BOOK/KILL bands (DL-K31/K32)
    tripwired/reaffirmed; the two completeness-hardening consolidation bands (DL-K33 engine-core
    hygiene EC-1..EC-13 → K.W0/K.W5/value.js; DL-K34 packaging+README+toolchain PKG-1..PKG-11 →
    K.WZ/K.W1/value.js) folded/HANDOFF per `PROGRESS.md §"Open deferrals"`.
  - **WHY a recap, not a tally:** the catastrophe was a FINAL that asserted "all requests resolved"
    against a proxy (and, in J, against an agent's taste). The K recap reconciles each row to a
    TERMINAL verdict with its gate/evidence — AND, for the design rows, with the USER's verdict; the
    recap is itself held to inv ε.

- **S3 — the chronic-closure SUBSTRATE TRANSITION (one motion · the gate re-run proving non-vacuity
  on the new substrate).** The meta-gate `proof:chronic-closure` parses a single authoritative
  `§"Open deferrals"` ledger. Through K's dev + impl phases the authoritative substrate is
  `J/PROGRESS.md §"Open deferrals"` (`proof-chronic-closure.mjs:109`, verified; the K ledger carries
  the SUBSTRATE-TRANSITION NOTE until this close — exactly the J.WZ pattern). At THIS close, in ONE
  motion:
  - **The parse target moves `J/PROGRESS.md` → `K/PROGRESS.md` §"Open deferrals"** — the single path
    constant in `proof-chronic-closure.mjs` (currently `:109`) re-points to the K ledger, which
    becomes authoritative in the SAME motion. The K ledger carries the machine-readable
    Chronicity-integer column the gate reads (every row leads with an INTEGER tranche-span; the
    ≥4-tranche EXIT-ONLY mandate, P-invariant-28, enforced mechanically off that integer — the ten
    `‡` riders of `deferred-ledger-k.md §2` each EXITED, ZERO bare BOOKs).
  - **The transition is itself GATED — non-vacuity proven on the new substrate (the binding bite).**
    Per the chronic-closure meta-invariant (a closure must cite a runtime/boundary gate that BIT; a
    HANDOFF row may target ONLY a PUBLISHED version or a kf-owned consume-edge, never a future version
    number / unreleased commit — the B7 vaporware lesson), the gate is RE-RUN against the K ledger and
    MUST BITE: a deliberately-malformed probe row (a FOLD row citing a SOURCE-SHAPE gate; a HANDOFF
    row targeting an UNPUBLISHED version; a ≥4-tranche row with no EXIT disposition) is planted and the
    gate REDs on it — proving the K substrate is parsed and policed, not a rubber-stamp swap. Then the
    planted row is removed and the gate GREENs on the true K ledger. **The born-RED witness:** the gate
    RED on the planted malformed K-ledger row BEFORE the transition is accepted; GREEN on the clean K
    ledger after.
  - **The P-SUBSTRATE precept binds (`precepts-k.md §3 T6`):** the substrate grooming (the K ledger
    becoming the parse target) and the K chronic registration happen in ONE motion, never in sequence
    — before any K chronic is opened OR closed against the K substrate, the parse target IS the K
    ledger AND green on a groomed K substrate. A swap that re-points the path without re-proving the
    gate bites on the new substrate is the exact paperwork-auditor failure mode the meta-gate exists
    to forbid (the deepest expression of the gate-blindspot — a source-shape gate auditing a markdown
    table). **WHY in the close, not earlier:** the K ledger is AUTHORITATIVE only when K's waves have
    landed and their closures are real (each cell cites a gate that BIT on a defect tree). Moving the
    substrate before the closures are real would make the gate parse aspirational rows — the
    transition is correctly the LAST motion, in lockstep with the K ledger becoming authoritative.

- **S4 — the version cut + npm publish (USER-DOMAIN · Mike Babb · confirm-first · content-honest).**
  This wave runs the CUT + PUBLISH, USER-DOMAIN, confirm-first:
  - **The cut (content-honest):** `changeset version` consumes the K changeset(s) → `package.json`
    `4.2.0 → 4.2.1` (the honest PATCH default: the P0 cold-path fix + the demo design totality + the
    glass-ui demo re-pin are NOT library-API changes — `packaging-k.md §7.1/§7.2`, the public surface
    is the stable 24 LIGHT + 16 HEAVY = 40 exports), OR `4.2.0 → 4.3.0` (minor) ONLY if a K wave
    landed a genuinely-useful NEW public export (the candidate: a canonical keyframe-editor composable
    from K.W4's spring editor IF it is exported for consumers, not just demo-internal —
    `packaging-k.md §7.1`). **The cut type is decided at the close against the LANDED content, the
    decision RECORDED in the FINAL** (the default is the patch — the P0 + design totality, no library
    API addition). The `package-lock.json` `4.1.0` drift is corrected in the same motion
    (`packaging-k.md §5.2/PKG-8`).
  - **The publish (PRE-CONDITION — the boundary gates GREEN first):** the publish fires ONLY after
    `proof:all == CI` is GREEN, `proof:published-surface` is GREEN on the cut tree (the tarball is
    the cut version, `_redirects`-free, the d.ts complete, the README executable — `packaging-k.md
    §1.1`), `proof:cold-entry` is GREEN (the COLD axis certified), the close-merge auto-deploy
    round-trip is OBSERVED (S6), AND the TASTE packet verdict is ON RECORD (S2 — the design band
    cannot close without it). The publish leg rides `release.yml` (tag-triggered, on the pushed
    `v4.2.x`/`v4.3.0` tag) — backfilled with `proof:published-surface` + `proof:readme-runs` (K.W1/K.W5,
    `packaging-k.md §6.4/PKG-11`) + `timeout-minutes: 15` (K.W1, `ci-cd-k.md §F-1`). The close RECORDS
    the run id + the published version as the OBSERVED publish oracle.
  - **The release.yml-gates-on-correctness decision (RECORD + decide — the J carry, `precepts-k.md
    §P20`):** the open USER-DOMAIN question — whether `release.yml` should additionally gate on the
    full `proof:correctness` suite, not just `proof:boundary` — is RESOLVED at the close (add the
    publish-boundary oracles per PKG-11 + RECORD the boundary-vs-correctness decision; the library
    tarball is glass-ui-free so `proof:boundary` + the green-CI merge gate is defensible). NOT a
    silent gap.
  - **USER-DOMAIN, confirm-first — NO autonomous registry mutation.** The version owner is **Mike
    Babb (`mike@babb.dev`)**; the close AUTHORS the cut trigger + the publish pre-conditions and
    PRESENTS them; the owner FIRES `changeset version` + the tag push, confirm-first. The wave's
    deliverable is the content-honest cut + the gated publish, not an autonomous npm push.

- **S5 — the AFTER review-packet capture (the W3-lib capture harness vs the K-open corpus · the
  TASTE-boundary visual record + the design-suffusion evidence).** Re-run the W3-lib capture harness
  (the TASTE review-packet generator, `K.md §K.W5`) on the post-W4 re-skinned tree to capture the
  AFTER review packet (per-pane before/after, desktop+mobile, the named deltas — the same matrix as
  the K-open corpus under `audit/screenshots-k/`), and diff it against the K-open shots:
  - **The expected deltas ARE the named design deltas** (`K.md §MANDATE` — *"isomorphic styling EXCEPT
    the named design deltas"*): the one font-voice authority (the dock joining the display voice), the
    derived anchoring grid (pathological-screen clustering), the green→red-dashed final-state register,
    the retired green-progress palette, the continuous (un-stepped) slider, the removed FourierField.
    EVERY delta is one a K wave named in its spec; an UNNAMED delta (a regression outside the
    enumerated totality) is a RED — the AFTER packet is the visual proof the totality is the ONLY
    change.
  - **The AFTER packet IS the TASTE-boundary artifact the user verdicts against** — this is NOT a
    separate motion from S2's verdict: the W4-close packet (S5) is GENERATED, PRESENTED to the user,
    and the per-pane verdict RECORDED (S2/clause (g)) BEFORE the version cut. The packet is the
    human-readable design record committed alongside the K FINAL; the per-finding runtime asserts
    (`PATH-FORWARD.md §6`) are the gate-side companions.
  - **The build/dist footgun guard (S6/state 6):** the AFTER capture MUST rebuild-right-before-serve
    (or kill the `vite build --watch` process) — the recorded `dist/gh-pages`-emptying hazard
    (`live-spring-sequence-mp-verdict.md §0`, MEMORY). A vacuous-empty capture is forbidden.
  - **WHY the capture at the close:** the design directive's success criterion is the RENDERED RESULT
    measured against the design intent — and, by the TASTE boundary, signed by the USER. The
    AFTER-vs-K-open diff + the user verdict is that measurement, closing the design boundary with an
    observed visual oracle AND the user's signature rather than prose or an agent's taste.

- **S6 — the close-merge auto-deploy round-trip RE-observed (the J.W0/K.W0 oracle on the close
  itself).** The K close merge to `master` is itself a real master push — the EXACT event the J.W0
  §Hard gate clause (c) certifies. This wave RE-witnesses the round-trip on the close merge's own CI
  run:
  - **The observed chain:** the close-merge push → the `ci` workflow concludes `success` (BOTH `gates`
    and `demo-smoke` GREEN on the Linux runner, `scene-control-dfa` + `proof:cold-entry` included via
    `navToScene`, ZERO escapes, the gate tail running to completion within the validated wall-clock —
    the K gate additions did NOT push `demo-smoke` past its re-measured ceiling, `ci-cd-k.md §F-2`,
    DL-K13) → `deploy-pages.yml`'s `workflow_run`-on-`push` arm evaluates true → the `deploy` job runs
    `scripts/pages-deploy.sh` → the live site (keyframes.babb.dev) serves the close-merge `head_sha`'s
    bytes. **RECORD the CI run id, the deploy run's TRIGGER (`event_name == 'workflow_run'`,
    `workflow_run.event == 'push'` — the AUTO arm, NOT a `workflow_dispatch` substitute), and the
    served-build hash** (`J.W0 §Hard gate clause (c)`, §No-workaround prohibitions).
  - **WHY re-observe on the close merge:** J.W0's observed round-trip proved the chain works on A
    master push; the K close-merge re-observation proves the K CLOSE itself ships through the honest
    auto-chain — the deploy boundary is true OF THE K CLOSE. **The terminal product-truth observation
    is compound:** the certified bytes are the bytes the live site serves, AND those bytes are the ones
    whose cold front door ANIMATES (the live site no longer serves a non-animating front door — the P0
    that decayed daily, `PATH-FORWARD.md §4, terminal-reading`). The close VERIFIES the deployed
    `keyframes.babb.dev` cold path animates on the served bytes (the live-origin re-verify
    `completeness-critic.md §FOLD CC-3 flagged was never done over the deployed origin — the close
    does it).

- **S7 — `L-SEED.md` COMMITTED (the frontier re-seeded UNCHANGED · the wholesale deferral on the
  record · the K terminal deliverable).** The close commits `docs/tranches/K/L-SEED.md` (authored in
  DEV — `K-SEED.md` re-filed UNCHANGED in content as the Tranche-L charter seed, with the reconciling
  preface `k-seed-reconciliation.md §4`):
  - **The deferral is reasoned, NOT a punt** (`L-SEED.md` head, `K.md §The frontier defers wholesale
    to L`): the value.js grammar gates (VJ.W1 scroll / VJ.W2 perceptual ramp) get the K interval to
    ship in value.js' own tranche process, so L starts UN-blocked; none of the frontier decays by
    waiting (the 12 KILLs non-re-litigable; the BOOKs tripwired); the P0 decayed DAILY, which is why
    K took the repair band first; L inherits a STRONGER substrate (ED-3's dogfood inversion is honest
    ONLY because K repaired the demo first — shipping ED-3 over a broken demo would re-commit the J
    sin). **L's authorization is its own** — L develops only after K closes, on L's own authorization
    (the dev/impl boundary).
  - **The ONLY frontier fold INTO K is K3-internal** (the 2 engine diagnostic rows, already J.W1-tagged
    — VERIFY landed, else a ~20-LoC K.W companion; `PATH-FORWARD.md §4`); WL2-B / PHYS-C are NOT folded
    (M+-effort waves wearing fold hats — `DL-K16/K17` dispositioned in the ledger, not manufactured as
    K work). The close RECORDS the L-SEED commit as the terminal frontier disposition.
  - **WHY at the close:** the L-SEED is authored in DEV but COMMITTED as the terminal K deliverable so
    the wholesale-deferral argument is on the record with the full K context (the P0 + the 20-finding
    register + the value.js-interval un-blocking) — the opposite of an un-rowed frontier punt.

## §Hard gate (the close's terminal oracle — the boundary battery GREEN, OBSERVED · the PACKET VERDICT on record · zero un-dispositioned rows)

The close's §Hard gate is NOT a new gate — it is the COMPOSITION of the K waves' boundary oracles,
re-witnessed on the close tree + the close merge, plus the substrate-transition non-vacuity bite AND
the USER-DOMAIN packet verdict on record. The close is GREEN iff ALL of the following hold, each
citing its OBSERVED oracle (inv ε mechanical — the close cannot overclaim because its arbiter is the
running product at the FIRST gesture + the user's signature on the design). **The charter's named
§Hard gate (`K.md §K.WZ`): proof:all green where proof:all==CI; the close merge auto-deploys; the
packet verdict ON RECORD.**

- **clause (a) — `proof:all == CI` GREEN, where `proof:all` IS the CI roster (CORRECTNESS).** The
  full `proof:all` suite GREEN, and the `proof:ci-coverage` two-way equivalence proves `proof:all` ==
  the CI gate roster both ways (no gate hard-gates CI without a named tier; no tier gate absent from
  CI). **BITE:** reds if any correctness gate reds, OR if the two-way equivalence finds a roster
  mismatch. The T1 gate-corpus decision (`precepts-k.md §3 T1` — net-delete the relabeled proxy
  lattice OR formally own the 109-gate corpus) is RESOLVED and recorded; the meta-gate one-directionality
  (T3) cured (the `WAVE_HARD_GATES` set derived from `proof:correctness` membership, `proof:demo-fonts`
  tier-decided).
- **clause (b) — `proof:cold-entry` GREEN reading the ENGINE's own write channel (CORRECTNESS · the
  COLD-axis invariant + the engine-write disambiguation rule, the headline K oracle).** From a fresh
  `localStorage.clear()` context with NO seed, `goto #/`, the hero rainbow play clicked as the FIRST
  gesture, the LOAD-BEARING engine-attributable pair holds — the dock aria flips Play→Pause AND
  `--ball-p`/the slider advances from 0 — PLUS the aria-GATED corroborator: the engine-write subject
  (the OrbitalDrag wrapper for cube/amiga) traverses ≥3 distinct transforms WHILE aria="Pause",
  `.idle-hover` excluded (the disambiguation is the idle-bob exclusion + the aria-gate + the `--ball-p`
  advance, NOT a bare single-element count — `.cube`=0 cold / 0-1 warm, `.graph`=13 engine-OFF, per
  `K.W0.md §Provenance` reconciling `live-session-gap-analysis.md §2` ↔ `gate-estate-k.md §3/§4`),
  every scene cold-entered from the hero CTA and from direct hash. **BITE:** reds on today's tree —
  `k-verify-gate-blindspot.mjs` greens B1=101 on the `.idle-hover` CSS bob while the play button reads
  "Play animation"; `proof:cold-entry` reds on exactly that (the resume-no-op leaves the engine OFF,
  the slider parked at "0", the aria precondition false); GREEN only when the adapter resume is total
  AND the oracle reads the engine's own hand. **NO escape:** NOT a longer settle, NOT a raised
  distinct-count, NOT a demo-side `play()` sprinkle (`K.md §MANDATE`, the four named forbiddings). **This
  is the K headline cure** — the product TRUE from the first gesture, the meta-chronic DL-K2 terminated.
- **clause (c) — the close-merge auto-deploy round-trip OBSERVED (CORRECTNESS · the J.W0 oracle
  RE-witnessed; the charter's "the close merge auto-deploys").** The close merge's own `ci` run
  concludes `success` on Linux, `deploy-pages.yml`'s `workflow_run`-on-`push` arm fires, and the live
  site serves the close-merge `head_sha` bytes — the deploy run's `event_name == 'workflow_run'` /
  `workflow_run.event == 'push'` (the AUTO arm), the CI run id + served hash recorded (S6); AND the
  deployed `keyframes.babb.dev` cold path ANIMATES on the served bytes (the live-origin re-verify).
  **BITE:** reds if the chain is blocked (any `demo-smoke` red — including a K-gate-addition wall-clock
  timeout, `ci-cd-k.md §F-2`/DL-K13), or if the deploy fired via `workflow_dispatch` / a manual
  `wrangler` bypass rather than the auto arm.
- **clause (d) — `proof:published-surface` GREEN on the CUT tree (CORRECTNESS · the PUBLISH oracle).**
  The packed tarball surface == the source's public exports == the README's taught (and EXECUTING) API
  == the runtime `loadAnimationEngine` surface; `_redirects` absent; the d.ts complete; the version the
  content-honest cut; the README Quick Start fixed (PKG-4); `proof:deps-current` floor advanced to
  3.13.0 in lockstep with the re-pin (PKG-10). **BITE:** reds on the pre-fix tree (the README Quick
  Start `new CSSKeyframesAnimation()` without `loadAnimationEngine()`; the floor at 3.11.2 against
  3.13.0); GREEN only when the tarball, the exports, the README, and the floor agree on the cut tree.
  The publish (S4) fires only on this GREEN.
- **clause (e) — the chronic-closure SUBSTRATE TRANSITION is non-vacuous on the new substrate
  (CORRECTNESS · the meta-gate's own bite).** `proof:chronic-closure` re-pointed to `K/PROGRESS.md
  §"Open deferrals"` REDs on a planted malformed K-ledger row (a FOLD row citing a source-shape gate;
  a HANDOFF targeting an unpublished version; a ≥4-tranche row with no EXIT), then GREENs on the clean
  K ledger (S3). **BITE:** the born-RED witness is the planted malformed row; a swap that greens
  vacuously (the gate not actually parsing/policing the K substrate) is the paperwork-auditor failure
  mode this clause exists to forbid — the deepest expression of the gate-blindspot DL-K2 closes.
- **clause (f) — the K ledger is TERMINAL: zero un-dispositioned rows (CORRECTNESS · the P-invariant
  close).** Every row in `K/PROGRESS.md §"Open deferrals"` carries a terminal disposition
  (FOLD-landed / RE-OPEN-then-FOLD / VERIFY-rerun / HANDOFF-with-born-RED-gate-or-published-consume-edge
  / SEED-deferred-to-L / BOOK-tripwired / RECORD / KILL / USER-DOMAIN-fired); every ≥4-tranche `‡`
  rider EXITED via a born-RED gate, a measurement, a published consume-edge, or a reasoned KILL (the
  ten riders of `deferred-ledger-k.md §2`; the close ledger carries ZERO rows tagged MEASURE-FIRST
  without a measurement — DL-K11 mobile-Lighthouse floors MEASURED on a quiet host, not deferred
  again). **BITE:** reds if any row is un-dispositioned, or any MEASURE-FIRST row lacks its measurement,
  or any ≥4-tranche rider is a bare BOOK on a fifth ride.
- **clause (g) — the TASTE review-packet verdict is ON RECORD (CORRECTNESS · the TASTE-boundary
  invariant; the charter's "the packet verdict ON RECORD").** The W4-close review packets (per-pane
  before/after, desktop+mobile, the named deltas — S5) were PRESENTED to the user, and the user's
  per-pane VERDICT is recorded in the K FINAL BEFORE the version cut (S4) — a named USER-DOMAIN step,
  scheduled BEFORE the close, never after. **BITE:** the design band does NOT close (the clause reds)
  if the packets were not presented, OR the verdict is not on record, OR the verdict is an agent's
  "designer-eye PASS" rather than the USER's (an agent verdict is corroboration, never the verdict —
  `K.md §invariant set`). **This is the catastrophe-shape K exists to end:** J closed its design band
  on an agent PASS while the user said "awful" hours later; the K design band closes ONLY on the
  user's signature on the packet. The per-finding runtime asserts (`PATH-FORWARD.md §6`) CORROBORATE;
  they may NOT substitute for the verdict.
- **clause (h) — the prompt-recap is TOTAL + `L-SEED.md` committed (CORRECTNESS · inv ε on the recap +
  the frontier terminal).** Every MANDATE row HELD-or-CARRIED; every U-K1..K20 row dispositioned to a
  terminal verdict (and, for the design rows, to the USER's verdict); every A→J PARTIAL + every DL-K
  fold row terminal (S2); `L-SEED.md` committed with the wholesale-deferral argument (S7). **BITE:**
  reds if any prompt-derived request is unstatused (the status-gap shape the prior closes caught), or
  if the frontier is an un-rowed punt rather than a committed reasoned deferral.

**The §spine bar — MUST bite.** Clauses (a)–(h) are CORRECTNESS: they actuate the running product at
every boundary K taught it to cross — `proof:all == CI` GREEN (the regime), `proof:cold-entry` GREEN
reading the engine's own hand (the COLD axis), the close-merge auto-deploy OBSERVED (deploy),
`proof:published-surface` GREEN on the cut tree (publish/docs), the substrate transition non-vacuous
(the meta-gate's own honesty), the ledger terminal (the P-invariant), the TASTE packet verdict ON
RECORD (the design band signed by the user), the recap TOTAL + L-SEED committed (inv ε + the frontier
terminal). **The close is GREEN iff (a)–(h) hold against the close tree + the close merge.** The inv-ε
arbiter is the boundary battery itself + the user's signature — the close CANNOT overclaim because its
evidence is the OBSERVED oracle at each boundary AND the user's recorded packet verdict, never a
chain-of-trust over a prior FINAL, never an agent's taste. **The two K-born blind axes are CERTIFIED,
recorded with their observed witnesses:** the COLD axis by clause (b) (`proof:cold-entry` reading the
isolated engine write); the TASTE boundary by clause (g) (the user's per-pane packet verdict on
record). **NO CARRIED gap is silent** — where K could not fully cure a boundary (the library frontier
deferred WHOLESALE to L; an open glass-ui consume-edge that publishes after K), the carry's NAMED
mechanism is recorded (S7; the consume-edge born-RED-able grep that flips the instant the publish
lands — `deferred-ledger-k.md §1e`).

## §No-workaround prohibitions (BINDING — the mandate's named forbiddings for the close)

- **NO overclaim in the FINAL.** Every boundary claim cites its OBSERVED oracle (the cold-path
  `proof:cold-entry` reading the isolated engine write; the deploy round-trip's recorded run id +
  auto-arm trigger; `proof:published-surface` GREEN; the user's recorded packet verdict). A claim
  that cannot cite an observed oracle is CARRIED with its named mechanism, never asserted. The J
  close's two unnamed blind axes (COLD + TASTE) are the precise shape forbidden — the K FINAL's
  product-truth + design claims are OBSERVED + USER-SIGNED, not agent-asserted.
- **NO design-band close without the USER packet verdict (the TASTE-boundary invariant — the headline
  K forbidding).** The design band closes ONLY on the user's verdict on the W4-close review packets,
  PRESENTED BEFORE the version cut, never after. An agent's "designer-eye PASS" is corroboration,
  never the verdict; a green appearance gate (the runtime font/cluster/slider/red-dashed asserts)
  CORROBORATES but does not certify design-good. The close CANNOT claim the design band on
  gate-green + agent-taste — the exact J.W7c failure shape (`live-spring-sequence-mp-verdict.md`,
  `wave-J.W7bc.md`).
- **NO cold-path workaround in the certification.** `proof:cold-entry` is GREEN because the adapter
  resume is TOTAL (autoplay-intent + freshly-bound group ⇒ `group.play()`) and the oracle reads the
  ENGINE's own write channel — NOT because of a longer settle, NOT a raised distinct-count, NOT a
  demo-side `play()` sprinkle, NOT an oracle that still counts the `.idle-hover` bob (the four named
  forbiddings, `K.md §MANDATE`). A certification that greens on idle CSS motion is the B1 vacuity the
  close exists to end.
- **NO two-narrative drift.** The K FINAL is the SOLE authoritative close voice; where it supersedes
  a frozen K wave spec / `K/PROGRESS.md` disposition it says so EXPLICITLY (a pointer, not a silent
  re-write). The `wave-I.WZ-postclose.md §G` pattern (two live narratives a future reader must
  adjudicate against the tree) is forbidden — one tranche, one close voice.
- **NO vacuous substrate transition.** The parse-target re-point (S3) is accepted ONLY after the
  re-run proves the gate BITES on a planted malformed K-ledger row — a swap that greens without
  re-policing the new substrate is the paperwork-auditor failure mode (the gate-blindspot's deepest
  expression). P-SUBSTRATE binds: one motion, never in sequence.
- **NO autonomous registry mutation.** The version cut + npm publish are USER-DOMAIN (Mike Babb,
  confirm-first); the close AUTHORS the trigger + the gated pre-conditions and PRESENTS them, the
  owner FIRES. The publish fires ONLY on `proof:all == CI` GREEN + `proof:cold-entry` GREEN +
  `proof:published-surface` GREEN + the close-merge round-trip OBSERVED + the TASTE packet verdict ON
  RECORD — never ahead of the boundary gates, never ahead of the user's verdict.
- **NO `workflow_dispatch` / manual-`wrangler` substitute for the close-merge round-trip.** The S6
  re-observation is discharged ONLY by the AUTO `workflow_run`-on-`push` arm, the `event_name`/
  `workflow_run.event` recorded — a manual dispatch proves bytes CAN ship, never that the auto-chain
  fired on the close merge (`J.W0 §No-workaround prohibitions`).
- **NO bare-BOOK fifth ride for any ≥4-tranche rider.** Each of the ten `‡` riders
  (`deferred-ledger-k.md §2`) EXITS K via a born-RED gate, a measurement, a published consume-edge,
  or a reasoned KILL — never a bare BOOK on a fifth ride (P-invariant-28). The mobile-Lighthouse floors
  (DL-K11) are MEASURED on a quiet host, not deferred again behind the P6-posture rationalization.
- **NO frontier punt disguised as a deferral.** `L-SEED.md` is committed with the wholesale-deferral
  ARGUMENT (the value.js-interval un-blocking; the daily P0 decay; the stronger-substrate inheritance)
  — a reasoned, rowed deferral, NOT an un-rowed "we'll get to it." The 12 KILLs stay non-re-litigable;
  the BOOKs stay tripwired.

## §Folds (every K.md-assigned close item, with its evidence citation)

- **FINAL.md held to inv ε** — S1 (the close report; each claim cites its OBSERVED oracle — the
  cold-path engine-write, the deploy round-trip, the published surface, the user packet verdict; the
  single-voice discipline). `K.md §K.WZ`, `precepts-k.md §S2 P-TASTE`.
- **The COLD-axis invariant certified** — CERTIFIED, recorded with its observed witness: S1 (the
  COLD-axis boundary §) + clause (b) (`proof:cold-entry` reading the isolated engine write channel — the aria-gated wrapper transform + the `--ball-p` advance).
  `K.md §invariant set`, `live-cold-play-path.md §P0-1`, `live-session-gap-analysis.md §1`,
  `deferred-ledger-k.md DL-K1/DL-K2`.
- **The engine-write disambiguation rule certified** — CERTIFIED: S1 + clause (b) (the de-vacuoused
  B1; the oracle reads `interpFrames`/group-composite mutation, NOT `getComputedStyle` churn).
  `K.md §invariant set`, `k-verify-gate-blindspot.mjs`, `live-session-gap-analysis.md §1`.
- **The TASTE boundary certified (the packet verdict ON RECORD)** — CERTIFIED, the user's signature
  on the record: S2 + S5 + clause (g) (the W4-close packets presented, the per-pane verdict recorded
  BEFORE the cut). `K.md §invariant set`, `design-synthesis-k.md §1/§5`,
  `live-spring-sequence-mp-verdict.md`, `precepts-k.md §S2 P-TASTE`.
- **The prompt-recap TOTAL (every MANDATE row + every U-K1..K20 + every A→J PARTIAL + every DL-K row)**
  — S2 + clause (h). `prompt-recap-k.md` (extended through the close), `K.md §MANDATE`,
  `deferred-ledger-k.md §1`.
- **The chronic-closure SUBSTRATE TRANSITION J→K (one motion · non-vacuity on the new substrate)** —
  S3 + clause (e). `proof-chronic-closure.mjs:109` (the current J target), `precepts-k.md §3 T6`
  (P-SUBSTRATE), `K.md §invariant set` (the chronic-closure meta-invariant).
- **The version cut + npm publish (USER-DOMAIN · content-honest)** — S4 + clause (d) (the publish
  gated on `proof:published-surface` GREEN). `packaging-k.md §7.1/§7.2` (the patch/minor decision),
  `ci-cd-k.md §F-1/§F-4` (release.yml backfill), `precepts-k.md §P20`.
- **The close-merge auto-deploy RE-observation** — S6 + clause (c). `J.W0 §Hand-off → J.WZ` (the
  oracle), `ci-cd-k.md §1.1`, `completeness-critic.md §FOLD CC-3` (the deployed-origin verify).
- **The AFTER review-packet capture (the W3-lib harness vs the K-open corpus)** — S5 + clause (g). The
  design-totality visual record; the named-deltas-only diff. `K.md §K.W5`, `audit/screenshots-k/`,
  `design-synthesis-k.md §5`.
- **`L-SEED.md` committed (the frontier re-seeded UNCHANGED · the wholesale deferral on the record)**
  — S7 + clause (h). `L-SEED.md` (head), `k-seed-reconciliation.md §4` (Shape A),
  `K.md §The frontier defers wholesale to L`, `deferred-ledger-k.md §1f` (DL-K26..K32).
- **The K ledger TERMINAL: zero un-dispositioned rows + the ten ≥4-tranche `‡` riders EXITED** — S2 +
  S3 + clause (f). `deferred-ledger-k.md §2` (the P-invariant-28 exit roll-up), `precepts-k.md §P16`.
- **The release.yml-gates-on-correctness decision** — S4 (RECORD + the recorded USER-DOMAIN
  decision). `precepts-k.md §P20`, `ci-cd-k.md §F-4`, `packaging-k.md §6.4`.
- **U-K19 RECORD (playground-only, NOT a deployed-SPA defect)** — S2 (the disposition honored). The
  critic's correction to the K seed. `completeness-critic.md §4/§FOLD CC-1`.
- **The two new K precepts on the record (P6-WITNESS + P-TASTE)** — S1 (recorded in the FINAL as
  permanent, reasoned). `precepts-k.md §S1/§S2/§6`.
- **The build/dist footgun guard (the capture/deploy hazard)** — S5/S6 (rebuild-right-before-serve or
  kill the watch). `live-spring-sequence-mp-verdict.md §0`, `completeness-critic.md §5`, MEMORY.
- **The supersession-coherence pattern (the §G drift to AVOID)** — S1 (the single-voice discipline;
  the K FINAL is the sole authoritative narrative, supersessions explicit). `wave-I.WZ-postclose.md §G`.

## §Hand-off / cross-wave boundaries (BINDING)

- **← K.W0 (the cold-entry oracle, BINDING):** the close RE-observes the auto-deploy round-trip on
  the close merge (S6) over the deterministic deploy chain `proof:cold-entry` joined; the
  `proof:cold-entry` GREEN + the de-vacuoused B1 are the COLD-axis certification clause (b) consumes.
  The close consumes; K.W0 authored the P0 cure + the oracle.
- **← K.W1 (the consume edge + publish-boundary, BINDING):** the close consumes the glass-ui
  `~3.13.0` re-pin + the `proof:deps-current` floor advance + the `release.yml` backfill
  (`proof:published-surface`/`proof:readme-runs`/`timeout-minutes`) — clauses (c)/(d) rest on them;
  the cut tree is the re-pinned, floor-advanced tree. K.W1 authored; the close FIRES the cut+publish.
- **← K.W2 ∥ K.W3 (the design roots, BINDING):** the close's design-band claim (S1) + the AFTER
  packet (S5) rest on the ONE font-voice authority (K.W2) + the derived anchoring grid (K.W3); the
  per-finding runtime asserts (`PATH-FORWARD.md §6`) are the gate-side companions to the packet.
- **← K.W4 (the panes + the TASTE packets, BINDING):** the close presents the W4-close review packets
  to the user and records the verdict (S2/S5/clause (g)) over K.W4's re-cut panes (the red-dashed
  final state, the continuous slider, the no-single-option totality, the removed FourierField). K.W4
  authored the panes + generated the packets; the close DISCHARGES the user-verdict step.
- **← K.W5 (the gate-truth + publish, BINDING):** the close's clause (a) consumes `proof:all == CI`
  (the `proof:ci-coverage` two-way equivalence) + the chronic-closure substrate the transition (S3)
  re-points; clause (d) consumes `proof:published-surface`; the TASTE review-packet generator (S5)
  rides the W3-lib capture harness K.W5 instrumented.
- **← K.W6 (the terminations, BINDING):** the close's clause (f) consumes the K ledger TERMINAL — the
  ten ≥4-tranche `‡` riders K.W6 EXITED (`deferred-ledger-k.md §2`); the mobile-Lighthouse floors
  K.W6 MEASURED on a quiet host (DL-K11); the U-K19 RECORD disposition (CC-1).
- **USER-DOMAIN (Mike Babb, confirm-first — the close FIRES on confirmation):** the TASTE review-packet
  VERDICT (the user drives the W4-close packets, the verdict recorded BEFORE the cut); the `changeset
  version` cut → `4.2.1` (or `4.3.0` if a public export landed); the tag push triggering `release.yml`;
  the npm publish; the close merge to master (which RE-fires the auto-deploy round-trip, S6). The close
  AUTHORS the triggers + the gated pre-conditions; the owner CONFIRMS and FIRES.
- **OUT / sibling (do NOT touch):** the value.js next-slice (VJ-1..9, rides the next re-pin,
  consume-edge-shaped — `deferred-ledger-k.md §1e`); the glass-ui consume-edges that publish after K
  (sibling-HANDOFF rows with their born-RED-able grep, never kf-patched — inv-16); the DEP-1 CNAME
  confirm (deploy-repo-owned, verify-only); the ENTIRE library frontier (CC-1 compiler, K1 ingestion,
  SO-1 scroll, PHYS-C, ED-1, the value.js half — DEFERRED WHOLESALE to L via `L-SEED.md`, S7). All
  inv-16 sibling-coordination + the reasoned wholesale deferral, never kf-patched.

## §Design decisions (trade-offs RESOLVED)

- **inv ε is MECHANICAL at the close — RESOLVED.** The K FINAL's evidence is the boundary battery
  GREEN on the fixed tree (`proof:all == CI` GREEN, `proof:cold-entry` GREEN reading the engine's own
  write channel, the close-merge auto-deploy OBSERVED, `proof:published-surface` GREEN) AND the user's
  recorded packet verdict. The running product at the FIRST gesture + the user's signature on the
  design are the arbiters — NO chain-of-trust over a prior FINAL, NO agent's taste. The close cannot
  overclaim because its oracle is the OBSERVED boundary battery + the user verdict; where a boundary is
  CARRIED (the L frontier; an open consume-edge), the carry's named mechanism is on the record.
- **The two K-born blind axes are CERTIFIED-with-witnesses, not re-asserted — RESOLVED.** The COLD
  axis is certified by `proof:cold-entry` reading the isolated engine write channel (clause (b), S1) —
  the J close's unnamed blind axis made TRUE, the product animating from the first gesture. The TASTE
  boundary is certified by the user's per-pane packet verdict ON RECORD (clause (g), S2/S5) — the J
  close's other unnamed blind axis (agent PASS vs user "awful") now NAMED, packaged, and USER-signed.
  Each is recorded WITH its observed oracle, the opposite of the assert-on-a-proxy / assert-on-agent-
  taste shape K was founded to end.
- **The TASTE verdict is the USER's, BEFORE the cut — RESOLVED.** The design band closes ONLY on the
  user's verdict on the W4-close review packets, a named USER-DOMAIN step scheduled BEFORE the version
  cut, never after. An agent's "designer-eye PASS" is corroboration, never the verdict; the runtime
  appearance asserts corroborate but do not certify design-good. This elevates `precepts-k.md §S2
  P-TASTE` from an episode lesson to the close's binding gate clause (g) — the catastrophe-shape (J
  closed its design band on an agent PASS) cannot recur.
- **The cold-path cure is the ADAPTER seam + the engine-write oracle, NOT a workaround — RESOLVED.**
  The P0 died at `scenePlaybackAdapters.ts` (resume made total) and the B1 vacuity died by engine-write
  DISAMBIGUATION — NOT a longer settle, a raised distinct-count, a demo-side `play()` sprinkle, or an
  oracle that still counts the `.idle-hover` bob (the four named forbiddings). The certification greens
  on the engine's own hand; a green on idle CSS motion is the vacuity the close exists to end (DL-K2,
  the ≥9-tranche meta-chronic, terminated as the root exit).
- **The substrate transition is GATED, not a swap — RESOLVED.** The parse-target re-point (`J/PROGRESS`
  → `K/PROGRESS`) is accepted ONLY after the gate re-run BITES on a planted malformed K-ledger row —
  non-vacuity on the new substrate proven, not assumed. P-SUBSTRATE binds the grooming + the K chronic
  registration to ONE motion. A vacuous swap is the gate-blindspot's deepest expression (a source-shape
  gate auditing a markdown table); the transition proves the K ledger is POLICED by a biting gate.
- **The content-honest cut + publish stay USER-DOMAIN, gated on the boundary GREEN + the verdict —
  RESOLVED.** The default cut is the honest PATCH `4.2.1` (the P0 fix + the demo design totality + the
  demo-only glass-ui re-pin — no library API change); a minor `4.3.0` fires ONLY if a K wave landed a
  genuinely-useful NEW public export (the candidate recorded, the decision made against the landed
  content). The publish fires only on `proof:all == CI` GREEN + `proof:cold-entry` GREEN +
  `proof:published-surface` GREEN + the close-merge round-trip OBSERVED + the TASTE packet verdict ON
  RECORD. The owner (Mike Babb) confirms and fires; the close authors the gated trigger.
- **The close-merge round-trip is RE-observed on the close itself, AND the live cold path verified —
  RESOLVED.** J.W0 proved the auto-chain on A master push; the K close re-witnesses it on the K close
  merge, AND verifies the deployed `keyframes.babb.dev` cold front door ANIMATES on the served bytes
  (the live-origin re-verify `completeness-critic.md §CC-3` flagged was never done). The terminal
  product-truth observation is compound: the certified bytes are the served bytes, and those bytes
  animate from the first gesture — the P0 that decayed daily is dead on the live site.
- **The frontier defers WHOLESALE to L, committed not punted — RESOLVED.** `L-SEED.md` is committed
  with the reasoned wholesale-deferral argument (the value.js grammar resolves in the K interval; none
  of the frontier decays by waiting; the P0 decayed daily; L inherits a stronger substrate because K
  repaired the demo first). The ONLY frontier fold into K is the K3-internal engine-diagnostic rows
  (~20 LoC if VERIFY fails); WL2-B/PHYS-C are NOT manufactured as K work. The 12 KILLs stay
  non-re-litigable; the BOOKs stay tripwired.
- **The close is single-voiced — RESOLVED.** The K FINAL is the sole authoritative narrative;
  supersessions of frozen DEV artifacts are explicit pointers, never silent re-writes that leave two
  live narratives. The `wave-I.WZ-postclose.md §G` two-narrative drift the I tranche was founded to
  forbid does not recur — one tranche, one close voice, every boundary (the COLD axis, the TASTE
  boundary, deploy, publish, docs) telling the same true story, signed by the only oracles that can
  judge them: the running product at the first gesture, and the user.
