# K.W12 — THE EXTERNALIZE (CLOSES Band II · the proof culture made externally consumable: the agent surface, the thin Vue adapter, the dogfood inversion at the PACKAGE boundary, the public color-fidelity harness — every claim signed by a runnable proof or a ΔE number)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** SHIP-in-K (Band II · the CLOSE of
  the frontier band; born-RED in the FRONTIER sense — NONE of the four externalize surfaces exist
  TODAY: `ls llms.txt llms-full.txt` → **No such file** (§State-verified), `ls -d packages/` → **No
  such directory** (no `@mkbabb/keyframes-vue`), `grep -rn "@mkbabb/keyframes" demo/` → **ZERO hits**
  (the demo reaches the engine through deep `@src/animation/*` paths, NOT the published barrel — the
  dogfood inversion is genuinely ABSENT), and `grep -rn "deltaEOK" src/` → **ZERO** (the value.js
  installed-0.11.2 `deltaEOK` producer is present but kf-unconsumed — no public color-fidelity harness
  exists). The capability is absent, not regressed: each gate reds because the surface does not
  exist.) · **Scope (NEW public artifacts + a NEW sibling package + the demo's barrel-import edge +
  ONE new gate; no engine source touched):** ED-1 `/llms.txt` + `/llms-full.txt` + the proof corpus
  as a public artifact + `proof:agent-surface` (the agent index can never drift from the published
  surface) + ED-2 `@mkbabb/keyframes-vue` (a thin NEW adapter — the declarative `<Keyframes css>`
  component + the `useKfAnimation` kernel; the extract-the-demo-composables framing was
  researched-FALSE) + ED-3 the DOGFOOD INVERSION (the demo imports `@mkbabb/keyframes.js`, the
  published barrel, NOT `@src` — the boundary-ORACLE at the PACKAGE boundary, honest ONLY because
  Band I (K.W0–K.W4) repaired the demo) + ED-4 the public color-FIDELITY conformance harness
  (midpoint ΔE vs CSS Color 4, `deltaEOK` RIPE-consumed) + the `proof:agent-surface` and
  `proof:demo-on-published-surface` gates, both born-RED in the frontier sense. ·
- **DAG-deps:** **CLOSES Band II** (`K.md §WAVE MAP`: "K.W12 (externalize) CLOSES the frontier band
  (ED-3's dogfood is honest only on the repaired demo + all prior surfaces published)";
  `waves/README.md`: "W0 → W1 → (W2 ∥ W3) → W4 → W7 → (W8 ∥ W9) → W10 → **W12** → WZ"). **Gated on
  ALL prior frontier surfaces + J.W5 publish (LANDED).** Three binding upstreams:
  (1) **J.W5's publish + `proof:published-surface` (LANDED)** — `@mkbabb/keyframes.js@4.2.0` is on
  npm (`node -p "require('./package.json').version"` → `4.2.0`; release.yml's first run, `K.md`
  lead); `docs/published-surface.md` IS the machine-checked export roster ED-1's llms.txt indexes and
  ED-3's barrel asserts against — ED-1/ED-3 build ON the J.W5 floor, they do NOT re-author it (the
  §Provenance §0 below names what J.W5 already owns, so nothing double-counts). (2) **Band I's
  repaired demo (K.W0–K.W4) — the ED-3 HONESTY PRECONDITION** — ED-3 flips the demo onto the
  published barrel; that is honest ONLY because Band I makes the demo WORK (the cold path PLAYS
  post-K.W0, the panes are re-cut post-K.W4) — a dogfood inversion onto a broken demo would certify a
  broken product against its own package (`K.md §The two bands … ED-3's dogfood inversion is honest
  only because Band I makes the demo WORK`). (3) **All prior Band-II surfaces (K.W7–K.W11) published**
  — ED-1's llms.txt indexes the round-trip recipe (the ingest K.W8, the compile K.W10, the scroll
  K.W9, the composition-honoring K.W7); ED-4's color-fidelity harness shares the ΔE proof discipline
  with K.W10's CC-2 densify (`css-compiler.md §7` — "the only measurement is FIDELITY (the pixel/ΔE
  proof)"). The value.js half is RIPE-CONSUMED (no OPEN gate): `deltaEOK` shipped in **0.11.2** and is
  present in the installed barrel at `:20` (`VALUEJS-N2-ASKS.md §2 row 12`; the §State-verified probe),
  so W12 carries NO acyclic-spine born-RED edge — like W7, it is a frontier wave whose every external
  dependency is already published (and unlike K.W9/K.W10's CC-2, W12 needs NOTHING from value.js's
  pending 0.13.0 cut).

## §Provenance (the frontier lane this wave consumes + the booked roots)

- **`../../J/audit/frontier/ecosystem-distribution.md` (ED-1/ED-2/ED-3/ED-4) — THE decisive frontier
  input (the whole lane this wave consumes).** The lane's central, self-skeptical verdict (§8 the
  synthesis table): the SINGLE genuinely-frontier idea is **ED-1 the agent-consumable surface**
  (K-HEADLINE-CANDIDATE), "the one ecosystem move that ONLY kf's three unique axes + its proof
  culture make credible"; ED-2 the Vue adapter is **K-CANDIDATE (Vue, narrowed) / BOOK (React)** with
  a hard honesty caveat; ED-3 the dogfood inversion is **K-CANDIDATE (rides §2's adapter)** — "the
  on-brand completion of the boundary-ORACLE precept"; ED-4 the color-fidelity harness is the
  **K-CANDIDATE-sliver** (the only benchmark that extends a unique axis and "survives the on-brand +
  un-spinnable tests where the throughput benchmark fails it" — §5.4). The lane's K-tranche shape
  (§8): "a PROOF-AS-PRODUCT distribution tranche whose headline is the agent-consumable surface (§6),
  whose body is the on-brand Vue adapter (§2) + the dogfood inversion (§4) + the color-fidelity
  harness (§5-sliver)."
  - **ED-1 (§6) — the agent surface (the HEADLINE).** §6.2: the on-brand test ("only a
    CSS-source-of-truth, proof-gated engine could do an agent surface THIS way") passes uniquely on
    THREE counts — (1) kf's docs are ALREADY structured for agent consumption (the proof corpus:
    ~120 `proof:*` gates, each a self-describing capability claim — `not 'the library claims springs'
    but 'the library PROVES springs, here is the gate, here is the runnable snippet CI executes'`);
    (2) the round-trippable CSS axis is uniquely agent-friendly (the agent's input format IS the
    library's source format — "every other library requires the agent to TRANSLATE CSS into a bespoke
    tween syntax; kf does not"); (3) it is anti-bloat by construction (a curated INDEX, not
    feature-chasing). §6.3 the concrete artifact: `/llms.txt` (the curated index — the 13 primitives
    from `docs/published-surface.md`, each with a one-line intent + a README anchor + its `proof:*`
    gate name) + `/llms-full.txt` (the round-trip recipe inline + the full export roster with the
    SAME runnable snippets `proof:published-surface` clause (c) already executes — "so the agent
    surface is, by construction, the GATE-VERIFIED surface; it cannot drift") + `proof:agent-surface`
    (the boundary-ORACLE extension — the agent index's linked exports == the `docs/published-surface.md`
    roster AND every cited `proof:*` gate exists in the `proof:all` roster). §6.4 the kill-list check:
    "brushes nothing on the ARCH kill list … a docs artifact + one gate. Clean."
  - **ED-2 (§2) — `@mkbabb/keyframes-vue` (the thin NEW adapter).** §1.3 the decisive finding: "there
    is NO clean set of 'library-adapter composables' sitting in the demo waiting to be lifted" — the
    engine-touching composables (`useRafScene`/`useAnimationSync`/`useAnimationGroupPlayback`) are
    "wired to the demo's OWN `ScenePlayback` contract + `sceneMachine` + `scenePlaybackAdapters`,
    which are demo-app concepts, NOT general animation-library concepts. Extracting them verbatim
    would ship the demo's scene-orchestration model as a 'Vue adapter' — a leaky, over-fit surface."
    §2.2 what CAN be extracted honestly: (1) the generic kernel `useKfAnimation(getAnimation)` →
    `{ t, playing, started, reversed }` via the `useAnimationSync` settle-and-pause idiom (~40 lines,
    "the deadlock-safe 'gate on inputs not outputs' discipline — a genuinely hard-won correctness
    property worth shipping"); (2) the declarative `<Keyframes :css :options v-slot="{ t }">`
    component — "THIS is the only on-brand adapter — the one a Motion/`vueuse-motion` adapter CANNOT
    write, because no other engine parses author CSS `@keyframes` as its source." §2.5 verdict:
    K-CANDIDATE (Vue) / BOOK (React, §2.4 — "no battle-tested code to extract and no dogfood demo to
    invert against; the demo is Vue"); the **hard caveat:** "a second published package doubles the
    release/version/CI surface — it must ride the SAME `proof:published-surface` discipline … or it
    re-opens the exact publish-boundary lie J.W5 closes."
  - **ED-3 (§4) — the dogfood inversion (the boundary-ORACLE at the PACKAGE boundary).** §1.2 the gap:
    "`grep -rn '@mkbabb/keyframes' demo/` → ZERO hits. Every demo import is a deep `@src/animation/*`
    path." §4 the frontier move: "make the demo import the PUBLISHED public surface — the running
    storefront exercises the exact surface a `npm i` consumer reaches … if a public export is missing,
    mis-typed, or wrongly tier-split, the demo fails to build. This is `proof:published-surface` made
    DYNAMIC." §4 the honest tension (why K, not J): "today the demo reaches the heavy engine via
    `@src/animation/engine` (the raw chunk), because reaching it via `loadAnimationEngine()` is
    genuinely more awkward for a hot demo (an `await` at mount). Flipping the demo onto the public
    boundary is a real architectural change to how every scene mounts its engine." §4 the gate:
    `proof:demo-on-published-surface` — "a build/grep oracle asserting the demo's kf imports are the
    published barrel ONLY (zero `@src/animation/*` deep imports outside the `env.d.ts`/test seams) …
    Born-RED today (every demo file imports `@src`)." §4 verdict: K-CANDIDATE (rides §2's adapter) —
    "the single most boundary-ORACLE-faithful idea in this lane after the agent-surface."
  - **ED-4 (§5.4) — the public color-fidelity conformance harness (the K-CANDIDATE-sliver).** §5.4:
    "There IS one benchmark only kf could publish honestly: a CORRECTNESS benchmark, not a throughput
    one … a public, reproducible fidelity harness ('animate `#C462D8 → #E85252`, sample the midpoint,
    compare ΔE against the CSS Color 4 reference') would showcase kf's UNIQUE axis (2) — perceptual
    color — and is un-spinnable (it measures correctness against a spec, not speed against a rival)."
    §5.3 (why the throughput benchmark is REJECTED): "cross-library benchmarks are a credibility
    minefield … kf cannot occupy that neutral seat" — the throughput benchmark is **BOOK**, the
    fidelity harness is the **only** benchmark that extends a unique axis. The value.js half:
    `deltaEOK` is RIPE (`ecosystem-distribution.md §0`; `K-SEED.md §7` KF-DELTAE "gates ED-4 + the
    CC-2 pixel proof").
- **`../L-SEED.md §2 ED-1` — the consumed frontier body (the §body→K.W12 map row).** The body-item map
  (`../L-SEED.md` "§body-item → K wave map"): "**ED-1 / ED-2 / ED-3 / ED-4** — the AGENT-CONSUMABLE
  SURFACE (§2) | llms.txt + proof corpus; the Vue adapter; the dogfood inversion; the color-fidelity
  harness | **K.W12** EXTERNALIZE". The §2 verbatim body (`../L-SEED.md §2`): "**ED-1 — the
  AGENT-CONSUMABLE SURFACE** — llms.txt + the proof corpus as a public artifact + a
  `proof:agent-surface` gate: only a proof-gated library HAS a proof corpus to expose; the 2026 B2A
  convention is the distribution face. Riders: ED-2 `@mkbabb/keyframes-vue` (a thin NEW adapter — the
  'extract the demo composables' framing was researched-FALSE, they are demo-coupled), ED-3 the
  dogfood inversion (the demo consumes the PUBLISHED barrel — the boundary-ORACLE at the package
  boundary), ED-4 the public color-FIDELITY conformance harness (the one benchmark only kf can publish
  honestly: midpoint deltaE vs CSS Color 4, not a throughput pissing match)."
- **The `deltaEOK` consume edge (RIPE since 0.11.2, no acyclic-spine born-RED).** `../VALUEJS-N2-ASKS.md
  §2 row 12`: "The RIPE-NOW edges all stand — `deltaEOK` … KF-DELTAE → ED-4/CC-2 — unchanged from
  L-SEED RIPE-NOW; now one publish fresher." **The precise currency (probe of record, 2026-06-15):**
  `deltaEOK` is RIPE-NOW since **0.11.2** — it is NOT a 0.12.0-only producer (`../L-SEED.md §7` "RIPE
  NOW (kf consume-edges gating on NOTHING — all shipped in 0.11.2) … KF-DELTAE `deltaEOK`"). The
  INSTALLED tree pins `^0.11.2` (`package.json:179`), and the probe
  `grep -rn "deltaEOK" node_modules/@mkbabb/value.js/dist/index.d.ts` →
  `:20 export { … deltaEOK …, gamutMapOKLab, … } from './units/color/gamut'` (the installed **0.11.2**
  barrel — the producer is present TODAY at `:20`, NOT pending). `deltaEOK` therefore consumes
  RIPE regardless of the K.W1 `^0.11.2 → ^0.12.0` re-pin — the re-pin (`K.md §The consume edge`)
  un-blocks OTHER N2 edges, but ED-4's ΔE kernel was already shipped a publish earlier. This is a
  PUBLISHED-CONSUME edge; W12 is the wave that lights it for ED-4 (the SAME `deltaEOK` the K.W10 CC-2
  densify pixel-proof consumes — one producer, two consumers, both RIPE). The value.js-side producer
  doc-lineage (`value.js/docs/tranches/N/GRAMMAR-FOLD.md §I.2`, the now-RATIFIED N.W11.D fold) names
  the SAME kernel: "`deltaEOK` (`gamut.ts:53`) … the spacing oracle AND the kf-side CC-2 ΔE
  ship-vs-refuse proof — the SAME kernel both sides consume; ships 0.12.0." W12's ΔE consume needs
  NOTHING from the pending 0.13.0 cut (unlike K.W9's `CSSTimelineOptions` / K.W10's CC-2
  `sampleColorRamp`, which born-RED-gate on N.W11′/N.W11.D's 0.13.0 publish) — ED-4 is RIPE on the
  installed 0.11.2.
- **The 12 KILLs carried (`../L-SEED.md §5`, this wave's anti-charter):** ED-6 JSR publish is KILLED
  ("provenance already owned via `npm --provenance` in release.yml; JSR transpile-on-publish would
  LOSE the hand-tuned static/dynamic boundary") — W12 does NOT add a JSR target. The BOOK band
  (`../L-SEED.md §6`): ED-2-React (after the Vue adapter proves the shape — `ecosystem-distribution.md
  §2.4`); K5 the embeddable inspector ("a distribution narrative — revisit after K1∘CC-1 exists").
  Neither is in W12 scope.
- **The booked invariant roots:** `K.md §invariant set` — the **boundary-ORACLE precept** (J-born,
  carried — `proof:agent-surface` is the gate-ORACLE at the AGENT boundary, `proof:demo-on-published-surface`
  the boundary-ORACLE at the PACKAGE boundary) + the **acyclic-spine invariant** (W12's value.js half
  is RIPE-consumed — `deltaEOK` shipped in 0.11.2, present in the installed `^0.11.2` barrel at
  `:20`; no OPEN gate, no born-RED edge, no unpublished symbol; the agent surface and the Vue
  adapter are kf-owned artifacts, value.js-grammar-independent) + the **TASTE boundary**
  (W12 carries NO appearance verdict — its surfaces are correctness artifacts; the K.W4 pane re-cut's
  TASTE verdict closes at K.WZ, NOT here). The ED-5 spurious-Vue-peer fold was DISCHARGED at J.W5
  (the §body→K-SEED `§4 J-amendments` row; `grep -n peerDependencies package.json` → NO block today —
  the Vue-free library declares NO Vue peer) — W12 does NOT re-litigate it; the `@mkbabb/keyframes-vue`
  package (ED-2), being a Vue adapter, is the package that DECLARES the Vue peer (§Design-decisions).

## §The state, verified (file:line / grep / registry probe — inv ε)

- **NO agent surface exists (CONFIRMED against the tree, 2026-06-15 — the born-RED root):**
  `ls llms.txt llms-full.txt` → **"No such file or directory"** (both). `grep -n "agent-surface"
  package.json scripts/*.mjs` → **NO hits** (no `proof:agent-surface` script). kf has no `/llms.txt`,
  no `/llms-full.txt`, no agent-surface gate. The capability is genuinely net-new.
- **NO `@mkbabb/keyframes-vue` package exists (CONFIRMED):** `ls -d packages/` → **"No such
  directory"**; `grep -rn "keyframes-vue" package.json README.md` → **NO hits**. There is no sibling
  Vue adapter package, no `<Keyframes>` component, no `useKfAnimation` kernel.
- **The demo does NOT consume the published barrel (CONFIRMED — the ED-3 born-RED root):**
  `grep -rn "@mkbabb/keyframes" demo/` → **ZERO hits**; `grep -rln "@src/animation" demo/` → **60
  files**. Every demo file reaches the engine through `@src/animation/*` deep paths, INCLUDING the
  heavy tier via the raw chunk: `demo/easing/useEasingDemo.ts:12`, `demo/sequence/useSequenceDemo.ts:4`,
  `demo/square/useSquareAnimations.ts:1`, `demo/cube/CubeTarget.vue:108` all
  `import { CSSKeyframesAnimation } from "@src/animation/engine"` — the deep chunk path that is NOT a
  public export (the public surface is `loadAnimationEngine()`, NOT the `engine.ts` chunk —
  `index.ts:20` "`loadAnimationEngine()` — an `await import('./engine')`"). The demo could not even be
  written against the published barrel TODAY without changing how it reaches the heavy tier — exactly
  the ED-3 honest tension (`ecosystem-distribution.md §1.2/§4`).
- **The vite self-alias is the reason the demo CAN write the barrel-specifier in dev (CONFIRMED — and
  it is a DOCUMENTED contract-v2 supersession, NOT a fossil to attack):** `vite.config.ts:152-156` —
  `"@src": …/src`, `"@mkbabb/keyframes.js": path.resolve(…, "src/animation/index.ts")`. The in-repo
  header (`:140-151`) names WHY the self-alias exists and WHY it is precept-legal: "with glass-ui
  consumed from the registry (not a workspace link), glass-ui's bare `import … from
  '@mkbabb/keyframes.js'` … has no installed package to resolve against — rolldown would stub it as an
  empty optional-peer-dep and drop the export. The self-alias dedupes glass-ui onto the SAME keyframes
  instance the demo uses via `@src` … **(Supersedes contract-v2's no-self-alias rule, which presupposed
  the `file:` symlink walk that the registry pin removed.)**" So the
  `cross-repo-dev-resolution.md §2.4` no-self-alias prohibition is ALREADY superseded in-repo for the
  registry-pin reason (gated quiet by `proof:resolution`, glass-ui-owned) — ED-3 does NOT touch the
  alias and does NOT re-litigate the precept. ED-3's change is solely that the DEMO'S OWN kf imports
  flip from `@src/animation/*` to the bare `@mkbabb/keyframes.js` specifier (which the existing alias
  resolves to source in dev, and which `npm i` resolves to the tarball in a real consumer) — a real
  architectural change to how every scene WRITES its engine import, NOT a doc edit and NOT an alias
  edit. The alias is the dev-RESOLUTION substrate; the inversion is the demo WRITING the public
  SPECIFIER over it.
- **`deltaEOK` is PUBLISHED-and-INSTALLED-and-RIPE but kf-UNCONSUMED (CONFIRMED 2026-06-15 — the ED-4
  consume edge, and the ED-4 born-RED root):**
  `grep -rn "deltaEOK" node_modules/@mkbabb/value.js/dist/index.d.ts` →
  `:20 export { DELTA_E_OK_JND, deltaEOK, oklabToLinearSRGB, …, gamutMapOKLab, … } from './units/color/gamut'`
  — the producer is present at `:20` of the **installed 0.11.2** barrel
  (`node -p "require('./node_modules/@mkbabb/value.js/package.json').version"` → **`0.11.2`**; the pin
  is `^0.11.2`, `package.json:179`). `grep -rn "deltaEOK" src/` → **ZERO** — kf does not yet consume
  it (the ED-4 born-RED: the producer ships, kf's harness does not exist). **The currency, precisely:**
  `deltaEOK` is RIPE-NOW since **0.11.2** (`../L-SEED.md §7` "RIPE NOW … all shipped in 0.11.2 … KF-DELTAE
  `deltaEOK`"), NOT a 0.12.0-only symbol — so the consume is NOT gated on the K.W1 `^0.11.2 → ^0.12.0`
  re-pin (the re-pin un-blocks the OTHER N2 edges, `K.md §The consume edge`; ED-4's ΔE kernel was already
  installed). ED-4's harness lights THIS edge (`VALUEJS-N2-ASKS.md §2 row 12`) on the installed barrel.
- **J.W5's publish + manifest + `proof:published-surface` LANDED (CONFIRMED — the ED-1/ED-3 floor):**
  `node -p "require('./package.json').version"` → **`4.2.0`** (published on npm, release.yml's first
  run); `ls docs/published-surface.md` → **present** (the authoritative export roster ED-1 indexes
  and ED-3 asserts against); `grep -n "proof:published-surface\|proof:dogfood" package.json` →
  `:44 proof:published-surface`, `:48 proof:dogfood` (both wired into the `proof:hygiene` aggregate at
  `package.json:160`, which `proof:all` (`:161`) chains via `proof:correctness && proof:hygiene`). The
  three-way published-surface oracle (tarball==`files`, every export ∈ README∪manifest, AnimationEngine
  interface ≡ runtime) is the STATIC floor ED-1 indexes and ED-3 makes DYNAMIC (`ecosystem-distribution.md
  §0/§4`). **W12 builds ON this floor; it does NOT re-author it** (inv ε — no manufactured work).
- **`proof:dogfood` (inv ζ) proves the demo runs on the engine's BEHAVIOR, not the PACKAGE boundary
  (CONFIRMED):** `scripts/proof-dogfood.mjs:1-20` — "the shop-window runs on its own engine … the demo
  carries NO hand-rolled rAF a light surface replaces." It proves the demo consumes
  `RAFPlayback`/`SmoothProgress`/`SpringProgress`/`NumericAnimation`/`decay` (the BEHAVIOR, via `@src`)
  — it does NOT prove the demo consumes the published PACKAGE. ED-3 extends inv ζ from the SOURCE
  boundary to the PACKAGE boundary (the new `proof:demo-on-published-surface`), the on-brand completion
  of the boundary-ORACLE precept (`ecosystem-distribution.md §4` verdict).
- **The 12 researched KILLs hold; ED-6 JSR is KILLED (CONFIRMED — no JSR target in the tree):**
  `grep -rn "jsr\|jsr.json" package.json` → no JSR publish config; W12 does not add one
  (`../L-SEED.md §5` ED-6: "provenance already owned … JSR transpile-on-publish would LOSE the
  hand-tuned static/dynamic boundary").

## §Goal

Make the proof culture EXTERNALLY CONSUMABLE — by agents, by frameworks, by kf's own dogfood, and by
the field — every claim signed by a runnable proof or a ΔE number: **kf exposes an agent-legible
surface where every capability is backed by a CI-verified gate (the agent index can never lie); ships
a thin, on-brand Vue adapter whose headline primitive is the declarative CSS-`@keyframes` component no
competitor can write; flips its own demo onto the PUBLISHED barrel so the storefront is the dynamic
integration test of the surface a `npm i` consumer reaches (honest ONLY because Band I made the demo
WORK); and publishes the ONE benchmark only kf can publish honestly — midpoint ΔE vs CSS Color 4, a
correctness conformance harness, not a throughput pissing match.** This is the CLOSE of Band II: the
round-trip made TOTAL (W7 honored, W8 ingested, W9 scrolled, W10 compiled, W11 sprung) is now
EXPOSED to its consumers — the proof corpus, the axes, the published surface, all made consumable by
humans, frameworks, and agents alike. Four moves, each at the gestalt altitude the mandate demands:

1. **ED-1 — the agent surface (S1 — the HEADLINE).** `/llms.txt` (the curated INDEX) + `/llms-full.txt`
   (the round-trip recipe inline + the runnable export roster) are GENERATED from the J.W5
   `docs/published-surface.md` roster + the `proof:*` gate inventory + the SAME README snippets
   `proof:published-surface` clause (c) executes — so the agent surface IS, by construction, the
   gate-verified surface; it cannot drift. `proof:agent-surface` asserts the index's linked exports ==
   the published-surface roster AND every cited gate ∈ `proof:all`. The agent index is a curated INDEX
   that ships kf's EXISTING correct surface to a new consumer — anti-bloat by construction, NOT a
   feature add.
2. **ED-2 — the thin Vue adapter (S2).** `@mkbabb/keyframes-vue` — a NEW sibling package whose
   headline primitive is the declarative `<Keyframes :css :options v-slot="{ t }">` component (the
   on-brand axis-1 carrier — only a CSS-source-of-truth engine ships it) + the `useKfAnimation`
   kernel (the ~40-line generic settle-and-pause idiom, the hard-won "gate on inputs not outputs"
   discipline). NOT the extract-the-demo-composables framing (researched-FALSE — the composables are
   demo-`ScenePlayback`-coupled). The package rides the SAME `proof:published-surface` discipline (its
   own tarball==declaration, its own runnable snippets) — or it re-opens the publish-boundary lie J.W5
   closes.
3. **ED-3 — the dogfood inversion (S3 — the boundary-ORACLE at the PACKAGE boundary).** The demo's own
   kf imports flip from `@src/animation/*` deep paths to `@mkbabb/keyframes.js` (the published barrel)
   — the light tier via the static named exports, the heavy tier via `loadAnimationEngine()` (the
   PUBLIC boundary, NOT the raw `engine.ts` chunk). `proof:demo-on-published-surface` asserts the
   demo's kf imports are the barrel ONLY (zero `@src/animation/*` deep imports outside the
   `env.d.ts`/test seams) AND the demo still mounts every scene's engine. **Honest ONLY because Band I
   repaired the demo** — the inversion certifies a WORKING demo against its own package (the cold path
   plays post-K.W0, the panes re-cut post-K.W4).
4. **ED-4 — the public color-fidelity harness (S4).** A reproducible conformance harness: animate
   `#C462D8 → #E85252` (and a corpus of pairs), sample the midpoint, compare ΔE against the CSS Color
   4 reference using value.js's `deltaEOK` (RIPE-consumed since 0.11.2, installed barrel `:20`) —
   PUBLISH the fidelity numbers.
   The ONE benchmark that extends a unique axis (axis-2 perceptual color) and is un-spinnable (it
   measures correctness against a spec, not speed against a rival). The throughput benchmark is BOOK
   (the credibility trap — `../L-SEED.md §5`/`ecosystem-distribution.md §5.3`); W12 ships the fidelity
   harness, NOT the throughput one.

## §Scope

- **S1 — ED-1 the agent surface (NEW public artifacts + ONE gate; NO engine source touched).** Locus:
  NEW `/llms.txt` + `/llms-full.txt` (repo root, served as public artifacts) + a NEW
  `scripts/proof-agent-surface.mjs` wired to `npm run proof:agent-surface` + chained into the
  `proof:hygiene` aggregate (`package.json:160`) that `proof:all` (`:161`) runs (the same wiring the
  existing `proof:published-surface`/`proof:dogfood` gates carry — `:160`). The artifacts are GENERATED
  (not hand-maintained drift surfaces):
  `/llms.txt` lists the 13 primitives from `docs/published-surface.md`, each with a one-line intent +
  its README anchor + its `proof:*` gate name; `/llms-full.txt` inlines the round-trip recipe (the
  parse→animate→serialize canonical recipe — kf's axis-1, the thing only kf does) + the full export
  roster with the SAME runnable snippets `proof:published-surface` clause (c) already executes.
  **WHY by-construction-gate-verified:** the snippets are the ones CI runs (`J.W5.md:257`
  "the docs oracle is runnable truth, not prose") — so the agent surface cannot drift from the
  published surface (`ecosystem-distribution.md §6.3`). `proof:agent-surface` asserts: (a) the
  llms.txt linked exports == the `docs/published-surface.md` roster (the agent index can never advertise
  an export the surface does not publish, nor omit one it does); (b) every `proof:*` gate cited in the
  index exists in the `proof:all` roster (the agent reads "the library PROVES springs, here is the
  gate" — and that gate is real). **WHY HEADLINE:** only a proof-gated library HAS a proof corpus to
  expose; only a CSS-source-of-truth engine can hand an agent the round-trip recipe as the canonical
  shape (`ecosystem-distribution.md §6.2`). **NO-WORKAROUND:** the artifacts are GENERATED from the
  J.W5 manifest + the gate roster, NOT a hand-maintained markdown that drifts (a hand-maintained index
  is the doc-rot the gate-ORACLE precept forbids — the index MUST be machine-checked against the
  published surface); NOT a feature add (a curated index of the EXISTING surface, KISS).
- **S2 — ED-2 the thin Vue adapter (a NEW sibling PACKAGE; rides the published-surface discipline).**
  Locus: a NEW `@mkbabb/keyframes-vue` package (a sibling, NOT in the core library tarball — the core
  library stays framework-agnostic). The surface: (a) `<Keyframes :css="…" :options="…" v-slot="{ t }">`
  — the declarative component that takes a CSS `@keyframes` string (kf's UNIQUE axis) and animates the
  host element, exposing the scalar `t` via the slot; (b) `useKfAnimation(getAnimation)` →
  `{ t, playing, started, reversed }` reactive refs via the `useAnimationSync` settle-and-pause idiom
  (the deadlock-safe "gate on inputs not outputs" discipline — the ONE genuinely hard-won correctness
  property worth shipping). The package consumes `@mkbabb/keyframes.js` (the published barrel — the
  light tier statically, `loadAnimationEngine()` for the heavy `CSSKeyframesAnimation`), declares the
  Vue peer (the adapter package is where the Vue peer BELONGS — the core library never), and rides the
  SAME `proof:published-surface` discipline (its own tarball==`files` declaration + its own runnable
  snippets). **WHY thin + on-brand, NOT the demo composables:** the demo composables are
  `ScenePlayback`/`sceneMachine`-coupled — extracting them ships the demo's scene-orchestration model
  as a leaky Vue adapter (`ecosystem-distribution.md §1.3` "the extract-the-composables framing is
  researched-FALSE"). The honest extraction is the ~40-line kernel + the on-brand `<Keyframes>`
  component (the one a Motion/`vueuse-motion` adapter CANNOT write — no other engine parses author CSS
  `@keyframes` as its source). **WHY a SIBLING package, not in-core:** the core library imports zero
  framework code (`src/env.d.ts:4,17` the only Vue reference is a dev-only `.vue` SFC shim) — the
  adapter is the package that owns the framework coupling AND the Vue peer. **NO-WORKAROUND:** NOT an
  extraction of the demo's `ScenePlayback`-coupled composables (researched-FALSE — a leaky over-fit
  surface); NOT a tween-prop me-too of Motion's component (the headline IS the declarative
  CSS-`@keyframes` component — the on-brand carrier); NOT a second published package that escapes the
  published-surface discipline (it rides the SAME `proof:published-surface` clauses, or it re-opens
  the J.W5 publish-boundary lie); NOT a React adapter (BOOK — no React demo to dogfood-invert against,
  `ecosystem-distribution.md §2.4`).
- **S3 — ED-3 the dogfood inversion (the demo's barrel-import edge; the boundary-ORACLE at the PACKAGE
  boundary; HONEST ONLY on Band I's repaired demo).** Locus: the demo's kf import sites (the 60
  `@src/animation/*` deep-import files, ALL flipped to `@mkbabb/keyframes.js`) + the heavy-tier mount
  path (the `import { CSSKeyframesAnimation } from "@src/animation/engine"` sites — `demo/easing/…:12`,
  `demo/sequence/…:4`, `demo/square/…:1`, `demo/cube/CubeTarget.vue:108` — flipped to the public
  `loadAnimationEngine()` boundary, an `await` at mount) + a NEW
  `scripts/proof-demo-on-published-surface.mjs` wired to `npm run proof:demo-on-published-surface` +
  chained into the `proof:hygiene` aggregate (`package.json:160`) that `proof:all` (`:161`) runs. The
  vite self-alias (`vite.config.ts:152-156`) already resolves
  `@mkbabb/keyframes.js` to source in dev (a DOCUMENTED contract-v2 supersession for the registry-pin
  reason — `:140-151` header; `cross-repo-dev-resolution.md §2.4` no-self-alias is already superseded
  in-repo, gated quiet by glass-ui's `proof:resolution`), so the dev HMR loop is preserved UNTOUCHED;
  the change is that the DEMO now WRITES the public specifier (not `@src`), so the running storefront
  exercises the exact surface a `npm i` consumer reaches — if a public export is missing, mis-typed, or
  wrongly tier-split, the demo fails to build. `proof:demo-on-published-surface` is a build/grep oracle: (a) the demo's kf
  imports are `@mkbabb/keyframes.js` ONLY (zero `@src/animation/*` deep imports outside the
  `env.d.ts`/test seams); (b) the demo still mounts every scene's engine (the runtime leg — the
  inversion did not break the heavy-tier reach). **WHY this is the boundary-ORACLE COMPLETION:**
  `proof:dogfood` (inv ζ) proves the demo runs on the engine's BEHAVIOR (via `@src`); ED-3 extends it
  to "what `npm i` installs is what the demo runs" — `proof:published-surface` made DYNAMIC
  (`ecosystem-distribution.md §4`). **WHY HONEST ONLY on Band I's repaired demo (the BINDING
  precondition):** flipping a BROKEN demo onto the published barrel would certify a broken product
  against its own package — the inversion is honest ONLY because Band I (K.W0–K.W4) makes the demo
  WORK (the cold path plays post-K.W0, the panes re-cut post-K.W4). The §Hand-off names this dependency
  on K.W0–K.W4 as BINDING. **WHY a real architectural change, not a doc edit:** reaching the heavy
  engine via `loadAnimationEngine()` (an `await` at mount) instead of the raw `@src/animation/engine`
  chunk is a real change to how every scene mounts its engine — the natural co-requisite of S2 (if
  `<Keyframes>` ships and the demo consumes it, the demo reaches the engine THROUGH the adapter THROUGH
  the public surface, and the inversion completes — `ecosystem-distribution.md §4`). **NO-WORKAROUND:**
  NOT a one-line vite-alias swap that keeps the demo writing `@src` (the demo must WRITE the public
  specifier — the alias is a dev-resolution convenience, not the inversion); NOT a partial flip that
  leaves the heavy tier on the raw `engine.ts` chunk (the heavy tier flips to `loadAnimationEngine()`
  — the PUBLIC boundary — or the dogfood is still source-boundary, not package-boundary); NOT flipping
  a still-broken demo (the inversion gates on Band I — a broken demo on the published barrel is a lie).
- **S4 — ED-4 the public color-fidelity harness (the `deltaEOK` consume; NO throughput benchmark).**
  Locus: a NEW public conformance harness (a reproducible script + a published results artifact —
  `proof:roundtrip-fidelity`/`bench/` lineage — `proof:roundtrip-fidelity` exists today,
  `package.json:74`; it consumes value.js's `deltaEOK`, RIPE since 0.11.2, installed barrel `:20`).
  The harness:
  animate a corpus of color pairs (e.g. `#C462D8 → #E85252`), sample the midpoint of kf's oklab lerp,
  compare ΔE against the CSS Color 4 reference midpoint using `deltaEOK` (the RIPE-consumed producer);
  PUBLISH the ΔE numbers (the conformance result — un-spinnable, a spec-measured correctness fact).
  **WHY the ONLY honest benchmark:** it measures kf's UNIQUE axis-2 (perceptual color) against a SPEC
  (CSS Color 4), not against a rival's throughput — "un-spinnable: it measures correctness against a
  spec, not speed against a rival" (`ecosystem-distribution.md §5.4`). GSAP animates oklch incorrectly
  (its own forums); Motion/anime mix in RGB — the fidelity harness showcases the axis the throughput
  benchmark cannot surface. **WHY it SHARES K.W10's ΔE discipline:** the same `deltaEOK` producer that
  K.W10's CC-2 densify pixel-proof consumes (`css-compiler.md §7` "the only measurement is FIDELITY
  (the pixel/ΔE proof)") — one producer, two consumers, both RIPE; the harness is the PUBLIC face of
  the same fidelity proof the compiler gates on internally. **NO-WORKAROUND:** NOT a throughput
  benchmark (kf vs GSAP/Motion — the credibility trap, BOOK, `../L-SEED.md §5`/`ecosystem-distribution.md
  §5.3`: "kf cannot occupy the neutral seat"); NOT a re-authored ΔE kernel (the `deltaEOK` producer is
  consumed PUBLISHED — a kf-local re-author breaches inv-16 published-only consumption and is the
  legacy the mandate forbids); the harness measures FIDELITY (correctness), never speed.

## §Hard gate (the proof:* that BITES — born-RED in the FRONTIER sense · the boundary-ORACLE at the agent + package boundaries)

**The oracle (per the boundary-ORACLE precept + the gate-ORACLE precept):** `proof:agent-surface`
asserts the agent index can never drift from the published surface (the index's linked exports == the
`docs/published-surface.md` roster AND every cited gate ∈ `proof:all`); `proof:demo-on-published-surface`
asserts the demo consumes the PUBLISHED barrel, not `@src` (the dynamic published-surface oracle).
Both are born-RED in the FRONTIER sense: NO llms.txt exists today (the surface is ABSENT), and every
demo file imports `@src` today (the inversion is ABSENT) — the gates red because the capability does
not exist, not because a regression was planted.

- **clause (a) — the agent index NEVER drifts from the published surface (`proof:agent-surface`;
  CORRECTNESS).** `/llms.txt`'s linked exports == the `docs/published-surface.md` roster (every
  published export is indexed; no indexed entry is a non-export) AND every `proof:*` gate the index
  cites exists in the `proof:all` roster. **BORN-RED WITNESS:** `ls llms.txt` → "No such file" (the
  §State-verified probe) → the gate cannot even read the index → it REDS by construction (the surface
  is ABSENT). **BITE:** reds until S1 generates the index from the manifest + the gate roster; greens
  when the index == the roster AND every cited gate is real; reds AGAIN if a future export is added to
  the surface but not the index (drift — the gate-ORACLE precept at the agent boundary). **NO escape:**
  the assert is set-equality against the J.W5 manifest + the `proof:all` roster — an index that lies
  (advertises an unpublished export, cites a non-existent gate) reds.
- **clause (b) — the demo consumes the PUBLISHED barrel, not `@src` (`proof:demo-on-published-surface`;
  CORRECTNESS · the boundary-ORACLE at the PACKAGE boundary).** The demo's kf imports are
  `@mkbabb/keyframes.js` ONLY — zero `@src/animation/*` deep imports outside the `env.d.ts`/test seams
  — AND the demo still mounts every scene's engine (the runtime leg). **BORN-RED WITNESS:**
  `grep -rln "@src/animation" demo/` → **60 files** today (the §State-verified probe), including the
  heavy-tier reach `import { CSSKeyframesAnimation } from "@src/animation/engine"` (a NON-public deep
  chunk) → the gate reds (the demo writes `@src`, not the barrel; the heavy tier reaches the raw chunk,
  not `loadAnimationEngine()`). **BITE:** reds until S3 flips every demo kf import to the barrel + the
  heavy tier to `loadAnimationEngine()`; greens when zero `@src/animation/*` deep imports survive AND
  every scene still mounts. **NO escape:** the assert is the grep (zero deep imports) + the runtime
  mount — a partial flip (heavy tier still on the raw chunk) reds; a still-`@src` demo reds.
- **clause (c) — the dogfood inversion is HONEST (the demo WORKS on the published barrel; CORRECTNESS
  · the Band-I precondition).** The inverted demo (S3) still PASSES `proof:cold-entry` (K.W0 — the
  cold path plays on the published barrel) and `proof:dogfood` (inv ζ — the shop-window runs on its
  own engine, now via the PACKAGE) and mounts every scene. **WHY:** the inversion is honest ONLY if
  the demo it certifies WORKS — a broken demo on the published barrel certifies a broken product
  against its own package. **BITE:** reds if the barrel-flipped demo fails the cold-entry/dogfood
  gates (the inversion certified a broken demo) — it gates on Band I (K.W0–K.W4) having repaired the
  demo. **NO escape:** the assert is the K.W0 cold-entry oracle + inv ζ run on the BARREL-imported
  demo — a regression-on-inversion reds.
- **clause (d) — the color-fidelity harness publishes the ΔE conformance (CORRECTNESS · the
  un-spinnable benchmark).** The harness animates the color corpus, samples the midpoint, and the
  midpoint ΔE vs the CSS Color 4 reference (via `deltaEOK`) is UNDER the conformance threshold AND the
  numbers are PUBLISHED (a public artifact). **BORN-RED WITNESS:** `grep -rn "deltaEOK" src/` →
  **ZERO** today (the producer is published but unconsumed) → no harness exists → the gate REDS by
  construction. **BITE:** reds until S4 builds the harness consuming `deltaEOK`; greens when the
  midpoint ΔE is under the conformance threshold (kf's oklab lerp matches the CSS Color 4 reference)
  AND the numbers are published. **NO escape:** the assert is the ΔE vs the SPEC reference, a
  spec-measured correctness fact — a lossy color lerp (RGB-mixed midpoint, wrong perceptual path) reds.
- **clause (e) — the `@mkbabb/keyframes-vue` package rides the published-surface discipline (HYGIENE —
  labeled, the J.W5 caveat).** The Vue adapter package (S2) carries its OWN `proof:published-surface`
  clauses (its tarball == its `files` declaration; its README snippets execute) AND declares the Vue
  peer (the adapter is where the Vue peer belongs — the core library never). *(Labeled HYGIENE — it
  guards the "second package doubles the publish surface" caveat `ecosystem-distribution.md §2.5`
  flags; the wave's GREEN depends on the boundary-ORACLE clauses (a)-(d). The adapter's existence is
  a K-CANDIDATE, narrowed; its CORRECTNESS is the published-surface discipline, its TASTE the user's.)*

**The §spine bar — MUST bite.** Clauses (a)-(d) are the externalize boundary-ORACLE oracle: the gate
asserts the agent index never drifts from the J.W5 published surface (a), the demo consumes the
PUBLISHED barrel not `@src` (b), the dogfood inversion is honest on Band I's repaired demo (c), and
the color-fidelity harness publishes the un-spinnable ΔE conformance (d). Revert S1 → (a) red (no
index to check). Revert S3 → (b) red (the demo writes `@src`). Revert Band I's repair (hypothetically)
→ (c) red (the inversion certifies a broken demo). Revert S4 → (d) red (no harness). **The born-RED
witness is CONCRETE:** today `ls llms.txt` → "No such file", `ls -d packages/` → "No such directory",
`grep -rn "@mkbabb/keyframes" demo/` → ZERO (60 files import `@src`), `grep -rn "deltaEOK" src/` →
ZERO — every surface is ABSENT, the gates red on exactly that observed shape. **Two-tier taxonomy:**
the wave's GREEN depends on the boundary-ORACLE clauses (a)-(d); clause (e) is a HYGIENE corroborator
(the adapter's publish discipline) — it may NEVER substitute for a red boundary-ORACLE clause; the
Vue adapter's developer-ergonomics/API TASTE is USER-DOMAIN (it does not gate green). **Replay-equality
posture (declared):** W12 carries NO replay-pixel-equality oracle of its own — it is the EXTERNALIZE,
not a round-trip widening; its faithfulness is the BOUNDARY-ORACLE (the agent index == the published
surface, the demo == the package). But it CLOSES the band on the prior surfaces' replay-equality:
ED-1's llms.txt indexes the round-trip recipe ONLY because W8 ingests + W10 compiles replay-pixel-equal
(the recipe is real, gate-verified), and ED-4's ΔE harness shares K.W10's CC-2 fidelity discipline
(`css-compiler.md §7`). **P6 posture (declared):** clauses (a)/(b)/(d) are device-INDEPENDENT
(set-equality of the index vs the manifest; the grep of demo imports; the ΔE-vs-spec computed fact) →
they hard-gate on the Linux runner; clause (c)'s cold-entry leg reuses K.W0's per-EXPECTED predicate
on the headed chrome-devtools-mcp tier (the aria flip + the slider advance, NOT a fixed settle).
**Budget 0** (the gate asserts POSITIVE product properties — the index exists and matches, the demo
consumes the barrel, the harness publishes the conformance — not an error count; the pre-cure tree
threw NOTHING, the surfaces were simply ABSENT). **value.js gate status:** RIPE-CONSUMED — `deltaEOK`
shipped in **0.11.2** and is present in the installed `^0.11.2` barrel at `:20` (the §State-verified
probe); W12 carries NO acyclic-spine born-RED edge (like W7, a frontier wave whose every external
dependency is already published — and unlike K.W9/K.W10's CC-2, which born-RED-gate on the OPEN
N.W11′/N.W11.D 0.13.0 cut, W12 needs NOTHING from 0.13.0). The K.W1 `^0.11.2 → ^0.12.0` re-pin
un-blocks the OTHER N2 consumes; ED-4's ΔE kernel was installed a publish earlier and consumes RIPE
either way.

## §No-workaround prohibitions (BINDING — the mandate's named forbiddings for this wave)

- **NO hand-maintained agent index that drifts (S1).** `/llms.txt`/`/llms-full.txt` are GENERATED from
  the J.W5 `docs/published-surface.md` manifest + the `proof:*` gate roster + the README snippets CI
  executes — `proof:agent-surface` asserts the index == the published surface. A hand-maintained
  markdown that drifts is the doc-rot the gate-ORACLE precept exists to kill (the index that lies about
  the surface is the publish-boundary lie at the AGENT boundary). The agent surface is, by construction,
  the gate-verified surface.
- **NO extraction of the demo's `ScenePlayback`-coupled composables (S2).** `@mkbabb/keyframes-vue` is
  the thin NEW adapter (the `<Keyframes>` component + the ~40-line `useKfAnimation` kernel), NOT a lift
  of `useRafScene`/`useAnimationSync`/`useAnimationGroupPlayback` (demo-`sceneMachine`-coupled — the
  extract-the-composables framing is researched-FALSE, `ecosystem-distribution.md §1.3`). Shipping the
  demo's scene-orchestration model as a "Vue adapter" is a leaky over-fit surface — forbidden.
- **NO Vue peer on the CORE library (S2).** The core `@mkbabb/keyframes.js` library imports zero
  framework code (`src/env.d.ts:4,17` the only Vue ref is a dev-only `.vue` SFC shim; the spurious Vue
  peer was already DELETED at J.W5 — `grep -n peerDependencies package.json` → no block). The Vue peer
  belongs on the `@mkbabb/keyframes-vue` ADAPTER package — the core library never re-advertises it.
- **NO partial dogfood inversion that leaves the heavy tier on the raw chunk (S3).** The demo's heavy
  tier flips from `import … from "@src/animation/engine"` (the raw, NON-public chunk) to the PUBLIC
  `loadAnimationEngine()` boundary — a partial flip that keeps the heavy tier on the raw chunk is still
  the SOURCE boundary, not the PACKAGE boundary (the inversion is incomplete). The demo must WRITE the
  public specifier; the vite self-alias (`vite.config.ts:152-156`, a documented contract-v2
  supersession) is the dev-RESOLUTION substrate that resolves that specifier to source in dev, NOT the
  inversion itself — the inversion does not edit the alias.
- **NO inversion onto a still-broken demo (S3 — the Band-I precondition).** The dogfood inversion gates
  on Band I (K.W0–K.W4) having repaired the demo — flipping a broken demo onto the published barrel
  certifies a broken product against its own package (a lie). Clause (c) asserts the cold-entry oracle
  + inv ζ on the barrel-imported demo; the inversion is honest ONLY on the repaired substrate.
- **NO throughput benchmark (S4).** ED-4 ships the color-FIDELITY conformance harness (midpoint ΔE vs
  CSS Color 4 — un-spinnable, spec-measured), NOT a throughput benchmark (kf vs GSAP/Motion — the
  credibility trap; "kf cannot occupy the neutral seat", `ecosystem-distribution.md §5.3`; the
  throughput benchmark is BOOK, `../L-SEED.md §5`). The harness measures CORRECTNESS against a spec,
  never speed against a rival.
- **NO JSR publish target (the ED-6 KILL).** W12 does NOT add a JSR target — provenance is already
  owned (`npm --provenance` in release.yml, J.W5/WZ); JSR transpile-on-publish would LOSE the
  hand-tuned static/dynamic boundary (`../L-SEED.md §5` ED-6, a researched KILL, non-re-litigable).
- **NO re-authored `deltaEOK` kernel (S4).** The color-fidelity harness CONSUMES value.js's PUBLISHED
  `deltaEOK` producer (RIPE since 0.11.2, installed barrel `:20`; `VALUEJS-N2-ASKS.md §2 row 12`; the
  same kernel the now-RATIFIED N.W11.D fold names at `GRAMMAR-FOLD.md §I.2`/`gamut.ts:53`), NOT a
  kf-local ΔE re-implementation (a
  re-author breaches inv-16 published-only consumption and is the legacy the mandate forbids). The
  acyclic-spine here is RIPE — value.js ships the ΔE math; kf consumes it.

## §Folds (every K.md-assigned fold, with its frontier-lane + L-SEED/N2 citation)

- **ED-1 (the agent surface)** — S1 (`/llms.txt` + `/llms-full.txt` + `proof:agent-surface`).
  `../L-SEED.md §2 ED-1` + the §body→K.W12 map; `ecosystem-distribution.md §6` (the HEADLINE,
  K-HEADLINE-CANDIDATE); `docs/published-surface.md` (the J.W5 manifest the index is generated from,
  `J.W5.md:191-237`); `scripts/proof-*` (the `proof:all` roster the index cites). **The proof culture
  made agent-legible.**
- **ED-2 (the thin Vue adapter)** — S2 (`@mkbabb/keyframes-vue` — the `<Keyframes>` component + the
  `useKfAnimation` kernel). `../L-SEED.md §2 ED-2` (the thin NEW adapter; the extract-the-composables
  framing researched-FALSE); `ecosystem-distribution.md §2` (K-CANDIDATE Vue / BOOK React); the demo's
  `useAnimationSync` settle-and-pause idiom (the kernel's hard-won discipline). React is BOOK
  (`ecosystem-distribution.md §2.4`); ED-2-React rides AFTER the Vue adapter proves the shape
  (`../L-SEED.md §6`).
- **ED-3 (the dogfood inversion)** — S3 (the demo's barrel-import edge + `proof:demo-on-published-surface`).
  `../L-SEED.md §2 ED-3` (the boundary-ORACLE at the package boundary, honest ONLY on Band I's repaired
  demo); `ecosystem-distribution.md §1.2/§4` (the gap + the frontier move + the K-not-J tension);
  `vite.config.ts:152-156` (the self-alias — the dev-RESOLUTION substrate, a documented contract-v2
  supersession the inversion does NOT touch; the demo flips its OWN written specifier, not the alias);
  the 60 `@src/animation/*` demo files (born-RED). **Gates on Band I (K.W0–K.W4) — the §Hand-off names
  the dependency.**
- **ED-4 (the public color-fidelity harness)** — S4 (the midpoint-ΔE conformance harness consuming
  `deltaEOK`). `../L-SEED.md §2 ED-4` (the one benchmark kf can publish honestly); `ecosystem-distribution.md
  §5.4` (the K-CANDIDATE-sliver — un-spinnable, axis-2); `VALUEJS-N2-ASKS.md §2 row 12` (the RIPE
  `deltaEOK` consume edge); `K-SEED.md §7` KF-DELTAE ("gates ED-4 + the CC-2 pixel proof");
  `css-compiler.md §7` (the shared ΔE fidelity discipline with K.W10's CC-2). The throughput benchmark
  is BOOK (`ecosystem-distribution.md §5.3`); ED-6 JSR is KILLED (`../L-SEED.md §5`).
- **The discharged-elsewhere rows (NOT re-litigated here):** ED-5 the spurious-Vue-peer removal +
  the `proof:published-surface` peer-dep clause was J-FOLDED into J.W5 (`K-SEED.md §4 J-amendments`;
  `ecosystem-distribution.md §3`; `grep -n peerDependencies package.json` → discharged); the
  TypeDoc/JSR chores were J-FOLD/KILL (`ecosystem-distribution.md §7`). W12 builds on the J.W5 floor;
  it does NOT re-author it (inv ε — no manufactured work).

## §Hand-off (the BINDING file-ownership boundary — §4B of the README, restated)

W12 is the LAST Band-II wave; it CLOSES the frontier band. Its loci (`waves/README.md §4B`,
"W12 … NEW public artifacts + a NEW sibling package (`keyframes-vue`) + the demo's barrel-import edge
(ED-3)"):

- **W12 owns NEW public artifacts + a NEW sibling package + the demo's barrel-import edge.** The agent
  artifacts (`/llms.txt`, `/llms-full.txt`) + the two new gates (`proof:agent-surface`,
  `proof:demo-on-published-surface`) + the `@mkbabb/keyframes-vue` package + the demo's kf-import
  flip + the color-fidelity harness. It does NOT edit `engine.ts` (W7's), `group.ts` (W7/W11's), the
  ingest module (W8's), the scroll module (W9's), or the compiler (W10's) — W12 is downstream of every
  source surface, it EXPOSES them, it does not edit them.
- **ED-3 GATES on Band I (K.W0–K.W4) — the BINDING honesty precondition.** The dogfood inversion flips
  the demo onto the published barrel; that is honest ONLY because Band I repaired the demo — the cold
  path PLAYS post-K.W0 (`proof:cold-entry` green), the panes are re-cut post-K.W4. A dogfood inversion
  onto a broken demo certifies a broken product against its own package (`K.md §The two bands`). Clause
  (c) asserts the cold-entry oracle + inv ζ on the barrel-imported demo. **This is a TEMPORAL
  dependency, not a co-edit:** Band I lands FIRST (the whole repair band), then W12 inverts onto the
  honest substrate it leaves.
- **ED-1 + ED-4 build ON the J.W5 floor (LANDED) — NOT a re-author.** `docs/published-surface.md` (the
  manifest), `proof:published-surface` (the static three-way oracle), and the runnable README snippets
  are J.W5's (LANDED — `4.2.0` published). ED-1 generates the agent index FROM them; ED-4 shares the ΔE
  discipline with K.W10's CC-2. W12 consumes the J.W5 surface; it does not re-litigate it (inv ε).
- **ED-2's `@mkbabb/keyframes-vue` consume of the core library is a PUBLISHED consume, NOT a `file:`
  link or a vendored copy** (the acyclic-spine invariant, applied to kf's OWN sibling). The adapter
  package consumes `@mkbabb/keyframes.js` from the registry (the published barrel — the light tier
  statically, `loadAnimationEngine()` for the heavy `CSSKeyframesAnimation`), born-correct-gated on its
  own `proof:published-surface` discipline. It declares the Vue peer; the core library never.
- **The value.js consume edge (`deltaEOK`) is a PUBLISHED consume, NOT a `file:` link or a vendored
  copy** (the acyclic-spine invariant). The producer is one tranche behind and already INSTALLED
  (`^0.11.2`, barrel `:20`), RIPE — no OPEN gate, no unpublished symbol, no born-RED edge (the source
  half is GREEN at value.js's side: `deltaEOK` shipped 0.11.2, the N.W11.D fold re-confirms it at
  `GRAMMAR-FOLD.md §I.2`/`gamut.ts:53` "ships 0.12.0", and W12's consume needs nothing from the pending
  0.13.0 cut). It does NOT depend on the K.W1 `^0.11.2 → ^0.12.0` re-pin (the re-pin un-blocks the
  OTHER N2 consumes; ED-4's kernel was installed already) — the SAME `deltaEOK` K.W10's CC-2 ΔE proof
  consumes, one producer, two RIPE consumers.
- **→ K.WZ (the close, BINDING):** W12 closes Band II on the boundary-ORACLE (the agent index ==
  the surface, the demo == the package, the fidelity conformance published). K.WZ records the frontier
  band CLOSED on the replay-equality invariant (W8/W10 replay pixel-equal, W12 exposes them); the
  version cut + publish (the agent artifacts + the `@mkbabb/keyframes-vue` package publish) are
  USER-DOMAIN (confirm-first, Mike Babb, K.WZ — `K.md §chronic + deferred fold`). W12 carries NO TASTE
  verdict — the design band's review-packet verdict (the K.W4 pane re-cut) closes at K.WZ, NOT here;
  W12's surfaces are CORRECTNESS artifacts (the boundary-ORACLE), not appearance.
- **OUT / sibling (do NOT touch in W12):** the value.js tree (the `deltaEOK` producer is consumed
  PUBLISHED, inv-16); the throughput benchmark (BOOK — the credibility trap); the JSR target (KILLED —
  ED-6); the React adapter (BOOK — after the Vue adapter proves the shape); the engine/ingest/scroll/
  compiler/physics source (W7–W11's — W12 exposes, never edits).

## §Design-decisions (the named calls this spec makes, so the impl does not re-litigate)

- **ED-1 the agent surface is the HEADLINE; the others are its body — RESOLVED.** The lane's
  self-skeptical verdict (`ecosystem-distribution.md §8`) is that the SINGLE genuinely-frontier idea is
  the agent-consumable surface — "the one ecosystem move that ONLY kf's three unique axes + its proof
  culture make credible." ED-2/ED-3/ED-4 are the on-brand body (the adapter, the dogfood inversion, the
  fidelity harness — facets of the proof culture made externally consumable); the chores (TypeDoc, the
  throughput benchmark, JSR) are sorted OUT (J-FOLD/BOOK/KILL). W12 ships the headline + the body, not
  the chores.
- **The agent index is GENERATED, gate-verified — RESOLVED.** `/llms.txt` cannot drift from the
  published surface because `proof:agent-surface` asserts the index == the J.W5 manifest + the
  `proof:all` roster, and the `/llms-full.txt` snippets are the ONES CI executes (`J.W5.md:257`). The
  agent surface is, by construction, the gate-verified surface — "what an agent reads about kf is what
  kf proves" (`ecosystem-distribution.md §6.3`). A hand-maintained index is the doc-rot forbidden.
- **The Vue adapter is the THIN on-brand `<Keyframes>` component, NOT the demo composables — RESOLVED.**
  The composables are `ScenePlayback`/`sceneMachine`-coupled (the extract-the-composables framing is
  researched-FALSE, `ecosystem-distribution.md §1.3`). The honest extraction is the ~40-line
  `useKfAnimation` kernel + the declarative `<Keyframes :css>` component — the one a Motion/`vueuse-motion`
  adapter CANNOT write (no other engine parses author CSS `@keyframes` as its source). The Vue peer
  lives on the adapter package; React is BOOK (no React demo to dogfood-invert).
- **The dogfood inversion is HONEST ONLY on Band I's repaired demo — RESOLVED.** Flipping the demo onto
  the published barrel certifies the product against its own package — honest ONLY because Band I makes
  the demo WORK (the cold path plays post-K.W0, the panes re-cut post-K.W4). The inversion gates on
  K.W0–K.W4 (clause (c) asserts the cold-entry oracle + inv ζ on the barrel-imported demo); the heavy
  tier flips to `loadAnimationEngine()` (the PUBLIC boundary), not a partial flip leaving the raw chunk.
  This is the on-brand completion of the boundary-ORACLE precept — "what `npm i` installs is what the
  demo runs."
- **ED-4 ships the FIDELITY harness, NOT the throughput benchmark — RESOLVED.** The throughput benchmark
  is the credibility trap (kf is the contestant; it cannot occupy the neutral seat —
  `ecosystem-distribution.md §5.3`) and is BOOK. The color-fidelity conformance harness (midpoint ΔE vs
  CSS Color 4, via the RIPE `deltaEOK`) is the ONE benchmark that extends a unique axis (axis-2) and is
  un-spinnable (it measures correctness against a spec, not speed against a rival). It shares K.W10's
  CC-2 ΔE fidelity discipline (`css-compiler.md §7`) — one producer (`deltaEOK`), two consumers, both
  RIPE.
- **W12 CLOSES Band II on the boundary-ORACLE, carries NO replay-pixel-equality of its own — RESOLVED.**
  W12 is the EXTERNALIZE, not a round-trip widening — its faithfulness is the BOUNDARY-ORACLE (the
  agent index == the published surface; the demo == the package). It CLOSES the band by EXPOSING the
  prior surfaces' replay-equality (ED-1 indexes the round-trip recipe ONLY because W8 ingests + W10
  compiles replay-pixel-equal; ED-4 shares K.W10's ΔE discipline). The replay-equality invariant
  (`K.md §invariant set`) closes the frontier band at K.WZ on W8/W10's pixel-proofs; W12 is the surface
  that makes those proofs externally consumable.
- **W12's value.js half is RIPE — no acyclic-spine born-RED — RESOLVED.** `deltaEOK` shipped in
  **0.11.2** and is present in the installed `^0.11.2` barrel at `:20` (`VALUEJS-N2-ASKS.md §2 row 12`;
  the §State-verified probe); the agent surface + the Vue adapter are kf-owned artifacts
  (value.js-grammar-independent). W12 carries NO OPEN gate, no unpublished symbol, no born-RED edge —
  like W7, it is a frontier wave whose every external dependency is already published. **Unlike
  K.W9/K.W10's CC-2** (which born-RED-gate on the OPEN VJ.W1/VJ.W2, dispatched as the now-RATIFIED
  value.js N.W11′/N.W11.D fold shipping in 0.13.0 — `GRAMMAR-FOLD.md`/`KF-TO-VALUEJS-GRAMMAR-ASKS.md`),
  W12 needs NOTHING from the pending 0.13.0 cut: its only value.js edge (`deltaEOK`) is RIPE on the
  installed 0.11.2. ED-4's consume does NOT depend on the K.W1 re-pin (the re-pin un-blocks the OTHER
  N2 consumes); the close-band publish (the agent artifacts + the `keyframes-vue` package) is
  USER-DOMAIN at K.WZ.
