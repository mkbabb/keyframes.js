# K.W5 — THE GATE-TRUTH WAVE (the axis-coverage map EXECUTED · the engine-write disambiguation made a SYSTEM gate · the TASTE boundary instrumented · the CI hazards dispositioned measure-first)

- **Phase:** DEV — spec authored, awaits IMPL+auth · **Class:** GATE-TRUTH-in-K (the oracle
  family itself; partitioned — its legs RIDE the surfaces they certify, none lands standalone).
  K.W5 is NOT a wave that ships first and is then verified; it is the **wave whose deliverable IS
  the verification of every other wave**. Its legs partition across the DAG: the cold-entry oracle
  (`proof:cold-entry`) + the de-vacuoused B1 are W0's OWN hard gate and land WITH W0 (this spec
  AUTHORS them as the consumed dependency, exactly as J.W0 authored `navToScene` for J.W3 to
  industrialize); the `proof:subject-animates` synthetic→REAL extension lands as the scenes it
  certifies stabilize; the single-option-select gate lands WITH W4's totality fix; the TASTE-packet
  generator generates at W4's close on the W3-lib capture harness; the release.yml `timeout-minutes`
  (F-1) rides W1; the demo-smoke wall-clock hazard is dispositioned MEASURE-FIRST. · **Scope (gate
  harness + the CI workflow surface — NO product source; the product fixes live in W0/W4):**
  `scripts/proof-cold-entry.mjs` (NEW — the LEAD oracle, born-RED on the live P0), the B1
  de-vacuity edit in `scripts/proof-live-session.mjs` (the `.idle-hover`/`.graph` exclusion + the
  `aria`-flip precondition), the `proof:subject-animates` ARM-C extension onto the demo
  matrix/CSS-var path (`scripts/proof-subject-animates.mjs` OR a sibling demo-arm),
  `scripts/proof-no-single-option-select.mjs` (NEW — the U4 totality gate), the TASTE-packet
  generator on the capture harness (`scripts/lib/`-resident, riding the `proof:visual-lock`
  region-screenshot seam + the existing `scripts/lib/demo-driver.mjs withPage` lib W3's
  layout-cluster gate also rides), `release.yml` (`timeout-minutes: 15` on the `publish`
  job), and the `ci.yml` `demo-smoke` `timeout-minutes` recalibration (measure-first, NOT a blind
  bump). · **DAG-deps:** **RIDES W0 (LEADS-with), W4 (totality + packets), W1 (release timeout);
  PARALLEL W6.** The cold-entry oracle + de-vacuoused B1 land WITH W0 (W0's §Hard gate IS
  `proof:cold-entry`); every later wave's verification CONSUMES the de-vacuoused B1 (a wave can no
  longer green over a frozen subject). The single-option gate + TASTE packets land WITH W4. The
  capture harness W5's packet generator rides is the EXISTING `scripts/lib/demo-driver.mjs withPage`
  lib (the single-sourced driver every browser gate — including W3's layout-cluster gate — already
  rides; "the W3-lib capture harness" per `K.md §clusters` K.W5 row = the lib W3's gate also rides,
  NOT a W3 consolidation deliverable). W5 AUTHORS the generator atop it; no second pipeline (the
  net-deletion seam, BINDING below). No K wave's design band closes until the TASTE packet it
  generates is verdicted by
  the user (the TASTE-boundary invariant) — K.W5 is the wave that BUILDS that packet, never the wave
  that VERDICTS it.

## §Provenance (the folded root causes + the booked follow-up)

- `live-session-gap-analysis.md` — **THE decisive input, wave-ready.** The meta-finding (§0): the
  WZ rounds certified `proof:all` GREEN; the user found the product broken hours later — "a
  structural axis the gate battery never drives." Three independent failure modes (§0): (1) the
  COLD-ENTRY path is unexercised (every play-driving leg `seedControlsOpen` and/or hash-navs
  DIRECTLY to `#/cube`); (2) the hero-CTA actuation gap (the four hero gates `grep -cE
  "click.*[Pp]lay|togglePlay|distinct.*transform|Pause animation"` → 0); (3) the B1 liveness oracle
  is satisfied by INDEPENDENT idle motion (`.idle-bob`), not the engine write. The axis-coverage map
  (§2, the lane mandate's classification: (a) covered-but-passed, (b) uncovered-axis, (c)
  post-cert-drift) is THE work-order this wave executes. The five gate-roster needs (§2 "What the K
  gate roster needs") are this wave's five legs: the COLD-ENTRY gate (the `--ball-p`/slider advance +
  aria flip load-bearing, an aria-gated engine-write-subject corroborator), the B1 de-noise, the amiga engine-started oracle, the positive font-voice assert,
  the `deps-current` widen. The single highest-leverage cut (§FOLD F3) is the cold-entry gate —
  "born-RED on the live P0 and closes the meta-finding's whole axis at once."
- `gate-estate-k.md` — the gate-estate ground truth + the structural taste question. §1: 99 proof
  scripts / 15 correctness + 100 hygiene; the proof layer (37 152 LoC) is 3× the vitest layer. §3:
  the cold-path is an unexercised axis (the `proof:gate-is-runtime` meta-gate is a HARNESS-SHAPE
  checker — it cannot see COVERAGE GAPS in the actuation path). §4: the lib-adoption coverage is
  SYNTHETIC — `proof:subject-animates` ARM-C uses `left:0→200px` on a synthetic `<div>`, never the
  demo's `matrix3d`/`--ball-p` path. §5: the visual-lock baseline IS the disliked state (drift the
  user dislikes is now GREEN — the W7c re-baseline encoded the rejected layout). §8: **the
  designer-eye gap** — "no gate can carry taste; the band is honestly human" — the structural answer
  this wave instruments (the TASTE-boundary protocol). §9: the K-gate-additions table (5 rows, each
  with a born-RED condition) is this wave's specification ledger.
- `ci-cd-k.md` — the CI/CD ground truth + the two hazards this wave dispositions. **F-1 (P1):
  `release.yml` has NO `timeout-minutes`** — `grep -n "timeout-minutes" release.yml` → no output; a
  stuck `npm publish` blocks for the GHA default 360m. The fix is `timeout-minutes: 15`. **F-2 (P1):
  the 35m demo-smoke budget was set at J.W4 and does NOT account for post-J.W4 gate additions** —
  the projected total is ~42m (the J.W4-era 28m + ~14m of new browser work), which EXCEEDS the 35m
  ceiling; the gap is structural (no automated re-measure). The disposition is MEASURE-FIRST: before
  K adds new browser gates to demo-smoke, observe the CURRENT wall-clock; recalibrate ONLY on a
  measured run. F-3 (P1) restates the cold-path absence; F-4/F-5/F-6/F-7 are W1/W6-owned (release
  gate parity, deploy-rebuild, glass-ui pin, the static-gate placement) — OUT of this wave (named in
  §Hand-off).
- `tests-bench-k.md` — the test/bench ground truth + the cold-path test blindspot. §5a: `proof:
  live-session` has a structural blindspot — every leg `seedControlsOpen` before nav, so the
  cold-user path (empty localStorage, default controls) is never driven; the fix is a dedicated
  `proof:cold-start` leg (the same oracle as B1 WITHOUT the seed). §5b: the U4 conditional-select
  removed the `onSelectAnimation`-as-implicit-play path for single-animation scenes — no unit test
  covers it (a W0/W4 product concern, not W5's; W5 gates the SINGLE-OPTION-RENDER, not the
  play-path). §3a: the two measure-tests (`sync-step.measure.test.ts`, `d3-changed-keys.measure.
  test.ts`) emit console.log noise on every `npm test` — a hygiene fold (W6-owned; recorded here as
  the test-noise seam, NOT W5 scope).
- The cross-rooted P0 chain (`live-cold-play-path.md §P0-1`, `demo-scenes-k.md §S1`,
  `deferred-ledger-k.md DL-K1/K2`) — the cold-entry oracle's BORN-RED witness substrate: the
  product defect (`scenePlaybackAdapters.ts:76-79` resume-no-op) is W0's to FIX; W5's
  `proof:cold-entry` is the oracle that REDS on it today and GREENS only when W0's fix lands.
- The booked follow-up: the `deferred-ledger-k.md` §0 meta-chronic — **DL-K2, the gate-blindspot
  itself, a ≥9-tranche (pre-A…J) rider, witnessed a FOURTH time.** Under P-invariant-28 it is the
  ROOT exit: "terminating it terminates the false-GREEN mechanism for the whole band" (DL-K1/K3/K4/
  K10 all falsely-TERMINATED THROUGH it). K.W5's de-vacuoused B1 + `proof:cold-entry` are the SYSTEM
  gate that exits DL-K2 — the structural precondition for honestly closing every other ≥4-tranche
  product chronic.

## §The state, verified (file:line / live anchors / command+output)

- **B1 greens engine-OFF (the meta-finding, verified live):** `k-verify-gate-blindspot.mjs` runs the
  VERBATIM B1 oracle (`.cube/.graph/.idle-hover` ≥3 distinct over 100 ticks) on the cold-BROKEN
  state → `B1 distinct transforms: 101 → B1 verdict: PASS (GREEN)` while `ACTUAL engine state: play
  button = "Play animation" → engine is NOT playing` (`live-session-gap-analysis.md §1`,
  `live-cold-play-path.md P1-1`). At REST with NO play: `IDLE-HOVER distinct transforms: 40`,
  `B1-formula distinct (.cube/.graph/.idle-hover): 41`, gate needs ≥3. **The idle CSS animation
  alone saturates the count.** This is the born-RED witness shape for the B1 de-vacuity edit.
- **The B1 leg's exact shape (post-J baseline), confirmed against the tree:**
  `scripts/proof-live-session.mjs:387` `await seedControlsOpen(page)` (the warm pre-seed);
  `:393-411` the sample loop over `[".cube", ".graph", ".idle-hover"]` asserting `distinct >= 3`;
  `:411` `dom.B1 = { distinct, live: distinct >= 3, pass: distinct >= 3 }`; the verdict label at
  `:1432` `verdict("B1", "B1 cube draw loop is LIVE after group-play (≥3 distinct transforms)",
  …)`. The idle-bob source: `demo/cube/CubeTarget.vue:207-208` `.idle-hover { animation: idle-bob 3s
  … infinite alternate }`; `:210-211` `.idle-hover.playing { animation: none }` (it stops ONLY when
  the engine IS playing — so at rest it runs and saturates B1). `seedControlsOpen` writes
  `{isControlsPanelOpen: true}` (`:225-228`) — state the genuine cold user never has.
- **`proof:subject-animates` is SYNTHETIC, by its own docstring:** `scripts/proof-subject-animates.
  mjs:1-31` (the header, READ) declares itself "THE MISSING ORACLE the whole gate battery lacked" —
  and names the exact B1 blindspot it does NOT cover for the demo: "`proof:live-session` B1 the same
  … those selectors ALSO carry the cube's standalone perspective tilt + the idle-hover bob + the
  orbital container matrix, ALL of which move INDEPENDENTLY of the engine play write." Its ARM-C (the
  `AnimationGroup` composite) "uses `left` interpolation on a synthetic subject — not the cube's
  matrix path or the spring ball's `--ball-p` path" (`gate-estate-k.md §4`). So the synthetic gate
  is HONEST for the library surface (the `dist/keyframes.js` `Animation.play()` over a `<div>`) and
  STRUCTURALLY does not exercise the demo's real transform path — the §4 gap this wave's ARM-C
  extension closes.
- **The single-option `<Select>` STILL renders, verified:** `live-dock-tabs-selects.md §2.1/§3 S1`
  — `ChromeDock.vue:199-221` guards `v-if="hasControlPanel"` where `hasControlPanel =
  allControlTabs.value.length > 0`. For easing (`["easing"]`) and spring (`["spring"]`)
  (`controlSurfaceDFA.ts:76-85`) `allControlTabs.value` has exactly ONE element → a `<Select>` with a
  single `<SelectItem>` renders (the U4 dead-chrome case). `TransportDock.vue:39` `v-if="animationNames.length
  > 1"` is the CORRECT pattern the rule must be made total against. **The U4 rule was declared
  "total" (U-K16) and is NOT** (`deferred-ledger-k.md DL-K5`). This is the born-RED witness for the
  single-option gate. (S9 `AssetPropertiesPanel.vue:114` is playground-only — OUT, per the U-K19/CC-1
  playground-scope finding.)
- **`release.yml` has NO `timeout-minutes`, verified:** `grep -n "timeout-minutes"
  .github/workflows/release.yml` → no output (`ci-cd-k.md F-1`, RE-confirmed against the tree). The
  `ci.yml` jobs DO carry timeouts (`:51` `gates: timeout-minutes: 10`; `:213` `demo-smoke:
  timeout-minutes: 35`). The release publish job's full sequence (`npm ci → check:lib → build:lib →
  test → proof:boundary → npm publish --provenance`) "measured under 5 minutes on warm runners"; 15m
  is ~3× with headroom (`ci-cd-k.md F-1` suggested fix).
- **The demo-smoke 35m budget is unverified post-J.W4:** `ci.yml:207-213` (the CICD-7 comment, READ
  via the audit) records the J.W4 re-size — "the pre-W4 roster measured 19m13s end-to-end (run
  27310054675) against the old 20m ceiling; J.W4 adds ~9m; 35m bounds the new roster with headroom."
  Eight gates added AFTER the J.W4 recalibration (`ci-cd-k.md F-2` table) project ~14m of new browser
  work → ~42m projected total, EXCEEDING the 35m ceiling. **There is no recent measured run after
  J.W7c's close** — the 35m ceiling "is now an unverified artifact" (`ci-cd-k.md F-2` seam). This
  wave's demo-smoke disposition is MEASURE-FIRST (observe the current wall-clock; recalibrate only on
  a measured run; do NOT pre-optimize). It pairs with `deferred-ledger-k.md DL-K13` (the acute
  deploy-block recurrence class — K adds cold-path + appearance + amiga + typography gates; the margin
  WILL be consumed; the silent-no-deploy hazard J.W0 fought).
- **The capture-harness substrate the TASTE packet rides, verified:** `proof:visual-lock`
  (`scripts/proof-visual-lock.mjs:44-47`) "screenshots each NAMED REGION (not full-page) … to a
  COMMITTED PNG under `scripts/baselines/visual-lock/`, then diffs the fresh capture against the
  committed baseline via `pixelmatch`." The region-screenshot machinery EXISTS; the TASTE packet
  generator REUSES it (per-pane before/after, desktop+mobile) — net-add over an existing seam, NOT a
  new screenshot pipeline. The harness it rides is `scripts/lib/demo-driver.mjs withPage` (the
  single-sourced driver every browser gate imports — including W3's layout-cluster gate; this is the
  "W3-lib capture harness" of `K.md §clusters` K.W5 row: the EXISTING lib W3's gate also rides, NOT a
  W3 deliverable — `demo-driver.mjs` already exists with `withPage`/`serveDist`/`SCENES`, confirmed by
  the J.W0 baseline). The
  `design-synthesis-k.md` screenshot corpus (`screenshots-k/ds-{hero,cube,spring,…}-{desktop,mobile}.
  png`) is the EXEMPLAR of a packet's shape (per-pane, both viewports, the named deltas) — the
  generator produces exactly this, automatically, at W4's close.
- **The taste anchors (PRESERVE — do not flatten):** `design-synthesis-k.md §5` — the rainbow play
  (PRESENT + CORRECT, the one sanctioned multi-color pop), the hero serif, the icon family, the
  red-dashed final state. The packet's "after" shots must show these intact; the de-vacuoused B1 and
  `proof:cold-entry` assert the rainbow play ACTUATES (the W0 fix), the TASTE packet asserts it
  remains BEAUTIFUL (the user's verdict).

## §Goal

Make the GATE FAMILY tell the truth on the two axes J's oracles never named — the COLD axis and the
TASTE boundary — and disposition the two CI hazards measure-first. The wave executes the
`live-session-gap-analysis.md §2` axis-coverage map as a roster of born-RED legs, each riding the
surface it certifies, with ZERO oracle relaxation (the cure is to DISAMBIGUATE engine writes from
CSS animation and to DRIVE the cold path — NEVER to raise a distinct-count threshold or seed a warm
state). Six moves, each at the gestalt altitude the mandate demands:

1. **The cold-entry oracle (S1 — the LEAD, lands WITH W0).** `proof:cold-entry`: fresh context
   (`localStorage.clear()`), NO seed, `goto #/`, click the hero rainbow play as the FIRST gesture;
   assert the LOAD-BEARING engine-attributable pair — the dock play aria flips **Play → Pause** AND
   the playback slider/`--ball-p` advances from 0 (the scalar provably "0" engine-off, advancing ONLY
   under the engine) — PLUS the aria-GATED corroborator: the per-scene **engine-write subject** (the
   OrbitalDrag-wrapper `matrix3d` the engine composites for cube/amiga, per S2) traverses ≥3 distinct
   transforms WHILE aria="Pause", `.idle-hover` excluded. The disambiguation is the EXCLUSION of the
   idle bob + the aria-GATE on the wrapper count + the `--ball-p` advance — NOT a bare single-element
   count (bare `.cube`=0/1 distinct even WARM, `gate-estate-k.md §4`; bare `.graph`=13 engine-OFF on
   the broken cold path, `k-isolate.mjs` — neither carries correctness alone; `K.W0.md §Provenance`
   reconciles the two source lanes). Born-RED on today's tree; green ONLY when W0's adapter-resume fix
   lands. Every scene cold-entered from the hero CTA AND from direct hash; the per-scene EXPECT table
   (with its `engineWriteSelector`) is the existing DFA's.
2. **The B1 de-vacuity (S2 — the engine-write disambiguation made a SYSTEM edit).** Drop `.idle-hover`
   (and the orbital `.graph`-only-CSS confound) from the B1 distinct-count sample; read the
   engine-write channel (the OrbitalDrag container `matrix3d` that the engine composites, attributable
   to `interpFrames`/the group write); ADD an `aria`-flip PRECONDITION (the assertion only counts after
   the dock play aria reads "Pause animation"). Every later wave's verification consumes this — a wave
   can no longer green over a frozen subject.
3. **`proof:subject-animates` extended synthetic→REAL (S3).** Add a demo-arm (ARM-D, or extend ARM-C)
   that drives the REAL scene's `matrix3d`/`--ball-p` transform path through the same `aria`=Pause-gated
   engine-write oracle — not the synthetic `<div>left`. The synthetic library arms STAY (they certify
   `dist/keyframes.js`); the new arm certifies the DEMO transform pipeline the synthetic arm structurally
   cannot reach (`gate-estate-k.md §4`).
4. **The single-option-select gate (S4 — lands WITH W4's totality fix).** `proof:no-single-option-select`:
   sweep every `<Select>`/`DockSelectTrigger` render in the deployed SPA; assert NONE renders with ≤1
   meaningful option (the count IS the gate — `>0 → >1` made total). Born-RED on the ChromeDock
   easing/spring 1-item dropdown today; green when W4's totality fix lands. The COUNT is the assertion;
   raising it is forbidden (it is already correct at `>1`).
5. **The TASTE-packet generator (S5 — generates at W4's close on the W3-lib capture harness).** A
   generator that, given the per-wave named deltas, produces a review packet: per-pane before/after
   screenshots, desktop+mobile, the deltas labeled, riding the `proof:visual-lock` region-screenshot
   seam + the existing `scripts/lib/demo-driver.mjs withPage` lib W3's layout-cluster gate also rides
   (the "W3-lib capture harness", not a W3 deliverable). The packet is the ARTIFACT of the
   TASTE-boundary protocol; the user's verdict
   on it is the wave's design-band close (the named USER-DOMAIN step, W0/W4/WZ-scheduled, never W5's to
   verdict). An agent's "designer-eye PASS" is corroboration, never the verdict.
6. **The CI hazards dispositioned measure-first (S6).** `release.yml` gains `timeout-minutes: 15` on
   the `publish` job (F-1, the single-line fix). The `ci.yml` demo-smoke wall-clock is MEASURED on the
   current tree BEFORE any K gate is added; the 35m ceiling is recalibrated ONLY on a measured run (F-2/
   DL-K13) — no blind bump, no pre-optimization, no `continue-on-error`.

The net-deletion holds: the de-vacuity edit DELETES the `.idle-hover` sample term (it never measured the
engine); the single-option gate's landing lets W4 DELETE the lone-option render path; the TASTE generator
REUSES the visual-lock screenshot seam (no second pipeline). NO new oracle is RELAXED; every leg either
DISAMBIGUATES or DRIVES — never seeds, never raises a threshold.

## §Scope

- **S1 — `proof:cold-entry` (NEW; the LEAD oracle; lands WITH W0 as W0's §Hard gate; correctness-tier).**
  Locus: NEW `scripts/proof-cold-entry.mjs`, wired into `proof:correctness` and `demo-smoke`, importing
  the `scripts/lib/demo-driver.mjs` harness (the `withPage`/`serveDist` lib). The oracle (from
  `live-session-gap-analysis.md §2` need #1 + `gate-estate-k.md §9` row 1 + `ci-cd-k.md F-3` suggested
  gate):
  - Fresh context: `localStorage.clear()` (the genuine COLD context — `live-cold-play-path.md`'s probe
    discipline), **NO `seedControlsOpen`** (the cold user never has `{isControlsPanelOpen:true}`).
  - `goto #/` (the hero), wait for the hero FSM rest (the start-screen present, the machine at home).
  - Find + click the hero rainbow play **as the FIRST gesture** (a real pointerdown→up, the human's
    actuation — `live-cold-play-path.md` repro). Do NOT override the nav with a second `location.hash`
    write (the B1 leg's exact mistake at `proof-live-session.mjs:395`).
  - Wait for the machine's `activeScene` to flip to `"cube"` (the per-EXPECTED-state settle, the J.W0
    `navToScene` discipline — a CEILING timeout, load-independent).
  - ASSERT, in this exact order — (i) and (ii) are the LOAD-BEARING engine-attributable pair, (iii) a
    GATED corroborator: (i) the dock play aria flips **"Play animation" → "Pause animation"**;
    (ii) the playback slider/`--ball-p` value advances from 0 (provably "0" engine-off, advancing ONLY
    under the engine); (iii) the **isolated engine-write element** (the OrbitalDrag `matrix3d`
    container, NOT `.idle-hover`/`.cube`-face/`.graph`-CSS) traverses ≥3 distinct transforms within the
    play window **WHILE aria="Pause animation"** (the aria-gate is what makes the count engine-
    attributable — bare, the container carries engine-OFF orbital churn, `k-isolate.mjs` `.graph`=13
    cold).
  - Repeat across every scene cold-entered BOTH from the hero CTA AND from direct hash (`#/cube`,
    `#/amiga`, `#/square`, `#/easing`, `#/spring`, `#/sequence`, `#/motion-path`) — the COLD-axis
    invariant: "exercised from its COLD/DEFAULT entry, not only through choreographed setup paths." The
    per-scene expected `{hasPlay, expectedAdvance, engineWriteSelector}` table is derivable from the DFA
    + the scene-contract matrix (`demo-scenes-k.md §2`); rAF scenes (easing auto-plays; spring/sequence/
    motion-path engage differently) carry their own per-scene EXPECTED state, NOT a uniform assertion.
  **BORN-RED WITNESS:** `k-verify-gate-blindspot.mjs` greens B1=101 while the play button reads "Play
  animation" — `proof:cold-entry` exists to RED on EXACTLY that. On today's tree the cold cube (i) never
  flips Play→Pause, (ii) the slider parks at 0, (iii) the engine-write element produces 1 distinct
  transform (`live-cold-play-path.md` `probe-cold-play.mjs`: 16 samples, `slider:"0"`, `isPlaying:false`,
  `vivid:false`). All three sub-asserts RED. **NO escape:** NOT a longer settle (a fixed `settleMs` is a
  load-dependent oracle — the J.W0 §No-workaround precept), NOT a raised distinct-count (the cure is the
  element-isolation + the cold drive). **WHY (the COLD-axis invariant + the engine-write disambiguation
  rule, jointly):** this single gate closes the meta-finding's whole axis — it drives the FIRST gesture
  AND reads the engine's own hand. It is the SYSTEM exit for `deferred-ledger-k.md DL-K2` (the
  ≥9-tranche gate-blindspot meta-chronic).

  > **The lib HOME + the W0 boundary (cross-wave seam, BINDING):** `proof:cold-entry` imports
  > `scripts/lib/demo-driver.mjs` (the EXISTING single-sourced lib — `withPage`/`serveDist`/`navToScene`
  > — that every browser gate rides, W3's layout-cluster gate included; not a W3 deliverable).
  > K.W5 AUTHORS `proof:cold-entry` (the consumed oracle); W0 OWNS the product fix it reds on
  > (`scenePlaybackAdapters.ts` resume-made-total — NOT a demo-side `play()` sprinkle, per the MANDATE).
  > The gate and the fix land TOGETHER in W0's motion: the gate is born-RED before the fix, GREEN after.
  > K.W5 does NOT author the product fix (that is W0's; the dev/impl boundary within K's own waves).

- **S2 — the B1 de-vacuity (the engine-write disambiguation made a SYSTEM edit in `proof:live-session`).**
  Locus: `scripts/proof-live-session.mjs` — the B1 leg (`:380-411`), plus every sibling distinct-count
  oracle that samples `.idle-hover` (the §1432 verdict, the §863/§970 PLAY legs that read mover states).
  The edit (from `live-session-gap-analysis.md §2` need #2 + `live-cold-play-path.md P1-1`):
  - DROP `.idle-hover` from the sample set (it runs `idle-bob` at REST — `CubeTarget.vue:207-211` — and
    saturates the count engine-OFF). DROP `.graph` if/where it carries only CSS-orbital churn independent
    of the engine composite; READ instead the engine-write channel (the OrbitalDrag container `matrix3d`
    attributable to the group composite write — the `apply-transform-to-container` seam `gate-estate-k.md
    §4` names).
  - ADD an `aria`-flip PRECONDITION: the ≥3-distinct assertion counts only AFTER the dock play aria reads
    "Pause animation" (the engine is genuinely playing). A distinct-count BEFORE the flip is not counted.
  - The de-vacuoused B1 is the property "the engine WROTE the subject," not "something on the page moved."
  **BORN-RED WITNESS:** the de-vacuoused B1 REDS on the cold-broken state (`k-verify-gate-blindspot.mjs`'s
  state: aria="Play animation" → the precondition never holds → the count is 0, not 101). It GREENS on the
  W0-fixed tree (aria flips, the engine-write element advances). **NO escape:** the cure is the SAMPLE-SET
  change + the precondition, NOT a raised threshold (the threshold ≥3 is correct; the SAMPLE was wrong).
  **WHY (the engine-write disambiguation rule):** "A liveness oracle must distinguish ENGINE-DRIVEN writes
  from decorative CSS animation … the oracle reads the engine's own write channel … never bare
  `getComputedStyle` churn." This is the rule's reference implementation in the gate roster.

  > **Boundary with W0 (BINDING):** W0's §Hard gate IS `proof:cold-entry` (S1) + the de-vacuoused B1 (S2)
  > — they land WITH W0, witnessing W0's product fix. K.W5 AUTHORS the oracle edits; W0 CONSUMES them as
  > its verification. The A7 cube idle-bob (`CubeTarget.vue:207-214`, `deferred-ledger-k.md DL-K19`) is
  > the EXACT confound this edit excludes; whether the idle-bob is itself re-cut to an engine-driven idle
  > (DL-K19's note) is a W0/cube-refinement product question, NOT W5's — W5 only stops COUNTING it.

- **S3 — `proof:subject-animates` extended synthetic→REAL (the lib-adoption coverage gap closed).**
  Locus: `scripts/proof-subject-animates.mjs` (a NEW demo-arm, or a sibling `proof:subject-animates-demo`
  that shares the engine-write oracle). The extension (from `live-session-gap-analysis.md §F4` +
  `gate-estate-k.md §4/§9` row 3):
  - The existing ARMs A/B/C STAY (they certify `dist/keyframes.js`'s `Animation.play()` over a synthetic
    `<div>`'s `left` — genuine library correctness, the J.W6 P0 oracle). They are NOT relaxed.
  - ADD a DEMO arm: drive the REAL scene (the choreographed cube/spring path, the surface the synthetic
    arm structurally cannot reach) and assert the engine writes the DEMO's transform channel —
    `matrix3d` on the OrbitalDrag container OR `--ball-p` on the spring ball — traversing ≥3 distinct
    values WHILE aria="Pause animation" (the same `aria`-gated engine-write oracle as S1/S2).
  **BORN-RED WITNESS:** "the synthetic gate never exercises the demo's matrix/CSS-var transform path"
  (`live-session-gap-analysis.md §F4`); `gate-estate-k.md §4` "choreo `.cube` produces 1 distinct
  transform while slider advances." The demo arm REDS on a non-animating composite that the synthetic
  ARM-C greens over; it GREENS when the demo transform pipeline genuinely writes. **NO escape:** the
  demo arm reads the engine-write channel (matrix3d/`--ball-p`), never `.idle-hover`; the assertion is
  the engine-attributable delta, not page churn. **WHY:** the published library and the demo are
  DIFFERENT transform paths (`left` vs `matrix3d`/`--ball-p`); the synthetic gate is honest for the lib
  and structurally blind to the demo — this arm certifies the path the user actually SEES.

- **S4 — `proof:no-single-option-select` (NEW; the U4 totality gate; lands WITH W4).** Locus: NEW
  `scripts/proof-no-single-option-select.mjs`, wired into the demo-smoke browser tier (it drives the
  deployed SPA). The oracle (from `live-dock-tabs-selects.md §2/§3 S1` + `deferred-ledger-k.md DL-K5` +
  `gate-estate-k.md` U-K16):
  - Cold-enter every scene; for every rendered `<Select>`/`DockSelectTrigger`, read its option count;
    ASSERT none renders with ≤1 MEANINGFUL option (the U4 rule made TOTAL — `>0 → >1`).
  - The ChromeDock controls-tab select (`ChromeDock.vue:199-221`) for easing (`["easing"]`)/spring
    (`["spring"]`) is the canonical born-RED site: a 1-item dropdown opening onto the value the trigger
    already shows. The TransportDock animation-name select (`TransportDock.vue:39`) is the CORRECT
    reference (`v-if="animationNames.length > 1"` + a static `v-else` label).
  - The static-option selects (DIRECTIONS×4, FILL_MODES×4, BLEND_MODES×3, JUMP_TERMS×4, the 2-option
    SegmentedTabs) are SAFE by construction (`live-dock-tabs-selects.md §2.3-2.13`) — the gate reads the
    runtime-rendered count, so they pass; only the DYNAMIC single-option renders RED.
  - The playground `AssetPropertiesPanel` (`:114`, S9) is OUT — playground-only, not deployed-SPA scope
    (the U-K19/CC-1 playground-scope finding; `completeness-critic.md §4`).
  **BORN-RED WITNESS:** `ChromeDock.vue:199` renders a 1-item dropdown for easing/spring TODAY
  (`live-dock-tabs-selects.md §2.1`); the gate REDS on it; it GREENS when W4's totality fix changes the
  guard to `> 1` + a static label. **NO escape:** the COUNT is the gate; the threshold `>1` is already
  correct — raising it is forbidden (the MANDATE: "the no-single-option rule … made total, NOT raising a
  count"). **WHY (the MANDATE's named forbidding):** "single-option selects NEVER render … the count IS
  the gate." K.W5 AUTHORS the gate; W4 OWNS the product totality fix it reds on.

  > **Boundary with W4 (BINDING):** W4 owns the PRODUCT fix (the per-scene/`ChromeDock` guard made `>1`
  > with a static-label `v-else`) + the broader pane re-cuts (U-K11/K12/K13/K17/K20). K.W5 owns ONLY the
  > `proof:no-single-option-select` gate that reds on the lone-option render. Disjoint loci: W4 at the
  > `<Select>` `v-if` guards; W5 at the runtime option-count assertion. The gate lands WITH W4's fix
  > (born-RED before, GREEN after); K.W5 does NOT author the guard.

- **S5 — the TASTE-packet generator (the TASTE-boundary protocol instrumented; generates at W4's close).**
  Locus: a `scripts/lib/`-resident generator (e.g. `scripts/lib/taste-packet.mjs`) + a thin
  `proof:taste-packet` or `npm run taste-packet` driver, riding the `proof:visual-lock` region-screenshot
  seam + the existing `scripts/lib/demo-driver.mjs withPage` lib W3's layout-cluster gate also rides
  (the "W3-lib capture harness", not a W3 deliverable). The generator (from the TASTE-boundary
  invariant + `gate-estate-k.md §5/§8` +
  `design-synthesis-k.md §5`):
  - INPUT: the per-wave named deltas (the design changes a wave declares — e.g. W2's font-voice deltas,
    W3's layout-cluster deltas, W4's pane re-cuts) + a BEFORE ref (the pre-wave tree, or the committed
    visual-lock baseline) + the AFTER tree.
  - OUTPUT: a review packet — per-pane BEFORE/AFTER screenshots, BOTH desktop AND mobile viewports
    (`design-synthesis-k.md`'s `ds-{pane}-{desktop,mobile}.png` shape is the exemplar), the named deltas
    labeled per pane, the taste-anchor checklist (the rainbow play / hero serif / icon family /
    red-dashed final state PRESERVED — `design-synthesis-k.md §5`).
  - The packet is an ARTIFACT (a directory of PNGs + a manifest), NOT an assertion. The generator does
    NOT verdict; it PACKAGES. The user's verdict on the packet is the wave's design-band close — a named
    USER-DOMAIN step, scheduled BEFORE the tranche close (W0/W4/WZ), never after.
  **BORN-RED WITNESS:** there is NO packet generator today — the J.W7c verify rounds asked agents for a
  "designer-eye PASS" and got it, while the user's same-day verdict was "still sucks"/"awful"
  (`PATH-FORWARD.md §1`, `live-spring-sequence-mp-verdict.md`). The "born-RED" here is the PROTOCOL
  absence: a wave's design band CANNOT close without a generated packet + a recorded user verdict; the
  generator's existence is what makes that gate-able (the packet must EXIST and be presented before WZ
  claims the design band). **NO escape:** an agent verdict NEVER substitutes for the packet + the user's
  verdict (the TASTE-boundary invariant: "An agent's 'designer-eye PASS' is corroboration, never the
  verdict"). **WHY (the TASTE-boundary invariant):** "Gates carry CORRECTNESS; they do not carry the
  design VERDICT … the protocol hands it to the user — named, packaged, and BEFORE the close." S5 builds
  the packaging; the user owns the verdict; `gate-estate-k.md §8`'s "no gate can carry taste; the band is
  honestly human" is the resolved structural answer.

  > **The capture-harness HOME (cross-wave seam, BINDING):** the packet generator RIDES the
  > `proof:visual-lock` region-screenshot seam + the EXISTING `scripts/lib/demo-driver.mjs withPage`
  > lib (the single-sourced driver every browser gate already rides, W3's layout-cluster gate
  > included). "The W3-lib capture harness" (`K.md §clusters` K.W5 row) names that lib by the wave
  > whose layout-cluster gate is its most prominent K rider — it is NOT a W3 deliverable to
  > "industrialize" or "consolidate" (W3's scope is the grid/anchoring redesign, M1–M4; it authors no
  > capture-lib). K.W5 AUTHORS the generator atop the existing lib (the consumer). One screenshot
  > authority, no second pipeline (the net-deletion seam). **WHY (transposition):** the design language
  > is made total at its ROOTS (W2/W3) and the verdict on it is packaged by ONE generator over ONE
  > capture seam — not per-wave ad-hoc screenshot scripts.

- **S6 — the CI hazards dispositioned (release timeout fix + demo-smoke wall-clock MEASURE-FIRST).**
  Locus: `.github/workflows/release.yml` (the `publish` job) + `.github/workflows/ci.yml` (the
  `demo-smoke` `timeout-minutes`). The disposition (from `ci-cd-k.md F-1/F-2` + `deferred-ledger-k.md
  DL-K13`):
  - **F-1 (the single-line fix):** add `timeout-minutes: 15` to the `publish` job in `release.yml`. The
    publish sequence measured <5m warm; 15m is ~3× headroom and terminates within the runner's billing
    epoch — closing the gap the documented `cancel-in-progress: false` asymmetry leaves open (a stuck
    publish is BLOCKED but never CANCELLED → it can stall 6h without a timeout).
  - **F-2 / DL-K13 (MEASURE-FIRST):** the demo-smoke 35m ceiling is UNVERIFIED post-J.W4 (~42m
    projected). BEFORE any K browser gate (S1/S3/S4) is added to demo-smoke, observe the CURRENT tree's
    demo-smoke wall-clock (a triggered CI run, the `27310054675`-class measurement). Recalibrate the
    ceiling ONLY on a measured run; if the K gates push it near the ceiling, the cure is the W6/W3 harness
    net-deletion (one shared chromium+server, `withBrowser` reuse — the J.W0 CICD-7 mechanism), MEASURED
    not pre-optimized. The new K gates DECLARE their P6 posture per clause (device-independent correctness
    hard-gates; device-dependent measurements observe-only).
  **NO escape:** NOT a blind ceiling bump (the F-2 mistake of an unverified artifact), NOT a
  `continue-on-error` to "get past" a freshly-surfaced timeout (the J.W0 §No-workaround precept), NOT a
  pre-optimization before the measurement (measure-first). **WHY:** F-1 closes a real 6h-stall hazard
  with one line; the demo-smoke margin is the deploy-block class (DL-K13 ACUTE — a timeout-killed CI run
  blocks the green-CI gate the auto-deploy chain rides, the H→I silent-no-deploy class). Both are
  dispositioned at the gate-truth altitude: a real hazard fixed deterministically, a budget validated by
  measurement.

## §Hard gate (the proof:* that BITES — born-RED on today's tree · the COLD-axis + engine-write-disambiguation + TASTE-boundary oracles)

**The gate-truth oracle family (the K-born invariants made falsifiable):** each clause ACTUATES the
running surface — the cold-entry oracle drives the FIRST human gesture from a cleared context; the
de-vacuoused B1 reads the engine's own write channel; the demo subject-animates arm exercises the real
transform path; the single-option gate reads the runtime-rendered option count; the TASTE generator
packages the named deltas for the user's verdict. Each asserts an EXACT property with a CONCRETE born-RED
witness on today's tree.

- **clause (a) — `proof:cold-entry` GREEN, born-RED on the live P0 (the LEAD; CORRECTNESS; lands WITH
  W0).** From a `localStorage.clear()` context, NO seed, `goto #/`, click the hero rainbow play ONCE;
  assert across every scene (hero-CTA AND direct-hash) the LOAD-BEARING pair + a gated corroborator:
  (i) dock play aria flips "Play animation" → "Pause animation"; (ii) the playback slider/`--ball-p`
  advances from 0 (the engine-attributable scalar — provably "0" engine-off); (iii) — the corroborator —
  the isolated engine-write element (the OrbitalDrag `matrix3d` container, NOT `.idle-hover`/`.cube`-
  face/`.graph`-CSS) traverses ≥3 distinct transforms WHILE aria="Pause" (the aria-gate makes the
  count engine-attributable; a bare element count is unfit BOTH ways — `.cube`=0/1 even warm,
  `.graph`=13 engine-OFF). **BORN-RED WITNESS:** on today's tree the cold cube fails all three
  (`live-cold-play-path.md` `probe-cold-play.mjs`: 16 samples `slider:"0"`, `isPlaying:false`,
  `vivid:false`; the engine-write element produces 1 distinct transform, the aria precondition never
  holds). **BITE:** reds on the pre-W0-fix tree (the `scenePlaybackAdapters.ts:76-79` resume-no-op);
  greens when W0's resume-made-total fix lands. **NO escape:** NOT a longer settle, NOT a raised
  distinct-count, NOT a seeded warm state, NOT a bare single-element count — the `--ball-p`/aria
  load-bearer + the idle-bob exclusion + the aria-gate + the cold drive ARE the cure (the engine-write
  disambiguation rule + the COLD-axis invariant).
- **clause (b) — the de-vacuoused B1 REDS on a frozen subject (the engine-write disambiguation;
  CORRECTNESS).** B1 (and every sibling distinct-count leg) samples the engine-write channel, NOT
  `.idle-hover`; the ≥3-distinct assertion is GATED on the dock aria reading "Pause animation".
  **BORN-RED WITNESS:** `k-verify-gate-blindspot.mjs` greens the OLD B1 at 101-distinct while aria="Play
  animation" — the de-vacuoused B1 REDS on exactly that state (the precondition never holds, the
  engine-write count is 0). **BITE:** reds on the cold-broken tree (no aria flip → no count); greens on
  the W0-fixed tree. **This is the no-false-GREEN guard:** a wave's verification can no longer pass over a
  frozen subject — the SYSTEM exit for `deferred-ledger-k.md DL-K2` (the ≥9-tranche gate-blindspot).
  **NO escape:** the cure is the sample-set change + the aria precondition, NEVER a threshold raise.
- **clause (c) — `proof:subject-animates` certifies the DEMO transform path (CORRECTNESS).** The new
  demo-arm drives the real cube/spring scene and asserts the engine writes `matrix3d`/`--ball-p` (≥3
  distinct, aria=Pause-gated) — the path the user SEES, distinct from the synthetic `<div>left` arm.
  **BORN-RED WITNESS:** "choreo `.cube` produces 1 distinct transform while slider advances"
  (`gate-estate-k.md §4`); the synthetic ARM-C greens over a non-animating demo composite. **BITE:** reds
  on a demo transform pipeline that does not write the subject (the §F4 gap); greens when the demo path
  genuinely composites. **NO escape:** the demo arm reads the engine-write channel, never page churn; the
  synthetic arms are NOT relaxed (they stay; the demo arm is ADDED).
- **clause (d) — `proof:no-single-option-select` REDS on a lone-option dropdown (CORRECTNESS; lands WITH
  W4).** Cold-enter every deployed-SPA scene; assert no `<Select>`/`DockSelectTrigger` renders with ≤1
  meaningful option. **BORN-RED WITNESS:** `ChromeDock.vue:199-221` renders a 1-item dropdown for
  easing/spring TODAY (`live-dock-tabs-selects.md §2.1/§3 S1`). **BITE:** reds on the lone-option render;
  greens when W4 makes the guard `>1` + a static label (mirroring `TransportDock.vue:39/92`). **NO
  escape:** the COUNT is the gate; the `>1` threshold is already correct — raising it is forbidden (the
  MANDATE). Playground sites (`AssetPropertiesPanel.vue:114`) are OUT-of-scope (not deployed-SPA).
- **clause (e) — the TASTE-packet generator produces the review packet (PROTOCOL; HYGIENE — labeled;
  the verdict is USER-DOMAIN, never the gate's).** Given a wave's named deltas, the generator emits a
  packet (per-pane before/after, desktop+mobile, deltas labeled, taste-anchors checklisted). **BORN-RED
  WITNESS:** no generator exists today; the J.W7c "designer-eye PASS" was contradicted by the user's
  "awful" same-day (`PATH-FORWARD.md §1`). **BITE:** the design-band close is BLOCKED until the packet
  EXISTS and is presented (the protocol-absence reds the close). *(Labeled HYGIENE/PROTOCOL per the
  two-tier taxonomy — the generator CORROBORATES the design close by packaging the evidence; it NEVER
  carries the verdict. The user's verdict on the packet is the CORRECTNESS-of-design oracle, and it is
  USER-DOMAIN, scheduled BEFORE the close per the TASTE-boundary invariant. An agent PASS is corroboration
  only.)*
- **clause (f) — `release.yml` publish has `timeout-minutes`; the demo-smoke ceiling is MEASURED
  (HYGIENE — labeled).** `grep -n "timeout-minutes" release.yml` shows `15` on the `publish` job; the
  `ci.yml` demo-smoke ceiling is recalibrated against a measured wall-clock (not the unverified 35m
  artifact). **BORN-RED WITNESS:** `grep -n "timeout-minutes" release.yml` → no output TODAY (`ci-cd-k.md
  F-1`); the 35m ceiling is unverified post-J.W4 (`ci-cd-k.md F-2`). **BITE:** the F-1 grep reds today
  (no timeout); the demo-smoke ceiling is born-unverified (no measured run after J.W7c). *(Labeled HYGIENE
  — F-1 closes a stall hazard; the demo-smoke disposition is MEASURE-FIRST, an observe-then-recalibrate
  posture, not a correctness assertion. Neither substitutes for clauses (a)-(d).)*

**The §spine bar — MUST bite.** Clauses (a)-(d) are the gate-truth correctness oracles: (a)
`proof:cold-entry` drives the FIRST human gesture from a cleared context and reds on the live P0; (b)
the de-vacuoused B1 reds on a frozen subject (the engine-write disambiguation); (c) the demo
subject-animates arm reds on a non-animating real transform path; (d) the single-option gate reds on a
lone-option dropdown. Each has a CONCRETE born-RED witness on today's tree — `k-verify-gate-blindspot.mjs`
greens the OLD B1 at 101 engine-OFF; `probe-cold-play.mjs` shows the cold cube frozen; `gate-estate-k.md
§4` shows choreo `.cube` at 1 distinct; `ChromeDock.vue:199` renders the 1-item dropdown. Revert W0's
product fix → (a) and (b) red. Revert the B1 sample-set edit → (b) greens vacuously over the idle bob
again (the regression this wave exists to prevent). Revert W4's totality fix → (d) reds. **Two-tier
taxonomy:** the wave's GREEN depends on the correctness clauses (a)-(d); clauses (e) (the TASTE packet)
and (f) (the CI hazards) are HYGIENE/PROTOCOL corroborators — (e)'s verdict is USER-DOMAIN (the
TASTE-boundary invariant forbids a gate carrying the design verdict), (f) is a stall-hazard fix + a
measure-first posture. They support the gate-truth close but may NEVER substitute for a red correctness
clause. **P6 posture (declared per clause):** (a)/(b)/(c)/(d) are device-INDEPENDENT correctness gates
(the rendered control set, the engine-write delta, the option count) — they hard-gate; (e) is a PROTOCOL
artifact (the verdict is human, not device); (f)'s demo-smoke wall-clock is device-DEPENDENT (observe-then-recalibrate,
not a hard timing gate). **This is the verification spine of the tranche:** the cold-entry oracle + the
de-vacuoused B1 are W0's own §Hard gate and are CONSUMED by every later wave — until they are green, no
later wave's design band can honestly claim "the subject animates."

## §No-workaround prohibitions (BINDING — the mandate's named forbiddings for this wave)

- **NO raised distinct-count threshold.** The B1 vacuity dies by DISAMBIGUATING engine writes from CSS
  animation (the sample-set change + the aria precondition), NOT by raising ≥3 to ≥N (the MANDATE: "the
  B1 vacuity dies by DISAMBIGUATING engine writes from CSS animation in the oracle, NOT by raising the
  distinct-count threshold"). The threshold ≥3 is correct; the SAMPLE (`.idle-hover`) was wrong. A higher
  count over the idle bob is STILL vacuous.
- **NO seeded warm state in the cold-entry oracle.** `proof:cold-entry` starts from `localStorage.clear()`
  with NO `seedControlsOpen` — the genuine cold context (`live-cold-play-path.md`'s discipline). Seeding
  `{isControlsPanelOpen:true}` re-creates the exact blindspot (`tests-bench-k.md §5a`): the gate would
  exercise the warm path the genuine cold user never has.
- **NO fixed `settleMs` in the cold-entry/transition waits.** Use the per-EXPECTED-state predicate with a
  CEILING timeout (the J.W0 `navToScene` precept — load-independent by construction); a fixed settle is a
  load-DEPENDENT oracle that asserts only elapsed time, never that the surface projected.
- **NO nav-override after the hero click.** The cold-entry oracle clicks the hero play and lets the
  MACHINE navigate (`autoPlayNext → runSceneSwitch`); it does NOT override with a second `location.hash`
  write (the B1 leg's exact mistake at `proof-live-session.mjs:395` — a DIFFERENT nav mechanism that
  remounts/re-resolves and masks the home→cube shared-key bug).
- **NO synthetic-only subject-animates.** The demo arm (S3) reads the REAL `matrix3d`/`--ball-p` channel;
  it does NOT proxy the demo with a synthetic `<div>left` (the structural blindspot `gate-estate-k.md §4`
  names). The synthetic arms STAY for the lib; the demo arm is ADDED, not substituted.
- **NO raised single-option count.** The U4 rule dies at `>1` made TOTAL across every dynamic `<Select>`,
  NOT at a higher count (the MANDATE: "single-option selects NEVER render … the count IS the gate"). The
  `>1` threshold is already the correct rule (`TransportDock.vue:39`).
- **NO agent verdict substituting for the user verdict.** The TASTE packet is PACKAGED by the generator
  and VERDICTED by the user (the TASTE-boundary invariant). An agent "designer-eye PASS" is corroboration,
  NEVER the design-band close — the J.W7c failure mode this wave exists to prevent.
- **NO blind demo-smoke ceiling bump.** The wall-clock is MEASURED before recalibration (F-2/DL-K13
  measure-first); the cure for an over-budget tail is the harness net-deletion (one shared chromium+server),
  MEASURED, not a panicked ceiling raise or a `continue-on-error` (the J.W0 §No-workaround precept).
- **NO re-baselining visual-lock BEFORE the design refinement lands.** The visual-lock baseline must be
  re-shot AFTER W2/W3/W4 land + the user verdicts the packet (`gate-estate-k.md §5/§9`, `live-session-gap-analysis.md
  §F10`), never before — re-shooting the disliked W7c state again would re-encode the rejected layout as
  GREEN. (The re-baseline is a WZ-close motion; W5 builds the packet that PRECEDES it.)

## §Folds (every K.md-assigned fold, with its evidence citation + U-K/DL-K id)

- **F3 / the cold-entry axis** (U-K2/U-K3/U-K5; no gate drives the hero CTA cold) — S1
  (`proof:cold-entry`, born-RED on the live P0). `live-session-gap-analysis.md §0/§2 need #1/§FOLD F3`,
  `gate-estate-k.md §9 row 1`, `ci-cd-k.md F-3`. DL-K1 (the cold-play race) + the COLD-axis invariant.
- **F2 / DL-K2 / the gate-blindspot meta-chronic** (B1 greens engine-OFF on the idle bob; the ≥9-tranche
  rider) — S2 (the de-vacuoused B1) + S1 (the cold drive). `live-session-gap-analysis.md §1/§FOLD F2`,
  `live-cold-play-path.md P1-1`, `deferred-ledger-k.md §0/DL-K2`. **The SYSTEM exit** for DL-K2 — the
  engine-write disambiguation rule's reference implementation; under P-invariant-28 it un-blinds
  DL-K1/K3/K4/K10.
- **F4 / the synthetic-only lib adoption** (`proof:subject-animates` guards a synthetic `<div>`, never
  the demo matrix/CSS-var path) — S3 (the demo arm). `live-session-gap-analysis.md §FOLD F4`,
  `gate-estate-k.md §4/§9 row 3`. U-K5.
- **DL-K5 / the U4 single-option rule not total** (other selects still render lone options) — S4
  (`proof:no-single-option-select`). `live-dock-tabs-selects.md §2.1/§3 S1`, `deferred-ledger-k.md DL-K5`,
  `gate-estate-k.md` U-K16. The gate lands WITH W4's totality fix.
- **The TASTE boundary** (the J taste-tension; agents PASS while the user says "awful") — S5 (the packet
  generator) + the user-verdict protocol. `gate-estate-k.md §5/§8` ("no gate can carry taste; the band is
  honestly human"), `live-spring-sequence-mp-verdict.md`, `design-synthesis-k.md §5`. The TASTE-boundary
  invariant instrumented.
- **F-1 / release.yml no `timeout-minutes`** (a stuck publish stalls 6h) — S6 (`timeout-minutes: 15`).
  `ci-cd-k.md F-1`. (RIDES W1; AUTHORED here as the gate-truth CI disposition.)
- **F-2 / DL-K13 / the demo-smoke wall-clock hazard** (35m unverified post-J.W4; ~42m projected; the
  deploy-block class) — S6 (MEASURE-FIRST). `ci-cd-k.md F-2`, `deferred-ledger-k.md DL-K13`, the J.W0
  CICD-7 baseline.
- **F10 / the visual-lock baseline IS the disliked state** (drift the user dislikes is now GREEN) —
  RECORD + the §No-workaround prohibition (re-baseline AFTER the refinement + the verdict, at WZ-close, NOT
  W5). `gate-estate-k.md §5/§9`, `live-session-gap-analysis.md §FOLD F10`. U-K7/K13/K17.
- **F11 / error-budget gates blind to silent no-op defects** (the cold path threw nothing) — RECORD;
  partially addressed by clauses (a)-(c) (every distinct-count leg now pairs with the engine-write/aria
  positive assertion — a budget leg can no longer green over a silent no-op).
  `live-session-gap-analysis.md §3/§FOLD F11`.
- **The measure-test noise** (`sync-step.measure.test.ts`/`d3-changed-keys.measure.test.ts` emit
  console.log on every `npm test`) — RECORD → W6 (hygiene; guard behind `KF_MEASURE=1` or move to bench/).
  `tests-bench-k.md §3a/§FOLD`. NOT W5 scope (named here so it has a terminal home).
- **EC-1 / the `lerpArray` SoA adoption (DL-K33 — engine totality)** — `engine-core-k.md §2/§FOLD EC-1`:
  the J.W6 S2 ADOPT decision (the `bench/interp-buffer.bench.ts` artifact measured the SoA win) is
  ELECTED but the IMPL is NOT yet landed — an active K-scope engine work item. The exit is a born-RED
  engine-totality gate (the bench is the witness substrate; the gate reds while the AoS path is live,
  greens on the SoA land at `engine.ts:interpFrames` + `frame-compiler.ts:finalizeFrameVars`). Folds
  HERE as the gate-truth wave's engine-totality leg (the only EC row needing a NEW gate; EC-13 is a
  W0 product fix, the EC-2..EC-12 remainder is W0 fold-on-touch hygiene). `PROGRESS.md §"Open deferrals"
  DL-K33`.

## §Hand-off / cross-wave boundaries (BINDING)

- **→ W0 (the product fix + the §Hard gate, BINDING):** K.W5 AUTHORS `proof:cold-entry` (S1) + the
  de-vacuoused B1 (S2) — they ARE W0's §Hard gate, landing WITH W0's product fix
  (`scenePlaybackAdapters.ts` resume-made-total — the adapter seam, NOT a demo-side `play()` sprinkle).
  The gate is born-RED before the fix, GREEN after; K.W5 does NOT author the product fix (W0 owns it). The
  cold-entry oracle + de-vacuoused B1 are CONSUMED by every later wave's verification.
- **→ W4 (the totality fix + the packet trigger, BINDING):** K.W5 AUTHORS `proof:no-single-option-select`
  (S4); W4 owns the product totality fix (`ChromeDock`/per-scene guards made `>1` + static label). The
  TASTE packet (S5) GENERATES at W4's close over W4's named pane deltas (U-K11/K12/K13/K17/K20); the user
  verdicts it BEFORE the close. Disjoint loci: W4 at the `<Select>` guards + pane re-cuts; W5 at the
  option-count gate + the packet generator.
- **→ W3 (the capture-harness lib, BINDING — reciprocal with K.W3 §Hand-off):** the TASTE-packet
  generator (S5) RIDES the `proof:visual-lock` region-screenshot seam + the EXISTING
  `scripts/lib/demo-driver.mjs withPage` lib (the single-sourced driver every browser gate already
  rides, W3's layout-cluster gate included). "The W3-lib capture harness" (`K.md §clusters` K.W5 row)
  = that lib named by its most prominent K rider (W3's layout gate), NOT a W3 deliverable: W3's scope
  is the grid/anchoring redesign (M1–M4); it authors NO capture-lib consolidation. K.W5 AUTHORS the
  generator atop the existing lib (the consumer); W3 DECLARES its layout-packet requirement (K.W3
  clause (e)) and rides the same lib for its own gate. One screenshot authority, no second pipeline.
- **→ W1 (the release timeout + glass-ui re-pin, BINDING):** F-1 (`release.yml timeout-minutes`) is
  authored here as the gate-truth CI disposition but RIDES W1's consume-edge motion (the re-pin wave
  touches the publish path). The release-gate PARITY (F-4 — adding `proof:published-surface`/`proof:readme-runs`/`proof:deps-current`
  to release.yml) is W1's, NOT W5's (named OUT below).
- **→ W6 (parallel; the ledger terminations, BINDING):** the demo-smoke wall-clock RE-bound or shard
  (if the measure-first run exceeds budget) is the W6/W3 harness net-deletion; the measure-test noise
  (`tests-bench-k.md §3a`) is W6's hygiene fold; the mobile-lighthouse floor re-assertion (DL-K11) is
  W6's. K.W5 declares the demo-smoke POSTURE (measure-first) per the tail; W6 industrializes any sharding.
- **OUT / sibling (do NOT touch):** the release-gate parity (F-4) → W1; the deploy-rebuild-independence
  (F-5) → W6-infra (hardening, not blocking); the glass-ui pin widen (F-6) → W1 (`proof:deps-current`
  floor `3.11.2 → 3.13.0`); the static-gate placement (F-7, move 15 source gates out of demo-smoke) →
  W6-infra (triage after the F-2 re-measurement); the visual-lock re-baseline → WZ-close (AFTER the
  refinement + verdict); the playground `AssetPropertiesPanel` single-option (S9) → playground-scope
  (U-K19/CC-1, OUT of the deployed-SPA gate); the U4 `onSelectAnimation`-as-implicit-play unit test
  (`tests-bench-k.md §5b`) → W0/W4 product (a play-path concern, NOT W5's render-count gate); the version
  cut + publish → WZ (USER-DOMAIN, confirm-first, Mike Babb).

## §Design decisions (trade-offs RESOLVED)

- **The cure is DISAMBIGUATION + the COLD DRIVE, NOT a stronger threshold — RESOLVED.** A higher
  distinct-count over `.idle-hover` is STILL vacuous (the idle bob gives 41 at rest). The cure is to read
  the engine's own write channel (the composite `matrix3d`/`--ball-p`), exclude the idle CSS, gate on the
  aria flip, and DRIVE the cold path. This is the engine-write disambiguation rule + the COLD-axis
  invariant, made falsifiable. The MANDATE forbids the threshold-raise explicitly.
- **The cold-entry oracle + de-vacuoused B1 land WITH W0, not standalone — RESOLVED.** They ARE W0's
  §Hard gate (the verification of W0's product fix). K.W5 is the wave whose deliverable is the
  VERIFICATION of every other wave; its legs partition across the DAG, riding the surfaces they certify
  (the charter's "K.W5's legs partition"). The gate is born-RED before the fix and GREEN after — the
  fix and its oracle are one motion.
- **The synthetic arms STAY; a demo arm is ADDED — RESOLVED.** `proof:subject-animates`' synthetic ARMs
  A/B/C are honest for the LIBRARY (`dist/keyframes.js` over a `<div>left`) and are NOT relaxed. The demo
  arm certifies the DEMO's `matrix3d`/`--ball-p` path the synthetic arm structurally cannot reach
  (`gate-estate-k.md §4`). Two different transform paths, two arms — not a substitution.
- **No gate carries taste; the TASTE generator PACKAGES, the user VERDICTS — RESOLVED.**
  `gate-estate-k.md §8`'s structural answer: "no gate can carry taste; the band is honestly human." The
  generator's job is to FREE the user's attention for taste by automating the packaging (per-pane
  before/after, both viewports, the named deltas, the taste-anchor checklist). The verdict is USER-DOMAIN,
  scheduled BEFORE the close (the TASTE-boundary invariant). An agent PASS is corroboration only — the
  J.W7c "designer-eye PASS" contradicted by the user's "awful" is the resolved failure mode.
- **The single-option gate's COUNT is the assertion — RESOLVED.** The U4 rule made total (`>1` across
  every dynamic `<Select>`) is the gate; the threshold `>1` is already correct (`TransportDock.vue:39`).
  The gate reads the runtime-rendered count, so static-option selects pass by construction and only the
  DYNAMIC lone-option reds. Playground-only sites are OUT (deployed-SPA scope).
- **The CI hazards are dispositioned at the gate-truth altitude — RESOLVED.** F-1 is a one-line stall-hazard
  fix (`timeout-minutes: 15`); the demo-smoke wall-clock is MEASURE-FIRST (observe-then-recalibrate, the
  cure being the harness net-deletion, not a blind bump). Both are gate-truth: a real hazard fixed
  deterministically, a budget validated by measurement — NEVER a `continue-on-error` or a pre-optimization.
- **Net-deletion holds — RESOLVED.** The B1 edit DELETES the `.idle-hover` sample term (it never measured
  the engine); the single-option gate's landing lets W4 DELETE the lone-option render path; the TASTE
  generator REUSES the visual-lock screenshot seam (no second pipeline). No gate is RELAXED; every leg
  either DISAMBIGUATES or DRIVES — the estate's net-deletion rule, honored at the gate-truth altitude.
