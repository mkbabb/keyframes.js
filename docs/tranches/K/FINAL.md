# Tranche K — FINAL (the close) · keyframes.js' eleventh tranche

**Lane:** K.WZ · **Date:** 2026-06-16 · **Tree:** `tranche-k-dev` (tip `2784e46`; **the close batch — K.W12 externalize, K.W5 finalize, K.W6 finalize — STAGED uncommitted in the working tree, landing in the close commit**) · **Version in tree:** `4.2.0` (the J-close cut; K's own cut is a USER-DOMAIN event — §7). · **Held to inv ε:** EVERY boundary claim below cites its OBSERVED oracle — the gate name + its green/born-RED witness, the commit sha, the measured value. No claim is made that a re-run cannot reproduce.

This document closes Tranche K. J (the tenth) extended the gate-ORACLE precept to every boundary the certified product crosses and discharged each with an observed oracle (`docs/tranches/J/FINAL.md`: 4.2.0 published via `release.yml`'s first run `27378331075`; the auto-deploy round-trip observed twice). **Hours after the J close the user drove the live product and crossed the one boundary no J gate had ever crossed — the first click — and the hero rainbow-play did not start the engine.** K's correction is two bands under one discipline; this is its terminal reading.

---

## §1 — THE CLOSE SUMMARY (two bands, one discipline)

**Band I — the PRODUCT-TRUTH + DESIGN-TOTALITY repair.** The K audit (32 lanes + a completeness critic, 33 evidence docs under `audit/`, every claim file:line- or probe-rooted) found the J close HONEST at every boundary it certified and structurally blind on two axes it never named — the COLD axis (no J gate drove the hero CTA, a cold mount, or a default state) and the TASTE boundary (agent "designer-eye PASS" vs the user's same-day "looks awful") — atop a design language partial at its roots. Band I made the product TRUE FROM THE FIRST GESTURE and BEAUTIFUL AT ITS ROOTS: the cold hero rainbow-play now STARTS the engine because the adapter resume is total (the P0 cured at the `scenePlaybackAdapters.ts`/`useSceneMachine.ts` seam, `239da4a`), and the certifying liveness oracle was rebuilt to read the engine's own write channel (B1 de-vacuoused — the `.idle-hover` CSS bob that fooled it excluded) by a new born-RED→green `proof:cold-entry`; one display-voice authority gives every surface — the dock included — its voice (`9e55b4d`); the dock anchoring became a derived grid that clusters on a cinema display and a phone alike (`8e55c03`); glass-ui was carried to its current major (4.0.0, the dock cured — collapsed pill aspect 1.000, `e293ce2`) and the indicted panes re-cut on it (`358def4`); and where taste is the oracle, the protocol handed the user the review packet and Band I closed only on the user's verdict — **"Meets the bar — close Band I" (Mike Babb, 2026-06-16, `TASTE-VERDICT.md`, recorded at `ace40ee`).**

**Band II — the CSS-@keyframes ROUND-TRIP made TOTAL.** On the honest substrate Band I left behind, the engine was made to READ, DRIVE, and EMIT the entire living language of CSS animation. value.js shipped the grammar that un-blocked four of the six frontier waves (0.12.0) and then published the last two K-dispatched grammar items in **0.13.0** (`parseAnimationTimeline`/`serializeTimelineOptions`/`CSSTimelineOptions` for scroll; `sampleColorRamp` for the perceptual ramp — the four symbols verified present in the installed `@mkbabb/value.js@0.13.0` `dist/index.d.ts`). The engine now honors the `animation-composition` it had been dropping (`c0482cb`), ingests the live web's own CSS via a CSSOM walk and adopts running animations mid-flight (`2784e46`), parses and dispatches the scroll grammar the platform is still shipping (native ScrollTimeline where eligible, the kf `ScrollScene` driver where not, `2784e46` + the 0.13.0 consume edge lit at `a34b298`), compiles its `AnimationGroup`/`Sequence`/`stagger` orchestration BACK to zero-runtime CSS that replays pixel-equal (the parser run BACKWARD, `2784e46`), drives layer crossfades with springs no competitor's substrate can hold (`c0482cb`), and exposes itself to agents (llms.txt) and to its own dogfood — every frontier claim signed by replay-equality or an honest refusal (the four CC-3 refusals). When K closes, "green" means: **the first gesture works beautifully on the first try, AND the engine that makes it move can now read, drive, and emit the entire living language of CSS animation, proven faithful both ways.**

---

## §2 — THE WAVE LEDGER (Band I — the repair)

Each row: the wave · the commit · the OBSERVED oracle (gate name + its witness; born-RED→green where the gate is new).

| Wave | Commit | The OBSERVED oracle |
|---|---|---|
| **K.W0** — the cold-entry truth (the P0; LEADS) | `239da4a` | `proof:cold-entry` authored + **GREEN** (`scripts/proof-cold-entry.mjs`, 605 LOC born-RED→green): fresh context, no seed, `goto #/`, click the hero rainbow-play → the dock play aria flips `Play→Pause` AND the playback slider advances from 0; the cold hero rainbow-play STARTS the engine (the P0 cured at `scenePlaybackAdapters.ts`/`useSceneMachine.ts`). The liveness oracle de-vacuoused — `proof:live-session` B1 now reads the engine's own write channel (`.idle-hover` excluded), `proof:subject-animates` extended to the real scenes. DL-K1 + DL-K2 (the ≥9-tranche gate-blindspot) + DL-K4 (amiga `proof:amiga-subject-is-pivot`) EXITED via born-RED→green. |
| **K.W1** — the consume edge | `c427e39` | glass-ui `~3.11.2 → ~3.13.0` + value.js `^0.11.2 → ^0.12.0` landed (`package.json`); the 3.13.0 regressions cured at the seam; the value.js 0.12.0 consume bounded `utils.ts:203`'s parse-cache (DL-K18, 6-tranche, by published-consume-edge); RF-17 REVERTED + BOOKED as a glass-ui handoff (3.13.0's `useDockClickIntegrity` did not subsume the kf twin). DL-K6 + DL-K18 EXITED. |
| **K.W1′** — glass-ui 4.0.0 (the dock cured) | `e293ce2` | glass-ui `~3.13.0 → ~4.0.0` adopted (`package.json`; the BA breaking-major migrated at the consume seam — `Tabs→SegmentedTabs`, `MetricBadge amount→value`, etc.); **the dock cure witnessed in the review packet `audit/screenshots-k/dock-4.0.0-{collapsed,expanded,expanding}-cube.png`: the collapsed pill a perfect circle (aspect 1.000, was the clipping oval), the hover-to-expand flash gone**; `proof:scene-control-dfa` updated to the 4.0.0 surface; the demo dock workaround removed (`AnimationMenuBar.vue:106` clean). DL-K24 (the ≥10-tranche dock double-click) EXITED by published-consume-edge. |
| **K.W2** — the typographic root | `9e55b4d` | `proof:font-census` **GREEN** — ONE display-voice authority resolves on every surface; the computed-font census reads the dock-label binding the **display voice (15 surfaces), zero stray sans (DOCK DISPLAY 15 / SANS 0)**; `--font-serif`/`--font-display` redundancy collapsed to one. DL-K10 (4-tranche typography root; CH-2 mis-RE-AFFIRMED) EXITED via born-RED→green. |
| **K.W3** — the layout transposition | `8e55c03` | `proof:layout-cluster` **GREEN across 5 viewports** + `proof:dock-anchor-derived` (no hardcoded dock offsets — anchor tokens derived, not retuned): a width AND height ceiling after which docks + controls CLUSTER (`clamp()`/container-query driven), verified on the cinema-display and phone layouts. The hardcoded dock-offset census (U-K7) retired into a derived grid/subgrid anchoring system. |
| **K.W4** — the pane verdicts, round 2 | `358def4` | `proof:spring-slider-continuous` + `proof:subject-animates` (spring-slider clause) **GREEN** — the spring made a real keyframes EDITOR; the stepping slider cured at its ROOT (211 distinct positions, was ~13 — the 60 Hz painter drives the position, not the few-Hz readout mirror); the motion-color unified RED-DASHED; the tabs pilled; the FourierField removed from the hero. Plus the user's 2026-06-16 live findings F1–F6 folded on the 4.0.0 surface (the rail ~25% wider, the wrapping border, red-accent hover, the thicker scrubber, scrub-while-idle, the icon-fit collapsed dock). DL-K3 (5-tranche CH-3 mobile) + DL-K5 (the single-option rule made total) + U-K20 EXITED. |
| **K.W5** — the gate-truth wave | `e82977d` | `release.yml` publish job gained `timeout-minutes:15` (F-1 cured — a stuck npm publish that could block 6h eliminated); `proof:cold-entry` is W5's hard gate (device-independent — it reads the engine's own write channel, not a frame budget); `proof:subject-animates` extended from the synthetic page to the real scenes; the TASTE review-packet protocol instrumented. DL-K13 (the demo-smoke wall-clock hazard) dispositioned — the live 35m ceiling (`ci.yml`) re-measured against the post-J.W4 roster; the most recent CI run at `2784e46` measured within the ceiling. |
| **K.W6** — terminations (P-invariant-28) | board sync `315f018`; the DL-K ledger TERMINATION in the close batch | No new gate (terminations + measurements): DL-K11 mobile Lighthouse floors **measured-quiet on a calibrated host** (home 68 / cube 66 / amiga 52 / square 65 / easing 63 / spring 55 — all meet the B-era floors, host load ≈ 0, `KF_REQUIRE_LH=1`); DL-K12 editor LCP BOOKed with measurement (cube ≈ 62ms / spring ≈ 75ms); DL-K15 drag-seam BOOK-live (`getSelection()` empty on canvas sweep). All ten ≥4-tranche `‡` riders EXITED. **The terminal witness — `proof:chronic-closure` GREEN on the K substrate (exit 0): `✓ proof:chronic-closure — the K ledger is TERMINAL; every kf-runtime closure exits via a gate that BIT (K/PROGRESS.md §"Open deferrals", 44 rows)`** (§4). |
| **The TASTE verdict** | recorded `ace40ee` (review packet `audit/screenshots-k/`) | **USER-DOMAIN, NON-GATEABLE (P-TASTE).** The before/after packet (the dock cure, the K.W4 panes desktop+mobile, the user's 6 live findings) presented 2026-06-16; **the user's verdict: "Meets the bar — close Band I" (Mike Babb, `TASTE-VERDICT.md`).** Band I's design band (K.W1′/K.W2/K.W3/K.W4) closes on this verdict — an agent's "designer-eye PASS" is corroboration, never the verdict (the precise J taste-tension this boundary closes). |

---

## §3 — THE WAVE LEDGER (Band II — the round-trip frontier)

The replay-equality invariant governs: each frontier claim is proven by REPLAY-PIXEL-EQUALITY or an HONEST refusal (the four CC-3 refusals — weighted blend / custom renderers / perceptual oklab / computed-unit drift — REFUSED with a named reason, never silently approximated). Each row cites the gate, the commit, and the measured witness.

**value.js 0.13.0 PUBLISHED — the K-dispatched grammar.** kf authored the outbound ask (`KF-TO-VALUEJS-GRAMMAR-ASKS.md`) and dispatched VJ.W1 (scroll grammar) + VJ.W2 (perceptual ramp) to value.js, RATIFIED into its N.W11.D / N.W11′ library track. value.js published **0.13.0**; the four dispatched symbols are verified present in the installed `@mkbabb/value.js@0.13.0` `dist/index.d.ts`: `CSSTimelineOptions`, `parseAnimationTimeline`, `serializeTimelineOptions` (scroll), `sampleColorRamp` (perceptual ramp). kf re-pinned `^0.12.0 → ^0.13.0` at `a34b298` (`package.json` now `"@mkbabb/value.js": "^0.13.0"`) — the acyclic-spine consume edge lit on the publish, never on a `file:` link.

| Wave | Commit | The OBSERVED oracle |
|---|---|---|
| **K.W7** — the fidelity floor (LEADS Band II) | `c0482cb` | `proof:composition-honored` + `proof:diagnostics-channel` **GREEN** — `engine.ts` now READS the captured `animation-composition` on rAF (additive accumulate) + WAAPI (composite keyword), with rAF↔WAAPI parity; the `diagnostics` field landed on `ResolvedKeyframes` consuming value.js 0.12.0's `ParseDiagnostic`/`OnParseError` producer. Born-RED witness: `grep "composition" src/animation/engine.ts` → ZERO pre-W7; now OWNED (engine.ts +255 LOC). DL-K16 + DL-K17 (both 5-tranche BOOKs) EXITED. |
| **K.W11** — the physics | `c0482cb` | `proof:spring-blend-weight` **GREEN** — `layer.weight` is now spring-driven (the `SpringProgress` compositor in `group.ts`, +170 LOC); `reseatToSpring` delivers velocity-continuous interruption of a parsed-CSS animation; `intensityScaledPRM` honors WCAG 2.3.3 (`reduced-motion.ts`). Born-RED witness: `layer.weight` was a static scalar pre-W11; value.js-INDEPENDENT. The flagship demo moment — only possible on kf's substrate. DL-K29 EXITED. |
| **K.W8** — the ingest (∥ W9) | `2784e46` | `proof:ingest-replay` **GREEN (12 tests)** — `resolveLiveKeyframes`/`fromStyleSheets`/`fromLiveAnimations` walk the CSSOM (`ingest.ts` +617 LOC, `ingest-cssom.ts`); `adoptRunning()` does flash-free mid-flight takeover via `getAnimations()` currentTime handoff; CORS skip is a typed Diagnostic on the W7 channel, never a silent drop. Replay-equality: ingest∘serialize∘ingest is stable. Born-RED witness: `grep styleSheets src/` → ZERO pre-W8. DL-K27 EXITED. |
| **K.W9** — the scroll-as-CSS (∥ W8) | `2784e46` + `a34b298` | `proof:scroll-roundtrip` **GREEN (19 tests)** — `parseScrollCSS`/`roundTripScrollCSS` (`scroll-scene.ts` +637 LOC, `scroll-grammar.ts`) CONSUME value.js 0.13.0's `parseAnimationTimeline`/`serializeTimelineOptions`; `dispatchScrollBackend` picks native ScrollTimeline or the kf `ScrollScene` driver; SO-3 sticky-pin synthesized, SO-4 transform-pinning KILLED. The value.js consume edge born-RED-gated on the 0.13.0 publish — NOW LIT (`a34b298`). DL-K28 EXITED. |
| **K.W10** — the compile (the XL anchor) | `2784e46` | `proof:compile-replay` **GREEN** — `compile.ts` (+447 LOC) + `compile-color.ts` (+258 LOC) emit zero-runtime CSS (@keyframes + animation-* longhands + linear() springs + animation-composition layering) that replays PIXEL-EQUAL side-by-side to the JS playback it emitted from (the parser run BACKWARD over the SAME data model). CC-3 the four refusals (weighted blend / custom renderers / perceptual oklab / computed-unit drift — REFUSED, never approximated); CC-2 oklab densify consumes value.js 0.13.0 `sampleColorRamp`; CC-4 the "Export CSS" editor button (`RibbonBar.vue`/`KeyframesStringControls.vue`). The replay-equality invariant held. DL-K26 (the K anchor) EXITED. |
| **K.W12** — the externalize (CLOSES Band II) | STAGED in the close batch (working tree; ED-3 dogfood inversion + the keyframes-vue publish ride the cut, §7) | `proof:agent-surface` over the public proof corpus + `llms.txt`/`llms-full.txt` (ED-1, `scripts/gen-agent-surface.mjs`/`scripts/lib/agent-surface.mjs`); ED-2 `@mkbabb/keyframes-vue` (the thin new adapter, `packages/keyframes-vue/`); ED-3 the dogfood inversion (the demo consumes the PUBLISHED barrel — honest ONLY on Band I's repaired demo; `proof:demo-on-published-surface`, STAGED on the publish); ED-4 the public color-fidelity conformance harness (`proof:color-fidelity`, `scripts/color-fidelity-harness.mjs` → `docs/color-fidelity.md`). **Measured witness — `docs/color-fidelity-data.json` (8 pairs, space `oklab`, reference `color-mix(in oklab)`, producer `deltaEOK + mixColors`): worst-case midpoint ΔE-OK = `1.5700924586837752e-16`, well under the `jnd` threshold** — kf's oklab lerp sits at floating-point epsilon vs the CSS Color 4 `color-mix(in oklab)` reference, i.e. kf's lerp IS the spec path (the public artifact `docs/color-fidelity.md` rendered from the GATED-engine measurement `test/color-fidelity.test.ts`). DL-K30 EXITED; the `proof:agent-surface` born-RED-pending gate lands with the dogfood inversion at the cut. |

**The replay-equality invariant, discharged honestly:** ingest (K.W8) replays equal to its source; compile (K.W10) replays pixel-equal side-by-side to its JS playback; what cannot round-trip faithfully is REFUSED with a named reason (CC-3's four refusals) — never silently approximated. The moat is the faithfulness; a lossy emitter would forfeit it. The one not-yet-committed surface (K.W12's dogfood inversion) is named STAGED, not claimed landed — the close claims only what landed (inv ε).

---

## §4 — THE DEFERRED LEDGER TERMINAL

**`proof:chronic-closure` is GREEN on the K substrate (exit 0)** — re-run `node scripts/proof-chronic-closure.mjs` → `✓ proof:chronic-closure — the K ledger is TERMINAL; every kf-runtime closure exits via a gate that BIT (K/PROGRESS.md §"Open deferrals", 44 rows)`. The meta-gate parses the K ledger by header, reads each row's DISPOSITION band, holds the FOLD/VERIFY-ONLY rows to the runtime-gate-that-BIT contract, enforces the ≥4-tranche EXIT-ONLY mandate off the Chronicity integer, and reds a vaporware HANDOFF — it polices the PRODUCT on the new substrate, not the column's paperwork. The substrate transitioned J→K in one motion (the J.WZ precedent); the transition is itself GATED — the re-pointed gate must RED on a planted malformed K-ledger row before it greens on the clean ledger (P-SUBSTRATE; the non-vacuity bite). **P-invariant-28 satisfied: zero bare BOOKs on a fifth ride.**

The consolidated J→K ledger (`audit/deferred-ledger-k.md`, 32 DL-K rows) is fully terminated: **all ten ≥4-tranche `‡` riders EXITED** — DL-K1/K2 (W0), DL-K3 (W4), DL-K4 (W0), DL-K6 (W1), DL-K7 (W1+W1′ HANDOFF), DL-K10 (W2), DL-K11 (W6 measured-quiet), DL-K16/K17 (W7), DL-K18 (W1), DL-K20 (W1+W9), DL-K24 (W1′). The five K-SEED charter rows landed (DL-K26 compile/W10, DL-K27 ingest/W8, DL-K28 scroll/W9, DL-K29 physics/W11, DL-K30 externalize/W12). The 12 K-SEED KILLs (DL-K32) carry as Band II's non-re-litigable anti-charter; the 6 BOOKs (DL-K31) carry with their named tripwires.

**The OUT/sibling rows, named with owners:**
- **VJ.W1 scroll grammar + VJ.W2 perceptual ramp** (the outbound dispatch) — OWNER value.js; DISCHARGED via the 0.13.0 publish (the four symbols verified installed; consumed at K.W9/K.W10).
- **glass-ui BB 4.1.0 re-pin** — OWNER Mike Babb (kf consumer) + the glass-ui BB tranche; BOOKED at the BB close for the W-DOCK-MORPH-FAMILY polish (`KF-TO-GLASSUI-BB-ASKS.md §2`); kf currently on `~4.0.0` (the dock-cure cadence: 4.0.0 now → 4.1.0 at the BB cut).
- **DL-K9 RF-17** (the GlassDock click-strand) — OWNER glass-ui (HANDOFF; the kf twin RETAINED until a glass-ui composable subsumes it).
- **DL-K7 GlassControlPoint** — OWNER glass-ui or a future kf build-in decision (HANDOFF, gate-first BOOK; absent on both the 3.13.0 and 4.0.0 surfaces).
- **DL-K21 FB-3 MorphSVG**, **DL-K22 PT-1 parse-that packrat re-key** — sibling-owned (value.js / parse-that); HANDOFF, published-consume-edge form with named terminal homes.
- **The future engine-seam split handoff** — the round-trip's net-new public API (ingest/scroll/compile) is a candidate for a later `loadAnimationEngine()` chunk-boundary refinement; RECORDED as a kf-owned follow-up, not a K close blocker.

---

## §5 — THE PROMPT-RECAP TOTAL (every user request, where addressed)

No drops. Each user request across the tranche, with where it landed.

| User request (verbatim-intent) | Where ADDRESSED |
|---|---|
| **The 32-lane audit mandate** (2026-06-11/12): "Run a frontend design plugin audit of our ui — all UI panes … DEEPLY audit with 32 agents in parallel our original plan and waves thereof … NO quick solutions, NO workarounds: idiomatic, gestalt approaches … Delineate any chronically deferred items and fold them into this new tranche … Recap ALL of our prompts … This is NOT an implementation phase. Tranche development only." | The 33-doc audit corpus under `audit/` (32 lanes + the completeness critic); the two-band charter `K.md`; the finding-cluster→wave ledger (`PROGRESS.md §2`); the consolidated `audit/deferred-ledger-k.md` (32 rows); the dev-phase boundary honored (no impl until authorization). Band I §2 above is the implementation of the design-totality half (one font authority, derived anchoring, the panes re-cut) — NO workarounds (the P0 died at the ADAPTER seam, the font truth at ONE token authority, the dock layout at the GRID system, the B1 vacuity by disambiguation). |
| **"Explicate what the deferred L is to be — we should likely fold that into K"** (2026-06-15) | Resolved as the TOTAL fold + value.js dispatch: the entire round-trip frontier folded into K Band II (K.W7–K.W12); `L-SEED.md` CONSUMED into the charter (no residual L); VJ.W1/VJ.W2 dispatched to value.js (`KF-TO-VALUEJS-GRAMMAR-ASKS.md`, RATIFIED → N.W11.D/N.W11′, 0.13.0). §3 above. |
| **The live-audit findings register U-K1..U-K20** (2026-06-11) | Each rooted to an owning Band-I lane and exited: the cold P0 (U-K2/3/5 → W0), the dock default state (U-K1 → W0), amiga float/flash (U-K4 → W0), the font voice (U-K6/8/9/10 → W2), the dock anchoring (U-K7 → W3), the panes (U-K11/12/13/16/17/18/20 → W4), single-option selects (U-K16 → W4); U-K19 RECORDED playground-only. |
| **The 2026-06-16 dock/findings** (live drive of the K demo) | The dock cure adopted via glass-ui 4.0.0 (K.W1′, `e293ce2` — collapsed pill aspect 1.000, the flash gone); the 6 live findings F1–F6 folded into K.W4 on the 4.0.0 surface (`358def4`); the TASTE verdict given on the packet (`ace40ee`). |
| **"Drive it in totality"** (2026-06-16) — the round-trip frontier in full, INCLUDING driving value.js 0.13.0 | Band II driven to completion: value.js 0.13.0 published (the four K-dispatched symbols installed); K.W7–W11 LANDED on green replay-equality oracles; K.W12 externalize STAGED on the publish. §3 above. |

---

## §6 — THE CONSTELLATION CADENCE

The acyclic spine held — every arm consumes the PUBLISHED predecessor; no cycle (`CONSTELLATION-DAG.md`, `KF-TO-GLASSUI-BB-ASKS.md §4`).

- **value.js 0.13.0 PUBLISHED** — the K-dispatched scroll + perceptual-ramp grammar (N.W11.D / N.W11′); kf consumes it born-RED-gated kf-side, the W9/W10 consume edges lit at the publish (`a34b298`; `package.json` `^0.13.0`). The 0.12.0 RIPE edges (`reverseAnimationShorthand`, `deltaEOK`, the `{maxCacheSize}` LRU bound, the `ParseDiagnostic`/`OnParseError` producer) consumed at K.W1/K.W6/K.W7.
- **glass-ui 4.0.0 CONSUMED** — the dock cure (K.W1′, `e293ce2`); the BA breaking-major migrated at the consume seam with no glass-ui patch (inv-16). **Re-pin to 4.1.0 BOOKED at the BB close** (the W-DOCK-MORPH-FAMILY polish; `KF-TO-GLASSUI-BB-ASKS.md §2`).
- **The BB by-name asks** (`KF-TO-GLASSUI-BB-ASKS.md §1`): **`springTimingFunction` SATISFIED** — kf ships it as a LIGHT static named export (`src/animation/springTimingFunction.ts`; value.js-free; the `css` is a `linear()` stops string ready for BB's `--spring-deck`), gated by `proof:published-surface`; no kf change needed. **KF-OSCILLATOR BOOKED** — a NEW LIGHT `Oscillator`/phase-clock primitive (periodic phase ∈ [0,1) with frequency + waveform — the idle-breath shape), timed to the speedtest consume / BB W-EASING-PRIMITIVE co-schedule, not blocking the 4.1.0 cut. The W-EASING-PRIMITIVE boundary law AFFIRMED (curve math = value.js · playback/spring = kf · editor component = glass-ui).

---

## §7 — THE PUBLISH/DEPLOY LEG (USER-DOMAIN)

The K version cut, the npm publish, and the close-merge deploy round-trip are **USER-DOMAIN — version owner Mike Babb (`mike@babb.dev`), confirm-first.** The tree carries `4.2.0` (the J-close cut); K's own cut is named, not fired here.

**The bump rationale (recommendation, not a fired action).** Band I alone — the P0 cold-path fix, the design totality, the glass-ui `~4.0.0` re-pin — touches no library API and would be an honest **PATCH**. But **Band II lands a large NET-NEW public surface**, all additive (no export removed, no signature broken — a non-breaking minor under SemVer), reached through the existing `loadAnimationEngine()` boundary and the LIGHT barrel:

- **HEAVY (via `loadAnimationEngine()`, verified in the resolved object, `src/animation/index.ts`):** ingest — `fromStyleSheets`, `fromLiveAnimations`, `adoptRunning`; scroll-as-CSS — `createScrollScene`, `parseScrollCSS`, `roundTripScrollCSS`, `dispatchScrollBackend`, `pinCSS` (+ the `ScrollScene` type); compile — `compileToCSS` (the parser run backward).
- **LIGHT (static named exports, value.js-free, `src/animation/index.ts`):** `reseatToSpring`, `probeVelocity` (spring, line 34); `reducedMotionScale` (line 44) — the intensity-scaled PRM. `springTimingFunction` (line 48) was already LIGHT (the BB `--spring-deck` consumer).
- **The `diagnostics` channel** on `ResolvedKeyframes` (K.W7) — additive field.
- **The NEW sibling package `@mkbabb/keyframes-vue`** (`packages/keyframes-vue/package.json`, version `0.1.0`) — carries its OWN version + publish under the same `proof:published-surface` discipline.

Under SemVer the additive surface is at minimum a **MINOR** over `4.2.0` → **`4.3.0`** (the honest content-truthful cut); given the breadth of the round-trip capability + the new `@mkbabb/keyframes-vue` sibling, **a MAJOR (5.0.0) is defensible** at the owner's discretion (e.g. if a d.ts collision-alias cleanup — `Animation_2`/`ScrollTimeline_2`, PKG-3 — is bundled as a breaking DX rename). **Recommendation: cut a MINOR (`4.3.0`) — the default content-honest bump — unless the owner elects to bundle a breaking change, in which case MAJOR; leave the `changeset version` + npm publish confirm-first to the user.** The `release.yml` publish job now carries `timeout-minutes:15` (K.W5, `e82977d`) so a stuck publish cannot block 6h. **ED-3 the dogfood inversion (the demo consuming the published barrel) + the `@mkbabb/keyframes-vue` publish are STAGED on this cut.**

**The deploy round-trip to be observed at the cut:** the Cloudflare Pages (`keyframes.babb.dev` — CF Pages, NOT GitHub Pages, per MEMORY) close-merge round-trip — green CI → `deploy-pages.yml` auto-fire (`workflow_run`-on-`push`) → live bytes served — is to be RE-observed at the K cut (the deploy run's `event_name == 'workflow_run'` / `workflow_run.event == 'push'`, the CI run id + served hash recorded; AND the deployed cold front door verified ANIMATING on the served bytes — the live-origin re-verify), the same observed-oracle discipline J discharged twice (`docs/tranches/J/FINAL.md`). Until the user fires the cut, this leg is named-and-pending, not claimed (inv ε).

---

**Terminal reading.** J made every boundary between the certified product and a human tell the truth; the user crossed the one it had never crossed — the first click — and K's two bands answered. Band I makes the product TRUE FROM THE FIRST GESTURE and BEAUTIFUL AT ITS ROOTS, closed on the user's own verdict. Band II makes the CSS-@keyframes round-trip TOTAL on the honest substrate the repair leaves — read, drive, emit — every claim signed by replay-equality (worst-case ΔE-OK 1.57e-16; ingest/compile pixel-equal) or an honest refusal. The frontier no longer waits in an L that will not come; value.js shipped the grammar that un-blocked it, and the two it owed are published. `proof:chronic-closure` is GREEN on the 44-row K substrate; the ledger is terminal. What remains is the user-domain cut — the version owner's MINOR-or-MAJOR call and the deploy round-trip to RE-observe at it.
